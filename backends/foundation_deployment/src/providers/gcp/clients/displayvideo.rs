//! Auto-generated API clients for gcp/displayvideo.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v4/advertisers/{advertisersId}:audit
/// Audits an advertiser. Returns the counts of used entities per resource type under the advertiser provided. Used entities count towards their respective resource limit. See <https://support.google.`com/displayvideo/answer/6071450`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_audit_execute()` to send, or `displayvideo_advertisers_audit` for simplest API.

pub fn displayvideo_advertisers_audit_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    readMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}:audit",
        advertiserId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = readMask.as_ref() {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}:audit
/// Audits an advertiser. Returns the counts of used entities per resource type under the advertiser provided. Used entities count towards their respective resource limit. See <https://support.google.`com/displayvideo/answer/6071450`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_audit_execute()` or `displayvideo_advertisers_audit`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_audit_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_audit_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AuditAdvertiserResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AuditAdvertiserResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}:audit
/// Audits an advertiser. Returns the counts of used entities per resource type under the advertiser provided. Used entities count towards their respective resource limit. See <https://support.google.`com/displayvideo/answer/6071450`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_audit_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_audit_task()`.
/// For the simplest API, use `displayvideo_advertisers_audit()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_audit_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_audit_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuditAdvertiserResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_audit_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_audit`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersAuditArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v4/advertisers/{advertisersId}:audit
/// Audits an advertiser. Returns the counts of used entities per resource type under the advertiser provided. Used entities count towards their respective resource limit. See <https://support.google.`com/displayvideo/answer/6071450`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_audit_builder()` + `displayvideo_advertisers_audit_execute()`.
/// For task-level control, use `displayvideo_advertisers_audit_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_audit(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersAuditArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AuditAdvertiserResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        displayvideo_advertisers_audit_builder(client, &args.advertiserId, &args.readMask)?;
    displayvideo_advertisers_audit_execute(builder)
}

/// POST v4/advertisers
/// Creates a new advertiser. Returns the newly created advertiser if successful. **This method regularly experiences high latency.** We recommend [increasing your default timeout](/display-`video/api/guides/best-practices/timeouts`#client_library_timeout) to avoid errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_create_execute()` to send, or `displayvideo_advertisers_create` for simplest API.

pub fn displayvideo_advertisers_create_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://displayvideo.googleapis.com/v4/advertisers",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/advertisers
/// Creates a new advertiser. Returns the newly created advertiser if successful. **This method regularly experiences high latency.** We recommend [increasing your default timeout](/display-`video/api/guides/best-practices/timeouts`#client_library_timeout) to avoid errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_create_execute()` or `displayvideo_advertisers_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Advertiser>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Advertiser = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/advertisers
/// Creates a new advertiser. Returns the newly created advertiser if successful. **This method regularly experiences high latency.** We recommend [increasing your default timeout](/display-`video/api/guides/best-practices/timeouts`#client_library_timeout) to avoid errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_create_task()`.
/// For the simplest API, use `displayvideo_advertisers_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Advertiser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v4/advertisers
/// Creates a new advertiser. Returns the newly created advertiser if successful. **This method regularly experiences high latency.** We recommend [increasing your default timeout](/display-`video/api/guides/best-practices/timeouts`#client_library_timeout) to avoid errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_create_builder()` + `displayvideo_advertisers_create_execute()`.
/// For task-level control, use `displayvideo_advertisers_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_create(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Advertiser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_create_builder(client)?;
    displayvideo_advertisers_create_execute(builder)
}

/// DELETE v4/advertisers/{advertisersId}
/// Deletes an advertiser. Deleting an advertiser will delete all of its child resources, for example, campaigns, insertion orders and line items. A deleted advertiser cannot be recovered.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_delete_execute()` to send, or `displayvideo_advertisers_delete` for simplest API.

pub fn displayvideo_advertisers_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}",
        advertiserId,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v4/advertisers/{advertisersId}
/// Deletes an advertiser. Deleting an advertiser will delete all of its child resources, for example, campaigns, insertion orders and line items. A deleted advertiser cannot be recovered.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_delete_execute()` or `displayvideo_advertisers_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v4/advertisers/{advertisersId}
/// Deletes an advertiser. Deleting an advertiser will delete all of its child resources, for example, campaigns, insertion orders and line items. A deleted advertiser cannot be recovered.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_delete_task()`.
/// For the simplest API, use `displayvideo_advertisers_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersDeleteArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
}

/// DELETE v4/advertisers/{advertisersId}
/// Deletes an advertiser. Deleting an advertiser will delete all of its child resources, for example, campaigns, insertion orders and line items. A deleted advertiser cannot be recovered.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_delete_builder()` + `displayvideo_advertisers_delete_execute()`.
/// For task-level control, use `displayvideo_advertisers_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_delete(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_delete_builder(client, &args.advertiserId)?;
    displayvideo_advertisers_delete_execute(builder)
}

/// POST v4/advertisers/{advertisersId}:editAssignedTargetingOptions
/// Edits targeting options under a single advertiser. The operation will delete the assigned targeting options provided in BulkEditAdvertiserAssignedTargetingOptionsRequest.delete_requests and then create the assigned targeting options provided in BulkEditAdvertiserAssignedTargetingOptionsRequest.create_requests .
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_edit_assigned_targeting_options_execute()` to send, or `displayvideo_advertisers_edit_assigned_targeting_options` for simplest API.

pub fn displayvideo_advertisers_edit_assigned_targeting_options_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}:editAssignedTargetingOptions",
        advertiserId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/advertisers/{advertisersId}:editAssignedTargetingOptions
/// Edits targeting options under a single advertiser. The operation will delete the assigned targeting options provided in BulkEditAdvertiserAssignedTargetingOptionsRequest.delete_requests and then create the assigned targeting options provided in BulkEditAdvertiserAssignedTargetingOptionsRequest.create_requests .
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_edit_assigned_targeting_options_execute()` or `displayvideo_advertisers_edit_assigned_targeting_options`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_edit_assigned_targeting_options_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_edit_assigned_targeting_options_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<BulkEditAdvertiserAssignedTargetingOptionsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BulkEditAdvertiserAssignedTargetingOptionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/advertisers/{advertisersId}:editAssignedTargetingOptions
/// Edits targeting options under a single advertiser. The operation will delete the assigned targeting options provided in BulkEditAdvertiserAssignedTargetingOptionsRequest.delete_requests and then create the assigned targeting options provided in BulkEditAdvertiserAssignedTargetingOptionsRequest.create_requests .
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_edit_assigned_targeting_options_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_edit_assigned_targeting_options_task()`.
/// For the simplest API, use `displayvideo_advertisers_edit_assigned_targeting_options()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_edit_assigned_targeting_options_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_edit_assigned_targeting_options_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BulkEditAdvertiserAssignedTargetingOptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_edit_assigned_targeting_options_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_edit_assigned_targeting_options`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersEditAssignedTargetingOptionsArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
}

/// POST v4/advertisers/{advertisersId}:editAssignedTargetingOptions
/// Edits targeting options under a single advertiser. The operation will delete the assigned targeting options provided in BulkEditAdvertiserAssignedTargetingOptionsRequest.delete_requests and then create the assigned targeting options provided in BulkEditAdvertiserAssignedTargetingOptionsRequest.create_requests .
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_edit_assigned_targeting_options_builder()` + `displayvideo_advertisers_edit_assigned_targeting_options_execute()`.
/// For task-level control, use `displayvideo_advertisers_edit_assigned_targeting_options_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_edit_assigned_targeting_options(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersEditAssignedTargetingOptionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BulkEditAdvertiserAssignedTargetingOptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_edit_assigned_targeting_options_builder(
        client,
        &args.advertiserId,
    )?;
    displayvideo_advertisers_edit_assigned_targeting_options_execute(builder)
}

/// GET v4/advertisers/{advertisersId}
/// Gets an advertiser.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_get_execute()` to send, or `displayvideo_advertisers_get` for simplest API.

pub fn displayvideo_advertisers_get_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}",
        advertiserId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}
/// Gets an advertiser.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_get_execute()` or `displayvideo_advertisers_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Advertiser>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Advertiser = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}
/// Gets an advertiser.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_get_task()`.
/// For the simplest API, use `displayvideo_advertisers_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Advertiser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersGetArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
}

/// GET v4/advertisers/{advertisersId}
/// Gets an advertiser.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_get_builder()` + `displayvideo_advertisers_get_execute()`.
/// For task-level control, use `displayvideo_advertisers_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_get(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Advertiser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_get_builder(client, &args.advertiserId)?;
    displayvideo_advertisers_get_execute(builder)
}

/// GET v4/advertisers
/// Lists advertisers that are accessible to the current user. The order is defined by the order_by parameter. A single partner_id is required. Cross-partner listing is not supported.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_list_execute()` to send, or `displayvideo_advertisers_list` for simplest API.

pub fn displayvideo_advertisers_list_builder<R>(
    client: &SimpleHttpClient<R>,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://displayvideo.googleapis.com/v4/advertisers",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers
/// Lists advertisers that are accessible to the current user. The order is defined by the order_by parameter. A single partner_id is required. Cross-partner listing is not supported.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_list_execute()` or `displayvideo_advertisers_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAdvertisersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAdvertisersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers
/// Lists advertisers that are accessible to the current user. The order is defined by the order_by parameter. A single partner_id is required. Cross-partner listing is not supported.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_list_task()`.
/// For the simplest API, use `displayvideo_advertisers_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAdvertisersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersListArgs {
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// GET v4/advertisers
/// Lists advertisers that are accessible to the current user. The order is defined by the order_by parameter. A single partner_id is required. Cross-partner listing is not supported.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_list_builder()` + `displayvideo_advertisers_list_execute()`.
/// For task-level control, use `displayvideo_advertisers_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAdvertisersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_list_builder(
        client,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.partnerId,
    )?;
    displayvideo_advertisers_list_execute(builder)
}

/// GET v4/advertisers/{advertisersId}:listAssignedTargetingOptions
/// Lists assigned targeting options of an advertiser across targeting types.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_list_assigned_targeting_options_execute()` to send, or `displayvideo_advertisers_list_assigned_targeting_options` for simplest API.

pub fn displayvideo_advertisers_list_assigned_targeting_options_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}:listAssignedTargetingOptions",
        advertiserId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}:listAssignedTargetingOptions
/// Lists assigned targeting options of an advertiser across targeting types.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_list_assigned_targeting_options_execute()` or `displayvideo_advertisers_list_assigned_targeting_options`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_list_assigned_targeting_options_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_list_assigned_targeting_options_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<BulkListAdvertiserAssignedTargetingOptionsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BulkListAdvertiserAssignedTargetingOptionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}:listAssignedTargetingOptions
/// Lists assigned targeting options of an advertiser across targeting types.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_list_assigned_targeting_options_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_list_assigned_targeting_options_task()`.
/// For the simplest API, use `displayvideo_advertisers_list_assigned_targeting_options()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_list_assigned_targeting_options_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_list_assigned_targeting_options_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BulkListAdvertiserAssignedTargetingOptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_list_assigned_targeting_options_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_list_assigned_targeting_options`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersListAssignedTargetingOptionsArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v4/advertisers/{advertisersId}:listAssignedTargetingOptions
/// Lists assigned targeting options of an advertiser across targeting types.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_list_assigned_targeting_options_builder()` + `displayvideo_advertisers_list_assigned_targeting_options_execute()`.
/// For task-level control, use `displayvideo_advertisers_list_assigned_targeting_options_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_list_assigned_targeting_options(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersListAssignedTargetingOptionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BulkListAdvertiserAssignedTargetingOptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_list_assigned_targeting_options_builder(
        client,
        &args.advertiserId,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    displayvideo_advertisers_list_assigned_targeting_options_execute(builder)
}

/// PATCH v4/advertisers/{advertisersId}
/// Updates an existing advertiser. Returns the updated advertiser if successful.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_patch_execute()` to send, or `displayvideo_advertisers_patch` for simplest API.

pub fn displayvideo_advertisers_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}",
        advertiserId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v4/advertisers/{advertisersId}
/// Updates an existing advertiser. Returns the updated advertiser if successful.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_patch_execute()` or `displayvideo_advertisers_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Advertiser>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Advertiser = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v4/advertisers/{advertisersId}
/// Updates an existing advertiser. Returns the updated advertiser if successful.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_patch_task()`.
/// For the simplest API, use `displayvideo_advertisers_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Advertiser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersPatchArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v4/advertisers/{advertisersId}
/// Updates an existing advertiser. Returns the updated advertiser if successful.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_patch_builder()` + `displayvideo_advertisers_patch_execute()`.
/// For task-level control, use `displayvideo_advertisers_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_patch(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Advertiser>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        displayvideo_advertisers_patch_builder(client, &args.advertiserId, &args.updateMask)?;
    displayvideo_advertisers_patch_execute(builder)
}

/// POST v4/advertisers/{advertisersId}/adAssets:bulkCreate
/// Creates multiple ad assets in a single request. Returns the newly-created ad assets if successful. Only supports the creation of assets of AdAssetType AD_ASSET_TYPE_YOUTUBE_VIDEO.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_ad_assets_bulk_create_execute()` to send, or `displayvideo_advertisers_ad_assets_bulk_create` for simplest API.

pub fn displayvideo_advertisers_ad_assets_bulk_create_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/adAssets:bulkCreate",
        advertiserId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/advertisers/{advertisersId}/adAssets:bulkCreate
/// Creates multiple ad assets in a single request. Returns the newly-created ad assets if successful. Only supports the creation of assets of AdAssetType AD_ASSET_TYPE_YOUTUBE_VIDEO.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_ad_assets_bulk_create_execute()` or `displayvideo_advertisers_ad_assets_bulk_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_assets_bulk_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_assets_bulk_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BulkCreateAdAssetsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BulkCreateAdAssetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/advertisers/{advertisersId}/adAssets:bulkCreate
/// Creates multiple ad assets in a single request. Returns the newly-created ad assets if successful. Only supports the creation of assets of AdAssetType AD_ASSET_TYPE_YOUTUBE_VIDEO.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_ad_assets_bulk_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_ad_assets_bulk_create_task()`.
/// For the simplest API, use `displayvideo_advertisers_ad_assets_bulk_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_assets_bulk_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_ad_assets_bulk_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BulkCreateAdAssetsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_ad_assets_bulk_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_ad_assets_bulk_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersAdAssetsBulkCreateArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
}

/// POST v4/advertisers/{advertisersId}/adAssets:bulkCreate
/// Creates multiple ad assets in a single request. Returns the newly-created ad assets if successful. Only supports the creation of assets of AdAssetType AD_ASSET_TYPE_YOUTUBE_VIDEO.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_ad_assets_bulk_create_builder()` + `displayvideo_advertisers_ad_assets_bulk_create_execute()`.
/// For task-level control, use `displayvideo_advertisers_ad_assets_bulk_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_assets_bulk_create(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersAdAssetsBulkCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BulkCreateAdAssetsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        displayvideo_advertisers_ad_assets_bulk_create_builder(client, &args.advertiserId)?;
    displayvideo_advertisers_ad_assets_bulk_create_execute(builder)
}

/// POST v4/advertisers/{advertisersId}/adAssets
/// Creates an ad asset. Returns the newly-created ad asset if successful. Only supports the creation of assets of AdAssetType AD_ASSET_TYPE_YOUTUBE_VIDEO.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_ad_assets_create_execute()` to send, or `displayvideo_advertisers_ad_assets_create` for simplest API.

pub fn displayvideo_advertisers_ad_assets_create_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/adAssets",
        advertiserId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/advertisers/{advertisersId}/adAssets
/// Creates an ad asset. Returns the newly-created ad asset if successful. Only supports the creation of assets of AdAssetType AD_ASSET_TYPE_YOUTUBE_VIDEO.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_ad_assets_create_execute()` or `displayvideo_advertisers_ad_assets_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_assets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_assets_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AdAsset>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AdAsset = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/advertisers/{advertisersId}/adAssets
/// Creates an ad asset. Returns the newly-created ad asset if successful. Only supports the creation of assets of AdAssetType AD_ASSET_TYPE_YOUTUBE_VIDEO.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_ad_assets_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_ad_assets_create_task()`.
/// For the simplest API, use `displayvideo_advertisers_ad_assets_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_assets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_ad_assets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdAsset>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_ad_assets_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_ad_assets_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersAdAssetsCreateArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
}

/// POST v4/advertisers/{advertisersId}/adAssets
/// Creates an ad asset. Returns the newly-created ad asset if successful. Only supports the creation of assets of AdAssetType AD_ASSET_TYPE_YOUTUBE_VIDEO.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_ad_assets_create_builder()` + `displayvideo_advertisers_ad_assets_create_execute()`.
/// For task-level control, use `displayvideo_advertisers_ad_assets_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_assets_create(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersAdAssetsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdAsset>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_ad_assets_create_builder(client, &args.advertiserId)?;
    displayvideo_advertisers_ad_assets_create_execute(builder)
}

/// GET v4/advertisers/{advertisersId}/adAssets/{adAssetsId}
/// Gets an ad asset. Only supports the retrieval of assets of AdAssetType AD_ASSET_TYPE_YOUTUBE_VIDEO.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_ad_assets_get_execute()` to send, or `displayvideo_advertisers_ad_assets_get` for simplest API.

pub fn displayvideo_advertisers_ad_assets_get_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    adAssetId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/adAssets/{}",
        advertiserId, adAssetId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}/adAssets/{adAssetsId}
/// Gets an ad asset. Only supports the retrieval of assets of AdAssetType AD_ASSET_TYPE_YOUTUBE_VIDEO.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_ad_assets_get_execute()` or `displayvideo_advertisers_ad_assets_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_assets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_assets_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AdAsset>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AdAsset = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}/adAssets/{adAssetsId}
/// Gets an ad asset. Only supports the retrieval of assets of AdAssetType AD_ASSET_TYPE_YOUTUBE_VIDEO.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_ad_assets_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_ad_assets_get_task()`.
/// For the simplest API, use `displayvideo_advertisers_ad_assets_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_assets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_ad_assets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdAsset>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_ad_assets_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_ad_assets_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersAdAssetsGetArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: adAssetId
    pub adAssetId: String,
}

/// GET v4/advertisers/{advertisersId}/adAssets/{adAssetsId}
/// Gets an ad asset. Only supports the retrieval of assets of AdAssetType AD_ASSET_TYPE_YOUTUBE_VIDEO.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_ad_assets_get_builder()` + `displayvideo_advertisers_ad_assets_get_execute()`.
/// For task-level control, use `displayvideo_advertisers_ad_assets_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_assets_get(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersAdAssetsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdAsset>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_ad_assets_get_builder(
        client,
        &args.advertiserId,
        &args.adAssetId,
    )?;
    displayvideo_advertisers_ad_assets_get_execute(builder)
}

/// GET v4/advertisers/{advertisersId}/adAssets
/// Lists ad assets under an advertiser ID. Only supports the retrieval of assets of AdAssetType AD_ASSET_TYPE_YOUTUBE_VIDEO.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_ad_assets_list_execute()` to send, or `displayvideo_advertisers_ad_assets_list` for simplest API.

pub fn displayvideo_advertisers_ad_assets_list_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/adAssets",
        advertiserId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}/adAssets
/// Lists ad assets under an advertiser ID. Only supports the retrieval of assets of AdAssetType AD_ASSET_TYPE_YOUTUBE_VIDEO.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_ad_assets_list_execute()` or `displayvideo_advertisers_ad_assets_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_assets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_assets_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAdAssetsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAdAssetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}/adAssets
/// Lists ad assets under an advertiser ID. Only supports the retrieval of assets of AdAssetType AD_ASSET_TYPE_YOUTUBE_VIDEO.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_ad_assets_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_ad_assets_list_task()`.
/// For the simplest API, use `displayvideo_advertisers_ad_assets_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_assets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_ad_assets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAdAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_ad_assets_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_ad_assets_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersAdAssetsListArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v4/advertisers/{advertisersId}/adAssets
/// Lists ad assets under an advertiser ID. Only supports the retrieval of assets of AdAssetType AD_ASSET_TYPE_YOUTUBE_VIDEO.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_ad_assets_list_builder()` + `displayvideo_advertisers_ad_assets_list_execute()`.
/// For task-level control, use `displayvideo_advertisers_ad_assets_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_assets_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersAdAssetsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAdAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_ad_assets_list_builder(
        client,
        &args.advertiserId,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    displayvideo_advertisers_ad_assets_list_execute(builder)
}

/// POST v4/advertisers/{advertisersId}/adAssets:uploadAdAsset
/// Uploads and creates an ad asset. Returns the ID of the newly-created ad asset if successful. Only supports the uploading of assets with the AdAssetType AD_ASSET_TYPE_IMAGE.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_ad_assets_upload_execute()` to send, or `displayvideo_advertisers_ad_assets_upload` for simplest API.

pub fn displayvideo_advertisers_ad_assets_upload_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/adAssets:uploadAdAsset",
        advertiserId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/advertisers/{advertisersId}/adAssets:uploadAdAsset
/// Uploads and creates an ad asset. Returns the ID of the newly-created ad asset if successful. Only supports the uploading of assets with the AdAssetType AD_ASSET_TYPE_IMAGE.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_ad_assets_upload_execute()` or `displayvideo_advertisers_ad_assets_upload`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_assets_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_assets_upload_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UploadAdAssetResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UploadAdAssetResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/advertisers/{advertisersId}/adAssets:uploadAdAsset
/// Uploads and creates an ad asset. Returns the ID of the newly-created ad asset if successful. Only supports the uploading of assets with the AdAssetType AD_ASSET_TYPE_IMAGE.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_ad_assets_upload_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_ad_assets_upload_task()`.
/// For the simplest API, use `displayvideo_advertisers_ad_assets_upload()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_assets_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_ad_assets_upload_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UploadAdAssetResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_ad_assets_upload_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_ad_assets_upload`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersAdAssetsUploadArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
}

/// POST v4/advertisers/{advertisersId}/adAssets:uploadAdAsset
/// Uploads and creates an ad asset. Returns the ID of the newly-created ad asset if successful. Only supports the uploading of assets with the AdAssetType AD_ASSET_TYPE_IMAGE.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_ad_assets_upload_builder()` + `displayvideo_advertisers_ad_assets_upload_execute()`.
/// For task-level control, use `displayvideo_advertisers_ad_assets_upload_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_assets_upload(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersAdAssetsUploadArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UploadAdAssetResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_ad_assets_upload_builder(client, &args.advertiserId)?;
    displayvideo_advertisers_ad_assets_upload_execute(builder)
}

/// POST v4/advertisers/{advertisersId}/adGroupAds
/// Creates an ad group ad. This method is only supported for Demand Gen ads. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_ad_group_ads_create_execute()` to send, or `displayvideo_advertisers_ad_group_ads_create` for simplest API.

pub fn displayvideo_advertisers_ad_group_ads_create_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/adGroupAds",
        advertiserId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/advertisers/{advertisersId}/adGroupAds
/// Creates an ad group ad. This method is only supported for Demand Gen ads. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_ad_group_ads_create_execute()` or `displayvideo_advertisers_ad_group_ads_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_group_ads_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_group_ads_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AdGroupAd>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AdGroupAd = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/advertisers/{advertisersId}/adGroupAds
/// Creates an ad group ad. This method is only supported for Demand Gen ads. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_ad_group_ads_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_ad_group_ads_create_task()`.
/// For the simplest API, use `displayvideo_advertisers_ad_group_ads_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_group_ads_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_ad_group_ads_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdGroupAd>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_ad_group_ads_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_ad_group_ads_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersAdGroupAdsCreateArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
}

/// POST v4/advertisers/{advertisersId}/adGroupAds
/// Creates an ad group ad. This method is only supported for Demand Gen ads. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_ad_group_ads_create_builder()` + `displayvideo_advertisers_ad_group_ads_create_execute()`.
/// For task-level control, use `displayvideo_advertisers_ad_group_ads_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_group_ads_create(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersAdGroupAdsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdGroupAd>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_ad_group_ads_create_builder(client, &args.advertiserId)?;
    displayvideo_advertisers_ad_group_ads_create_execute(builder)
}

/// DELETE v4/advertisers/{advertisersId}/adGroupAds/{adGroupAdsId}
/// Deletes an ad group ad. This method is only supported for Demand Gen ads. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_ad_group_ads_delete_execute()` to send, or `displayvideo_advertisers_ad_group_ads_delete` for simplest API.

pub fn displayvideo_advertisers_ad_group_ads_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    adGroupAdId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/adGroupAds/{}",
        advertiserId, adGroupAdId,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v4/advertisers/{advertisersId}/adGroupAds/{adGroupAdsId}
/// Deletes an ad group ad. This method is only supported for Demand Gen ads. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_ad_group_ads_delete_execute()` or `displayvideo_advertisers_ad_group_ads_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_group_ads_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_group_ads_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v4/advertisers/{advertisersId}/adGroupAds/{adGroupAdsId}
/// Deletes an ad group ad. This method is only supported for Demand Gen ads. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_ad_group_ads_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_ad_group_ads_delete_task()`.
/// For the simplest API, use `displayvideo_advertisers_ad_group_ads_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_group_ads_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_ad_group_ads_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_ad_group_ads_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_ad_group_ads_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersAdGroupAdsDeleteArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: adGroupAdId
    pub adGroupAdId: String,
}

/// DELETE v4/advertisers/{advertisersId}/adGroupAds/{adGroupAdsId}
/// Deletes an ad group ad. This method is only supported for Demand Gen ads. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_ad_group_ads_delete_builder()` + `displayvideo_advertisers_ad_group_ads_delete_execute()`.
/// For task-level control, use `displayvideo_advertisers_ad_group_ads_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_group_ads_delete(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersAdGroupAdsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_ad_group_ads_delete_builder(
        client,
        &args.advertiserId,
        &args.adGroupAdId,
    )?;
    displayvideo_advertisers_ad_group_ads_delete_execute(builder)
}

/// GET v4/advertisers/{advertisersId}/adGroupAds/{adGroupAdsId}
/// Gets an ad group ad.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_ad_group_ads_get_execute()` to send, or `displayvideo_advertisers_ad_group_ads_get` for simplest API.

pub fn displayvideo_advertisers_ad_group_ads_get_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    adGroupAdId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/adGroupAds/{}",
        advertiserId, adGroupAdId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}/adGroupAds/{adGroupAdsId}
/// Gets an ad group ad.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_ad_group_ads_get_execute()` or `displayvideo_advertisers_ad_group_ads_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_group_ads_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_group_ads_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AdGroupAd>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AdGroupAd = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}/adGroupAds/{adGroupAdsId}
/// Gets an ad group ad.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_ad_group_ads_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_ad_group_ads_get_task()`.
/// For the simplest API, use `displayvideo_advertisers_ad_group_ads_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_group_ads_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_ad_group_ads_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdGroupAd>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_ad_group_ads_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_ad_group_ads_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersAdGroupAdsGetArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: adGroupAdId
    pub adGroupAdId: String,
}

/// GET v4/advertisers/{advertisersId}/adGroupAds/{adGroupAdsId}
/// Gets an ad group ad.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_ad_group_ads_get_builder()` + `displayvideo_advertisers_ad_group_ads_get_execute()`.
/// For task-level control, use `displayvideo_advertisers_ad_group_ads_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_group_ads_get(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersAdGroupAdsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdGroupAd>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_ad_group_ads_get_builder(
        client,
        &args.advertiserId,
        &args.adGroupAdId,
    )?;
    displayvideo_advertisers_ad_group_ads_get_execute(builder)
}

/// GET v4/advertisers/{advertisersId}/adGroupAds
/// Lists ad group ads.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_ad_group_ads_list_execute()` to send, or `displayvideo_advertisers_ad_group_ads_list` for simplest API.

pub fn displayvideo_advertisers_ad_group_ads_list_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/adGroupAds",
        advertiserId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}/adGroupAds
/// Lists ad group ads.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_ad_group_ads_list_execute()` or `displayvideo_advertisers_ad_group_ads_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_group_ads_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_group_ads_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAdGroupAdsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAdGroupAdsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}/adGroupAds
/// Lists ad group ads.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_ad_group_ads_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_ad_group_ads_list_task()`.
/// For the simplest API, use `displayvideo_advertisers_ad_group_ads_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_group_ads_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_ad_group_ads_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAdGroupAdsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_ad_group_ads_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_ad_group_ads_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersAdGroupAdsListArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v4/advertisers/{advertisersId}/adGroupAds
/// Lists ad group ads.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_ad_group_ads_list_builder()` + `displayvideo_advertisers_ad_group_ads_list_execute()`.
/// For task-level control, use `displayvideo_advertisers_ad_group_ads_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_group_ads_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersAdGroupAdsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAdGroupAdsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_ad_group_ads_list_builder(
        client,
        &args.advertiserId,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    displayvideo_advertisers_ad_group_ads_list_execute(builder)
}

/// PATCH v4/advertisers/{advertisersId}/adGroupAds/{adGroupAdsId}
/// Updates an ad group ad. This method is only supported for Demand Gen ads. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_ad_group_ads_patch_execute()` to send, or `displayvideo_advertisers_ad_group_ads_patch` for simplest API.

pub fn displayvideo_advertisers_ad_group_ads_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    adGroupAdId: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/adGroupAds/{}",
        advertiserId, adGroupAdId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v4/advertisers/{advertisersId}/adGroupAds/{adGroupAdsId}
/// Updates an ad group ad. This method is only supported for Demand Gen ads. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_ad_group_ads_patch_execute()` or `displayvideo_advertisers_ad_group_ads_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_group_ads_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_group_ads_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AdGroupAd>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AdGroupAd = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v4/advertisers/{advertisersId}/adGroupAds/{adGroupAdsId}
/// Updates an ad group ad. This method is only supported for Demand Gen ads. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_ad_group_ads_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_ad_group_ads_patch_task()`.
/// For the simplest API, use `displayvideo_advertisers_ad_group_ads_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_group_ads_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_ad_group_ads_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdGroupAd>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_ad_group_ads_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_ad_group_ads_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersAdGroupAdsPatchArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: adGroupAdId
    pub adGroupAdId: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v4/advertisers/{advertisersId}/adGroupAds/{adGroupAdsId}
/// Updates an ad group ad. This method is only supported for Demand Gen ads. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_ad_group_ads_patch_builder()` + `displayvideo_advertisers_ad_group_ads_patch_execute()`.
/// For task-level control, use `displayvideo_advertisers_ad_group_ads_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_group_ads_patch(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersAdGroupAdsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdGroupAd>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_ad_group_ads_patch_builder(
        client,
        &args.advertiserId,
        &args.adGroupAdId,
        &args.updateMask,
    )?;
    displayvideo_advertisers_ad_group_ads_patch_execute(builder)
}

/// POST v4/advertisers/{advertisersId}/adGroups:bulkEditAssignedTargetingOptions
/// Bulk edits targeting options for multiple ad groups. The same set of delete and create requests will be applied to all specified ad groups. Specifically, the operation will delete the assigned targeting options provided in BulkEditAdGroupAssignedTargetingOptionsRequest.delete_requests from each ad group, and then create the assigned targeting options provided in BulkEditAdGroupAssignedTargetingOptionsRequest.create_requests. This method is only supported for Demand Gen ad groups. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_ad_groups_bulk_edit_assigned_targeting_options_execute()` to send, or `displayvideo_advertisers_ad_groups_bulk_edit_assigned_targeting_options` for simplest API.

pub fn displayvideo_advertisers_ad_groups_bulk_edit_assigned_targeting_options_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/adGroups:bulkEditAssignedTargetingOptions",
        advertiserId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/advertisers/{advertisersId}/adGroups:bulkEditAssignedTargetingOptions
/// Bulk edits targeting options for multiple ad groups. The same set of delete and create requests will be applied to all specified ad groups. Specifically, the operation will delete the assigned targeting options provided in BulkEditAdGroupAssignedTargetingOptionsRequest.delete_requests from each ad group, and then create the assigned targeting options provided in BulkEditAdGroupAssignedTargetingOptionsRequest.create_requests. This method is only supported for Demand Gen ad groups. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_ad_groups_bulk_edit_assigned_targeting_options_execute()` or `displayvideo_advertisers_ad_groups_bulk_edit_assigned_targeting_options`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_groups_bulk_edit_assigned_targeting_options_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_groups_bulk_edit_assigned_targeting_options_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BulkEditAdGroupAssignedTargetingOptionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BulkEditAdGroupAssignedTargetingOptionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/advertisers/{advertisersId}/adGroups:bulkEditAssignedTargetingOptions
/// Bulk edits targeting options for multiple ad groups. The same set of delete and create requests will be applied to all specified ad groups. Specifically, the operation will delete the assigned targeting options provided in BulkEditAdGroupAssignedTargetingOptionsRequest.delete_requests from each ad group, and then create the assigned targeting options provided in BulkEditAdGroupAssignedTargetingOptionsRequest.create_requests. This method is only supported for Demand Gen ad groups. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_ad_groups_bulk_edit_assigned_targeting_options_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_ad_groups_bulk_edit_assigned_targeting_options_task()`.
/// For the simplest API, use `displayvideo_advertisers_ad_groups_bulk_edit_assigned_targeting_options()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_groups_bulk_edit_assigned_targeting_options_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_ad_groups_bulk_edit_assigned_targeting_options_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BulkEditAdGroupAssignedTargetingOptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        displayvideo_advertisers_ad_groups_bulk_edit_assigned_targeting_options_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_ad_groups_bulk_edit_assigned_targeting_options`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersAdGroupsBulkEditAssignedTargetingOptionsArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
}

/// POST v4/advertisers/{advertisersId}/adGroups:bulkEditAssignedTargetingOptions
/// Bulk edits targeting options for multiple ad groups. The same set of delete and create requests will be applied to all specified ad groups. Specifically, the operation will delete the assigned targeting options provided in BulkEditAdGroupAssignedTargetingOptionsRequest.delete_requests from each ad group, and then create the assigned targeting options provided in BulkEditAdGroupAssignedTargetingOptionsRequest.create_requests. This method is only supported for Demand Gen ad groups. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_ad_groups_bulk_edit_assigned_targeting_options_builder()` + `displayvideo_advertisers_ad_groups_bulk_edit_assigned_targeting_options_execute()`.
/// For task-level control, use `displayvideo_advertisers_ad_groups_bulk_edit_assigned_targeting_options_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_groups_bulk_edit_assigned_targeting_options(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersAdGroupsBulkEditAssignedTargetingOptionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BulkEditAdGroupAssignedTargetingOptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_ad_groups_bulk_edit_assigned_targeting_options_builder(
        client,
        &args.advertiserId,
    )?;
    displayvideo_advertisers_ad_groups_bulk_edit_assigned_targeting_options_execute(builder)
}

/// GET v4/advertisers/{advertisersId}/adGroups:bulkListAssignedTargetingOptions
/// Lists assigned targeting options for multiple ad groups across targeting types. Inherited assigned targeting options are not included.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_ad_groups_bulk_list_assigned_targeting_options_execute()` to send, or `displayvideo_advertisers_ad_groups_bulk_list_assigned_targeting_options` for simplest API.

pub fn displayvideo_advertisers_ad_groups_bulk_list_assigned_targeting_options_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    adGroupIds: &Option<String>,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/adGroups:bulkListAssignedTargetingOptions",
        advertiserId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = adGroupIds.as_ref() {
        query_parts.push(format!("adGroupIds={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}/adGroups:bulkListAssignedTargetingOptions
/// Lists assigned targeting options for multiple ad groups across targeting types. Inherited assigned targeting options are not included.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_ad_groups_bulk_list_assigned_targeting_options_execute()` or `displayvideo_advertisers_ad_groups_bulk_list_assigned_targeting_options`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_groups_bulk_list_assigned_targeting_options_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_groups_bulk_list_assigned_targeting_options_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BulkListAdGroupAssignedTargetingOptionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BulkListAdGroupAssignedTargetingOptionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}/adGroups:bulkListAssignedTargetingOptions
/// Lists assigned targeting options for multiple ad groups across targeting types. Inherited assigned targeting options are not included.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_ad_groups_bulk_list_assigned_targeting_options_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_ad_groups_bulk_list_assigned_targeting_options_task()`.
/// For the simplest API, use `displayvideo_advertisers_ad_groups_bulk_list_assigned_targeting_options()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_groups_bulk_list_assigned_targeting_options_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_ad_groups_bulk_list_assigned_targeting_options_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BulkListAdGroupAssignedTargetingOptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        displayvideo_advertisers_ad_groups_bulk_list_assigned_targeting_options_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_ad_groups_bulk_list_assigned_targeting_options`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersAdGroupsBulkListAssignedTargetingOptionsArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Query parameter: adGroupIds
    pub adGroupIds: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v4/advertisers/{advertisersId}/adGroups:bulkListAssignedTargetingOptions
/// Lists assigned targeting options for multiple ad groups across targeting types. Inherited assigned targeting options are not included.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_ad_groups_bulk_list_assigned_targeting_options_builder()` + `displayvideo_advertisers_ad_groups_bulk_list_assigned_targeting_options_execute()`.
/// For task-level control, use `displayvideo_advertisers_ad_groups_bulk_list_assigned_targeting_options_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_groups_bulk_list_assigned_targeting_options(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersAdGroupsBulkListAssignedTargetingOptionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BulkListAdGroupAssignedTargetingOptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_ad_groups_bulk_list_assigned_targeting_options_builder(
        client,
        &args.advertiserId,
        &args.adGroupIds,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    displayvideo_advertisers_ad_groups_bulk_list_assigned_targeting_options_execute(builder)
}

/// POST v4/advertisers/{advertisersId}/adGroups
/// Creates a new ad group. Returns the newly created ad group if successful. This method is only supported for Demand Gen ad groups. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_ad_groups_create_execute()` to send, or `displayvideo_advertisers_ad_groups_create` for simplest API.

pub fn displayvideo_advertisers_ad_groups_create_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/adGroups",
        advertiserId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/advertisers/{advertisersId}/adGroups
/// Creates a new ad group. Returns the newly created ad group if successful. This method is only supported for Demand Gen ad groups. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_ad_groups_create_execute()` or `displayvideo_advertisers_ad_groups_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_groups_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AdGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AdGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/advertisers/{advertisersId}/adGroups
/// Creates a new ad group. Returns the newly created ad group if successful. This method is only supported for Demand Gen ad groups. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_ad_groups_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_ad_groups_create_task()`.
/// For the simplest API, use `displayvideo_advertisers_ad_groups_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_ad_groups_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdGroup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_ad_groups_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_ad_groups_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersAdGroupsCreateArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
}

/// POST v4/advertisers/{advertisersId}/adGroups
/// Creates a new ad group. Returns the newly created ad group if successful. This method is only supported for Demand Gen ad groups. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_ad_groups_create_builder()` + `displayvideo_advertisers_ad_groups_create_execute()`.
/// For task-level control, use `displayvideo_advertisers_ad_groups_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_groups_create(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersAdGroupsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdGroup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_ad_groups_create_builder(client, &args.advertiserId)?;
    displayvideo_advertisers_ad_groups_create_execute(builder)
}

/// DELETE v4/advertisers/{advertisersId}/adGroups/{adGroupsId}
/// Deletes a AdGroup. Returns error code NOT_FOUND if the ad group does not exist. This method is only supported for Demand Gen ad groups. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_ad_groups_delete_execute()` to send, or `displayvideo_advertisers_ad_groups_delete` for simplest API.

pub fn displayvideo_advertisers_ad_groups_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    adGroupId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/adGroups/{}",
        advertiserId, adGroupId,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v4/advertisers/{advertisersId}/adGroups/{adGroupsId}
/// Deletes a AdGroup. Returns error code NOT_FOUND if the ad group does not exist. This method is only supported for Demand Gen ad groups. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_ad_groups_delete_execute()` or `displayvideo_advertisers_ad_groups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_groups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v4/advertisers/{advertisersId}/adGroups/{adGroupsId}
/// Deletes a AdGroup. Returns error code NOT_FOUND if the ad group does not exist. This method is only supported for Demand Gen ad groups. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_ad_groups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_ad_groups_delete_task()`.
/// For the simplest API, use `displayvideo_advertisers_ad_groups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_ad_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_ad_groups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_ad_groups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersAdGroupsDeleteArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: adGroupId
    pub adGroupId: String,
}

/// DELETE v4/advertisers/{advertisersId}/adGroups/{adGroupsId}
/// Deletes a AdGroup. Returns error code NOT_FOUND if the ad group does not exist. This method is only supported for Demand Gen ad groups. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_ad_groups_delete_builder()` + `displayvideo_advertisers_ad_groups_delete_execute()`.
/// For task-level control, use `displayvideo_advertisers_ad_groups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_groups_delete(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersAdGroupsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_ad_groups_delete_builder(
        client,
        &args.advertiserId,
        &args.adGroupId,
    )?;
    displayvideo_advertisers_ad_groups_delete_execute(builder)
}

/// GET v4/advertisers/{advertisersId}/adGroups/{adGroupsId}
/// Gets an ad group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_ad_groups_get_execute()` to send, or `displayvideo_advertisers_ad_groups_get` for simplest API.

pub fn displayvideo_advertisers_ad_groups_get_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    adGroupId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/adGroups/{}",
        advertiserId, adGroupId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}/adGroups/{adGroupsId}
/// Gets an ad group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_ad_groups_get_execute()` or `displayvideo_advertisers_ad_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AdGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AdGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}/adGroups/{adGroupsId}
/// Gets an ad group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_ad_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_ad_groups_get_task()`.
/// For the simplest API, use `displayvideo_advertisers_ad_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_ad_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdGroup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_ad_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_ad_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersAdGroupsGetArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: adGroupId
    pub adGroupId: String,
}

/// GET v4/advertisers/{advertisersId}/adGroups/{adGroupsId}
/// Gets an ad group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_ad_groups_get_builder()` + `displayvideo_advertisers_ad_groups_get_execute()`.
/// For task-level control, use `displayvideo_advertisers_ad_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_groups_get(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersAdGroupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdGroup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_ad_groups_get_builder(
        client,
        &args.advertiserId,
        &args.adGroupId,
    )?;
    displayvideo_advertisers_ad_groups_get_execute(builder)
}

/// GET v4/advertisers/{advertisersId}/adGroups
/// Lists ad groups.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_ad_groups_list_execute()` to send, or `displayvideo_advertisers_ad_groups_list` for simplest API.

pub fn displayvideo_advertisers_ad_groups_list_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/adGroups",
        advertiserId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}/adGroups
/// Lists ad groups.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_ad_groups_list_execute()` or `displayvideo_advertisers_ad_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAdGroupsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAdGroupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}/adGroups
/// Lists ad groups.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_ad_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_ad_groups_list_task()`.
/// For the simplest API, use `displayvideo_advertisers_ad_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_ad_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAdGroupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_ad_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_ad_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersAdGroupsListArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v4/advertisers/{advertisersId}/adGroups
/// Lists ad groups.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_ad_groups_list_builder()` + `displayvideo_advertisers_ad_groups_list_execute()`.
/// For task-level control, use `displayvideo_advertisers_ad_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_groups_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersAdGroupsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAdGroupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_ad_groups_list_builder(
        client,
        &args.advertiserId,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    displayvideo_advertisers_ad_groups_list_execute(builder)
}

/// PATCH v4/advertisers/{advertisersId}/adGroups/{adGroupsId}
/// Updates an existing ad group. Returns the updated ad group if successful. This method is only supported for Demand Gen ad groups. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_ad_groups_patch_execute()` to send, or `displayvideo_advertisers_ad_groups_patch` for simplest API.

pub fn displayvideo_advertisers_ad_groups_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    adGroupId: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/adGroups/{}",
        advertiserId, adGroupId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v4/advertisers/{advertisersId}/adGroups/{adGroupsId}
/// Updates an existing ad group. Returns the updated ad group if successful. This method is only supported for Demand Gen ad groups. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_ad_groups_patch_execute()` or `displayvideo_advertisers_ad_groups_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_groups_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AdGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AdGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v4/advertisers/{advertisersId}/adGroups/{adGroupsId}
/// Updates an existing ad group. Returns the updated ad group if successful. This method is only supported for Demand Gen ad groups. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_ad_groups_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_ad_groups_patch_task()`.
/// For the simplest API, use `displayvideo_advertisers_ad_groups_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_ad_groups_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdGroup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_ad_groups_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_ad_groups_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersAdGroupsPatchArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: adGroupId
    pub adGroupId: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v4/advertisers/{advertisersId}/adGroups/{adGroupsId}
/// Updates an existing ad group. Returns the updated ad group if successful. This method is only supported for Demand Gen ad groups. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_ad_groups_patch_builder()` + `displayvideo_advertisers_ad_groups_patch_execute()`.
/// For task-level control, use `displayvideo_advertisers_ad_groups_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_groups_patch(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersAdGroupsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdGroup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_ad_groups_patch_builder(
        client,
        &args.advertiserId,
        &args.adGroupId,
        &args.updateMask,
    )?;
    displayvideo_advertisers_ad_groups_patch_execute(builder)
}

/// POST v4/advertisers/{advertisersId}/adGroups/{adGroupsId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions
/// Assigns a targeting option to an ad group. Returns the assigned targeting option if successful. This method is only supported for Demand Gen ad groups. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_create_execute()` to send, or `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_create` for simplest API.

pub fn displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_create_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    adGroupId: &String,
    targetingType: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/adGroups/{}/targetingTypes/{}/assignedTargetingOptions",
        advertiserId,
        adGroupId,
        targetingType,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/advertisers/{advertisersId}/adGroups/{adGroupsId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions
/// Assigns a targeting option to an ad group. Returns the assigned targeting option if successful. This method is only supported for Demand Gen ad groups. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_create_execute()` or `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AssignedTargetingOption>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AssignedTargetingOption = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/advertisers/{advertisersId}/adGroups/{adGroupsId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions
/// Assigns a targeting option to an ad group. Returns the assigned targeting option if successful. This method is only supported for Demand Gen ad groups. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_create_task()`.
/// For the simplest API, use `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AssignedTargetingOption>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_create_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersAdGroupsTargetingTypesAssignedTargetingOptionsCreateArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: adGroupId
    pub adGroupId: String,
    /// Path parameter: targetingType
    pub targetingType: String,
}

/// POST v4/advertisers/{advertisersId}/adGroups/{adGroupsId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions
/// Assigns a targeting option to an ad group. Returns the assigned targeting option if successful. This method is only supported for Demand Gen ad groups. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_create_builder()` + `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_create_execute()`.
/// For task-level control, use `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_create(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersAdGroupsTargetingTypesAssignedTargetingOptionsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AssignedTargetingOption>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_create_builder(client, &args.advertiserId, &args.adGroupId, &args.targetingType)?;
    displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_create_execute(
        builder,
    )
}

/// DELETE v4/advertisers/{advertisersId}/adGroups/{adGroupsId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions/{assignedTargetingOptionsId}
/// Deletes an assigned targeting option from an ad group. This method is only supported for Demand Gen ad groups with the AdGroupFormat AD_GROUP_FORMAT_DEMAND_GEN. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_delete_execute()` to send, or `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_delete` for simplest API.

pub fn displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_delete_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    adGroupId: &String,
    targetingType: &String,
    assignedTargetingOptionId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/adGroups/{}/targetingTypes/{}/assignedTargetingOptions/{}",
        advertiserId,
        adGroupId,
        targetingType,
        assignedTargetingOptionId,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v4/advertisers/{advertisersId}/adGroups/{adGroupsId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions/{assignedTargetingOptionsId}
/// Deletes an assigned targeting option from an ad group. This method is only supported for Demand Gen ad groups with the AdGroupFormat AD_GROUP_FORMAT_DEMAND_GEN. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_delete_execute()` or `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v4/advertisers/{advertisersId}/adGroups/{adGroupsId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions/{assignedTargetingOptionsId}
/// Deletes an assigned targeting option from an ad group. This method is only supported for Demand Gen ad groups with the AdGroupFormat AD_GROUP_FORMAT_DEMAND_GEN. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_delete_task()`.
/// For the simplest API, use `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_delete_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersAdGroupsTargetingTypesAssignedTargetingOptionsDeleteArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: adGroupId
    pub adGroupId: String,
    /// Path parameter: targetingType
    pub targetingType: String,
    /// Path parameter: assignedTargetingOptionId
    pub assignedTargetingOptionId: String,
}

/// DELETE v4/advertisers/{advertisersId}/adGroups/{adGroupsId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions/{assignedTargetingOptionsId}
/// Deletes an assigned targeting option from an ad group. This method is only supported for Demand Gen ad groups with the AdGroupFormat AD_GROUP_FORMAT_DEMAND_GEN. Retrieval and management of Demand Gen resources is currently in beta. This method is only available to allowlisted users.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_delete_builder()` + `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_delete_execute()`.
/// For task-level control, use `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_delete(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersAdGroupsTargetingTypesAssignedTargetingOptionsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_delete_builder(client, &args.advertiserId, &args.adGroupId, &args.targetingType, &args.assignedTargetingOptionId)?;
    displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_delete_execute(
        builder,
    )
}

/// GET v4/advertisers/{advertisersId}/adGroups/{adGroupsId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions/{assignedTargetingOptionsId}
/// Gets a single targeting option assigned to an ad group. Inherited assigned targeting options are not included.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_get_execute()` to send, or `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_get` for simplest API.

pub fn displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_get_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    adGroupId: &String,
    targetingType: &String,
    assignedTargetingOptionId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/adGroups/{}/targetingTypes/{}/assignedTargetingOptions/{}",
        advertiserId,
        adGroupId,
        targetingType,
        assignedTargetingOptionId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}/adGroups/{adGroupsId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions/{assignedTargetingOptionsId}
/// Gets a single targeting option assigned to an ad group. Inherited assigned targeting options are not included.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_get_execute()` or `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AssignedTargetingOption>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AssignedTargetingOption = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}/adGroups/{adGroupsId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions/{assignedTargetingOptionsId}
/// Gets a single targeting option assigned to an ad group. Inherited assigned targeting options are not included.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_get_task()`.
/// For the simplest API, use `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AssignedTargetingOption>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_get_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersAdGroupsTargetingTypesAssignedTargetingOptionsGetArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: adGroupId
    pub adGroupId: String,
    /// Path parameter: targetingType
    pub targetingType: String,
    /// Path parameter: assignedTargetingOptionId
    pub assignedTargetingOptionId: String,
}

/// GET v4/advertisers/{advertisersId}/adGroups/{adGroupsId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions/{assignedTargetingOptionsId}
/// Gets a single targeting option assigned to an ad group. Inherited assigned targeting options are not included.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_get_builder()` + `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_get_execute()`.
/// For task-level control, use `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_get(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersAdGroupsTargetingTypesAssignedTargetingOptionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AssignedTargetingOption>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_get_builder(
            client,
            &args.advertiserId,
            &args.adGroupId,
            &args.targetingType,
            &args.assignedTargetingOptionId,
        )?;
    displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_get_execute(
        builder,
    )
}

/// GET v4/advertisers/{advertisersId}/adGroups/{adGroupsId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions
/// Lists the targeting options assigned to an ad group. Inherited assigned targeting options are not included.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_list_execute()` to send, or `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_list` for simplest API.

pub fn displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_list_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    adGroupId: &String,
    targetingType: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/adGroups/{}/targetingTypes/{}/assignedTargetingOptions",
        advertiserId,
        adGroupId,
        targetingType,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}/adGroups/{adGroupsId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions
/// Lists the targeting options assigned to an ad group. Inherited assigned targeting options are not included.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_list_execute()` or `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAdGroupAssignedTargetingOptionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAdGroupAssignedTargetingOptionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}/adGroups/{adGroupsId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions
/// Lists the targeting options assigned to an ad group. Inherited assigned targeting options are not included.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_list_task()`.
/// For the simplest API, use `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAdGroupAssignedTargetingOptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_list_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersAdGroupsTargetingTypesAssignedTargetingOptionsListArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: adGroupId
    pub adGroupId: String,
    /// Path parameter: targetingType
    pub targetingType: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v4/advertisers/{advertisersId}/adGroups/{adGroupsId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions
/// Lists the targeting options assigned to an ad group. Inherited assigned targeting options are not included.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_list_builder()` + `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_list_execute()`.
/// For task-level control, use `displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersAdGroupsTargetingTypesAssignedTargetingOptionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAdGroupAssignedTargetingOptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_list_builder(
            client,
            &args.advertiserId,
            &args.adGroupId,
            &args.targetingType,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )?;
    displayvideo_advertisers_ad_groups_targeting_types_assigned_targeting_options_list_execute(
        builder,
    )
}

/// POST v4/advertisers/{advertisersId}/adGroups/{adGroupsId}/youtubeAssetTypes/{youtubeAssetTypesId}/youtubeAssetAssociations
/// Creates a new association between the identified resource and a YouTube asset. Returns the newly-created association. *Warning:* This method is only available to an informed subset of users.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_create_execute()` to send, or `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_create` for simplest API.

pub fn displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_create_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    adGroupId: &String,
    youtubeAssetType: &String,
    linkedEntity_lineItemId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/adGroups/{}/youtubeAssetTypes/{}/youtubeAssetAssociations",
        advertiserId,
        adGroupId,
        youtubeAssetType,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = linkedEntity_lineItemId.as_ref() {
        query_parts.push(format!("linkedEntity.lineItemId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/advertisers/{advertisersId}/adGroups/{adGroupsId}/youtubeAssetTypes/{youtubeAssetTypesId}/youtubeAssetAssociations
/// Creates a new association between the identified resource and a YouTube asset. Returns the newly-created association. *Warning:* This method is only available to an informed subset of users.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_create_execute()` or `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<YoutubeAssetAssociation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: YoutubeAssetAssociation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/advertisers/{advertisersId}/adGroups/{adGroupsId}/youtubeAssetTypes/{youtubeAssetTypesId}/youtubeAssetAssociations
/// Creates a new association between the identified resource and a YouTube asset. Returns the newly-created association. *Warning:* This method is only available to an informed subset of users.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_create_task()`.
/// For the simplest API, use `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<YoutubeAssetAssociation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersAdGroupsYoutubeAssetTypesYoutubeAssetAssociationsCreateArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: adGroupId
    pub adGroupId: String,
    /// Path parameter: youtubeAssetType
    pub youtubeAssetType: String,
    /// Query parameter: linkedEntity_lineItemId
    pub linkedEntity_lineItemId: Option<String>,
}

/// POST v4/advertisers/{advertisersId}/adGroups/{adGroupsId}/youtubeAssetTypes/{youtubeAssetTypesId}/youtubeAssetAssociations
/// Creates a new association between the identified resource and a YouTube asset. Returns the newly-created association. *Warning:* This method is only available to an informed subset of users.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_create_builder()` + `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_create_execute()`.
/// For task-level control, use `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_create(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersAdGroupsYoutubeAssetTypesYoutubeAssetAssociationsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<YoutubeAssetAssociation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_create_builder(client, &args.advertiserId, &args.adGroupId, &args.youtubeAssetType, &args.linkedEntity_lineItemId)?;
    displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_create_execute(
        builder,
    )
}

/// DELETE v4/advertisers/{advertisersId}/adGroups/{adGroupsId}/youtubeAssetTypes/{youtubeAssetTypesId}/youtubeAssetAssociations/{youtubeAssetAssociationsId}
/// Deletes an existing association between the identified resource and a YouTube asset. *Warning:* This method is only available to an informed subset of users.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_delete_execute()` to send, or `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_delete` for simplest API.

pub fn displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_delete_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    adGroupId: &String,
    youtubeAssetType: &String,
    youtubeAssetAssociationId: &String,
    linkedEntity_lineItemId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/adGroups/{}/youtubeAssetTypes/{}/youtubeAssetAssociations/{}",
        advertiserId,
        adGroupId,
        youtubeAssetType,
        youtubeAssetAssociationId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = linkedEntity_lineItemId.as_ref() {
        query_parts.push(format!("linkedEntity.lineItemId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v4/advertisers/{advertisersId}/adGroups/{adGroupsId}/youtubeAssetTypes/{youtubeAssetTypesId}/youtubeAssetAssociations/{youtubeAssetAssociationsId}
/// Deletes an existing association between the identified resource and a YouTube asset. *Warning:* This method is only available to an informed subset of users.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_delete_execute()` or `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v4/advertisers/{advertisersId}/adGroups/{adGroupsId}/youtubeAssetTypes/{youtubeAssetTypesId}/youtubeAssetAssociations/{youtubeAssetAssociationsId}
/// Deletes an existing association between the identified resource and a YouTube asset. *Warning:* This method is only available to an informed subset of users.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_delete_task()`.
/// For the simplest API, use `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersAdGroupsYoutubeAssetTypesYoutubeAssetAssociationsDeleteArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: adGroupId
    pub adGroupId: String,
    /// Path parameter: youtubeAssetType
    pub youtubeAssetType: String,
    /// Path parameter: youtubeAssetAssociationId
    pub youtubeAssetAssociationId: String,
    /// Query parameter: linkedEntity_lineItemId
    pub linkedEntity_lineItemId: Option<String>,
}

/// DELETE v4/advertisers/{advertisersId}/adGroups/{adGroupsId}/youtubeAssetTypes/{youtubeAssetTypesId}/youtubeAssetAssociations/{youtubeAssetAssociationsId}
/// Deletes an existing association between the identified resource and a YouTube asset. *Warning:* This method is only available to an informed subset of users.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_delete_builder()` + `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_delete_execute()`.
/// For task-level control, use `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_delete(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersAdGroupsYoutubeAssetTypesYoutubeAssetAssociationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_delete_builder(client, &args.advertiserId, &args.adGroupId, &args.youtubeAssetType, &args.youtubeAssetAssociationId, &args.linkedEntity_lineItemId)?;
    displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_delete_execute(
        builder,
    )
}

/// GET v4/advertisers/{advertisersId}/adGroups/{adGroupsId}/youtubeAssetTypes/{youtubeAssetTypesId}/youtubeAssetAssociations
/// Lists the YouTube asset associations linked to the given resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_list_execute()` to send, or `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_list` for simplest API.

pub fn displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_list_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    adGroupId: &String,
    youtubeAssetType: &String,
    linkedEntity_lineItemId: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/adGroups/{}/youtubeAssetTypes/{}/youtubeAssetAssociations",
        advertiserId,
        adGroupId,
        youtubeAssetType,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = linkedEntity_lineItemId.as_ref() {
        query_parts.push(format!("linkedEntity.lineItemId={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}/adGroups/{adGroupsId}/youtubeAssetTypes/{youtubeAssetTypesId}/youtubeAssetAssociations
/// Lists the YouTube asset associations linked to the given resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_list_execute()` or `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListYoutubeAssetAssociationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListYoutubeAssetAssociationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}/adGroups/{adGroupsId}/youtubeAssetTypes/{youtubeAssetTypesId}/youtubeAssetAssociations
/// Lists the YouTube asset associations linked to the given resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_list_task()`.
/// For the simplest API, use `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListYoutubeAssetAssociationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersAdGroupsYoutubeAssetTypesYoutubeAssetAssociationsListArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: adGroupId
    pub adGroupId: String,
    /// Path parameter: youtubeAssetType
    pub youtubeAssetType: String,
    /// Query parameter: linkedEntity_lineItemId
    pub linkedEntity_lineItemId: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v4/advertisers/{advertisersId}/adGroups/{adGroupsId}/youtubeAssetTypes/{youtubeAssetTypesId}/youtubeAssetAssociations
/// Lists the YouTube asset associations linked to the given resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_list_builder()` + `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_list_execute()`.
/// For task-level control, use `displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersAdGroupsYoutubeAssetTypesYoutubeAssetAssociationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListYoutubeAssetAssociationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_list_builder(client, &args.advertiserId, &args.adGroupId, &args.youtubeAssetType, &args.linkedEntity_lineItemId, &args.orderBy, &args.pageSize, &args.pageToken)?;
    displayvideo_advertisers_ad_groups_youtube_asset_types_youtube_asset_associations_list_execute(
        builder,
    )
}

/// POST v4/advertisers/{advertisersId}/assets
/// Uploads an asset. Returns the ID of the newly uploaded asset if successful. The asset file size should be no more than 10 MB for images, 200 MB for ZIP files, and 1 GB for videos. Must be used within the [multipart media upload process](/display-`video/api/guides/how-tos/upload`#multipart). Examples using provided client libraries can be found in our [Creating Creatives guide](/display-`video/api/guides/creating-creatives/overview`#upload_an_asset).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_assets_upload_execute()` to send, or `displayvideo_advertisers_assets_upload` for simplest API.

pub fn displayvideo_advertisers_assets_upload_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/assets",
        advertiserId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/advertisers/{advertisersId}/assets
/// Uploads an asset. Returns the ID of the newly uploaded asset if successful. The asset file size should be no more than 10 MB for images, 200 MB for ZIP files, and 1 GB for videos. Must be used within the [multipart media upload process](/display-`video/api/guides/how-tos/upload`#multipart). Examples using provided client libraries can be found in our [Creating Creatives guide](/display-`video/api/guides/creating-creatives/overview`#upload_an_asset).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_assets_upload_execute()` or `displayvideo_advertisers_assets_upload`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_assets_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_assets_upload_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CreateAssetResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CreateAssetResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/advertisers/{advertisersId}/assets
/// Uploads an asset. Returns the ID of the newly uploaded asset if successful. The asset file size should be no more than 10 MB for images, 200 MB for ZIP files, and 1 GB for videos. Must be used within the [multipart media upload process](/display-`video/api/guides/how-tos/upload`#multipart). Examples using provided client libraries can be found in our [Creating Creatives guide](/display-`video/api/guides/creating-creatives/overview`#upload_an_asset).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_assets_upload_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_assets_upload_task()`.
/// For the simplest API, use `displayvideo_advertisers_assets_upload()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_assets_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_assets_upload_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreateAssetResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_assets_upload_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_assets_upload`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersAssetsUploadArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
}

/// POST v4/advertisers/{advertisersId}/assets
/// Uploads an asset. Returns the ID of the newly uploaded asset if successful. The asset file size should be no more than 10 MB for images, 200 MB for ZIP files, and 1 GB for videos. Must be used within the [multipart media upload process](/display-`video/api/guides/how-tos/upload`#multipart). Examples using provided client libraries can be found in our [Creating Creatives guide](/display-`video/api/guides/creating-creatives/overview`#upload_an_asset).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_assets_upload_builder()` + `displayvideo_advertisers_assets_upload_execute()`.
/// For task-level control, use `displayvideo_advertisers_assets_upload_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_assets_upload(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersAssetsUploadArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CreateAssetResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_assets_upload_builder(client, &args.advertiserId)?;
    displayvideo_advertisers_assets_upload_execute(builder)
}

/// POST v4/advertisers/{advertisersId}/campaigns
/// Creates a new campaign. Returns the newly created campaign if successful.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_campaigns_create_execute()` to send, or `displayvideo_advertisers_campaigns_create` for simplest API.

pub fn displayvideo_advertisers_campaigns_create_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/campaigns",
        advertiserId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/advertisers/{advertisersId}/campaigns
/// Creates a new campaign. Returns the newly created campaign if successful.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_campaigns_create_execute()` or `displayvideo_advertisers_campaigns_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_campaigns_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_campaigns_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Campaign>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Campaign = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/advertisers/{advertisersId}/campaigns
/// Creates a new campaign. Returns the newly created campaign if successful.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_campaigns_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_campaigns_create_task()`.
/// For the simplest API, use `displayvideo_advertisers_campaigns_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_campaigns_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_campaigns_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Campaign>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_campaigns_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_campaigns_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersCampaignsCreateArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
}

/// POST v4/advertisers/{advertisersId}/campaigns
/// Creates a new campaign. Returns the newly created campaign if successful.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_campaigns_create_builder()` + `displayvideo_advertisers_campaigns_create_execute()`.
/// For task-level control, use `displayvideo_advertisers_campaigns_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_campaigns_create(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersCampaignsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Campaign>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_campaigns_create_builder(client, &args.advertiserId)?;
    displayvideo_advertisers_campaigns_create_execute(builder)
}

/// DELETE v4/advertisers/{advertisersId}/campaigns/{campaignsId}
/// Permanently deletes a campaign. A deleted campaign cannot be recovered. The campaign should be archived first, i.e. set entity_status to ENTITY_STATUS_ARCHIVED, to be able to delete it. **This method regularly experiences high latency.** We recommend [increasing your default timeout](/display-`video/api/guides/best-practices/timeouts`#client_library_timeout) to avoid errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_campaigns_delete_execute()` to send, or `displayvideo_advertisers_campaigns_delete` for simplest API.

pub fn displayvideo_advertisers_campaigns_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    campaignId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/campaigns/{}",
        advertiserId, campaignId,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v4/advertisers/{advertisersId}/campaigns/{campaignsId}
/// Permanently deletes a campaign. A deleted campaign cannot be recovered. The campaign should be archived first, i.e. set entity_status to ENTITY_STATUS_ARCHIVED, to be able to delete it. **This method regularly experiences high latency.** We recommend [increasing your default timeout](/display-`video/api/guides/best-practices/timeouts`#client_library_timeout) to avoid errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_campaigns_delete_execute()` or `displayvideo_advertisers_campaigns_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_campaigns_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_campaigns_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v4/advertisers/{advertisersId}/campaigns/{campaignsId}
/// Permanently deletes a campaign. A deleted campaign cannot be recovered. The campaign should be archived first, i.e. set entity_status to ENTITY_STATUS_ARCHIVED, to be able to delete it. **This method regularly experiences high latency.** We recommend [increasing your default timeout](/display-`video/api/guides/best-practices/timeouts`#client_library_timeout) to avoid errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_campaigns_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_campaigns_delete_task()`.
/// For the simplest API, use `displayvideo_advertisers_campaigns_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_campaigns_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_campaigns_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_campaigns_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_campaigns_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersCampaignsDeleteArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: campaignId
    pub campaignId: String,
}

/// DELETE v4/advertisers/{advertisersId}/campaigns/{campaignsId}
/// Permanently deletes a campaign. A deleted campaign cannot be recovered. The campaign should be archived first, i.e. set entity_status to ENTITY_STATUS_ARCHIVED, to be able to delete it. **This method regularly experiences high latency.** We recommend [increasing your default timeout](/display-`video/api/guides/best-practices/timeouts`#client_library_timeout) to avoid errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_campaigns_delete_builder()` + `displayvideo_advertisers_campaigns_delete_execute()`.
/// For task-level control, use `displayvideo_advertisers_campaigns_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_campaigns_delete(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersCampaignsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_campaigns_delete_builder(
        client,
        &args.advertiserId,
        &args.campaignId,
    )?;
    displayvideo_advertisers_campaigns_delete_execute(builder)
}

/// GET v4/advertisers/{advertisersId}/campaigns/{campaignsId}
/// Gets a campaign.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_campaigns_get_execute()` to send, or `displayvideo_advertisers_campaigns_get` for simplest API.

pub fn displayvideo_advertisers_campaigns_get_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    campaignId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/campaigns/{}",
        advertiserId, campaignId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}/campaigns/{campaignsId}
/// Gets a campaign.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_campaigns_get_execute()` or `displayvideo_advertisers_campaigns_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_campaigns_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_campaigns_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Campaign>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Campaign = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}/campaigns/{campaignsId}
/// Gets a campaign.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_campaigns_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_campaigns_get_task()`.
/// For the simplest API, use `displayvideo_advertisers_campaigns_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_campaigns_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_campaigns_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Campaign>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_campaigns_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_campaigns_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersCampaignsGetArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: campaignId
    pub campaignId: String,
}

/// GET v4/advertisers/{advertisersId}/campaigns/{campaignsId}
/// Gets a campaign.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_campaigns_get_builder()` + `displayvideo_advertisers_campaigns_get_execute()`.
/// For task-level control, use `displayvideo_advertisers_campaigns_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_campaigns_get(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersCampaignsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Campaign>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_campaigns_get_builder(
        client,
        &args.advertiserId,
        &args.campaignId,
    )?;
    displayvideo_advertisers_campaigns_get_execute(builder)
}

/// GET v4/advertisers/{advertisersId}/campaigns
/// Lists campaigns in an advertiser. The order is defined by the order_by parameter. If a filter by entity_status is not specified, campaigns with ENTITY_STATUS_ARCHIVED will not be included in the results.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_campaigns_list_execute()` to send, or `displayvideo_advertisers_campaigns_list` for simplest API.

pub fn displayvideo_advertisers_campaigns_list_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/campaigns",
        advertiserId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}/campaigns
/// Lists campaigns in an advertiser. The order is defined by the order_by parameter. If a filter by entity_status is not specified, campaigns with ENTITY_STATUS_ARCHIVED will not be included in the results.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_campaigns_list_execute()` or `displayvideo_advertisers_campaigns_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_campaigns_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_campaigns_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCampaignsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCampaignsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}/campaigns
/// Lists campaigns in an advertiser. The order is defined by the order_by parameter. If a filter by entity_status is not specified, campaigns with ENTITY_STATUS_ARCHIVED will not be included in the results.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_campaigns_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_campaigns_list_task()`.
/// For the simplest API, use `displayvideo_advertisers_campaigns_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_campaigns_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_campaigns_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCampaignsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_campaigns_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_campaigns_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersCampaignsListArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v4/advertisers/{advertisersId}/campaigns
/// Lists campaigns in an advertiser. The order is defined by the order_by parameter. If a filter by entity_status is not specified, campaigns with ENTITY_STATUS_ARCHIVED will not be included in the results.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_campaigns_list_builder()` + `displayvideo_advertisers_campaigns_list_execute()`.
/// For task-level control, use `displayvideo_advertisers_campaigns_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_campaigns_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersCampaignsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCampaignsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_campaigns_list_builder(
        client,
        &args.advertiserId,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    displayvideo_advertisers_campaigns_list_execute(builder)
}

/// PATCH v4/advertisers/{advertisersId}/campaigns/{campaignsId}
/// Updates an existing campaign. Returns the updated campaign if successful.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_campaigns_patch_execute()` to send, or `displayvideo_advertisers_campaigns_patch` for simplest API.

pub fn displayvideo_advertisers_campaigns_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    campaignId: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/campaigns/{}",
        advertiserId, campaignId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v4/advertisers/{advertisersId}/campaigns/{campaignsId}
/// Updates an existing campaign. Returns the updated campaign if successful.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_campaigns_patch_execute()` or `displayvideo_advertisers_campaigns_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_campaigns_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_campaigns_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Campaign>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Campaign = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v4/advertisers/{advertisersId}/campaigns/{campaignsId}
/// Updates an existing campaign. Returns the updated campaign if successful.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_campaigns_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_campaigns_patch_task()`.
/// For the simplest API, use `displayvideo_advertisers_campaigns_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_campaigns_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_campaigns_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Campaign>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_campaigns_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_campaigns_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersCampaignsPatchArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: campaignId
    pub campaignId: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v4/advertisers/{advertisersId}/campaigns/{campaignsId}
/// Updates an existing campaign. Returns the updated campaign if successful.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_campaigns_patch_builder()` + `displayvideo_advertisers_campaigns_patch_execute()`.
/// For task-level control, use `displayvideo_advertisers_campaigns_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_campaigns_patch(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersCampaignsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Campaign>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_campaigns_patch_builder(
        client,
        &args.advertiserId,
        &args.campaignId,
        &args.updateMask,
    )?;
    displayvideo_advertisers_campaigns_patch_execute(builder)
}

/// POST v4/advertisers/{advertisersId}/channels
/// Creates a new channel. Returns the newly created channel if successful.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_channels_create_execute()` to send, or `displayvideo_advertisers_channels_create` for simplest API.

pub fn displayvideo_advertisers_channels_create_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/channels",
        advertiserId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/advertisers/{advertisersId}/channels
/// Creates a new channel. Returns the newly created channel if successful.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_channels_create_execute()` or `displayvideo_advertisers_channels_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_channels_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_channels_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Channel>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Channel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/advertisers/{advertisersId}/channels
/// Creates a new channel. Returns the newly created channel if successful.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_channels_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_channels_create_task()`.
/// For the simplest API, use `displayvideo_advertisers_channels_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_channels_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_channels_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_channels_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_channels_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersChannelsCreateArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// POST v4/advertisers/{advertisersId}/channels
/// Creates a new channel. Returns the newly created channel if successful.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_channels_create_builder()` + `displayvideo_advertisers_channels_create_execute()`.
/// For task-level control, use `displayvideo_advertisers_channels_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_channels_create(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersChannelsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_channels_create_builder(
        client,
        &args.advertiserId,
        &args.partnerId,
    )?;
    displayvideo_advertisers_channels_create_execute(builder)
}

/// GET v4/advertisers/{advertisersId}/channels/{channelsId}
/// Gets a channel for a partner or advertiser.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_channels_get_execute()` to send, or `displayvideo_advertisers_channels_get` for simplest API.

pub fn displayvideo_advertisers_channels_get_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    channelId: &String,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/channels/{}",
        advertiserId, channelId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}/channels/{channelsId}
/// Gets a channel for a partner or advertiser.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_channels_get_execute()` or `displayvideo_advertisers_channels_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_channels_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_channels_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Channel>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Channel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}/channels/{channelsId}
/// Gets a channel for a partner or advertiser.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_channels_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_channels_get_task()`.
/// For the simplest API, use `displayvideo_advertisers_channels_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_channels_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_channels_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_channels_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_channels_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersChannelsGetArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: channelId
    pub channelId: String,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// GET v4/advertisers/{advertisersId}/channels/{channelsId}
/// Gets a channel for a partner or advertiser.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_channels_get_builder()` + `displayvideo_advertisers_channels_get_execute()`.
/// For task-level control, use `displayvideo_advertisers_channels_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_channels_get(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersChannelsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_channels_get_builder(
        client,
        &args.advertiserId,
        &args.channelId,
        &args.partnerId,
    )?;
    displayvideo_advertisers_channels_get_execute(builder)
}

/// GET v4/advertisers/{advertisersId}/channels
/// Lists channels for a partner or advertiser.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_channels_list_execute()` to send, or `displayvideo_advertisers_channels_list` for simplest API.

pub fn displayvideo_advertisers_channels_list_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/channels",
        advertiserId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}/channels
/// Lists channels for a partner or advertiser.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_channels_list_execute()` or `displayvideo_advertisers_channels_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_channels_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_channels_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListChannelsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListChannelsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}/channels
/// Lists channels for a partner or advertiser.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_channels_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_channels_list_task()`.
/// For the simplest API, use `displayvideo_advertisers_channels_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_channels_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_channels_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListChannelsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_channels_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_channels_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersChannelsListArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// GET v4/advertisers/{advertisersId}/channels
/// Lists channels for a partner or advertiser.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_channels_list_builder()` + `displayvideo_advertisers_channels_list_execute()`.
/// For task-level control, use `displayvideo_advertisers_channels_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_channels_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersChannelsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListChannelsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_channels_list_builder(
        client,
        &args.advertiserId,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.partnerId,
    )?;
    displayvideo_advertisers_channels_list_execute(builder)
}

/// PATCH v4/advertisers/{advertisersId}/channels/{channelId}
/// Updates a channel. Returns the updated channel if successful.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_channels_patch_execute()` to send, or `displayvideo_advertisers_channels_patch` for simplest API.

pub fn displayvideo_advertisers_channels_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    channelId: &String,
    partnerId: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/channels/{}",
        advertiserId, channelId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v4/advertisers/{advertisersId}/channels/{channelId}
/// Updates a channel. Returns the updated channel if successful.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_channels_patch_execute()` or `displayvideo_advertisers_channels_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_channels_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_channels_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Channel>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Channel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v4/advertisers/{advertisersId}/channels/{channelId}
/// Updates a channel. Returns the updated channel if successful.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_channels_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_channels_patch_task()`.
/// For the simplest API, use `displayvideo_advertisers_channels_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_channels_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_channels_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_channels_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_channels_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersChannelsPatchArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: channelId
    pub channelId: String,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v4/advertisers/{advertisersId}/channels/{channelId}
/// Updates a channel. Returns the updated channel if successful.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_channels_patch_builder()` + `displayvideo_advertisers_channels_patch_execute()`.
/// For task-level control, use `displayvideo_advertisers_channels_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_channels_patch(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersChannelsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_channels_patch_builder(
        client,
        &args.advertiserId,
        &args.channelId,
        &args.partnerId,
        &args.updateMask,
    )?;
    displayvideo_advertisers_channels_patch_execute(builder)
}

/// POST v4/advertisers/{advertiserId}/channels/{channelsId}/sites:bulkEdit
/// Bulk edits sites under a single channel. The operation will delete the sites provided in BulkEditSitesRequest.deleted_sites and then create the sites provided in BulkEditSitesRequest.created_sites.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_channels_sites_bulk_edit_execute()` to send, or `displayvideo_advertisers_channels_sites_bulk_edit` for simplest API.

pub fn displayvideo_advertisers_channels_sites_bulk_edit_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    channelId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/channels/{}/sites:bulkEdit",
        advertiserId, channelId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/advertisers/{advertiserId}/channels/{channelsId}/sites:bulkEdit
/// Bulk edits sites under a single channel. The operation will delete the sites provided in BulkEditSitesRequest.deleted_sites and then create the sites provided in BulkEditSitesRequest.created_sites.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_channels_sites_bulk_edit_execute()` or `displayvideo_advertisers_channels_sites_bulk_edit`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_channels_sites_bulk_edit_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_channels_sites_bulk_edit_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BulkEditSitesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BulkEditSitesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/advertisers/{advertiserId}/channels/{channelsId}/sites:bulkEdit
/// Bulk edits sites under a single channel. The operation will delete the sites provided in BulkEditSitesRequest.deleted_sites and then create the sites provided in BulkEditSitesRequest.created_sites.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_channels_sites_bulk_edit_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_channels_sites_bulk_edit_task()`.
/// For the simplest API, use `displayvideo_advertisers_channels_sites_bulk_edit()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_channels_sites_bulk_edit_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_channels_sites_bulk_edit_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BulkEditSitesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_channels_sites_bulk_edit_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_channels_sites_bulk_edit`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersChannelsSitesBulkEditArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: channelId
    pub channelId: String,
}

/// POST v4/advertisers/{advertiserId}/channels/{channelsId}/sites:bulkEdit
/// Bulk edits sites under a single channel. The operation will delete the sites provided in BulkEditSitesRequest.deleted_sites and then create the sites provided in BulkEditSitesRequest.created_sites.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_channels_sites_bulk_edit_builder()` + `displayvideo_advertisers_channels_sites_bulk_edit_execute()`.
/// For task-level control, use `displayvideo_advertisers_channels_sites_bulk_edit_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_channels_sites_bulk_edit(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersChannelsSitesBulkEditArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BulkEditSitesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_channels_sites_bulk_edit_builder(
        client,
        &args.advertiserId,
        &args.channelId,
    )?;
    displayvideo_advertisers_channels_sites_bulk_edit_execute(builder)
}

/// POST v4/advertisers/{advertiserId}/channels/{channelsId}/sites
/// Creates a site in a channel.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_channels_sites_create_execute()` to send, or `displayvideo_advertisers_channels_sites_create` for simplest API.

pub fn displayvideo_advertisers_channels_sites_create_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    channelId: &String,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/channels/{}/sites",
        advertiserId, channelId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/advertisers/{advertiserId}/channels/{channelsId}/sites
/// Creates a site in a channel.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_channels_sites_create_execute()` or `displayvideo_advertisers_channels_sites_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_channels_sites_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_channels_sites_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Site>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Site = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/advertisers/{advertiserId}/channels/{channelsId}/sites
/// Creates a site in a channel.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_channels_sites_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_channels_sites_create_task()`.
/// For the simplest API, use `displayvideo_advertisers_channels_sites_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_channels_sites_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_channels_sites_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Site>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_channels_sites_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_channels_sites_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersChannelsSitesCreateArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: channelId
    pub channelId: String,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// POST v4/advertisers/{advertiserId}/channels/{channelsId}/sites
/// Creates a site in a channel.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_channels_sites_create_builder()` + `displayvideo_advertisers_channels_sites_create_execute()`.
/// For task-level control, use `displayvideo_advertisers_channels_sites_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_channels_sites_create(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersChannelsSitesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Site>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_channels_sites_create_builder(
        client,
        &args.advertiserId,
        &args.channelId,
        &args.partnerId,
    )?;
    displayvideo_advertisers_channels_sites_create_execute(builder)
}

/// DELETE v4/advertisers/{advertiserId}/channels/{channelsId}/sites/{sitesId}
/// Deletes a site from a channel.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_channels_sites_delete_execute()` to send, or `displayvideo_advertisers_channels_sites_delete` for simplest API.

pub fn displayvideo_advertisers_channels_sites_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    channelId: &String,
    urlOrAppId: &String,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/channels/{}/sites/{}",
        advertiserId, channelId, urlOrAppId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v4/advertisers/{advertiserId}/channels/{channelsId}/sites/{sitesId}
/// Deletes a site from a channel.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_channels_sites_delete_execute()` or `displayvideo_advertisers_channels_sites_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_channels_sites_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_channels_sites_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v4/advertisers/{advertiserId}/channels/{channelsId}/sites/{sitesId}
/// Deletes a site from a channel.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_channels_sites_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_channels_sites_delete_task()`.
/// For the simplest API, use `displayvideo_advertisers_channels_sites_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_channels_sites_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_channels_sites_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_channels_sites_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_channels_sites_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersChannelsSitesDeleteArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: channelId
    pub channelId: String,
    /// Path parameter: urlOrAppId
    pub urlOrAppId: String,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// DELETE v4/advertisers/{advertiserId}/channels/{channelsId}/sites/{sitesId}
/// Deletes a site from a channel.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_channels_sites_delete_builder()` + `displayvideo_advertisers_channels_sites_delete_execute()`.
/// For task-level control, use `displayvideo_advertisers_channels_sites_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_channels_sites_delete(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersChannelsSitesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_channels_sites_delete_builder(
        client,
        &args.advertiserId,
        &args.channelId,
        &args.urlOrAppId,
        &args.partnerId,
    )?;
    displayvideo_advertisers_channels_sites_delete_execute(builder)
}

/// GET v4/advertisers/{advertisersId}/channels/{channelsId}/sites
/// Lists sites in a channel.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_channels_sites_list_execute()` to send, or `displayvideo_advertisers_channels_sites_list` for simplest API.

pub fn displayvideo_advertisers_channels_sites_list_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    channelId: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/channels/{}/sites",
        advertiserId, channelId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}/channels/{channelsId}/sites
/// Lists sites in a channel.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_channels_sites_list_execute()` or `displayvideo_advertisers_channels_sites_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_channels_sites_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_channels_sites_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSitesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSitesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}/channels/{channelsId}/sites
/// Lists sites in a channel.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_channels_sites_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_channels_sites_list_task()`.
/// For the simplest API, use `displayvideo_advertisers_channels_sites_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_channels_sites_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_channels_sites_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSitesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_channels_sites_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_channels_sites_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersChannelsSitesListArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: channelId
    pub channelId: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// GET v4/advertisers/{advertisersId}/channels/{channelsId}/sites
/// Lists sites in a channel.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_channels_sites_list_builder()` + `displayvideo_advertisers_channels_sites_list_execute()`.
/// For task-level control, use `displayvideo_advertisers_channels_sites_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_channels_sites_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersChannelsSitesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSitesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_channels_sites_list_builder(
        client,
        &args.advertiserId,
        &args.channelId,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.partnerId,
    )?;
    displayvideo_advertisers_channels_sites_list_execute(builder)
}

/// POST v4/advertisers/{advertiserId}/channels/{channelsId}/sites:replace
/// Replaces all of the sites under a single channel. The operation will replace the sites under a channel with the sites provided in ReplaceSitesRequest.new_sites. **This method regularly experiences high latency.** We recommend [increasing your default timeout](/display-`video/api/guides/best-practices/timeouts`#client_library_timeout) to avoid errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_channels_sites_replace_execute()` to send, or `displayvideo_advertisers_channels_sites_replace` for simplest API.

pub fn displayvideo_advertisers_channels_sites_replace_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    channelId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/channels/{}/sites:replace",
        advertiserId, channelId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/advertisers/{advertiserId}/channels/{channelsId}/sites:replace
/// Replaces all of the sites under a single channel. The operation will replace the sites under a channel with the sites provided in ReplaceSitesRequest.new_sites. **This method regularly experiences high latency.** We recommend [increasing your default timeout](/display-`video/api/guides/best-practices/timeouts`#client_library_timeout) to avoid errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_channels_sites_replace_execute()` or `displayvideo_advertisers_channels_sites_replace`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_channels_sites_replace_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_channels_sites_replace_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReplaceSitesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReplaceSitesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/advertisers/{advertiserId}/channels/{channelsId}/sites:replace
/// Replaces all of the sites under a single channel. The operation will replace the sites under a channel with the sites provided in ReplaceSitesRequest.new_sites. **This method regularly experiences high latency.** We recommend [increasing your default timeout](/display-`video/api/guides/best-practices/timeouts`#client_library_timeout) to avoid errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_channels_sites_replace_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_channels_sites_replace_task()`.
/// For the simplest API, use `displayvideo_advertisers_channels_sites_replace()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_channels_sites_replace_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_channels_sites_replace_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReplaceSitesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_channels_sites_replace_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_channels_sites_replace`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersChannelsSitesReplaceArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: channelId
    pub channelId: String,
}

/// POST v4/advertisers/{advertiserId}/channels/{channelsId}/sites:replace
/// Replaces all of the sites under a single channel. The operation will replace the sites under a channel with the sites provided in ReplaceSitesRequest.new_sites. **This method regularly experiences high latency.** We recommend [increasing your default timeout](/display-`video/api/guides/best-practices/timeouts`#client_library_timeout) to avoid errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_channels_sites_replace_builder()` + `displayvideo_advertisers_channels_sites_replace_execute()`.
/// For task-level control, use `displayvideo_advertisers_channels_sites_replace_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_channels_sites_replace(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersChannelsSitesReplaceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReplaceSitesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_channels_sites_replace_builder(
        client,
        &args.advertiserId,
        &args.channelId,
    )?;
    displayvideo_advertisers_channels_sites_replace_execute(builder)
}

/// POST v4/advertisers/{advertisersId}/creatives
/// Creates a new creative. Returns the newly created creative if successful. A ["Standard" user role](//support.google.`com/displayvideo/answer/2723011`) or greater for the parent advertiser or partner is required to make this request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_creatives_create_execute()` to send, or `displayvideo_advertisers_creatives_create` for simplest API.

pub fn displayvideo_advertisers_creatives_create_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/creatives",
        advertiserId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/advertisers/{advertisersId}/creatives
/// Creates a new creative. Returns the newly created creative if successful. A ["Standard" user role](//support.google.`com/displayvideo/answer/2723011`) or greater for the parent advertiser or partner is required to make this request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_creatives_create_execute()` or `displayvideo_advertisers_creatives_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_creatives_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_creatives_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Creative>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Creative = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/advertisers/{advertisersId}/creatives
/// Creates a new creative. Returns the newly created creative if successful. A ["Standard" user role](//support.google.`com/displayvideo/answer/2723011`) or greater for the parent advertiser or partner is required to make this request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_creatives_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_creatives_create_task()`.
/// For the simplest API, use `displayvideo_advertisers_creatives_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_creatives_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_creatives_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Creative>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_creatives_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_creatives_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersCreativesCreateArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
}

/// POST v4/advertisers/{advertisersId}/creatives
/// Creates a new creative. Returns the newly created creative if successful. A ["Standard" user role](//support.google.`com/displayvideo/answer/2723011`) or greater for the parent advertiser or partner is required to make this request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_creatives_create_builder()` + `displayvideo_advertisers_creatives_create_execute()`.
/// For task-level control, use `displayvideo_advertisers_creatives_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_creatives_create(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersCreativesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Creative>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_creatives_create_builder(client, &args.advertiserId)?;
    displayvideo_advertisers_creatives_create_execute(builder)
}

/// DELETE v4/advertisers/{advertisersId}/creatives/{creativesId}
/// Deletes a creative. Returns error code NOT_FOUND if the creative does not exist. The creative should be archived first, i.e. set entity_status to ENTITY_STATUS_ARCHIVED, before it can be deleted. A ["Standard" user role](//support.google.`com/displayvideo/answer/2723011`) or greater for the parent advertiser or partner is required to make this request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_creatives_delete_execute()` to send, or `displayvideo_advertisers_creatives_delete` for simplest API.

pub fn displayvideo_advertisers_creatives_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    creativeId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/creatives/{}",
        advertiserId, creativeId,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v4/advertisers/{advertisersId}/creatives/{creativesId}
/// Deletes a creative. Returns error code NOT_FOUND if the creative does not exist. The creative should be archived first, i.e. set entity_status to ENTITY_STATUS_ARCHIVED, before it can be deleted. A ["Standard" user role](//support.google.`com/displayvideo/answer/2723011`) or greater for the parent advertiser or partner is required to make this request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_creatives_delete_execute()` or `displayvideo_advertisers_creatives_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_creatives_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_creatives_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v4/advertisers/{advertisersId}/creatives/{creativesId}
/// Deletes a creative. Returns error code NOT_FOUND if the creative does not exist. The creative should be archived first, i.e. set entity_status to ENTITY_STATUS_ARCHIVED, before it can be deleted. A ["Standard" user role](//support.google.`com/displayvideo/answer/2723011`) or greater for the parent advertiser or partner is required to make this request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_creatives_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_creatives_delete_task()`.
/// For the simplest API, use `displayvideo_advertisers_creatives_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_creatives_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_creatives_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_creatives_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_creatives_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersCreativesDeleteArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: creativeId
    pub creativeId: String,
}

/// DELETE v4/advertisers/{advertisersId}/creatives/{creativesId}
/// Deletes a creative. Returns error code NOT_FOUND if the creative does not exist. The creative should be archived first, i.e. set entity_status to ENTITY_STATUS_ARCHIVED, before it can be deleted. A ["Standard" user role](//support.google.`com/displayvideo/answer/2723011`) or greater for the parent advertiser or partner is required to make this request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_creatives_delete_builder()` + `displayvideo_advertisers_creatives_delete_execute()`.
/// For task-level control, use `displayvideo_advertisers_creatives_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_creatives_delete(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersCreativesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_creatives_delete_builder(
        client,
        &args.advertiserId,
        &args.creativeId,
    )?;
    displayvideo_advertisers_creatives_delete_execute(builder)
}

/// GET v4/advertisers/{advertisersId}/creatives/{creativesId}
/// Gets a creative.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_creatives_get_execute()` to send, or `displayvideo_advertisers_creatives_get` for simplest API.

pub fn displayvideo_advertisers_creatives_get_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    creativeId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/creatives/{}",
        advertiserId, creativeId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}/creatives/{creativesId}
/// Gets a creative.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_creatives_get_execute()` or `displayvideo_advertisers_creatives_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_creatives_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_creatives_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Creative>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Creative = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}/creatives/{creativesId}
/// Gets a creative.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_creatives_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_creatives_get_task()`.
/// For the simplest API, use `displayvideo_advertisers_creatives_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_creatives_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_creatives_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Creative>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_creatives_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_creatives_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersCreativesGetArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: creativeId
    pub creativeId: String,
}

/// GET v4/advertisers/{advertisersId}/creatives/{creativesId}
/// Gets a creative.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_creatives_get_builder()` + `displayvideo_advertisers_creatives_get_execute()`.
/// For task-level control, use `displayvideo_advertisers_creatives_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_creatives_get(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersCreativesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Creative>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_creatives_get_builder(
        client,
        &args.advertiserId,
        &args.creativeId,
    )?;
    displayvideo_advertisers_creatives_get_execute(builder)
}

/// GET v4/advertisers/{advertisersId}/creatives
/// Lists creatives in an advertiser. The order is defined by the order_by parameter. If a filter by entity_status is not specified, creatives with ENTITY_STATUS_ARCHIVED will not be included in the results.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_creatives_list_execute()` to send, or `displayvideo_advertisers_creatives_list` for simplest API.

pub fn displayvideo_advertisers_creatives_list_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/creatives",
        advertiserId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}/creatives
/// Lists creatives in an advertiser. The order is defined by the order_by parameter. If a filter by entity_status is not specified, creatives with ENTITY_STATUS_ARCHIVED will not be included in the results.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_creatives_list_execute()` or `displayvideo_advertisers_creatives_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_creatives_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_creatives_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCreativesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCreativesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}/creatives
/// Lists creatives in an advertiser. The order is defined by the order_by parameter. If a filter by entity_status is not specified, creatives with ENTITY_STATUS_ARCHIVED will not be included in the results.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_creatives_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_creatives_list_task()`.
/// For the simplest API, use `displayvideo_advertisers_creatives_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_creatives_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_creatives_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCreativesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_creatives_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_creatives_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersCreativesListArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v4/advertisers/{advertisersId}/creatives
/// Lists creatives in an advertiser. The order is defined by the order_by parameter. If a filter by entity_status is not specified, creatives with ENTITY_STATUS_ARCHIVED will not be included in the results.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_creatives_list_builder()` + `displayvideo_advertisers_creatives_list_execute()`.
/// For task-level control, use `displayvideo_advertisers_creatives_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_creatives_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersCreativesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCreativesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_creatives_list_builder(
        client,
        &args.advertiserId,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    displayvideo_advertisers_creatives_list_execute(builder)
}

/// PATCH v4/advertisers/{advertisersId}/creatives/{creativesId}
/// Updates an existing creative. Returns the updated creative if successful. A ["Standard" user role](//support.google.`com/displayvideo/answer/2723011`) or greater for the parent advertiser or partner is required to make this request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_creatives_patch_execute()` to send, or `displayvideo_advertisers_creatives_patch` for simplest API.

pub fn displayvideo_advertisers_creatives_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    creativeId: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/creatives/{}",
        advertiserId, creativeId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v4/advertisers/{advertisersId}/creatives/{creativesId}
/// Updates an existing creative. Returns the updated creative if successful. A ["Standard" user role](//support.google.`com/displayvideo/answer/2723011`) or greater for the parent advertiser or partner is required to make this request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_creatives_patch_execute()` or `displayvideo_advertisers_creatives_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_creatives_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_creatives_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Creative>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Creative = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v4/advertisers/{advertisersId}/creatives/{creativesId}
/// Updates an existing creative. Returns the updated creative if successful. A ["Standard" user role](//support.google.`com/displayvideo/answer/2723011`) or greater for the parent advertiser or partner is required to make this request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_creatives_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_creatives_patch_task()`.
/// For the simplest API, use `displayvideo_advertisers_creatives_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_creatives_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_creatives_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Creative>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_creatives_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_creatives_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersCreativesPatchArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: creativeId
    pub creativeId: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v4/advertisers/{advertisersId}/creatives/{creativesId}
/// Updates an existing creative. Returns the updated creative if successful. A ["Standard" user role](//support.google.`com/displayvideo/answer/2723011`) or greater for the parent advertiser or partner is required to make this request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_creatives_patch_builder()` + `displayvideo_advertisers_creatives_patch_execute()`.
/// For task-level control, use `displayvideo_advertisers_creatives_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_creatives_patch(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersCreativesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Creative>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_creatives_patch_builder(
        client,
        &args.advertiserId,
        &args.creativeId,
        &args.updateMask,
    )?;
    displayvideo_advertisers_creatives_patch_execute(builder)
}

/// POST v4/advertisers/{advertisersId}/insertionOrders
/// Creates a new insertion order. Returns the newly created insertion order if successful.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_insertion_orders_create_execute()` to send, or `displayvideo_advertisers_insertion_orders_create` for simplest API.

pub fn displayvideo_advertisers_insertion_orders_create_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/insertionOrders",
        advertiserId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/advertisers/{advertisersId}/insertionOrders
/// Creates a new insertion order. Returns the newly created insertion order if successful.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_insertion_orders_create_execute()` or `displayvideo_advertisers_insertion_orders_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_insertion_orders_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_insertion_orders_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InsertionOrder>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InsertionOrder = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/advertisers/{advertisersId}/insertionOrders
/// Creates a new insertion order. Returns the newly created insertion order if successful.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_insertion_orders_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_insertion_orders_create_task()`.
/// For the simplest API, use `displayvideo_advertisers_insertion_orders_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_insertion_orders_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_insertion_orders_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InsertionOrder>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_insertion_orders_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_insertion_orders_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersInsertionOrdersCreateArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
}

/// POST v4/advertisers/{advertisersId}/insertionOrders
/// Creates a new insertion order. Returns the newly created insertion order if successful.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_insertion_orders_create_builder()` + `displayvideo_advertisers_insertion_orders_create_execute()`.
/// For task-level control, use `displayvideo_advertisers_insertion_orders_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_insertion_orders_create(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersInsertionOrdersCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InsertionOrder>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        displayvideo_advertisers_insertion_orders_create_builder(client, &args.advertiserId)?;
    displayvideo_advertisers_insertion_orders_create_execute(builder)
}

/// DELETE v4/advertisers/{advertisersId}/insertionOrders/{insertionOrdersId}
/// Deletes an insertion order. Returns error code NOT_FOUND if the insertion order does not exist. The insertion order should be archived first, i.e. set entity_status to ENTITY_STATUS_ARCHIVED, to be able to delete it.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_insertion_orders_delete_execute()` to send, or `displayvideo_advertisers_insertion_orders_delete` for simplest API.

pub fn displayvideo_advertisers_insertion_orders_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    insertionOrderId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/insertionOrders/{}",
        advertiserId, insertionOrderId,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v4/advertisers/{advertisersId}/insertionOrders/{insertionOrdersId}
/// Deletes an insertion order. Returns error code NOT_FOUND if the insertion order does not exist. The insertion order should be archived first, i.e. set entity_status to ENTITY_STATUS_ARCHIVED, to be able to delete it.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_insertion_orders_delete_execute()` or `displayvideo_advertisers_insertion_orders_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_insertion_orders_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_insertion_orders_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v4/advertisers/{advertisersId}/insertionOrders/{insertionOrdersId}
/// Deletes an insertion order. Returns error code NOT_FOUND if the insertion order does not exist. The insertion order should be archived first, i.e. set entity_status to ENTITY_STATUS_ARCHIVED, to be able to delete it.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_insertion_orders_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_insertion_orders_delete_task()`.
/// For the simplest API, use `displayvideo_advertisers_insertion_orders_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_insertion_orders_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_insertion_orders_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_insertion_orders_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_insertion_orders_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersInsertionOrdersDeleteArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: insertionOrderId
    pub insertionOrderId: String,
}

/// DELETE v4/advertisers/{advertisersId}/insertionOrders/{insertionOrdersId}
/// Deletes an insertion order. Returns error code NOT_FOUND if the insertion order does not exist. The insertion order should be archived first, i.e. set entity_status to ENTITY_STATUS_ARCHIVED, to be able to delete it.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_insertion_orders_delete_builder()` + `displayvideo_advertisers_insertion_orders_delete_execute()`.
/// For task-level control, use `displayvideo_advertisers_insertion_orders_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_insertion_orders_delete(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersInsertionOrdersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_insertion_orders_delete_builder(
        client,
        &args.advertiserId,
        &args.insertionOrderId,
    )?;
    displayvideo_advertisers_insertion_orders_delete_execute(builder)
}

/// GET v4/advertisers/{advertisersId}/insertionOrders/{insertionOrdersId}
/// Gets an insertion order. Returns error code NOT_FOUND if the insertion order does not exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_insertion_orders_get_execute()` to send, or `displayvideo_advertisers_insertion_orders_get` for simplest API.

pub fn displayvideo_advertisers_insertion_orders_get_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    insertionOrderId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/insertionOrders/{}",
        advertiserId, insertionOrderId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}/insertionOrders/{insertionOrdersId}
/// Gets an insertion order. Returns error code NOT_FOUND if the insertion order does not exist.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_insertion_orders_get_execute()` or `displayvideo_advertisers_insertion_orders_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_insertion_orders_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_insertion_orders_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InsertionOrder>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InsertionOrder = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}/insertionOrders/{insertionOrdersId}
/// Gets an insertion order. Returns error code NOT_FOUND if the insertion order does not exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_insertion_orders_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_insertion_orders_get_task()`.
/// For the simplest API, use `displayvideo_advertisers_insertion_orders_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_insertion_orders_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_insertion_orders_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InsertionOrder>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_insertion_orders_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_insertion_orders_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersInsertionOrdersGetArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: insertionOrderId
    pub insertionOrderId: String,
}

/// GET v4/advertisers/{advertisersId}/insertionOrders/{insertionOrdersId}
/// Gets an insertion order. Returns error code NOT_FOUND if the insertion order does not exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_insertion_orders_get_builder()` + `displayvideo_advertisers_insertion_orders_get_execute()`.
/// For task-level control, use `displayvideo_advertisers_insertion_orders_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_insertion_orders_get(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersInsertionOrdersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InsertionOrder>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_insertion_orders_get_builder(
        client,
        &args.advertiserId,
        &args.insertionOrderId,
    )?;
    displayvideo_advertisers_insertion_orders_get_execute(builder)
}

/// GET v4/advertisers/{advertisersId}/insertionOrders
/// Lists insertion orders in an advertiser. The order is defined by the order_by parameter. If a filter by entity_status is not specified, insertion orders with ENTITY_STATUS_ARCHIVED will not be included in the results.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_insertion_orders_list_execute()` to send, or `displayvideo_advertisers_insertion_orders_list` for simplest API.

pub fn displayvideo_advertisers_insertion_orders_list_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/insertionOrders",
        advertiserId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}/insertionOrders
/// Lists insertion orders in an advertiser. The order is defined by the order_by parameter. If a filter by entity_status is not specified, insertion orders with ENTITY_STATUS_ARCHIVED will not be included in the results.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_insertion_orders_list_execute()` or `displayvideo_advertisers_insertion_orders_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_insertion_orders_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_insertion_orders_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListInsertionOrdersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListInsertionOrdersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}/insertionOrders
/// Lists insertion orders in an advertiser. The order is defined by the order_by parameter. If a filter by entity_status is not specified, insertion orders with ENTITY_STATUS_ARCHIVED will not be included in the results.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_insertion_orders_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_insertion_orders_list_task()`.
/// For the simplest API, use `displayvideo_advertisers_insertion_orders_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_insertion_orders_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_insertion_orders_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListInsertionOrdersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_insertion_orders_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_insertion_orders_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersInsertionOrdersListArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v4/advertisers/{advertisersId}/insertionOrders
/// Lists insertion orders in an advertiser. The order is defined by the order_by parameter. If a filter by entity_status is not specified, insertion orders with ENTITY_STATUS_ARCHIVED will not be included in the results.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_insertion_orders_list_builder()` + `displayvideo_advertisers_insertion_orders_list_execute()`.
/// For task-level control, use `displayvideo_advertisers_insertion_orders_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_insertion_orders_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersInsertionOrdersListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListInsertionOrdersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_insertion_orders_list_builder(
        client,
        &args.advertiserId,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    displayvideo_advertisers_insertion_orders_list_execute(builder)
}

/// PATCH v4/advertisers/{advertisersId}/insertionOrders/{insertionOrdersId}
/// Updates an existing insertion order. Returns the updated insertion order if successful.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_insertion_orders_patch_execute()` to send, or `displayvideo_advertisers_insertion_orders_patch` for simplest API.

pub fn displayvideo_advertisers_insertion_orders_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    insertionOrderId: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/insertionOrders/{}",
        advertiserId, insertionOrderId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v4/advertisers/{advertisersId}/insertionOrders/{insertionOrdersId}
/// Updates an existing insertion order. Returns the updated insertion order if successful.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_insertion_orders_patch_execute()` or `displayvideo_advertisers_insertion_orders_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_insertion_orders_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_insertion_orders_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InsertionOrder>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InsertionOrder = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v4/advertisers/{advertisersId}/insertionOrders/{insertionOrdersId}
/// Updates an existing insertion order. Returns the updated insertion order if successful.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_insertion_orders_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_insertion_orders_patch_task()`.
/// For the simplest API, use `displayvideo_advertisers_insertion_orders_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_insertion_orders_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_insertion_orders_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InsertionOrder>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_insertion_orders_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_insertion_orders_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersInsertionOrdersPatchArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: insertionOrderId
    pub insertionOrderId: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v4/advertisers/{advertisersId}/insertionOrders/{insertionOrdersId}
/// Updates an existing insertion order. Returns the updated insertion order if successful.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_insertion_orders_patch_builder()` + `displayvideo_advertisers_insertion_orders_patch_execute()`.
/// For task-level control, use `displayvideo_advertisers_insertion_orders_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_insertion_orders_patch(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersInsertionOrdersPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InsertionOrder>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_insertion_orders_patch_builder(
        client,
        &args.advertiserId,
        &args.insertionOrderId,
        &args.updateMask,
    )?;
    displayvideo_advertisers_insertion_orders_patch_execute(builder)
}

/// GET v4/advertisers/{advertisersId}/invoices
/// Lists invoices posted for an advertiser in a given month. Invoices generated by billing profiles with a "Partner" invoice level are not retrievable through this method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_invoices_list_execute()` to send, or `displayvideo_advertisers_invoices_list` for simplest API.

pub fn displayvideo_advertisers_invoices_list_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    issueMonth: &Option<String>,
    loiSapinInvoiceType: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/invoices",
        advertiserId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = issueMonth.as_ref() {
        query_parts.push(format!("issueMonth={}", val));
    }
    if let Some(val) = loiSapinInvoiceType.as_ref() {
        query_parts.push(format!("loiSapinInvoiceType={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}/invoices
/// Lists invoices posted for an advertiser in a given month. Invoices generated by billing profiles with a "Partner" invoice level are not retrievable through this method.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_invoices_list_execute()` or `displayvideo_advertisers_invoices_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_invoices_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_invoices_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListInvoicesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListInvoicesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}/invoices
/// Lists invoices posted for an advertiser in a given month. Invoices generated by billing profiles with a "Partner" invoice level are not retrievable through this method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_invoices_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_invoices_list_task()`.
/// For the simplest API, use `displayvideo_advertisers_invoices_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_invoices_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_invoices_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListInvoicesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_invoices_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_invoices_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersInvoicesListArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Query parameter: issueMonth
    pub issueMonth: Option<String>,
    /// Query parameter: loiSapinInvoiceType
    pub loiSapinInvoiceType: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v4/advertisers/{advertisersId}/invoices
/// Lists invoices posted for an advertiser in a given month. Invoices generated by billing profiles with a "Partner" invoice level are not retrievable through this method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_invoices_list_builder()` + `displayvideo_advertisers_invoices_list_execute()`.
/// For task-level control, use `displayvideo_advertisers_invoices_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_invoices_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersInvoicesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListInvoicesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_invoices_list_builder(
        client,
        &args.advertiserId,
        &args.issueMonth,
        &args.loiSapinInvoiceType,
        &args.pageSize,
        &args.pageToken,
    )?;
    displayvideo_advertisers_invoices_list_execute(builder)
}

/// GET v4/advertisers/{advertisersId}/invoices:lookupInvoiceCurrency
/// Retrieves the invoice currency used by an advertiser in a given month.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_invoices_lookup_invoice_currency_execute()` to send, or `displayvideo_advertisers_invoices_lookup_invoice_currency` for simplest API.

pub fn displayvideo_advertisers_invoices_lookup_invoice_currency_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    invoiceMonth: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/invoices:lookupInvoiceCurrency",
        advertiserId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = invoiceMonth.as_ref() {
        query_parts.push(format!("invoiceMonth={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}/invoices:lookupInvoiceCurrency
/// Retrieves the invoice currency used by an advertiser in a given month.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_invoices_lookup_invoice_currency_execute()` or `displayvideo_advertisers_invoices_lookup_invoice_currency`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_invoices_lookup_invoice_currency_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_invoices_lookup_invoice_currency_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LookupInvoiceCurrencyResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LookupInvoiceCurrencyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}/invoices:lookupInvoiceCurrency
/// Retrieves the invoice currency used by an advertiser in a given month.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_invoices_lookup_invoice_currency_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_invoices_lookup_invoice_currency_task()`.
/// For the simplest API, use `displayvideo_advertisers_invoices_lookup_invoice_currency()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_invoices_lookup_invoice_currency_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_invoices_lookup_invoice_currency_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LookupInvoiceCurrencyResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_invoices_lookup_invoice_currency_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_invoices_lookup_invoice_currency`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersInvoicesLookupInvoiceCurrencyArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Query parameter: invoiceMonth
    pub invoiceMonth: Option<String>,
}

/// GET v4/advertisers/{advertisersId}/invoices:lookupInvoiceCurrency
/// Retrieves the invoice currency used by an advertiser in a given month.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_invoices_lookup_invoice_currency_builder()` + `displayvideo_advertisers_invoices_lookup_invoice_currency_execute()`.
/// For task-level control, use `displayvideo_advertisers_invoices_lookup_invoice_currency_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_invoices_lookup_invoice_currency(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersInvoicesLookupInvoiceCurrencyArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LookupInvoiceCurrencyResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_invoices_lookup_invoice_currency_builder(
        client,
        &args.advertiserId,
        &args.invoiceMonth,
    )?;
    displayvideo_advertisers_invoices_lookup_invoice_currency_execute(builder)
}

/// POST v4/advertisers/{advertisersId}/lineItems:bulkEditAssignedTargetingOptions
/// Bulk edits targeting options under multiple line items. The operation will delete the assigned targeting options provided in BulkEditAssignedTargetingOptionsRequest.delete_requests and then create the assigned targeting options provided in BulkEditAssignedTargetingOptionsRequest.create_requests. Requests to this endpoint cannot be made concurrently with the following requests updating the same line item: * `lineItems`.`bulkUpdate` * `lineItems`.patch * `assignedTargetingOptions`.create * `assignedTargetingOptions`.delete YouTube & Partners line items cannot be created or updated using the API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_line_items_bulk_edit_assigned_targeting_options_execute()` to send, or `displayvideo_advertisers_line_items_bulk_edit_assigned_targeting_options` for simplest API.

pub fn displayvideo_advertisers_line_items_bulk_edit_assigned_targeting_options_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/lineItems:bulkEditAssignedTargetingOptions",
        advertiserId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/advertisers/{advertisersId}/lineItems:bulkEditAssignedTargetingOptions
/// Bulk edits targeting options under multiple line items. The operation will delete the assigned targeting options provided in BulkEditAssignedTargetingOptionsRequest.delete_requests and then create the assigned targeting options provided in BulkEditAssignedTargetingOptionsRequest.create_requests. Requests to this endpoint cannot be made concurrently with the following requests updating the same line item: * `lineItems`.`bulkUpdate` * `lineItems`.patch * `assignedTargetingOptions`.create * `assignedTargetingOptions`.delete YouTube & Partners line items cannot be created or updated using the API.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_line_items_bulk_edit_assigned_targeting_options_execute()` or `displayvideo_advertisers_line_items_bulk_edit_assigned_targeting_options`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_line_items_bulk_edit_assigned_targeting_options_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_line_items_bulk_edit_assigned_targeting_options_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BulkEditAssignedTargetingOptionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BulkEditAssignedTargetingOptionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/advertisers/{advertisersId}/lineItems:bulkEditAssignedTargetingOptions
/// Bulk edits targeting options under multiple line items. The operation will delete the assigned targeting options provided in BulkEditAssignedTargetingOptionsRequest.delete_requests and then create the assigned targeting options provided in BulkEditAssignedTargetingOptionsRequest.create_requests. Requests to this endpoint cannot be made concurrently with the following requests updating the same line item: * `lineItems`.`bulkUpdate` * `lineItems`.patch * `assignedTargetingOptions`.create * `assignedTargetingOptions`.delete YouTube & Partners line items cannot be created or updated using the API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_line_items_bulk_edit_assigned_targeting_options_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_line_items_bulk_edit_assigned_targeting_options_task()`.
/// For the simplest API, use `displayvideo_advertisers_line_items_bulk_edit_assigned_targeting_options()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_line_items_bulk_edit_assigned_targeting_options_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_line_items_bulk_edit_assigned_targeting_options_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BulkEditAssignedTargetingOptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        displayvideo_advertisers_line_items_bulk_edit_assigned_targeting_options_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_line_items_bulk_edit_assigned_targeting_options`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersLineItemsBulkEditAssignedTargetingOptionsArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
}

/// POST v4/advertisers/{advertisersId}/lineItems:bulkEditAssignedTargetingOptions
/// Bulk edits targeting options under multiple line items. The operation will delete the assigned targeting options provided in BulkEditAssignedTargetingOptionsRequest.delete_requests and then create the assigned targeting options provided in BulkEditAssignedTargetingOptionsRequest.create_requests. Requests to this endpoint cannot be made concurrently with the following requests updating the same line item: * `lineItems`.`bulkUpdate` * `lineItems`.patch * `assignedTargetingOptions`.create * `assignedTargetingOptions`.delete YouTube & Partners line items cannot be created or updated using the API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_line_items_bulk_edit_assigned_targeting_options_builder()` + `displayvideo_advertisers_line_items_bulk_edit_assigned_targeting_options_execute()`.
/// For task-level control, use `displayvideo_advertisers_line_items_bulk_edit_assigned_targeting_options_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_line_items_bulk_edit_assigned_targeting_options(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersLineItemsBulkEditAssignedTargetingOptionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BulkEditAssignedTargetingOptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_line_items_bulk_edit_assigned_targeting_options_builder(
        client,
        &args.advertiserId,
    )?;
    displayvideo_advertisers_line_items_bulk_edit_assigned_targeting_options_execute(builder)
}

/// GET v4/advertisers/{advertisersId}/lineItems:bulkListAssignedTargetingOptions
/// Lists assigned targeting options for multiple line items across targeting types.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_line_items_bulk_list_assigned_targeting_options_execute()` to send, or `displayvideo_advertisers_line_items_bulk_list_assigned_targeting_options` for simplest API.

pub fn displayvideo_advertisers_line_items_bulk_list_assigned_targeting_options_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    filter: &Option<String>,
    lineItemIds: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/lineItems:bulkListAssignedTargetingOptions",
        advertiserId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = lineItemIds.as_ref() {
        query_parts.push(format!("lineItemIds={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}/lineItems:bulkListAssignedTargetingOptions
/// Lists assigned targeting options for multiple line items across targeting types.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_line_items_bulk_list_assigned_targeting_options_execute()` or `displayvideo_advertisers_line_items_bulk_list_assigned_targeting_options`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_line_items_bulk_list_assigned_targeting_options_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_line_items_bulk_list_assigned_targeting_options_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BulkListAssignedTargetingOptionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BulkListAssignedTargetingOptionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}/lineItems:bulkListAssignedTargetingOptions
/// Lists assigned targeting options for multiple line items across targeting types.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_line_items_bulk_list_assigned_targeting_options_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_line_items_bulk_list_assigned_targeting_options_task()`.
/// For the simplest API, use `displayvideo_advertisers_line_items_bulk_list_assigned_targeting_options()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_line_items_bulk_list_assigned_targeting_options_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_line_items_bulk_list_assigned_targeting_options_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BulkListAssignedTargetingOptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        displayvideo_advertisers_line_items_bulk_list_assigned_targeting_options_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_line_items_bulk_list_assigned_targeting_options`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersLineItemsBulkListAssignedTargetingOptionsArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: lineItemIds
    pub lineItemIds: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v4/advertisers/{advertisersId}/lineItems:bulkListAssignedTargetingOptions
/// Lists assigned targeting options for multiple line items across targeting types.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_line_items_bulk_list_assigned_targeting_options_builder()` + `displayvideo_advertisers_line_items_bulk_list_assigned_targeting_options_execute()`.
/// For task-level control, use `displayvideo_advertisers_line_items_bulk_list_assigned_targeting_options_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_line_items_bulk_list_assigned_targeting_options(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersLineItemsBulkListAssignedTargetingOptionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BulkListAssignedTargetingOptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_line_items_bulk_list_assigned_targeting_options_builder(
        client,
        &args.advertiserId,
        &args.filter,
        &args.lineItemIds,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    displayvideo_advertisers_line_items_bulk_list_assigned_targeting_options_execute(builder)
}

/// POST v4/advertisers/{advertisersId}/lineItems:bulkUpdate
/// Updates multiple line items. Requests to this endpoint cannot be made concurrently with the following requests updating the same line item: * BulkEditAssignedTargetingOptions * UpdateLineItem * `assignedTargetingOptions`.create * `assignedTargetingOptions`.delete YouTube & Partners line items cannot be created or updated using the API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_line_items_bulk_update_execute()` to send, or `displayvideo_advertisers_line_items_bulk_update` for simplest API.

pub fn displayvideo_advertisers_line_items_bulk_update_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/lineItems:bulkUpdate",
        advertiserId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/advertisers/{advertisersId}/lineItems:bulkUpdate
/// Updates multiple line items. Requests to this endpoint cannot be made concurrently with the following requests updating the same line item: * BulkEditAssignedTargetingOptions * UpdateLineItem * `assignedTargetingOptions`.create * `assignedTargetingOptions`.delete YouTube & Partners line items cannot be created or updated using the API.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_line_items_bulk_update_execute()` or `displayvideo_advertisers_line_items_bulk_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_line_items_bulk_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_line_items_bulk_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BulkUpdateLineItemsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BulkUpdateLineItemsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/advertisers/{advertisersId}/lineItems:bulkUpdate
/// Updates multiple line items. Requests to this endpoint cannot be made concurrently with the following requests updating the same line item: * BulkEditAssignedTargetingOptions * UpdateLineItem * `assignedTargetingOptions`.create * `assignedTargetingOptions`.delete YouTube & Partners line items cannot be created or updated using the API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_line_items_bulk_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_line_items_bulk_update_task()`.
/// For the simplest API, use `displayvideo_advertisers_line_items_bulk_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_line_items_bulk_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_line_items_bulk_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BulkUpdateLineItemsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_line_items_bulk_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_line_items_bulk_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersLineItemsBulkUpdateArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
}

/// POST v4/advertisers/{advertisersId}/lineItems:bulkUpdate
/// Updates multiple line items. Requests to this endpoint cannot be made concurrently with the following requests updating the same line item: * BulkEditAssignedTargetingOptions * UpdateLineItem * `assignedTargetingOptions`.create * `assignedTargetingOptions`.delete YouTube & Partners line items cannot be created or updated using the API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_line_items_bulk_update_builder()` + `displayvideo_advertisers_line_items_bulk_update_execute()`.
/// For task-level control, use `displayvideo_advertisers_line_items_bulk_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_line_items_bulk_update(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersLineItemsBulkUpdateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BulkUpdateLineItemsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        displayvideo_advertisers_line_items_bulk_update_builder(client, &args.advertiserId)?;
    displayvideo_advertisers_line_items_bulk_update_execute(builder)
}

/// POST v4/advertisers/{advertisersId}/lineItems
/// Creates a new line item. Returns the newly created line item if successful. YouTube & Partners line items cannot be created or updated using the API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_line_items_create_execute()` to send, or `displayvideo_advertisers_line_items_create` for simplest API.

pub fn displayvideo_advertisers_line_items_create_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/lineItems",
        advertiserId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/advertisers/{advertisersId}/lineItems
/// Creates a new line item. Returns the newly created line item if successful. YouTube & Partners line items cannot be created or updated using the API.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_line_items_create_execute()` or `displayvideo_advertisers_line_items_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_line_items_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_line_items_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LineItem>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LineItem = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/advertisers/{advertisersId}/lineItems
/// Creates a new line item. Returns the newly created line item if successful. YouTube & Partners line items cannot be created or updated using the API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_line_items_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_line_items_create_task()`.
/// For the simplest API, use `displayvideo_advertisers_line_items_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_line_items_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_line_items_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LineItem>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_line_items_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_line_items_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersLineItemsCreateArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
}

/// POST v4/advertisers/{advertisersId}/lineItems
/// Creates a new line item. Returns the newly created line item if successful. YouTube & Partners line items cannot be created or updated using the API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_line_items_create_builder()` + `displayvideo_advertisers_line_items_create_execute()`.
/// For task-level control, use `displayvideo_advertisers_line_items_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_line_items_create(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersLineItemsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LineItem>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_line_items_create_builder(client, &args.advertiserId)?;
    displayvideo_advertisers_line_items_create_execute(builder)
}

/// DELETE v4/advertisers/{advertisersId}/lineItems/{lineItemsId}
/// Deletes a line item. Returns error code NOT_FOUND if the line item does not exist. The line item should be archived first, i.e. set entity_status to ENTITY_STATUS_ARCHIVED, to be able to delete it. YouTube & Partners line items cannot be created or updated using the API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_line_items_delete_execute()` to send, or `displayvideo_advertisers_line_items_delete` for simplest API.

pub fn displayvideo_advertisers_line_items_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    lineItemId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/lineItems/{}",
        advertiserId, lineItemId,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v4/advertisers/{advertisersId}/lineItems/{lineItemsId}
/// Deletes a line item. Returns error code NOT_FOUND if the line item does not exist. The line item should be archived first, i.e. set entity_status to ENTITY_STATUS_ARCHIVED, to be able to delete it. YouTube & Partners line items cannot be created or updated using the API.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_line_items_delete_execute()` or `displayvideo_advertisers_line_items_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_line_items_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_line_items_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v4/advertisers/{advertisersId}/lineItems/{lineItemsId}
/// Deletes a line item. Returns error code NOT_FOUND if the line item does not exist. The line item should be archived first, i.e. set entity_status to ENTITY_STATUS_ARCHIVED, to be able to delete it. YouTube & Partners line items cannot be created or updated using the API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_line_items_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_line_items_delete_task()`.
/// For the simplest API, use `displayvideo_advertisers_line_items_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_line_items_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_line_items_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_line_items_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_line_items_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersLineItemsDeleteArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: lineItemId
    pub lineItemId: String,
}

/// DELETE v4/advertisers/{advertisersId}/lineItems/{lineItemsId}
/// Deletes a line item. Returns error code NOT_FOUND if the line item does not exist. The line item should be archived first, i.e. set entity_status to ENTITY_STATUS_ARCHIVED, to be able to delete it. YouTube & Partners line items cannot be created or updated using the API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_line_items_delete_builder()` + `displayvideo_advertisers_line_items_delete_execute()`.
/// For task-level control, use `displayvideo_advertisers_line_items_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_line_items_delete(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersLineItemsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_line_items_delete_builder(
        client,
        &args.advertiserId,
        &args.lineItemId,
    )?;
    displayvideo_advertisers_line_items_delete_execute(builder)
}

/// POST v4/advertisers/{advertisersId}/lineItems/{lineItemsId}:duplicate
/// Duplicates a line item. Returns the ID of the created line item if successful. YouTube & Partners line items cannot be created or updated using the API. **This method regularly experiences high latency.** We recommend [increasing your default timeout](/display-`video/api/guides/best-practices/timeouts`#client_library_timeout) to avoid errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_line_items_duplicate_execute()` to send, or `displayvideo_advertisers_line_items_duplicate` for simplest API.

pub fn displayvideo_advertisers_line_items_duplicate_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    lineItemId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/lineItems/{}:duplicate",
        advertiserId, lineItemId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/advertisers/{advertisersId}/lineItems/{lineItemsId}:duplicate
/// Duplicates a line item. Returns the ID of the created line item if successful. YouTube & Partners line items cannot be created or updated using the API. **This method regularly experiences high latency.** We recommend [increasing your default timeout](/display-`video/api/guides/best-practices/timeouts`#client_library_timeout) to avoid errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_line_items_duplicate_execute()` or `displayvideo_advertisers_line_items_duplicate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_line_items_duplicate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_line_items_duplicate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DuplicateLineItemResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DuplicateLineItemResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/advertisers/{advertisersId}/lineItems/{lineItemsId}:duplicate
/// Duplicates a line item. Returns the ID of the created line item if successful. YouTube & Partners line items cannot be created or updated using the API. **This method regularly experiences high latency.** We recommend [increasing your default timeout](/display-`video/api/guides/best-practices/timeouts`#client_library_timeout) to avoid errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_line_items_duplicate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_line_items_duplicate_task()`.
/// For the simplest API, use `displayvideo_advertisers_line_items_duplicate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_line_items_duplicate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_line_items_duplicate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DuplicateLineItemResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_line_items_duplicate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_line_items_duplicate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersLineItemsDuplicateArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: lineItemId
    pub lineItemId: String,
}

/// POST v4/advertisers/{advertisersId}/lineItems/{lineItemsId}:duplicate
/// Duplicates a line item. Returns the ID of the created line item if successful. YouTube & Partners line items cannot be created or updated using the API. **This method regularly experiences high latency.** We recommend [increasing your default timeout](/display-`video/api/guides/best-practices/timeouts`#client_library_timeout) to avoid errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_line_items_duplicate_builder()` + `displayvideo_advertisers_line_items_duplicate_execute()`.
/// For task-level control, use `displayvideo_advertisers_line_items_duplicate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_line_items_duplicate(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersLineItemsDuplicateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DuplicateLineItemResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_line_items_duplicate_builder(
        client,
        &args.advertiserId,
        &args.lineItemId,
    )?;
    displayvideo_advertisers_line_items_duplicate_execute(builder)
}

/// GET v4/advertisers/{advertisersId}/lineItems/{lineItemsId}
/// Gets a line item.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_line_items_get_execute()` to send, or `displayvideo_advertisers_line_items_get` for simplest API.

pub fn displayvideo_advertisers_line_items_get_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    lineItemId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/lineItems/{}",
        advertiserId, lineItemId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}/lineItems/{lineItemsId}
/// Gets a line item.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_line_items_get_execute()` or `displayvideo_advertisers_line_items_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_line_items_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_line_items_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LineItem>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LineItem = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}/lineItems/{lineItemsId}
/// Gets a line item.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_line_items_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_line_items_get_task()`.
/// For the simplest API, use `displayvideo_advertisers_line_items_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_line_items_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_line_items_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LineItem>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_line_items_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_line_items_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersLineItemsGetArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: lineItemId
    pub lineItemId: String,
}

/// GET v4/advertisers/{advertisersId}/lineItems/{lineItemsId}
/// Gets a line item.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_line_items_get_builder()` + `displayvideo_advertisers_line_items_get_execute()`.
/// For task-level control, use `displayvideo_advertisers_line_items_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_line_items_get(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersLineItemsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LineItem>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_line_items_get_builder(
        client,
        &args.advertiserId,
        &args.lineItemId,
    )?;
    displayvideo_advertisers_line_items_get_execute(builder)
}

/// GET v4/advertisers/{advertisersId}/lineItems
/// Lists line items in an advertiser. The order is defined by the order_by parameter. If a filter by entity_status is not specified, line items with ENTITY_STATUS_ARCHIVED will not be included in the results.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_line_items_list_execute()` to send, or `displayvideo_advertisers_line_items_list` for simplest API.

pub fn displayvideo_advertisers_line_items_list_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/lineItems",
        advertiserId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}/lineItems
/// Lists line items in an advertiser. The order is defined by the order_by parameter. If a filter by entity_status is not specified, line items with ENTITY_STATUS_ARCHIVED will not be included in the results.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_line_items_list_execute()` or `displayvideo_advertisers_line_items_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_line_items_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_line_items_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLineItemsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLineItemsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}/lineItems
/// Lists line items in an advertiser. The order is defined by the order_by parameter. If a filter by entity_status is not specified, line items with ENTITY_STATUS_ARCHIVED will not be included in the results.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_line_items_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_line_items_list_task()`.
/// For the simplest API, use `displayvideo_advertisers_line_items_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_line_items_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_line_items_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLineItemsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_line_items_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_line_items_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersLineItemsListArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v4/advertisers/{advertisersId}/lineItems
/// Lists line items in an advertiser. The order is defined by the order_by parameter. If a filter by entity_status is not specified, line items with ENTITY_STATUS_ARCHIVED will not be included in the results.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_line_items_list_builder()` + `displayvideo_advertisers_line_items_list_execute()`.
/// For task-level control, use `displayvideo_advertisers_line_items_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_line_items_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersLineItemsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLineItemsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_line_items_list_builder(
        client,
        &args.advertiserId,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    displayvideo_advertisers_line_items_list_execute(builder)
}

/// PATCH v4/advertisers/{advertisersId}/lineItems/{lineItemsId}
/// Updates an existing line item. Returns the updated line item if successful. Requests to this endpoint cannot be made concurrently with the following requests updating the same line item: * BulkEditAssignedTargetingOptions * BulkUpdateLineItems * `assignedTargetingOptions`.create * `assignedTargetingOptions`.delete YouTube & Partners line items cannot be created or updated using the API. **This method regularly experiences high latency.** We recommend [increasing your default timeout](/display-`video/api/guides/best-practices/timeouts`#client_library_timeout) to avoid errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_line_items_patch_execute()` to send, or `displayvideo_advertisers_line_items_patch` for simplest API.

pub fn displayvideo_advertisers_line_items_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    lineItemId: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/lineItems/{}",
        advertiserId, lineItemId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v4/advertisers/{advertisersId}/lineItems/{lineItemsId}
/// Updates an existing line item. Returns the updated line item if successful. Requests to this endpoint cannot be made concurrently with the following requests updating the same line item: * BulkEditAssignedTargetingOptions * BulkUpdateLineItems * `assignedTargetingOptions`.create * `assignedTargetingOptions`.delete YouTube & Partners line items cannot be created or updated using the API. **This method regularly experiences high latency.** We recommend [increasing your default timeout](/display-`video/api/guides/best-practices/timeouts`#client_library_timeout) to avoid errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_line_items_patch_execute()` or `displayvideo_advertisers_line_items_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_line_items_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_line_items_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LineItem>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LineItem = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v4/advertisers/{advertisersId}/lineItems/{lineItemsId}
/// Updates an existing line item. Returns the updated line item if successful. Requests to this endpoint cannot be made concurrently with the following requests updating the same line item: * BulkEditAssignedTargetingOptions * BulkUpdateLineItems * `assignedTargetingOptions`.create * `assignedTargetingOptions`.delete YouTube & Partners line items cannot be created or updated using the API. **This method regularly experiences high latency.** We recommend [increasing your default timeout](/display-`video/api/guides/best-practices/timeouts`#client_library_timeout) to avoid errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_line_items_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_line_items_patch_task()`.
/// For the simplest API, use `displayvideo_advertisers_line_items_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_line_items_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_line_items_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LineItem>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_line_items_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_line_items_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersLineItemsPatchArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: lineItemId
    pub lineItemId: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v4/advertisers/{advertisersId}/lineItems/{lineItemsId}
/// Updates an existing line item. Returns the updated line item if successful. Requests to this endpoint cannot be made concurrently with the following requests updating the same line item: * BulkEditAssignedTargetingOptions * BulkUpdateLineItems * `assignedTargetingOptions`.create * `assignedTargetingOptions`.delete YouTube & Partners line items cannot be created or updated using the API. **This method regularly experiences high latency.** We recommend [increasing your default timeout](/display-`video/api/guides/best-practices/timeouts`#client_library_timeout) to avoid errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_line_items_patch_builder()` + `displayvideo_advertisers_line_items_patch_execute()`.
/// For task-level control, use `displayvideo_advertisers_line_items_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_line_items_patch(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersLineItemsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LineItem>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_line_items_patch_builder(
        client,
        &args.advertiserId,
        &args.lineItemId,
        &args.updateMask,
    )?;
    displayvideo_advertisers_line_items_patch_execute(builder)
}

/// POST v4/advertisers/{advertisersId}/lineItems/{lineItemsId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions
/// Assigns a targeting option to a line item. Returns the assigned targeting option if successful. Requests to this endpoint cannot be made concurrently with the following requests updating the same line item: * `lineItems`.`bulkEditAssignedTargetingOptions` * `lineItems`.`bulkUpdate` * `lineItems`.patch * DeleteLineItemAssignedTargetingOption YouTube & Partners line items cannot be created or updated using the API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_create_execute()` to send, or `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_create` for simplest API.

pub fn displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_create_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    lineItemId: &String,
    targetingType: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/lineItems/{}/targetingTypes/{}/assignedTargetingOptions",
        advertiserId,
        lineItemId,
        targetingType,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/advertisers/{advertisersId}/lineItems/{lineItemsId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions
/// Assigns a targeting option to a line item. Returns the assigned targeting option if successful. Requests to this endpoint cannot be made concurrently with the following requests updating the same line item: * `lineItems`.`bulkEditAssignedTargetingOptions` * `lineItems`.`bulkUpdate` * `lineItems`.patch * DeleteLineItemAssignedTargetingOption YouTube & Partners line items cannot be created or updated using the API.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_create_execute()` or `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AssignedTargetingOption>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AssignedTargetingOption = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/advertisers/{advertisersId}/lineItems/{lineItemsId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions
/// Assigns a targeting option to a line item. Returns the assigned targeting option if successful. Requests to this endpoint cannot be made concurrently with the following requests updating the same line item: * `lineItems`.`bulkEditAssignedTargetingOptions` * `lineItems`.`bulkUpdate` * `lineItems`.patch * DeleteLineItemAssignedTargetingOption YouTube & Partners line items cannot be created or updated using the API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_create_task()`.
/// For the simplest API, use `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AssignedTargetingOption>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_create_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersLineItemsTargetingTypesAssignedTargetingOptionsCreateArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: lineItemId
    pub lineItemId: String,
    /// Path parameter: targetingType
    pub targetingType: String,
}

/// POST v4/advertisers/{advertisersId}/lineItems/{lineItemsId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions
/// Assigns a targeting option to a line item. Returns the assigned targeting option if successful. Requests to this endpoint cannot be made concurrently with the following requests updating the same line item: * `lineItems`.`bulkEditAssignedTargetingOptions` * `lineItems`.`bulkUpdate` * `lineItems`.patch * DeleteLineItemAssignedTargetingOption YouTube & Partners line items cannot be created or updated using the API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_create_builder()` + `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_create_execute()`.
/// For task-level control, use `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_create(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersLineItemsTargetingTypesAssignedTargetingOptionsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AssignedTargetingOption>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_create_builder(client, &args.advertiserId, &args.lineItemId, &args.targetingType)?;
    displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_create_execute(
        builder,
    )
}

/// DELETE v4/advertisers/{advertisersId}/lineItems/{lineItemsId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions/{assignedTargetingOptionsId}
/// Deletes an assigned targeting option from a line item. Requests to this endpoint cannot be made concurrently with the following requests updating the same line item: * `lineItems`.`bulkEditAssignedTargetingOptions` * `lineItems`.`bulkUpdate` * `lineItems`.patch * CreateLineItemAssignedTargetingOption YouTube & Partners line items cannot be created or updated using the API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_delete_execute()` to send, or `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_delete` for simplest API.

pub fn displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_delete_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    lineItemId: &String,
    targetingType: &String,
    assignedTargetingOptionId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/lineItems/{}/targetingTypes/{}/assignedTargetingOptions/{}",
        advertiserId,
        lineItemId,
        targetingType,
        assignedTargetingOptionId,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v4/advertisers/{advertisersId}/lineItems/{lineItemsId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions/{assignedTargetingOptionsId}
/// Deletes an assigned targeting option from a line item. Requests to this endpoint cannot be made concurrently with the following requests updating the same line item: * `lineItems`.`bulkEditAssignedTargetingOptions` * `lineItems`.`bulkUpdate` * `lineItems`.patch * CreateLineItemAssignedTargetingOption YouTube & Partners line items cannot be created or updated using the API.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_delete_execute()` or `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v4/advertisers/{advertisersId}/lineItems/{lineItemsId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions/{assignedTargetingOptionsId}
/// Deletes an assigned targeting option from a line item. Requests to this endpoint cannot be made concurrently with the following requests updating the same line item: * `lineItems`.`bulkEditAssignedTargetingOptions` * `lineItems`.`bulkUpdate` * `lineItems`.patch * CreateLineItemAssignedTargetingOption YouTube & Partners line items cannot be created or updated using the API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_delete_task()`.
/// For the simplest API, use `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_delete_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersLineItemsTargetingTypesAssignedTargetingOptionsDeleteArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: lineItemId
    pub lineItemId: String,
    /// Path parameter: targetingType
    pub targetingType: String,
    /// Path parameter: assignedTargetingOptionId
    pub assignedTargetingOptionId: String,
}

/// DELETE v4/advertisers/{advertisersId}/lineItems/{lineItemsId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions/{assignedTargetingOptionsId}
/// Deletes an assigned targeting option from a line item. Requests to this endpoint cannot be made concurrently with the following requests updating the same line item: * `lineItems`.`bulkEditAssignedTargetingOptions` * `lineItems`.`bulkUpdate` * `lineItems`.patch * CreateLineItemAssignedTargetingOption YouTube & Partners line items cannot be created or updated using the API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_delete_builder()` + `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_delete_execute()`.
/// For task-level control, use `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_delete(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersLineItemsTargetingTypesAssignedTargetingOptionsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_delete_builder(client, &args.advertiserId, &args.lineItemId, &args.targetingType, &args.assignedTargetingOptionId)?;
    displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_delete_execute(
        builder,
    )
}

/// GET v4/advertisers/{advertisersId}/lineItems/{lineItemsId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions/{assignedTargetingOptionsId}
/// Gets a single targeting option assigned to a line item.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_get_execute()` to send, or `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_get` for simplest API.

pub fn displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_get_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    lineItemId: &String,
    targetingType: &String,
    assignedTargetingOptionId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/lineItems/{}/targetingTypes/{}/assignedTargetingOptions/{}",
        advertiserId,
        lineItemId,
        targetingType,
        assignedTargetingOptionId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}/lineItems/{lineItemsId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions/{assignedTargetingOptionsId}
/// Gets a single targeting option assigned to a line item.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_get_execute()` or `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AssignedTargetingOption>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AssignedTargetingOption = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}/lineItems/{lineItemsId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions/{assignedTargetingOptionsId}
/// Gets a single targeting option assigned to a line item.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_get_task()`.
/// For the simplest API, use `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AssignedTargetingOption>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_get_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersLineItemsTargetingTypesAssignedTargetingOptionsGetArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: lineItemId
    pub lineItemId: String,
    /// Path parameter: targetingType
    pub targetingType: String,
    /// Path parameter: assignedTargetingOptionId
    pub assignedTargetingOptionId: String,
}

/// GET v4/advertisers/{advertisersId}/lineItems/{lineItemsId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions/{assignedTargetingOptionsId}
/// Gets a single targeting option assigned to a line item.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_get_builder()` + `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_get_execute()`.
/// For task-level control, use `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_get(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersLineItemsTargetingTypesAssignedTargetingOptionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AssignedTargetingOption>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_get_builder(
            client,
            &args.advertiserId,
            &args.lineItemId,
            &args.targetingType,
            &args.assignedTargetingOptionId,
        )?;
    displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_get_execute(
        builder,
    )
}

/// GET v4/advertisers/{advertisersId}/lineItems/{lineItemsId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions
/// Lists the targeting options assigned to a line item.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_list_execute()` to send, or `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_list` for simplest API.

pub fn displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_list_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    lineItemId: &String,
    targetingType: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/lineItems/{}/targetingTypes/{}/assignedTargetingOptions",
        advertiserId,
        lineItemId,
        targetingType,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}/lineItems/{lineItemsId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions
/// Lists the targeting options assigned to a line item.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_list_execute()` or `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLineItemAssignedTargetingOptionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLineItemAssignedTargetingOptionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}/lineItems/{lineItemsId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions
/// Lists the targeting options assigned to a line item.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_list_task()`.
/// For the simplest API, use `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListLineItemAssignedTargetingOptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_list_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersLineItemsTargetingTypesAssignedTargetingOptionsListArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: lineItemId
    pub lineItemId: String,
    /// Path parameter: targetingType
    pub targetingType: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v4/advertisers/{advertisersId}/lineItems/{lineItemsId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions
/// Lists the targeting options assigned to a line item.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_list_builder()` + `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_list_execute()`.
/// For task-level control, use `displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersLineItemsTargetingTypesAssignedTargetingOptionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListLineItemAssignedTargetingOptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_list_builder(client, &args.advertiserId, &args.lineItemId, &args.targetingType, &args.filter, &args.orderBy, &args.pageSize, &args.pageToken)?;
    displayvideo_advertisers_line_items_targeting_types_assigned_targeting_options_list_execute(
        builder,
    )
}

/// POST v4/advertisers/{advertisersId}/lineItems/{lineItemsId}/youtubeAssetTypes/{youtubeAssetTypesId}/youtubeAssetAssociations
/// Creates a new association between the identified resource and a YouTube asset. Returns the newly-created association. *Warning:* This method is only available to an informed subset of users.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_create_execute()` to send, or `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_create` for simplest API.

pub fn displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_create_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    lineItemId: &String,
    youtubeAssetType: &String,
    linkedEntity_adGroupId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/lineItems/{}/youtubeAssetTypes/{}/youtubeAssetAssociations",
        advertiserId,
        lineItemId,
        youtubeAssetType,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = linkedEntity_adGroupId.as_ref() {
        query_parts.push(format!("linkedEntity.adGroupId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/advertisers/{advertisersId}/lineItems/{lineItemsId}/youtubeAssetTypes/{youtubeAssetTypesId}/youtubeAssetAssociations
/// Creates a new association between the identified resource and a YouTube asset. Returns the newly-created association. *Warning:* This method is only available to an informed subset of users.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_create_execute()` or `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<YoutubeAssetAssociation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: YoutubeAssetAssociation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/advertisers/{advertisersId}/lineItems/{lineItemsId}/youtubeAssetTypes/{youtubeAssetTypesId}/youtubeAssetAssociations
/// Creates a new association between the identified resource and a YouTube asset. Returns the newly-created association. *Warning:* This method is only available to an informed subset of users.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_create_task()`.
/// For the simplest API, use `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<YoutubeAssetAssociation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersLineItemsYoutubeAssetTypesYoutubeAssetAssociationsCreateArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: lineItemId
    pub lineItemId: String,
    /// Path parameter: youtubeAssetType
    pub youtubeAssetType: String,
    /// Query parameter: linkedEntity_adGroupId
    pub linkedEntity_adGroupId: Option<String>,
}

/// POST v4/advertisers/{advertisersId}/lineItems/{lineItemsId}/youtubeAssetTypes/{youtubeAssetTypesId}/youtubeAssetAssociations
/// Creates a new association between the identified resource and a YouTube asset. Returns the newly-created association. *Warning:* This method is only available to an informed subset of users.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_create_builder()` + `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_create_execute()`.
/// For task-level control, use `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_create(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersLineItemsYoutubeAssetTypesYoutubeAssetAssociationsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<YoutubeAssetAssociation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_create_builder(client, &args.advertiserId, &args.lineItemId, &args.youtubeAssetType, &args.linkedEntity_adGroupId)?;
    displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_create_execute(builder)
}

/// DELETE v4/advertisers/{advertisersId}/lineItems/{lineItemsId}/youtubeAssetTypes/{youtubeAssetTypesId}/youtubeAssetAssociations/{youtubeAssetAssociationsId}
/// Deletes an existing association between the identified resource and a YouTube asset. *Warning:* This method is only available to an informed subset of users.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_delete_execute()` to send, or `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_delete` for simplest API.

pub fn displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_delete_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    lineItemId: &String,
    youtubeAssetType: &String,
    youtubeAssetAssociationId: &String,
    linkedEntity_adGroupId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/lineItems/{}/youtubeAssetTypes/{}/youtubeAssetAssociations/{}",
        advertiserId,
        lineItemId,
        youtubeAssetType,
        youtubeAssetAssociationId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = linkedEntity_adGroupId.as_ref() {
        query_parts.push(format!("linkedEntity.adGroupId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v4/advertisers/{advertisersId}/lineItems/{lineItemsId}/youtubeAssetTypes/{youtubeAssetTypesId}/youtubeAssetAssociations/{youtubeAssetAssociationsId}
/// Deletes an existing association between the identified resource and a YouTube asset. *Warning:* This method is only available to an informed subset of users.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_delete_execute()` or `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v4/advertisers/{advertisersId}/lineItems/{lineItemsId}/youtubeAssetTypes/{youtubeAssetTypesId}/youtubeAssetAssociations/{youtubeAssetAssociationsId}
/// Deletes an existing association between the identified resource and a YouTube asset. *Warning:* This method is only available to an informed subset of users.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_delete_task()`.
/// For the simplest API, use `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersLineItemsYoutubeAssetTypesYoutubeAssetAssociationsDeleteArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: lineItemId
    pub lineItemId: String,
    /// Path parameter: youtubeAssetType
    pub youtubeAssetType: String,
    /// Path parameter: youtubeAssetAssociationId
    pub youtubeAssetAssociationId: String,
    /// Query parameter: linkedEntity_adGroupId
    pub linkedEntity_adGroupId: Option<String>,
}

/// DELETE v4/advertisers/{advertisersId}/lineItems/{lineItemsId}/youtubeAssetTypes/{youtubeAssetTypesId}/youtubeAssetAssociations/{youtubeAssetAssociationsId}
/// Deletes an existing association between the identified resource and a YouTube asset. *Warning:* This method is only available to an informed subset of users.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_delete_builder()` + `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_delete_execute()`.
/// For task-level control, use `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_delete(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersLineItemsYoutubeAssetTypesYoutubeAssetAssociationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_delete_builder(client, &args.advertiserId, &args.lineItemId, &args.youtubeAssetType, &args.youtubeAssetAssociationId, &args.linkedEntity_adGroupId)?;
    displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_delete_execute(builder)
}

/// GET v4/advertisers/{advertisersId}/lineItems/{lineItemsId}/youtubeAssetTypes/{youtubeAssetTypesId}/youtubeAssetAssociations
/// Lists the YouTube asset associations linked to the given resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_list_execute()` to send, or `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_list` for simplest API.

pub fn displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_list_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    lineItemId: &String,
    youtubeAssetType: &String,
    linkedEntity_adGroupId: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/lineItems/{}/youtubeAssetTypes/{}/youtubeAssetAssociations",
        advertiserId,
        lineItemId,
        youtubeAssetType,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = linkedEntity_adGroupId.as_ref() {
        query_parts.push(format!("linkedEntity.adGroupId={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}/lineItems/{lineItemsId}/youtubeAssetTypes/{youtubeAssetTypesId}/youtubeAssetAssociations
/// Lists the YouTube asset associations linked to the given resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_list_execute()` or `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListYoutubeAssetAssociationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListYoutubeAssetAssociationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}/lineItems/{lineItemsId}/youtubeAssetTypes/{youtubeAssetTypesId}/youtubeAssetAssociations
/// Lists the YouTube asset associations linked to the given resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_list_task()`.
/// For the simplest API, use `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListYoutubeAssetAssociationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersLineItemsYoutubeAssetTypesYoutubeAssetAssociationsListArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: lineItemId
    pub lineItemId: String,
    /// Path parameter: youtubeAssetType
    pub youtubeAssetType: String,
    /// Query parameter: linkedEntity_adGroupId
    pub linkedEntity_adGroupId: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v4/advertisers/{advertisersId}/lineItems/{lineItemsId}/youtubeAssetTypes/{youtubeAssetTypesId}/youtubeAssetAssociations
/// Lists the YouTube asset associations linked to the given resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_list_builder()` + `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_list_execute()`.
/// For task-level control, use `displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersLineItemsYoutubeAssetTypesYoutubeAssetAssociationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListYoutubeAssetAssociationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_list_builder(client, &args.advertiserId, &args.lineItemId, &args.youtubeAssetType, &args.linkedEntity_adGroupId, &args.orderBy, &args.pageSize, &args.pageToken)?;
    displayvideo_advertisers_line_items_youtube_asset_types_youtube_asset_associations_list_execute(
        builder,
    )
}

/// POST v4/advertisers/{advertisersId}/locationLists
/// Creates a new location list. Returns the newly created location list if successful.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_location_lists_create_execute()` to send, or `displayvideo_advertisers_location_lists_create` for simplest API.

pub fn displayvideo_advertisers_location_lists_create_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/locationLists",
        advertiserId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/advertisers/{advertisersId}/locationLists
/// Creates a new location list. Returns the newly created location list if successful.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_location_lists_create_execute()` or `displayvideo_advertisers_location_lists_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_location_lists_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_location_lists_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LocationList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LocationList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/advertisers/{advertisersId}/locationLists
/// Creates a new location list. Returns the newly created location list if successful.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_location_lists_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_location_lists_create_task()`.
/// For the simplest API, use `displayvideo_advertisers_location_lists_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_location_lists_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_location_lists_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LocationList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_location_lists_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_location_lists_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersLocationListsCreateArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
}

/// POST v4/advertisers/{advertisersId}/locationLists
/// Creates a new location list. Returns the newly created location list if successful.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_location_lists_create_builder()` + `displayvideo_advertisers_location_lists_create_execute()`.
/// For task-level control, use `displayvideo_advertisers_location_lists_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_location_lists_create(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersLocationListsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LocationList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        displayvideo_advertisers_location_lists_create_builder(client, &args.advertiserId)?;
    displayvideo_advertisers_location_lists_create_execute(builder)
}

/// GET v4/advertisers/{advertisersId}/locationLists/{locationListsId}
/// Gets a location list.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_location_lists_get_execute()` to send, or `displayvideo_advertisers_location_lists_get` for simplest API.

pub fn displayvideo_advertisers_location_lists_get_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    locationListId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/locationLists/{}",
        advertiserId, locationListId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}/locationLists/{locationListsId}
/// Gets a location list.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_location_lists_get_execute()` or `displayvideo_advertisers_location_lists_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_location_lists_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_location_lists_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LocationList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LocationList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}/locationLists/{locationListsId}
/// Gets a location list.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_location_lists_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_location_lists_get_task()`.
/// For the simplest API, use `displayvideo_advertisers_location_lists_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_location_lists_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_location_lists_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LocationList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_location_lists_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_location_lists_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersLocationListsGetArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: locationListId
    pub locationListId: String,
}

/// GET v4/advertisers/{advertisersId}/locationLists/{locationListsId}
/// Gets a location list.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_location_lists_get_builder()` + `displayvideo_advertisers_location_lists_get_execute()`.
/// For task-level control, use `displayvideo_advertisers_location_lists_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_location_lists_get(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersLocationListsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LocationList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_location_lists_get_builder(
        client,
        &args.advertiserId,
        &args.locationListId,
    )?;
    displayvideo_advertisers_location_lists_get_execute(builder)
}

/// GET v4/advertisers/{advertisersId}/locationLists
/// Lists location lists based on a given advertiser id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_location_lists_list_execute()` to send, or `displayvideo_advertisers_location_lists_list` for simplest API.

pub fn displayvideo_advertisers_location_lists_list_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/locationLists",
        advertiserId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}/locationLists
/// Lists location lists based on a given advertiser id.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_location_lists_list_execute()` or `displayvideo_advertisers_location_lists_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_location_lists_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_location_lists_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLocationListsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationListsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}/locationLists
/// Lists location lists based on a given advertiser id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_location_lists_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_location_lists_list_task()`.
/// For the simplest API, use `displayvideo_advertisers_location_lists_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_location_lists_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_location_lists_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationListsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_location_lists_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_location_lists_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersLocationListsListArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v4/advertisers/{advertisersId}/locationLists
/// Lists location lists based on a given advertiser id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_location_lists_list_builder()` + `displayvideo_advertisers_location_lists_list_execute()`.
/// For task-level control, use `displayvideo_advertisers_location_lists_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_location_lists_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersLocationListsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationListsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_location_lists_list_builder(
        client,
        &args.advertiserId,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    displayvideo_advertisers_location_lists_list_execute(builder)
}

/// PATCH v4/advertisers/{advertisersId}/locationLists/{locationListId}
/// Updates a location list. Returns the updated location list if successful.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_location_lists_patch_execute()` to send, or `displayvideo_advertisers_location_lists_patch` for simplest API.

pub fn displayvideo_advertisers_location_lists_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    locationListId: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/locationLists/{}",
        advertiserId, locationListId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v4/advertisers/{advertisersId}/locationLists/{locationListId}
/// Updates a location list. Returns the updated location list if successful.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_location_lists_patch_execute()` or `displayvideo_advertisers_location_lists_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_location_lists_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_location_lists_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LocationList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LocationList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v4/advertisers/{advertisersId}/locationLists/{locationListId}
/// Updates a location list. Returns the updated location list if successful.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_location_lists_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_location_lists_patch_task()`.
/// For the simplest API, use `displayvideo_advertisers_location_lists_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_location_lists_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_location_lists_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LocationList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_location_lists_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_location_lists_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersLocationListsPatchArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: locationListId
    pub locationListId: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v4/advertisers/{advertisersId}/locationLists/{locationListId}
/// Updates a location list. Returns the updated location list if successful.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_location_lists_patch_builder()` + `displayvideo_advertisers_location_lists_patch_execute()`.
/// For task-level control, use `displayvideo_advertisers_location_lists_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_location_lists_patch(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersLocationListsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LocationList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_location_lists_patch_builder(
        client,
        &args.advertiserId,
        &args.locationListId,
        &args.updateMask,
    )?;
    displayvideo_advertisers_location_lists_patch_execute(builder)
}

/// POST v4/advertisers/{advertiserId}/locationLists/{locationListsId}/assignedLocations:bulkEdit
/// Bulk edits multiple assignments between locations and a single location list. The operation will delete the assigned locations provided in `deletedAssignedLocations` and then create the assigned locations provided in `createdAssignedLocations`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_location_lists_assigned_locations_bulk_edit_execute()` to send, or `displayvideo_advertisers_location_lists_assigned_locations_bulk_edit` for simplest API.

pub fn displayvideo_advertisers_location_lists_assigned_locations_bulk_edit_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    locationListId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/locationLists/{}/assignedLocations:bulkEdit",
        advertiserId,
        locationListId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/advertisers/{advertiserId}/locationLists/{locationListsId}/assignedLocations:bulkEdit
/// Bulk edits multiple assignments between locations and a single location list. The operation will delete the assigned locations provided in `deletedAssignedLocations` and then create the assigned locations provided in `createdAssignedLocations`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_location_lists_assigned_locations_bulk_edit_execute()` or `displayvideo_advertisers_location_lists_assigned_locations_bulk_edit`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_location_lists_assigned_locations_bulk_edit_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_location_lists_assigned_locations_bulk_edit_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BulkEditAssignedLocationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BulkEditAssignedLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/advertisers/{advertiserId}/locationLists/{locationListsId}/assignedLocations:bulkEdit
/// Bulk edits multiple assignments between locations and a single location list. The operation will delete the assigned locations provided in `deletedAssignedLocations` and then create the assigned locations provided in `createdAssignedLocations`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_location_lists_assigned_locations_bulk_edit_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_location_lists_assigned_locations_bulk_edit_task()`.
/// For the simplest API, use `displayvideo_advertisers_location_lists_assigned_locations_bulk_edit()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_location_lists_assigned_locations_bulk_edit_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_location_lists_assigned_locations_bulk_edit_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BulkEditAssignedLocationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_location_lists_assigned_locations_bulk_edit_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_location_lists_assigned_locations_bulk_edit`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersLocationListsAssignedLocationsBulkEditArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: locationListId
    pub locationListId: String,
}

/// POST v4/advertisers/{advertiserId}/locationLists/{locationListsId}/assignedLocations:bulkEdit
/// Bulk edits multiple assignments between locations and a single location list. The operation will delete the assigned locations provided in `deletedAssignedLocations` and then create the assigned locations provided in `createdAssignedLocations`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_location_lists_assigned_locations_bulk_edit_builder()` + `displayvideo_advertisers_location_lists_assigned_locations_bulk_edit_execute()`.
/// For task-level control, use `displayvideo_advertisers_location_lists_assigned_locations_bulk_edit_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_location_lists_assigned_locations_bulk_edit(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersLocationListsAssignedLocationsBulkEditArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BulkEditAssignedLocationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_location_lists_assigned_locations_bulk_edit_builder(
        client,
        &args.advertiserId,
        &args.locationListId,
    )?;
    displayvideo_advertisers_location_lists_assigned_locations_bulk_edit_execute(builder)
}

/// POST v4/advertisers/{advertiserId}/locationLists/{locationListId}/assignedLocations
/// Creates an assignment between a location and a location list.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_location_lists_assigned_locations_create_execute()` to send, or `displayvideo_advertisers_location_lists_assigned_locations_create` for simplest API.

pub fn displayvideo_advertisers_location_lists_assigned_locations_create_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    locationListId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/locationLists/{}/assignedLocations",
        advertiserId, locationListId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/advertisers/{advertiserId}/locationLists/{locationListId}/assignedLocations
/// Creates an assignment between a location and a location list.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_location_lists_assigned_locations_create_execute()` or `displayvideo_advertisers_location_lists_assigned_locations_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_location_lists_assigned_locations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_location_lists_assigned_locations_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AssignedLocation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AssignedLocation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/advertisers/{advertiserId}/locationLists/{locationListId}/assignedLocations
/// Creates an assignment between a location and a location list.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_location_lists_assigned_locations_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_location_lists_assigned_locations_create_task()`.
/// For the simplest API, use `displayvideo_advertisers_location_lists_assigned_locations_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_location_lists_assigned_locations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_location_lists_assigned_locations_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AssignedLocation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_location_lists_assigned_locations_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_location_lists_assigned_locations_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersLocationListsAssignedLocationsCreateArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: locationListId
    pub locationListId: String,
}

/// POST v4/advertisers/{advertiserId}/locationLists/{locationListId}/assignedLocations
/// Creates an assignment between a location and a location list.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_location_lists_assigned_locations_create_builder()` + `displayvideo_advertisers_location_lists_assigned_locations_create_execute()`.
/// For task-level control, use `displayvideo_advertisers_location_lists_assigned_locations_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_location_lists_assigned_locations_create(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersLocationListsAssignedLocationsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AssignedLocation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_location_lists_assigned_locations_create_builder(
        client,
        &args.advertiserId,
        &args.locationListId,
    )?;
    displayvideo_advertisers_location_lists_assigned_locations_create_execute(builder)
}

/// DELETE v4/advertisers/{advertiserId}/locationLists/{locationListId}/assignedLocations/{assignedLocationsId}
/// Deletes the assignment between a location and a location list.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_location_lists_assigned_locations_delete_execute()` to send, or `displayvideo_advertisers_location_lists_assigned_locations_delete` for simplest API.

pub fn displayvideo_advertisers_location_lists_assigned_locations_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    locationListId: &String,
    assignedLocationId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/locationLists/{}/assignedLocations/{}",
        advertiserId,
        locationListId,
        assignedLocationId,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v4/advertisers/{advertiserId}/locationLists/{locationListId}/assignedLocations/{assignedLocationsId}
/// Deletes the assignment between a location and a location list.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_location_lists_assigned_locations_delete_execute()` or `displayvideo_advertisers_location_lists_assigned_locations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_location_lists_assigned_locations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_location_lists_assigned_locations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v4/advertisers/{advertiserId}/locationLists/{locationListId}/assignedLocations/{assignedLocationsId}
/// Deletes the assignment between a location and a location list.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_location_lists_assigned_locations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_location_lists_assigned_locations_delete_task()`.
/// For the simplest API, use `displayvideo_advertisers_location_lists_assigned_locations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_location_lists_assigned_locations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_location_lists_assigned_locations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_location_lists_assigned_locations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_location_lists_assigned_locations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersLocationListsAssignedLocationsDeleteArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: locationListId
    pub locationListId: String,
    /// Path parameter: assignedLocationId
    pub assignedLocationId: String,
}

/// DELETE v4/advertisers/{advertiserId}/locationLists/{locationListId}/assignedLocations/{assignedLocationsId}
/// Deletes the assignment between a location and a location list.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_location_lists_assigned_locations_delete_builder()` + `displayvideo_advertisers_location_lists_assigned_locations_delete_execute()`.
/// For task-level control, use `displayvideo_advertisers_location_lists_assigned_locations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_location_lists_assigned_locations_delete(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersLocationListsAssignedLocationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_location_lists_assigned_locations_delete_builder(
        client,
        &args.advertiserId,
        &args.locationListId,
        &args.assignedLocationId,
    )?;
    displayvideo_advertisers_location_lists_assigned_locations_delete_execute(builder)
}

/// GET v4/advertisers/{advertiserId}/locationLists/{locationListId}/assignedLocations
/// Lists locations assigned to a location list.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_location_lists_assigned_locations_list_execute()` to send, or `displayvideo_advertisers_location_lists_assigned_locations_list` for simplest API.

pub fn displayvideo_advertisers_location_lists_assigned_locations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    locationListId: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/locationLists/{}/assignedLocations",
        advertiserId, locationListId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertiserId}/locationLists/{locationListId}/assignedLocations
/// Lists locations assigned to a location list.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_location_lists_assigned_locations_list_execute()` or `displayvideo_advertisers_location_lists_assigned_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_location_lists_assigned_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_location_lists_assigned_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAssignedLocationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAssignedLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertiserId}/locationLists/{locationListId}/assignedLocations
/// Lists locations assigned to a location list.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_location_lists_assigned_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_location_lists_assigned_locations_list_task()`.
/// For the simplest API, use `displayvideo_advertisers_location_lists_assigned_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_location_lists_assigned_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_location_lists_assigned_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAssignedLocationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_location_lists_assigned_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_location_lists_assigned_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersLocationListsAssignedLocationsListArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: locationListId
    pub locationListId: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v4/advertisers/{advertiserId}/locationLists/{locationListId}/assignedLocations
/// Lists locations assigned to a location list.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_location_lists_assigned_locations_list_builder()` + `displayvideo_advertisers_location_lists_assigned_locations_list_execute()`.
/// For task-level control, use `displayvideo_advertisers_location_lists_assigned_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_location_lists_assigned_locations_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersLocationListsAssignedLocationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAssignedLocationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_location_lists_assigned_locations_list_builder(
        client,
        &args.advertiserId,
        &args.locationListId,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    displayvideo_advertisers_location_lists_assigned_locations_list_execute(builder)
}

/// POST v4/advertisers/{advertisersId}/negativeKeywordLists
/// Creates a new negative keyword list. Returns the newly created negative keyword list if successful.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_negative_keyword_lists_create_execute()` to send, or `displayvideo_advertisers_negative_keyword_lists_create` for simplest API.

pub fn displayvideo_advertisers_negative_keyword_lists_create_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/negativeKeywordLists",
        advertiserId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/advertisers/{advertisersId}/negativeKeywordLists
/// Creates a new negative keyword list. Returns the newly created negative keyword list if successful.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_negative_keyword_lists_create_execute()` or `displayvideo_advertisers_negative_keyword_lists_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_negative_keyword_lists_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_negative_keyword_lists_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NegativeKeywordList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NegativeKeywordList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/advertisers/{advertisersId}/negativeKeywordLists
/// Creates a new negative keyword list. Returns the newly created negative keyword list if successful.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_negative_keyword_lists_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_negative_keyword_lists_create_task()`.
/// For the simplest API, use `displayvideo_advertisers_negative_keyword_lists_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_negative_keyword_lists_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_negative_keyword_lists_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NegativeKeywordList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_negative_keyword_lists_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_negative_keyword_lists_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersNegativeKeywordListsCreateArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
}

/// POST v4/advertisers/{advertisersId}/negativeKeywordLists
/// Creates a new negative keyword list. Returns the newly created negative keyword list if successful.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_negative_keyword_lists_create_builder()` + `displayvideo_advertisers_negative_keyword_lists_create_execute()`.
/// For task-level control, use `displayvideo_advertisers_negative_keyword_lists_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_negative_keyword_lists_create(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersNegativeKeywordListsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NegativeKeywordList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        displayvideo_advertisers_negative_keyword_lists_create_builder(client, &args.advertiserId)?;
    displayvideo_advertisers_negative_keyword_lists_create_execute(builder)
}

/// DELETE v4/advertisers/{advertisersId}/negativeKeywordLists/{negativeKeywordListsId}
/// Deletes a negative keyword list given an advertiser ID and a negative keyword list ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_negative_keyword_lists_delete_execute()` to send, or `displayvideo_advertisers_negative_keyword_lists_delete` for simplest API.

pub fn displayvideo_advertisers_negative_keyword_lists_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    negativeKeywordListId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/negativeKeywordLists/{}",
        advertiserId, negativeKeywordListId,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v4/advertisers/{advertisersId}/negativeKeywordLists/{negativeKeywordListsId}
/// Deletes a negative keyword list given an advertiser ID and a negative keyword list ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_negative_keyword_lists_delete_execute()` or `displayvideo_advertisers_negative_keyword_lists_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_negative_keyword_lists_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_negative_keyword_lists_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v4/advertisers/{advertisersId}/negativeKeywordLists/{negativeKeywordListsId}
/// Deletes a negative keyword list given an advertiser ID and a negative keyword list ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_negative_keyword_lists_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_negative_keyword_lists_delete_task()`.
/// For the simplest API, use `displayvideo_advertisers_negative_keyword_lists_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_negative_keyword_lists_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_negative_keyword_lists_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_negative_keyword_lists_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_negative_keyword_lists_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersNegativeKeywordListsDeleteArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: negativeKeywordListId
    pub negativeKeywordListId: String,
}

/// DELETE v4/advertisers/{advertisersId}/negativeKeywordLists/{negativeKeywordListsId}
/// Deletes a negative keyword list given an advertiser ID and a negative keyword list ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_negative_keyword_lists_delete_builder()` + `displayvideo_advertisers_negative_keyword_lists_delete_execute()`.
/// For task-level control, use `displayvideo_advertisers_negative_keyword_lists_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_negative_keyword_lists_delete(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersNegativeKeywordListsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_negative_keyword_lists_delete_builder(
        client,
        &args.advertiserId,
        &args.negativeKeywordListId,
    )?;
    displayvideo_advertisers_negative_keyword_lists_delete_execute(builder)
}

/// GET v4/advertisers/{advertisersId}/negativeKeywordLists/{negativeKeywordListsId}
/// Gets a negative keyword list given an advertiser ID and a negative keyword list ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_negative_keyword_lists_get_execute()` to send, or `displayvideo_advertisers_negative_keyword_lists_get` for simplest API.

pub fn displayvideo_advertisers_negative_keyword_lists_get_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    negativeKeywordListId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/negativeKeywordLists/{}",
        advertiserId, negativeKeywordListId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}/negativeKeywordLists/{negativeKeywordListsId}
/// Gets a negative keyword list given an advertiser ID and a negative keyword list ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_negative_keyword_lists_get_execute()` or `displayvideo_advertisers_negative_keyword_lists_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_negative_keyword_lists_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_negative_keyword_lists_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NegativeKeywordList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NegativeKeywordList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}/negativeKeywordLists/{negativeKeywordListsId}
/// Gets a negative keyword list given an advertiser ID and a negative keyword list ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_negative_keyword_lists_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_negative_keyword_lists_get_task()`.
/// For the simplest API, use `displayvideo_advertisers_negative_keyword_lists_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_negative_keyword_lists_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_negative_keyword_lists_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NegativeKeywordList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_negative_keyword_lists_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_negative_keyword_lists_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersNegativeKeywordListsGetArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: negativeKeywordListId
    pub negativeKeywordListId: String,
}

/// GET v4/advertisers/{advertisersId}/negativeKeywordLists/{negativeKeywordListsId}
/// Gets a negative keyword list given an advertiser ID and a negative keyword list ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_negative_keyword_lists_get_builder()` + `displayvideo_advertisers_negative_keyword_lists_get_execute()`.
/// For task-level control, use `displayvideo_advertisers_negative_keyword_lists_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_negative_keyword_lists_get(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersNegativeKeywordListsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NegativeKeywordList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_negative_keyword_lists_get_builder(
        client,
        &args.advertiserId,
        &args.negativeKeywordListId,
    )?;
    displayvideo_advertisers_negative_keyword_lists_get_execute(builder)
}

/// GET v4/advertisers/{advertisersId}/negativeKeywordLists
/// Lists negative keyword lists based on a given advertiser id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_negative_keyword_lists_list_execute()` to send, or `displayvideo_advertisers_negative_keyword_lists_list` for simplest API.

pub fn displayvideo_advertisers_negative_keyword_lists_list_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/negativeKeywordLists",
        advertiserId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}/negativeKeywordLists
/// Lists negative keyword lists based on a given advertiser id.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_negative_keyword_lists_list_execute()` or `displayvideo_advertisers_negative_keyword_lists_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_negative_keyword_lists_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_negative_keyword_lists_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListNegativeKeywordListsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListNegativeKeywordListsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}/negativeKeywordLists
/// Lists negative keyword lists based on a given advertiser id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_negative_keyword_lists_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_negative_keyword_lists_list_task()`.
/// For the simplest API, use `displayvideo_advertisers_negative_keyword_lists_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_negative_keyword_lists_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_negative_keyword_lists_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListNegativeKeywordListsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_negative_keyword_lists_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_negative_keyword_lists_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersNegativeKeywordListsListArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v4/advertisers/{advertisersId}/negativeKeywordLists
/// Lists negative keyword lists based on a given advertiser id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_negative_keyword_lists_list_builder()` + `displayvideo_advertisers_negative_keyword_lists_list_execute()`.
/// For task-level control, use `displayvideo_advertisers_negative_keyword_lists_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_negative_keyword_lists_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersNegativeKeywordListsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListNegativeKeywordListsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_negative_keyword_lists_list_builder(
        client,
        &args.advertiserId,
        &args.pageSize,
        &args.pageToken,
    )?;
    displayvideo_advertisers_negative_keyword_lists_list_execute(builder)
}

/// PATCH v4/advertisers/{advertisersId}/negativeKeywordLists/{negativeKeywordListId}
/// Updates a negative keyword list. Returns the updated negative keyword list if successful.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_negative_keyword_lists_patch_execute()` to send, or `displayvideo_advertisers_negative_keyword_lists_patch` for simplest API.

pub fn displayvideo_advertisers_negative_keyword_lists_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    negativeKeywordListId: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/negativeKeywordLists/{}",
        advertiserId, negativeKeywordListId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v4/advertisers/{advertisersId}/negativeKeywordLists/{negativeKeywordListId}
/// Updates a negative keyword list. Returns the updated negative keyword list if successful.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_negative_keyword_lists_patch_execute()` or `displayvideo_advertisers_negative_keyword_lists_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_negative_keyword_lists_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_negative_keyword_lists_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NegativeKeywordList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NegativeKeywordList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v4/advertisers/{advertisersId}/negativeKeywordLists/{negativeKeywordListId}
/// Updates a negative keyword list. Returns the updated negative keyword list if successful.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_negative_keyword_lists_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_negative_keyword_lists_patch_task()`.
/// For the simplest API, use `displayvideo_advertisers_negative_keyword_lists_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_negative_keyword_lists_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_negative_keyword_lists_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NegativeKeywordList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_advertisers_negative_keyword_lists_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_negative_keyword_lists_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersNegativeKeywordListsPatchArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: negativeKeywordListId
    pub negativeKeywordListId: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v4/advertisers/{advertisersId}/negativeKeywordLists/{negativeKeywordListId}
/// Updates a negative keyword list. Returns the updated negative keyword list if successful.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_negative_keyword_lists_patch_builder()` + `displayvideo_advertisers_negative_keyword_lists_patch_execute()`.
/// For task-level control, use `displayvideo_advertisers_negative_keyword_lists_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_negative_keyword_lists_patch(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersNegativeKeywordListsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NegativeKeywordList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_negative_keyword_lists_patch_builder(
        client,
        &args.advertiserId,
        &args.negativeKeywordListId,
        &args.updateMask,
    )?;
    displayvideo_advertisers_negative_keyword_lists_patch_execute(builder)
}

/// POST v4/advertisers/{advertiserId}/negativeKeywordLists/{negativeKeywordListsId}/negativeKeywords:bulkEdit
/// Bulk edits negative keywords in a single negative keyword list. The operation will delete the negative keywords provided in BulkEditNegativeKeywordsRequest.deleted_negative_keywords and then create the negative keywords provided in BulkEditNegativeKeywordsRequest.created_negative_keywords. This operation is guaranteed to be atomic and will never result in a partial success or partial failure.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_bulk_edit_execute()` to send, or `displayvideo_advertisers_negative_keyword_lists_negative_keywords_bulk_edit` for simplest API.

pub fn displayvideo_advertisers_negative_keyword_lists_negative_keywords_bulk_edit_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    negativeKeywordListId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/negativeKeywordLists/{}/negativeKeywords:bulkEdit",
        advertiserId,
        negativeKeywordListId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/advertisers/{advertiserId}/negativeKeywordLists/{negativeKeywordListsId}/negativeKeywords:bulkEdit
/// Bulk edits negative keywords in a single negative keyword list. The operation will delete the negative keywords provided in BulkEditNegativeKeywordsRequest.deleted_negative_keywords and then create the negative keywords provided in BulkEditNegativeKeywordsRequest.created_negative_keywords. This operation is guaranteed to be atomic and will never result in a partial success or partial failure.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_bulk_edit_execute()` or `displayvideo_advertisers_negative_keyword_lists_negative_keywords_bulk_edit`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_negative_keyword_lists_negative_keywords_bulk_edit_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_negative_keyword_lists_negative_keywords_bulk_edit_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BulkEditNegativeKeywordsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BulkEditNegativeKeywordsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/advertisers/{advertiserId}/negativeKeywordLists/{negativeKeywordListsId}/negativeKeywords:bulkEdit
/// Bulk edits negative keywords in a single negative keyword list. The operation will delete the negative keywords provided in BulkEditNegativeKeywordsRequest.deleted_negative_keywords and then create the negative keywords provided in BulkEditNegativeKeywordsRequest.created_negative_keywords. This operation is guaranteed to be atomic and will never result in a partial success or partial failure.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_bulk_edit_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_bulk_edit_task()`.
/// For the simplest API, use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_bulk_edit()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_negative_keyword_lists_negative_keywords_bulk_edit_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_negative_keyword_lists_negative_keywords_bulk_edit_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BulkEditNegativeKeywordsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        displayvideo_advertisers_negative_keyword_lists_negative_keywords_bulk_edit_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_negative_keyword_lists_negative_keywords_bulk_edit`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersNegativeKeywordListsNegativeKeywordsBulkEditArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: negativeKeywordListId
    pub negativeKeywordListId: String,
}

/// POST v4/advertisers/{advertiserId}/negativeKeywordLists/{negativeKeywordListsId}/negativeKeywords:bulkEdit
/// Bulk edits negative keywords in a single negative keyword list. The operation will delete the negative keywords provided in BulkEditNegativeKeywordsRequest.deleted_negative_keywords and then create the negative keywords provided in BulkEditNegativeKeywordsRequest.created_negative_keywords. This operation is guaranteed to be atomic and will never result in a partial success or partial failure.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_bulk_edit_builder()` + `displayvideo_advertisers_negative_keyword_lists_negative_keywords_bulk_edit_execute()`.
/// For task-level control, use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_bulk_edit_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_negative_keyword_lists_negative_keywords_bulk_edit(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersNegativeKeywordListsNegativeKeywordsBulkEditArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BulkEditNegativeKeywordsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        displayvideo_advertisers_negative_keyword_lists_negative_keywords_bulk_edit_builder(
            client,
            &args.advertiserId,
            &args.negativeKeywordListId,
        )?;
    displayvideo_advertisers_negative_keyword_lists_negative_keywords_bulk_edit_execute(builder)
}

/// POST v4/advertisers/{advertiserId}/negativeKeywordLists/{negativeKeywordListsId}/negativeKeywords
/// Creates a negative keyword in a negative keyword list.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_create_execute()` to send, or `displayvideo_advertisers_negative_keyword_lists_negative_keywords_create` for simplest API.

pub fn displayvideo_advertisers_negative_keyword_lists_negative_keywords_create_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    negativeKeywordListId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/negativeKeywordLists/{}/negativeKeywords",
        advertiserId,
        negativeKeywordListId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/advertisers/{advertiserId}/negativeKeywordLists/{negativeKeywordListsId}/negativeKeywords
/// Creates a negative keyword in a negative keyword list.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_create_execute()` or `displayvideo_advertisers_negative_keyword_lists_negative_keywords_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_negative_keyword_lists_negative_keywords_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_negative_keyword_lists_negative_keywords_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NegativeKeyword>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NegativeKeyword = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/advertisers/{advertiserId}/negativeKeywordLists/{negativeKeywordListsId}/negativeKeywords
/// Creates a negative keyword in a negative keyword list.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_create_task()`.
/// For the simplest API, use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_negative_keyword_lists_negative_keywords_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_negative_keyword_lists_negative_keywords_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NegativeKeyword>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        displayvideo_advertisers_negative_keyword_lists_negative_keywords_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_negative_keyword_lists_negative_keywords_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersNegativeKeywordListsNegativeKeywordsCreateArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: negativeKeywordListId
    pub negativeKeywordListId: String,
}

/// POST v4/advertisers/{advertiserId}/negativeKeywordLists/{negativeKeywordListsId}/negativeKeywords
/// Creates a negative keyword in a negative keyword list.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_create_builder()` + `displayvideo_advertisers_negative_keyword_lists_negative_keywords_create_execute()`.
/// For task-level control, use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_negative_keyword_lists_negative_keywords_create(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersNegativeKeywordListsNegativeKeywordsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NegativeKeyword>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_negative_keyword_lists_negative_keywords_create_builder(
        client,
        &args.advertiserId,
        &args.negativeKeywordListId,
    )?;
    displayvideo_advertisers_negative_keyword_lists_negative_keywords_create_execute(builder)
}

/// DELETE v4/advertisers/{advertiserId}/negativeKeywordLists/{negativeKeywordListsId}/negativeKeywords/{negativeKeywordsId}
/// Deletes a negative keyword from a negative keyword list.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_delete_execute()` to send, or `displayvideo_advertisers_negative_keyword_lists_negative_keywords_delete` for simplest API.

pub fn displayvideo_advertisers_negative_keyword_lists_negative_keywords_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    negativeKeywordListId: &String,
    keywordValue: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/negativeKeywordLists/{}/negativeKeywords/{}",
        advertiserId,
        negativeKeywordListId,
        keywordValue,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v4/advertisers/{advertiserId}/negativeKeywordLists/{negativeKeywordListsId}/negativeKeywords/{negativeKeywordsId}
/// Deletes a negative keyword from a negative keyword list.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_delete_execute()` or `displayvideo_advertisers_negative_keyword_lists_negative_keywords_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_negative_keyword_lists_negative_keywords_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_negative_keyword_lists_negative_keywords_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v4/advertisers/{advertiserId}/negativeKeywordLists/{negativeKeywordListsId}/negativeKeywords/{negativeKeywordsId}
/// Deletes a negative keyword from a negative keyword list.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_delete_task()`.
/// For the simplest API, use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_negative_keyword_lists_negative_keywords_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_negative_keyword_lists_negative_keywords_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        displayvideo_advertisers_negative_keyword_lists_negative_keywords_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_negative_keyword_lists_negative_keywords_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersNegativeKeywordListsNegativeKeywordsDeleteArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: negativeKeywordListId
    pub negativeKeywordListId: String,
    /// Path parameter: keywordValue
    pub keywordValue: String,
}

/// DELETE v4/advertisers/{advertiserId}/negativeKeywordLists/{negativeKeywordListsId}/negativeKeywords/{negativeKeywordsId}
/// Deletes a negative keyword from a negative keyword list.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_delete_builder()` + `displayvideo_advertisers_negative_keyword_lists_negative_keywords_delete_execute()`.
/// For task-level control, use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_negative_keyword_lists_negative_keywords_delete(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersNegativeKeywordListsNegativeKeywordsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_negative_keyword_lists_negative_keywords_delete_builder(
        client,
        &args.advertiserId,
        &args.negativeKeywordListId,
        &args.keywordValue,
    )?;
    displayvideo_advertisers_negative_keyword_lists_negative_keywords_delete_execute(builder)
}

/// GET v4/advertisers/{advertisersId}/negativeKeywordLists/{negativeKeywordListsId}/negativeKeywords
/// Lists negative keywords in a negative keyword list.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_list_execute()` to send, or `displayvideo_advertisers_negative_keyword_lists_negative_keywords_list` for simplest API.

pub fn displayvideo_advertisers_negative_keyword_lists_negative_keywords_list_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    negativeKeywordListId: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/negativeKeywordLists/{}/negativeKeywords",
        advertiserId,
        negativeKeywordListId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}/negativeKeywordLists/{negativeKeywordListsId}/negativeKeywords
/// Lists negative keywords in a negative keyword list.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_list_execute()` or `displayvideo_advertisers_negative_keyword_lists_negative_keywords_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_negative_keyword_lists_negative_keywords_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_negative_keyword_lists_negative_keywords_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListNegativeKeywordsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListNegativeKeywordsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}/negativeKeywordLists/{negativeKeywordListsId}/negativeKeywords
/// Lists negative keywords in a negative keyword list.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_list_task()`.
/// For the simplest API, use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_negative_keyword_lists_negative_keywords_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_negative_keyword_lists_negative_keywords_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListNegativeKeywordsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        displayvideo_advertisers_negative_keyword_lists_negative_keywords_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_negative_keyword_lists_negative_keywords_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersNegativeKeywordListsNegativeKeywordsListArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: negativeKeywordListId
    pub negativeKeywordListId: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v4/advertisers/{advertisersId}/negativeKeywordLists/{negativeKeywordListsId}/negativeKeywords
/// Lists negative keywords in a negative keyword list.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_list_builder()` + `displayvideo_advertisers_negative_keyword_lists_negative_keywords_list_execute()`.
/// For task-level control, use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_negative_keyword_lists_negative_keywords_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersNegativeKeywordListsNegativeKeywordsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListNegativeKeywordsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_negative_keyword_lists_negative_keywords_list_builder(
        client,
        &args.advertiserId,
        &args.negativeKeywordListId,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    displayvideo_advertisers_negative_keyword_lists_negative_keywords_list_execute(builder)
}

/// POST v4/advertisers/{advertiserId}/negativeKeywordLists/{negativeKeywordListsId}/negativeKeywords:replace
/// Replaces all negative keywords in a single negative keyword list. The operation will replace the keywords in a negative keyword list with keywords provided in ReplaceNegativeKeywordsRequest.new_negative_keywords.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_replace_execute()` to send, or `displayvideo_advertisers_negative_keyword_lists_negative_keywords_replace` for simplest API.

pub fn displayvideo_advertisers_negative_keyword_lists_negative_keywords_replace_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    negativeKeywordListId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/negativeKeywordLists/{}/negativeKeywords:replace",
        advertiserId,
        negativeKeywordListId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/advertisers/{advertiserId}/negativeKeywordLists/{negativeKeywordListsId}/negativeKeywords:replace
/// Replaces all negative keywords in a single negative keyword list. The operation will replace the keywords in a negative keyword list with keywords provided in ReplaceNegativeKeywordsRequest.new_negative_keywords.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_replace_execute()` or `displayvideo_advertisers_negative_keyword_lists_negative_keywords_replace`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_negative_keyword_lists_negative_keywords_replace_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_negative_keyword_lists_negative_keywords_replace_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReplaceNegativeKeywordsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReplaceNegativeKeywordsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/advertisers/{advertiserId}/negativeKeywordLists/{negativeKeywordListsId}/negativeKeywords:replace
/// Replaces all negative keywords in a single negative keyword list. The operation will replace the keywords in a negative keyword list with keywords provided in ReplaceNegativeKeywordsRequest.new_negative_keywords.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_replace_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_replace_task()`.
/// For the simplest API, use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_replace()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_negative_keyword_lists_negative_keywords_replace_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_negative_keyword_lists_negative_keywords_replace_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReplaceNegativeKeywordsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        displayvideo_advertisers_negative_keyword_lists_negative_keywords_replace_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_negative_keyword_lists_negative_keywords_replace`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersNegativeKeywordListsNegativeKeywordsReplaceArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: negativeKeywordListId
    pub negativeKeywordListId: String,
}

/// POST v4/advertisers/{advertiserId}/negativeKeywordLists/{negativeKeywordListsId}/negativeKeywords:replace
/// Replaces all negative keywords in a single negative keyword list. The operation will replace the keywords in a negative keyword list with keywords provided in ReplaceNegativeKeywordsRequest.new_negative_keywords.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_replace_builder()` + `displayvideo_advertisers_negative_keyword_lists_negative_keywords_replace_execute()`.
/// For task-level control, use `displayvideo_advertisers_negative_keyword_lists_negative_keywords_replace_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_negative_keyword_lists_negative_keywords_replace(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersNegativeKeywordListsNegativeKeywordsReplaceArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReplaceNegativeKeywordsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        displayvideo_advertisers_negative_keyword_lists_negative_keywords_replace_builder(
            client,
            &args.advertiserId,
            &args.negativeKeywordListId,
        )?;
    displayvideo_advertisers_negative_keyword_lists_negative_keywords_replace_execute(builder)
}

/// POST v4/advertisers/{advertisersId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions
/// Assigns a targeting option to an advertiser. Returns the assigned targeting option if successful.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_targeting_types_assigned_targeting_options_create_execute()` to send, or `displayvideo_advertisers_targeting_types_assigned_targeting_options_create` for simplest API.

pub fn displayvideo_advertisers_targeting_types_assigned_targeting_options_create_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    targetingType: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/targetingTypes/{}/assignedTargetingOptions",
        advertiserId,
        targetingType,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/advertisers/{advertisersId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions
/// Assigns a targeting option to an advertiser. Returns the assigned targeting option if successful.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_targeting_types_assigned_targeting_options_create_execute()` or `displayvideo_advertisers_targeting_types_assigned_targeting_options_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_targeting_types_assigned_targeting_options_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_targeting_types_assigned_targeting_options_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AssignedTargetingOption>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AssignedTargetingOption = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/advertisers/{advertisersId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions
/// Assigns a targeting option to an advertiser. Returns the assigned targeting option if successful.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_targeting_types_assigned_targeting_options_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_targeting_types_assigned_targeting_options_create_task()`.
/// For the simplest API, use `displayvideo_advertisers_targeting_types_assigned_targeting_options_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_targeting_types_assigned_targeting_options_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_targeting_types_assigned_targeting_options_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AssignedTargetingOption>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        displayvideo_advertisers_targeting_types_assigned_targeting_options_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_targeting_types_assigned_targeting_options_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersTargetingTypesAssignedTargetingOptionsCreateArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: targetingType
    pub targetingType: String,
}

/// POST v4/advertisers/{advertisersId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions
/// Assigns a targeting option to an advertiser. Returns the assigned targeting option if successful.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_targeting_types_assigned_targeting_options_create_builder()` + `displayvideo_advertisers_targeting_types_assigned_targeting_options_create_execute()`.
/// For task-level control, use `displayvideo_advertisers_targeting_types_assigned_targeting_options_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_targeting_types_assigned_targeting_options_create(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersTargetingTypesAssignedTargetingOptionsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AssignedTargetingOption>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        displayvideo_advertisers_targeting_types_assigned_targeting_options_create_builder(
            client,
            &args.advertiserId,
            &args.targetingType,
        )?;
    displayvideo_advertisers_targeting_types_assigned_targeting_options_create_execute(builder)
}

/// DELETE v4/advertisers/{advertisersId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions/{assignedTargetingOptionsId}
/// Deletes an assigned targeting option from an advertiser.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_targeting_types_assigned_targeting_options_delete_execute()` to send, or `displayvideo_advertisers_targeting_types_assigned_targeting_options_delete` for simplest API.

pub fn displayvideo_advertisers_targeting_types_assigned_targeting_options_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    targetingType: &String,
    assignedTargetingOptionId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/targetingTypes/{}/assignedTargetingOptions/{}",
        advertiserId,
        targetingType,
        assignedTargetingOptionId,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v4/advertisers/{advertisersId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions/{assignedTargetingOptionsId}
/// Deletes an assigned targeting option from an advertiser.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_targeting_types_assigned_targeting_options_delete_execute()` or `displayvideo_advertisers_targeting_types_assigned_targeting_options_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_targeting_types_assigned_targeting_options_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_targeting_types_assigned_targeting_options_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v4/advertisers/{advertisersId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions/{assignedTargetingOptionsId}
/// Deletes an assigned targeting option from an advertiser.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_targeting_types_assigned_targeting_options_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_targeting_types_assigned_targeting_options_delete_task()`.
/// For the simplest API, use `displayvideo_advertisers_targeting_types_assigned_targeting_options_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_targeting_types_assigned_targeting_options_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_targeting_types_assigned_targeting_options_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        displayvideo_advertisers_targeting_types_assigned_targeting_options_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_targeting_types_assigned_targeting_options_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersTargetingTypesAssignedTargetingOptionsDeleteArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: targetingType
    pub targetingType: String,
    /// Path parameter: assignedTargetingOptionId
    pub assignedTargetingOptionId: String,
}

/// DELETE v4/advertisers/{advertisersId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions/{assignedTargetingOptionsId}
/// Deletes an assigned targeting option from an advertiser.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_targeting_types_assigned_targeting_options_delete_builder()` + `displayvideo_advertisers_targeting_types_assigned_targeting_options_delete_execute()`.
/// For task-level control, use `displayvideo_advertisers_targeting_types_assigned_targeting_options_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_targeting_types_assigned_targeting_options_delete(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersTargetingTypesAssignedTargetingOptionsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        displayvideo_advertisers_targeting_types_assigned_targeting_options_delete_builder(
            client,
            &args.advertiserId,
            &args.targetingType,
            &args.assignedTargetingOptionId,
        )?;
    displayvideo_advertisers_targeting_types_assigned_targeting_options_delete_execute(builder)
}

/// GET v4/advertisers/{advertisersId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions/{assignedTargetingOptionsId}
/// Gets a single targeting option assigned to an advertiser.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_targeting_types_assigned_targeting_options_get_execute()` to send, or `displayvideo_advertisers_targeting_types_assigned_targeting_options_get` for simplest API.

pub fn displayvideo_advertisers_targeting_types_assigned_targeting_options_get_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    targetingType: &String,
    assignedTargetingOptionId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/targetingTypes/{}/assignedTargetingOptions/{}",
        advertiserId,
        targetingType,
        assignedTargetingOptionId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions/{assignedTargetingOptionsId}
/// Gets a single targeting option assigned to an advertiser.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_targeting_types_assigned_targeting_options_get_execute()` or `displayvideo_advertisers_targeting_types_assigned_targeting_options_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_targeting_types_assigned_targeting_options_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_targeting_types_assigned_targeting_options_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AssignedTargetingOption>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AssignedTargetingOption = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions/{assignedTargetingOptionsId}
/// Gets a single targeting option assigned to an advertiser.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_targeting_types_assigned_targeting_options_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_targeting_types_assigned_targeting_options_get_task()`.
/// For the simplest API, use `displayvideo_advertisers_targeting_types_assigned_targeting_options_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_targeting_types_assigned_targeting_options_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_targeting_types_assigned_targeting_options_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AssignedTargetingOption>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        displayvideo_advertisers_targeting_types_assigned_targeting_options_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_targeting_types_assigned_targeting_options_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersTargetingTypesAssignedTargetingOptionsGetArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: targetingType
    pub targetingType: String,
    /// Path parameter: assignedTargetingOptionId
    pub assignedTargetingOptionId: String,
}

/// GET v4/advertisers/{advertisersId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions/{assignedTargetingOptionsId}
/// Gets a single targeting option assigned to an advertiser.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_targeting_types_assigned_targeting_options_get_builder()` + `displayvideo_advertisers_targeting_types_assigned_targeting_options_get_execute()`.
/// For task-level control, use `displayvideo_advertisers_targeting_types_assigned_targeting_options_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_targeting_types_assigned_targeting_options_get(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersTargetingTypesAssignedTargetingOptionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AssignedTargetingOption>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_targeting_types_assigned_targeting_options_get_builder(
        client,
        &args.advertiserId,
        &args.targetingType,
        &args.assignedTargetingOptionId,
    )?;
    displayvideo_advertisers_targeting_types_assigned_targeting_options_get_execute(builder)
}

/// GET v4/advertisers/{advertisersId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions
/// Lists the targeting options assigned to an advertiser.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_advertisers_targeting_types_assigned_targeting_options_list_execute()` to send, or `displayvideo_advertisers_targeting_types_assigned_targeting_options_list` for simplest API.

pub fn displayvideo_advertisers_targeting_types_assigned_targeting_options_list_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &String,
    targetingType: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/advertisers/{}/targetingTypes/{}/assignedTargetingOptions",
        advertiserId,
        targetingType,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/advertisers/{advertisersId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions
/// Lists the targeting options assigned to an advertiser.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_advertisers_targeting_types_assigned_targeting_options_list_execute()` or `displayvideo_advertisers_targeting_types_assigned_targeting_options_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_targeting_types_assigned_targeting_options_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_targeting_types_assigned_targeting_options_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAdvertiserAssignedTargetingOptionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAdvertiserAssignedTargetingOptionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/advertisers/{advertisersId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions
/// Lists the targeting options assigned to an advertiser.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_advertisers_targeting_types_assigned_targeting_options_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_advertisers_targeting_types_assigned_targeting_options_list_task()`.
/// For the simplest API, use `displayvideo_advertisers_targeting_types_assigned_targeting_options_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_advertisers_targeting_types_assigned_targeting_options_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_advertisers_targeting_types_assigned_targeting_options_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAdvertiserAssignedTargetingOptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        displayvideo_advertisers_targeting_types_assigned_targeting_options_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_advertisers_targeting_types_assigned_targeting_options_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoAdvertisersTargetingTypesAssignedTargetingOptionsListArgs {
    /// Path parameter: advertiserId
    pub advertiserId: String,
    /// Path parameter: targetingType
    pub targetingType: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v4/advertisers/{advertisersId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions
/// Lists the targeting options assigned to an advertiser.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_advertisers_targeting_types_assigned_targeting_options_list_builder()` + `displayvideo_advertisers_targeting_types_assigned_targeting_options_list_execute()`.
/// For task-level control, use `displayvideo_advertisers_targeting_types_assigned_targeting_options_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_advertisers_targeting_types_assigned_targeting_options_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoAdvertisersTargetingTypesAssignedTargetingOptionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAdvertiserAssignedTargetingOptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_advertisers_targeting_types_assigned_targeting_options_list_builder(
        client,
        &args.advertiserId,
        &args.targetingType,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    displayvideo_advertisers_targeting_types_assigned_targeting_options_list_execute(builder)
}

/// GET v4/combinedAudiences/{combinedAudiencesId}
/// Gets a combined audience.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_combined_audiences_get_execute()` to send, or `displayvideo_combined_audiences_get` for simplest API.

pub fn displayvideo_combined_audiences_get_builder<R>(
    client: &SimpleHttpClient<R>,
    combinedAudienceId: &String,
    advertiserId: &Option<String>,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/combinedAudiences/{}",
        combinedAudienceId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/combinedAudiences/{combinedAudiencesId}
/// Gets a combined audience.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_combined_audiences_get_execute()` or `displayvideo_combined_audiences_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_combined_audiences_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_combined_audiences_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CombinedAudience>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CombinedAudience = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/combinedAudiences/{combinedAudiencesId}
/// Gets a combined audience.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_combined_audiences_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_combined_audiences_get_task()`.
/// For the simplest API, use `displayvideo_combined_audiences_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_combined_audiences_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_combined_audiences_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CombinedAudience>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_combined_audiences_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_combined_audiences_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoCombinedAudiencesGetArgs {
    /// Path parameter: combinedAudienceId
    pub combinedAudienceId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// GET v4/combinedAudiences/{combinedAudiencesId}
/// Gets a combined audience.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_combined_audiences_get_builder()` + `displayvideo_combined_audiences_get_execute()`.
/// For task-level control, use `displayvideo_combined_audiences_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_combined_audiences_get(
    client: &SimpleHttpClient,
    args: &DisplayvideoCombinedAudiencesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CombinedAudience>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_combined_audiences_get_builder(
        client,
        &args.combinedAudienceId,
        &args.advertiserId,
        &args.partnerId,
    )?;
    displayvideo_combined_audiences_get_execute(builder)
}

/// GET v4/combinedAudiences
/// Lists combined audiences. The order is defined by the order_by parameter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_combined_audiences_list_execute()` to send, or `displayvideo_combined_audiences_list` for simplest API.

pub fn displayvideo_combined_audiences_list_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &Option<String>,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://displayvideo.googleapis.com/v4/combinedAudiences",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/combinedAudiences
/// Lists combined audiences. The order is defined by the order_by parameter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_combined_audiences_list_execute()` or `displayvideo_combined_audiences_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_combined_audiences_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_combined_audiences_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCombinedAudiencesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCombinedAudiencesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/combinedAudiences
/// Lists combined audiences. The order is defined by the order_by parameter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_combined_audiences_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_combined_audiences_list_task()`.
/// For the simplest API, use `displayvideo_combined_audiences_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_combined_audiences_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_combined_audiences_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCombinedAudiencesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_combined_audiences_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_combined_audiences_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoCombinedAudiencesListArgs {
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// GET v4/combinedAudiences
/// Lists combined audiences. The order is defined by the order_by parameter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_combined_audiences_list_builder()` + `displayvideo_combined_audiences_list_execute()`.
/// For task-level control, use `displayvideo_combined_audiences_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_combined_audiences_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoCombinedAudiencesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCombinedAudiencesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_combined_audiences_list_builder(
        client,
        &args.advertiserId,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.partnerId,
    )?;
    displayvideo_combined_audiences_list_execute(builder)
}

/// POST v4/customBiddingAlgorithms
/// Creates a new custom bidding algorithm. Returns the newly created custom bidding algorithm if successful.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_custom_bidding_algorithms_create_execute()` to send, or `displayvideo_custom_bidding_algorithms_create` for simplest API.

pub fn displayvideo_custom_bidding_algorithms_create_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://displayvideo.googleapis.com/v4/customBiddingAlgorithms",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/customBiddingAlgorithms
/// Creates a new custom bidding algorithm. Returns the newly created custom bidding algorithm if successful.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_custom_bidding_algorithms_create_execute()` or `displayvideo_custom_bidding_algorithms_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_custom_bidding_algorithms_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_custom_bidding_algorithms_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CustomBiddingAlgorithm>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomBiddingAlgorithm = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/customBiddingAlgorithms
/// Creates a new custom bidding algorithm. Returns the newly created custom bidding algorithm if successful.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_custom_bidding_algorithms_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_custom_bidding_algorithms_create_task()`.
/// For the simplest API, use `displayvideo_custom_bidding_algorithms_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_custom_bidding_algorithms_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_custom_bidding_algorithms_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomBiddingAlgorithm>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_custom_bidding_algorithms_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v4/customBiddingAlgorithms
/// Creates a new custom bidding algorithm. Returns the newly created custom bidding algorithm if successful.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_custom_bidding_algorithms_create_builder()` + `displayvideo_custom_bidding_algorithms_create_execute()`.
/// For task-level control, use `displayvideo_custom_bidding_algorithms_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_custom_bidding_algorithms_create(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomBiddingAlgorithm>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_custom_bidding_algorithms_create_builder(client)?;
    displayvideo_custom_bidding_algorithms_create_execute(builder)
}

/// GET v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}
/// Gets a custom bidding algorithm.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_custom_bidding_algorithms_get_execute()` to send, or `displayvideo_custom_bidding_algorithms_get` for simplest API.

pub fn displayvideo_custom_bidding_algorithms_get_builder<R>(
    client: &SimpleHttpClient<R>,
    customBiddingAlgorithmId: &String,
    advertiserId: &Option<String>,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/customBiddingAlgorithms/{}",
        customBiddingAlgorithmId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}
/// Gets a custom bidding algorithm.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_custom_bidding_algorithms_get_execute()` or `displayvideo_custom_bidding_algorithms_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_custom_bidding_algorithms_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_custom_bidding_algorithms_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CustomBiddingAlgorithm>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomBiddingAlgorithm = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}
/// Gets a custom bidding algorithm.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_custom_bidding_algorithms_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_custom_bidding_algorithms_get_task()`.
/// For the simplest API, use `displayvideo_custom_bidding_algorithms_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_custom_bidding_algorithms_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_custom_bidding_algorithms_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomBiddingAlgorithm>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_custom_bidding_algorithms_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_custom_bidding_algorithms_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoCustomBiddingAlgorithmsGetArgs {
    /// Path parameter: customBiddingAlgorithmId
    pub customBiddingAlgorithmId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// GET v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}
/// Gets a custom bidding algorithm.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_custom_bidding_algorithms_get_builder()` + `displayvideo_custom_bidding_algorithms_get_execute()`.
/// For task-level control, use `displayvideo_custom_bidding_algorithms_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_custom_bidding_algorithms_get(
    client: &SimpleHttpClient,
    args: &DisplayvideoCustomBiddingAlgorithmsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomBiddingAlgorithm>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_custom_bidding_algorithms_get_builder(
        client,
        &args.customBiddingAlgorithmId,
        &args.advertiserId,
        &args.partnerId,
    )?;
    displayvideo_custom_bidding_algorithms_get_execute(builder)
}

/// GET v4/customBiddingAlgorithms
/// Lists custom bidding algorithms that are accessible to the current user and can be used in bidding stratgies. The order is defined by the order_by parameter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_custom_bidding_algorithms_list_execute()` to send, or `displayvideo_custom_bidding_algorithms_list` for simplest API.

pub fn displayvideo_custom_bidding_algorithms_list_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &Option<String>,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://displayvideo.googleapis.com/v4/customBiddingAlgorithms",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/customBiddingAlgorithms
/// Lists custom bidding algorithms that are accessible to the current user and can be used in bidding stratgies. The order is defined by the order_by parameter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_custom_bidding_algorithms_list_execute()` or `displayvideo_custom_bidding_algorithms_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_custom_bidding_algorithms_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_custom_bidding_algorithms_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCustomBiddingAlgorithmsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCustomBiddingAlgorithmsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/customBiddingAlgorithms
/// Lists custom bidding algorithms that are accessible to the current user and can be used in bidding stratgies. The order is defined by the order_by parameter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_custom_bidding_algorithms_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_custom_bidding_algorithms_list_task()`.
/// For the simplest API, use `displayvideo_custom_bidding_algorithms_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_custom_bidding_algorithms_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_custom_bidding_algorithms_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCustomBiddingAlgorithmsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_custom_bidding_algorithms_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_custom_bidding_algorithms_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoCustomBiddingAlgorithmsListArgs {
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// GET v4/customBiddingAlgorithms
/// Lists custom bidding algorithms that are accessible to the current user and can be used in bidding stratgies. The order is defined by the order_by parameter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_custom_bidding_algorithms_list_builder()` + `displayvideo_custom_bidding_algorithms_list_execute()`.
/// For task-level control, use `displayvideo_custom_bidding_algorithms_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_custom_bidding_algorithms_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoCustomBiddingAlgorithmsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCustomBiddingAlgorithmsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_custom_bidding_algorithms_list_builder(
        client,
        &args.advertiserId,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.partnerId,
    )?;
    displayvideo_custom_bidding_algorithms_list_execute(builder)
}

/// PATCH v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}
/// Updates an existing custom bidding algorithm. Returns the updated custom bidding algorithm if successful. Requests updating a custom bidding algorithm assigned to a line item will return an error.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_custom_bidding_algorithms_patch_execute()` to send, or `displayvideo_custom_bidding_algorithms_patch` for simplest API.

pub fn displayvideo_custom_bidding_algorithms_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    customBiddingAlgorithmId: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/customBiddingAlgorithms/{}",
        customBiddingAlgorithmId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}
/// Updates an existing custom bidding algorithm. Returns the updated custom bidding algorithm if successful. Requests updating a custom bidding algorithm assigned to a line item will return an error.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_custom_bidding_algorithms_patch_execute()` or `displayvideo_custom_bidding_algorithms_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_custom_bidding_algorithms_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_custom_bidding_algorithms_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CustomBiddingAlgorithm>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomBiddingAlgorithm = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}
/// Updates an existing custom bidding algorithm. Returns the updated custom bidding algorithm if successful. Requests updating a custom bidding algorithm assigned to a line item will return an error.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_custom_bidding_algorithms_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_custom_bidding_algorithms_patch_task()`.
/// For the simplest API, use `displayvideo_custom_bidding_algorithms_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_custom_bidding_algorithms_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_custom_bidding_algorithms_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomBiddingAlgorithm>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_custom_bidding_algorithms_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_custom_bidding_algorithms_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoCustomBiddingAlgorithmsPatchArgs {
    /// Path parameter: customBiddingAlgorithmId
    pub customBiddingAlgorithmId: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}
/// Updates an existing custom bidding algorithm. Returns the updated custom bidding algorithm if successful. Requests updating a custom bidding algorithm assigned to a line item will return an error.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_custom_bidding_algorithms_patch_builder()` + `displayvideo_custom_bidding_algorithms_patch_execute()`.
/// For task-level control, use `displayvideo_custom_bidding_algorithms_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_custom_bidding_algorithms_patch(
    client: &SimpleHttpClient,
    args: &DisplayvideoCustomBiddingAlgorithmsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomBiddingAlgorithm>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_custom_bidding_algorithms_patch_builder(
        client,
        &args.customBiddingAlgorithmId,
        &args.updateMask,
    )?;
    displayvideo_custom_bidding_algorithms_patch_execute(builder)
}

/// GET v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}:uploadRules
/// Creates a rules reference object for an AlgorithmRules file. The resulting reference object provides a resource path where the AlgorithmRules file should be uploaded. This reference object should be included when creating a new CustomBiddingAlgorithmRules resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_custom_bidding_algorithms_upload_rules_execute()` to send, or `displayvideo_custom_bidding_algorithms_upload_rules` for simplest API.

pub fn displayvideo_custom_bidding_algorithms_upload_rules_builder<R>(
    client: &SimpleHttpClient<R>,
    customBiddingAlgorithmId: &String,
    advertiserId: &Option<String>,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/customBiddingAlgorithms/{}:uploadRules",
        customBiddingAlgorithmId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}:uploadRules
/// Creates a rules reference object for an AlgorithmRules file. The resulting reference object provides a resource path where the AlgorithmRules file should be uploaded. This reference object should be included when creating a new CustomBiddingAlgorithmRules resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_custom_bidding_algorithms_upload_rules_execute()` or `displayvideo_custom_bidding_algorithms_upload_rules`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_custom_bidding_algorithms_upload_rules_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_custom_bidding_algorithms_upload_rules_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CustomBiddingAlgorithmRulesRef>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomBiddingAlgorithmRulesRef = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}:uploadRules
/// Creates a rules reference object for an AlgorithmRules file. The resulting reference object provides a resource path where the AlgorithmRules file should be uploaded. This reference object should be included when creating a new CustomBiddingAlgorithmRules resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_custom_bidding_algorithms_upload_rules_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_custom_bidding_algorithms_upload_rules_task()`.
/// For the simplest API, use `displayvideo_custom_bidding_algorithms_upload_rules()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_custom_bidding_algorithms_upload_rules_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_custom_bidding_algorithms_upload_rules_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CustomBiddingAlgorithmRulesRef>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_custom_bidding_algorithms_upload_rules_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_custom_bidding_algorithms_upload_rules`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoCustomBiddingAlgorithmsUploadRulesArgs {
    /// Path parameter: customBiddingAlgorithmId
    pub customBiddingAlgorithmId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// GET v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}:uploadRules
/// Creates a rules reference object for an AlgorithmRules file. The resulting reference object provides a resource path where the AlgorithmRules file should be uploaded. This reference object should be included when creating a new CustomBiddingAlgorithmRules resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_custom_bidding_algorithms_upload_rules_builder()` + `displayvideo_custom_bidding_algorithms_upload_rules_execute()`.
/// For task-level control, use `displayvideo_custom_bidding_algorithms_upload_rules_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_custom_bidding_algorithms_upload_rules(
    client: &SimpleHttpClient,
    args: &DisplayvideoCustomBiddingAlgorithmsUploadRulesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CustomBiddingAlgorithmRulesRef>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_custom_bidding_algorithms_upload_rules_builder(
        client,
        &args.customBiddingAlgorithmId,
        &args.advertiserId,
        &args.partnerId,
    )?;
    displayvideo_custom_bidding_algorithms_upload_rules_execute(builder)
}

/// GET v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}:uploadScript
/// Creates a custom bidding script reference object for a script file. The resulting reference object provides a resource path to which the script file should be uploaded. This reference object should be included in when creating a new custom bidding script object.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_custom_bidding_algorithms_upload_script_execute()` to send, or `displayvideo_custom_bidding_algorithms_upload_script` for simplest API.

pub fn displayvideo_custom_bidding_algorithms_upload_script_builder<R>(
    client: &SimpleHttpClient<R>,
    customBiddingAlgorithmId: &String,
    advertiserId: &Option<String>,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/customBiddingAlgorithms/{}:uploadScript",
        customBiddingAlgorithmId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}:uploadScript
/// Creates a custom bidding script reference object for a script file. The resulting reference object provides a resource path to which the script file should be uploaded. This reference object should be included in when creating a new custom bidding script object.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_custom_bidding_algorithms_upload_script_execute()` or `displayvideo_custom_bidding_algorithms_upload_script`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_custom_bidding_algorithms_upload_script_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_custom_bidding_algorithms_upload_script_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CustomBiddingScriptRef>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomBiddingScriptRef = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}:uploadScript
/// Creates a custom bidding script reference object for a script file. The resulting reference object provides a resource path to which the script file should be uploaded. This reference object should be included in when creating a new custom bidding script object.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_custom_bidding_algorithms_upload_script_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_custom_bidding_algorithms_upload_script_task()`.
/// For the simplest API, use `displayvideo_custom_bidding_algorithms_upload_script()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_custom_bidding_algorithms_upload_script_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_custom_bidding_algorithms_upload_script_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomBiddingScriptRef>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_custom_bidding_algorithms_upload_script_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_custom_bidding_algorithms_upload_script`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoCustomBiddingAlgorithmsUploadScriptArgs {
    /// Path parameter: customBiddingAlgorithmId
    pub customBiddingAlgorithmId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// GET v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}:uploadScript
/// Creates a custom bidding script reference object for a script file. The resulting reference object provides a resource path to which the script file should be uploaded. This reference object should be included in when creating a new custom bidding script object.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_custom_bidding_algorithms_upload_script_builder()` + `displayvideo_custom_bidding_algorithms_upload_script_execute()`.
/// For task-level control, use `displayvideo_custom_bidding_algorithms_upload_script_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_custom_bidding_algorithms_upload_script(
    client: &SimpleHttpClient,
    args: &DisplayvideoCustomBiddingAlgorithmsUploadScriptArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomBiddingScriptRef>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_custom_bidding_algorithms_upload_script_builder(
        client,
        &args.customBiddingAlgorithmId,
        &args.advertiserId,
        &args.partnerId,
    )?;
    displayvideo_custom_bidding_algorithms_upload_script_execute(builder)
}

/// POST v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}/rules
/// Creates a new rules resource. Returns the newly created rules resource if successful. Requests creating a custom bidding rules resource under an algorithm assigned to a line item will return an error.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_custom_bidding_algorithms_rules_create_execute()` to send, or `displayvideo_custom_bidding_algorithms_rules_create` for simplest API.

pub fn displayvideo_custom_bidding_algorithms_rules_create_builder<R>(
    client: &SimpleHttpClient<R>,
    customBiddingAlgorithmId: &String,
    advertiserId: &Option<String>,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/customBiddingAlgorithms/{}/rules",
        customBiddingAlgorithmId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}/rules
/// Creates a new rules resource. Returns the newly created rules resource if successful. Requests creating a custom bidding rules resource under an algorithm assigned to a line item will return an error.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_custom_bidding_algorithms_rules_create_execute()` or `displayvideo_custom_bidding_algorithms_rules_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_custom_bidding_algorithms_rules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_custom_bidding_algorithms_rules_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CustomBiddingAlgorithmRules>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomBiddingAlgorithmRules = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}/rules
/// Creates a new rules resource. Returns the newly created rules resource if successful. Requests creating a custom bidding rules resource under an algorithm assigned to a line item will return an error.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_custom_bidding_algorithms_rules_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_custom_bidding_algorithms_rules_create_task()`.
/// For the simplest API, use `displayvideo_custom_bidding_algorithms_rules_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_custom_bidding_algorithms_rules_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_custom_bidding_algorithms_rules_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CustomBiddingAlgorithmRules>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_custom_bidding_algorithms_rules_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_custom_bidding_algorithms_rules_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoCustomBiddingAlgorithmsRulesCreateArgs {
    /// Path parameter: customBiddingAlgorithmId
    pub customBiddingAlgorithmId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// POST v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}/rules
/// Creates a new rules resource. Returns the newly created rules resource if successful. Requests creating a custom bidding rules resource under an algorithm assigned to a line item will return an error.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_custom_bidding_algorithms_rules_create_builder()` + `displayvideo_custom_bidding_algorithms_rules_create_execute()`.
/// For task-level control, use `displayvideo_custom_bidding_algorithms_rules_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_custom_bidding_algorithms_rules_create(
    client: &SimpleHttpClient,
    args: &DisplayvideoCustomBiddingAlgorithmsRulesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CustomBiddingAlgorithmRules>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_custom_bidding_algorithms_rules_create_builder(
        client,
        &args.customBiddingAlgorithmId,
        &args.advertiserId,
        &args.partnerId,
    )?;
    displayvideo_custom_bidding_algorithms_rules_create_execute(builder)
}

/// GET v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}/rules/{rulesId}
/// Retrieves a rules resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_custom_bidding_algorithms_rules_get_execute()` to send, or `displayvideo_custom_bidding_algorithms_rules_get` for simplest API.

pub fn displayvideo_custom_bidding_algorithms_rules_get_builder<R>(
    client: &SimpleHttpClient<R>,
    customBiddingAlgorithmId: &String,
    customBiddingAlgorithmRulesId: &String,
    advertiserId: &Option<String>,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/customBiddingAlgorithms/{}/rules/{}",
        customBiddingAlgorithmId, customBiddingAlgorithmRulesId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}/rules/{rulesId}
/// Retrieves a rules resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_custom_bidding_algorithms_rules_get_execute()` or `displayvideo_custom_bidding_algorithms_rules_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_custom_bidding_algorithms_rules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_custom_bidding_algorithms_rules_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CustomBiddingAlgorithmRules>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomBiddingAlgorithmRules = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}/rules/{rulesId}
/// Retrieves a rules resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_custom_bidding_algorithms_rules_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_custom_bidding_algorithms_rules_get_task()`.
/// For the simplest API, use `displayvideo_custom_bidding_algorithms_rules_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_custom_bidding_algorithms_rules_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_custom_bidding_algorithms_rules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CustomBiddingAlgorithmRules>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_custom_bidding_algorithms_rules_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_custom_bidding_algorithms_rules_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoCustomBiddingAlgorithmsRulesGetArgs {
    /// Path parameter: customBiddingAlgorithmId
    pub customBiddingAlgorithmId: String,
    /// Path parameter: customBiddingAlgorithmRulesId
    pub customBiddingAlgorithmRulesId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// GET v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}/rules/{rulesId}
/// Retrieves a rules resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_custom_bidding_algorithms_rules_get_builder()` + `displayvideo_custom_bidding_algorithms_rules_get_execute()`.
/// For task-level control, use `displayvideo_custom_bidding_algorithms_rules_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_custom_bidding_algorithms_rules_get(
    client: &SimpleHttpClient,
    args: &DisplayvideoCustomBiddingAlgorithmsRulesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CustomBiddingAlgorithmRules>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_custom_bidding_algorithms_rules_get_builder(
        client,
        &args.customBiddingAlgorithmId,
        &args.customBiddingAlgorithmRulesId,
        &args.advertiserId,
        &args.partnerId,
    )?;
    displayvideo_custom_bidding_algorithms_rules_get_execute(builder)
}

/// GET v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}/rules
/// Lists rules resources that belong to the given algorithm. The order is defined by the order_by parameter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_custom_bidding_algorithms_rules_list_execute()` to send, or `displayvideo_custom_bidding_algorithms_rules_list` for simplest API.

pub fn displayvideo_custom_bidding_algorithms_rules_list_builder<R>(
    client: &SimpleHttpClient<R>,
    customBiddingAlgorithmId: &String,
    advertiserId: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/customBiddingAlgorithms/{}/rules",
        customBiddingAlgorithmId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}/rules
/// Lists rules resources that belong to the given algorithm. The order is defined by the order_by parameter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_custom_bidding_algorithms_rules_list_execute()` or `displayvideo_custom_bidding_algorithms_rules_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_custom_bidding_algorithms_rules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_custom_bidding_algorithms_rules_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCustomBiddingAlgorithmRulesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCustomBiddingAlgorithmRulesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}/rules
/// Lists rules resources that belong to the given algorithm. The order is defined by the order_by parameter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_custom_bidding_algorithms_rules_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_custom_bidding_algorithms_rules_list_task()`.
/// For the simplest API, use `displayvideo_custom_bidding_algorithms_rules_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_custom_bidding_algorithms_rules_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_custom_bidding_algorithms_rules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCustomBiddingAlgorithmRulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_custom_bidding_algorithms_rules_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_custom_bidding_algorithms_rules_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoCustomBiddingAlgorithmsRulesListArgs {
    /// Path parameter: customBiddingAlgorithmId
    pub customBiddingAlgorithmId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// GET v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}/rules
/// Lists rules resources that belong to the given algorithm. The order is defined by the order_by parameter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_custom_bidding_algorithms_rules_list_builder()` + `displayvideo_custom_bidding_algorithms_rules_list_execute()`.
/// For task-level control, use `displayvideo_custom_bidding_algorithms_rules_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_custom_bidding_algorithms_rules_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoCustomBiddingAlgorithmsRulesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCustomBiddingAlgorithmRulesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_custom_bidding_algorithms_rules_list_builder(
        client,
        &args.customBiddingAlgorithmId,
        &args.advertiserId,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.partnerId,
    )?;
    displayvideo_custom_bidding_algorithms_rules_list_execute(builder)
}

/// POST v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}/scripts
/// Creates a new custom bidding script. Returns the newly created script if successful. Requests creating a custom bidding script under an algorithm assigned to a line item will return an error.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_custom_bidding_algorithms_scripts_create_execute()` to send, or `displayvideo_custom_bidding_algorithms_scripts_create` for simplest API.

pub fn displayvideo_custom_bidding_algorithms_scripts_create_builder<R>(
    client: &SimpleHttpClient<R>,
    customBiddingAlgorithmId: &String,
    advertiserId: &Option<String>,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/customBiddingAlgorithms/{}/scripts",
        customBiddingAlgorithmId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}/scripts
/// Creates a new custom bidding script. Returns the newly created script if successful. Requests creating a custom bidding script under an algorithm assigned to a line item will return an error.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_custom_bidding_algorithms_scripts_create_execute()` or `displayvideo_custom_bidding_algorithms_scripts_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_custom_bidding_algorithms_scripts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_custom_bidding_algorithms_scripts_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CustomBiddingScript>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomBiddingScript = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}/scripts
/// Creates a new custom bidding script. Returns the newly created script if successful. Requests creating a custom bidding script under an algorithm assigned to a line item will return an error.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_custom_bidding_algorithms_scripts_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_custom_bidding_algorithms_scripts_create_task()`.
/// For the simplest API, use `displayvideo_custom_bidding_algorithms_scripts_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_custom_bidding_algorithms_scripts_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_custom_bidding_algorithms_scripts_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomBiddingScript>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_custom_bidding_algorithms_scripts_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_custom_bidding_algorithms_scripts_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoCustomBiddingAlgorithmsScriptsCreateArgs {
    /// Path parameter: customBiddingAlgorithmId
    pub customBiddingAlgorithmId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// POST v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}/scripts
/// Creates a new custom bidding script. Returns the newly created script if successful. Requests creating a custom bidding script under an algorithm assigned to a line item will return an error.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_custom_bidding_algorithms_scripts_create_builder()` + `displayvideo_custom_bidding_algorithms_scripts_create_execute()`.
/// For task-level control, use `displayvideo_custom_bidding_algorithms_scripts_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_custom_bidding_algorithms_scripts_create(
    client: &SimpleHttpClient,
    args: &DisplayvideoCustomBiddingAlgorithmsScriptsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomBiddingScript>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_custom_bidding_algorithms_scripts_create_builder(
        client,
        &args.customBiddingAlgorithmId,
        &args.advertiserId,
        &args.partnerId,
    )?;
    displayvideo_custom_bidding_algorithms_scripts_create_execute(builder)
}

/// GET v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}/scripts/{scriptsId}
/// Gets a custom bidding script.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_custom_bidding_algorithms_scripts_get_execute()` to send, or `displayvideo_custom_bidding_algorithms_scripts_get` for simplest API.

pub fn displayvideo_custom_bidding_algorithms_scripts_get_builder<R>(
    client: &SimpleHttpClient<R>,
    customBiddingAlgorithmId: &String,
    customBiddingScriptId: &String,
    advertiserId: &Option<String>,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/customBiddingAlgorithms/{}/scripts/{}",
        customBiddingAlgorithmId, customBiddingScriptId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}/scripts/{scriptsId}
/// Gets a custom bidding script.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_custom_bidding_algorithms_scripts_get_execute()` or `displayvideo_custom_bidding_algorithms_scripts_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_custom_bidding_algorithms_scripts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_custom_bidding_algorithms_scripts_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CustomBiddingScript>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomBiddingScript = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}/scripts/{scriptsId}
/// Gets a custom bidding script.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_custom_bidding_algorithms_scripts_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_custom_bidding_algorithms_scripts_get_task()`.
/// For the simplest API, use `displayvideo_custom_bidding_algorithms_scripts_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_custom_bidding_algorithms_scripts_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_custom_bidding_algorithms_scripts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomBiddingScript>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_custom_bidding_algorithms_scripts_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_custom_bidding_algorithms_scripts_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoCustomBiddingAlgorithmsScriptsGetArgs {
    /// Path parameter: customBiddingAlgorithmId
    pub customBiddingAlgorithmId: String,
    /// Path parameter: customBiddingScriptId
    pub customBiddingScriptId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// GET v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}/scripts/{scriptsId}
/// Gets a custom bidding script.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_custom_bidding_algorithms_scripts_get_builder()` + `displayvideo_custom_bidding_algorithms_scripts_get_execute()`.
/// For task-level control, use `displayvideo_custom_bidding_algorithms_scripts_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_custom_bidding_algorithms_scripts_get(
    client: &SimpleHttpClient,
    args: &DisplayvideoCustomBiddingAlgorithmsScriptsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomBiddingScript>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_custom_bidding_algorithms_scripts_get_builder(
        client,
        &args.customBiddingAlgorithmId,
        &args.customBiddingScriptId,
        &args.advertiserId,
        &args.partnerId,
    )?;
    displayvideo_custom_bidding_algorithms_scripts_get_execute(builder)
}

/// GET v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}/scripts
/// Lists custom bidding scripts that belong to the given algorithm. The order is defined by the order_by parameter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_custom_bidding_algorithms_scripts_list_execute()` to send, or `displayvideo_custom_bidding_algorithms_scripts_list` for simplest API.

pub fn displayvideo_custom_bidding_algorithms_scripts_list_builder<R>(
    client: &SimpleHttpClient<R>,
    customBiddingAlgorithmId: &String,
    advertiserId: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/customBiddingAlgorithms/{}/scripts",
        customBiddingAlgorithmId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}/scripts
/// Lists custom bidding scripts that belong to the given algorithm. The order is defined by the order_by parameter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_custom_bidding_algorithms_scripts_list_execute()` or `displayvideo_custom_bidding_algorithms_scripts_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_custom_bidding_algorithms_scripts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_custom_bidding_algorithms_scripts_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCustomBiddingScriptsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCustomBiddingScriptsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}/scripts
/// Lists custom bidding scripts that belong to the given algorithm. The order is defined by the order_by parameter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_custom_bidding_algorithms_scripts_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_custom_bidding_algorithms_scripts_list_task()`.
/// For the simplest API, use `displayvideo_custom_bidding_algorithms_scripts_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_custom_bidding_algorithms_scripts_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_custom_bidding_algorithms_scripts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCustomBiddingScriptsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_custom_bidding_algorithms_scripts_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_custom_bidding_algorithms_scripts_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoCustomBiddingAlgorithmsScriptsListArgs {
    /// Path parameter: customBiddingAlgorithmId
    pub customBiddingAlgorithmId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// GET v4/customBiddingAlgorithms/{customBiddingAlgorithmsId}/scripts
/// Lists custom bidding scripts that belong to the given algorithm. The order is defined by the order_by parameter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_custom_bidding_algorithms_scripts_list_builder()` + `displayvideo_custom_bidding_algorithms_scripts_list_execute()`.
/// For task-level control, use `displayvideo_custom_bidding_algorithms_scripts_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_custom_bidding_algorithms_scripts_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoCustomBiddingAlgorithmsScriptsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCustomBiddingScriptsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_custom_bidding_algorithms_scripts_list_builder(
        client,
        &args.customBiddingAlgorithmId,
        &args.advertiserId,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.partnerId,
    )?;
    displayvideo_custom_bidding_algorithms_scripts_list_execute(builder)
}

/// GET v4/customLists/{customListsId}
/// Gets a custom list.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_custom_lists_get_execute()` to send, or `displayvideo_custom_lists_get` for simplest API.

pub fn displayvideo_custom_lists_get_builder<R>(
    client: &SimpleHttpClient<R>,
    customListId: &String,
    advertiserId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/customLists/{}",
        customListId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/customLists/{customListsId}
/// Gets a custom list.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_custom_lists_get_execute()` or `displayvideo_custom_lists_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_custom_lists_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_custom_lists_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CustomList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/customLists/{customListsId}
/// Gets a custom list.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_custom_lists_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_custom_lists_get_task()`.
/// For the simplest API, use `displayvideo_custom_lists_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_custom_lists_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_custom_lists_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_custom_lists_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_custom_lists_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoCustomListsGetArgs {
    /// Path parameter: customListId
    pub customListId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
}

/// GET v4/customLists/{customListsId}
/// Gets a custom list.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_custom_lists_get_builder()` + `displayvideo_custom_lists_get_execute()`.
/// For task-level control, use `displayvideo_custom_lists_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_custom_lists_get(
    client: &SimpleHttpClient,
    args: &DisplayvideoCustomListsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        displayvideo_custom_lists_get_builder(client, &args.customListId, &args.advertiserId)?;
    displayvideo_custom_lists_get_execute(builder)
}

/// GET v4/customLists
/// Lists custom lists. The order is defined by the order_by parameter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_custom_lists_list_execute()` to send, or `displayvideo_custom_lists_list` for simplest API.

pub fn displayvideo_custom_lists_list_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &Option<String>,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://displayvideo.googleapis.com/v4/customLists",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/customLists
/// Lists custom lists. The order is defined by the order_by parameter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_custom_lists_list_execute()` or `displayvideo_custom_lists_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_custom_lists_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_custom_lists_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCustomListsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCustomListsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/customLists
/// Lists custom lists. The order is defined by the order_by parameter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_custom_lists_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_custom_lists_list_task()`.
/// For the simplest API, use `displayvideo_custom_lists_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_custom_lists_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_custom_lists_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCustomListsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_custom_lists_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_custom_lists_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoCustomListsListArgs {
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v4/customLists
/// Lists custom lists. The order is defined by the order_by parameter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_custom_lists_list_builder()` + `displayvideo_custom_lists_list_execute()`.
/// For task-level control, use `displayvideo_custom_lists_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_custom_lists_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoCustomListsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCustomListsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_custom_lists_list_builder(
        client,
        &args.advertiserId,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    displayvideo_custom_lists_list_execute(builder)
}

/// POST v4/firstPartyAndPartnerAudiences
/// Creates a FirstPartyAndPartnerAudience. Only supported for the following audience_type: * CUSTOMER_MATCH_CONTACT_INFO * CUSTOMER_MATCH_DEVICE_ID
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_first_party_and_partner_audiences_create_execute()` to send, or `displayvideo_first_party_and_partner_audiences_create` for simplest API.

pub fn displayvideo_first_party_and_partner_audiences_create_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://displayvideo.googleapis.com/v4/firstPartyAndPartnerAudiences",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/firstPartyAndPartnerAudiences
/// Creates a FirstPartyAndPartnerAudience. Only supported for the following audience_type: * CUSTOMER_MATCH_CONTACT_INFO * CUSTOMER_MATCH_DEVICE_ID
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_first_party_and_partner_audiences_create_execute()` or `displayvideo_first_party_and_partner_audiences_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_first_party_and_partner_audiences_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_first_party_and_partner_audiences_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirstPartyAndPartnerAudience>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirstPartyAndPartnerAudience = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/firstPartyAndPartnerAudiences
/// Creates a FirstPartyAndPartnerAudience. Only supported for the following audience_type: * CUSTOMER_MATCH_CONTACT_INFO * CUSTOMER_MATCH_DEVICE_ID
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_first_party_and_partner_audiences_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_first_party_and_partner_audiences_create_task()`.
/// For the simplest API, use `displayvideo_first_party_and_partner_audiences_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_first_party_and_partner_audiences_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_first_party_and_partner_audiences_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FirstPartyAndPartnerAudience>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_first_party_and_partner_audiences_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_first_party_and_partner_audiences_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoFirstPartyAndPartnerAudiencesCreateArgs {
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
}

/// POST v4/firstPartyAndPartnerAudiences
/// Creates a FirstPartyAndPartnerAudience. Only supported for the following audience_type: * CUSTOMER_MATCH_CONTACT_INFO * CUSTOMER_MATCH_DEVICE_ID
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_first_party_and_partner_audiences_create_builder()` + `displayvideo_first_party_and_partner_audiences_create_execute()`.
/// For task-level control, use `displayvideo_first_party_and_partner_audiences_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_first_party_and_partner_audiences_create(
    client: &SimpleHttpClient,
    args: &DisplayvideoFirstPartyAndPartnerAudiencesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FirstPartyAndPartnerAudience>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        displayvideo_first_party_and_partner_audiences_create_builder(client, &args.advertiserId)?;
    displayvideo_first_party_and_partner_audiences_create_execute(builder)
}

/// POST v4/firstPartyAndPartnerAudiences/{firstPartyAndPartnerAudiencesId}:editCustomerMatchMembers
/// Updates the member list of a Customer Match audience. Only supported for the following audience_type: * CUSTOMER_MATCH_CONTACT_INFO * CUSTOMER_MATCH_DEVICE_ID
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_first_party_and_partner_audiences_edit_customer_match_members_execute()` to send, or `displayvideo_first_party_and_partner_audiences_edit_customer_match_members` for simplest API.

pub fn displayvideo_first_party_and_partner_audiences_edit_customer_match_members_builder<R>(
    client: &SimpleHttpClient<R>,
    firstPartyAndPartnerAudienceId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/firstPartyAndPartnerAudiences/{}:editCustomerMatchMembers",
        firstPartyAndPartnerAudienceId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/firstPartyAndPartnerAudiences/{firstPartyAndPartnerAudiencesId}:editCustomerMatchMembers
/// Updates the member list of a Customer Match audience. Only supported for the following audience_type: * CUSTOMER_MATCH_CONTACT_INFO * CUSTOMER_MATCH_DEVICE_ID
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_first_party_and_partner_audiences_edit_customer_match_members_execute()` or `displayvideo_first_party_and_partner_audiences_edit_customer_match_members`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_first_party_and_partner_audiences_edit_customer_match_members_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_first_party_and_partner_audiences_edit_customer_match_members_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EditCustomerMatchMembersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EditCustomerMatchMembersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/firstPartyAndPartnerAudiences/{firstPartyAndPartnerAudiencesId}:editCustomerMatchMembers
/// Updates the member list of a Customer Match audience. Only supported for the following audience_type: * CUSTOMER_MATCH_CONTACT_INFO * CUSTOMER_MATCH_DEVICE_ID
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_first_party_and_partner_audiences_edit_customer_match_members_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_first_party_and_partner_audiences_edit_customer_match_members_task()`.
/// For the simplest API, use `displayvideo_first_party_and_partner_audiences_edit_customer_match_members()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_first_party_and_partner_audiences_edit_customer_match_members_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_first_party_and_partner_audiences_edit_customer_match_members_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EditCustomerMatchMembersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        displayvideo_first_party_and_partner_audiences_edit_customer_match_members_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_first_party_and_partner_audiences_edit_customer_match_members`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoFirstPartyAndPartnerAudiencesEditCustomerMatchMembersArgs {
    /// Path parameter: firstPartyAndPartnerAudienceId
    pub firstPartyAndPartnerAudienceId: String,
}

/// POST v4/firstPartyAndPartnerAudiences/{firstPartyAndPartnerAudiencesId}:editCustomerMatchMembers
/// Updates the member list of a Customer Match audience. Only supported for the following audience_type: * CUSTOMER_MATCH_CONTACT_INFO * CUSTOMER_MATCH_DEVICE_ID
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_first_party_and_partner_audiences_edit_customer_match_members_builder()` + `displayvideo_first_party_and_partner_audiences_edit_customer_match_members_execute()`.
/// For task-level control, use `displayvideo_first_party_and_partner_audiences_edit_customer_match_members_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_first_party_and_partner_audiences_edit_customer_match_members(
    client: &SimpleHttpClient,
    args: &DisplayvideoFirstPartyAndPartnerAudiencesEditCustomerMatchMembersArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EditCustomerMatchMembersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        displayvideo_first_party_and_partner_audiences_edit_customer_match_members_builder(
            client,
            &args.firstPartyAndPartnerAudienceId,
        )?;
    displayvideo_first_party_and_partner_audiences_edit_customer_match_members_execute(builder)
}

/// GET v4/firstPartyAndPartnerAudiences/{firstPartyAndPartnerAudiencesId}
/// Gets a first party or partner audience.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_first_party_and_partner_audiences_get_execute()` to send, or `displayvideo_first_party_and_partner_audiences_get` for simplest API.

pub fn displayvideo_first_party_and_partner_audiences_get_builder<R>(
    client: &SimpleHttpClient<R>,
    firstPartyAndPartnerAudienceId: &String,
    advertiserId: &Option<String>,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/firstPartyAndPartnerAudiences/{}",
        firstPartyAndPartnerAudienceId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/firstPartyAndPartnerAudiences/{firstPartyAndPartnerAudiencesId}
/// Gets a first party or partner audience.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_first_party_and_partner_audiences_get_execute()` or `displayvideo_first_party_and_partner_audiences_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_first_party_and_partner_audiences_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_first_party_and_partner_audiences_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirstPartyAndPartnerAudience>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirstPartyAndPartnerAudience = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/firstPartyAndPartnerAudiences/{firstPartyAndPartnerAudiencesId}
/// Gets a first party or partner audience.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_first_party_and_partner_audiences_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_first_party_and_partner_audiences_get_task()`.
/// For the simplest API, use `displayvideo_first_party_and_partner_audiences_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_first_party_and_partner_audiences_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_first_party_and_partner_audiences_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FirstPartyAndPartnerAudience>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_first_party_and_partner_audiences_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_first_party_and_partner_audiences_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoFirstPartyAndPartnerAudiencesGetArgs {
    /// Path parameter: firstPartyAndPartnerAudienceId
    pub firstPartyAndPartnerAudienceId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// GET v4/firstPartyAndPartnerAudiences/{firstPartyAndPartnerAudiencesId}
/// Gets a first party or partner audience.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_first_party_and_partner_audiences_get_builder()` + `displayvideo_first_party_and_partner_audiences_get_execute()`.
/// For task-level control, use `displayvideo_first_party_and_partner_audiences_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_first_party_and_partner_audiences_get(
    client: &SimpleHttpClient,
    args: &DisplayvideoFirstPartyAndPartnerAudiencesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FirstPartyAndPartnerAudience>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_first_party_and_partner_audiences_get_builder(
        client,
        &args.firstPartyAndPartnerAudienceId,
        &args.advertiserId,
        &args.partnerId,
    )?;
    displayvideo_first_party_and_partner_audiences_get_execute(builder)
}

/// GET v4/firstPartyAndPartnerAudiences
/// Lists first party and partner audiences. The order is defined by the order_by parameter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_first_party_and_partner_audiences_list_execute()` to send, or `displayvideo_first_party_and_partner_audiences_list` for simplest API.

pub fn displayvideo_first_party_and_partner_audiences_list_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &Option<String>,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://displayvideo.googleapis.com/v4/firstPartyAndPartnerAudiences",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/firstPartyAndPartnerAudiences
/// Lists first party and partner audiences. The order is defined by the order_by parameter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_first_party_and_partner_audiences_list_execute()` or `displayvideo_first_party_and_partner_audiences_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_first_party_and_partner_audiences_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_first_party_and_partner_audiences_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListFirstPartyAndPartnerAudiencesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListFirstPartyAndPartnerAudiencesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/firstPartyAndPartnerAudiences
/// Lists first party and partner audiences. The order is defined by the order_by parameter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_first_party_and_partner_audiences_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_first_party_and_partner_audiences_list_task()`.
/// For the simplest API, use `displayvideo_first_party_and_partner_audiences_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_first_party_and_partner_audiences_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_first_party_and_partner_audiences_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListFirstPartyAndPartnerAudiencesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_first_party_and_partner_audiences_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_first_party_and_partner_audiences_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoFirstPartyAndPartnerAudiencesListArgs {
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// GET v4/firstPartyAndPartnerAudiences
/// Lists first party and partner audiences. The order is defined by the order_by parameter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_first_party_and_partner_audiences_list_builder()` + `displayvideo_first_party_and_partner_audiences_list_execute()`.
/// For task-level control, use `displayvideo_first_party_and_partner_audiences_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_first_party_and_partner_audiences_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoFirstPartyAndPartnerAudiencesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListFirstPartyAndPartnerAudiencesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_first_party_and_partner_audiences_list_builder(
        client,
        &args.advertiserId,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.partnerId,
    )?;
    displayvideo_first_party_and_partner_audiences_list_execute(builder)
}

/// PATCH v4/firstPartyAndPartnerAudiences/{firstPartyAndPartnerAudiencesId}
/// Updates an existing FirstPartyAndPartnerAudience. Only supported for the following audience_type: * CUSTOMER_MATCH_CONTACT_INFO * CUSTOMER_MATCH_DEVICE_ID
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_first_party_and_partner_audiences_patch_execute()` to send, or `displayvideo_first_party_and_partner_audiences_patch` for simplest API.

pub fn displayvideo_first_party_and_partner_audiences_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    firstPartyAndPartnerAudienceId: &String,
    advertiserId: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/firstPartyAndPartnerAudiences/{}",
        firstPartyAndPartnerAudienceId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v4/firstPartyAndPartnerAudiences/{firstPartyAndPartnerAudiencesId}
/// Updates an existing FirstPartyAndPartnerAudience. Only supported for the following audience_type: * CUSTOMER_MATCH_CONTACT_INFO * CUSTOMER_MATCH_DEVICE_ID
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_first_party_and_partner_audiences_patch_execute()` or `displayvideo_first_party_and_partner_audiences_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_first_party_and_partner_audiences_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_first_party_and_partner_audiences_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FirstPartyAndPartnerAudience>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FirstPartyAndPartnerAudience = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v4/firstPartyAndPartnerAudiences/{firstPartyAndPartnerAudiencesId}
/// Updates an existing FirstPartyAndPartnerAudience. Only supported for the following audience_type: * CUSTOMER_MATCH_CONTACT_INFO * CUSTOMER_MATCH_DEVICE_ID
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_first_party_and_partner_audiences_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_first_party_and_partner_audiences_patch_task()`.
/// For the simplest API, use `displayvideo_first_party_and_partner_audiences_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_first_party_and_partner_audiences_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_first_party_and_partner_audiences_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FirstPartyAndPartnerAudience>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_first_party_and_partner_audiences_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_first_party_and_partner_audiences_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoFirstPartyAndPartnerAudiencesPatchArgs {
    /// Path parameter: firstPartyAndPartnerAudienceId
    pub firstPartyAndPartnerAudienceId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v4/firstPartyAndPartnerAudiences/{firstPartyAndPartnerAudiencesId}
/// Updates an existing FirstPartyAndPartnerAudience. Only supported for the following audience_type: * CUSTOMER_MATCH_CONTACT_INFO * CUSTOMER_MATCH_DEVICE_ID
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_first_party_and_partner_audiences_patch_builder()` + `displayvideo_first_party_and_partner_audiences_patch_execute()`.
/// For task-level control, use `displayvideo_first_party_and_partner_audiences_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_first_party_and_partner_audiences_patch(
    client: &SimpleHttpClient,
    args: &DisplayvideoFirstPartyAndPartnerAudiencesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FirstPartyAndPartnerAudience>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_first_party_and_partner_audiences_patch_builder(
        client,
        &args.firstPartyAndPartnerAudienceId,
        &args.advertiserId,
        &args.updateMask,
    )?;
    displayvideo_first_party_and_partner_audiences_patch_execute(builder)
}

/// GET v4/floodlightGroups/{floodlightGroupsId}
/// Gets a Floodlight group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_floodlight_groups_get_execute()` to send, or `displayvideo_floodlight_groups_get` for simplest API.

pub fn displayvideo_floodlight_groups_get_builder<R>(
    client: &SimpleHttpClient<R>,
    floodlightGroupId: &String,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/floodlightGroups/{}",
        floodlightGroupId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/floodlightGroups/{floodlightGroupsId}
/// Gets a Floodlight group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_floodlight_groups_get_execute()` or `displayvideo_floodlight_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_floodlight_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_floodlight_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FloodlightGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FloodlightGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/floodlightGroups/{floodlightGroupsId}
/// Gets a Floodlight group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_floodlight_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_floodlight_groups_get_task()`.
/// For the simplest API, use `displayvideo_floodlight_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_floodlight_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_floodlight_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FloodlightGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_floodlight_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_floodlight_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoFloodlightGroupsGetArgs {
    /// Path parameter: floodlightGroupId
    pub floodlightGroupId: String,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// GET v4/floodlightGroups/{floodlightGroupsId}
/// Gets a Floodlight group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_floodlight_groups_get_builder()` + `displayvideo_floodlight_groups_get_execute()`.
/// For task-level control, use `displayvideo_floodlight_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_floodlight_groups_get(
    client: &SimpleHttpClient,
    args: &DisplayvideoFloodlightGroupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FloodlightGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_floodlight_groups_get_builder(
        client,
        &args.floodlightGroupId,
        &args.partnerId,
    )?;
    displayvideo_floodlight_groups_get_execute(builder)
}

/// PATCH v4/floodlightGroups/{floodlightGroupId}
/// Updates an existing Floodlight group. Returns the updated Floodlight group if successful.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_floodlight_groups_patch_execute()` to send, or `displayvideo_floodlight_groups_patch` for simplest API.

pub fn displayvideo_floodlight_groups_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    floodlightGroupId: &String,
    partnerId: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/floodlightGroups/{}",
        floodlightGroupId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v4/floodlightGroups/{floodlightGroupId}
/// Updates an existing Floodlight group. Returns the updated Floodlight group if successful.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_floodlight_groups_patch_execute()` or `displayvideo_floodlight_groups_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_floodlight_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_floodlight_groups_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FloodlightGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FloodlightGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v4/floodlightGroups/{floodlightGroupId}
/// Updates an existing Floodlight group. Returns the updated Floodlight group if successful.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_floodlight_groups_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_floodlight_groups_patch_task()`.
/// For the simplest API, use `displayvideo_floodlight_groups_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_floodlight_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_floodlight_groups_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FloodlightGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_floodlight_groups_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_floodlight_groups_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoFloodlightGroupsPatchArgs {
    /// Path parameter: floodlightGroupId
    pub floodlightGroupId: String,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v4/floodlightGroups/{floodlightGroupId}
/// Updates an existing Floodlight group. Returns the updated Floodlight group if successful.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_floodlight_groups_patch_builder()` + `displayvideo_floodlight_groups_patch_execute()`.
/// For task-level control, use `displayvideo_floodlight_groups_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_floodlight_groups_patch(
    client: &SimpleHttpClient,
    args: &DisplayvideoFloodlightGroupsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FloodlightGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_floodlight_groups_patch_builder(
        client,
        &args.floodlightGroupId,
        &args.partnerId,
        &args.updateMask,
    )?;
    displayvideo_floodlight_groups_patch_execute(builder)
}

/// GET v4/floodlightGroups/{floodlightGroupsId}/floodlightActivities/{floodlightActivitiesId}
/// Gets a Floodlight activity.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_floodlight_groups_floodlight_activities_get_execute()` to send, or `displayvideo_floodlight_groups_floodlight_activities_get` for simplest API.

pub fn displayvideo_floodlight_groups_floodlight_activities_get_builder<R>(
    client: &SimpleHttpClient<R>,
    floodlightGroupId: &String,
    floodlightActivityId: &String,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/floodlightGroups/{}/floodlightActivities/{}",
        floodlightGroupId, floodlightActivityId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/floodlightGroups/{floodlightGroupsId}/floodlightActivities/{floodlightActivitiesId}
/// Gets a Floodlight activity.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_floodlight_groups_floodlight_activities_get_execute()` or `displayvideo_floodlight_groups_floodlight_activities_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_floodlight_groups_floodlight_activities_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_floodlight_groups_floodlight_activities_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FloodlightActivity>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FloodlightActivity = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/floodlightGroups/{floodlightGroupsId}/floodlightActivities/{floodlightActivitiesId}
/// Gets a Floodlight activity.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_floodlight_groups_floodlight_activities_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_floodlight_groups_floodlight_activities_get_task()`.
/// For the simplest API, use `displayvideo_floodlight_groups_floodlight_activities_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_floodlight_groups_floodlight_activities_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_floodlight_groups_floodlight_activities_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FloodlightActivity>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_floodlight_groups_floodlight_activities_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_floodlight_groups_floodlight_activities_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoFloodlightGroupsFloodlightActivitiesGetArgs {
    /// Path parameter: floodlightGroupId
    pub floodlightGroupId: String,
    /// Path parameter: floodlightActivityId
    pub floodlightActivityId: String,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// GET v4/floodlightGroups/{floodlightGroupsId}/floodlightActivities/{floodlightActivitiesId}
/// Gets a Floodlight activity.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_floodlight_groups_floodlight_activities_get_builder()` + `displayvideo_floodlight_groups_floodlight_activities_get_execute()`.
/// For task-level control, use `displayvideo_floodlight_groups_floodlight_activities_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_floodlight_groups_floodlight_activities_get(
    client: &SimpleHttpClient,
    args: &DisplayvideoFloodlightGroupsFloodlightActivitiesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FloodlightActivity>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_floodlight_groups_floodlight_activities_get_builder(
        client,
        &args.floodlightGroupId,
        &args.floodlightActivityId,
        &args.partnerId,
    )?;
    displayvideo_floodlight_groups_floodlight_activities_get_execute(builder)
}

/// GET v4/floodlightGroups/{floodlightGroupsId}/floodlightActivities
/// Lists Floodlight activities in a Floodlight group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_floodlight_groups_floodlight_activities_list_execute()` to send, or `displayvideo_floodlight_groups_floodlight_activities_list` for simplest API.

pub fn displayvideo_floodlight_groups_floodlight_activities_list_builder<R>(
    client: &SimpleHttpClient<R>,
    floodlightGroupId: &String,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/floodlightGroups/{}/floodlightActivities",
        floodlightGroupId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/floodlightGroups/{floodlightGroupsId}/floodlightActivities
/// Lists Floodlight activities in a Floodlight group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_floodlight_groups_floodlight_activities_list_execute()` or `displayvideo_floodlight_groups_floodlight_activities_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_floodlight_groups_floodlight_activities_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_floodlight_groups_floodlight_activities_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListFloodlightActivitiesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListFloodlightActivitiesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/floodlightGroups/{floodlightGroupsId}/floodlightActivities
/// Lists Floodlight activities in a Floodlight group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_floodlight_groups_floodlight_activities_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_floodlight_groups_floodlight_activities_list_task()`.
/// For the simplest API, use `displayvideo_floodlight_groups_floodlight_activities_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_floodlight_groups_floodlight_activities_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_floodlight_groups_floodlight_activities_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListFloodlightActivitiesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_floodlight_groups_floodlight_activities_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_floodlight_groups_floodlight_activities_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoFloodlightGroupsFloodlightActivitiesListArgs {
    /// Path parameter: floodlightGroupId
    pub floodlightGroupId: String,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// GET v4/floodlightGroups/{floodlightGroupsId}/floodlightActivities
/// Lists Floodlight activities in a Floodlight group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_floodlight_groups_floodlight_activities_list_builder()` + `displayvideo_floodlight_groups_floodlight_activities_list_execute()`.
/// For task-level control, use `displayvideo_floodlight_groups_floodlight_activities_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_floodlight_groups_floodlight_activities_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoFloodlightGroupsFloodlightActivitiesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListFloodlightActivitiesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_floodlight_groups_floodlight_activities_list_builder(
        client,
        &args.floodlightGroupId,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.partnerId,
    )?;
    displayvideo_floodlight_groups_floodlight_activities_list_execute(builder)
}

/// GET v4/googleAudiences/{googleAudiencesId}
/// Gets a Google audience.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_google_audiences_get_execute()` to send, or `displayvideo_google_audiences_get` for simplest API.

pub fn displayvideo_google_audiences_get_builder<R>(
    client: &SimpleHttpClient<R>,
    googleAudienceId: &String,
    advertiserId: &Option<String>,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/googleAudiences/{}",
        googleAudienceId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/googleAudiences/{googleAudiencesId}
/// Gets a Google audience.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_google_audiences_get_execute()` or `displayvideo_google_audiences_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_google_audiences_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_google_audiences_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleAudience>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAudience = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/googleAudiences/{googleAudiencesId}
/// Gets a Google audience.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_google_audiences_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_google_audiences_get_task()`.
/// For the simplest API, use `displayvideo_google_audiences_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_google_audiences_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_google_audiences_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleAudience>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_google_audiences_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_google_audiences_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoGoogleAudiencesGetArgs {
    /// Path parameter: googleAudienceId
    pub googleAudienceId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// GET v4/googleAudiences/{googleAudiencesId}
/// Gets a Google audience.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_google_audiences_get_builder()` + `displayvideo_google_audiences_get_execute()`.
/// For task-level control, use `displayvideo_google_audiences_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_google_audiences_get(
    client: &SimpleHttpClient,
    args: &DisplayvideoGoogleAudiencesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleAudience>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_google_audiences_get_builder(
        client,
        &args.googleAudienceId,
        &args.advertiserId,
        &args.partnerId,
    )?;
    displayvideo_google_audiences_get_execute(builder)
}

/// GET v4/googleAudiences
/// Lists Google audiences. The order is defined by the order_by parameter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_google_audiences_list_execute()` to send, or `displayvideo_google_audiences_list` for simplest API.

pub fn displayvideo_google_audiences_list_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &Option<String>,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://displayvideo.googleapis.com/v4/googleAudiences",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/googleAudiences
/// Lists Google audiences. The order is defined by the order_by parameter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_google_audiences_list_execute()` or `displayvideo_google_audiences_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_google_audiences_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_google_audiences_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListGoogleAudiencesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListGoogleAudiencesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/googleAudiences
/// Lists Google audiences. The order is defined by the order_by parameter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_google_audiences_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_google_audiences_list_task()`.
/// For the simplest API, use `displayvideo_google_audiences_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_google_audiences_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_google_audiences_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListGoogleAudiencesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_google_audiences_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_google_audiences_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoGoogleAudiencesListArgs {
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// GET v4/googleAudiences
/// Lists Google audiences. The order is defined by the order_by parameter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_google_audiences_list_builder()` + `displayvideo_google_audiences_list_execute()`.
/// For task-level control, use `displayvideo_google_audiences_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_google_audiences_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoGoogleAudiencesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListGoogleAudiencesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_google_audiences_list_builder(
        client,
        &args.advertiserId,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.partnerId,
    )?;
    displayvideo_google_audiences_list_execute(builder)
}

/// POST v4/guaranteedOrders
/// Creates a new guaranteed order. Returns the newly created guaranteed order if successful.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_guaranteed_orders_create_execute()` to send, or `displayvideo_guaranteed_orders_create` for simplest API.

pub fn displayvideo_guaranteed_orders_create_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &Option<String>,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://displayvideo.googleapis.com/v4/guaranteedOrders",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/guaranteedOrders
/// Creates a new guaranteed order. Returns the newly created guaranteed order if successful.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_guaranteed_orders_create_execute()` or `displayvideo_guaranteed_orders_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_guaranteed_orders_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_guaranteed_orders_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GuaranteedOrder>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GuaranteedOrder = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/guaranteedOrders
/// Creates a new guaranteed order. Returns the newly created guaranteed order if successful.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_guaranteed_orders_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_guaranteed_orders_create_task()`.
/// For the simplest API, use `displayvideo_guaranteed_orders_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_guaranteed_orders_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_guaranteed_orders_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GuaranteedOrder>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_guaranteed_orders_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_guaranteed_orders_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoGuaranteedOrdersCreateArgs {
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// POST v4/guaranteedOrders
/// Creates a new guaranteed order. Returns the newly created guaranteed order if successful.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_guaranteed_orders_create_builder()` + `displayvideo_guaranteed_orders_create_execute()`.
/// For task-level control, use `displayvideo_guaranteed_orders_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_guaranteed_orders_create(
    client: &SimpleHttpClient,
    args: &DisplayvideoGuaranteedOrdersCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GuaranteedOrder>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        displayvideo_guaranteed_orders_create_builder(client, &args.advertiserId, &args.partnerId)?;
    displayvideo_guaranteed_orders_create_execute(builder)
}

/// POST v4/guaranteedOrders/{guaranteedOrdersId}:editGuaranteedOrderReadAccessors
/// Edits read advertisers of a guaranteed order.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_guaranteed_orders_edit_guaranteed_order_read_accessors_execute()` to send, or `displayvideo_guaranteed_orders_edit_guaranteed_order_read_accessors` for simplest API.

pub fn displayvideo_guaranteed_orders_edit_guaranteed_order_read_accessors_builder<R>(
    client: &SimpleHttpClient<R>,
    guaranteedOrderId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/guaranteedOrders/{}:editGuaranteedOrderReadAccessors",
        guaranteedOrderId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/guaranteedOrders/{guaranteedOrdersId}:editGuaranteedOrderReadAccessors
/// Edits read advertisers of a guaranteed order.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_guaranteed_orders_edit_guaranteed_order_read_accessors_execute()` or `displayvideo_guaranteed_orders_edit_guaranteed_order_read_accessors`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_guaranteed_orders_edit_guaranteed_order_read_accessors_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_guaranteed_orders_edit_guaranteed_order_read_accessors_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<EditGuaranteedOrderReadAccessorsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EditGuaranteedOrderReadAccessorsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/guaranteedOrders/{guaranteedOrdersId}:editGuaranteedOrderReadAccessors
/// Edits read advertisers of a guaranteed order.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_guaranteed_orders_edit_guaranteed_order_read_accessors_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_guaranteed_orders_edit_guaranteed_order_read_accessors_task()`.
/// For the simplest API, use `displayvideo_guaranteed_orders_edit_guaranteed_order_read_accessors()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_guaranteed_orders_edit_guaranteed_order_read_accessors_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_guaranteed_orders_edit_guaranteed_order_read_accessors_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EditGuaranteedOrderReadAccessorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_guaranteed_orders_edit_guaranteed_order_read_accessors_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_guaranteed_orders_edit_guaranteed_order_read_accessors`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoGuaranteedOrdersEditGuaranteedOrderReadAccessorsArgs {
    /// Path parameter: guaranteedOrderId
    pub guaranteedOrderId: String,
}

/// POST v4/guaranteedOrders/{guaranteedOrdersId}:editGuaranteedOrderReadAccessors
/// Edits read advertisers of a guaranteed order.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_guaranteed_orders_edit_guaranteed_order_read_accessors_builder()` + `displayvideo_guaranteed_orders_edit_guaranteed_order_read_accessors_execute()`.
/// For task-level control, use `displayvideo_guaranteed_orders_edit_guaranteed_order_read_accessors_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_guaranteed_orders_edit_guaranteed_order_read_accessors(
    client: &SimpleHttpClient,
    args: &DisplayvideoGuaranteedOrdersEditGuaranteedOrderReadAccessorsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<EditGuaranteedOrderReadAccessorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_guaranteed_orders_edit_guaranteed_order_read_accessors_builder(
        client,
        &args.guaranteedOrderId,
    )?;
    displayvideo_guaranteed_orders_edit_guaranteed_order_read_accessors_execute(builder)
}

/// GET v4/guaranteedOrders/{guaranteedOrdersId}
/// Gets a guaranteed order.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_guaranteed_orders_get_execute()` to send, or `displayvideo_guaranteed_orders_get` for simplest API.

pub fn displayvideo_guaranteed_orders_get_builder<R>(
    client: &SimpleHttpClient<R>,
    guaranteedOrderId: &String,
    advertiserId: &Option<String>,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/guaranteedOrders/{}",
        guaranteedOrderId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/guaranteedOrders/{guaranteedOrdersId}
/// Gets a guaranteed order.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_guaranteed_orders_get_execute()` or `displayvideo_guaranteed_orders_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_guaranteed_orders_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_guaranteed_orders_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GuaranteedOrder>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GuaranteedOrder = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/guaranteedOrders/{guaranteedOrdersId}
/// Gets a guaranteed order.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_guaranteed_orders_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_guaranteed_orders_get_task()`.
/// For the simplest API, use `displayvideo_guaranteed_orders_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_guaranteed_orders_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_guaranteed_orders_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GuaranteedOrder>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_guaranteed_orders_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_guaranteed_orders_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoGuaranteedOrdersGetArgs {
    /// Path parameter: guaranteedOrderId
    pub guaranteedOrderId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// GET v4/guaranteedOrders/{guaranteedOrdersId}
/// Gets a guaranteed order.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_guaranteed_orders_get_builder()` + `displayvideo_guaranteed_orders_get_execute()`.
/// For task-level control, use `displayvideo_guaranteed_orders_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_guaranteed_orders_get(
    client: &SimpleHttpClient,
    args: &DisplayvideoGuaranteedOrdersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GuaranteedOrder>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_guaranteed_orders_get_builder(
        client,
        &args.guaranteedOrderId,
        &args.advertiserId,
        &args.partnerId,
    )?;
    displayvideo_guaranteed_orders_get_execute(builder)
}

/// GET v4/guaranteedOrders
/// Lists guaranteed orders that are accessible to the current user. The order is defined by the order_by parameter. If a filter by entity_status is not specified, guaranteed orders with entity status ENTITY_STATUS_ARCHIVED will not be included in the results.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_guaranteed_orders_list_execute()` to send, or `displayvideo_guaranteed_orders_list` for simplest API.

pub fn displayvideo_guaranteed_orders_list_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &Option<String>,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://displayvideo.googleapis.com/v4/guaranteedOrders",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/guaranteedOrders
/// Lists guaranteed orders that are accessible to the current user. The order is defined by the order_by parameter. If a filter by entity_status is not specified, guaranteed orders with entity status ENTITY_STATUS_ARCHIVED will not be included in the results.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_guaranteed_orders_list_execute()` or `displayvideo_guaranteed_orders_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_guaranteed_orders_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_guaranteed_orders_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListGuaranteedOrdersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListGuaranteedOrdersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/guaranteedOrders
/// Lists guaranteed orders that are accessible to the current user. The order is defined by the order_by parameter. If a filter by entity_status is not specified, guaranteed orders with entity status ENTITY_STATUS_ARCHIVED will not be included in the results.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_guaranteed_orders_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_guaranteed_orders_list_task()`.
/// For the simplest API, use `displayvideo_guaranteed_orders_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_guaranteed_orders_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_guaranteed_orders_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListGuaranteedOrdersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_guaranteed_orders_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_guaranteed_orders_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoGuaranteedOrdersListArgs {
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// GET v4/guaranteedOrders
/// Lists guaranteed orders that are accessible to the current user. The order is defined by the order_by parameter. If a filter by entity_status is not specified, guaranteed orders with entity status ENTITY_STATUS_ARCHIVED will not be included in the results.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_guaranteed_orders_list_builder()` + `displayvideo_guaranteed_orders_list_execute()`.
/// For task-level control, use `displayvideo_guaranteed_orders_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_guaranteed_orders_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoGuaranteedOrdersListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListGuaranteedOrdersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_guaranteed_orders_list_builder(
        client,
        &args.advertiserId,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.partnerId,
    )?;
    displayvideo_guaranteed_orders_list_execute(builder)
}

/// PATCH v4/guaranteedOrders/{guaranteedOrdersId}
/// Updates an existing guaranteed order. Returns the updated guaranteed order if successful.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_guaranteed_orders_patch_execute()` to send, or `displayvideo_guaranteed_orders_patch` for simplest API.

pub fn displayvideo_guaranteed_orders_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    guaranteedOrderId: &String,
    advertiserId: &Option<String>,
    partnerId: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/guaranteedOrders/{}",
        guaranteedOrderId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v4/guaranteedOrders/{guaranteedOrdersId}
/// Updates an existing guaranteed order. Returns the updated guaranteed order if successful.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_guaranteed_orders_patch_execute()` or `displayvideo_guaranteed_orders_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_guaranteed_orders_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_guaranteed_orders_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GuaranteedOrder>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GuaranteedOrder = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v4/guaranteedOrders/{guaranteedOrdersId}
/// Updates an existing guaranteed order. Returns the updated guaranteed order if successful.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_guaranteed_orders_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_guaranteed_orders_patch_task()`.
/// For the simplest API, use `displayvideo_guaranteed_orders_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_guaranteed_orders_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_guaranteed_orders_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GuaranteedOrder>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_guaranteed_orders_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_guaranteed_orders_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoGuaranteedOrdersPatchArgs {
    /// Path parameter: guaranteedOrderId
    pub guaranteedOrderId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v4/guaranteedOrders/{guaranteedOrdersId}
/// Updates an existing guaranteed order. Returns the updated guaranteed order if successful.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_guaranteed_orders_patch_builder()` + `displayvideo_guaranteed_orders_patch_execute()`.
/// For task-level control, use `displayvideo_guaranteed_orders_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_guaranteed_orders_patch(
    client: &SimpleHttpClient,
    args: &DisplayvideoGuaranteedOrdersPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GuaranteedOrder>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_guaranteed_orders_patch_builder(
        client,
        &args.guaranteedOrderId,
        &args.advertiserId,
        &args.partnerId,
        &args.updateMask,
    )?;
    displayvideo_guaranteed_orders_patch_execute(builder)
}

/// POST v4/inventorySourceGroups
/// Creates a new inventory source group. Returns the newly created inventory source group if successful.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_inventory_source_groups_create_execute()` to send, or `displayvideo_inventory_source_groups_create` for simplest API.

pub fn displayvideo_inventory_source_groups_create_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &Option<String>,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://displayvideo.googleapis.com/v4/inventorySourceGroups",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/inventorySourceGroups
/// Creates a new inventory source group. Returns the newly created inventory source group if successful.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_inventory_source_groups_create_execute()` or `displayvideo_inventory_source_groups_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_inventory_source_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_inventory_source_groups_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InventorySourceGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InventorySourceGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/inventorySourceGroups
/// Creates a new inventory source group. Returns the newly created inventory source group if successful.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_inventory_source_groups_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_inventory_source_groups_create_task()`.
/// For the simplest API, use `displayvideo_inventory_source_groups_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_inventory_source_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_inventory_source_groups_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InventorySourceGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_inventory_source_groups_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_inventory_source_groups_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoInventorySourceGroupsCreateArgs {
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// POST v4/inventorySourceGroups
/// Creates a new inventory source group. Returns the newly created inventory source group if successful.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_inventory_source_groups_create_builder()` + `displayvideo_inventory_source_groups_create_execute()`.
/// For task-level control, use `displayvideo_inventory_source_groups_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_inventory_source_groups_create(
    client: &SimpleHttpClient,
    args: &DisplayvideoInventorySourceGroupsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InventorySourceGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_inventory_source_groups_create_builder(
        client,
        &args.advertiserId,
        &args.partnerId,
    )?;
    displayvideo_inventory_source_groups_create_execute(builder)
}

/// DELETE v4/inventorySourceGroups/{inventorySourceGroupsId}
/// Deletes an inventory source group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_inventory_source_groups_delete_execute()` to send, or `displayvideo_inventory_source_groups_delete` for simplest API.

pub fn displayvideo_inventory_source_groups_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    inventorySourceGroupId: &String,
    advertiserId: &Option<String>,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/inventorySourceGroups/{}",
        inventorySourceGroupId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v4/inventorySourceGroups/{inventorySourceGroupsId}
/// Deletes an inventory source group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_inventory_source_groups_delete_execute()` or `displayvideo_inventory_source_groups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_inventory_source_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_inventory_source_groups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v4/inventorySourceGroups/{inventorySourceGroupsId}
/// Deletes an inventory source group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_inventory_source_groups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_inventory_source_groups_delete_task()`.
/// For the simplest API, use `displayvideo_inventory_source_groups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_inventory_source_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_inventory_source_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_inventory_source_groups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_inventory_source_groups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoInventorySourceGroupsDeleteArgs {
    /// Path parameter: inventorySourceGroupId
    pub inventorySourceGroupId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// DELETE v4/inventorySourceGroups/{inventorySourceGroupsId}
/// Deletes an inventory source group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_inventory_source_groups_delete_builder()` + `displayvideo_inventory_source_groups_delete_execute()`.
/// For task-level control, use `displayvideo_inventory_source_groups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_inventory_source_groups_delete(
    client: &SimpleHttpClient,
    args: &DisplayvideoInventorySourceGroupsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_inventory_source_groups_delete_builder(
        client,
        &args.inventorySourceGroupId,
        &args.advertiserId,
        &args.partnerId,
    )?;
    displayvideo_inventory_source_groups_delete_execute(builder)
}

/// GET v4/inventorySourceGroups/{inventorySourceGroupsId}
/// Gets an inventory source group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_inventory_source_groups_get_execute()` to send, or `displayvideo_inventory_source_groups_get` for simplest API.

pub fn displayvideo_inventory_source_groups_get_builder<R>(
    client: &SimpleHttpClient<R>,
    inventorySourceGroupId: &String,
    advertiserId: &Option<String>,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/inventorySourceGroups/{}",
        inventorySourceGroupId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/inventorySourceGroups/{inventorySourceGroupsId}
/// Gets an inventory source group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_inventory_source_groups_get_execute()` or `displayvideo_inventory_source_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_inventory_source_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_inventory_source_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InventorySourceGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InventorySourceGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/inventorySourceGroups/{inventorySourceGroupsId}
/// Gets an inventory source group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_inventory_source_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_inventory_source_groups_get_task()`.
/// For the simplest API, use `displayvideo_inventory_source_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_inventory_source_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_inventory_source_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InventorySourceGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_inventory_source_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_inventory_source_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoInventorySourceGroupsGetArgs {
    /// Path parameter: inventorySourceGroupId
    pub inventorySourceGroupId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// GET v4/inventorySourceGroups/{inventorySourceGroupsId}
/// Gets an inventory source group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_inventory_source_groups_get_builder()` + `displayvideo_inventory_source_groups_get_execute()`.
/// For task-level control, use `displayvideo_inventory_source_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_inventory_source_groups_get(
    client: &SimpleHttpClient,
    args: &DisplayvideoInventorySourceGroupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InventorySourceGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_inventory_source_groups_get_builder(
        client,
        &args.inventorySourceGroupId,
        &args.advertiserId,
        &args.partnerId,
    )?;
    displayvideo_inventory_source_groups_get_execute(builder)
}

/// GET v4/inventorySourceGroups
/// Lists inventory source groups that are accessible to the current user. The order is defined by the order_by parameter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_inventory_source_groups_list_execute()` to send, or `displayvideo_inventory_source_groups_list` for simplest API.

pub fn displayvideo_inventory_source_groups_list_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &Option<String>,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://displayvideo.googleapis.com/v4/inventorySourceGroups",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/inventorySourceGroups
/// Lists inventory source groups that are accessible to the current user. The order is defined by the order_by parameter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_inventory_source_groups_list_execute()` or `displayvideo_inventory_source_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_inventory_source_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_inventory_source_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListInventorySourceGroupsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListInventorySourceGroupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/inventorySourceGroups
/// Lists inventory source groups that are accessible to the current user. The order is defined by the order_by parameter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_inventory_source_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_inventory_source_groups_list_task()`.
/// For the simplest API, use `displayvideo_inventory_source_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_inventory_source_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_inventory_source_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListInventorySourceGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_inventory_source_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_inventory_source_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoInventorySourceGroupsListArgs {
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// GET v4/inventorySourceGroups
/// Lists inventory source groups that are accessible to the current user. The order is defined by the order_by parameter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_inventory_source_groups_list_builder()` + `displayvideo_inventory_source_groups_list_execute()`.
/// For task-level control, use `displayvideo_inventory_source_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_inventory_source_groups_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoInventorySourceGroupsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListInventorySourceGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_inventory_source_groups_list_builder(
        client,
        &args.advertiserId,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.partnerId,
    )?;
    displayvideo_inventory_source_groups_list_execute(builder)
}

/// PATCH v4/inventorySourceGroups/{inventorySourceGroupId}
/// Updates an inventory source group. Returns the updated inventory source group if successful.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_inventory_source_groups_patch_execute()` to send, or `displayvideo_inventory_source_groups_patch` for simplest API.

pub fn displayvideo_inventory_source_groups_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    inventorySourceGroupId: &String,
    advertiserId: &Option<String>,
    partnerId: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/inventorySourceGroups/{}",
        inventorySourceGroupId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v4/inventorySourceGroups/{inventorySourceGroupId}
/// Updates an inventory source group. Returns the updated inventory source group if successful.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_inventory_source_groups_patch_execute()` or `displayvideo_inventory_source_groups_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_inventory_source_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_inventory_source_groups_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InventorySourceGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InventorySourceGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v4/inventorySourceGroups/{inventorySourceGroupId}
/// Updates an inventory source group. Returns the updated inventory source group if successful.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_inventory_source_groups_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_inventory_source_groups_patch_task()`.
/// For the simplest API, use `displayvideo_inventory_source_groups_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_inventory_source_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_inventory_source_groups_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InventorySourceGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_inventory_source_groups_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_inventory_source_groups_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoInventorySourceGroupsPatchArgs {
    /// Path parameter: inventorySourceGroupId
    pub inventorySourceGroupId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v4/inventorySourceGroups/{inventorySourceGroupId}
/// Updates an inventory source group. Returns the updated inventory source group if successful.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_inventory_source_groups_patch_builder()` + `displayvideo_inventory_source_groups_patch_execute()`.
/// For task-level control, use `displayvideo_inventory_source_groups_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_inventory_source_groups_patch(
    client: &SimpleHttpClient,
    args: &DisplayvideoInventorySourceGroupsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InventorySourceGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_inventory_source_groups_patch_builder(
        client,
        &args.inventorySourceGroupId,
        &args.advertiserId,
        &args.partnerId,
        &args.updateMask,
    )?;
    displayvideo_inventory_source_groups_patch_execute(builder)
}

/// POST v4/inventorySourceGroups/{inventorySourceGroupsId}/assignedInventorySources:bulkEdit
/// Bulk edits multiple assignments between inventory sources and a single inventory source group. The operation will delete the assigned inventory sources provided in BulkEditAssignedInventorySourcesRequest.deleted_assigned_inventory_sources and then create the assigned inventory sources provided in BulkEditAssignedInventorySourcesRequest.created_assigned_inventory_sources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_inventory_source_groups_assigned_inventory_sources_bulk_edit_execute()` to send, or `displayvideo_inventory_source_groups_assigned_inventory_sources_bulk_edit` for simplest API.

pub fn displayvideo_inventory_source_groups_assigned_inventory_sources_bulk_edit_builder<R>(
    client: &SimpleHttpClient<R>,
    inventorySourceGroupId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/inventorySourceGroups/{}/assignedInventorySources:bulkEdit",
        inventorySourceGroupId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/inventorySourceGroups/{inventorySourceGroupsId}/assignedInventorySources:bulkEdit
/// Bulk edits multiple assignments between inventory sources and a single inventory source group. The operation will delete the assigned inventory sources provided in BulkEditAssignedInventorySourcesRequest.deleted_assigned_inventory_sources and then create the assigned inventory sources provided in BulkEditAssignedInventorySourcesRequest.created_assigned_inventory_sources.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_inventory_source_groups_assigned_inventory_sources_bulk_edit_execute()` or `displayvideo_inventory_source_groups_assigned_inventory_sources_bulk_edit`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_inventory_source_groups_assigned_inventory_sources_bulk_edit_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_inventory_source_groups_assigned_inventory_sources_bulk_edit_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BulkEditAssignedInventorySourcesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BulkEditAssignedInventorySourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/inventorySourceGroups/{inventorySourceGroupsId}/assignedInventorySources:bulkEdit
/// Bulk edits multiple assignments between inventory sources and a single inventory source group. The operation will delete the assigned inventory sources provided in BulkEditAssignedInventorySourcesRequest.deleted_assigned_inventory_sources and then create the assigned inventory sources provided in BulkEditAssignedInventorySourcesRequest.created_assigned_inventory_sources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_inventory_source_groups_assigned_inventory_sources_bulk_edit_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_inventory_source_groups_assigned_inventory_sources_bulk_edit_task()`.
/// For the simplest API, use `displayvideo_inventory_source_groups_assigned_inventory_sources_bulk_edit()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_inventory_source_groups_assigned_inventory_sources_bulk_edit_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_inventory_source_groups_assigned_inventory_sources_bulk_edit_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BulkEditAssignedInventorySourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        displayvideo_inventory_source_groups_assigned_inventory_sources_bulk_edit_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_inventory_source_groups_assigned_inventory_sources_bulk_edit`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoInventorySourceGroupsAssignedInventorySourcesBulkEditArgs {
    /// Path parameter: inventorySourceGroupId
    pub inventorySourceGroupId: String,
}

/// POST v4/inventorySourceGroups/{inventorySourceGroupsId}/assignedInventorySources:bulkEdit
/// Bulk edits multiple assignments between inventory sources and a single inventory source group. The operation will delete the assigned inventory sources provided in BulkEditAssignedInventorySourcesRequest.deleted_assigned_inventory_sources and then create the assigned inventory sources provided in BulkEditAssignedInventorySourcesRequest.created_assigned_inventory_sources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_inventory_source_groups_assigned_inventory_sources_bulk_edit_builder()` + `displayvideo_inventory_source_groups_assigned_inventory_sources_bulk_edit_execute()`.
/// For task-level control, use `displayvideo_inventory_source_groups_assigned_inventory_sources_bulk_edit_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_inventory_source_groups_assigned_inventory_sources_bulk_edit(
    client: &SimpleHttpClient,
    args: &DisplayvideoInventorySourceGroupsAssignedInventorySourcesBulkEditArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BulkEditAssignedInventorySourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        displayvideo_inventory_source_groups_assigned_inventory_sources_bulk_edit_builder(
            client,
            &args.inventorySourceGroupId,
        )?;
    displayvideo_inventory_source_groups_assigned_inventory_sources_bulk_edit_execute(builder)
}

/// POST v4/inventorySourceGroups/{inventorySourceGroupsId}/assignedInventorySources
/// Creates an assignment between an inventory source and an inventory source group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_inventory_source_groups_assigned_inventory_sources_create_execute()` to send, or `displayvideo_inventory_source_groups_assigned_inventory_sources_create` for simplest API.

pub fn displayvideo_inventory_source_groups_assigned_inventory_sources_create_builder<R>(
    client: &SimpleHttpClient<R>,
    inventorySourceGroupId: &String,
    advertiserId: &Option<String>,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/inventorySourceGroups/{}/assignedInventorySources",
        inventorySourceGroupId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/inventorySourceGroups/{inventorySourceGroupsId}/assignedInventorySources
/// Creates an assignment between an inventory source and an inventory source group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_inventory_source_groups_assigned_inventory_sources_create_execute()` or `displayvideo_inventory_source_groups_assigned_inventory_sources_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_inventory_source_groups_assigned_inventory_sources_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_inventory_source_groups_assigned_inventory_sources_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AssignedInventorySource>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AssignedInventorySource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/inventorySourceGroups/{inventorySourceGroupsId}/assignedInventorySources
/// Creates an assignment between an inventory source and an inventory source group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_inventory_source_groups_assigned_inventory_sources_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_inventory_source_groups_assigned_inventory_sources_create_task()`.
/// For the simplest API, use `displayvideo_inventory_source_groups_assigned_inventory_sources_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_inventory_source_groups_assigned_inventory_sources_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_inventory_source_groups_assigned_inventory_sources_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AssignedInventorySource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        displayvideo_inventory_source_groups_assigned_inventory_sources_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_inventory_source_groups_assigned_inventory_sources_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoInventorySourceGroupsAssignedInventorySourcesCreateArgs {
    /// Path parameter: inventorySourceGroupId
    pub inventorySourceGroupId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// POST v4/inventorySourceGroups/{inventorySourceGroupsId}/assignedInventorySources
/// Creates an assignment between an inventory source and an inventory source group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_inventory_source_groups_assigned_inventory_sources_create_builder()` + `displayvideo_inventory_source_groups_assigned_inventory_sources_create_execute()`.
/// For task-level control, use `displayvideo_inventory_source_groups_assigned_inventory_sources_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_inventory_source_groups_assigned_inventory_sources_create(
    client: &SimpleHttpClient,
    args: &DisplayvideoInventorySourceGroupsAssignedInventorySourcesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AssignedInventorySource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_inventory_source_groups_assigned_inventory_sources_create_builder(
        client,
        &args.inventorySourceGroupId,
        &args.advertiserId,
        &args.partnerId,
    )?;
    displayvideo_inventory_source_groups_assigned_inventory_sources_create_execute(builder)
}

/// DELETE v4/inventorySourceGroups/{inventorySourceGroupsId}/assignedInventorySources/{assignedInventorySourcesId}
/// Deletes the assignment between an inventory source and an inventory source group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_inventory_source_groups_assigned_inventory_sources_delete_execute()` to send, or `displayvideo_inventory_source_groups_assigned_inventory_sources_delete` for simplest API.

pub fn displayvideo_inventory_source_groups_assigned_inventory_sources_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    inventorySourceGroupId: &String,
    assignedInventorySourceId: &String,
    advertiserId: &Option<String>,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/inventorySourceGroups/{}/assignedInventorySources/{}",
        inventorySourceGroupId,
        assignedInventorySourceId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v4/inventorySourceGroups/{inventorySourceGroupsId}/assignedInventorySources/{assignedInventorySourcesId}
/// Deletes the assignment between an inventory source and an inventory source group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_inventory_source_groups_assigned_inventory_sources_delete_execute()` or `displayvideo_inventory_source_groups_assigned_inventory_sources_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_inventory_source_groups_assigned_inventory_sources_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_inventory_source_groups_assigned_inventory_sources_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v4/inventorySourceGroups/{inventorySourceGroupsId}/assignedInventorySources/{assignedInventorySourcesId}
/// Deletes the assignment between an inventory source and an inventory source group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_inventory_source_groups_assigned_inventory_sources_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_inventory_source_groups_assigned_inventory_sources_delete_task()`.
/// For the simplest API, use `displayvideo_inventory_source_groups_assigned_inventory_sources_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_inventory_source_groups_assigned_inventory_sources_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_inventory_source_groups_assigned_inventory_sources_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        displayvideo_inventory_source_groups_assigned_inventory_sources_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_inventory_source_groups_assigned_inventory_sources_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoInventorySourceGroupsAssignedInventorySourcesDeleteArgs {
    /// Path parameter: inventorySourceGroupId
    pub inventorySourceGroupId: String,
    /// Path parameter: assignedInventorySourceId
    pub assignedInventorySourceId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// DELETE v4/inventorySourceGroups/{inventorySourceGroupsId}/assignedInventorySources/{assignedInventorySourcesId}
/// Deletes the assignment between an inventory source and an inventory source group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_inventory_source_groups_assigned_inventory_sources_delete_builder()` + `displayvideo_inventory_source_groups_assigned_inventory_sources_delete_execute()`.
/// For task-level control, use `displayvideo_inventory_source_groups_assigned_inventory_sources_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_inventory_source_groups_assigned_inventory_sources_delete(
    client: &SimpleHttpClient,
    args: &DisplayvideoInventorySourceGroupsAssignedInventorySourcesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_inventory_source_groups_assigned_inventory_sources_delete_builder(
        client,
        &args.inventorySourceGroupId,
        &args.assignedInventorySourceId,
        &args.advertiserId,
        &args.partnerId,
    )?;
    displayvideo_inventory_source_groups_assigned_inventory_sources_delete_execute(builder)
}

/// GET v4/inventorySourceGroups/{inventorySourceGroupsId}/assignedInventorySources
/// Lists inventory sources assigned to an inventory source group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_inventory_source_groups_assigned_inventory_sources_list_execute()` to send, or `displayvideo_inventory_source_groups_assigned_inventory_sources_list` for simplest API.

pub fn displayvideo_inventory_source_groups_assigned_inventory_sources_list_builder<R>(
    client: &SimpleHttpClient<R>,
    inventorySourceGroupId: &String,
    advertiserId: &Option<String>,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/inventorySourceGroups/{}/assignedInventorySources",
        inventorySourceGroupId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/inventorySourceGroups/{inventorySourceGroupsId}/assignedInventorySources
/// Lists inventory sources assigned to an inventory source group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_inventory_source_groups_assigned_inventory_sources_list_execute()` or `displayvideo_inventory_source_groups_assigned_inventory_sources_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_inventory_source_groups_assigned_inventory_sources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_inventory_source_groups_assigned_inventory_sources_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAssignedInventorySourcesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAssignedInventorySourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/inventorySourceGroups/{inventorySourceGroupsId}/assignedInventorySources
/// Lists inventory sources assigned to an inventory source group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_inventory_source_groups_assigned_inventory_sources_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_inventory_source_groups_assigned_inventory_sources_list_task()`.
/// For the simplest API, use `displayvideo_inventory_source_groups_assigned_inventory_sources_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_inventory_source_groups_assigned_inventory_sources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_inventory_source_groups_assigned_inventory_sources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAssignedInventorySourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_inventory_source_groups_assigned_inventory_sources_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_inventory_source_groups_assigned_inventory_sources_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoInventorySourceGroupsAssignedInventorySourcesListArgs {
    /// Path parameter: inventorySourceGroupId
    pub inventorySourceGroupId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// GET v4/inventorySourceGroups/{inventorySourceGroupsId}/assignedInventorySources
/// Lists inventory sources assigned to an inventory source group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_inventory_source_groups_assigned_inventory_sources_list_builder()` + `displayvideo_inventory_source_groups_assigned_inventory_sources_list_execute()`.
/// For task-level control, use `displayvideo_inventory_source_groups_assigned_inventory_sources_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_inventory_source_groups_assigned_inventory_sources_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoInventorySourceGroupsAssignedInventorySourcesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAssignedInventorySourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_inventory_source_groups_assigned_inventory_sources_list_builder(
        client,
        &args.inventorySourceGroupId,
        &args.advertiserId,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.partnerId,
    )?;
    displayvideo_inventory_source_groups_assigned_inventory_sources_list_execute(builder)
}

/// POST v4/inventorySources
/// Creates a new inventory source. Returns the newly created inventory source if successful.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_inventory_sources_create_execute()` to send, or `displayvideo_inventory_sources_create` for simplest API.

pub fn displayvideo_inventory_sources_create_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &Option<String>,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://displayvideo.googleapis.com/v4/inventorySources",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/inventorySources
/// Creates a new inventory source. Returns the newly created inventory source if successful.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_inventory_sources_create_execute()` or `displayvideo_inventory_sources_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_inventory_sources_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_inventory_sources_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InventorySource>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InventorySource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/inventorySources
/// Creates a new inventory source. Returns the newly created inventory source if successful.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_inventory_sources_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_inventory_sources_create_task()`.
/// For the simplest API, use `displayvideo_inventory_sources_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_inventory_sources_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_inventory_sources_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InventorySource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_inventory_sources_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_inventory_sources_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoInventorySourcesCreateArgs {
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// POST v4/inventorySources
/// Creates a new inventory source. Returns the newly created inventory source if successful.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_inventory_sources_create_builder()` + `displayvideo_inventory_sources_create_execute()`.
/// For task-level control, use `displayvideo_inventory_sources_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_inventory_sources_create(
    client: &SimpleHttpClient,
    args: &DisplayvideoInventorySourcesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InventorySource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        displayvideo_inventory_sources_create_builder(client, &args.advertiserId, &args.partnerId)?;
    displayvideo_inventory_sources_create_execute(builder)
}

/// POST v4/inventorySources/{inventorySourcesId}:editInventorySourceReadWriteAccessors
/// Edits `read/write` accessors of an inventory source. Returns the updated read_write_accessors for the inventory source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_inventory_sources_edit_inventory_source_read_write_accessors_execute()` to send, or `displayvideo_inventory_sources_edit_inventory_source_read_write_accessors` for simplest API.

pub fn displayvideo_inventory_sources_edit_inventory_source_read_write_accessors_builder<R>(
    client: &SimpleHttpClient<R>,
    inventorySourceId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/inventorySources/{}:editInventorySourceReadWriteAccessors",
        inventorySourceId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/inventorySources/{inventorySourcesId}:editInventorySourceReadWriteAccessors
/// Edits `read/write` accessors of an inventory source. Returns the updated read_write_accessors for the inventory source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_inventory_sources_edit_inventory_source_read_write_accessors_execute()` or `displayvideo_inventory_sources_edit_inventory_source_read_write_accessors`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_inventory_sources_edit_inventory_source_read_write_accessors_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_inventory_sources_edit_inventory_source_read_write_accessors_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InventorySourceAccessors>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InventorySourceAccessors = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/inventorySources/{inventorySourcesId}:editInventorySourceReadWriteAccessors
/// Edits `read/write` accessors of an inventory source. Returns the updated read_write_accessors for the inventory source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_inventory_sources_edit_inventory_source_read_write_accessors_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_inventory_sources_edit_inventory_source_read_write_accessors_task()`.
/// For the simplest API, use `displayvideo_inventory_sources_edit_inventory_source_read_write_accessors()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_inventory_sources_edit_inventory_source_read_write_accessors_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_inventory_sources_edit_inventory_source_read_write_accessors_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InventorySourceAccessors>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        displayvideo_inventory_sources_edit_inventory_source_read_write_accessors_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_inventory_sources_edit_inventory_source_read_write_accessors`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoInventorySourcesEditInventorySourceReadWriteAccessorsArgs {
    /// Path parameter: inventorySourceId
    pub inventorySourceId: String,
}

/// POST v4/inventorySources/{inventorySourcesId}:editInventorySourceReadWriteAccessors
/// Edits `read/write` accessors of an inventory source. Returns the updated read_write_accessors for the inventory source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_inventory_sources_edit_inventory_source_read_write_accessors_builder()` + `displayvideo_inventory_sources_edit_inventory_source_read_write_accessors_execute()`.
/// For task-level control, use `displayvideo_inventory_sources_edit_inventory_source_read_write_accessors_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_inventory_sources_edit_inventory_source_read_write_accessors(
    client: &SimpleHttpClient,
    args: &DisplayvideoInventorySourcesEditInventorySourceReadWriteAccessorsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InventorySourceAccessors>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        displayvideo_inventory_sources_edit_inventory_source_read_write_accessors_builder(
            client,
            &args.inventorySourceId,
        )?;
    displayvideo_inventory_sources_edit_inventory_source_read_write_accessors_execute(builder)
}

/// GET v4/inventorySources/{inventorySourcesId}
/// Gets an inventory source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_inventory_sources_get_execute()` to send, or `displayvideo_inventory_sources_get` for simplest API.

pub fn displayvideo_inventory_sources_get_builder<R>(
    client: &SimpleHttpClient<R>,
    inventorySourceId: &String,
    advertiserId: &Option<String>,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/inventorySources/{}",
        inventorySourceId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/inventorySources/{inventorySourcesId}
/// Gets an inventory source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_inventory_sources_get_execute()` or `displayvideo_inventory_sources_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_inventory_sources_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_inventory_sources_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InventorySource>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InventorySource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/inventorySources/{inventorySourcesId}
/// Gets an inventory source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_inventory_sources_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_inventory_sources_get_task()`.
/// For the simplest API, use `displayvideo_inventory_sources_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_inventory_sources_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_inventory_sources_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InventorySource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_inventory_sources_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_inventory_sources_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoInventorySourcesGetArgs {
    /// Path parameter: inventorySourceId
    pub inventorySourceId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// GET v4/inventorySources/{inventorySourcesId}
/// Gets an inventory source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_inventory_sources_get_builder()` + `displayvideo_inventory_sources_get_execute()`.
/// For task-level control, use `displayvideo_inventory_sources_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_inventory_sources_get(
    client: &SimpleHttpClient,
    args: &DisplayvideoInventorySourcesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InventorySource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_inventory_sources_get_builder(
        client,
        &args.inventorySourceId,
        &args.advertiserId,
        &args.partnerId,
    )?;
    displayvideo_inventory_sources_get_execute(builder)
}

/// GET v4/inventorySources
/// Lists inventory sources that are accessible to the current user. The order is defined by the order_by parameter. If a filter by entity_status is not specified, inventory sources with entity status ENTITY_STATUS_ARCHIVED will not be included in the results.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_inventory_sources_list_execute()` to send, or `displayvideo_inventory_sources_list` for simplest API.

pub fn displayvideo_inventory_sources_list_builder<R>(
    client: &SimpleHttpClient<R>,
    advertiserId: &Option<String>,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    partnerId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://displayvideo.googleapis.com/v4/inventorySources",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/inventorySources
/// Lists inventory sources that are accessible to the current user. The order is defined by the order_by parameter. If a filter by entity_status is not specified, inventory sources with entity status ENTITY_STATUS_ARCHIVED will not be included in the results.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_inventory_sources_list_execute()` or `displayvideo_inventory_sources_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_inventory_sources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_inventory_sources_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListInventorySourcesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListInventorySourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/inventorySources
/// Lists inventory sources that are accessible to the current user. The order is defined by the order_by parameter. If a filter by entity_status is not specified, inventory sources with entity status ENTITY_STATUS_ARCHIVED will not be included in the results.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_inventory_sources_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_inventory_sources_list_task()`.
/// For the simplest API, use `displayvideo_inventory_sources_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_inventory_sources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_inventory_sources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListInventorySourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_inventory_sources_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_inventory_sources_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoInventorySourcesListArgs {
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
}

/// GET v4/inventorySources
/// Lists inventory sources that are accessible to the current user. The order is defined by the order_by parameter. If a filter by entity_status is not specified, inventory sources with entity status ENTITY_STATUS_ARCHIVED will not be included in the results.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_inventory_sources_list_builder()` + `displayvideo_inventory_sources_list_execute()`.
/// For task-level control, use `displayvideo_inventory_sources_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_inventory_sources_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoInventorySourcesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListInventorySourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_inventory_sources_list_builder(
        client,
        &args.advertiserId,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.partnerId,
    )?;
    displayvideo_inventory_sources_list_execute(builder)
}

/// PATCH v4/inventorySources/{inventorySourcesId}
/// Updates an existing inventory source. Returns the updated inventory source if successful.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_inventory_sources_patch_execute()` to send, or `displayvideo_inventory_sources_patch` for simplest API.

pub fn displayvideo_inventory_sources_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    inventorySourceId: &String,
    advertiserId: &Option<String>,
    partnerId: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/inventorySources/{}",
        inventorySourceId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = partnerId.as_ref() {
        query_parts.push(format!("partnerId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v4/inventorySources/{inventorySourcesId}
/// Updates an existing inventory source. Returns the updated inventory source if successful.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_inventory_sources_patch_execute()` or `displayvideo_inventory_sources_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_inventory_sources_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_inventory_sources_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InventorySource>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InventorySource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v4/inventorySources/{inventorySourcesId}
/// Updates an existing inventory source. Returns the updated inventory source if successful.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_inventory_sources_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_inventory_sources_patch_task()`.
/// For the simplest API, use `displayvideo_inventory_sources_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_inventory_sources_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_inventory_sources_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InventorySource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_inventory_sources_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_inventory_sources_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoInventorySourcesPatchArgs {
    /// Path parameter: inventorySourceId
    pub inventorySourceId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: partnerId
    pub partnerId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v4/inventorySources/{inventorySourcesId}
/// Updates an existing inventory source. Returns the updated inventory source if successful.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_inventory_sources_patch_builder()` + `displayvideo_inventory_sources_patch_execute()`.
/// For task-level control, use `displayvideo_inventory_sources_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_inventory_sources_patch(
    client: &SimpleHttpClient,
    args: &DisplayvideoInventorySourcesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InventorySource>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_inventory_sources_patch_builder(
        client,
        &args.inventorySourceId,
        &args.advertiserId,
        &args.partnerId,
        &args.updateMask,
    )?;
    displayvideo_inventory_sources_patch_execute(builder)
}

/// GET download/{downloadId}
/// Downloads media. Download is supported on the URI /`download/{resource_name`=**}?alt=media. **Note**: Download requests will not be successful without including alt=media query string.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_media_download_execute()` to send, or `displayvideo_media_download` for simplest API.

pub fn displayvideo_media_download_builder<R>(
    client: &SimpleHttpClient<R>,
    resourceName: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/download/{}",
        resourceName,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET download/{downloadId}
/// Downloads media. Download is supported on the URI /`download/{resource_name`=**}?alt=media. **Note**: Download requests will not be successful without including alt=media query string.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_media_download_execute()` or `displayvideo_media_download`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_media_download_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_media_download_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleBytestreamMedia>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleBytestreamMedia = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET download/{downloadId}
/// Downloads media. Download is supported on the URI /`download/{resource_name`=**}?alt=media. **Note**: Download requests will not be successful without including alt=media query string.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_media_download_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_media_download_task()`.
/// For the simplest API, use `displayvideo_media_download()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_media_download_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_media_download_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleBytestreamMedia>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_media_download_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_media_download`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoMediaDownloadArgs {
    /// Path parameter: resourceName
    pub resourceName: String,
}

/// GET download/{downloadId}
/// Downloads media. Download is supported on the URI /`download/{resource_name`=**}?alt=media. **Note**: Download requests will not be successful without including alt=media query string.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_media_download_builder()` + `displayvideo_media_download_execute()`.
/// For task-level control, use `displayvideo_media_download_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_media_download(
    client: &SimpleHttpClient,
    args: &DisplayvideoMediaDownloadArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleBytestreamMedia>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_media_download_builder(client, &args.resourceName)?;
    displayvideo_media_download_execute(builder)
}

/// POST media/{mediaId}
/// Uploads media. Upload is supported on the URI /`upload/media/{resource_name`=**}?upload_type=media. **Note**: Upload requests will not be successful without including upload_type=media query string.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_media_upload_execute()` to send, or `displayvideo_media_upload` for simplest API.

pub fn displayvideo_media_upload_builder<R>(
    client: &SimpleHttpClient<R>,
    resourceName: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://displayvideo.googleapis.com/media/{}", resourceName,);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST media/{mediaId}
/// Uploads media. Upload is supported on the URI /`upload/media/{resource_name`=**}?upload_type=media. **Note**: Upload requests will not be successful without including upload_type=media query string.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_media_upload_execute()` or `displayvideo_media_upload`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_media_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_media_upload_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleBytestreamMedia>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleBytestreamMedia = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST media/{mediaId}
/// Uploads media. Upload is supported on the URI /`upload/media/{resource_name`=**}?upload_type=media. **Note**: Upload requests will not be successful without including upload_type=media query string.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_media_upload_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_media_upload_task()`.
/// For the simplest API, use `displayvideo_media_upload()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_media_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_media_upload_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleBytestreamMedia>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_media_upload_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_media_upload`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoMediaUploadArgs {
    /// Path parameter: resourceName
    pub resourceName: String,
}

/// POST media/{mediaId}
/// Uploads media. Upload is supported on the URI /`upload/media/{resource_name`=**}?upload_type=media. **Note**: Upload requests will not be successful without including upload_type=media query string.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_media_upload_builder()` + `displayvideo_media_upload_execute()`.
/// For task-level control, use `displayvideo_media_upload_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_media_upload(
    client: &SimpleHttpClient,
    args: &DisplayvideoMediaUploadArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleBytestreamMedia>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_media_upload_builder(client, &args.resourceName)?;
    displayvideo_media_upload_execute(builder)
}

/// POST v4/partners/{partnersId}:editAssignedTargetingOptions
/// Edits targeting options under a single partner. The operation will delete the assigned targeting options provided in BulkEditPartnerAssignedTargetingOptionsRequest.`deleteRequests` and then create the assigned targeting options provided in BulkEditPartnerAssignedTargetingOptionsRequest.`createRequests` .
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_partners_edit_assigned_targeting_options_execute()` to send, or `displayvideo_partners_edit_assigned_targeting_options` for simplest API.

pub fn displayvideo_partners_edit_assigned_targeting_options_builder<R>(
    client: &SimpleHttpClient<R>,
    partnerId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/partners/{}:editAssignedTargetingOptions",
        partnerId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/partners/{partnersId}:editAssignedTargetingOptions
/// Edits targeting options under a single partner. The operation will delete the assigned targeting options provided in BulkEditPartnerAssignedTargetingOptionsRequest.`deleteRequests` and then create the assigned targeting options provided in BulkEditPartnerAssignedTargetingOptionsRequest.`createRequests` .
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_partners_edit_assigned_targeting_options_execute()` or `displayvideo_partners_edit_assigned_targeting_options`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_partners_edit_assigned_targeting_options_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_partners_edit_assigned_targeting_options_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BulkEditPartnerAssignedTargetingOptionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BulkEditPartnerAssignedTargetingOptionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/partners/{partnersId}:editAssignedTargetingOptions
/// Edits targeting options under a single partner. The operation will delete the assigned targeting options provided in BulkEditPartnerAssignedTargetingOptionsRequest.`deleteRequests` and then create the assigned targeting options provided in BulkEditPartnerAssignedTargetingOptionsRequest.`createRequests` .
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_partners_edit_assigned_targeting_options_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_partners_edit_assigned_targeting_options_task()`.
/// For the simplest API, use `displayvideo_partners_edit_assigned_targeting_options()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_partners_edit_assigned_targeting_options_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_partners_edit_assigned_targeting_options_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BulkEditPartnerAssignedTargetingOptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_partners_edit_assigned_targeting_options_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_partners_edit_assigned_targeting_options`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoPartnersEditAssignedTargetingOptionsArgs {
    /// Path parameter: partnerId
    pub partnerId: String,
}

/// POST v4/partners/{partnersId}:editAssignedTargetingOptions
/// Edits targeting options under a single partner. The operation will delete the assigned targeting options provided in BulkEditPartnerAssignedTargetingOptionsRequest.`deleteRequests` and then create the assigned targeting options provided in BulkEditPartnerAssignedTargetingOptionsRequest.`createRequests` .
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_partners_edit_assigned_targeting_options_builder()` + `displayvideo_partners_edit_assigned_targeting_options_execute()`.
/// For task-level control, use `displayvideo_partners_edit_assigned_targeting_options_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_partners_edit_assigned_targeting_options(
    client: &SimpleHttpClient,
    args: &DisplayvideoPartnersEditAssignedTargetingOptionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BulkEditPartnerAssignedTargetingOptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        displayvideo_partners_edit_assigned_targeting_options_builder(client, &args.partnerId)?;
    displayvideo_partners_edit_assigned_targeting_options_execute(builder)
}

/// GET v4/partners/{partnersId}
/// Gets a partner.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_partners_get_execute()` to send, or `displayvideo_partners_get` for simplest API.

pub fn displayvideo_partners_get_builder<R>(
    client: &SimpleHttpClient<R>,
    partnerId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/partners/{}",
        partnerId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/partners/{partnersId}
/// Gets a partner.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_partners_get_execute()` or `displayvideo_partners_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_partners_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_partners_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Partner>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Partner = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/partners/{partnersId}
/// Gets a partner.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_partners_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_partners_get_task()`.
/// For the simplest API, use `displayvideo_partners_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_partners_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_partners_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Partner>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_partners_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_partners_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoPartnersGetArgs {
    /// Path parameter: partnerId
    pub partnerId: String,
}

/// GET v4/partners/{partnersId}
/// Gets a partner.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_partners_get_builder()` + `displayvideo_partners_get_execute()`.
/// For task-level control, use `displayvideo_partners_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_partners_get(
    client: &SimpleHttpClient,
    args: &DisplayvideoPartnersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Partner>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_partners_get_builder(client, &args.partnerId)?;
    displayvideo_partners_get_execute(builder)
}

/// GET v4/partners
/// Lists partners that are accessible to the current user. The order is defined by the order_by parameter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_partners_list_execute()` to send, or `displayvideo_partners_list` for simplest API.

pub fn displayvideo_partners_list_builder<R>(
    client: &SimpleHttpClient<R>,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://displayvideo.googleapis.com/v4/partners",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/partners
/// Lists partners that are accessible to the current user. The order is defined by the order_by parameter.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_partners_list_execute()` or `displayvideo_partners_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_partners_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_partners_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListPartnersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListPartnersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/partners
/// Lists partners that are accessible to the current user. The order is defined by the order_by parameter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_partners_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_partners_list_task()`.
/// For the simplest API, use `displayvideo_partners_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_partners_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_partners_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListPartnersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_partners_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_partners_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoPartnersListArgs {
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v4/partners
/// Lists partners that are accessible to the current user. The order is defined by the order_by parameter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_partners_list_builder()` + `displayvideo_partners_list_execute()`.
/// For task-level control, use `displayvideo_partners_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_partners_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoPartnersListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListPartnersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_partners_list_builder(
        client,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    displayvideo_partners_list_execute(builder)
}

/// POST v4/partners/{partnersId}/channels
/// Creates a new channel. Returns the newly created channel if successful.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_partners_channels_create_execute()` to send, or `displayvideo_partners_channels_create` for simplest API.

pub fn displayvideo_partners_channels_create_builder<R>(
    client: &SimpleHttpClient<R>,
    partnerId: &String,
    advertiserId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/partners/{}/channels",
        partnerId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/partners/{partnersId}/channels
/// Creates a new channel. Returns the newly created channel if successful.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_partners_channels_create_execute()` or `displayvideo_partners_channels_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_partners_channels_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_partners_channels_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Channel>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Channel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/partners/{partnersId}/channels
/// Creates a new channel. Returns the newly created channel if successful.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_partners_channels_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_partners_channels_create_task()`.
/// For the simplest API, use `displayvideo_partners_channels_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_partners_channels_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_partners_channels_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_partners_channels_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_partners_channels_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoPartnersChannelsCreateArgs {
    /// Path parameter: partnerId
    pub partnerId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
}

/// POST v4/partners/{partnersId}/channels
/// Creates a new channel. Returns the newly created channel if successful.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_partners_channels_create_builder()` + `displayvideo_partners_channels_create_execute()`.
/// For task-level control, use `displayvideo_partners_channels_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_partners_channels_create(
    client: &SimpleHttpClient,
    args: &DisplayvideoPartnersChannelsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        displayvideo_partners_channels_create_builder(client, &args.partnerId, &args.advertiserId)?;
    displayvideo_partners_channels_create_execute(builder)
}

/// GET v4/partners/{partnersId}/channels/{channelsId}
/// Gets a channel for a partner or advertiser.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_partners_channels_get_execute()` to send, or `displayvideo_partners_channels_get` for simplest API.

pub fn displayvideo_partners_channels_get_builder<R>(
    client: &SimpleHttpClient<R>,
    partnerId: &String,
    channelId: &String,
    advertiserId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/partners/{}/channels/{}",
        partnerId, channelId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/partners/{partnersId}/channels/{channelsId}
/// Gets a channel for a partner or advertiser.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_partners_channels_get_execute()` or `displayvideo_partners_channels_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_partners_channels_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_partners_channels_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Channel>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Channel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/partners/{partnersId}/channels/{channelsId}
/// Gets a channel for a partner or advertiser.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_partners_channels_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_partners_channels_get_task()`.
/// For the simplest API, use `displayvideo_partners_channels_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_partners_channels_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_partners_channels_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_partners_channels_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_partners_channels_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoPartnersChannelsGetArgs {
    /// Path parameter: partnerId
    pub partnerId: String,
    /// Path parameter: channelId
    pub channelId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
}

/// GET v4/partners/{partnersId}/channels/{channelsId}
/// Gets a channel for a partner or advertiser.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_partners_channels_get_builder()` + `displayvideo_partners_channels_get_execute()`.
/// For task-level control, use `displayvideo_partners_channels_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_partners_channels_get(
    client: &SimpleHttpClient,
    args: &DisplayvideoPartnersChannelsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_partners_channels_get_builder(
        client,
        &args.partnerId,
        &args.channelId,
        &args.advertiserId,
    )?;
    displayvideo_partners_channels_get_execute(builder)
}

/// GET v4/partners/{partnersId}/channels
/// Lists channels for a partner or advertiser.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_partners_channels_list_execute()` to send, or `displayvideo_partners_channels_list` for simplest API.

pub fn displayvideo_partners_channels_list_builder<R>(
    client: &SimpleHttpClient<R>,
    partnerId: &String,
    advertiserId: &Option<String>,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/partners/{}/channels",
        partnerId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/partners/{partnersId}/channels
/// Lists channels for a partner or advertiser.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_partners_channels_list_execute()` or `displayvideo_partners_channels_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_partners_channels_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_partners_channels_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListChannelsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListChannelsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/partners/{partnersId}/channels
/// Lists channels for a partner or advertiser.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_partners_channels_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_partners_channels_list_task()`.
/// For the simplest API, use `displayvideo_partners_channels_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_partners_channels_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_partners_channels_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListChannelsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_partners_channels_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_partners_channels_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoPartnersChannelsListArgs {
    /// Path parameter: partnerId
    pub partnerId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v4/partners/{partnersId}/channels
/// Lists channels for a partner or advertiser.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_partners_channels_list_builder()` + `displayvideo_partners_channels_list_execute()`.
/// For task-level control, use `displayvideo_partners_channels_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_partners_channels_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoPartnersChannelsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListChannelsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_partners_channels_list_builder(
        client,
        &args.partnerId,
        &args.advertiserId,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    displayvideo_partners_channels_list_execute(builder)
}

/// PATCH v4/partners/{partnersId}/channels/{channelId}
/// Updates a channel. Returns the updated channel if successful.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_partners_channels_patch_execute()` to send, or `displayvideo_partners_channels_patch` for simplest API.

pub fn displayvideo_partners_channels_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    partnerId: &String,
    channelId: &String,
    advertiserId: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/partners/{}/channels/{}",
        partnerId, channelId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v4/partners/{partnersId}/channels/{channelId}
/// Updates a channel. Returns the updated channel if successful.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_partners_channels_patch_execute()` or `displayvideo_partners_channels_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_partners_channels_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_partners_channels_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Channel>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Channel = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v4/partners/{partnersId}/channels/{channelId}
/// Updates a channel. Returns the updated channel if successful.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_partners_channels_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_partners_channels_patch_task()`.
/// For the simplest API, use `displayvideo_partners_channels_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_partners_channels_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_partners_channels_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_partners_channels_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_partners_channels_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoPartnersChannelsPatchArgs {
    /// Path parameter: partnerId
    pub partnerId: String,
    /// Path parameter: channelId
    pub channelId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v4/partners/{partnersId}/channels/{channelId}
/// Updates a channel. Returns the updated channel if successful.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_partners_channels_patch_builder()` + `displayvideo_partners_channels_patch_execute()`.
/// For task-level control, use `displayvideo_partners_channels_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_partners_channels_patch(
    client: &SimpleHttpClient,
    args: &DisplayvideoPartnersChannelsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Channel>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_partners_channels_patch_builder(
        client,
        &args.partnerId,
        &args.channelId,
        &args.advertiserId,
        &args.updateMask,
    )?;
    displayvideo_partners_channels_patch_execute(builder)
}

/// POST v4/partners/{partnerId}/channels/{channelsId}/sites:bulkEdit
/// Bulk edits sites under a single channel. The operation will delete the sites provided in BulkEditSitesRequest.deleted_sites and then create the sites provided in BulkEditSitesRequest.created_sites.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_partners_channels_sites_bulk_edit_execute()` to send, or `displayvideo_partners_channels_sites_bulk_edit` for simplest API.

pub fn displayvideo_partners_channels_sites_bulk_edit_builder<R>(
    client: &SimpleHttpClient<R>,
    partnerId: &String,
    channelId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/partners/{}/channels/{}/sites:bulkEdit",
        partnerId, channelId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/partners/{partnerId}/channels/{channelsId}/sites:bulkEdit
/// Bulk edits sites under a single channel. The operation will delete the sites provided in BulkEditSitesRequest.deleted_sites and then create the sites provided in BulkEditSitesRequest.created_sites.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_partners_channels_sites_bulk_edit_execute()` or `displayvideo_partners_channels_sites_bulk_edit`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_partners_channels_sites_bulk_edit_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_partners_channels_sites_bulk_edit_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BulkEditSitesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BulkEditSitesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/partners/{partnerId}/channels/{channelsId}/sites:bulkEdit
/// Bulk edits sites under a single channel. The operation will delete the sites provided in BulkEditSitesRequest.deleted_sites and then create the sites provided in BulkEditSitesRequest.created_sites.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_partners_channels_sites_bulk_edit_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_partners_channels_sites_bulk_edit_task()`.
/// For the simplest API, use `displayvideo_partners_channels_sites_bulk_edit()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_partners_channels_sites_bulk_edit_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_partners_channels_sites_bulk_edit_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BulkEditSitesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_partners_channels_sites_bulk_edit_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_partners_channels_sites_bulk_edit`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoPartnersChannelsSitesBulkEditArgs {
    /// Path parameter: partnerId
    pub partnerId: String,
    /// Path parameter: channelId
    pub channelId: String,
}

/// POST v4/partners/{partnerId}/channels/{channelsId}/sites:bulkEdit
/// Bulk edits sites under a single channel. The operation will delete the sites provided in BulkEditSitesRequest.deleted_sites and then create the sites provided in BulkEditSitesRequest.created_sites.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_partners_channels_sites_bulk_edit_builder()` + `displayvideo_partners_channels_sites_bulk_edit_execute()`.
/// For task-level control, use `displayvideo_partners_channels_sites_bulk_edit_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_partners_channels_sites_bulk_edit(
    client: &SimpleHttpClient,
    args: &DisplayvideoPartnersChannelsSitesBulkEditArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BulkEditSitesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_partners_channels_sites_bulk_edit_builder(
        client,
        &args.partnerId,
        &args.channelId,
    )?;
    displayvideo_partners_channels_sites_bulk_edit_execute(builder)
}

/// POST v4/partners/{partnerId}/channels/{channelsId}/sites
/// Creates a site in a channel.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_partners_channels_sites_create_execute()` to send, or `displayvideo_partners_channels_sites_create` for simplest API.

pub fn displayvideo_partners_channels_sites_create_builder<R>(
    client: &SimpleHttpClient<R>,
    partnerId: &String,
    channelId: &String,
    advertiserId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/partners/{}/channels/{}/sites",
        partnerId, channelId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/partners/{partnerId}/channels/{channelsId}/sites
/// Creates a site in a channel.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_partners_channels_sites_create_execute()` or `displayvideo_partners_channels_sites_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_partners_channels_sites_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_partners_channels_sites_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Site>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Site = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/partners/{partnerId}/channels/{channelsId}/sites
/// Creates a site in a channel.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_partners_channels_sites_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_partners_channels_sites_create_task()`.
/// For the simplest API, use `displayvideo_partners_channels_sites_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_partners_channels_sites_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_partners_channels_sites_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Site>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_partners_channels_sites_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_partners_channels_sites_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoPartnersChannelsSitesCreateArgs {
    /// Path parameter: partnerId
    pub partnerId: String,
    /// Path parameter: channelId
    pub channelId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
}

/// POST v4/partners/{partnerId}/channels/{channelsId}/sites
/// Creates a site in a channel.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_partners_channels_sites_create_builder()` + `displayvideo_partners_channels_sites_create_execute()`.
/// For task-level control, use `displayvideo_partners_channels_sites_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_partners_channels_sites_create(
    client: &SimpleHttpClient,
    args: &DisplayvideoPartnersChannelsSitesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Site>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_partners_channels_sites_create_builder(
        client,
        &args.partnerId,
        &args.channelId,
        &args.advertiserId,
    )?;
    displayvideo_partners_channels_sites_create_execute(builder)
}

/// DELETE v4/partners/{partnerId}/channels/{channelsId}/sites/{sitesId}
/// Deletes a site from a channel.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_partners_channels_sites_delete_execute()` to send, or `displayvideo_partners_channels_sites_delete` for simplest API.

pub fn displayvideo_partners_channels_sites_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    partnerId: &String,
    channelId: &String,
    urlOrAppId: &String,
    advertiserId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/partners/{}/channels/{}/sites/{}",
        partnerId, channelId, urlOrAppId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v4/partners/{partnerId}/channels/{channelsId}/sites/{sitesId}
/// Deletes a site from a channel.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_partners_channels_sites_delete_execute()` or `displayvideo_partners_channels_sites_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_partners_channels_sites_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_partners_channels_sites_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v4/partners/{partnerId}/channels/{channelsId}/sites/{sitesId}
/// Deletes a site from a channel.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_partners_channels_sites_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_partners_channels_sites_delete_task()`.
/// For the simplest API, use `displayvideo_partners_channels_sites_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_partners_channels_sites_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_partners_channels_sites_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_partners_channels_sites_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_partners_channels_sites_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoPartnersChannelsSitesDeleteArgs {
    /// Path parameter: partnerId
    pub partnerId: String,
    /// Path parameter: channelId
    pub channelId: String,
    /// Path parameter: urlOrAppId
    pub urlOrAppId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
}

/// DELETE v4/partners/{partnerId}/channels/{channelsId}/sites/{sitesId}
/// Deletes a site from a channel.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_partners_channels_sites_delete_builder()` + `displayvideo_partners_channels_sites_delete_execute()`.
/// For task-level control, use `displayvideo_partners_channels_sites_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_partners_channels_sites_delete(
    client: &SimpleHttpClient,
    args: &DisplayvideoPartnersChannelsSitesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_partners_channels_sites_delete_builder(
        client,
        &args.partnerId,
        &args.channelId,
        &args.urlOrAppId,
        &args.advertiserId,
    )?;
    displayvideo_partners_channels_sites_delete_execute(builder)
}

/// GET v4/partners/{partnersId}/channels/{channelsId}/sites
/// Lists sites in a channel.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_partners_channels_sites_list_execute()` to send, or `displayvideo_partners_channels_sites_list` for simplest API.

pub fn displayvideo_partners_channels_sites_list_builder<R>(
    client: &SimpleHttpClient<R>,
    partnerId: &String,
    channelId: &String,
    advertiserId: &Option<String>,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/partners/{}/channels/{}/sites",
        partnerId, channelId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/partners/{partnersId}/channels/{channelsId}/sites
/// Lists sites in a channel.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_partners_channels_sites_list_execute()` or `displayvideo_partners_channels_sites_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_partners_channels_sites_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_partners_channels_sites_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSitesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSitesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/partners/{partnersId}/channels/{channelsId}/sites
/// Lists sites in a channel.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_partners_channels_sites_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_partners_channels_sites_list_task()`.
/// For the simplest API, use `displayvideo_partners_channels_sites_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_partners_channels_sites_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_partners_channels_sites_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSitesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_partners_channels_sites_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_partners_channels_sites_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoPartnersChannelsSitesListArgs {
    /// Path parameter: partnerId
    pub partnerId: String,
    /// Path parameter: channelId
    pub channelId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v4/partners/{partnersId}/channels/{channelsId}/sites
/// Lists sites in a channel.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_partners_channels_sites_list_builder()` + `displayvideo_partners_channels_sites_list_execute()`.
/// For task-level control, use `displayvideo_partners_channels_sites_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_partners_channels_sites_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoPartnersChannelsSitesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSitesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_partners_channels_sites_list_builder(
        client,
        &args.partnerId,
        &args.channelId,
        &args.advertiserId,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    displayvideo_partners_channels_sites_list_execute(builder)
}

/// POST v4/partners/{partnerId}/channels/{channelsId}/sites:replace
/// Replaces all of the sites under a single channel. The operation will replace the sites under a channel with the sites provided in ReplaceSitesRequest.new_sites. **This method regularly experiences high latency.** We recommend [increasing your default timeout](/display-`video/api/guides/best-practices/timeouts`#client_library_timeout) to avoid errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_partners_channels_sites_replace_execute()` to send, or `displayvideo_partners_channels_sites_replace` for simplest API.

pub fn displayvideo_partners_channels_sites_replace_builder<R>(
    client: &SimpleHttpClient<R>,
    partnerId: &String,
    channelId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/partners/{}/channels/{}/sites:replace",
        partnerId, channelId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/partners/{partnerId}/channels/{channelsId}/sites:replace
/// Replaces all of the sites under a single channel. The operation will replace the sites under a channel with the sites provided in ReplaceSitesRequest.new_sites. **This method regularly experiences high latency.** We recommend [increasing your default timeout](/display-`video/api/guides/best-practices/timeouts`#client_library_timeout) to avoid errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_partners_channels_sites_replace_execute()` or `displayvideo_partners_channels_sites_replace`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_partners_channels_sites_replace_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_partners_channels_sites_replace_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReplaceSitesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReplaceSitesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/partners/{partnerId}/channels/{channelsId}/sites:replace
/// Replaces all of the sites under a single channel. The operation will replace the sites under a channel with the sites provided in ReplaceSitesRequest.new_sites. **This method regularly experiences high latency.** We recommend [increasing your default timeout](/display-`video/api/guides/best-practices/timeouts`#client_library_timeout) to avoid errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_partners_channels_sites_replace_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_partners_channels_sites_replace_task()`.
/// For the simplest API, use `displayvideo_partners_channels_sites_replace()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_partners_channels_sites_replace_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_partners_channels_sites_replace_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReplaceSitesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_partners_channels_sites_replace_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_partners_channels_sites_replace`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoPartnersChannelsSitesReplaceArgs {
    /// Path parameter: partnerId
    pub partnerId: String,
    /// Path parameter: channelId
    pub channelId: String,
}

/// POST v4/partners/{partnerId}/channels/{channelsId}/sites:replace
/// Replaces all of the sites under a single channel. The operation will replace the sites under a channel with the sites provided in ReplaceSitesRequest.new_sites. **This method regularly experiences high latency.** We recommend [increasing your default timeout](/display-`video/api/guides/best-practices/timeouts`#client_library_timeout) to avoid errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_partners_channels_sites_replace_builder()` + `displayvideo_partners_channels_sites_replace_execute()`.
/// For task-level control, use `displayvideo_partners_channels_sites_replace_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_partners_channels_sites_replace(
    client: &SimpleHttpClient,
    args: &DisplayvideoPartnersChannelsSitesReplaceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ReplaceSitesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_partners_channels_sites_replace_builder(
        client,
        &args.partnerId,
        &args.channelId,
    )?;
    displayvideo_partners_channels_sites_replace_execute(builder)
}

/// POST v4/partners/{partnersId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions
/// Assigns a targeting option to a partner. Returns the assigned targeting option if successful.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_partners_targeting_types_assigned_targeting_options_create_execute()` to send, or `displayvideo_partners_targeting_types_assigned_targeting_options_create` for simplest API.

pub fn displayvideo_partners_targeting_types_assigned_targeting_options_create_builder<R>(
    client: &SimpleHttpClient<R>,
    partnerId: &String,
    targetingType: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/partners/{}/targetingTypes/{}/assignedTargetingOptions",
        partnerId,
        targetingType,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/partners/{partnersId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions
/// Assigns a targeting option to a partner. Returns the assigned targeting option if successful.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_partners_targeting_types_assigned_targeting_options_create_execute()` or `displayvideo_partners_targeting_types_assigned_targeting_options_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_partners_targeting_types_assigned_targeting_options_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_partners_targeting_types_assigned_targeting_options_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AssignedTargetingOption>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AssignedTargetingOption = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/partners/{partnersId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions
/// Assigns a targeting option to a partner. Returns the assigned targeting option if successful.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_partners_targeting_types_assigned_targeting_options_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_partners_targeting_types_assigned_targeting_options_create_task()`.
/// For the simplest API, use `displayvideo_partners_targeting_types_assigned_targeting_options_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_partners_targeting_types_assigned_targeting_options_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_partners_targeting_types_assigned_targeting_options_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AssignedTargetingOption>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        displayvideo_partners_targeting_types_assigned_targeting_options_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_partners_targeting_types_assigned_targeting_options_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoPartnersTargetingTypesAssignedTargetingOptionsCreateArgs {
    /// Path parameter: partnerId
    pub partnerId: String,
    /// Path parameter: targetingType
    pub targetingType: String,
}

/// POST v4/partners/{partnersId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions
/// Assigns a targeting option to a partner. Returns the assigned targeting option if successful.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_partners_targeting_types_assigned_targeting_options_create_builder()` + `displayvideo_partners_targeting_types_assigned_targeting_options_create_execute()`.
/// For task-level control, use `displayvideo_partners_targeting_types_assigned_targeting_options_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_partners_targeting_types_assigned_targeting_options_create(
    client: &SimpleHttpClient,
    args: &DisplayvideoPartnersTargetingTypesAssignedTargetingOptionsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AssignedTargetingOption>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_partners_targeting_types_assigned_targeting_options_create_builder(
        client,
        &args.partnerId,
        &args.targetingType,
    )?;
    displayvideo_partners_targeting_types_assigned_targeting_options_create_execute(builder)
}

/// DELETE v4/partners/{partnersId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions/{assignedTargetingOptionsId}
/// Deletes an assigned targeting option from a partner.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_partners_targeting_types_assigned_targeting_options_delete_execute()` to send, or `displayvideo_partners_targeting_types_assigned_targeting_options_delete` for simplest API.

pub fn displayvideo_partners_targeting_types_assigned_targeting_options_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    partnerId: &String,
    targetingType: &String,
    assignedTargetingOptionId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/partners/{}/targetingTypes/{}/assignedTargetingOptions/{}",
        partnerId,
        targetingType,
        assignedTargetingOptionId,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v4/partners/{partnersId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions/{assignedTargetingOptionsId}
/// Deletes an assigned targeting option from a partner.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_partners_targeting_types_assigned_targeting_options_delete_execute()` or `displayvideo_partners_targeting_types_assigned_targeting_options_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_partners_targeting_types_assigned_targeting_options_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_partners_targeting_types_assigned_targeting_options_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v4/partners/{partnersId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions/{assignedTargetingOptionsId}
/// Deletes an assigned targeting option from a partner.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_partners_targeting_types_assigned_targeting_options_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_partners_targeting_types_assigned_targeting_options_delete_task()`.
/// For the simplest API, use `displayvideo_partners_targeting_types_assigned_targeting_options_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_partners_targeting_types_assigned_targeting_options_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_partners_targeting_types_assigned_targeting_options_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        displayvideo_partners_targeting_types_assigned_targeting_options_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_partners_targeting_types_assigned_targeting_options_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoPartnersTargetingTypesAssignedTargetingOptionsDeleteArgs {
    /// Path parameter: partnerId
    pub partnerId: String,
    /// Path parameter: targetingType
    pub targetingType: String,
    /// Path parameter: assignedTargetingOptionId
    pub assignedTargetingOptionId: String,
}

/// DELETE v4/partners/{partnersId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions/{assignedTargetingOptionsId}
/// Deletes an assigned targeting option from a partner.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_partners_targeting_types_assigned_targeting_options_delete_builder()` + `displayvideo_partners_targeting_types_assigned_targeting_options_delete_execute()`.
/// For task-level control, use `displayvideo_partners_targeting_types_assigned_targeting_options_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_partners_targeting_types_assigned_targeting_options_delete(
    client: &SimpleHttpClient,
    args: &DisplayvideoPartnersTargetingTypesAssignedTargetingOptionsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_partners_targeting_types_assigned_targeting_options_delete_builder(
        client,
        &args.partnerId,
        &args.targetingType,
        &args.assignedTargetingOptionId,
    )?;
    displayvideo_partners_targeting_types_assigned_targeting_options_delete_execute(builder)
}

/// GET v4/partners/{partnersId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions/{assignedTargetingOptionsId}
/// Gets a single targeting option assigned to a partner.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_partners_targeting_types_assigned_targeting_options_get_execute()` to send, or `displayvideo_partners_targeting_types_assigned_targeting_options_get` for simplest API.

pub fn displayvideo_partners_targeting_types_assigned_targeting_options_get_builder<R>(
    client: &SimpleHttpClient<R>,
    partnerId: &String,
    targetingType: &String,
    assignedTargetingOptionId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/partners/{}/targetingTypes/{}/assignedTargetingOptions/{}",
        partnerId,
        targetingType,
        assignedTargetingOptionId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/partners/{partnersId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions/{assignedTargetingOptionsId}
/// Gets a single targeting option assigned to a partner.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_partners_targeting_types_assigned_targeting_options_get_execute()` or `displayvideo_partners_targeting_types_assigned_targeting_options_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_partners_targeting_types_assigned_targeting_options_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_partners_targeting_types_assigned_targeting_options_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AssignedTargetingOption>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AssignedTargetingOption = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/partners/{partnersId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions/{assignedTargetingOptionsId}
/// Gets a single targeting option assigned to a partner.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_partners_targeting_types_assigned_targeting_options_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_partners_targeting_types_assigned_targeting_options_get_task()`.
/// For the simplest API, use `displayvideo_partners_targeting_types_assigned_targeting_options_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_partners_targeting_types_assigned_targeting_options_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_partners_targeting_types_assigned_targeting_options_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AssignedTargetingOption>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_partners_targeting_types_assigned_targeting_options_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_partners_targeting_types_assigned_targeting_options_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoPartnersTargetingTypesAssignedTargetingOptionsGetArgs {
    /// Path parameter: partnerId
    pub partnerId: String,
    /// Path parameter: targetingType
    pub targetingType: String,
    /// Path parameter: assignedTargetingOptionId
    pub assignedTargetingOptionId: String,
}

/// GET v4/partners/{partnersId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions/{assignedTargetingOptionsId}
/// Gets a single targeting option assigned to a partner.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_partners_targeting_types_assigned_targeting_options_get_builder()` + `displayvideo_partners_targeting_types_assigned_targeting_options_get_execute()`.
/// For task-level control, use `displayvideo_partners_targeting_types_assigned_targeting_options_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_partners_targeting_types_assigned_targeting_options_get(
    client: &SimpleHttpClient,
    args: &DisplayvideoPartnersTargetingTypesAssignedTargetingOptionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AssignedTargetingOption>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_partners_targeting_types_assigned_targeting_options_get_builder(
        client,
        &args.partnerId,
        &args.targetingType,
        &args.assignedTargetingOptionId,
    )?;
    displayvideo_partners_targeting_types_assigned_targeting_options_get_execute(builder)
}

/// GET v4/partners/{partnersId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions
/// Lists the targeting options assigned to a partner.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_partners_targeting_types_assigned_targeting_options_list_execute()` to send, or `displayvideo_partners_targeting_types_assigned_targeting_options_list` for simplest API.

pub fn displayvideo_partners_targeting_types_assigned_targeting_options_list_builder<R>(
    client: &SimpleHttpClient<R>,
    partnerId: &String,
    targetingType: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/partners/{}/targetingTypes/{}/assignedTargetingOptions",
        partnerId,
        targetingType,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/partners/{partnersId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions
/// Lists the targeting options assigned to a partner.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_partners_targeting_types_assigned_targeting_options_list_execute()` or `displayvideo_partners_targeting_types_assigned_targeting_options_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_partners_targeting_types_assigned_targeting_options_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_partners_targeting_types_assigned_targeting_options_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListPartnerAssignedTargetingOptionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListPartnerAssignedTargetingOptionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/partners/{partnersId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions
/// Lists the targeting options assigned to a partner.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_partners_targeting_types_assigned_targeting_options_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_partners_targeting_types_assigned_targeting_options_list_task()`.
/// For the simplest API, use `displayvideo_partners_targeting_types_assigned_targeting_options_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_partners_targeting_types_assigned_targeting_options_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_partners_targeting_types_assigned_targeting_options_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListPartnerAssignedTargetingOptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_partners_targeting_types_assigned_targeting_options_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_partners_targeting_types_assigned_targeting_options_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoPartnersTargetingTypesAssignedTargetingOptionsListArgs {
    /// Path parameter: partnerId
    pub partnerId: String,
    /// Path parameter: targetingType
    pub targetingType: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v4/partners/{partnersId}/targetingTypes/{targetingTypesId}/assignedTargetingOptions
/// Lists the targeting options assigned to a partner.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_partners_targeting_types_assigned_targeting_options_list_builder()` + `displayvideo_partners_targeting_types_assigned_targeting_options_list_execute()`.
/// For task-level control, use `displayvideo_partners_targeting_types_assigned_targeting_options_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_partners_targeting_types_assigned_targeting_options_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoPartnersTargetingTypesAssignedTargetingOptionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListPartnerAssignedTargetingOptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_partners_targeting_types_assigned_targeting_options_list_builder(
        client,
        &args.partnerId,
        &args.targetingType,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    displayvideo_partners_targeting_types_assigned_targeting_options_list_execute(builder)
}

/// POST v4/sdfdownloadtasks
/// Creates an SDF Download Task. Returns an Operation. An SDF Download Task is a long-running, asynchronous operation. The metadata type of this operation is SdfDownloadTaskMetadata. If the request is successful, the response type of the operation is SdfDownloadTask. The response will not include the download files, which must be retrieved with media.download. The state of operation can be retrieved with sdfdownloadtasks.operations.get. Any errors can be found in the error.message. Note that error.details is expected to be empty.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_sdfdownloadtasks_create_execute()` to send, or `displayvideo_sdfdownloadtasks_create` for simplest API.

pub fn displayvideo_sdfdownloadtasks_create_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://displayvideo.googleapis.com/v4/sdfdownloadtasks",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/sdfdownloadtasks
/// Creates an SDF Download Task. Returns an Operation. An SDF Download Task is a long-running, asynchronous operation. The metadata type of this operation is SdfDownloadTaskMetadata. If the request is successful, the response type of the operation is SdfDownloadTask. The response will not include the download files, which must be retrieved with media.download. The state of operation can be retrieved with sdfdownloadtasks.operations.get. Any errors can be found in the error.message. Note that error.details is expected to be empty.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_sdfdownloadtasks_create_execute()` or `displayvideo_sdfdownloadtasks_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_sdfdownloadtasks_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_sdfdownloadtasks_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/sdfdownloadtasks
/// Creates an SDF Download Task. Returns an Operation. An SDF Download Task is a long-running, asynchronous operation. The metadata type of this operation is SdfDownloadTaskMetadata. If the request is successful, the response type of the operation is SdfDownloadTask. The response will not include the download files, which must be retrieved with media.download. The state of operation can be retrieved with sdfdownloadtasks.operations.get. Any errors can be found in the error.message. Note that error.details is expected to be empty.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_sdfdownloadtasks_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_sdfdownloadtasks_create_task()`.
/// For the simplest API, use `displayvideo_sdfdownloadtasks_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_sdfdownloadtasks_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_sdfdownloadtasks_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_sdfdownloadtasks_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v4/sdfdownloadtasks
/// Creates an SDF Download Task. Returns an Operation. An SDF Download Task is a long-running, asynchronous operation. The metadata type of this operation is SdfDownloadTaskMetadata. If the request is successful, the response type of the operation is SdfDownloadTask. The response will not include the download files, which must be retrieved with media.download. The state of operation can be retrieved with sdfdownloadtasks.operations.get. Any errors can be found in the error.message. Note that error.details is expected to be empty.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_sdfdownloadtasks_create_builder()` + `displayvideo_sdfdownloadtasks_create_execute()`.
/// For task-level control, use `displayvideo_sdfdownloadtasks_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_sdfdownloadtasks_create(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_sdfdownloadtasks_create_builder(client)?;
    displayvideo_sdfdownloadtasks_create_execute(builder)
}

/// GET v4/sdfdownloadtasks/operations/{operationsId}
/// Gets the latest state of an asynchronous SDF download task operation. Clients should poll this method at intervals of 30 seconds.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_sdfdownloadtasks_operations_get_execute()` to send, or `displayvideo_sdfdownloadtasks_operations_get` for simplest API.

pub fn displayvideo_sdfdownloadtasks_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/sdfdownloadtasks/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/sdfdownloadtasks/operations/{operationsId}
/// Gets the latest state of an asynchronous SDF download task operation. Clients should poll this method at intervals of 30 seconds.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_sdfdownloadtasks_operations_get_execute()` or `displayvideo_sdfdownloadtasks_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_sdfdownloadtasks_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_sdfdownloadtasks_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/sdfdownloadtasks/operations/{operationsId}
/// Gets the latest state of an asynchronous SDF download task operation. Clients should poll this method at intervals of 30 seconds.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_sdfdownloadtasks_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_sdfdownloadtasks_operations_get_task()`.
/// For the simplest API, use `displayvideo_sdfdownloadtasks_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_sdfdownloadtasks_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_sdfdownloadtasks_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_sdfdownloadtasks_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_sdfdownloadtasks_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoSdfdownloadtasksOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v4/sdfdownloadtasks/operations/{operationsId}
/// Gets the latest state of an asynchronous SDF download task operation. Clients should poll this method at intervals of 30 seconds.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_sdfdownloadtasks_operations_get_builder()` + `displayvideo_sdfdownloadtasks_operations_get_execute()`.
/// For task-level control, use `displayvideo_sdfdownloadtasks_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_sdfdownloadtasks_operations_get(
    client: &SimpleHttpClient,
    args: &DisplayvideoSdfdownloadtasksOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_sdfdownloadtasks_operations_get_builder(client, &args.name)?;
    displayvideo_sdfdownloadtasks_operations_get_execute(builder)
}

/// GET v4/sdfuploadtasks/operations/{operationsId}
/// Gets the latest state of an asynchronous SDF download task operation. Clients should poll this method at intervals of 30 seconds.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_sdfuploadtasks_operations_get_execute()` to send, or `displayvideo_sdfuploadtasks_operations_get` for simplest API.

pub fn displayvideo_sdfuploadtasks_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/sdfuploadtasks/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/sdfuploadtasks/operations/{operationsId}
/// Gets the latest state of an asynchronous SDF download task operation. Clients should poll this method at intervals of 30 seconds.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_sdfuploadtasks_operations_get_execute()` or `displayvideo_sdfuploadtasks_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_sdfuploadtasks_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_sdfuploadtasks_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/sdfuploadtasks/operations/{operationsId}
/// Gets the latest state of an asynchronous SDF download task operation. Clients should poll this method at intervals of 30 seconds.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_sdfuploadtasks_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_sdfuploadtasks_operations_get_task()`.
/// For the simplest API, use `displayvideo_sdfuploadtasks_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_sdfuploadtasks_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_sdfuploadtasks_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_sdfuploadtasks_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_sdfuploadtasks_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoSdfuploadtasksOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v4/sdfuploadtasks/operations/{operationsId}
/// Gets the latest state of an asynchronous SDF download task operation. Clients should poll this method at intervals of 30 seconds.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_sdfuploadtasks_operations_get_builder()` + `displayvideo_sdfuploadtasks_operations_get_execute()`.
/// For task-level control, use `displayvideo_sdfuploadtasks_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_sdfuploadtasks_operations_get(
    client: &SimpleHttpClient,
    args: &DisplayvideoSdfuploadtasksOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_sdfuploadtasks_operations_get_builder(client, &args.name)?;
    displayvideo_sdfuploadtasks_operations_get_execute(builder)
}

/// GET v4/targetingTypes/{targetingTypesId}/targetingOptions/{targetingOptionsId}
/// Gets a single targeting option.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_targeting_types_targeting_options_get_execute()` to send, or `displayvideo_targeting_types_targeting_options_get` for simplest API.

pub fn displayvideo_targeting_types_targeting_options_get_builder<R>(
    client: &SimpleHttpClient<R>,
    targetingType: &String,
    targetingOptionId: &String,
    advertiserId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/targetingTypes/{}/targetingOptions/{}",
        targetingType, targetingOptionId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/targetingTypes/{targetingTypesId}/targetingOptions/{targetingOptionsId}
/// Gets a single targeting option.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_targeting_types_targeting_options_get_execute()` or `displayvideo_targeting_types_targeting_options_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_targeting_types_targeting_options_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_targeting_types_targeting_options_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TargetingOption>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TargetingOption = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/targetingTypes/{targetingTypesId}/targetingOptions/{targetingOptionsId}
/// Gets a single targeting option.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_targeting_types_targeting_options_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_targeting_types_targeting_options_get_task()`.
/// For the simplest API, use `displayvideo_targeting_types_targeting_options_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_targeting_types_targeting_options_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_targeting_types_targeting_options_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetingOption>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_targeting_types_targeting_options_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_targeting_types_targeting_options_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoTargetingTypesTargetingOptionsGetArgs {
    /// Path parameter: targetingType
    pub targetingType: String,
    /// Path parameter: targetingOptionId
    pub targetingOptionId: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
}

/// GET v4/targetingTypes/{targetingTypesId}/targetingOptions/{targetingOptionsId}
/// Gets a single targeting option.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_targeting_types_targeting_options_get_builder()` + `displayvideo_targeting_types_targeting_options_get_execute()`.
/// For task-level control, use `displayvideo_targeting_types_targeting_options_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_targeting_types_targeting_options_get(
    client: &SimpleHttpClient,
    args: &DisplayvideoTargetingTypesTargetingOptionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TargetingOption>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_targeting_types_targeting_options_get_builder(
        client,
        &args.targetingType,
        &args.targetingOptionId,
        &args.advertiserId,
    )?;
    displayvideo_targeting_types_targeting_options_get_execute(builder)
}

/// GET v4/targetingTypes/{targetingTypesId}/targetingOptions
/// Lists targeting options of a given type.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_targeting_types_targeting_options_list_execute()` to send, or `displayvideo_targeting_types_targeting_options_list` for simplest API.

pub fn displayvideo_targeting_types_targeting_options_list_builder<R>(
    client: &SimpleHttpClient<R>,
    targetingType: &String,
    advertiserId: &Option<String>,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/targetingTypes/{}/targetingOptions",
        targetingType,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = advertiserId.as_ref() {
        query_parts.push(format!("advertiserId={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/targetingTypes/{targetingTypesId}/targetingOptions
/// Lists targeting options of a given type.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_targeting_types_targeting_options_list_execute()` or `displayvideo_targeting_types_targeting_options_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_targeting_types_targeting_options_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_targeting_types_targeting_options_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListTargetingOptionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTargetingOptionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/targetingTypes/{targetingTypesId}/targetingOptions
/// Lists targeting options of a given type.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_targeting_types_targeting_options_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_targeting_types_targeting_options_list_task()`.
/// For the simplest API, use `displayvideo_targeting_types_targeting_options_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_targeting_types_targeting_options_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_targeting_types_targeting_options_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListTargetingOptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_targeting_types_targeting_options_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_targeting_types_targeting_options_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoTargetingTypesTargetingOptionsListArgs {
    /// Path parameter: targetingType
    pub targetingType: String,
    /// Query parameter: advertiserId
    pub advertiserId: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v4/targetingTypes/{targetingTypesId}/targetingOptions
/// Lists targeting options of a given type.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_targeting_types_targeting_options_list_builder()` + `displayvideo_targeting_types_targeting_options_list_execute()`.
/// For task-level control, use `displayvideo_targeting_types_targeting_options_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_targeting_types_targeting_options_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoTargetingTypesTargetingOptionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListTargetingOptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_targeting_types_targeting_options_list_builder(
        client,
        &args.targetingType,
        &args.advertiserId,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    displayvideo_targeting_types_targeting_options_list_execute(builder)
}

/// POST v4/targetingTypes/{targetingTypesId}/targetingOptions:search
/// Searches for targeting options of a given type based on the given search terms.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_targeting_types_targeting_options_search_execute()` to send, or `displayvideo_targeting_types_targeting_options_search` for simplest API.

pub fn displayvideo_targeting_types_targeting_options_search_builder<R>(
    client: &SimpleHttpClient<R>,
    targetingType: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/targetingTypes/{}/targetingOptions:search",
        targetingType,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/targetingTypes/{targetingTypesId}/targetingOptions:search
/// Searches for targeting options of a given type based on the given search terms.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_targeting_types_targeting_options_search_execute()` or `displayvideo_targeting_types_targeting_options_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_targeting_types_targeting_options_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_targeting_types_targeting_options_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchTargetingOptionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchTargetingOptionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/targetingTypes/{targetingTypesId}/targetingOptions:search
/// Searches for targeting options of a given type based on the given search terms.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_targeting_types_targeting_options_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_targeting_types_targeting_options_search_task()`.
/// For the simplest API, use `displayvideo_targeting_types_targeting_options_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_targeting_types_targeting_options_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_targeting_types_targeting_options_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchTargetingOptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_targeting_types_targeting_options_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_targeting_types_targeting_options_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoTargetingTypesTargetingOptionsSearchArgs {
    /// Path parameter: targetingType
    pub targetingType: String,
}

/// POST v4/targetingTypes/{targetingTypesId}/targetingOptions:search
/// Searches for targeting options of a given type based on the given search terms.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_targeting_types_targeting_options_search_builder()` + `displayvideo_targeting_types_targeting_options_search_execute()`.
/// For task-level control, use `displayvideo_targeting_types_targeting_options_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_targeting_types_targeting_options_search(
    client: &SimpleHttpClient,
    args: &DisplayvideoTargetingTypesTargetingOptionsSearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchTargetingOptionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        displayvideo_targeting_types_targeting_options_search_builder(client, &args.targetingType)?;
    displayvideo_targeting_types_targeting_options_search_execute(builder)
}

/// POST v4/users/{usersId}:bulkEditAssignedUserRoles
/// Bulk edits user roles for a user. The operation will delete the assigned user roles provided in BulkEditAssignedUserRolesRequest.`deletedAssignedUserRoles` and then assign the user roles provided in BulkEditAssignedUserRolesRequest.`createdAssignedUserRoles`. This method has unique authentication requirements. Read the prerequisites in our [Managing Users guide](/display-`video/api/guides/users/overview`#prerequisites) before using this method. The "Try this method" feature does not work for this method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_users_bulk_edit_assigned_user_roles_execute()` to send, or `displayvideo_users_bulk_edit_assigned_user_roles` for simplest API.

pub fn displayvideo_users_bulk_edit_assigned_user_roles_builder<R>(
    client: &SimpleHttpClient<R>,
    userId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://displayvideo.googleapis.com/v4/users/{}:bulkEditAssignedUserRoles",
        userId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/users/{usersId}:bulkEditAssignedUserRoles
/// Bulk edits user roles for a user. The operation will delete the assigned user roles provided in BulkEditAssignedUserRolesRequest.`deletedAssignedUserRoles` and then assign the user roles provided in BulkEditAssignedUserRolesRequest.`createdAssignedUserRoles`. This method has unique authentication requirements. Read the prerequisites in our [Managing Users guide](/display-`video/api/guides/users/overview`#prerequisites) before using this method. The "Try this method" feature does not work for this method.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_users_bulk_edit_assigned_user_roles_execute()` or `displayvideo_users_bulk_edit_assigned_user_roles`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_users_bulk_edit_assigned_user_roles_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_users_bulk_edit_assigned_user_roles_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BulkEditAssignedUserRolesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BulkEditAssignedUserRolesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/users/{usersId}:bulkEditAssignedUserRoles
/// Bulk edits user roles for a user. The operation will delete the assigned user roles provided in BulkEditAssignedUserRolesRequest.`deletedAssignedUserRoles` and then assign the user roles provided in BulkEditAssignedUserRolesRequest.`createdAssignedUserRoles`. This method has unique authentication requirements. Read the prerequisites in our [Managing Users guide](/display-`video/api/guides/users/overview`#prerequisites) before using this method. The "Try this method" feature does not work for this method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_users_bulk_edit_assigned_user_roles_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_users_bulk_edit_assigned_user_roles_task()`.
/// For the simplest API, use `displayvideo_users_bulk_edit_assigned_user_roles()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_users_bulk_edit_assigned_user_roles_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_users_bulk_edit_assigned_user_roles_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BulkEditAssignedUserRolesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_users_bulk_edit_assigned_user_roles_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_users_bulk_edit_assigned_user_roles`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoUsersBulkEditAssignedUserRolesArgs {
    /// Path parameter: userId
    pub userId: String,
}

/// POST v4/users/{usersId}:bulkEditAssignedUserRoles
/// Bulk edits user roles for a user. The operation will delete the assigned user roles provided in BulkEditAssignedUserRolesRequest.`deletedAssignedUserRoles` and then assign the user roles provided in BulkEditAssignedUserRolesRequest.`createdAssignedUserRoles`. This method has unique authentication requirements. Read the prerequisites in our [Managing Users guide](/display-`video/api/guides/users/overview`#prerequisites) before using this method. The "Try this method" feature does not work for this method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_users_bulk_edit_assigned_user_roles_builder()` + `displayvideo_users_bulk_edit_assigned_user_roles_execute()`.
/// For task-level control, use `displayvideo_users_bulk_edit_assigned_user_roles_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_users_bulk_edit_assigned_user_roles(
    client: &SimpleHttpClient,
    args: &DisplayvideoUsersBulkEditAssignedUserRolesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BulkEditAssignedUserRolesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_users_bulk_edit_assigned_user_roles_builder(client, &args.userId)?;
    displayvideo_users_bulk_edit_assigned_user_roles_execute(builder)
}

/// POST v4/users
/// Creates a new user. Returns the newly created user if successful. This method has unique authentication requirements. Read the prerequisites in our [Managing Users guide](/display-`video/api/guides/users/overview`#prerequisites) before using this method. The "Try this method" feature does not work for this method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_users_create_execute()` to send, or `displayvideo_users_create` for simplest API.

pub fn displayvideo_users_create_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://displayvideo.googleapis.com/v4/users",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v4/users
/// Creates a new user. Returns the newly created user if successful. This method has unique authentication requirements. Read the prerequisites in our [Managing Users guide](/display-`video/api/guides/users/overview`#prerequisites) before using this method. The "Try this method" feature does not work for this method.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_users_create_execute()` or `displayvideo_users_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_users_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_users_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<User>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: User = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v4/users
/// Creates a new user. Returns the newly created user if successful. This method has unique authentication requirements. Read the prerequisites in our [Managing Users guide](/display-`video/api/guides/users/overview`#prerequisites) before using this method. The "Try this method" feature does not work for this method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_users_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_users_create_task()`.
/// For the simplest API, use `displayvideo_users_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_users_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_users_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<User>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_users_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v4/users
/// Creates a new user. Returns the newly created user if successful. This method has unique authentication requirements. Read the prerequisites in our [Managing Users guide](/display-`video/api/guides/users/overview`#prerequisites) before using this method. The "Try this method" feature does not work for this method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_users_create_builder()` + `displayvideo_users_create_execute()`.
/// For task-level control, use `displayvideo_users_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_users_create(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<User>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_users_create_builder(client)?;
    displayvideo_users_create_execute(builder)
}

/// DELETE v4/users/{usersId}
/// Deletes a user. This method has unique authentication requirements. Read the prerequisites in our [Managing Users guide](/display-`video/api/guides/users/overview`#prerequisites) before using this method. The "Try this method" feature does not work for this method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_users_delete_execute()` to send, or `displayvideo_users_delete` for simplest API.

pub fn displayvideo_users_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    userId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://displayvideo.googleapis.com/v4/users/{}", userId,);

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v4/users/{usersId}
/// Deletes a user. This method has unique authentication requirements. Read the prerequisites in our [Managing Users guide](/display-`video/api/guides/users/overview`#prerequisites) before using this method. The "Try this method" feature does not work for this method.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_users_delete_execute()` or `displayvideo_users_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_users_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_users_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v4/users/{usersId}
/// Deletes a user. This method has unique authentication requirements. Read the prerequisites in our [Managing Users guide](/display-`video/api/guides/users/overview`#prerequisites) before using this method. The "Try this method" feature does not work for this method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_users_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_users_delete_task()`.
/// For the simplest API, use `displayvideo_users_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_users_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_users_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_users_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_users_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoUsersDeleteArgs {
    /// Path parameter: userId
    pub userId: String,
}

/// DELETE v4/users/{usersId}
/// Deletes a user. This method has unique authentication requirements. Read the prerequisites in our [Managing Users guide](/display-`video/api/guides/users/overview`#prerequisites) before using this method. The "Try this method" feature does not work for this method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_users_delete_builder()` + `displayvideo_users_delete_execute()`.
/// For task-level control, use `displayvideo_users_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_users_delete(
    client: &SimpleHttpClient,
    args: &DisplayvideoUsersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_users_delete_builder(client, &args.userId)?;
    displayvideo_users_delete_execute(builder)
}

/// GET v4/users/{usersId}
/// Gets a user. This method has unique authentication requirements. Read the prerequisites in our [Managing Users guide](/display-`video/api/guides/users/overview`#prerequisites) before using this method. The "Try this method" feature does not work for this method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_users_get_execute()` to send, or `displayvideo_users_get` for simplest API.

pub fn displayvideo_users_get_builder<R>(
    client: &SimpleHttpClient<R>,
    userId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://displayvideo.googleapis.com/v4/users/{}", userId,);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/users/{usersId}
/// Gets a user. This method has unique authentication requirements. Read the prerequisites in our [Managing Users guide](/display-`video/api/guides/users/overview`#prerequisites) before using this method. The "Try this method" feature does not work for this method.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_users_get_execute()` or `displayvideo_users_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_users_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_users_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<User>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: User = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/users/{usersId}
/// Gets a user. This method has unique authentication requirements. Read the prerequisites in our [Managing Users guide](/display-`video/api/guides/users/overview`#prerequisites) before using this method. The "Try this method" feature does not work for this method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_users_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_users_get_task()`.
/// For the simplest API, use `displayvideo_users_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_users_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_users_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<User>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_users_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_users_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoUsersGetArgs {
    /// Path parameter: userId
    pub userId: String,
}

/// GET v4/users/{usersId}
/// Gets a user. This method has unique authentication requirements. Read the prerequisites in our [Managing Users guide](/display-`video/api/guides/users/overview`#prerequisites) before using this method. The "Try this method" feature does not work for this method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_users_get_builder()` + `displayvideo_users_get_execute()`.
/// For task-level control, use `displayvideo_users_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_users_get(
    client: &SimpleHttpClient,
    args: &DisplayvideoUsersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<User>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_users_get_builder(client, &args.userId)?;
    displayvideo_users_get_execute(builder)
}

/// GET v4/users
/// Lists users that are accessible to the current user. If two users have user roles on the same partner or advertiser, they can access each other. This method has unique authentication requirements. Read the prerequisites in our [Managing Users guide](/display-`video/api/guides/users/overview`#prerequisites) before using this method. The "Try this method" feature does not work for this method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_users_list_execute()` to send, or `displayvideo_users_list` for simplest API.

pub fn displayvideo_users_list_builder<R>(
    client: &SimpleHttpClient<R>,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://displayvideo.googleapis.com/v4/users",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v4/users
/// Lists users that are accessible to the current user. If two users have user roles on the same partner or advertiser, they can access each other. This method has unique authentication requirements. Read the prerequisites in our [Managing Users guide](/display-`video/api/guides/users/overview`#prerequisites) before using this method. The "Try this method" feature does not work for this method.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_users_list_execute()` or `displayvideo_users_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_users_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_users_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListUsersResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListUsersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v4/users
/// Lists users that are accessible to the current user. If two users have user roles on the same partner or advertiser, they can access each other. This method has unique authentication requirements. Read the prerequisites in our [Managing Users guide](/display-`video/api/guides/users/overview`#prerequisites) before using this method. The "Try this method" feature does not work for this method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_users_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_users_list_task()`.
/// For the simplest API, use `displayvideo_users_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_users_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_users_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListUsersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = displayvideo_users_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_users_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoUsersListArgs {
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v4/users
/// Lists users that are accessible to the current user. If two users have user roles on the same partner or advertiser, they can access each other. This method has unique authentication requirements. Read the prerequisites in our [Managing Users guide](/display-`video/api/guides/users/overview`#prerequisites) before using this method. The "Try this method" feature does not work for this method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_users_list_builder()` + `displayvideo_users_list_execute()`.
/// For task-level control, use `displayvideo_users_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_users_list(
    client: &SimpleHttpClient,
    args: &DisplayvideoUsersListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListUsersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = displayvideo_users_list_builder(
        client,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    displayvideo_users_list_execute(builder)
}

/// PATCH v4/users/{usersId}
/// Updates an existing user. Returns the updated user if successful. This method has unique authentication requirements. Read the prerequisites in our [Managing Users guide](/display-`video/api/guides/users/overview`#prerequisites) before using this method. The "Try this method" feature does not work for this method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `displayvideo_users_patch_execute()` to send, or `displayvideo_users_patch` for simplest API.

pub fn displayvideo_users_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    userId: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://displayvideo.googleapis.com/v4/users/{}", userId,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v4/users/{usersId}
/// Updates an existing user. Returns the updated user if successful. This method has unique authentication requirements. Read the prerequisites in our [Managing Users guide](/display-`video/api/guides/users/overview`#prerequisites) before using this method. The "Try this method" feature does not work for this method.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `displayvideo_users_patch_execute()` or `displayvideo_users_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_users_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_users_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<User>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: User = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v4/users/{usersId}
/// Updates an existing user. Returns the updated user if successful. This method has unique authentication requirements. Read the prerequisites in our [Managing Users guide](/display-`video/api/guides/users/overview`#prerequisites) before using this method. The "Try this method" feature does not work for this method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `displayvideo_users_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `displayvideo_users_patch_task()`.
/// For the simplest API, use `displayvideo_users_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `displayvideo_users_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn displayvideo_users_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<User>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = displayvideo_users_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`displayvideo_users_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DisplayvideoUsersPatchArgs {
    /// Path parameter: userId
    pub userId: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v4/users/{usersId}
/// Updates an existing user. Returns the updated user if successful. This method has unique authentication requirements. Read the prerequisites in our [Managing Users guide](/display-`video/api/guides/users/overview`#prerequisites) before using this method. The "Try this method" feature does not work for this method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `displayvideo_users_patch_builder()` + `displayvideo_users_patch_execute()`.
/// For task-level control, use `displayvideo_users_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn displayvideo_users_patch(
    client: &SimpleHttpClient,
    args: &DisplayvideoUsersPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<User>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = displayvideo_users_patch_builder(client, &args.userId, &args.updateMask)?;
    displayvideo_users_patch_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for AuditAdvertiserResponse
// =============================================================================

/// ResourceIdentifier implementation for AuditAdvertiserResponse with DisplayvideoAdvertisersAuditArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersAuditArgs> for AuditAdvertiserResponse {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersAuditArgs) -> String {
        format!(
            "gcp::displayvideo::AuditAdvertiserResponse/{}",
            input.advertiserId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::AuditAdvertiserResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with DisplayvideoAdvertisersDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersDeleteArgs) -> String {
        format!("gcp::displayvideo::Empty/{}", input.advertiserId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BulkEditAdvertiserAssignedTargetingOptionsResponse
// =============================================================================

/// ResourceIdentifier implementation for BulkEditAdvertiserAssignedTargetingOptionsResponse with DisplayvideoAdvertisersEditAssignedTargetingOptionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersEditAssignedTargetingOptionsArgs>
    for BulkEditAdvertiserAssignedTargetingOptionsResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersEditAssignedTargetingOptionsArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::BulkEditAdvertiserAssignedTargetingOptionsResponse/{}",
            input.advertiserId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::BulkEditAdvertiserAssignedTargetingOptionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Advertiser
// =============================================================================

/// ResourceIdentifier implementation for Advertiser with DisplayvideoAdvertisersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersGetArgs> for Advertiser {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersGetArgs) -> String {
        format!("gcp::displayvideo::Advertiser/{}", input.advertiserId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Advertiser"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListAdvertisersResponse
// =============================================================================

/// ResourceIdentifier implementation for ListAdvertisersResponse with DisplayvideoAdvertisersListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersListArgs> for ListAdvertisersResponse {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersListArgs) -> String {
        "gcp::displayvideo::ListAdvertisersResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListAdvertisersResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BulkListAdvertiserAssignedTargetingOptionsResponse
// =============================================================================

/// ResourceIdentifier implementation for BulkListAdvertiserAssignedTargetingOptionsResponse with DisplayvideoAdvertisersListAssignedTargetingOptionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersListAssignedTargetingOptionsArgs>
    for BulkListAdvertiserAssignedTargetingOptionsResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersListAssignedTargetingOptionsArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::BulkListAdvertiserAssignedTargetingOptionsResponse/{}",
            input.advertiserId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::BulkListAdvertiserAssignedTargetingOptionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Advertiser
// =============================================================================

/// ResourceIdentifier implementation for Advertiser with DisplayvideoAdvertisersPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersPatchArgs> for Advertiser {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersPatchArgs) -> String {
        format!("gcp::displayvideo::Advertiser/{}", input.advertiserId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Advertiser"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BulkCreateAdAssetsResponse
// =============================================================================

/// ResourceIdentifier implementation for BulkCreateAdAssetsResponse with DisplayvideoAdvertisersAdAssetsBulkCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersAdAssetsBulkCreateArgs>
    for BulkCreateAdAssetsResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersAdAssetsBulkCreateArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::BulkCreateAdAssetsResponse/{}",
            input.advertiserId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::BulkCreateAdAssetsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AdAsset
// =============================================================================

/// ResourceIdentifier implementation for AdAsset with DisplayvideoAdvertisersAdAssetsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersAdAssetsCreateArgs> for AdAsset {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersAdAssetsCreateArgs) -> String {
        format!("gcp::displayvideo::AdAsset/{}", input.advertiserId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::AdAsset"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AdAsset
// =============================================================================

/// ResourceIdentifier implementation for AdAsset with DisplayvideoAdvertisersAdAssetsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersAdAssetsGetArgs> for AdAsset {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersAdAssetsGetArgs) -> String {
        format!(
            "gcp::displayvideo::AdAsset/{}/{}",
            input.advertiserId, input.adAssetId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::AdAsset"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListAdAssetsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListAdAssetsResponse with DisplayvideoAdvertisersAdAssetsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersAdAssetsListArgs> for ListAdAssetsResponse {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersAdAssetsListArgs) -> String {
        format!(
            "gcp::displayvideo::ListAdAssetsResponse/{}",
            input.advertiserId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListAdAssetsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UploadAdAssetResponse
// =============================================================================

/// ResourceIdentifier implementation for UploadAdAssetResponse with DisplayvideoAdvertisersAdAssetsUploadArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersAdAssetsUploadArgs> for UploadAdAssetResponse {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersAdAssetsUploadArgs) -> String {
        format!(
            "gcp::displayvideo::UploadAdAssetResponse/{}",
            input.advertiserId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::UploadAdAssetResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AdGroupAd
// =============================================================================

/// ResourceIdentifier implementation for AdGroupAd with DisplayvideoAdvertisersAdGroupAdsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersAdGroupAdsCreateArgs> for AdGroupAd {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersAdGroupAdsCreateArgs) -> String {
        format!("gcp::displayvideo::AdGroupAd/{}", input.advertiserId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::AdGroupAd"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with DisplayvideoAdvertisersAdGroupAdsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersAdGroupAdsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersAdGroupAdsDeleteArgs) -> String {
        format!(
            "gcp::displayvideo::Empty/{}/{}",
            input.advertiserId, input.adGroupAdId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AdGroupAd
// =============================================================================

/// ResourceIdentifier implementation for AdGroupAd with DisplayvideoAdvertisersAdGroupAdsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersAdGroupAdsGetArgs> for AdGroupAd {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersAdGroupAdsGetArgs) -> String {
        format!(
            "gcp::displayvideo::AdGroupAd/{}/{}",
            input.advertiserId, input.adGroupAdId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::AdGroupAd"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListAdGroupAdsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListAdGroupAdsResponse with DisplayvideoAdvertisersAdGroupAdsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersAdGroupAdsListArgs> for ListAdGroupAdsResponse {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersAdGroupAdsListArgs) -> String {
        format!(
            "gcp::displayvideo::ListAdGroupAdsResponse/{}",
            input.advertiserId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListAdGroupAdsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AdGroupAd
// =============================================================================

/// ResourceIdentifier implementation for AdGroupAd with DisplayvideoAdvertisersAdGroupAdsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersAdGroupAdsPatchArgs> for AdGroupAd {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersAdGroupAdsPatchArgs) -> String {
        format!(
            "gcp::displayvideo::AdGroupAd/{}/{}",
            input.advertiserId, input.adGroupAdId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::AdGroupAd"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BulkEditAdGroupAssignedTargetingOptionsResponse
// =============================================================================

/// ResourceIdentifier implementation for BulkEditAdGroupAssignedTargetingOptionsResponse with DisplayvideoAdvertisersAdGroupsBulkEditAssignedTargetingOptionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersAdGroupsBulkEditAssignedTargetingOptionsArgs>
    for BulkEditAdGroupAssignedTargetingOptionsResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersAdGroupsBulkEditAssignedTargetingOptionsArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::BulkEditAdGroupAssignedTargetingOptionsResponse/{}",
            input.advertiserId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::BulkEditAdGroupAssignedTargetingOptionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BulkListAdGroupAssignedTargetingOptionsResponse
// =============================================================================

/// ResourceIdentifier implementation for BulkListAdGroupAssignedTargetingOptionsResponse with DisplayvideoAdvertisersAdGroupsBulkListAssignedTargetingOptionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersAdGroupsBulkListAssignedTargetingOptionsArgs>
    for BulkListAdGroupAssignedTargetingOptionsResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersAdGroupsBulkListAssignedTargetingOptionsArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::BulkListAdGroupAssignedTargetingOptionsResponse/{}",
            input.advertiserId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::BulkListAdGroupAssignedTargetingOptionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AdGroup
// =============================================================================

/// ResourceIdentifier implementation for AdGroup with DisplayvideoAdvertisersAdGroupsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersAdGroupsCreateArgs> for AdGroup {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersAdGroupsCreateArgs) -> String {
        format!("gcp::displayvideo::AdGroup/{}", input.advertiserId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::AdGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with DisplayvideoAdvertisersAdGroupsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersAdGroupsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersAdGroupsDeleteArgs) -> String {
        format!(
            "gcp::displayvideo::Empty/{}/{}",
            input.advertiserId, input.adGroupId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AdGroup
// =============================================================================

/// ResourceIdentifier implementation for AdGroup with DisplayvideoAdvertisersAdGroupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersAdGroupsGetArgs> for AdGroup {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersAdGroupsGetArgs) -> String {
        format!(
            "gcp::displayvideo::AdGroup/{}/{}",
            input.advertiserId, input.adGroupId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::AdGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListAdGroupsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListAdGroupsResponse with DisplayvideoAdvertisersAdGroupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersAdGroupsListArgs> for ListAdGroupsResponse {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersAdGroupsListArgs) -> String {
        format!(
            "gcp::displayvideo::ListAdGroupsResponse/{}",
            input.advertiserId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListAdGroupsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AdGroup
// =============================================================================

/// ResourceIdentifier implementation for AdGroup with DisplayvideoAdvertisersAdGroupsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersAdGroupsPatchArgs> for AdGroup {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersAdGroupsPatchArgs) -> String {
        format!(
            "gcp::displayvideo::AdGroup/{}/{}",
            input.advertiserId, input.adGroupId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::AdGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AssignedTargetingOption
// =============================================================================

/// ResourceIdentifier implementation for AssignedTargetingOption with DisplayvideoAdvertisersAdGroupsTargetingTypesAssignedTargetingOptionsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        DisplayvideoAdvertisersAdGroupsTargetingTypesAssignedTargetingOptionsCreateArgs,
    > for AssignedTargetingOption
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersAdGroupsTargetingTypesAssignedTargetingOptionsCreateArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::AssignedTargetingOption/{}/{}/{}",
            input.advertiserId, input.adGroupId, input.targetingType
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::AssignedTargetingOption"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with DisplayvideoAdvertisersAdGroupsTargetingTypesAssignedTargetingOptionsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        DisplayvideoAdvertisersAdGroupsTargetingTypesAssignedTargetingOptionsDeleteArgs,
    > for Empty
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersAdGroupsTargetingTypesAssignedTargetingOptionsDeleteArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::Empty/{}/{}/{}/{}",
            input.advertiserId,
            input.adGroupId,
            input.targetingType,
            input.assignedTargetingOptionId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AssignedTargetingOption
// =============================================================================

/// ResourceIdentifier implementation for AssignedTargetingOption with DisplayvideoAdvertisersAdGroupsTargetingTypesAssignedTargetingOptionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<DisplayvideoAdvertisersAdGroupsTargetingTypesAssignedTargetingOptionsGetArgs>
    for AssignedTargetingOption
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersAdGroupsTargetingTypesAssignedTargetingOptionsGetArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::AssignedTargetingOption/{}/{}/{}/{}",
            input.advertiserId,
            input.adGroupId,
            input.targetingType,
            input.assignedTargetingOptionId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::AssignedTargetingOption"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListAdGroupAssignedTargetingOptionsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListAdGroupAssignedTargetingOptionsResponse with DisplayvideoAdvertisersAdGroupsTargetingTypesAssignedTargetingOptionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        DisplayvideoAdvertisersAdGroupsTargetingTypesAssignedTargetingOptionsListArgs,
    > for ListAdGroupAssignedTargetingOptionsResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersAdGroupsTargetingTypesAssignedTargetingOptionsListArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::ListAdGroupAssignedTargetingOptionsResponse/{}/{}/{}",
            input.advertiserId, input.adGroupId, input.targetingType
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListAdGroupAssignedTargetingOptionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for YoutubeAssetAssociation
// =============================================================================

/// ResourceIdentifier implementation for YoutubeAssetAssociation with DisplayvideoAdvertisersAdGroupsYoutubeAssetTypesYoutubeAssetAssociationsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        DisplayvideoAdvertisersAdGroupsYoutubeAssetTypesYoutubeAssetAssociationsCreateArgs,
    > for YoutubeAssetAssociation
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersAdGroupsYoutubeAssetTypesYoutubeAssetAssociationsCreateArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::YoutubeAssetAssociation/{}/{}/{}",
            input.advertiserId, input.adGroupId, input.youtubeAssetType
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::YoutubeAssetAssociation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with DisplayvideoAdvertisersAdGroupsYoutubeAssetTypesYoutubeAssetAssociationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        DisplayvideoAdvertisersAdGroupsYoutubeAssetTypesYoutubeAssetAssociationsDeleteArgs,
    > for Empty
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersAdGroupsYoutubeAssetTypesYoutubeAssetAssociationsDeleteArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::Empty/{}/{}/{}/{}",
            input.advertiserId,
            input.adGroupId,
            input.youtubeAssetType,
            input.youtubeAssetAssociationId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListYoutubeAssetAssociationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListYoutubeAssetAssociationsResponse with DisplayvideoAdvertisersAdGroupsYoutubeAssetTypesYoutubeAssetAssociationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        DisplayvideoAdvertisersAdGroupsYoutubeAssetTypesYoutubeAssetAssociationsListArgs,
    > for ListYoutubeAssetAssociationsResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersAdGroupsYoutubeAssetTypesYoutubeAssetAssociationsListArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::ListYoutubeAssetAssociationsResponse/{}/{}/{}",
            input.advertiserId, input.adGroupId, input.youtubeAssetType
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListYoutubeAssetAssociationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CreateAssetResponse
// =============================================================================

/// ResourceIdentifier implementation for CreateAssetResponse with DisplayvideoAdvertisersAssetsUploadArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersAssetsUploadArgs> for CreateAssetResponse {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersAssetsUploadArgs) -> String {
        format!(
            "gcp::displayvideo::CreateAssetResponse/{}",
            input.advertiserId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::CreateAssetResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Campaign
// =============================================================================

/// ResourceIdentifier implementation for Campaign with DisplayvideoAdvertisersCampaignsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersCampaignsCreateArgs> for Campaign {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersCampaignsCreateArgs) -> String {
        format!("gcp::displayvideo::Campaign/{}", input.advertiserId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Campaign"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with DisplayvideoAdvertisersCampaignsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersCampaignsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersCampaignsDeleteArgs) -> String {
        format!(
            "gcp::displayvideo::Empty/{}/{}",
            input.advertiserId, input.campaignId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Campaign
// =============================================================================

/// ResourceIdentifier implementation for Campaign with DisplayvideoAdvertisersCampaignsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersCampaignsGetArgs> for Campaign {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersCampaignsGetArgs) -> String {
        format!(
            "gcp::displayvideo::Campaign/{}/{}",
            input.advertiserId, input.campaignId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Campaign"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListCampaignsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListCampaignsResponse with DisplayvideoAdvertisersCampaignsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersCampaignsListArgs> for ListCampaignsResponse {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersCampaignsListArgs) -> String {
        format!(
            "gcp::displayvideo::ListCampaignsResponse/{}",
            input.advertiserId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListCampaignsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Campaign
// =============================================================================

/// ResourceIdentifier implementation for Campaign with DisplayvideoAdvertisersCampaignsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersCampaignsPatchArgs> for Campaign {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersCampaignsPatchArgs) -> String {
        format!(
            "gcp::displayvideo::Campaign/{}/{}",
            input.advertiserId, input.campaignId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Campaign"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Channel
// =============================================================================

/// ResourceIdentifier implementation for Channel with DisplayvideoAdvertisersChannelsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersChannelsCreateArgs> for Channel {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersChannelsCreateArgs) -> String {
        format!("gcp::displayvideo::Channel/{}", input.advertiserId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Channel"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Channel
// =============================================================================

/// ResourceIdentifier implementation for Channel with DisplayvideoAdvertisersChannelsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersChannelsGetArgs> for Channel {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersChannelsGetArgs) -> String {
        format!(
            "gcp::displayvideo::Channel/{}/{}",
            input.advertiserId, input.channelId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Channel"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListChannelsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListChannelsResponse with DisplayvideoAdvertisersChannelsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersChannelsListArgs> for ListChannelsResponse {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersChannelsListArgs) -> String {
        format!(
            "gcp::displayvideo::ListChannelsResponse/{}",
            input.advertiserId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListChannelsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Channel
// =============================================================================

/// ResourceIdentifier implementation for Channel with DisplayvideoAdvertisersChannelsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersChannelsPatchArgs> for Channel {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersChannelsPatchArgs) -> String {
        format!(
            "gcp::displayvideo::Channel/{}/{}",
            input.advertiserId, input.channelId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Channel"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BulkEditSitesResponse
// =============================================================================

/// ResourceIdentifier implementation for BulkEditSitesResponse with DisplayvideoAdvertisersChannelsSitesBulkEditArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersChannelsSitesBulkEditArgs>
    for BulkEditSitesResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersChannelsSitesBulkEditArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::BulkEditSitesResponse/{}/{}",
            input.advertiserId, input.channelId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::BulkEditSitesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Site
// =============================================================================

/// ResourceIdentifier implementation for Site with DisplayvideoAdvertisersChannelsSitesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersChannelsSitesCreateArgs> for Site {
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersChannelsSitesCreateArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::Site/{}/{}",
            input.advertiserId, input.channelId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Site"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with DisplayvideoAdvertisersChannelsSitesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersChannelsSitesDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersChannelsSitesDeleteArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::Empty/{}/{}/{}",
            input.advertiserId, input.channelId, input.urlOrAppId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListSitesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListSitesResponse with DisplayvideoAdvertisersChannelsSitesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersChannelsSitesListArgs> for ListSitesResponse {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersChannelsSitesListArgs) -> String {
        format!(
            "gcp::displayvideo::ListSitesResponse/{}/{}",
            input.advertiserId, input.channelId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListSitesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ReplaceSitesResponse
// =============================================================================

/// ResourceIdentifier implementation for ReplaceSitesResponse with DisplayvideoAdvertisersChannelsSitesReplaceArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersChannelsSitesReplaceArgs> for ReplaceSitesResponse {
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersChannelsSitesReplaceArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::ReplaceSitesResponse/{}/{}",
            input.advertiserId, input.channelId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ReplaceSitesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Creative
// =============================================================================

/// ResourceIdentifier implementation for Creative with DisplayvideoAdvertisersCreativesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersCreativesCreateArgs> for Creative {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersCreativesCreateArgs) -> String {
        format!("gcp::displayvideo::Creative/{}", input.advertiserId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Creative"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with DisplayvideoAdvertisersCreativesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersCreativesDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersCreativesDeleteArgs) -> String {
        format!(
            "gcp::displayvideo::Empty/{}/{}",
            input.advertiserId, input.creativeId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Creative
// =============================================================================

/// ResourceIdentifier implementation for Creative with DisplayvideoAdvertisersCreativesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersCreativesGetArgs> for Creative {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersCreativesGetArgs) -> String {
        format!(
            "gcp::displayvideo::Creative/{}/{}",
            input.advertiserId, input.creativeId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Creative"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListCreativesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListCreativesResponse with DisplayvideoAdvertisersCreativesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersCreativesListArgs> for ListCreativesResponse {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersCreativesListArgs) -> String {
        format!(
            "gcp::displayvideo::ListCreativesResponse/{}",
            input.advertiserId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListCreativesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Creative
// =============================================================================

/// ResourceIdentifier implementation for Creative with DisplayvideoAdvertisersCreativesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersCreativesPatchArgs> for Creative {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersCreativesPatchArgs) -> String {
        format!(
            "gcp::displayvideo::Creative/{}/{}",
            input.advertiserId, input.creativeId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Creative"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InsertionOrder
// =============================================================================

/// ResourceIdentifier implementation for InsertionOrder with DisplayvideoAdvertisersInsertionOrdersCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersInsertionOrdersCreateArgs> for InsertionOrder {
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersInsertionOrdersCreateArgs,
    ) -> String {
        format!("gcp::displayvideo::InsertionOrder/{}", input.advertiserId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::InsertionOrder"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with DisplayvideoAdvertisersInsertionOrdersDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersInsertionOrdersDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersInsertionOrdersDeleteArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::Empty/{}/{}",
            input.advertiserId, input.insertionOrderId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InsertionOrder
// =============================================================================

/// ResourceIdentifier implementation for InsertionOrder with DisplayvideoAdvertisersInsertionOrdersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersInsertionOrdersGetArgs> for InsertionOrder {
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersInsertionOrdersGetArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::InsertionOrder/{}/{}",
            input.advertiserId, input.insertionOrderId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::InsertionOrder"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListInsertionOrdersResponse
// =============================================================================

/// ResourceIdentifier implementation for ListInsertionOrdersResponse with DisplayvideoAdvertisersInsertionOrdersListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersInsertionOrdersListArgs>
    for ListInsertionOrdersResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersInsertionOrdersListArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::ListInsertionOrdersResponse/{}",
            input.advertiserId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListInsertionOrdersResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InsertionOrder
// =============================================================================

/// ResourceIdentifier implementation for InsertionOrder with DisplayvideoAdvertisersInsertionOrdersPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersInsertionOrdersPatchArgs> for InsertionOrder {
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersInsertionOrdersPatchArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::InsertionOrder/{}/{}",
            input.advertiserId, input.insertionOrderId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::InsertionOrder"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListInvoicesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListInvoicesResponse with DisplayvideoAdvertisersInvoicesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersInvoicesListArgs> for ListInvoicesResponse {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersInvoicesListArgs) -> String {
        format!(
            "gcp::displayvideo::ListInvoicesResponse/{}",
            input.advertiserId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListInvoicesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LookupInvoiceCurrencyResponse
// =============================================================================

/// ResourceIdentifier implementation for LookupInvoiceCurrencyResponse with DisplayvideoAdvertisersInvoicesLookupInvoiceCurrencyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersInvoicesLookupInvoiceCurrencyArgs>
    for LookupInvoiceCurrencyResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersInvoicesLookupInvoiceCurrencyArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::LookupInvoiceCurrencyResponse/{}",
            input.advertiserId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::LookupInvoiceCurrencyResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BulkEditAssignedTargetingOptionsResponse
// =============================================================================

/// ResourceIdentifier implementation for BulkEditAssignedTargetingOptionsResponse with DisplayvideoAdvertisersLineItemsBulkEditAssignedTargetingOptionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersLineItemsBulkEditAssignedTargetingOptionsArgs>
    for BulkEditAssignedTargetingOptionsResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersLineItemsBulkEditAssignedTargetingOptionsArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::BulkEditAssignedTargetingOptionsResponse/{}",
            input.advertiserId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::BulkEditAssignedTargetingOptionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BulkListAssignedTargetingOptionsResponse
// =============================================================================

/// ResourceIdentifier implementation for BulkListAssignedTargetingOptionsResponse with DisplayvideoAdvertisersLineItemsBulkListAssignedTargetingOptionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersLineItemsBulkListAssignedTargetingOptionsArgs>
    for BulkListAssignedTargetingOptionsResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersLineItemsBulkListAssignedTargetingOptionsArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::BulkListAssignedTargetingOptionsResponse/{}",
            input.advertiserId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::BulkListAssignedTargetingOptionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BulkUpdateLineItemsResponse
// =============================================================================

/// ResourceIdentifier implementation for BulkUpdateLineItemsResponse with DisplayvideoAdvertisersLineItemsBulkUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersLineItemsBulkUpdateArgs>
    for BulkUpdateLineItemsResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersLineItemsBulkUpdateArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::BulkUpdateLineItemsResponse/{}",
            input.advertiserId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::BulkUpdateLineItemsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LineItem
// =============================================================================

/// ResourceIdentifier implementation for LineItem with DisplayvideoAdvertisersLineItemsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersLineItemsCreateArgs> for LineItem {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersLineItemsCreateArgs) -> String {
        format!("gcp::displayvideo::LineItem/{}", input.advertiserId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::LineItem"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with DisplayvideoAdvertisersLineItemsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersLineItemsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersLineItemsDeleteArgs) -> String {
        format!(
            "gcp::displayvideo::Empty/{}/{}",
            input.advertiserId, input.lineItemId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DuplicateLineItemResponse
// =============================================================================

/// ResourceIdentifier implementation for DuplicateLineItemResponse with DisplayvideoAdvertisersLineItemsDuplicateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersLineItemsDuplicateArgs>
    for DuplicateLineItemResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersLineItemsDuplicateArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::DuplicateLineItemResponse/{}/{}",
            input.advertiserId, input.lineItemId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::DuplicateLineItemResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LineItem
// =============================================================================

/// ResourceIdentifier implementation for LineItem with DisplayvideoAdvertisersLineItemsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersLineItemsGetArgs> for LineItem {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersLineItemsGetArgs) -> String {
        format!(
            "gcp::displayvideo::LineItem/{}/{}",
            input.advertiserId, input.lineItemId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::LineItem"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLineItemsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLineItemsResponse with DisplayvideoAdvertisersLineItemsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersLineItemsListArgs> for ListLineItemsResponse {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersLineItemsListArgs) -> String {
        format!(
            "gcp::displayvideo::ListLineItemsResponse/{}",
            input.advertiserId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListLineItemsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LineItem
// =============================================================================

/// ResourceIdentifier implementation for LineItem with DisplayvideoAdvertisersLineItemsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersLineItemsPatchArgs> for LineItem {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersLineItemsPatchArgs) -> String {
        format!(
            "gcp::displayvideo::LineItem/{}/{}",
            input.advertiserId, input.lineItemId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::LineItem"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AssignedTargetingOption
// =============================================================================

/// ResourceIdentifier implementation for AssignedTargetingOption with DisplayvideoAdvertisersLineItemsTargetingTypesAssignedTargetingOptionsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        DisplayvideoAdvertisersLineItemsTargetingTypesAssignedTargetingOptionsCreateArgs,
    > for AssignedTargetingOption
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersLineItemsTargetingTypesAssignedTargetingOptionsCreateArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::AssignedTargetingOption/{}/{}/{}",
            input.advertiserId, input.lineItemId, input.targetingType
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::AssignedTargetingOption"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with DisplayvideoAdvertisersLineItemsTargetingTypesAssignedTargetingOptionsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        DisplayvideoAdvertisersLineItemsTargetingTypesAssignedTargetingOptionsDeleteArgs,
    > for Empty
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersLineItemsTargetingTypesAssignedTargetingOptionsDeleteArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::Empty/{}/{}/{}/{}",
            input.advertiserId,
            input.lineItemId,
            input.targetingType,
            input.assignedTargetingOptionId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AssignedTargetingOption
// =============================================================================

/// ResourceIdentifier implementation for AssignedTargetingOption with DisplayvideoAdvertisersLineItemsTargetingTypesAssignedTargetingOptionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        DisplayvideoAdvertisersLineItemsTargetingTypesAssignedTargetingOptionsGetArgs,
    > for AssignedTargetingOption
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersLineItemsTargetingTypesAssignedTargetingOptionsGetArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::AssignedTargetingOption/{}/{}/{}/{}",
            input.advertiserId,
            input.lineItemId,
            input.targetingType,
            input.assignedTargetingOptionId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::AssignedTargetingOption"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLineItemAssignedTargetingOptionsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLineItemAssignedTargetingOptionsResponse with DisplayvideoAdvertisersLineItemsTargetingTypesAssignedTargetingOptionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        DisplayvideoAdvertisersLineItemsTargetingTypesAssignedTargetingOptionsListArgs,
    > for ListLineItemAssignedTargetingOptionsResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersLineItemsTargetingTypesAssignedTargetingOptionsListArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::ListLineItemAssignedTargetingOptionsResponse/{}/{}/{}",
            input.advertiserId, input.lineItemId, input.targetingType
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListLineItemAssignedTargetingOptionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for YoutubeAssetAssociation
// =============================================================================

/// ResourceIdentifier implementation for YoutubeAssetAssociation with DisplayvideoAdvertisersLineItemsYoutubeAssetTypesYoutubeAssetAssociationsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        DisplayvideoAdvertisersLineItemsYoutubeAssetTypesYoutubeAssetAssociationsCreateArgs,
    > for YoutubeAssetAssociation
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersLineItemsYoutubeAssetTypesYoutubeAssetAssociationsCreateArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::YoutubeAssetAssociation/{}/{}/{}",
            input.advertiserId, input.lineItemId, input.youtubeAssetType
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::YoutubeAssetAssociation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with DisplayvideoAdvertisersLineItemsYoutubeAssetTypesYoutubeAssetAssociationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        DisplayvideoAdvertisersLineItemsYoutubeAssetTypesYoutubeAssetAssociationsDeleteArgs,
    > for Empty
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersLineItemsYoutubeAssetTypesYoutubeAssetAssociationsDeleteArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::Empty/{}/{}/{}/{}",
            input.advertiserId,
            input.lineItemId,
            input.youtubeAssetType,
            input.youtubeAssetAssociationId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListYoutubeAssetAssociationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListYoutubeAssetAssociationsResponse with DisplayvideoAdvertisersLineItemsYoutubeAssetTypesYoutubeAssetAssociationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        DisplayvideoAdvertisersLineItemsYoutubeAssetTypesYoutubeAssetAssociationsListArgs,
    > for ListYoutubeAssetAssociationsResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersLineItemsYoutubeAssetTypesYoutubeAssetAssociationsListArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::ListYoutubeAssetAssociationsResponse/{}/{}/{}",
            input.advertiserId, input.lineItemId, input.youtubeAssetType
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListYoutubeAssetAssociationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LocationList
// =============================================================================

/// ResourceIdentifier implementation for LocationList with DisplayvideoAdvertisersLocationListsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersLocationListsCreateArgs> for LocationList {
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersLocationListsCreateArgs,
    ) -> String {
        format!("gcp::displayvideo::LocationList/{}", input.advertiserId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::LocationList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LocationList
// =============================================================================

/// ResourceIdentifier implementation for LocationList with DisplayvideoAdvertisersLocationListsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersLocationListsGetArgs> for LocationList {
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersLocationListsGetArgs) -> String {
        format!(
            "gcp::displayvideo::LocationList/{}/{}",
            input.advertiserId, input.locationListId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::LocationList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLocationListsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLocationListsResponse with DisplayvideoAdvertisersLocationListsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersLocationListsListArgs>
    for ListLocationListsResponse
{
    fn generate_resource_id(&self, input: &DisplayvideoAdvertisersLocationListsListArgs) -> String {
        format!(
            "gcp::displayvideo::ListLocationListsResponse/{}",
            input.advertiserId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListLocationListsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LocationList
// =============================================================================

/// ResourceIdentifier implementation for LocationList with DisplayvideoAdvertisersLocationListsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersLocationListsPatchArgs> for LocationList {
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersLocationListsPatchArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::LocationList/{}/{}",
            input.advertiserId, input.locationListId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::LocationList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BulkEditAssignedLocationsResponse
// =============================================================================

/// ResourceIdentifier implementation for BulkEditAssignedLocationsResponse with DisplayvideoAdvertisersLocationListsAssignedLocationsBulkEditArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersLocationListsAssignedLocationsBulkEditArgs>
    for BulkEditAssignedLocationsResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersLocationListsAssignedLocationsBulkEditArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::BulkEditAssignedLocationsResponse/{}/{}",
            input.advertiserId, input.locationListId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::BulkEditAssignedLocationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AssignedLocation
// =============================================================================

/// ResourceIdentifier implementation for AssignedLocation with DisplayvideoAdvertisersLocationListsAssignedLocationsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersLocationListsAssignedLocationsCreateArgs>
    for AssignedLocation
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersLocationListsAssignedLocationsCreateArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::AssignedLocation/{}/{}",
            input.advertiserId, input.locationListId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::AssignedLocation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with DisplayvideoAdvertisersLocationListsAssignedLocationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersLocationListsAssignedLocationsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersLocationListsAssignedLocationsDeleteArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::Empty/{}/{}/{}",
            input.advertiserId, input.locationListId, input.assignedLocationId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListAssignedLocationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListAssignedLocationsResponse with DisplayvideoAdvertisersLocationListsAssignedLocationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersLocationListsAssignedLocationsListArgs>
    for ListAssignedLocationsResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersLocationListsAssignedLocationsListArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::ListAssignedLocationsResponse/{}/{}",
            input.advertiserId, input.locationListId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListAssignedLocationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NegativeKeywordList
// =============================================================================

/// ResourceIdentifier implementation for NegativeKeywordList with DisplayvideoAdvertisersNegativeKeywordListsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersNegativeKeywordListsCreateArgs>
    for NegativeKeywordList
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersNegativeKeywordListsCreateArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::NegativeKeywordList/{}",
            input.advertiserId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::NegativeKeywordList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with DisplayvideoAdvertisersNegativeKeywordListsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersNegativeKeywordListsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersNegativeKeywordListsDeleteArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::Empty/{}/{}",
            input.advertiserId, input.negativeKeywordListId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NegativeKeywordList
// =============================================================================

/// ResourceIdentifier implementation for NegativeKeywordList with DisplayvideoAdvertisersNegativeKeywordListsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersNegativeKeywordListsGetArgs>
    for NegativeKeywordList
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersNegativeKeywordListsGetArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::NegativeKeywordList/{}/{}",
            input.advertiserId, input.negativeKeywordListId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::NegativeKeywordList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListNegativeKeywordListsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListNegativeKeywordListsResponse with DisplayvideoAdvertisersNegativeKeywordListsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersNegativeKeywordListsListArgs>
    for ListNegativeKeywordListsResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersNegativeKeywordListsListArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::ListNegativeKeywordListsResponse/{}",
            input.advertiserId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListNegativeKeywordListsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NegativeKeywordList
// =============================================================================

/// ResourceIdentifier implementation for NegativeKeywordList with DisplayvideoAdvertisersNegativeKeywordListsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersNegativeKeywordListsPatchArgs>
    for NegativeKeywordList
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersNegativeKeywordListsPatchArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::NegativeKeywordList/{}/{}",
            input.advertiserId, input.negativeKeywordListId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::NegativeKeywordList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BulkEditNegativeKeywordsResponse
// =============================================================================

/// ResourceIdentifier implementation for BulkEditNegativeKeywordsResponse with DisplayvideoAdvertisersNegativeKeywordListsNegativeKeywordsBulkEditArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersNegativeKeywordListsNegativeKeywordsBulkEditArgs>
    for BulkEditNegativeKeywordsResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersNegativeKeywordListsNegativeKeywordsBulkEditArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::BulkEditNegativeKeywordsResponse/{}/{}",
            input.advertiserId, input.negativeKeywordListId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::BulkEditNegativeKeywordsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NegativeKeyword
// =============================================================================

/// ResourceIdentifier implementation for NegativeKeyword with DisplayvideoAdvertisersNegativeKeywordListsNegativeKeywordsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersNegativeKeywordListsNegativeKeywordsCreateArgs>
    for NegativeKeyword
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersNegativeKeywordListsNegativeKeywordsCreateArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::NegativeKeyword/{}/{}",
            input.advertiserId, input.negativeKeywordListId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::NegativeKeyword"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with DisplayvideoAdvertisersNegativeKeywordListsNegativeKeywordsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersNegativeKeywordListsNegativeKeywordsDeleteArgs>
    for Empty
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersNegativeKeywordListsNegativeKeywordsDeleteArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::Empty/{}/{}/{}",
            input.advertiserId, input.negativeKeywordListId, input.keywordValue
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListNegativeKeywordsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListNegativeKeywordsResponse with DisplayvideoAdvertisersNegativeKeywordListsNegativeKeywordsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersNegativeKeywordListsNegativeKeywordsListArgs>
    for ListNegativeKeywordsResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersNegativeKeywordListsNegativeKeywordsListArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::ListNegativeKeywordsResponse/{}/{}",
            input.advertiserId, input.negativeKeywordListId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListNegativeKeywordsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ReplaceNegativeKeywordsResponse
// =============================================================================

/// ResourceIdentifier implementation for ReplaceNegativeKeywordsResponse with DisplayvideoAdvertisersNegativeKeywordListsNegativeKeywordsReplaceArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersNegativeKeywordListsNegativeKeywordsReplaceArgs>
    for ReplaceNegativeKeywordsResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersNegativeKeywordListsNegativeKeywordsReplaceArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::ReplaceNegativeKeywordsResponse/{}/{}",
            input.advertiserId, input.negativeKeywordListId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ReplaceNegativeKeywordsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AssignedTargetingOption
// =============================================================================

/// ResourceIdentifier implementation for AssignedTargetingOption with DisplayvideoAdvertisersTargetingTypesAssignedTargetingOptionsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersTargetingTypesAssignedTargetingOptionsCreateArgs>
    for AssignedTargetingOption
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersTargetingTypesAssignedTargetingOptionsCreateArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::AssignedTargetingOption/{}/{}",
            input.advertiserId, input.targetingType
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::AssignedTargetingOption"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with DisplayvideoAdvertisersTargetingTypesAssignedTargetingOptionsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersTargetingTypesAssignedTargetingOptionsDeleteArgs>
    for Empty
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersTargetingTypesAssignedTargetingOptionsDeleteArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::Empty/{}/{}/{}",
            input.advertiserId, input.targetingType, input.assignedTargetingOptionId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AssignedTargetingOption
// =============================================================================

/// ResourceIdentifier implementation for AssignedTargetingOption with DisplayvideoAdvertisersTargetingTypesAssignedTargetingOptionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersTargetingTypesAssignedTargetingOptionsGetArgs>
    for AssignedTargetingOption
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersTargetingTypesAssignedTargetingOptionsGetArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::AssignedTargetingOption/{}/{}/{}",
            input.advertiserId, input.targetingType, input.assignedTargetingOptionId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::AssignedTargetingOption"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListAdvertiserAssignedTargetingOptionsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListAdvertiserAssignedTargetingOptionsResponse with DisplayvideoAdvertisersTargetingTypesAssignedTargetingOptionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoAdvertisersTargetingTypesAssignedTargetingOptionsListArgs>
    for ListAdvertiserAssignedTargetingOptionsResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoAdvertisersTargetingTypesAssignedTargetingOptionsListArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::ListAdvertiserAssignedTargetingOptionsResponse/{}/{}",
            input.advertiserId, input.targetingType
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListAdvertiserAssignedTargetingOptionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CombinedAudience
// =============================================================================

/// ResourceIdentifier implementation for CombinedAudience with DisplayvideoCombinedAudiencesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoCombinedAudiencesGetArgs> for CombinedAudience {
    fn generate_resource_id(&self, input: &DisplayvideoCombinedAudiencesGetArgs) -> String {
        format!(
            "gcp::displayvideo::CombinedAudience/{}",
            input.combinedAudienceId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::CombinedAudience"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListCombinedAudiencesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListCombinedAudiencesResponse with DisplayvideoCombinedAudiencesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoCombinedAudiencesListArgs> for ListCombinedAudiencesResponse {
    fn generate_resource_id(&self, input: &DisplayvideoCombinedAudiencesListArgs) -> String {
        "gcp::displayvideo::ListCombinedAudiencesResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListCombinedAudiencesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CustomBiddingAlgorithm
// =============================================================================

/// ResourceIdentifier implementation for CustomBiddingAlgorithm with DisplayvideoCustomBiddingAlgorithmsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoCustomBiddingAlgorithmsGetArgs> for CustomBiddingAlgorithm {
    fn generate_resource_id(&self, input: &DisplayvideoCustomBiddingAlgorithmsGetArgs) -> String {
        format!(
            "gcp::displayvideo::CustomBiddingAlgorithm/{}",
            input.customBiddingAlgorithmId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::CustomBiddingAlgorithm"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListCustomBiddingAlgorithmsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListCustomBiddingAlgorithmsResponse with DisplayvideoCustomBiddingAlgorithmsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoCustomBiddingAlgorithmsListArgs>
    for ListCustomBiddingAlgorithmsResponse
{
    fn generate_resource_id(&self, input: &DisplayvideoCustomBiddingAlgorithmsListArgs) -> String {
        "gcp::displayvideo::ListCustomBiddingAlgorithmsResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListCustomBiddingAlgorithmsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CustomBiddingAlgorithm
// =============================================================================

/// ResourceIdentifier implementation for CustomBiddingAlgorithm with DisplayvideoCustomBiddingAlgorithmsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoCustomBiddingAlgorithmsPatchArgs> for CustomBiddingAlgorithm {
    fn generate_resource_id(&self, input: &DisplayvideoCustomBiddingAlgorithmsPatchArgs) -> String {
        format!(
            "gcp::displayvideo::CustomBiddingAlgorithm/{}",
            input.customBiddingAlgorithmId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::CustomBiddingAlgorithm"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CustomBiddingAlgorithmRulesRef
// =============================================================================

/// ResourceIdentifier implementation for CustomBiddingAlgorithmRulesRef with DisplayvideoCustomBiddingAlgorithmsUploadRulesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoCustomBiddingAlgorithmsUploadRulesArgs>
    for CustomBiddingAlgorithmRulesRef
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoCustomBiddingAlgorithmsUploadRulesArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::CustomBiddingAlgorithmRulesRef/{}",
            input.customBiddingAlgorithmId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::CustomBiddingAlgorithmRulesRef"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CustomBiddingScriptRef
// =============================================================================

/// ResourceIdentifier implementation for CustomBiddingScriptRef with DisplayvideoCustomBiddingAlgorithmsUploadScriptArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoCustomBiddingAlgorithmsUploadScriptArgs>
    for CustomBiddingScriptRef
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoCustomBiddingAlgorithmsUploadScriptArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::CustomBiddingScriptRef/{}",
            input.customBiddingAlgorithmId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::CustomBiddingScriptRef"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CustomBiddingAlgorithmRules
// =============================================================================

/// ResourceIdentifier implementation for CustomBiddingAlgorithmRules with DisplayvideoCustomBiddingAlgorithmsRulesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoCustomBiddingAlgorithmsRulesCreateArgs>
    for CustomBiddingAlgorithmRules
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoCustomBiddingAlgorithmsRulesCreateArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::CustomBiddingAlgorithmRules/{}",
            input.customBiddingAlgorithmId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::CustomBiddingAlgorithmRules"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CustomBiddingAlgorithmRules
// =============================================================================

/// ResourceIdentifier implementation for CustomBiddingAlgorithmRules with DisplayvideoCustomBiddingAlgorithmsRulesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoCustomBiddingAlgorithmsRulesGetArgs>
    for CustomBiddingAlgorithmRules
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoCustomBiddingAlgorithmsRulesGetArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::CustomBiddingAlgorithmRules/{}/{}",
            input.customBiddingAlgorithmId, input.customBiddingAlgorithmRulesId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::CustomBiddingAlgorithmRules"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListCustomBiddingAlgorithmRulesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListCustomBiddingAlgorithmRulesResponse with DisplayvideoCustomBiddingAlgorithmsRulesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoCustomBiddingAlgorithmsRulesListArgs>
    for ListCustomBiddingAlgorithmRulesResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoCustomBiddingAlgorithmsRulesListArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::ListCustomBiddingAlgorithmRulesResponse/{}",
            input.customBiddingAlgorithmId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListCustomBiddingAlgorithmRulesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CustomBiddingScript
// =============================================================================

/// ResourceIdentifier implementation for CustomBiddingScript with DisplayvideoCustomBiddingAlgorithmsScriptsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoCustomBiddingAlgorithmsScriptsCreateArgs>
    for CustomBiddingScript
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoCustomBiddingAlgorithmsScriptsCreateArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::CustomBiddingScript/{}",
            input.customBiddingAlgorithmId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::CustomBiddingScript"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CustomBiddingScript
// =============================================================================

/// ResourceIdentifier implementation for CustomBiddingScript with DisplayvideoCustomBiddingAlgorithmsScriptsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoCustomBiddingAlgorithmsScriptsGetArgs> for CustomBiddingScript {
    fn generate_resource_id(
        &self,
        input: &DisplayvideoCustomBiddingAlgorithmsScriptsGetArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::CustomBiddingScript/{}/{}",
            input.customBiddingAlgorithmId, input.customBiddingScriptId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::CustomBiddingScript"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListCustomBiddingScriptsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListCustomBiddingScriptsResponse with DisplayvideoCustomBiddingAlgorithmsScriptsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoCustomBiddingAlgorithmsScriptsListArgs>
    for ListCustomBiddingScriptsResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoCustomBiddingAlgorithmsScriptsListArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::ListCustomBiddingScriptsResponse/{}",
            input.customBiddingAlgorithmId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListCustomBiddingScriptsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CustomList
// =============================================================================

/// ResourceIdentifier implementation for CustomList with DisplayvideoCustomListsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoCustomListsGetArgs> for CustomList {
    fn generate_resource_id(&self, input: &DisplayvideoCustomListsGetArgs) -> String {
        format!("gcp::displayvideo::CustomList/{}", input.customListId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::CustomList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListCustomListsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListCustomListsResponse with DisplayvideoCustomListsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoCustomListsListArgs> for ListCustomListsResponse {
    fn generate_resource_id(&self, input: &DisplayvideoCustomListsListArgs) -> String {
        "gcp::displayvideo::ListCustomListsResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListCustomListsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FirstPartyAndPartnerAudience
// =============================================================================

/// ResourceIdentifier implementation for FirstPartyAndPartnerAudience with DisplayvideoFirstPartyAndPartnerAudiencesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoFirstPartyAndPartnerAudiencesCreateArgs>
    for FirstPartyAndPartnerAudience
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoFirstPartyAndPartnerAudiencesCreateArgs,
    ) -> String {
        "gcp::displayvideo::FirstPartyAndPartnerAudience".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::FirstPartyAndPartnerAudience"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for EditCustomerMatchMembersResponse
// =============================================================================

/// ResourceIdentifier implementation for EditCustomerMatchMembersResponse with DisplayvideoFirstPartyAndPartnerAudiencesEditCustomerMatchMembersArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoFirstPartyAndPartnerAudiencesEditCustomerMatchMembersArgs>
    for EditCustomerMatchMembersResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoFirstPartyAndPartnerAudiencesEditCustomerMatchMembersArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::EditCustomerMatchMembersResponse/{}",
            input.firstPartyAndPartnerAudienceId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::EditCustomerMatchMembersResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FirstPartyAndPartnerAudience
// =============================================================================

/// ResourceIdentifier implementation for FirstPartyAndPartnerAudience with DisplayvideoFirstPartyAndPartnerAudiencesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoFirstPartyAndPartnerAudiencesGetArgs>
    for FirstPartyAndPartnerAudience
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoFirstPartyAndPartnerAudiencesGetArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::FirstPartyAndPartnerAudience/{}",
            input.firstPartyAndPartnerAudienceId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::FirstPartyAndPartnerAudience"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListFirstPartyAndPartnerAudiencesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListFirstPartyAndPartnerAudiencesResponse with DisplayvideoFirstPartyAndPartnerAudiencesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoFirstPartyAndPartnerAudiencesListArgs>
    for ListFirstPartyAndPartnerAudiencesResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoFirstPartyAndPartnerAudiencesListArgs,
    ) -> String {
        "gcp::displayvideo::ListFirstPartyAndPartnerAudiencesResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListFirstPartyAndPartnerAudiencesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FirstPartyAndPartnerAudience
// =============================================================================

/// ResourceIdentifier implementation for FirstPartyAndPartnerAudience with DisplayvideoFirstPartyAndPartnerAudiencesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoFirstPartyAndPartnerAudiencesPatchArgs>
    for FirstPartyAndPartnerAudience
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoFirstPartyAndPartnerAudiencesPatchArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::FirstPartyAndPartnerAudience/{}",
            input.firstPartyAndPartnerAudienceId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::FirstPartyAndPartnerAudience"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FloodlightGroup
// =============================================================================

/// ResourceIdentifier implementation for FloodlightGroup with DisplayvideoFloodlightGroupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoFloodlightGroupsGetArgs> for FloodlightGroup {
    fn generate_resource_id(&self, input: &DisplayvideoFloodlightGroupsGetArgs) -> String {
        format!(
            "gcp::displayvideo::FloodlightGroup/{}",
            input.floodlightGroupId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::FloodlightGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FloodlightGroup
// =============================================================================

/// ResourceIdentifier implementation for FloodlightGroup with DisplayvideoFloodlightGroupsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoFloodlightGroupsPatchArgs> for FloodlightGroup {
    fn generate_resource_id(&self, input: &DisplayvideoFloodlightGroupsPatchArgs) -> String {
        format!(
            "gcp::displayvideo::FloodlightGroup/{}",
            input.floodlightGroupId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::FloodlightGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FloodlightActivity
// =============================================================================

/// ResourceIdentifier implementation for FloodlightActivity with DisplayvideoFloodlightGroupsFloodlightActivitiesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoFloodlightGroupsFloodlightActivitiesGetArgs>
    for FloodlightActivity
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoFloodlightGroupsFloodlightActivitiesGetArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::FloodlightActivity/{}/{}",
            input.floodlightGroupId, input.floodlightActivityId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::FloodlightActivity"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListFloodlightActivitiesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListFloodlightActivitiesResponse with DisplayvideoFloodlightGroupsFloodlightActivitiesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoFloodlightGroupsFloodlightActivitiesListArgs>
    for ListFloodlightActivitiesResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoFloodlightGroupsFloodlightActivitiesListArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::ListFloodlightActivitiesResponse/{}",
            input.floodlightGroupId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListFloodlightActivitiesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleAudience
// =============================================================================

/// ResourceIdentifier implementation for GoogleAudience with DisplayvideoGoogleAudiencesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoGoogleAudiencesGetArgs> for GoogleAudience {
    fn generate_resource_id(&self, input: &DisplayvideoGoogleAudiencesGetArgs) -> String {
        format!(
            "gcp::displayvideo::GoogleAudience/{}",
            input.googleAudienceId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::GoogleAudience"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListGoogleAudiencesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListGoogleAudiencesResponse with DisplayvideoGoogleAudiencesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoGoogleAudiencesListArgs> for ListGoogleAudiencesResponse {
    fn generate_resource_id(&self, input: &DisplayvideoGoogleAudiencesListArgs) -> String {
        "gcp::displayvideo::ListGoogleAudiencesResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListGoogleAudiencesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GuaranteedOrder
// =============================================================================

/// ResourceIdentifier implementation for GuaranteedOrder with DisplayvideoGuaranteedOrdersCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoGuaranteedOrdersCreateArgs> for GuaranteedOrder {
    fn generate_resource_id(&self, input: &DisplayvideoGuaranteedOrdersCreateArgs) -> String {
        "gcp::displayvideo::GuaranteedOrder".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::GuaranteedOrder"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for EditGuaranteedOrderReadAccessorsResponse
// =============================================================================

/// ResourceIdentifier implementation for EditGuaranteedOrderReadAccessorsResponse with DisplayvideoGuaranteedOrdersEditGuaranteedOrderReadAccessorsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoGuaranteedOrdersEditGuaranteedOrderReadAccessorsArgs>
    for EditGuaranteedOrderReadAccessorsResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoGuaranteedOrdersEditGuaranteedOrderReadAccessorsArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::EditGuaranteedOrderReadAccessorsResponse/{}",
            input.guaranteedOrderId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::EditGuaranteedOrderReadAccessorsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GuaranteedOrder
// =============================================================================

/// ResourceIdentifier implementation for GuaranteedOrder with DisplayvideoGuaranteedOrdersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoGuaranteedOrdersGetArgs> for GuaranteedOrder {
    fn generate_resource_id(&self, input: &DisplayvideoGuaranteedOrdersGetArgs) -> String {
        format!(
            "gcp::displayvideo::GuaranteedOrder/{}",
            input.guaranteedOrderId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::GuaranteedOrder"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListGuaranteedOrdersResponse
// =============================================================================

/// ResourceIdentifier implementation for ListGuaranteedOrdersResponse with DisplayvideoGuaranteedOrdersListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoGuaranteedOrdersListArgs> for ListGuaranteedOrdersResponse {
    fn generate_resource_id(&self, input: &DisplayvideoGuaranteedOrdersListArgs) -> String {
        "gcp::displayvideo::ListGuaranteedOrdersResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListGuaranteedOrdersResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GuaranteedOrder
// =============================================================================

/// ResourceIdentifier implementation for GuaranteedOrder with DisplayvideoGuaranteedOrdersPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoGuaranteedOrdersPatchArgs> for GuaranteedOrder {
    fn generate_resource_id(&self, input: &DisplayvideoGuaranteedOrdersPatchArgs) -> String {
        format!(
            "gcp::displayvideo::GuaranteedOrder/{}",
            input.guaranteedOrderId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::GuaranteedOrder"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InventorySourceGroup
// =============================================================================

/// ResourceIdentifier implementation for InventorySourceGroup with DisplayvideoInventorySourceGroupsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoInventorySourceGroupsCreateArgs> for InventorySourceGroup {
    fn generate_resource_id(&self, input: &DisplayvideoInventorySourceGroupsCreateArgs) -> String {
        "gcp::displayvideo::InventorySourceGroup".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::InventorySourceGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with DisplayvideoInventorySourceGroupsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoInventorySourceGroupsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &DisplayvideoInventorySourceGroupsDeleteArgs) -> String {
        format!("gcp::displayvideo::Empty/{}", input.inventorySourceGroupId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InventorySourceGroup
// =============================================================================

/// ResourceIdentifier implementation for InventorySourceGroup with DisplayvideoInventorySourceGroupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoInventorySourceGroupsGetArgs> for InventorySourceGroup {
    fn generate_resource_id(&self, input: &DisplayvideoInventorySourceGroupsGetArgs) -> String {
        format!(
            "gcp::displayvideo::InventorySourceGroup/{}",
            input.inventorySourceGroupId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::InventorySourceGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListInventorySourceGroupsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListInventorySourceGroupsResponse with DisplayvideoInventorySourceGroupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoInventorySourceGroupsListArgs>
    for ListInventorySourceGroupsResponse
{
    fn generate_resource_id(&self, input: &DisplayvideoInventorySourceGroupsListArgs) -> String {
        "gcp::displayvideo::ListInventorySourceGroupsResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListInventorySourceGroupsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InventorySourceGroup
// =============================================================================

/// ResourceIdentifier implementation for InventorySourceGroup with DisplayvideoInventorySourceGroupsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoInventorySourceGroupsPatchArgs> for InventorySourceGroup {
    fn generate_resource_id(&self, input: &DisplayvideoInventorySourceGroupsPatchArgs) -> String {
        format!(
            "gcp::displayvideo::InventorySourceGroup/{}",
            input.inventorySourceGroupId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::InventorySourceGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BulkEditAssignedInventorySourcesResponse
// =============================================================================

/// ResourceIdentifier implementation for BulkEditAssignedInventorySourcesResponse with DisplayvideoInventorySourceGroupsAssignedInventorySourcesBulkEditArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoInventorySourceGroupsAssignedInventorySourcesBulkEditArgs>
    for BulkEditAssignedInventorySourcesResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoInventorySourceGroupsAssignedInventorySourcesBulkEditArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::BulkEditAssignedInventorySourcesResponse/{}",
            input.inventorySourceGroupId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::BulkEditAssignedInventorySourcesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AssignedInventorySource
// =============================================================================

/// ResourceIdentifier implementation for AssignedInventorySource with DisplayvideoInventorySourceGroupsAssignedInventorySourcesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoInventorySourceGroupsAssignedInventorySourcesCreateArgs>
    for AssignedInventorySource
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoInventorySourceGroupsAssignedInventorySourcesCreateArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::AssignedInventorySource/{}",
            input.inventorySourceGroupId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::AssignedInventorySource"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with DisplayvideoInventorySourceGroupsAssignedInventorySourcesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoInventorySourceGroupsAssignedInventorySourcesDeleteArgs>
    for Empty
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoInventorySourceGroupsAssignedInventorySourcesDeleteArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::Empty/{}/{}",
            input.inventorySourceGroupId, input.assignedInventorySourceId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListAssignedInventorySourcesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListAssignedInventorySourcesResponse with DisplayvideoInventorySourceGroupsAssignedInventorySourcesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoInventorySourceGroupsAssignedInventorySourcesListArgs>
    for ListAssignedInventorySourcesResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoInventorySourceGroupsAssignedInventorySourcesListArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::ListAssignedInventorySourcesResponse/{}",
            input.inventorySourceGroupId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListAssignedInventorySourcesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InventorySource
// =============================================================================

/// ResourceIdentifier implementation for InventorySource with DisplayvideoInventorySourcesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoInventorySourcesCreateArgs> for InventorySource {
    fn generate_resource_id(&self, input: &DisplayvideoInventorySourcesCreateArgs) -> String {
        "gcp::displayvideo::InventorySource".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::InventorySource"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InventorySourceAccessors
// =============================================================================

/// ResourceIdentifier implementation for InventorySourceAccessors with DisplayvideoInventorySourcesEditInventorySourceReadWriteAccessorsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoInventorySourcesEditInventorySourceReadWriteAccessorsArgs>
    for InventorySourceAccessors
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoInventorySourcesEditInventorySourceReadWriteAccessorsArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::InventorySourceAccessors/{}",
            input.inventorySourceId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::InventorySourceAccessors"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InventorySource
// =============================================================================

/// ResourceIdentifier implementation for InventorySource with DisplayvideoInventorySourcesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoInventorySourcesGetArgs> for InventorySource {
    fn generate_resource_id(&self, input: &DisplayvideoInventorySourcesGetArgs) -> String {
        format!(
            "gcp::displayvideo::InventorySource/{}",
            input.inventorySourceId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::InventorySource"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListInventorySourcesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListInventorySourcesResponse with DisplayvideoInventorySourcesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoInventorySourcesListArgs> for ListInventorySourcesResponse {
    fn generate_resource_id(&self, input: &DisplayvideoInventorySourcesListArgs) -> String {
        "gcp::displayvideo::ListInventorySourcesResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListInventorySourcesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InventorySource
// =============================================================================

/// ResourceIdentifier implementation for InventorySource with DisplayvideoInventorySourcesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoInventorySourcesPatchArgs> for InventorySource {
    fn generate_resource_id(&self, input: &DisplayvideoInventorySourcesPatchArgs) -> String {
        format!(
            "gcp::displayvideo::InventorySource/{}",
            input.inventorySourceId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::InventorySource"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleBytestreamMedia
// =============================================================================

/// ResourceIdentifier implementation for GoogleBytestreamMedia with DisplayvideoMediaDownloadArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoMediaDownloadArgs> for GoogleBytestreamMedia {
    fn generate_resource_id(&self, input: &DisplayvideoMediaDownloadArgs) -> String {
        format!(
            "gcp::displayvideo::GoogleBytestreamMedia/{}",
            input.resourceName
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::GoogleBytestreamMedia"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleBytestreamMedia
// =============================================================================

/// ResourceIdentifier implementation for GoogleBytestreamMedia with DisplayvideoMediaUploadArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoMediaUploadArgs> for GoogleBytestreamMedia {
    fn generate_resource_id(&self, input: &DisplayvideoMediaUploadArgs) -> String {
        format!(
            "gcp::displayvideo::GoogleBytestreamMedia/{}",
            input.resourceName
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::GoogleBytestreamMedia"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BulkEditPartnerAssignedTargetingOptionsResponse
// =============================================================================

/// ResourceIdentifier implementation for BulkEditPartnerAssignedTargetingOptionsResponse with DisplayvideoPartnersEditAssignedTargetingOptionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoPartnersEditAssignedTargetingOptionsArgs>
    for BulkEditPartnerAssignedTargetingOptionsResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoPartnersEditAssignedTargetingOptionsArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::BulkEditPartnerAssignedTargetingOptionsResponse/{}",
            input.partnerId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::BulkEditPartnerAssignedTargetingOptionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Partner
// =============================================================================

/// ResourceIdentifier implementation for Partner with DisplayvideoPartnersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoPartnersGetArgs> for Partner {
    fn generate_resource_id(&self, input: &DisplayvideoPartnersGetArgs) -> String {
        format!("gcp::displayvideo::Partner/{}", input.partnerId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Partner"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListPartnersResponse
// =============================================================================

/// ResourceIdentifier implementation for ListPartnersResponse with DisplayvideoPartnersListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoPartnersListArgs> for ListPartnersResponse {
    fn generate_resource_id(&self, input: &DisplayvideoPartnersListArgs) -> String {
        "gcp::displayvideo::ListPartnersResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListPartnersResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Channel
// =============================================================================

/// ResourceIdentifier implementation for Channel with DisplayvideoPartnersChannelsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoPartnersChannelsCreateArgs> for Channel {
    fn generate_resource_id(&self, input: &DisplayvideoPartnersChannelsCreateArgs) -> String {
        format!("gcp::displayvideo::Channel/{}", input.partnerId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Channel"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Channel
// =============================================================================

/// ResourceIdentifier implementation for Channel with DisplayvideoPartnersChannelsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoPartnersChannelsGetArgs> for Channel {
    fn generate_resource_id(&self, input: &DisplayvideoPartnersChannelsGetArgs) -> String {
        format!(
            "gcp::displayvideo::Channel/{}/{}",
            input.partnerId, input.channelId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Channel"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListChannelsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListChannelsResponse with DisplayvideoPartnersChannelsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoPartnersChannelsListArgs> for ListChannelsResponse {
    fn generate_resource_id(&self, input: &DisplayvideoPartnersChannelsListArgs) -> String {
        format!(
            "gcp::displayvideo::ListChannelsResponse/{}",
            input.partnerId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListChannelsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Channel
// =============================================================================

/// ResourceIdentifier implementation for Channel with DisplayvideoPartnersChannelsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoPartnersChannelsPatchArgs> for Channel {
    fn generate_resource_id(&self, input: &DisplayvideoPartnersChannelsPatchArgs) -> String {
        format!(
            "gcp::displayvideo::Channel/{}/{}",
            input.partnerId, input.channelId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Channel"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BulkEditSitesResponse
// =============================================================================

/// ResourceIdentifier implementation for BulkEditSitesResponse with DisplayvideoPartnersChannelsSitesBulkEditArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoPartnersChannelsSitesBulkEditArgs> for BulkEditSitesResponse {
    fn generate_resource_id(
        &self,
        input: &DisplayvideoPartnersChannelsSitesBulkEditArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::BulkEditSitesResponse/{}/{}",
            input.partnerId, input.channelId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::BulkEditSitesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Site
// =============================================================================

/// ResourceIdentifier implementation for Site with DisplayvideoPartnersChannelsSitesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoPartnersChannelsSitesCreateArgs> for Site {
    fn generate_resource_id(&self, input: &DisplayvideoPartnersChannelsSitesCreateArgs) -> String {
        format!(
            "gcp::displayvideo::Site/{}/{}",
            input.partnerId, input.channelId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Site"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with DisplayvideoPartnersChannelsSitesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoPartnersChannelsSitesDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &DisplayvideoPartnersChannelsSitesDeleteArgs) -> String {
        format!(
            "gcp::displayvideo::Empty/{}/{}/{}",
            input.partnerId, input.channelId, input.urlOrAppId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListSitesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListSitesResponse with DisplayvideoPartnersChannelsSitesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoPartnersChannelsSitesListArgs> for ListSitesResponse {
    fn generate_resource_id(&self, input: &DisplayvideoPartnersChannelsSitesListArgs) -> String {
        format!(
            "gcp::displayvideo::ListSitesResponse/{}/{}",
            input.partnerId, input.channelId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListSitesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ReplaceSitesResponse
// =============================================================================

/// ResourceIdentifier implementation for ReplaceSitesResponse with DisplayvideoPartnersChannelsSitesReplaceArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoPartnersChannelsSitesReplaceArgs> for ReplaceSitesResponse {
    fn generate_resource_id(&self, input: &DisplayvideoPartnersChannelsSitesReplaceArgs) -> String {
        format!(
            "gcp::displayvideo::ReplaceSitesResponse/{}/{}",
            input.partnerId, input.channelId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ReplaceSitesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AssignedTargetingOption
// =============================================================================

/// ResourceIdentifier implementation for AssignedTargetingOption with DisplayvideoPartnersTargetingTypesAssignedTargetingOptionsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoPartnersTargetingTypesAssignedTargetingOptionsCreateArgs>
    for AssignedTargetingOption
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoPartnersTargetingTypesAssignedTargetingOptionsCreateArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::AssignedTargetingOption/{}/{}",
            input.partnerId, input.targetingType
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::AssignedTargetingOption"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with DisplayvideoPartnersTargetingTypesAssignedTargetingOptionsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoPartnersTargetingTypesAssignedTargetingOptionsDeleteArgs>
    for Empty
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoPartnersTargetingTypesAssignedTargetingOptionsDeleteArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::Empty/{}/{}/{}",
            input.partnerId, input.targetingType, input.assignedTargetingOptionId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AssignedTargetingOption
// =============================================================================

/// ResourceIdentifier implementation for AssignedTargetingOption with DisplayvideoPartnersTargetingTypesAssignedTargetingOptionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoPartnersTargetingTypesAssignedTargetingOptionsGetArgs>
    for AssignedTargetingOption
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoPartnersTargetingTypesAssignedTargetingOptionsGetArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::AssignedTargetingOption/{}/{}/{}",
            input.partnerId, input.targetingType, input.assignedTargetingOptionId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::AssignedTargetingOption"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListPartnerAssignedTargetingOptionsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListPartnerAssignedTargetingOptionsResponse with DisplayvideoPartnersTargetingTypesAssignedTargetingOptionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoPartnersTargetingTypesAssignedTargetingOptionsListArgs>
    for ListPartnerAssignedTargetingOptionsResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoPartnersTargetingTypesAssignedTargetingOptionsListArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::ListPartnerAssignedTargetingOptionsResponse/{}/{}",
            input.partnerId, input.targetingType
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListPartnerAssignedTargetingOptionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with DisplayvideoSdfdownloadtasksOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoSdfdownloadtasksOperationsGetArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &DisplayvideoSdfdownloadtasksOperationsGetArgs,
    ) -> String {
        format!("gcp::displayvideo::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with DisplayvideoSdfuploadtasksOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoSdfuploadtasksOperationsGetArgs> for Operation {
    fn generate_resource_id(&self, input: &DisplayvideoSdfuploadtasksOperationsGetArgs) -> String {
        format!("gcp::displayvideo::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TargetingOption
// =============================================================================

/// ResourceIdentifier implementation for TargetingOption with DisplayvideoTargetingTypesTargetingOptionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoTargetingTypesTargetingOptionsGetArgs> for TargetingOption {
    fn generate_resource_id(
        &self,
        input: &DisplayvideoTargetingTypesTargetingOptionsGetArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::TargetingOption/{}/{}",
            input.targetingType, input.targetingOptionId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::TargetingOption"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListTargetingOptionsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListTargetingOptionsResponse with DisplayvideoTargetingTypesTargetingOptionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoTargetingTypesTargetingOptionsListArgs>
    for ListTargetingOptionsResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoTargetingTypesTargetingOptionsListArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::ListTargetingOptionsResponse/{}",
            input.targetingType
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListTargetingOptionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SearchTargetingOptionsResponse
// =============================================================================

/// ResourceIdentifier implementation for SearchTargetingOptionsResponse with DisplayvideoTargetingTypesTargetingOptionsSearchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoTargetingTypesTargetingOptionsSearchArgs>
    for SearchTargetingOptionsResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoTargetingTypesTargetingOptionsSearchArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::SearchTargetingOptionsResponse/{}",
            input.targetingType
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::SearchTargetingOptionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BulkEditAssignedUserRolesResponse
// =============================================================================

/// ResourceIdentifier implementation for BulkEditAssignedUserRolesResponse with DisplayvideoUsersBulkEditAssignedUserRolesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoUsersBulkEditAssignedUserRolesArgs>
    for BulkEditAssignedUserRolesResponse
{
    fn generate_resource_id(
        &self,
        input: &DisplayvideoUsersBulkEditAssignedUserRolesArgs,
    ) -> String {
        format!(
            "gcp::displayvideo::BulkEditAssignedUserRolesResponse/{}",
            input.userId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::BulkEditAssignedUserRolesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with DisplayvideoUsersDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoUsersDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &DisplayvideoUsersDeleteArgs) -> String {
        format!("gcp::displayvideo::Empty/{}", input.userId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for User
// =============================================================================

/// ResourceIdentifier implementation for User with DisplayvideoUsersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoUsersGetArgs> for User {
    fn generate_resource_id(&self, input: &DisplayvideoUsersGetArgs) -> String {
        format!("gcp::displayvideo::User/{}", input.userId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::User"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListUsersResponse
// =============================================================================

/// ResourceIdentifier implementation for ListUsersResponse with DisplayvideoUsersListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoUsersListArgs> for ListUsersResponse {
    fn generate_resource_id(&self, input: &DisplayvideoUsersListArgs) -> String {
        "gcp::displayvideo::ListUsersResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::ListUsersResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for User
// =============================================================================

/// ResourceIdentifier implementation for User with DisplayvideoUsersPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DisplayvideoUsersPatchArgs> for User {
    fn generate_resource_id(&self, input: &DisplayvideoUsersPatchArgs) -> String {
        format!("gcp::displayvideo::User/{}", input.userId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::displayvideo::User"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
