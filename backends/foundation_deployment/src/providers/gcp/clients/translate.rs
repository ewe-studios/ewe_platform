//! Auto-generated API clients for gcp/translate.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v3/projects/{projectsId}:detectLanguage
/// Detects the language of text within a request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_detect_language_execute()` to send, or `translate_projects_detect_language` for simplest API.

pub fn translate_projects_detect_language_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &DetectLanguageRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://translation.googleapis.com/v3/projects/{}:detectLanguage",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}:detectLanguage
/// Detects the language of text within a request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `translate_projects_detect_language_execute()` or `translate_projects_detect_language`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `translate_projects_detect_language_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_detect_language_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DetectLanguageResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DetectLanguageResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}:detectLanguage
/// Detects the language of text within a request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `translate_projects_detect_language_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `translate_projects_detect_language_task()`.
/// For the simplest API, use `translate_projects_detect_language()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `translate_projects_detect_language_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn translate_projects_detect_language_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DetectLanguageResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = translate_projects_detect_language_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`translate_projects_detect_language`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TranslateProjectsDetectLanguageArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: DetectLanguageRequest,
}

/// GET v3/projects/{projectsId}:detectLanguage
/// Detects the language of text within a request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_detect_language_builder()` + `translate_projects_detect_language_execute()`.
/// For task-level control, use `translate_projects_detect_language_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_detect_language(
    client: &SimpleHttpClient,
    args: &TranslateProjectsDetectLanguageArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DetectLanguageResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = translate_projects_detect_language_builder(client, &args.parent, &args.body)?;
    translate_projects_detect_language_execute(builder)
}

/// GET v3/projects/{projectsId}/supportedLanguages
/// Returns a list of supported languages for translation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_get_supported_languages_execute()` to send, or `translate_projects_get_supported_languages` for simplest API.

pub fn translate_projects_get_supported_languages_builder(
    client: &SimpleHttpClient,
    parent: &String,
    displayLanguageCode: &Option<String>,
    model: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://translation.googleapis.com/v3/projects/{}/supportedLanguages",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = displayLanguageCode.as_ref() {
        query_parts.push(format!("displayLanguageCode={}", val));
    }
    if let Some(val) = model.as_ref() {
        query_parts.push(format!("model={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/supportedLanguages
/// Returns a list of supported languages for translation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `translate_projects_get_supported_languages_execute()` or `translate_projects_get_supported_languages`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `translate_projects_get_supported_languages_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_get_supported_languages_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SupportedLanguages>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SupportedLanguages = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/supportedLanguages
/// Returns a list of supported languages for translation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `translate_projects_get_supported_languages_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `translate_projects_get_supported_languages_task()`.
/// For the simplest API, use `translate_projects_get_supported_languages()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `translate_projects_get_supported_languages_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn translate_projects_get_supported_languages_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SupportedLanguages>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = translate_projects_get_supported_languages_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`translate_projects_get_supported_languages`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TranslateProjectsGetSupportedLanguagesArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: displayLanguageCode
    pub displayLanguageCode: Option<String>,
    /// Query parameter: model
    pub model: Option<String>,
}

/// GET v3/projects/{projectsId}/supportedLanguages
/// Returns a list of supported languages for translation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_get_supported_languages_builder()` + `translate_projects_get_supported_languages_execute()`.
/// For task-level control, use `translate_projects_get_supported_languages_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_get_supported_languages(
    client: &SimpleHttpClient,
    args: &TranslateProjectsGetSupportedLanguagesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SupportedLanguages>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = translate_projects_get_supported_languages_builder(
        client,
        &args.parent,
        &args.displayLanguageCode,
        &args.model,
    )?;
    translate_projects_get_supported_languages_execute(builder)
}

/// GET v3/projects/{projectsId}:romanizeText
/// Romanize input text written in non-Latin scripts to Latin text.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_romanize_text_execute()` to send, or `translate_projects_romanize_text` for simplest API.

pub fn translate_projects_romanize_text_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &RomanizeTextRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://translation.googleapis.com/v3/projects/{}:romanizeText",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}:romanizeText
/// Romanize input text written in non-Latin scripts to Latin text.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `translate_projects_romanize_text_execute()` or `translate_projects_romanize_text`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `translate_projects_romanize_text_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_romanize_text_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RomanizeTextResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RomanizeTextResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}:romanizeText
/// Romanize input text written in non-Latin scripts to Latin text.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `translate_projects_romanize_text_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `translate_projects_romanize_text_task()`.
/// For the simplest API, use `translate_projects_romanize_text()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `translate_projects_romanize_text_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn translate_projects_romanize_text_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RomanizeTextResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = translate_projects_romanize_text_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`translate_projects_romanize_text`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TranslateProjectsRomanizeTextArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: RomanizeTextRequest,
}

/// GET v3/projects/{projectsId}:romanizeText
/// Romanize input text written in non-Latin scripts to Latin text.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_romanize_text_builder()` + `translate_projects_romanize_text_execute()`.
/// For task-level control, use `translate_projects_romanize_text_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_romanize_text(
    client: &SimpleHttpClient,
    args: &TranslateProjectsRomanizeTextArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RomanizeTextResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = translate_projects_romanize_text_builder(client, &args.parent, &args.body)?;
    translate_projects_romanize_text_execute(builder)
}

/// GET v3/projects/{projectsId}:translateText
/// Translates input text and returns translated text.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_translate_text_execute()` to send, or `translate_projects_translate_text` for simplest API.

pub fn translate_projects_translate_text_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &TranslateTextRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://translation.googleapis.com/v3/projects/{}:translateText",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}:translateText
/// Translates input text and returns translated text.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `translate_projects_translate_text_execute()` or `translate_projects_translate_text`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `translate_projects_translate_text_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_translate_text_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TranslateTextResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TranslateTextResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}:translateText
/// Translates input text and returns translated text.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `translate_projects_translate_text_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `translate_projects_translate_text_task()`.
/// For the simplest API, use `translate_projects_translate_text()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `translate_projects_translate_text_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn translate_projects_translate_text_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TranslateTextResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = translate_projects_translate_text_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`translate_projects_translate_text`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TranslateProjectsTranslateTextArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: TranslateTextRequest,
}

/// GET v3/projects/{projectsId}:translateText
/// Translates input text and returns translated text.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_translate_text_builder()` + `translate_projects_translate_text_execute()`.
/// For task-level control, use `translate_projects_translate_text_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_translate_text(
    client: &SimpleHttpClient,
    args: &TranslateProjectsTranslateTextArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TranslateTextResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = translate_projects_translate_text_builder(client, &args.parent, &args.body)?;
    translate_projects_translate_text_execute(builder)
}

/// GET v3/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method can be called in two ways: * **List all public locations:** Use the path GET /v1/locations. * **List project-visible locations:** Use the path GET /v1/`projects/{project_id}/locations`. This may include public locations as well as private or other locations specifically visible to the project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_list_execute()` to send, or `translate_projects_locations_list` for simplest API.

pub fn translate_projects_locations_list_builder(
    client: &SimpleHttpClient,
    name: &String,
    extraLocationTypes: &Option<String>,
    filter: &Option<String>,
    pageSize: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://translation.googleapis.com/v3/projects/{}/locations",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes.as_ref() {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method can be called in two ways: * **List all public locations:** Use the path GET /v1/locations. * **List project-visible locations:** Use the path GET /v1/`projects/{project_id}/locations`. This may include public locations as well as private or other locations specifically visible to the project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `translate_projects_locations_list_execute()` or `translate_projects_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `translate_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLocationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method can be called in two ways: * **List all public locations:** Use the path GET /v1/locations. * **List project-visible locations:** Use the path GET /v1/`projects/{project_id}/locations`. This may include public locations as well as private or other locations specifically visible to the project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `translate_projects_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `translate_projects_locations_list_task()`.
/// For the simplest API, use `translate_projects_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `translate_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn translate_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = translate_projects_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`translate_projects_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TranslateProjectsLocationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: extraLocationTypes
    pub extraLocationTypes: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v3/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method can be called in two ways: * **List all public locations:** Use the path GET /v1/locations. * **List project-visible locations:** Use the path GET /v1/`projects/{project_id}/locations`. This may include public locations as well as private or other locations specifically visible to the project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_list_builder()` + `translate_projects_locations_list_execute()`.
/// For task-level control, use `translate_projects_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_list(
    client: &SimpleHttpClient,
    args: &TranslateProjectsLocationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_list_builder(
        client,
        &args.name,
        &args.extraLocationTypes,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    translate_projects_locations_list_execute(builder)
}
