//! Auto-generated API clients for gcp/translate.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/translate`

#![cfg(feature = "gcp/translate")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v3/projects/{projectsId}:detectLanguage
/// Detects the language of text within a request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_detect_language_execute()` to send, or `translate_projects_detect_language` for simplest API.

pub fn translate_projects_detect_language_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &DetectLanguageRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}:detectLanguage",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}:detectLanguage
/// Detects the language of text within a request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_detect_language_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_detect_language_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_detect_language_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DetectLanguageResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DetectLanguageResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}:detectLanguage
/// Detects the language of text within a request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_detect_language_builder()` + `translate_projects_detect_language_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_detect_language(
    client: &SimpleHttpClient,
    parent: &str,
    body: &DetectLanguageRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DetectLanguageResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = translate_projects_detect_language_builder(client, parent, body)?;
    translate_projects_detect_language_execute(builder)
}

/// GET v3/projects/{projectsId}/supportedLanguages
/// Returns a list of supported languages for translation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_get_supported_languages_execute()` to send, or `translate_projects_get_supported_languages` for simplest API.

pub fn translate_projects_get_supported_languages_builder(
    client: &SimpleHttpClient,
    parent: &str,
    displayLanguageCode: Option<&str>,
    model: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/supportedLanguages",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = displayLanguageCode {
        query_parts.push(format!("displayLanguageCode={}", val));
    }
    if let Some(val) = model {
        query_parts.push(format!("model={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/supportedLanguages
/// Returns a list of supported languages for translation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_get_supported_languages_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_get_supported_languages_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_get_supported_languages_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SupportedLanguages>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SupportedLanguages = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/supportedLanguages
/// Returns a list of supported languages for translation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_get_supported_languages_builder()` + `translate_projects_get_supported_languages_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_get_supported_languages(
    client: &SimpleHttpClient,
    parent: &str,
    displayLanguageCode: Option<&str>,
    model: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SupportedLanguages>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = translate_projects_get_supported_languages_builder(
        client,
        parent,
        displayLanguageCode,
        model,
    )?;
    translate_projects_get_supported_languages_execute(builder)
}

/// GET v3/projects/{projectsId}:romanizeText
/// Romanize input text written in non-Latin scripts to Latin text.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_romanize_text_execute()` to send, or `translate_projects_romanize_text` for simplest API.

pub fn translate_projects_romanize_text_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &RomanizeTextRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}:romanizeText",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}:romanizeText
/// Romanize input text written in non-Latin scripts to Latin text.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_romanize_text_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_romanize_text_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_romanize_text_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RomanizeTextResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RomanizeTextResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}:romanizeText
/// Romanize input text written in non-Latin scripts to Latin text.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_romanize_text_builder()` + `translate_projects_romanize_text_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_romanize_text(
    client: &SimpleHttpClient,
    parent: &str,
    body: &RomanizeTextRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RomanizeTextResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = translate_projects_romanize_text_builder(client, parent, body)?;
    translate_projects_romanize_text_execute(builder)
}

/// GET v3/projects/{projectsId}:translateText
/// Translates input text and returns translated text.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_translate_text_execute()` to send, or `translate_projects_translate_text` for simplest API.

pub fn translate_projects_translate_text_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &TranslateTextRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}:translateText",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}:translateText
/// Translates input text and returns translated text.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_translate_text_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_translate_text_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_translate_text_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TranslateTextResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TranslateTextResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}:translateText
/// Translates input text and returns translated text.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_translate_text_builder()` + `translate_projects_translate_text_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_translate_text(
    client: &SimpleHttpClient,
    parent: &str,
    body: &TranslateTextRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TranslateTextResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = translate_projects_translate_text_builder(client, parent, body)?;
    translate_projects_translate_text_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}:adaptiveMtTranslate
/// Translate text using Adaptive MT.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_adaptive_mt_translate_execute()` to send, or `translate_projects_locations_adaptive_mt_translate` for simplest API.

pub fn translate_projects_locations_adaptive_mt_translate_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &AdaptiveMtTranslateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}:adaptiveMtTranslate",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}:adaptiveMtTranslate
/// Translate text using Adaptive MT.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_adaptive_mt_translate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_adaptive_mt_translate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_adaptive_mt_translate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AdaptiveMtTranslateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AdaptiveMtTranslateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}:adaptiveMtTranslate
/// Translate text using Adaptive MT.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_adaptive_mt_translate_builder()` + `translate_projects_locations_adaptive_mt_translate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_adaptive_mt_translate(
    client: &SimpleHttpClient,
    parent: &str,
    body: &AdaptiveMtTranslateRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AdaptiveMtTranslateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_adaptive_mt_translate_builder(client, parent, body)?;
    translate_projects_locations_adaptive_mt_translate_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}:batchTranslateDocument
/// Translates a large volume of document in asynchronous batch mode. This function provides real-time output as the inputs are being processed. If caller cancels a request, the partial results (for an input file, it's all or nothing) may still be available on the specified output location. This call returns immediately and you can use google.longrunning.Operation.name to poll the status of the call.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_batch_translate_document_execute()` to send, or `translate_projects_locations_batch_translate_document` for simplest API.

pub fn translate_projects_locations_batch_translate_document_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &BatchTranslateDocumentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}:batchTranslateDocument",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}:batchTranslateDocument
/// Translates a large volume of document in asynchronous batch mode. This function provides real-time output as the inputs are being processed. If caller cancels a request, the partial results (for an input file, it's all or nothing) may still be available on the specified output location. This call returns immediately and you can use google.longrunning.Operation.name to poll the status of the call.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_batch_translate_document_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_batch_translate_document_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_batch_translate_document_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}:batchTranslateDocument
/// Translates a large volume of document in asynchronous batch mode. This function provides real-time output as the inputs are being processed. If caller cancels a request, the partial results (for an input file, it's all or nothing) may still be available on the specified output location. This call returns immediately and you can use google.longrunning.Operation.name to poll the status of the call.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_batch_translate_document_builder()` + `translate_projects_locations_batch_translate_document_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_batch_translate_document(
    client: &SimpleHttpClient,
    parent: &str,
    body: &BatchTranslateDocumentRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        translate_projects_locations_batch_translate_document_builder(client, parent, body)?;
    translate_projects_locations_batch_translate_document_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}:batchTranslateText
/// Translates a large volume of text in asynchronous batch mode. This function provides real-time output as the inputs are being processed. If caller cancels a request, the partial results (for an input file, it's all or nothing) may still be available on the specified output location. This call returns immediately and you can use google.longrunning.Operation.name to poll the status of the call.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_batch_translate_text_execute()` to send, or `translate_projects_locations_batch_translate_text` for simplest API.

pub fn translate_projects_locations_batch_translate_text_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &BatchTranslateTextRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}:batchTranslateText",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}:batchTranslateText
/// Translates a large volume of text in asynchronous batch mode. This function provides real-time output as the inputs are being processed. If caller cancels a request, the partial results (for an input file, it's all or nothing) may still be available on the specified output location. This call returns immediately and you can use google.longrunning.Operation.name to poll the status of the call.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_batch_translate_text_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_batch_translate_text_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_batch_translate_text_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}:batchTranslateText
/// Translates a large volume of text in asynchronous batch mode. This function provides real-time output as the inputs are being processed. If caller cancels a request, the partial results (for an input file, it's all or nothing) may still be available on the specified output location. This call returns immediately and you can use google.longrunning.Operation.name to poll the status of the call.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_batch_translate_text_builder()` + `translate_projects_locations_batch_translate_text_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_batch_translate_text(
    client: &SimpleHttpClient,
    parent: &str,
    body: &BatchTranslateTextRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_batch_translate_text_builder(client, parent, body)?;
    translate_projects_locations_batch_translate_text_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}:detectLanguage
/// Detects the language of text within a request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_detect_language_execute()` to send, or `translate_projects_locations_detect_language` for simplest API.

pub fn translate_projects_locations_detect_language_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &DetectLanguageRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}:detectLanguage",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}:detectLanguage
/// Detects the language of text within a request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_detect_language_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_detect_language_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_detect_language_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DetectLanguageResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DetectLanguageResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}:detectLanguage
/// Detects the language of text within a request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_detect_language_builder()` + `translate_projects_locations_detect_language_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_detect_language(
    client: &SimpleHttpClient,
    parent: &str,
    body: &DetectLanguageRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DetectLanguageResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_detect_language_builder(client, parent, body)?;
    translate_projects_locations_detect_language_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_get_execute()` to send, or `translate_projects_locations_get` for simplest API.

pub fn translate_projects_locations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_get_builder()` + `translate_projects_locations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_get_builder(client, name)?;
    translate_projects_locations_get_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/supportedLanguages
/// Returns a list of supported languages for translation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_get_supported_languages_execute()` to send, or `translate_projects_locations_get_supported_languages` for simplest API.

pub fn translate_projects_locations_get_supported_languages_builder(
    client: &SimpleHttpClient,
    parent: &str,
    displayLanguageCode: Option<&str>,
    model: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/supportedLanguages",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = displayLanguageCode {
        query_parts.push(format!("displayLanguageCode={}", val));
    }
    if let Some(val) = model {
        query_parts.push(format!("model={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/supportedLanguages
/// Returns a list of supported languages for translation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_get_supported_languages_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_get_supported_languages_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_get_supported_languages_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SupportedLanguages>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SupportedLanguages = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/supportedLanguages
/// Returns a list of supported languages for translation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_get_supported_languages_builder()` + `translate_projects_locations_get_supported_languages_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_get_supported_languages(
    client: &SimpleHttpClient,
    parent: &str,
    displayLanguageCode: Option<&str>,
    model: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SupportedLanguages>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_get_supported_languages_builder(
        client,
        parent,
        displayLanguageCode,
        model,
    )?;
    translate_projects_locations_get_supported_languages_execute(builder)
}

/// GET v3/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method can be called in two ways: * **List all public locations:** Use the path GET /v1/locations. * **List project-visible locations:** Use the path GET /v1/`projects/{project_id}/locations`. This may include public locations as well as private or other locations specifically visible to the project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_list_execute()` to send, or `translate_projects_locations_list` for simplest API.

pub fn translate_projects_locations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method can be called in two ways: * **List all public locations:** Use the path GET /v1/locations. * **List project-visible locations:** Use the path GET /v1/`projects/{project_id}/locations`. This may include public locations as well as private or other locations specifically visible to the project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method can be called in two ways: * **List all public locations:** Use the path GET /v1/locations. * **List project-visible locations:** Use the path GET /v1/`projects/{project_id}/locations`. This may include public locations as well as private or other locations specifically visible to the project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_list_builder()` + `translate_projects_locations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_list(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_list_builder(
        client,
        name,
        extraLocationTypes,
        filter,
        pageSize,
        pageToken,
    )?;
    translate_projects_locations_list_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}:refineText
/// Refines the input translated text to improve the quality.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_refine_text_execute()` to send, or `translate_projects_locations_refine_text` for simplest API.

pub fn translate_projects_locations_refine_text_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &RefineTextRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}:refineText",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}:refineText
/// Refines the input translated text to improve the quality.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_refine_text_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_refine_text_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_refine_text_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RefineTextResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RefineTextResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}:refineText
/// Refines the input translated text to improve the quality.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_refine_text_builder()` + `translate_projects_locations_refine_text_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_refine_text(
    client: &SimpleHttpClient,
    parent: &str,
    body: &RefineTextRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RefineTextResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_refine_text_builder(client, parent, body)?;
    translate_projects_locations_refine_text_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}:romanizeText
/// Romanize input text written in non-Latin scripts to Latin text.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_romanize_text_execute()` to send, or `translate_projects_locations_romanize_text` for simplest API.

pub fn translate_projects_locations_romanize_text_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &RomanizeTextRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}:romanizeText",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}:romanizeText
/// Romanize input text written in non-Latin scripts to Latin text.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_romanize_text_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_romanize_text_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_romanize_text_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RomanizeTextResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RomanizeTextResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}:romanizeText
/// Romanize input text written in non-Latin scripts to Latin text.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_romanize_text_builder()` + `translate_projects_locations_romanize_text_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_romanize_text(
    client: &SimpleHttpClient,
    parent: &str,
    body: &RomanizeTextRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RomanizeTextResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_romanize_text_builder(client, parent, body)?;
    translate_projects_locations_romanize_text_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}:translateDocument
/// Translates documents in synchronous mode.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_translate_document_execute()` to send, or `translate_projects_locations_translate_document` for simplest API.

pub fn translate_projects_locations_translate_document_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &TranslateDocumentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}:translateDocument",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}:translateDocument
/// Translates documents in synchronous mode.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_translate_document_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_translate_document_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_translate_document_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TranslateDocumentResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TranslateDocumentResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}:translateDocument
/// Translates documents in synchronous mode.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_translate_document_builder()` + `translate_projects_locations_translate_document_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_translate_document(
    client: &SimpleHttpClient,
    parent: &str,
    body: &TranslateDocumentRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TranslateDocumentResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_translate_document_builder(client, parent, body)?;
    translate_projects_locations_translate_document_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}:translateText
/// Translates input text and returns translated text.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_translate_text_execute()` to send, or `translate_projects_locations_translate_text` for simplest API.

pub fn translate_projects_locations_translate_text_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &TranslateTextRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}:translateText",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}:translateText
/// Translates input text and returns translated text.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_translate_text_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_translate_text_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_translate_text_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TranslateTextResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TranslateTextResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}:translateText
/// Translates input text and returns translated text.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_translate_text_builder()` + `translate_projects_locations_translate_text_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_translate_text(
    client: &SimpleHttpClient,
    parent: &str,
    body: &TranslateTextRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TranslateTextResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_translate_text_builder(client, parent, body)?;
    translate_projects_locations_translate_text_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/adaptiveMtDatasets
/// Creates an Adaptive MT dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_adaptive_mt_datasets_create_execute()` to send, or `translate_projects_locations_adaptive_mt_datasets_create` for simplest API.

pub fn translate_projects_locations_adaptive_mt_datasets_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &AdaptiveMtDataset,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/adaptiveMtDatasets",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/adaptiveMtDatasets
/// Creates an Adaptive MT dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_adaptive_mt_datasets_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_adaptive_mt_datasets_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_adaptive_mt_datasets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdaptiveMtDataset>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AdaptiveMtDataset = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/adaptiveMtDatasets
/// Creates an Adaptive MT dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_adaptive_mt_datasets_create_builder()` + `translate_projects_locations_adaptive_mt_datasets_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_adaptive_mt_datasets_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &AdaptiveMtDataset,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdaptiveMtDataset>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        translate_projects_locations_adaptive_mt_datasets_create_builder(client, parent, body)?;
    translate_projects_locations_adaptive_mt_datasets_create_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/adaptiveMtDatasets/{adaptiveMtDatasetsId}
/// Deletes an Adaptive MT dataset, including all its entries and associated metadata.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_adaptive_mt_datasets_delete_execute()` to send, or `translate_projects_locations_adaptive_mt_datasets_delete` for simplest API.

pub fn translate_projects_locations_adaptive_mt_datasets_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/adaptiveMtDatasets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/adaptiveMtDatasets/{adaptiveMtDatasetsId}
/// Deletes an Adaptive MT dataset, including all its entries and associated metadata.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_adaptive_mt_datasets_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_adaptive_mt_datasets_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_adaptive_mt_datasets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/adaptiveMtDatasets/{adaptiveMtDatasetsId}
/// Deletes an Adaptive MT dataset, including all its entries and associated metadata.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_adaptive_mt_datasets_delete_builder()` + `translate_projects_locations_adaptive_mt_datasets_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_adaptive_mt_datasets_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_adaptive_mt_datasets_delete_builder(client, name)?;
    translate_projects_locations_adaptive_mt_datasets_delete_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/adaptiveMtDatasets/{adaptiveMtDatasetsId}
/// Gets the Adaptive MT dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_adaptive_mt_datasets_get_execute()` to send, or `translate_projects_locations_adaptive_mt_datasets_get` for simplest API.

pub fn translate_projects_locations_adaptive_mt_datasets_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/adaptiveMtDatasets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/adaptiveMtDatasets/{adaptiveMtDatasetsId}
/// Gets the Adaptive MT dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_adaptive_mt_datasets_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_adaptive_mt_datasets_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_adaptive_mt_datasets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdaptiveMtDataset>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AdaptiveMtDataset = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/adaptiveMtDatasets/{adaptiveMtDatasetsId}
/// Gets the Adaptive MT dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_adaptive_mt_datasets_get_builder()` + `translate_projects_locations_adaptive_mt_datasets_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_adaptive_mt_datasets_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdaptiveMtDataset>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_adaptive_mt_datasets_get_builder(client, name)?;
    translate_projects_locations_adaptive_mt_datasets_get_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/adaptiveMtDatasets/{adaptiveMtDatasetsId}:importAdaptiveMtFile
/// Imports an AdaptiveMtFile and adds all of its sentences into the AdaptiveMtDataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_adaptive_mt_datasets_import_adaptive_mt_file_execute()` to send, or `translate_projects_locations_adaptive_mt_datasets_import_adaptive_mt_file` for simplest API.

pub fn translate_projects_locations_adaptive_mt_datasets_import_adaptive_mt_file_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &ImportAdaptiveMtFileRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/adaptiveMtDatasets/{}:importAdaptiveMtFile",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/adaptiveMtDatasets/{adaptiveMtDatasetsId}:importAdaptiveMtFile
/// Imports an AdaptiveMtFile and adds all of its sentences into the AdaptiveMtDataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_adaptive_mt_datasets_import_adaptive_mt_file_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_adaptive_mt_datasets_import_adaptive_mt_file_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_adaptive_mt_datasets_import_adaptive_mt_file_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ImportAdaptiveMtFileResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ImportAdaptiveMtFileResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/adaptiveMtDatasets/{adaptiveMtDatasetsId}:importAdaptiveMtFile
/// Imports an AdaptiveMtFile and adds all of its sentences into the AdaptiveMtDataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_adaptive_mt_datasets_import_adaptive_mt_file_builder()` + `translate_projects_locations_adaptive_mt_datasets_import_adaptive_mt_file_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_adaptive_mt_datasets_import_adaptive_mt_file(
    client: &SimpleHttpClient,
    parent: &str,
    body: &ImportAdaptiveMtFileRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ImportAdaptiveMtFileResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        translate_projects_locations_adaptive_mt_datasets_import_adaptive_mt_file_builder(
            client, parent, body,
        )?;
    translate_projects_locations_adaptive_mt_datasets_import_adaptive_mt_file_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/adaptiveMtDatasets
/// Lists all Adaptive MT datasets for which the caller has read permission.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_adaptive_mt_datasets_list_execute()` to send, or `translate_projects_locations_adaptive_mt_datasets_list` for simplest API.

pub fn translate_projects_locations_adaptive_mt_datasets_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/adaptiveMtDatasets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/adaptiveMtDatasets
/// Lists all Adaptive MT datasets for which the caller has read permission.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_adaptive_mt_datasets_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_adaptive_mt_datasets_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_adaptive_mt_datasets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAdaptiveMtDatasetsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAdaptiveMtDatasetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/adaptiveMtDatasets
/// Lists all Adaptive MT datasets for which the caller has read permission.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_adaptive_mt_datasets_list_builder()` + `translate_projects_locations_adaptive_mt_datasets_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_adaptive_mt_datasets_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAdaptiveMtDatasetsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_adaptive_mt_datasets_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    translate_projects_locations_adaptive_mt_datasets_list_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/adaptiveMtDatasets/{adaptiveMtDatasetsId}/adaptiveMtFiles/{adaptiveMtFilesId}
/// Deletes an AdaptiveMtFile along with its sentences.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_delete_execute()` to send, or `translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_delete` for simplest API.

pub fn translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/adaptiveMtDatasets/{}/adaptiveMtFiles/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/adaptiveMtDatasets/{adaptiveMtDatasetsId}/adaptiveMtFiles/{adaptiveMtFilesId}
/// Deletes an AdaptiveMtFile along with its sentences.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/adaptiveMtDatasets/{adaptiveMtDatasetsId}/adaptiveMtFiles/{adaptiveMtFilesId}
/// Deletes an AdaptiveMtFile along with its sentences.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_delete_builder()` + `translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_delete_builder(
            client, name,
        )?;
    translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_delete_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/adaptiveMtDatasets/{adaptiveMtDatasetsId}/adaptiveMtFiles/{adaptiveMtFilesId}
/// Gets and AdaptiveMtFile
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_get_execute()` to send, or `translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_get` for simplest API.

pub fn translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/adaptiveMtDatasets/{}/adaptiveMtFiles/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/adaptiveMtDatasets/{adaptiveMtDatasetsId}/adaptiveMtFiles/{adaptiveMtFilesId}
/// Gets and AdaptiveMtFile
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdaptiveMtFile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AdaptiveMtFile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/adaptiveMtDatasets/{adaptiveMtDatasetsId}/adaptiveMtFiles/{adaptiveMtFilesId}
/// Gets and AdaptiveMtFile
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_get_builder()` + `translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AdaptiveMtFile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_get_builder(
        client, name,
    )?;
    translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_get_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/adaptiveMtDatasets/{adaptiveMtDatasetsId}/adaptiveMtFiles
/// Lists all AdaptiveMtFiles associated to an AdaptiveMtDataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_list_execute()` to send, or `translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_list` for simplest API.

pub fn translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/adaptiveMtDatasets/{}/adaptiveMtFiles",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/adaptiveMtDatasets/{adaptiveMtDatasetsId}/adaptiveMtFiles
/// Lists all AdaptiveMtFiles associated to an AdaptiveMtDataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAdaptiveMtFilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAdaptiveMtFilesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/adaptiveMtDatasets/{adaptiveMtDatasetsId}/adaptiveMtFiles
/// Lists all AdaptiveMtFiles associated to an AdaptiveMtDataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_list_builder()` + `translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAdaptiveMtFilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_list_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/adaptiveMtDatasets/{adaptiveMtDatasetsId}/adaptiveMtFiles/{adaptiveMtFilesId}/adaptiveMtSentences
/// Lists all AdaptiveMtSentences under a given `file/dataset`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_adaptive_mt_sentences_list_execute()` to send, or `translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_adaptive_mt_sentences_list` for simplest API.

pub fn translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_adaptive_mt_sentences_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/adaptiveMtDatasets/{}/adaptiveMtFiles/{}/adaptiveMtSentences",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/adaptiveMtDatasets/{adaptiveMtDatasetsId}/adaptiveMtFiles/{adaptiveMtFilesId}/adaptiveMtSentences
/// Lists all AdaptiveMtSentences under a given `file/dataset`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_adaptive_mt_sentences_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_adaptive_mt_sentences_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_adaptive_mt_sentences_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAdaptiveMtSentencesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAdaptiveMtSentencesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/adaptiveMtDatasets/{adaptiveMtDatasetsId}/adaptiveMtFiles/{adaptiveMtFilesId}/adaptiveMtSentences
/// Lists all AdaptiveMtSentences under a given `file/dataset`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_adaptive_mt_sentences_list_builder()` + `translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_adaptive_mt_sentences_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_adaptive_mt_sentences_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAdaptiveMtSentencesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_adaptive_mt_sentences_list_builder(client, parent, pageSize, pageToken)?;
    translate_projects_locations_adaptive_mt_datasets_adaptive_mt_files_adaptive_mt_sentences_list_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/adaptiveMtDatasets/{adaptiveMtDatasetsId}/adaptiveMtSentences
/// Lists all AdaptiveMtSentences under a given `file/dataset`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_adaptive_mt_datasets_adaptive_mt_sentences_list_execute()` to send, or `translate_projects_locations_adaptive_mt_datasets_adaptive_mt_sentences_list` for simplest API.

pub fn translate_projects_locations_adaptive_mt_datasets_adaptive_mt_sentences_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/adaptiveMtDatasets/{}/adaptiveMtSentences",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/adaptiveMtDatasets/{adaptiveMtDatasetsId}/adaptiveMtSentences
/// Lists all AdaptiveMtSentences under a given `file/dataset`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_adaptive_mt_datasets_adaptive_mt_sentences_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_adaptive_mt_datasets_adaptive_mt_sentences_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_adaptive_mt_datasets_adaptive_mt_sentences_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAdaptiveMtSentencesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAdaptiveMtSentencesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/adaptiveMtDatasets/{adaptiveMtDatasetsId}/adaptiveMtSentences
/// Lists all AdaptiveMtSentences under a given `file/dataset`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_adaptive_mt_datasets_adaptive_mt_sentences_list_builder()` + `translate_projects_locations_adaptive_mt_datasets_adaptive_mt_sentences_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_adaptive_mt_datasets_adaptive_mt_sentences_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAdaptiveMtSentencesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        translate_projects_locations_adaptive_mt_datasets_adaptive_mt_sentences_list_builder(
            client, parent, pageSize, pageToken,
        )?;
    translate_projects_locations_adaptive_mt_datasets_adaptive_mt_sentences_list_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/datasets
/// Creates a Dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_datasets_create_execute()` to send, or `translate_projects_locations_datasets_create` for simplest API.

pub fn translate_projects_locations_datasets_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Dataset,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/datasets",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/datasets
/// Creates a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_datasets_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_datasets_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_datasets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/datasets
/// Creates a Dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_datasets_create_builder()` + `translate_projects_locations_datasets_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_datasets_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Dataset,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_datasets_create_builder(client, parent, body)?;
    translate_projects_locations_datasets_create_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Deletes a dataset and all of its contents.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_datasets_delete_execute()` to send, or `translate_projects_locations_datasets_delete` for simplest API.

pub fn translate_projects_locations_datasets_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/datasets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Deletes a dataset and all of its contents.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_datasets_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_datasets_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_datasets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Deletes a dataset and all of its contents.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_datasets_delete_builder()` + `translate_projects_locations_datasets_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_datasets_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_datasets_delete_builder(client, name)?;
    translate_projects_locations_datasets_delete_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:exportData
/// Exports dataset's data to the provided output location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_datasets_export_data_execute()` to send, or `translate_projects_locations_datasets_export_data` for simplest API.

pub fn translate_projects_locations_datasets_export_data_builder(
    client: &SimpleHttpClient,
    dataset: &str,
    body: &ExportDataRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/datasets/{}:exportData",
        dataset,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:exportData
/// Exports dataset's data to the provided output location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_datasets_export_data_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_datasets_export_data_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_datasets_export_data_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:exportData
/// Exports dataset's data to the provided output location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_datasets_export_data_builder()` + `translate_projects_locations_datasets_export_data_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_datasets_export_data(
    client: &SimpleHttpClient,
    dataset: &str,
    body: &ExportDataRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_datasets_export_data_builder(client, dataset, body)?;
    translate_projects_locations_datasets_export_data_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Gets a Dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_datasets_get_execute()` to send, or `translate_projects_locations_datasets_get` for simplest API.

pub fn translate_projects_locations_datasets_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/datasets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Gets a Dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_datasets_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_datasets_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_datasets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Dataset>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Dataset = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}
/// Gets a Dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_datasets_get_builder()` + `translate_projects_locations_datasets_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_datasets_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Dataset>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_datasets_get_builder(client, name)?;
    translate_projects_locations_datasets_get_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:importData
/// Import sentence pairs into translation Dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_datasets_import_data_execute()` to send, or `translate_projects_locations_datasets_import_data` for simplest API.

pub fn translate_projects_locations_datasets_import_data_builder(
    client: &SimpleHttpClient,
    dataset: &str,
    body: &ImportDataRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/datasets/{}:importData",
        dataset,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:importData
/// Import sentence pairs into translation Dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_datasets_import_data_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_datasets_import_data_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_datasets_import_data_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}:importData
/// Import sentence pairs into translation Dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_datasets_import_data_builder()` + `translate_projects_locations_datasets_import_data_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_datasets_import_data(
    client: &SimpleHttpClient,
    dataset: &str,
    body: &ImportDataRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_datasets_import_data_builder(client, dataset, body)?;
    translate_projects_locations_datasets_import_data_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/datasets
/// Lists datasets.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_datasets_list_execute()` to send, or `translate_projects_locations_datasets_list` for simplest API.

pub fn translate_projects_locations_datasets_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/datasets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/datasets
/// Lists datasets.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_datasets_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_datasets_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_datasets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDatasetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDatasetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/datasets
/// Lists datasets.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_datasets_list_builder()` + `translate_projects_locations_datasets_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_datasets_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDatasetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        translate_projects_locations_datasets_list_builder(client, parent, pageSize, pageToken)?;
    translate_projects_locations_datasets_list_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/examples
/// Lists sentence pairs in the dataset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_datasets_examples_list_execute()` to send, or `translate_projects_locations_datasets_examples_list` for simplest API.

pub fn translate_projects_locations_datasets_examples_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/datasets/{}/examples",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/examples
/// Lists sentence pairs in the dataset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_datasets_examples_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_datasets_examples_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_datasets_examples_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListExamplesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListExamplesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/examples
/// Lists sentence pairs in the dataset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_datasets_examples_list_builder()` + `translate_projects_locations_datasets_examples_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_datasets_examples_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListExamplesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_datasets_examples_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    translate_projects_locations_datasets_examples_list_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/glossaries
/// Creates a glossary and returns the long-running operation. Returns NOT_FOUND, if the project doesn't exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_glossaries_create_execute()` to send, or `translate_projects_locations_glossaries_create` for simplest API.

pub fn translate_projects_locations_glossaries_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Glossary,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/glossaries",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/glossaries
/// Creates a glossary and returns the long-running operation. Returns NOT_FOUND, if the project doesn't exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_glossaries_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_glossaries_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_glossaries_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/glossaries
/// Creates a glossary and returns the long-running operation. Returns NOT_FOUND, if the project doesn't exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_glossaries_create_builder()` + `translate_projects_locations_glossaries_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_glossaries_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Glossary,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_glossaries_create_builder(client, parent, body)?;
    translate_projects_locations_glossaries_create_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/glossaries/{glossariesId}
/// Deletes a glossary, or cancels glossary construction if the glossary isn't created yet. Returns NOT_FOUND, if the glossary doesn't exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_glossaries_delete_execute()` to send, or `translate_projects_locations_glossaries_delete` for simplest API.

pub fn translate_projects_locations_glossaries_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/glossaries/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/glossaries/{glossariesId}
/// Deletes a glossary, or cancels glossary construction if the glossary isn't created yet. Returns NOT_FOUND, if the glossary doesn't exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_glossaries_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_glossaries_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_glossaries_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/glossaries/{glossariesId}
/// Deletes a glossary, or cancels glossary construction if the glossary isn't created yet. Returns NOT_FOUND, if the glossary doesn't exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_glossaries_delete_builder()` + `translate_projects_locations_glossaries_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_glossaries_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_glossaries_delete_builder(client, name)?;
    translate_projects_locations_glossaries_delete_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/glossaries/{glossariesId}
/// Gets a glossary. Returns NOT_FOUND, if the glossary doesn't exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_glossaries_get_execute()` to send, or `translate_projects_locations_glossaries_get` for simplest API.

pub fn translate_projects_locations_glossaries_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/glossaries/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/glossaries/{glossariesId}
/// Gets a glossary. Returns NOT_FOUND, if the glossary doesn't exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_glossaries_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_glossaries_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_glossaries_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Glossary>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Glossary = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/glossaries/{glossariesId}
/// Gets a glossary. Returns NOT_FOUND, if the glossary doesn't exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_glossaries_get_builder()` + `translate_projects_locations_glossaries_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_glossaries_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Glossary>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_glossaries_get_builder(client, name)?;
    translate_projects_locations_glossaries_get_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/glossaries
/// Lists glossaries in a project. Returns NOT_FOUND, if the project doesn't exist.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_glossaries_list_execute()` to send, or `translate_projects_locations_glossaries_list` for simplest API.

pub fn translate_projects_locations_glossaries_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/glossaries",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/glossaries
/// Lists glossaries in a project. Returns NOT_FOUND, if the project doesn't exist.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_glossaries_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_glossaries_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_glossaries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListGlossariesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListGlossariesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/glossaries
/// Lists glossaries in a project. Returns NOT_FOUND, if the project doesn't exist.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_glossaries_list_builder()` + `translate_projects_locations_glossaries_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_glossaries_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListGlossariesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_glossaries_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    translate_projects_locations_glossaries_list_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/glossaries/{glossariesId}
/// Updates a glossary. A LRO is used since the update can be async if the glossary's entry file is updated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_glossaries_patch_execute()` to send, or `translate_projects_locations_glossaries_patch` for simplest API.

pub fn translate_projects_locations_glossaries_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Glossary,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/glossaries/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/glossaries/{glossariesId}
/// Updates a glossary. A LRO is used since the update can be async if the glossary's entry file is updated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_glossaries_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_glossaries_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_glossaries_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/glossaries/{glossariesId}
/// Updates a glossary. A LRO is used since the update can be async if the glossary's entry file is updated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_glossaries_patch_builder()` + `translate_projects_locations_glossaries_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_glossaries_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Glossary,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        translate_projects_locations_glossaries_patch_builder(client, name, updateMask, body)?;
    translate_projects_locations_glossaries_patch_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/glossaries/{glossariesId}/glossaryEntries
/// Creates a glossary entry.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_glossaries_glossary_entries_create_execute()` to send, or `translate_projects_locations_glossaries_glossary_entries_create` for simplest API.

pub fn translate_projects_locations_glossaries_glossary_entries_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GlossaryEntry,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/glossaries/{}/glossaryEntries",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/glossaries/{glossariesId}/glossaryEntries
/// Creates a glossary entry.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_glossaries_glossary_entries_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_glossaries_glossary_entries_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_glossaries_glossary_entries_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GlossaryEntry>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GlossaryEntry = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/glossaries/{glossariesId}/glossaryEntries
/// Creates a glossary entry.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_glossaries_glossary_entries_create_builder()` + `translate_projects_locations_glossaries_glossary_entries_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_glossaries_glossary_entries_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GlossaryEntry,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GlossaryEntry>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_glossaries_glossary_entries_create_builder(
        client, parent, body,
    )?;
    translate_projects_locations_glossaries_glossary_entries_create_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/glossaries/{glossariesId}/glossaryEntries/{glossaryEntriesId}
/// Deletes a single entry from the glossary
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_glossaries_glossary_entries_delete_execute()` to send, or `translate_projects_locations_glossaries_glossary_entries_delete` for simplest API.

pub fn translate_projects_locations_glossaries_glossary_entries_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/glossaries/{}/glossaryEntries/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/glossaries/{glossariesId}/glossaryEntries/{glossaryEntriesId}
/// Deletes a single entry from the glossary
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_glossaries_glossary_entries_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_glossaries_glossary_entries_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_glossaries_glossary_entries_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/glossaries/{glossariesId}/glossaryEntries/{glossaryEntriesId}
/// Deletes a single entry from the glossary
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_glossaries_glossary_entries_delete_builder()` + `translate_projects_locations_glossaries_glossary_entries_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_glossaries_glossary_entries_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        translate_projects_locations_glossaries_glossary_entries_delete_builder(client, name)?;
    translate_projects_locations_glossaries_glossary_entries_delete_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/glossaries/{glossariesId}/glossaryEntries/{glossaryEntriesId}
/// Gets a single glossary entry by the given id.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_glossaries_glossary_entries_get_execute()` to send, or `translate_projects_locations_glossaries_glossary_entries_get` for simplest API.

pub fn translate_projects_locations_glossaries_glossary_entries_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/glossaries/{}/glossaryEntries/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/glossaries/{glossariesId}/glossaryEntries/{glossaryEntriesId}
/// Gets a single glossary entry by the given id.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_glossaries_glossary_entries_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_glossaries_glossary_entries_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_glossaries_glossary_entries_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GlossaryEntry>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GlossaryEntry = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/glossaries/{glossariesId}/glossaryEntries/{glossaryEntriesId}
/// Gets a single glossary entry by the given id.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_glossaries_glossary_entries_get_builder()` + `translate_projects_locations_glossaries_glossary_entries_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_glossaries_glossary_entries_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GlossaryEntry>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        translate_projects_locations_glossaries_glossary_entries_get_builder(client, name)?;
    translate_projects_locations_glossaries_glossary_entries_get_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/glossaries/{glossariesId}/glossaryEntries
/// List the entries for the glossary.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_glossaries_glossary_entries_list_execute()` to send, or `translate_projects_locations_glossaries_glossary_entries_list` for simplest API.

pub fn translate_projects_locations_glossaries_glossary_entries_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/glossaries/{}/glossaryEntries",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/glossaries/{glossariesId}/glossaryEntries
/// List the entries for the glossary.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_glossaries_glossary_entries_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_glossaries_glossary_entries_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_glossaries_glossary_entries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListGlossaryEntriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListGlossaryEntriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/glossaries/{glossariesId}/glossaryEntries
/// List the entries for the glossary.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_glossaries_glossary_entries_list_builder()` + `translate_projects_locations_glossaries_glossary_entries_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_glossaries_glossary_entries_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListGlossaryEntriesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_glossaries_glossary_entries_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    translate_projects_locations_glossaries_glossary_entries_list_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/glossaries/{glossariesId}/glossaryEntries/{glossaryEntriesId}
/// Updates a glossary entry.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_glossaries_glossary_entries_patch_execute()` to send, or `translate_projects_locations_glossaries_glossary_entries_patch` for simplest API.

pub fn translate_projects_locations_glossaries_glossary_entries_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GlossaryEntry,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/glossaries/{}/glossaryEntries/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/glossaries/{glossariesId}/glossaryEntries/{glossaryEntriesId}
/// Updates a glossary entry.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_glossaries_glossary_entries_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_glossaries_glossary_entries_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_glossaries_glossary_entries_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GlossaryEntry>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GlossaryEntry = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/glossaries/{glossariesId}/glossaryEntries/{glossaryEntriesId}
/// Updates a glossary entry.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_glossaries_glossary_entries_patch_builder()` + `translate_projects_locations_glossaries_glossary_entries_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_glossaries_glossary_entries_patch(
    client: &SimpleHttpClient,
    name: &str,
    body: &GlossaryEntry,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GlossaryEntry>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        translate_projects_locations_glossaries_glossary_entries_patch_builder(client, name, body)?;
    translate_projects_locations_glossaries_glossary_entries_patch_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/models
/// Creates a Model.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_models_create_execute()` to send, or `translate_projects_locations_models_create` for simplest API.

pub fn translate_projects_locations_models_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Model,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/models",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/models
/// Creates a Model.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_models_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_models_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_models_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/models
/// Creates a Model.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_models_create_builder()` + `translate_projects_locations_models_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_models_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Model,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_models_create_builder(client, parent, body)?;
    translate_projects_locations_models_create_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/models/{modelsId}
/// Deletes a model.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_models_delete_execute()` to send, or `translate_projects_locations_models_delete` for simplest API.

pub fn translate_projects_locations_models_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/models/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/models/{modelsId}
/// Deletes a model.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_models_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_models_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_models_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/models/{modelsId}
/// Deletes a model.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_models_delete_builder()` + `translate_projects_locations_models_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_models_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_models_delete_builder(client, name)?;
    translate_projects_locations_models_delete_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/models/{modelsId}
/// Gets a model.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_models_get_execute()` to send, or `translate_projects_locations_models_get` for simplest API.

pub fn translate_projects_locations_models_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/models/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/models/{modelsId}
/// Gets a model.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_models_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_models_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_models_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Model>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Model = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/models/{modelsId}
/// Gets a model.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_models_get_builder()` + `translate_projects_locations_models_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_models_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Model>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_models_get_builder(client, name)?;
    translate_projects_locations_models_get_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/models
/// Lists models.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_models_list_execute()` to send, or `translate_projects_locations_models_list` for simplest API.

pub fn translate_projects_locations_models_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/models",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/models
/// Lists models.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_models_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_models_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_models_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListModelsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListModelsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/models
/// Lists models.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_models_list_builder()` + `translate_projects_locations_models_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_models_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListModelsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_models_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    translate_projects_locations_models_list_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_operations_cancel_execute()` to send, or `translate_projects_locations_operations_cancel` for simplest API.

pub fn translate_projects_locations_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_operations_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_operations_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_operations_cancel_builder()` + `translate_projects_locations_operations_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    name: &str,
    body: &CancelOperationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_operations_cancel_builder(client, name, body)?;
    translate_projects_locations_operations_cancel_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_operations_delete_execute()` to send, or `translate_projects_locations_operations_delete` for simplest API.

pub fn translate_projects_locations_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_operations_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_operations_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_operations_delete_builder()` + `translate_projects_locations_operations_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_operations_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_operations_delete_builder(client, name)?;
    translate_projects_locations_operations_delete_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_operations_get_execute()` to send, or `translate_projects_locations_operations_get` for simplest API.

pub fn translate_projects_locations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_operations_get_builder()` + `translate_projects_locations_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_operations_get_builder(client, name)?;
    translate_projects_locations_operations_get_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_operations_list_execute()` to send, or `translate_projects_locations_operations_list` for simplest API.

pub fn translate_projects_locations_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_operations_list_builder()` + `translate_projects_locations_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_operations_list(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_operations_list_builder(
        client,
        name,
        filter,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    translate_projects_locations_operations_list_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `translate_projects_locations_operations_wait_execute()` to send, or `translate_projects_locations_operations_wait` for simplest API.

pub fn translate_projects_locations_operations_wait_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &WaitOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://translation.googleapis.com/v3/projects/{}/locations/{}/operations/{}:wait",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `translate_projects_locations_operations_wait_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from translate_projects_locations_operations_wait_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `translate_projects_locations_operations_wait_builder()` + `translate_projects_locations_operations_wait_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn translate_projects_locations_operations_wait(
    client: &SimpleHttpClient,
    name: &str,
    body: &WaitOperationRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = translate_projects_locations_operations_wait_builder(client, name, body)?;
    translate_projects_locations_operations_wait_execute(builder)
}
