//! Auto-generated API clients for gcp/fitness.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST {userId}/dataSources
/// Creates a new data source that is unique across all data sources belonging to this user. A data source is a unique source of sensor data. Data sources can expose raw data coming from hardware sensors on local or companion devices. They can also expose derived data, created by transforming or merging other data sources. Multiple data sources can exist for the same data type. Every data point in every dataset inserted into or read from the Fitness API has an associated data source. Each data source produces a unique stream of dataset updates, with a unique data source identifier. Not all changes to data source affect the data stream ID, so that data collected by updated versions of the same `application/device` can still be considered to belong to the same data source. Data sources are identified using a string generated by the server, based on the contents of the source being created. The `dataStreamId` field should not be set when invoking this method. It will be automatically generated by the server with the correct format. If a `dataStreamId` is set, it must match the format that the server would generate. This format is a combination of some fields from the data source, and has a specific order. If it doesn''t match, the request will fail with an error. Specifying a DataType which is not a known type (beginning with "com.google.") will create a DataSource with a *custom data type*. Custom data types are only readable by the application that created them. Custom data types are *deprecated*; use standard data types instead. In addition to the data source fields included in the data source ID, the developer project number that is authenticated when creating the data source is included. This developer project number is obfuscated when read by any other developer reading public data types.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `fitness_users_data_sources_create_execute()` to send, or `fitness_users_data_sources_create` for simplest API.

pub fn fitness_users_data_sources_create_builder<R>(
    client: &SimpleHttpClient<R>,
    userId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://fitness.googleapis.com/fitness/v1/users/{}/dataSources",
        userId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST {userId}/dataSources
/// Creates a new data source that is unique across all data sources belonging to this user. A data source is a unique source of sensor data. Data sources can expose raw data coming from hardware sensors on local or companion devices. They can also expose derived data, created by transforming or merging other data sources. Multiple data sources can exist for the same data type. Every data point in every dataset inserted into or read from the Fitness API has an associated data source. Each data source produces a unique stream of dataset updates, with a unique data source identifier. Not all changes to data source affect the data stream ID, so that data collected by updated versions of the same `application/device` can still be considered to belong to the same data source. Data sources are identified using a string generated by the server, based on the contents of the source being created. The `dataStreamId` field should not be set when invoking this method. It will be automatically generated by the server with the correct format. If a `dataStreamId` is set, it must match the format that the server would generate. This format is a combination of some fields from the data source, and has a specific order. If it doesn''t match, the request will fail with an error. Specifying a DataType which is not a known type (beginning with "com.google.") will create a DataSource with a *custom data type*. Custom data types are only readable by the application that created them. Custom data types are *deprecated*; use standard data types instead. In addition to the data source fields included in the data source ID, the developer project number that is authenticated when creating the data source is included. This developer project number is obfuscated when read by any other developer reading public data types.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `fitness_users_data_sources_create_execute()` or `fitness_users_data_sources_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `fitness_users_data_sources_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn fitness_users_data_sources_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DataSource>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DataSource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST {userId}/dataSources
/// Creates a new data source that is unique across all data sources belonging to this user. A data source is a unique source of sensor data. Data sources can expose raw data coming from hardware sensors on local or companion devices. They can also expose derived data, created by transforming or merging other data sources. Multiple data sources can exist for the same data type. Every data point in every dataset inserted into or read from the Fitness API has an associated data source. Each data source produces a unique stream of dataset updates, with a unique data source identifier. Not all changes to data source affect the data stream ID, so that data collected by updated versions of the same `application/device` can still be considered to belong to the same data source. Data sources are identified using a string generated by the server, based on the contents of the source being created. The `dataStreamId` field should not be set when invoking this method. It will be automatically generated by the server with the correct format. If a `dataStreamId` is set, it must match the format that the server would generate. This format is a combination of some fields from the data source, and has a specific order. If it doesn''t match, the request will fail with an error. Specifying a DataType which is not a known type (beginning with "com.google.") will create a DataSource with a *custom data type*. Custom data types are only readable by the application that created them. Custom data types are *deprecated*; use standard data types instead. In addition to the data source fields included in the data source ID, the developer project number that is authenticated when creating the data source is included. This developer project number is obfuscated when read by any other developer reading public data types.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `fitness_users_data_sources_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `fitness_users_data_sources_create_task()`.
/// For the simplest API, use `fitness_users_data_sources_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `fitness_users_data_sources_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn fitness_users_data_sources_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataSource>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = fitness_users_data_sources_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`fitness_users_data_sources_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FitnessUsersDataSourcesCreateArgs {
    /// Path parameter: userId
    pub userId: String,
}

/// POST {userId}/dataSources
/// Creates a new data source that is unique across all data sources belonging to this user. A data source is a unique source of sensor data. Data sources can expose raw data coming from hardware sensors on local or companion devices. They can also expose derived data, created by transforming or merging other data sources. Multiple data sources can exist for the same data type. Every data point in every dataset inserted into or read from the Fitness API has an associated data source. Each data source produces a unique stream of dataset updates, with a unique data source identifier. Not all changes to data source affect the data stream ID, so that data collected by updated versions of the same `application/device` can still be considered to belong to the same data source. Data sources are identified using a string generated by the server, based on the contents of the source being created. The `dataStreamId` field should not be set when invoking this method. It will be automatically generated by the server with the correct format. If a `dataStreamId` is set, it must match the format that the server would generate. This format is a combination of some fields from the data source, and has a specific order. If it doesn''t match, the request will fail with an error. Specifying a DataType which is not a known type (beginning with "com.google.") will create a DataSource with a *custom data type*. Custom data types are only readable by the application that created them. Custom data types are *deprecated*; use standard data types instead. In addition to the data source fields included in the data source ID, the developer project number that is authenticated when creating the data source is included. This developer project number is obfuscated when read by any other developer reading public data types.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `fitness_users_data_sources_create_builder()` + `fitness_users_data_sources_create_execute()`.
/// For task-level control, use `fitness_users_data_sources_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn fitness_users_data_sources_create(
    client: &SimpleHttpClient,
    args: &FitnessUsersDataSourcesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataSource>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = fitness_users_data_sources_create_builder(client, &args.userId)?;
    fitness_users_data_sources_create_execute(builder)
}

/// DELETE {userId}/dataSources/{dataSourceId}
/// Deletes the specified data source. The request will fail if the data source contains any data points.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `fitness_users_data_sources_delete_execute()` to send, or `fitness_users_data_sources_delete` for simplest API.

pub fn fitness_users_data_sources_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    userId: &String,
    dataSourceId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://fitness.googleapis.com/fitness/v1/users/{}/dataSources/{}",
        userId, dataSourceId,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE {userId}/dataSources/{dataSourceId}
/// Deletes the specified data source. The request will fail if the data source contains any data points.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `fitness_users_data_sources_delete_execute()` or `fitness_users_data_sources_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `fitness_users_data_sources_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn fitness_users_data_sources_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DataSource>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DataSource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE {userId}/dataSources/{dataSourceId}
/// Deletes the specified data source. The request will fail if the data source contains any data points.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `fitness_users_data_sources_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `fitness_users_data_sources_delete_task()`.
/// For the simplest API, use `fitness_users_data_sources_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `fitness_users_data_sources_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn fitness_users_data_sources_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataSource>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = fitness_users_data_sources_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`fitness_users_data_sources_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FitnessUsersDataSourcesDeleteArgs {
    /// Path parameter: userId
    pub userId: String,
    /// Path parameter: dataSourceId
    pub dataSourceId: String,
}

/// DELETE {userId}/dataSources/{dataSourceId}
/// Deletes the specified data source. The request will fail if the data source contains any data points.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `fitness_users_data_sources_delete_builder()` + `fitness_users_data_sources_delete_execute()`.
/// For task-level control, use `fitness_users_data_sources_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn fitness_users_data_sources_delete(
    client: &SimpleHttpClient,
    args: &FitnessUsersDataSourcesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataSource>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        fitness_users_data_sources_delete_builder(client, &args.userId, &args.dataSourceId)?;
    fitness_users_data_sources_delete_execute(builder)
}

/// GET {userId}/dataSources/{dataSourceId}
/// Returns the specified data source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `fitness_users_data_sources_get_execute()` to send, or `fitness_users_data_sources_get` for simplest API.

pub fn fitness_users_data_sources_get_builder<R>(
    client: &SimpleHttpClient<R>,
    userId: &String,
    dataSourceId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://fitness.googleapis.com/fitness/v1/users/{}/dataSources/{}",
        userId, dataSourceId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {userId}/dataSources/{dataSourceId}
/// Returns the specified data source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `fitness_users_data_sources_get_execute()` or `fitness_users_data_sources_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `fitness_users_data_sources_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn fitness_users_data_sources_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DataSource>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DataSource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {userId}/dataSources/{dataSourceId}
/// Returns the specified data source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `fitness_users_data_sources_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `fitness_users_data_sources_get_task()`.
/// For the simplest API, use `fitness_users_data_sources_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `fitness_users_data_sources_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn fitness_users_data_sources_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataSource>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = fitness_users_data_sources_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`fitness_users_data_sources_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FitnessUsersDataSourcesGetArgs {
    /// Path parameter: userId
    pub userId: String,
    /// Path parameter: dataSourceId
    pub dataSourceId: String,
}

/// GET {userId}/dataSources/{dataSourceId}
/// Returns the specified data source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `fitness_users_data_sources_get_builder()` + `fitness_users_data_sources_get_execute()`.
/// For task-level control, use `fitness_users_data_sources_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn fitness_users_data_sources_get(
    client: &SimpleHttpClient,
    args: &FitnessUsersDataSourcesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataSource>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = fitness_users_data_sources_get_builder(client, &args.userId, &args.dataSourceId)?;
    fitness_users_data_sources_get_execute(builder)
}

/// GET {userId}/dataSources
/// Lists all data sources that are visible to the developer, using the OAuth scopes provided. The list is not exhaustive; the user may have private data sources that are only visible to other developers, or calls using other scopes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `fitness_users_data_sources_list_execute()` to send, or `fitness_users_data_sources_list` for simplest API.

pub fn fitness_users_data_sources_list_builder<R>(
    client: &SimpleHttpClient<R>,
    userId: &String,
    dataTypeName: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://fitness.googleapis.com/fitness/v1/users/{}/dataSources",
        userId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = dataTypeName.as_ref() {
        query_parts.push(format!("dataTypeName={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {userId}/dataSources
/// Lists all data sources that are visible to the developer, using the OAuth scopes provided. The list is not exhaustive; the user may have private data sources that are only visible to other developers, or calls using other scopes.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `fitness_users_data_sources_list_execute()` or `fitness_users_data_sources_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `fitness_users_data_sources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn fitness_users_data_sources_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDataSourcesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDataSourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {userId}/dataSources
/// Lists all data sources that are visible to the developer, using the OAuth scopes provided. The list is not exhaustive; the user may have private data sources that are only visible to other developers, or calls using other scopes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `fitness_users_data_sources_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `fitness_users_data_sources_list_task()`.
/// For the simplest API, use `fitness_users_data_sources_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `fitness_users_data_sources_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn fitness_users_data_sources_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDataSourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = fitness_users_data_sources_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`fitness_users_data_sources_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FitnessUsersDataSourcesListArgs {
    /// Path parameter: userId
    pub userId: String,
    /// Query parameter: dataTypeName
    pub dataTypeName: Option<Option<String>>,
}

/// GET {userId}/dataSources
/// Lists all data sources that are visible to the developer, using the OAuth scopes provided. The list is not exhaustive; the user may have private data sources that are only visible to other developers, or calls using other scopes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `fitness_users_data_sources_list_builder()` + `fitness_users_data_sources_list_execute()`.
/// For task-level control, use `fitness_users_data_sources_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn fitness_users_data_sources_list(
    client: &SimpleHttpClient,
    args: &FitnessUsersDataSourcesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDataSourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        fitness_users_data_sources_list_builder(client, &args.userId, &args.dataTypeName)?;
    fitness_users_data_sources_list_execute(builder)
}

/// PUT {userId}/dataSources/{dataSourceId}
/// Updates the specified data source. The `dataStreamId`, `dataType`, type, `dataStreamName`, and device properties with the exception of version, cannot be modified. Data sources are identified by their `dataStreamId`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `fitness_users_data_sources_update_execute()` to send, or `fitness_users_data_sources_update` for simplest API.

pub fn fitness_users_data_sources_update_builder<R>(
    client: &SimpleHttpClient<R>,
    userId: &String,
    dataSourceId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://fitness.googleapis.com/fitness/v1/users/{}/dataSources/{}",
        userId, dataSourceId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT {userId}/dataSources/{dataSourceId}
/// Updates the specified data source. The `dataStreamId`, `dataType`, type, `dataStreamName`, and device properties with the exception of version, cannot be modified. Data sources are identified by their `dataStreamId`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `fitness_users_data_sources_update_execute()` or `fitness_users_data_sources_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `fitness_users_data_sources_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn fitness_users_data_sources_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DataSource>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DataSource = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT {userId}/dataSources/{dataSourceId}
/// Updates the specified data source. The `dataStreamId`, `dataType`, type, `dataStreamName`, and device properties with the exception of version, cannot be modified. Data sources are identified by their `dataStreamId`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `fitness_users_data_sources_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `fitness_users_data_sources_update_task()`.
/// For the simplest API, use `fitness_users_data_sources_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `fitness_users_data_sources_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn fitness_users_data_sources_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataSource>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = fitness_users_data_sources_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`fitness_users_data_sources_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FitnessUsersDataSourcesUpdateArgs {
    /// Path parameter: userId
    pub userId: String,
    /// Path parameter: dataSourceId
    pub dataSourceId: String,
}

/// PUT {userId}/dataSources/{dataSourceId}
/// Updates the specified data source. The `dataStreamId`, `dataType`, type, `dataStreamName`, and device properties with the exception of version, cannot be modified. Data sources are identified by their `dataStreamId`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `fitness_users_data_sources_update_builder()` + `fitness_users_data_sources_update_execute()`.
/// For task-level control, use `fitness_users_data_sources_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn fitness_users_data_sources_update(
    client: &SimpleHttpClient,
    args: &FitnessUsersDataSourcesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataSource>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        fitness_users_data_sources_update_builder(client, &args.userId, &args.dataSourceId)?;
    fitness_users_data_sources_update_execute(builder)
}

/// GET {userId}/dataSources/{dataSourceId}/dataPointChanges
/// Queries for user''s data point changes for a particular data source.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `fitness_users_data_sources_data_point_changes_list_execute()` to send, or `fitness_users_data_sources_data_point_changes_list` for simplest API.

pub fn fitness_users_data_sources_data_point_changes_list_builder<R>(
    client: &SimpleHttpClient<R>,
    userId: &String,
    dataSourceId: &String,
    limit: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://fitness.googleapis.com/fitness/v1/users/{}/dataSources/{}/dataPointChanges",
        userId, dataSourceId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = limit.as_ref() {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {userId}/dataSources/{dataSourceId}/dataPointChanges
/// Queries for user''s data point changes for a particular data source.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `fitness_users_data_sources_data_point_changes_list_execute()` or `fitness_users_data_sources_data_point_changes_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `fitness_users_data_sources_data_point_changes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn fitness_users_data_sources_data_point_changes_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDataPointChangesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDataPointChangesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {userId}/dataSources/{dataSourceId}/dataPointChanges
/// Queries for user''s data point changes for a particular data source.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `fitness_users_data_sources_data_point_changes_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `fitness_users_data_sources_data_point_changes_list_task()`.
/// For the simplest API, use `fitness_users_data_sources_data_point_changes_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `fitness_users_data_sources_data_point_changes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn fitness_users_data_sources_data_point_changes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDataPointChangesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = fitness_users_data_sources_data_point_changes_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`fitness_users_data_sources_data_point_changes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FitnessUsersDataSourcesDataPointChangesListArgs {
    /// Path parameter: userId
    pub userId: String,
    /// Path parameter: dataSourceId
    pub dataSourceId: String,
    /// Query parameter: limit
    pub limit: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET {userId}/dataSources/{dataSourceId}/dataPointChanges
/// Queries for user''s data point changes for a particular data source.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `fitness_users_data_sources_data_point_changes_list_builder()` + `fitness_users_data_sources_data_point_changes_list_execute()`.
/// For task-level control, use `fitness_users_data_sources_data_point_changes_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn fitness_users_data_sources_data_point_changes_list(
    client: &SimpleHttpClient,
    args: &FitnessUsersDataSourcesDataPointChangesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListDataPointChangesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = fitness_users_data_sources_data_point_changes_list_builder(
        client,
        &args.userId,
        &args.dataSourceId,
        &args.limit,
        &args.pageToken,
    )?;
    fitness_users_data_sources_data_point_changes_list_execute(builder)
}

/// DELETE {userId}/dataSources/{dataSourceId}/datasets/{datasetId}
/// Performs an inclusive delete of all data points whose start and end times have any overlap with the time range specified by the dataset ID. For most data types, the entire data point will be deleted. For data types where the time span represents a consistent value (such as com.google.activity.segment), and a data point straddles either end point of the dataset, only the overlapping portion of the data point will be deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `fitness_users_data_sources_datasets_delete_execute()` to send, or `fitness_users_data_sources_datasets_delete` for simplest API.

pub fn fitness_users_data_sources_datasets_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    userId: &String,
    dataSourceId: &String,
    datasetId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://fitness.googleapis.com/fitness/v1/users/{}/dataSources/{}/datasets/{}",
        userId, dataSourceId, datasetId,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE {userId}/dataSources/{dataSourceId}/datasets/{datasetId}
/// Performs an inclusive delete of all data points whose start and end times have any overlap with the time range specified by the dataset ID. For most data types, the entire data point will be deleted. For data types where the time span represents a consistent value (such as com.google.activity.segment), and a data point straddles either end point of the dataset, only the overlapping portion of the data point will be deleted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `fitness_users_data_sources_datasets_delete_execute()` or `fitness_users_data_sources_datasets_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `fitness_users_data_sources_datasets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn fitness_users_data_sources_datasets_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE {userId}/dataSources/{dataSourceId}/datasets/{datasetId}
/// Performs an inclusive delete of all data points whose start and end times have any overlap with the time range specified by the dataset ID. For most data types, the entire data point will be deleted. For data types where the time span represents a consistent value (such as com.google.activity.segment), and a data point straddles either end point of the dataset, only the overlapping portion of the data point will be deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `fitness_users_data_sources_datasets_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `fitness_users_data_sources_datasets_delete_task()`.
/// For the simplest API, use `fitness_users_data_sources_datasets_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `fitness_users_data_sources_datasets_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn fitness_users_data_sources_datasets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = fitness_users_data_sources_datasets_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`fitness_users_data_sources_datasets_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FitnessUsersDataSourcesDatasetsDeleteArgs {
    /// Path parameter: userId
    pub userId: String,
    /// Path parameter: dataSourceId
    pub dataSourceId: String,
    /// Path parameter: datasetId
    pub datasetId: String,
}

/// DELETE {userId}/dataSources/{dataSourceId}/datasets/{datasetId}
/// Performs an inclusive delete of all data points whose start and end times have any overlap with the time range specified by the dataset ID. For most data types, the entire data point will be deleted. For data types where the time span represents a consistent value (such as com.google.activity.segment), and a data point straddles either end point of the dataset, only the overlapping portion of the data point will be deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `fitness_users_data_sources_datasets_delete_builder()` + `fitness_users_data_sources_datasets_delete_execute()`.
/// For task-level control, use `fitness_users_data_sources_datasets_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn fitness_users_data_sources_datasets_delete(
    client: &SimpleHttpClient,
    args: &FitnessUsersDataSourcesDatasetsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = fitness_users_data_sources_datasets_delete_builder(
        client,
        &args.userId,
        &args.dataSourceId,
        &args.datasetId,
    )?;
    fitness_users_data_sources_datasets_delete_execute(builder)
}

/// GET {userId}/dataSources/{dataSourceId}/datasets/{datasetId}
/// Returns a dataset containing all data points whose start and end times overlap with the specified range of the dataset minimum start time and maximum end time. Specifically, any data point whose start time is less than or equal to the dataset end time and whose end time is greater than or equal to the dataset start time.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `fitness_users_data_sources_datasets_get_execute()` to send, or `fitness_users_data_sources_datasets_get` for simplest API.

pub fn fitness_users_data_sources_datasets_get_builder<R>(
    client: &SimpleHttpClient<R>,
    userId: &String,
    dataSourceId: &String,
    datasetId: &String,
    limit: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://fitness.googleapis.com/fitness/v1/users/{}/dataSources/{}/datasets/{}",
        userId, dataSourceId, datasetId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = limit.as_ref() {
        query_parts.push(format!("limit={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {userId}/dataSources/{dataSourceId}/datasets/{datasetId}
/// Returns a dataset containing all data points whose start and end times overlap with the specified range of the dataset minimum start time and maximum end time. Specifically, any data point whose start time is less than or equal to the dataset end time and whose end time is greater than or equal to the dataset start time.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `fitness_users_data_sources_datasets_get_execute()` or `fitness_users_data_sources_datasets_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `fitness_users_data_sources_datasets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn fitness_users_data_sources_datasets_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Dataset>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Dataset = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {userId}/dataSources/{dataSourceId}/datasets/{datasetId}
/// Returns a dataset containing all data points whose start and end times overlap with the specified range of the dataset minimum start time and maximum end time. Specifically, any data point whose start time is less than or equal to the dataset end time and whose end time is greater than or equal to the dataset start time.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `fitness_users_data_sources_datasets_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `fitness_users_data_sources_datasets_get_task()`.
/// For the simplest API, use `fitness_users_data_sources_datasets_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `fitness_users_data_sources_datasets_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn fitness_users_data_sources_datasets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Dataset>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = fitness_users_data_sources_datasets_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`fitness_users_data_sources_datasets_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FitnessUsersDataSourcesDatasetsGetArgs {
    /// Path parameter: userId
    pub userId: String,
    /// Path parameter: dataSourceId
    pub dataSourceId: String,
    /// Path parameter: datasetId
    pub datasetId: String,
    /// Query parameter: limit
    pub limit: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET {userId}/dataSources/{dataSourceId}/datasets/{datasetId}
/// Returns a dataset containing all data points whose start and end times overlap with the specified range of the dataset minimum start time and maximum end time. Specifically, any data point whose start time is less than or equal to the dataset end time and whose end time is greater than or equal to the dataset start time.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `fitness_users_data_sources_datasets_get_builder()` + `fitness_users_data_sources_datasets_get_execute()`.
/// For task-level control, use `fitness_users_data_sources_datasets_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn fitness_users_data_sources_datasets_get(
    client: &SimpleHttpClient,
    args: &FitnessUsersDataSourcesDatasetsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Dataset>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = fitness_users_data_sources_datasets_get_builder(
        client,
        &args.userId,
        &args.dataSourceId,
        &args.datasetId,
        &args.limit,
        &args.pageToken,
    )?;
    fitness_users_data_sources_datasets_get_execute(builder)
}

/// PATCH {userId}/dataSources/{dataSourceId}/datasets/{datasetId}
/// Adds data points to a dataset. The dataset need not be previously created. All points within the given dataset will be returned with subsquent calls to retrieve this dataset. Data points can belong to more than one dataset. This method does not use patch semantics: the data points provided are merely inserted, with no existing data replaced.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `fitness_users_data_sources_datasets_patch_execute()` to send, or `fitness_users_data_sources_datasets_patch` for simplest API.

pub fn fitness_users_data_sources_datasets_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    userId: &String,
    dataSourceId: &String,
    datasetId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://fitness.googleapis.com/fitness/v1/users/{}/dataSources/{}/datasets/{}",
        userId, dataSourceId, datasetId,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH {userId}/dataSources/{dataSourceId}/datasets/{datasetId}
/// Adds data points to a dataset. The dataset need not be previously created. All points within the given dataset will be returned with subsquent calls to retrieve this dataset. Data points can belong to more than one dataset. This method does not use patch semantics: the data points provided are merely inserted, with no existing data replaced.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `fitness_users_data_sources_datasets_patch_execute()` or `fitness_users_data_sources_datasets_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `fitness_users_data_sources_datasets_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn fitness_users_data_sources_datasets_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Dataset>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Dataset = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH {userId}/dataSources/{dataSourceId}/datasets/{datasetId}
/// Adds data points to a dataset. The dataset need not be previously created. All points within the given dataset will be returned with subsquent calls to retrieve this dataset. Data points can belong to more than one dataset. This method does not use patch semantics: the data points provided are merely inserted, with no existing data replaced.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `fitness_users_data_sources_datasets_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `fitness_users_data_sources_datasets_patch_task()`.
/// For the simplest API, use `fitness_users_data_sources_datasets_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `fitness_users_data_sources_datasets_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn fitness_users_data_sources_datasets_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Dataset>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = fitness_users_data_sources_datasets_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`fitness_users_data_sources_datasets_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FitnessUsersDataSourcesDatasetsPatchArgs {
    /// Path parameter: userId
    pub userId: String,
    /// Path parameter: dataSourceId
    pub dataSourceId: String,
    /// Path parameter: datasetId
    pub datasetId: String,
}

/// PATCH {userId}/dataSources/{dataSourceId}/datasets/{datasetId}
/// Adds data points to a dataset. The dataset need not be previously created. All points within the given dataset will be returned with subsquent calls to retrieve this dataset. Data points can belong to more than one dataset. This method does not use patch semantics: the data points provided are merely inserted, with no existing data replaced.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `fitness_users_data_sources_datasets_patch_builder()` + `fitness_users_data_sources_datasets_patch_execute()`.
/// For task-level control, use `fitness_users_data_sources_datasets_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn fitness_users_data_sources_datasets_patch(
    client: &SimpleHttpClient,
    args: &FitnessUsersDataSourcesDatasetsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Dataset>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = fitness_users_data_sources_datasets_patch_builder(
        client,
        &args.userId,
        &args.dataSourceId,
        &args.datasetId,
    )?;
    fitness_users_data_sources_datasets_patch_execute(builder)
}

/// POST {userId}/dataset:aggregate
/// Aggregates data of a certain type or stream into buckets divided by a given type of boundary. Multiple data sets of multiple types and from multiple sources can be aggregated into exactly one bucket type per request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `fitness_users_dataset_aggregate_execute()` to send, or `fitness_users_dataset_aggregate` for simplest API.

pub fn fitness_users_dataset_aggregate_builder<R>(
    client: &SimpleHttpClient<R>,
    userId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://fitness.googleapis.com/fitness/v1/users/{}/dataset:aggregate",
        userId,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST {userId}/dataset:aggregate
/// Aggregates data of a certain type or stream into buckets divided by a given type of boundary. Multiple data sets of multiple types and from multiple sources can be aggregated into exactly one bucket type per request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `fitness_users_dataset_aggregate_execute()` or `fitness_users_dataset_aggregate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `fitness_users_dataset_aggregate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn fitness_users_dataset_aggregate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AggregateResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AggregateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST {userId}/dataset:aggregate
/// Aggregates data of a certain type or stream into buckets divided by a given type of boundary. Multiple data sets of multiple types and from multiple sources can be aggregated into exactly one bucket type per request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `fitness_users_dataset_aggregate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `fitness_users_dataset_aggregate_task()`.
/// For the simplest API, use `fitness_users_dataset_aggregate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `fitness_users_dataset_aggregate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn fitness_users_dataset_aggregate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AggregateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = fitness_users_dataset_aggregate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`fitness_users_dataset_aggregate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FitnessUsersDatasetAggregateArgs {
    /// Path parameter: userId
    pub userId: String,
}

/// POST {userId}/dataset:aggregate
/// Aggregates data of a certain type or stream into buckets divided by a given type of boundary. Multiple data sets of multiple types and from multiple sources can be aggregated into exactly one bucket type per request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `fitness_users_dataset_aggregate_builder()` + `fitness_users_dataset_aggregate_execute()`.
/// For task-level control, use `fitness_users_dataset_aggregate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn fitness_users_dataset_aggregate(
    client: &SimpleHttpClient,
    args: &FitnessUsersDatasetAggregateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AggregateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = fitness_users_dataset_aggregate_builder(client, &args.userId)?;
    fitness_users_dataset_aggregate_execute(builder)
}

/// DELETE {userId}/sessions/{sessionId}
/// Deletes a session specified by the given session ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `fitness_users_sessions_delete_execute()` to send, or `fitness_users_sessions_delete` for simplest API.

pub fn fitness_users_sessions_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    userId: &String,
    sessionId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://fitness.googleapis.com/fitness/v1/users/{}/sessions/{}",
        userId, sessionId,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE {userId}/sessions/{sessionId}
/// Deletes a session specified by the given session ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `fitness_users_sessions_delete_execute()` or `fitness_users_sessions_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `fitness_users_sessions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn fitness_users_sessions_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE {userId}/sessions/{sessionId}
/// Deletes a session specified by the given session ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `fitness_users_sessions_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `fitness_users_sessions_delete_task()`.
/// For the simplest API, use `fitness_users_sessions_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `fitness_users_sessions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn fitness_users_sessions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = fitness_users_sessions_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`fitness_users_sessions_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FitnessUsersSessionsDeleteArgs {
    /// Path parameter: userId
    pub userId: String,
    /// Path parameter: sessionId
    pub sessionId: String,
}

/// DELETE {userId}/sessions/{sessionId}
/// Deletes a session specified by the given session ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `fitness_users_sessions_delete_builder()` + `fitness_users_sessions_delete_execute()`.
/// For task-level control, use `fitness_users_sessions_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn fitness_users_sessions_delete(
    client: &SimpleHttpClient,
    args: &FitnessUsersSessionsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = fitness_users_sessions_delete_builder(client, &args.userId, &args.sessionId)?;
    fitness_users_sessions_delete_execute(builder)
}

/// GET {userId}/sessions
/// Lists sessions previously created.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `fitness_users_sessions_list_execute()` to send, or `fitness_users_sessions_list` for simplest API.

pub fn fitness_users_sessions_list_builder<R>(
    client: &SimpleHttpClient<R>,
    userId: &String,
    activityType: &Option<Option<String>>,
    endTime: &Option<Option<String>>,
    includeDeleted: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
    startTime: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://fitness.googleapis.com/fitness/v1/users/{}/sessions",
        userId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = activityType.as_ref() {
        query_parts.push(format!("activityType={}", val));
    }
    if let Some(val) = endTime.as_ref() {
        query_parts.push(format!("endTime={}", val));
    }
    if let Some(val) = includeDeleted.as_ref() {
        query_parts.push(format!("includeDeleted={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = startTime.as_ref() {
        query_parts.push(format!("startTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET {userId}/sessions
/// Lists sessions previously created.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `fitness_users_sessions_list_execute()` or `fitness_users_sessions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `fitness_users_sessions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn fitness_users_sessions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSessionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSessionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET {userId}/sessions
/// Lists sessions previously created.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `fitness_users_sessions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `fitness_users_sessions_list_task()`.
/// For the simplest API, use `fitness_users_sessions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `fitness_users_sessions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn fitness_users_sessions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSessionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = fitness_users_sessions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`fitness_users_sessions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FitnessUsersSessionsListArgs {
    /// Path parameter: userId
    pub userId: String,
    /// Query parameter: activityType
    pub activityType: Option<Option<String>>,
    /// Query parameter: endTime
    pub endTime: Option<Option<String>>,
    /// Query parameter: includeDeleted
    pub includeDeleted: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
    /// Query parameter: startTime
    pub startTime: Option<Option<String>>,
}

/// GET {userId}/sessions
/// Lists sessions previously created.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `fitness_users_sessions_list_builder()` + `fitness_users_sessions_list_execute()`.
/// For task-level control, use `fitness_users_sessions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn fitness_users_sessions_list(
    client: &SimpleHttpClient,
    args: &FitnessUsersSessionsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSessionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = fitness_users_sessions_list_builder(
        client,
        &args.userId,
        &args.activityType,
        &args.endTime,
        &args.includeDeleted,
        &args.pageToken,
        &args.startTime,
    )?;
    fitness_users_sessions_list_execute(builder)
}

/// PUT {userId}/sessions/{sessionId}
/// Updates or insert a given session.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `fitness_users_sessions_update_execute()` to send, or `fitness_users_sessions_update` for simplest API.

pub fn fitness_users_sessions_update_builder<R>(
    client: &SimpleHttpClient<R>,
    userId: &String,
    sessionId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://fitness.googleapis.com/fitness/v1/users/{}/sessions/{}",
        userId, sessionId,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT {userId}/sessions/{sessionId}
/// Updates or insert a given session.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `fitness_users_sessions_update_execute()` or `fitness_users_sessions_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `fitness_users_sessions_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn fitness_users_sessions_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Session>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Session = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT {userId}/sessions/{sessionId}
/// Updates or insert a given session.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `fitness_users_sessions_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `fitness_users_sessions_update_task()`.
/// For the simplest API, use `fitness_users_sessions_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `fitness_users_sessions_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn fitness_users_sessions_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Session>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = fitness_users_sessions_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`fitness_users_sessions_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FitnessUsersSessionsUpdateArgs {
    /// Path parameter: userId
    pub userId: String,
    /// Path parameter: sessionId
    pub sessionId: String,
}

/// PUT {userId}/sessions/{sessionId}
/// Updates or insert a given session.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `fitness_users_sessions_update_builder()` + `fitness_users_sessions_update_execute()`.
/// For task-level control, use `fitness_users_sessions_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn fitness_users_sessions_update(
    client: &SimpleHttpClient,
    args: &FitnessUsersSessionsUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Session>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = fitness_users_sessions_update_builder(client, &args.userId, &args.sessionId)?;
    fitness_users_sessions_update_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for DataSource
// =============================================================================

/// ResourceIdentifier implementation for DataSource with FitnessUsersDataSourcesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FitnessUsersDataSourcesCreateArgs> for DataSource {
    fn generate_resource_id(&self, input: &FitnessUsersDataSourcesCreateArgs) -> String {
        format!("gcp::fitness::DataSource/{}", input.userId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::fitness::DataSource"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DataSource
// =============================================================================

/// ResourceIdentifier implementation for DataSource with FitnessUsersDataSourcesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FitnessUsersDataSourcesDeleteArgs> for DataSource {
    fn generate_resource_id(&self, input: &FitnessUsersDataSourcesDeleteArgs) -> String {
        format!(
            "gcp::fitness::DataSource/{}/{}",
            input.userId, input.dataSourceId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::fitness::DataSource"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DataSource
// =============================================================================

/// ResourceIdentifier implementation for DataSource with FitnessUsersDataSourcesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FitnessUsersDataSourcesGetArgs> for DataSource {
    fn generate_resource_id(&self, input: &FitnessUsersDataSourcesGetArgs) -> String {
        format!(
            "gcp::fitness::DataSource/{}/{}",
            input.userId, input.dataSourceId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::fitness::DataSource"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListDataSourcesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListDataSourcesResponse with FitnessUsersDataSourcesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FitnessUsersDataSourcesListArgs> for ListDataSourcesResponse {
    fn generate_resource_id(&self, input: &FitnessUsersDataSourcesListArgs) -> String {
        format!("gcp::fitness::ListDataSourcesResponse/{}", input.userId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::fitness::ListDataSourcesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DataSource
// =============================================================================

/// ResourceIdentifier implementation for DataSource with FitnessUsersDataSourcesUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FitnessUsersDataSourcesUpdateArgs> for DataSource {
    fn generate_resource_id(&self, input: &FitnessUsersDataSourcesUpdateArgs) -> String {
        format!(
            "gcp::fitness::DataSource/{}/{}",
            input.userId, input.dataSourceId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::fitness::DataSource"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListDataPointChangesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListDataPointChangesResponse with FitnessUsersDataSourcesDataPointChangesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FitnessUsersDataSourcesDataPointChangesListArgs>
    for ListDataPointChangesResponse
{
    fn generate_resource_id(
        &self,
        input: &FitnessUsersDataSourcesDataPointChangesListArgs,
    ) -> String {
        format!(
            "gcp::fitness::ListDataPointChangesResponse/{}/{}",
            input.userId, input.dataSourceId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::fitness::ListDataPointChangesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Dataset
// =============================================================================

/// ResourceIdentifier implementation for Dataset with FitnessUsersDataSourcesDatasetsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FitnessUsersDataSourcesDatasetsGetArgs> for Dataset {
    fn generate_resource_id(&self, input: &FitnessUsersDataSourcesDatasetsGetArgs) -> String {
        format!(
            "gcp::fitness::Dataset/{}/{}/{}",
            input.userId, input.dataSourceId, input.datasetId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::fitness::Dataset"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Dataset
// =============================================================================

/// ResourceIdentifier implementation for Dataset with FitnessUsersDataSourcesDatasetsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FitnessUsersDataSourcesDatasetsPatchArgs> for Dataset {
    fn generate_resource_id(&self, input: &FitnessUsersDataSourcesDatasetsPatchArgs) -> String {
        format!(
            "gcp::fitness::Dataset/{}/{}/{}",
            input.userId, input.dataSourceId, input.datasetId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::fitness::Dataset"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AggregateResponse
// =============================================================================

/// ResourceIdentifier implementation for AggregateResponse with FitnessUsersDatasetAggregateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FitnessUsersDatasetAggregateArgs> for AggregateResponse {
    fn generate_resource_id(&self, input: &FitnessUsersDatasetAggregateArgs) -> String {
        format!("gcp::fitness::AggregateResponse/{}", input.userId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::fitness::AggregateResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListSessionsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListSessionsResponse with FitnessUsersSessionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FitnessUsersSessionsListArgs> for ListSessionsResponse {
    fn generate_resource_id(&self, input: &FitnessUsersSessionsListArgs) -> String {
        format!("gcp::fitness::ListSessionsResponse/{}", input.userId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::fitness::ListSessionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Session
// =============================================================================

/// ResourceIdentifier implementation for Session with FitnessUsersSessionsUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<FitnessUsersSessionsUpdateArgs> for Session {
    fn generate_resource_id(&self, input: &FitnessUsersSessionsUpdateArgs) -> String {
        format!("gcp::fitness::Session/{}/{}", input.userId, input.sessionId)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::fitness::Session"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
