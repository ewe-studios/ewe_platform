//! Auto-generated API clients for Google Cloud Platform - speech.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/operations/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `speech_operations_get_execute()` to send, or `speech_operations_get` for simplest API.

pub fn speech_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/operations/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/operations/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `speech_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from speech_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn speech_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/operations/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `speech_operations_get_builder()` + `speech_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn speech_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = speech_operations_get_builder(client, name)?;
    speech_operations_get_execute(builder)
}

/// GET v1/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `speech_operations_list_execute()` to send, or `speech_operations_list` for simplest API.

pub fn speech_operations_list_builder(
    client: &SimpleHttpClient,
    filter: Option<&str>,
    name: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/operations",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "name",
            name.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnPartialSuccess",
            returnPartialSuccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `speech_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from speech_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn speech_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `speech_operations_list_builder()` + `speech_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn speech_operations_list(
    client: &SimpleHttpClient,
    filter: Option<&str>,
    name: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = speech_operations_list_builder(
        client,
        filter,
        name,
        pageSize,
        pageToken,
        returnPartialSuccess,
    )?;
    speech_operations_list_execute(builder)
}

/// GET v1/{+parent}/customClasses
/// Create a custom class.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `speech_projects_locations_custom_classes_create_execute()` to send, or `speech_projects_locations_custom_classes_create` for simplest API.

pub fn speech_projects_locations_custom_classes_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &CreateCustomClassRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/customClasses", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/customClasses
/// Create a custom class.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `speech_projects_locations_custom_classes_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from speech_projects_locations_custom_classes_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn speech_projects_locations_custom_classes_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomClass = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/customClasses
/// Create a custom class.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `speech_projects_locations_custom_classes_create_builder()` + `speech_projects_locations_custom_classes_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn speech_projects_locations_custom_classes_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &CreateCustomClassRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = speech_projects_locations_custom_classes_create_builder(client, parent, body)?;
    speech_projects_locations_custom_classes_create_execute(builder)
}

/// GET v1/{+name}
/// Delete a custom class.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `speech_projects_locations_custom_classes_delete_execute()` to send, or `speech_projects_locations_custom_classes_delete` for simplest API.

pub fn speech_projects_locations_custom_classes_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Delete a custom class.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `speech_projects_locations_custom_classes_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from speech_projects_locations_custom_classes_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn speech_projects_locations_custom_classes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Delete a custom class.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `speech_projects_locations_custom_classes_delete_builder()` + `speech_projects_locations_custom_classes_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn speech_projects_locations_custom_classes_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = speech_projects_locations_custom_classes_delete_builder(client, name)?;
    speech_projects_locations_custom_classes_delete_execute(builder)
}

/// GET v1/{+name}
/// Get a custom class.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `speech_projects_locations_custom_classes_get_execute()` to send, or `speech_projects_locations_custom_classes_get` for simplest API.

pub fn speech_projects_locations_custom_classes_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get a custom class.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `speech_projects_locations_custom_classes_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from speech_projects_locations_custom_classes_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn speech_projects_locations_custom_classes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomClass = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get a custom class.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `speech_projects_locations_custom_classes_get_builder()` + `speech_projects_locations_custom_classes_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn speech_projects_locations_custom_classes_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = speech_projects_locations_custom_classes_get_builder(client, name)?;
    speech_projects_locations_custom_classes_get_execute(builder)
}

/// GET v1/{+parent}/customClasses
/// List custom classes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `speech_projects_locations_custom_classes_list_execute()` to send, or `speech_projects_locations_custom_classes_list` for simplest API.

pub fn speech_projects_locations_custom_classes_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/customClasses", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/customClasses
/// List custom classes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `speech_projects_locations_custom_classes_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from speech_projects_locations_custom_classes_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn speech_projects_locations_custom_classes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCustomClassesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCustomClassesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/customClasses
/// List custom classes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `speech_projects_locations_custom_classes_list_builder()` + `speech_projects_locations_custom_classes_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn speech_projects_locations_custom_classes_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCustomClassesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        speech_projects_locations_custom_classes_list_builder(client, parent, pageSize, pageToken)?;
    speech_projects_locations_custom_classes_list_execute(builder)
}

/// GET v1/{+name}
/// Update a custom class.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `speech_projects_locations_custom_classes_patch_execute()` to send, or `speech_projects_locations_custom_classes_patch` for simplest API.

pub fn speech_projects_locations_custom_classes_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &CustomClass,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Update a custom class.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `speech_projects_locations_custom_classes_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from speech_projects_locations_custom_classes_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn speech_projects_locations_custom_classes_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomClass = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Update a custom class.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `speech_projects_locations_custom_classes_patch_builder()` + `speech_projects_locations_custom_classes_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn speech_projects_locations_custom_classes_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &CustomClass,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomClass>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        speech_projects_locations_custom_classes_patch_builder(client, name, updateMask, body)?;
    speech_projects_locations_custom_classes_patch_execute(builder)
}

/// GET v1/{+parent}/phraseSets
/// Create a set of phrase hints. Each item in the set can be a single word or a multi-word phrase. The items in the PhraseSet are favored by the recognition model when you send a call that includes the PhraseSet.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `speech_projects_locations_phrase_sets_create_execute()` to send, or `speech_projects_locations_phrase_sets_create` for simplest API.

pub fn speech_projects_locations_phrase_sets_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &CreatePhraseSetRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/phraseSets", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/phraseSets
/// Create a set of phrase hints. Each item in the set can be a single word or a multi-word phrase. The items in the PhraseSet are favored by the recognition model when you send a call that includes the PhraseSet.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `speech_projects_locations_phrase_sets_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from speech_projects_locations_phrase_sets_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn speech_projects_locations_phrase_sets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PhraseSet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PhraseSet = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/phraseSets
/// Create a set of phrase hints. Each item in the set can be a single word or a multi-word phrase. The items in the PhraseSet are favored by the recognition model when you send a call that includes the PhraseSet.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `speech_projects_locations_phrase_sets_create_builder()` + `speech_projects_locations_phrase_sets_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn speech_projects_locations_phrase_sets_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &CreatePhraseSetRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PhraseSet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = speech_projects_locations_phrase_sets_create_builder(client, parent, body)?;
    speech_projects_locations_phrase_sets_create_execute(builder)
}

/// GET v1/{+name}
/// Delete a phrase set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `speech_projects_locations_phrase_sets_delete_execute()` to send, or `speech_projects_locations_phrase_sets_delete` for simplest API.

pub fn speech_projects_locations_phrase_sets_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Delete a phrase set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `speech_projects_locations_phrase_sets_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from speech_projects_locations_phrase_sets_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn speech_projects_locations_phrase_sets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Delete a phrase set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `speech_projects_locations_phrase_sets_delete_builder()` + `speech_projects_locations_phrase_sets_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn speech_projects_locations_phrase_sets_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = speech_projects_locations_phrase_sets_delete_builder(client, name)?;
    speech_projects_locations_phrase_sets_delete_execute(builder)
}

/// GET v1/{+name}
/// Get a phrase set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `speech_projects_locations_phrase_sets_get_execute()` to send, or `speech_projects_locations_phrase_sets_get` for simplest API.

pub fn speech_projects_locations_phrase_sets_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get a phrase set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `speech_projects_locations_phrase_sets_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from speech_projects_locations_phrase_sets_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn speech_projects_locations_phrase_sets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PhraseSet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PhraseSet = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get a phrase set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `speech_projects_locations_phrase_sets_get_builder()` + `speech_projects_locations_phrase_sets_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn speech_projects_locations_phrase_sets_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PhraseSet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = speech_projects_locations_phrase_sets_get_builder(client, name)?;
    speech_projects_locations_phrase_sets_get_execute(builder)
}

/// GET v1/{+parent}/phraseSets
/// List phrase sets.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `speech_projects_locations_phrase_sets_list_execute()` to send, or `speech_projects_locations_phrase_sets_list` for simplest API.

pub fn speech_projects_locations_phrase_sets_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/phraseSets", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/phraseSets
/// List phrase sets.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `speech_projects_locations_phrase_sets_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from speech_projects_locations_phrase_sets_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn speech_projects_locations_phrase_sets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListPhraseSetResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListPhraseSetResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/phraseSets
/// List phrase sets.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `speech_projects_locations_phrase_sets_list_builder()` + `speech_projects_locations_phrase_sets_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn speech_projects_locations_phrase_sets_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListPhraseSetResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        speech_projects_locations_phrase_sets_list_builder(client, parent, pageSize, pageToken)?;
    speech_projects_locations_phrase_sets_list_execute(builder)
}

/// GET v1/{+name}
/// Update a phrase set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `speech_projects_locations_phrase_sets_patch_execute()` to send, or `speech_projects_locations_phrase_sets_patch` for simplest API.

pub fn speech_projects_locations_phrase_sets_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &PhraseSet,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Update a phrase set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `speech_projects_locations_phrase_sets_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from speech_projects_locations_phrase_sets_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn speech_projects_locations_phrase_sets_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PhraseSet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PhraseSet = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Update a phrase set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `speech_projects_locations_phrase_sets_patch_builder()` + `speech_projects_locations_phrase_sets_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn speech_projects_locations_phrase_sets_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &PhraseSet,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PhraseSet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        speech_projects_locations_phrase_sets_patch_builder(client, name, updateMask, body)?;
    speech_projects_locations_phrase_sets_patch_execute(builder)
}

/// GET v1/speech:longrunningrecognize
/// Performs asynchronous speech recognition: receive results via the google.longrunning.Operations interface. Returns either an Operation.error or an Operation.response which contains a LongRunningRecognizeResponse message. For more information on asynchronous speech recognition, see the [how-to](<https://cloud.google.`com/speech-to-text/docs/async-recognize`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `speech_speech_longrunningrecognize_execute()` to send, or `speech_speech_longrunningrecognize` for simplest API.

pub fn speech_speech_longrunningrecognize_builder(
    client: &SimpleHttpClient,
    body: &LongRunningRecognizeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/speech:longrunningrecognize",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/speech:longrunningrecognize
/// Performs asynchronous speech recognition: receive results via the google.longrunning.Operations interface. Returns either an Operation.error or an Operation.response which contains a LongRunningRecognizeResponse message. For more information on asynchronous speech recognition, see the [how-to](<https://cloud.google.`com/speech-to-text/docs/async-recognize`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `speech_speech_longrunningrecognize_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from speech_speech_longrunningrecognize_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn speech_speech_longrunningrecognize_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/speech:longrunningrecognize
/// Performs asynchronous speech recognition: receive results via the google.longrunning.Operations interface. Returns either an Operation.error or an Operation.response which contains a LongRunningRecognizeResponse message. For more information on asynchronous speech recognition, see the [how-to](<https://cloud.google.`com/speech-to-text/docs/async-recognize`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `speech_speech_longrunningrecognize_builder()` + `speech_speech_longrunningrecognize_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn speech_speech_longrunningrecognize(
    client: &SimpleHttpClient,
    body: &LongRunningRecognizeRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = speech_speech_longrunningrecognize_builder(client, body)?;
    speech_speech_longrunningrecognize_execute(builder)
}

/// GET v1/speech:recognize
/// Performs synchronous speech recognition: receive results after all audio has been sent and processed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `speech_speech_recognize_execute()` to send, or `speech_speech_recognize` for simplest API.

pub fn speech_speech_recognize_builder(
    client: &SimpleHttpClient,
    body: &RecognizeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/speech:recognize",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/speech:recognize
/// Performs synchronous speech recognition: receive results after all audio has been sent and processed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `speech_speech_recognize_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from speech_speech_recognize_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn speech_speech_recognize_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RecognizeResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RecognizeResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/speech:recognize
/// Performs synchronous speech recognition: receive results after all audio has been sent and processed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `speech_speech_recognize_builder()` + `speech_speech_recognize_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn speech_speech_recognize(
    client: &SimpleHttpClient,
    body: &RecognizeRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RecognizeResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = speech_speech_recognize_builder(client, body)?;
    speech_speech_recognize_execute(builder)
}
