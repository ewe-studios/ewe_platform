//! Auto-generated API clients for gcp/doubleclicksearch.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET doubleclicksearch/v2/conversion
/// Inserts a batch of new conversions into DoubleClick Search.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `doubleclicksearch_conversion_insert_execute()` to send, or `doubleclicksearch_conversion_insert` for simplest API.

pub fn doubleclicksearch_conversion_insert_builder(
    client: &SimpleHttpClient,
    body: &ConversionList,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://doubleclicksearch.googleapis.com/doubleclicksearch/v2/conversion",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET doubleclicksearch/v2/conversion
/// Inserts a batch of new conversions into DoubleClick Search.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `doubleclicksearch_conversion_insert_execute()` or `doubleclicksearch_conversion_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `doubleclicksearch_conversion_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_conversion_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ConversionList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ConversionList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET doubleclicksearch/v2/conversion
/// Inserts a batch of new conversions into DoubleClick Search.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `doubleclicksearch_conversion_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `doubleclicksearch_conversion_insert_task()`.
/// For the simplest API, use `doubleclicksearch_conversion_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `doubleclicksearch_conversion_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn doubleclicksearch_conversion_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConversionList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = doubleclicksearch_conversion_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`doubleclicksearch_conversion_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DoubleclicksearchConversionInsertArgs {
    /// Request body.
    pub body: ConversionList,
}

/// GET doubleclicksearch/v2/conversion
/// Inserts a batch of new conversions into DoubleClick Search.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `doubleclicksearch_conversion_insert_builder()` + `doubleclicksearch_conversion_insert_execute()`.
/// For task-level control, use `doubleclicksearch_conversion_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_conversion_insert(
    client: &SimpleHttpClient,
    args: &DoubleclicksearchConversionInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConversionList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = doubleclicksearch_conversion_insert_builder(client, &args.body)?;
    doubleclicksearch_conversion_insert_execute(builder)
}

/// GET doubleclicksearch/v2/conversion/updateAvailability
/// Updates the availabilities of a batch of floodlight activities in DoubleClick Search.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `doubleclicksearch_conversion_update_availability_execute()` to send, or `doubleclicksearch_conversion_update_availability` for simplest API.

pub fn doubleclicksearch_conversion_update_availability_builder(
    client: &SimpleHttpClient,
    body: &UpdateAvailabilityRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://doubleclicksearch.googleapis.com/doubleclicksearch/v2/conversion/updateAvailability",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET doubleclicksearch/v2/conversion/updateAvailability
/// Updates the availabilities of a batch of floodlight activities in DoubleClick Search.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `doubleclicksearch_conversion_update_availability_execute()` or `doubleclicksearch_conversion_update_availability`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `doubleclicksearch_conversion_update_availability_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_conversion_update_availability_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UpdateAvailabilityResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UpdateAvailabilityResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET doubleclicksearch/v2/conversion/updateAvailability
/// Updates the availabilities of a batch of floodlight activities in DoubleClick Search.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `doubleclicksearch_conversion_update_availability_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `doubleclicksearch_conversion_update_availability_task()`.
/// For the simplest API, use `doubleclicksearch_conversion_update_availability()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `doubleclicksearch_conversion_update_availability_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn doubleclicksearch_conversion_update_availability_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UpdateAvailabilityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = doubleclicksearch_conversion_update_availability_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`doubleclicksearch_conversion_update_availability`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DoubleclicksearchConversionUpdateAvailabilityArgs {
    /// Request body.
    pub body: UpdateAvailabilityRequest,
}

/// GET doubleclicksearch/v2/conversion/updateAvailability
/// Updates the availabilities of a batch of floodlight activities in DoubleClick Search.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `doubleclicksearch_conversion_update_availability_builder()` + `doubleclicksearch_conversion_update_availability_execute()`.
/// For task-level control, use `doubleclicksearch_conversion_update_availability_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_conversion_update_availability(
    client: &SimpleHttpClient,
    args: &DoubleclicksearchConversionUpdateAvailabilityArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UpdateAvailabilityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = doubleclicksearch_conversion_update_availability_builder(client, &args.body)?;
    doubleclicksearch_conversion_update_availability_execute(builder)
}

/// GET doubleclicksearch/v2/reports/generate
/// Generates and returns a report immediately.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `doubleclicksearch_reports_generate_execute()` to send, or `doubleclicksearch_reports_generate` for simplest API.

pub fn doubleclicksearch_reports_generate_builder(
    client: &SimpleHttpClient,
    body: &ReportRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://doubleclicksearch.googleapis.com/doubleclicksearch/v2/reports/generate",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET doubleclicksearch/v2/reports/generate
/// Generates and returns a report immediately.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `doubleclicksearch_reports_generate_execute()` or `doubleclicksearch_reports_generate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `doubleclicksearch_reports_generate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_reports_generate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Report>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Report = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET doubleclicksearch/v2/reports/generate
/// Generates and returns a report immediately.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `doubleclicksearch_reports_generate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `doubleclicksearch_reports_generate_task()`.
/// For the simplest API, use `doubleclicksearch_reports_generate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `doubleclicksearch_reports_generate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn doubleclicksearch_reports_generate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Report>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = doubleclicksearch_reports_generate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`doubleclicksearch_reports_generate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DoubleclicksearchReportsGenerateArgs {
    /// Request body.
    pub body: ReportRequest,
}

/// GET doubleclicksearch/v2/reports/generate
/// Generates and returns a report immediately.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `doubleclicksearch_reports_generate_builder()` + `doubleclicksearch_reports_generate_execute()`.
/// For task-level control, use `doubleclicksearch_reports_generate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_reports_generate(
    client: &SimpleHttpClient,
    args: &DoubleclicksearchReportsGenerateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Report>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = doubleclicksearch_reports_generate_builder(client, &args.body)?;
    doubleclicksearch_reports_generate_execute(builder)
}

/// GET doubleclicksearch/v2/reports/{reportId}
/// Polls for the status of a report request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `doubleclicksearch_reports_get_execute()` to send, or `doubleclicksearch_reports_get` for simplest API.

pub fn doubleclicksearch_reports_get_builder(
    client: &SimpleHttpClient,
    reportId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://doubleclicksearch.googleapis.com/doubleclicksearch/v2/reports/{}",
        reportId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET doubleclicksearch/v2/reports/{reportId}
/// Polls for the status of a report request.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `doubleclicksearch_reports_get_execute()` or `doubleclicksearch_reports_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `doubleclicksearch_reports_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_reports_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Report>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Report = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET doubleclicksearch/v2/reports/{reportId}
/// Polls for the status of a report request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `doubleclicksearch_reports_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `doubleclicksearch_reports_get_task()`.
/// For the simplest API, use `doubleclicksearch_reports_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `doubleclicksearch_reports_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn doubleclicksearch_reports_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Report>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = doubleclicksearch_reports_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`doubleclicksearch_reports_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DoubleclicksearchReportsGetArgs {
    /// Path parameter: reportId
    pub reportId: String,
}

/// GET doubleclicksearch/v2/reports/{reportId}
/// Polls for the status of a report request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `doubleclicksearch_reports_get_builder()` + `doubleclicksearch_reports_get_execute()`.
/// For task-level control, use `doubleclicksearch_reports_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_reports_get(
    client: &SimpleHttpClient,
    args: &DoubleclicksearchReportsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Report>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = doubleclicksearch_reports_get_builder(client, &args.reportId)?;
    doubleclicksearch_reports_get_execute(builder)
}

/// GET doubleclicksearch/v2/reports/{reportId}/files/{reportFragment}
/// Downloads a report file encoded in UTF-8.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `doubleclicksearch_reports_get_file_execute()` to send, or `doubleclicksearch_reports_get_file` for simplest API.

pub fn doubleclicksearch_reports_get_file_builder(
    client: &SimpleHttpClient,
    reportId: &String,
    reportFragment: &i32,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://doubleclicksearch.googleapis.com/doubleclicksearch/v2/reports/{}/files/{}",
        reportId, reportFragment,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET doubleclicksearch/v2/reports/{reportId}/files/{reportFragment}
/// Downloads a report file encoded in UTF-8.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `doubleclicksearch_reports_get_file_execute()` or `doubleclicksearch_reports_get_file`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `doubleclicksearch_reports_get_file_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_reports_get_file_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET doubleclicksearch/v2/reports/{reportId}/files/{reportFragment}
/// Downloads a report file encoded in UTF-8.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `doubleclicksearch_reports_get_file_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `doubleclicksearch_reports_get_file_task()`.
/// For the simplest API, use `doubleclicksearch_reports_get_file()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `doubleclicksearch_reports_get_file_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn doubleclicksearch_reports_get_file_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = doubleclicksearch_reports_get_file_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`doubleclicksearch_reports_get_file`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DoubleclicksearchReportsGetFileArgs {
    /// Path parameter: reportId
    pub reportId: String,
    /// Path parameter: reportFragment
    pub reportFragment: i32,
}

/// GET doubleclicksearch/v2/reports/{reportId}/files/{reportFragment}
/// Downloads a report file encoded in UTF-8.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `doubleclicksearch_reports_get_file_builder()` + `doubleclicksearch_reports_get_file_execute()`.
/// For task-level control, use `doubleclicksearch_reports_get_file_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_reports_get_file(
    client: &SimpleHttpClient,
    args: &DoubleclicksearchReportsGetFileArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        doubleclicksearch_reports_get_file_builder(client, &args.reportId, &args.reportFragment)?;
    doubleclicksearch_reports_get_file_execute(builder)
}

/// GET doubleclicksearch/v2/agency/{agencyId}/advertiser/{advertiserId}/idmapping
/// Downloads a csv file(encoded in UTF-8) that contains ID mappings between legacy SA360 and new SA360. The file includes all children entities of the given advertiser(e.g. engine accounts, campaigns, ad groups, etc.) that exist in both legacy SA360 and new SA360.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `doubleclicksearch_reports_get_id_mapping_file_execute()` to send, or `doubleclicksearch_reports_get_id_mapping_file` for simplest API.

pub fn doubleclicksearch_reports_get_id_mapping_file_builder(
    client: &SimpleHttpClient,
    agencyId: &String,
    advertiserId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://doubleclicksearch.googleapis.com/doubleclicksearch/v2/agency/{}/advertiser/{}/idmapping",
        agencyId,
        advertiserId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET doubleclicksearch/v2/agency/{agencyId}/advertiser/{advertiserId}/idmapping
/// Downloads a csv file(encoded in UTF-8) that contains ID mappings between legacy SA360 and new SA360. The file includes all children entities of the given advertiser(e.g. engine accounts, campaigns, ad groups, etc.) that exist in both legacy SA360 and new SA360.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `doubleclicksearch_reports_get_id_mapping_file_execute()` or `doubleclicksearch_reports_get_id_mapping_file`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `doubleclicksearch_reports_get_id_mapping_file_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_reports_get_id_mapping_file_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<IdMappingFile>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IdMappingFile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET doubleclicksearch/v2/agency/{agencyId}/advertiser/{advertiserId}/idmapping
/// Downloads a csv file(encoded in UTF-8) that contains ID mappings between legacy SA360 and new SA360. The file includes all children entities of the given advertiser(e.g. engine accounts, campaigns, ad groups, etc.) that exist in both legacy SA360 and new SA360.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `doubleclicksearch_reports_get_id_mapping_file_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `doubleclicksearch_reports_get_id_mapping_file_task()`.
/// For the simplest API, use `doubleclicksearch_reports_get_id_mapping_file()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `doubleclicksearch_reports_get_id_mapping_file_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn doubleclicksearch_reports_get_id_mapping_file_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IdMappingFile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = doubleclicksearch_reports_get_id_mapping_file_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`doubleclicksearch_reports_get_id_mapping_file`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DoubleclicksearchReportsGetIdMappingFileArgs {
    /// Path parameter: agencyId
    pub agencyId: String,
    /// Path parameter: advertiserId
    pub advertiserId: String,
}

/// GET doubleclicksearch/v2/agency/{agencyId}/advertiser/{advertiserId}/idmapping
/// Downloads a csv file(encoded in UTF-8) that contains ID mappings between legacy SA360 and new SA360. The file includes all children entities of the given advertiser(e.g. engine accounts, campaigns, ad groups, etc.) that exist in both legacy SA360 and new SA360.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `doubleclicksearch_reports_get_id_mapping_file_builder()` + `doubleclicksearch_reports_get_id_mapping_file_execute()`.
/// For task-level control, use `doubleclicksearch_reports_get_id_mapping_file_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_reports_get_id_mapping_file(
    client: &SimpleHttpClient,
    args: &DoubleclicksearchReportsGetIdMappingFileArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IdMappingFile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = doubleclicksearch_reports_get_id_mapping_file_builder(
        client,
        &args.agencyId,
        &args.advertiserId,
    )?;
    doubleclicksearch_reports_get_id_mapping_file_execute(builder)
}

/// GET doubleclicksearch/v2/reports
/// Inserts a report request into the reporting system.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `doubleclicksearch_reports_request_execute()` to send, or `doubleclicksearch_reports_request` for simplest API.

pub fn doubleclicksearch_reports_request_builder(
    client: &SimpleHttpClient,
    body: &ReportRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://doubleclicksearch.googleapis.com/doubleclicksearch/v2/reports",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET doubleclicksearch/v2/reports
/// Inserts a report request into the reporting system.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `doubleclicksearch_reports_request_execute()` or `doubleclicksearch_reports_request`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `doubleclicksearch_reports_request_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_reports_request_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Report>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Report = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET doubleclicksearch/v2/reports
/// Inserts a report request into the reporting system.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `doubleclicksearch_reports_request_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `doubleclicksearch_reports_request_task()`.
/// For the simplest API, use `doubleclicksearch_reports_request()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `doubleclicksearch_reports_request_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn doubleclicksearch_reports_request_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Report>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = doubleclicksearch_reports_request_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`doubleclicksearch_reports_request`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DoubleclicksearchReportsRequestArgs {
    /// Request body.
    pub body: ReportRequest,
}

/// GET doubleclicksearch/v2/reports
/// Inserts a report request into the reporting system.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `doubleclicksearch_reports_request_builder()` + `doubleclicksearch_reports_request_execute()`.
/// For task-level control, use `doubleclicksearch_reports_request_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_reports_request(
    client: &SimpleHttpClient,
    args: &DoubleclicksearchReportsRequestArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Report>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = doubleclicksearch_reports_request_builder(client, &args.body)?;
    doubleclicksearch_reports_request_execute(builder)
}

/// GET doubleclicksearch/v2/agency/{agencyId}/advertiser/{advertiserId}/savedcolumns
/// Retrieve the list of saved columns for a specified advertiser.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `doubleclicksearch_saved_columns_list_execute()` to send, or `doubleclicksearch_saved_columns_list` for simplest API.

pub fn doubleclicksearch_saved_columns_list_builder(
    client: &SimpleHttpClient,
    agencyId: &String,
    advertiserId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://doubleclicksearch.googleapis.com/doubleclicksearch/v2/agency/{}/advertiser/{}/savedcolumns",
        agencyId,
        advertiserId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET doubleclicksearch/v2/agency/{agencyId}/advertiser/{advertiserId}/savedcolumns
/// Retrieve the list of saved columns for a specified advertiser.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `doubleclicksearch_saved_columns_list_execute()` or `doubleclicksearch_saved_columns_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `doubleclicksearch_saved_columns_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_saved_columns_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SavedColumnList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SavedColumnList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET doubleclicksearch/v2/agency/{agencyId}/advertiser/{advertiserId}/savedcolumns
/// Retrieve the list of saved columns for a specified advertiser.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `doubleclicksearch_saved_columns_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `doubleclicksearch_saved_columns_list_task()`.
/// For the simplest API, use `doubleclicksearch_saved_columns_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `doubleclicksearch_saved_columns_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn doubleclicksearch_saved_columns_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedColumnList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = doubleclicksearch_saved_columns_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`doubleclicksearch_saved_columns_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DoubleclicksearchSavedColumnsListArgs {
    /// Path parameter: agencyId
    pub agencyId: String,
    /// Path parameter: advertiserId
    pub advertiserId: String,
}

/// GET doubleclicksearch/v2/agency/{agencyId}/advertiser/{advertiserId}/savedcolumns
/// Retrieve the list of saved columns for a specified advertiser.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `doubleclicksearch_saved_columns_list_builder()` + `doubleclicksearch_saved_columns_list_execute()`.
/// For task-level control, use `doubleclicksearch_saved_columns_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_saved_columns_list(
    client: &SimpleHttpClient,
    args: &DoubleclicksearchSavedColumnsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedColumnList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        doubleclicksearch_saved_columns_list_builder(client, &args.agencyId, &args.advertiserId)?;
    doubleclicksearch_saved_columns_list_execute(builder)
}
