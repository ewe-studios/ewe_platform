//! Auto-generated API clients for gcp/doubleclicksearch.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET doubleclicksearch/v2/agency/{agencyId}/advertiser/{advertiserId}/engine/{engineAccountId}/conversion
/// Retrieves a list of conversions from a DoubleClick Search engine account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `doubleclicksearch_conversion_get_execute()` to send, or `doubleclicksearch_conversion_get` for simplest API.

pub fn doubleclicksearch_conversion_get_builder(
    client: &SimpleHttpClient,
    advertiserId: &str,
    agencyId: &str,
    engineAccountId: &str,
    adGroupId: Option<&str>,
    adId: Option<&str>,
    campaignId: Option<&str>,
    criterionId: Option<&str>,
    customerId: Option<&str>,
    endDate: Option<i32>,
    rowCount: Option<i32>,
    startDate: Option<i32>,
    startRow: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comdoubleclicksearch/v2/agency/{}/advertiser/{}/engine/{}/conversion",
        advertiserId, agencyId, engineAccountId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "adGroupId",
            adGroupId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "adId",
            adId.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "campaignId",
            campaignId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "criterionId",
            criterionId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "customerId",
            customerId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "endDate",
            endDate
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "rowCount",
            rowCount
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "startDate",
            startDate
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "startRow",
            startRow
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET doubleclicksearch/v2/agency/{agencyId}/advertiser/{advertiserId}/engine/{engineAccountId}/conversion
/// Retrieves a list of conversions from a DoubleClick Search engine account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `doubleclicksearch_conversion_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from doubleclicksearch_conversion_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_conversion_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConversionList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ConversionList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET doubleclicksearch/v2/agency/{agencyId}/advertiser/{advertiserId}/engine/{engineAccountId}/conversion
/// Retrieves a list of conversions from a DoubleClick Search engine account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `doubleclicksearch_conversion_get_builder()` + `doubleclicksearch_conversion_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_conversion_get(
    client: &SimpleHttpClient,
    advertiserId: &str,
    agencyId: &str,
    engineAccountId: &str,
    adGroupId: Option<&str>,
    adId: Option<&str>,
    campaignId: Option<&str>,
    criterionId: Option<&str>,
    customerId: Option<&str>,
    endDate: Option<i32>,
    rowCount: Option<i32>,
    startDate: Option<i32>,
    startRow: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConversionList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = doubleclicksearch_conversion_get_builder(
        client,
        advertiserId,
        agencyId,
        engineAccountId,
        adGroupId,
        adId,
        campaignId,
        criterionId,
        customerId,
        endDate,
        rowCount,
        startDate,
        startRow,
    )?;
    doubleclicksearch_conversion_get_execute(builder)
}

/// GET doubleclicksearch/v2/customer/{customerId}/conversion
/// Retrieves a list of conversions from a DoubleClick Search engine account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `doubleclicksearch_conversion_get_by_customer_id_execute()` to send, or `doubleclicksearch_conversion_get_by_customer_id` for simplest API.

pub fn doubleclicksearch_conversion_get_by_customer_id_builder(
    client: &SimpleHttpClient,
    customerId: &str,
    adGroupId: Option<&str>,
    adId: Option<&str>,
    advertiserId: Option<&str>,
    agencyId: Option<&str>,
    campaignId: Option<&str>,
    criterionId: Option<&str>,
    endDate: Option<i32>,
    engineAccountId: Option<&str>,
    rowCount: Option<i32>,
    startDate: Option<i32>,
    startRow: Option<i32>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comdoubleclicksearch/v2/customer/{}/conversion",
        customerId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "adGroupId",
            adGroupId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "adId",
            adId.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "advertiserId",
            advertiserId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "agencyId",
            agencyId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "campaignId",
            campaignId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "criterionId",
            criterionId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "endDate",
            endDate
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "engineAccountId",
            engineAccountId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "rowCount",
            rowCount
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "startDate",
            startDate
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "startRow",
            startRow
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET doubleclicksearch/v2/customer/{customerId}/conversion
/// Retrieves a list of conversions from a DoubleClick Search engine account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `doubleclicksearch_conversion_get_by_customer_id_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from doubleclicksearch_conversion_get_by_customer_id_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_conversion_get_by_customer_id_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConversionList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ConversionList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET doubleclicksearch/v2/customer/{customerId}/conversion
/// Retrieves a list of conversions from a DoubleClick Search engine account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `doubleclicksearch_conversion_get_by_customer_id_builder()` + `doubleclicksearch_conversion_get_by_customer_id_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_conversion_get_by_customer_id(
    client: &SimpleHttpClient,
    customerId: &str,
    adGroupId: Option<&str>,
    adId: Option<&str>,
    advertiserId: Option<&str>,
    agencyId: Option<&str>,
    campaignId: Option<&str>,
    criterionId: Option<&str>,
    endDate: Option<i32>,
    engineAccountId: Option<&str>,
    rowCount: Option<i32>,
    startDate: Option<i32>,
    startRow: Option<i32>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConversionList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = doubleclicksearch_conversion_get_by_customer_id_builder(
        client,
        customerId,
        adGroupId,
        adId,
        advertiserId,
        agencyId,
        campaignId,
        criterionId,
        endDate,
        engineAccountId,
        rowCount,
        startDate,
        startRow,
    )?;
    doubleclicksearch_conversion_get_by_customer_id_execute(builder)
}

/// GET doubleclicksearch/v2/conversion
/// Inserts a batch of new conversions into DoubleClick Search.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `doubleclicksearch_conversion_insert_execute()` to send, or `doubleclicksearch_conversion_insert` for simplest API.

pub fn doubleclicksearch_conversion_insert_builder(
    client: &SimpleHttpClient,
    body: &ConversionList,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comdoubleclicksearch/v2/conversion",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET doubleclicksearch/v2/conversion
/// Inserts a batch of new conversions into DoubleClick Search.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `doubleclicksearch_conversion_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from doubleclicksearch_conversion_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_conversion_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConversionList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ConversionList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET doubleclicksearch/v2/conversion
/// Inserts a batch of new conversions into DoubleClick Search.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `doubleclicksearch_conversion_insert_builder()` + `doubleclicksearch_conversion_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_conversion_insert(
    client: &SimpleHttpClient,
    body: &ConversionList,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConversionList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = doubleclicksearch_conversion_insert_builder(client, body)?;
    doubleclicksearch_conversion_insert_execute(builder)
}

/// GET doubleclicksearch/v2/conversion
/// Updates a batch of conversions in DoubleClick Search.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `doubleclicksearch_conversion_update_execute()` to send, or `doubleclicksearch_conversion_update` for simplest API.

pub fn doubleclicksearch_conversion_update_builder(
    client: &SimpleHttpClient,
    body: &ConversionList,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comdoubleclicksearch/v2/conversion",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET doubleclicksearch/v2/conversion
/// Updates a batch of conversions in DoubleClick Search.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `doubleclicksearch_conversion_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from doubleclicksearch_conversion_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_conversion_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConversionList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ConversionList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET doubleclicksearch/v2/conversion
/// Updates a batch of conversions in DoubleClick Search.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `doubleclicksearch_conversion_update_builder()` + `doubleclicksearch_conversion_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_conversion_update(
    client: &SimpleHttpClient,
    body: &ConversionList,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConversionList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = doubleclicksearch_conversion_update_builder(client, body)?;
    doubleclicksearch_conversion_update_execute(builder)
}

/// GET doubleclicksearch/v2/conversion/updateAvailability
/// Updates the availabilities of a batch of floodlight activities in DoubleClick Search.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `doubleclicksearch_conversion_update_availability_execute()` to send, or `doubleclicksearch_conversion_update_availability` for simplest API.

pub fn doubleclicksearch_conversion_update_availability_builder(
    client: &SimpleHttpClient,
    body: &UpdateAvailabilityRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comdoubleclicksearch/v2/conversion/updateAvailability",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET doubleclicksearch/v2/conversion/updateAvailability
/// Updates the availabilities of a batch of floodlight activities in DoubleClick Search.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `doubleclicksearch_conversion_update_availability_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from doubleclicksearch_conversion_update_availability_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_conversion_update_availability_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UpdateAvailabilityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UpdateAvailabilityResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET doubleclicksearch/v2/conversion/updateAvailability
/// Updates the availabilities of a batch of floodlight activities in DoubleClick Search.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `doubleclicksearch_conversion_update_availability_builder()` + `doubleclicksearch_conversion_update_availability_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_conversion_update_availability(
    client: &SimpleHttpClient,
    body: &UpdateAvailabilityRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<UpdateAvailabilityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = doubleclicksearch_conversion_update_availability_builder(client, body)?;
    doubleclicksearch_conversion_update_availability_execute(builder)
}

/// GET doubleclicksearch/v2/reports/generate
/// Generates and returns a report immediately.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `doubleclicksearch_reports_generate_execute()` to send, or `doubleclicksearch_reports_generate` for simplest API.

pub fn doubleclicksearch_reports_generate_builder(
    client: &SimpleHttpClient,
    body: &ReportRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comdoubleclicksearch/v2/reports/generate",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET doubleclicksearch/v2/reports/generate
/// Generates and returns a report immediately.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `doubleclicksearch_reports_generate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from doubleclicksearch_reports_generate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_reports_generate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Report>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Report = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET doubleclicksearch/v2/reports/generate
/// Generates and returns a report immediately.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `doubleclicksearch_reports_generate_builder()` + `doubleclicksearch_reports_generate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_reports_generate(
    client: &SimpleHttpClient,
    body: &ReportRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Report>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = doubleclicksearch_reports_generate_builder(client, body)?;
    doubleclicksearch_reports_generate_execute(builder)
}

/// GET doubleclicksearch/v2/reports/{reportId}
/// Polls for the status of a report request.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `doubleclicksearch_reports_get_execute()` to send, or `doubleclicksearch_reports_get` for simplest API.

pub fn doubleclicksearch_reports_get_builder(
    client: &SimpleHttpClient,
    reportId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comdoubleclicksearch/v2/reports/{}",
        reportId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET doubleclicksearch/v2/reports/{reportId}
/// Polls for the status of a report request.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `doubleclicksearch_reports_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from doubleclicksearch_reports_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_reports_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Report>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Report = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET doubleclicksearch/v2/reports/{reportId}
/// Polls for the status of a report request.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `doubleclicksearch_reports_get_builder()` + `doubleclicksearch_reports_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_reports_get(
    client: &SimpleHttpClient,
    reportId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Report>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = doubleclicksearch_reports_get_builder(client, reportId)?;
    doubleclicksearch_reports_get_execute(builder)
}

/// GET doubleclicksearch/v2/reports/{reportId}/files/{reportFragment}
/// Downloads a report file encoded in UTF-8.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `doubleclicksearch_reports_get_file_execute()` to send, or `doubleclicksearch_reports_get_file` for simplest API.

pub fn doubleclicksearch_reports_get_file_builder(
    client: &SimpleHttpClient,
    reportFragment: &str,
    reportId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comdoubleclicksearch/v2/reports/{}/files/{}",
        reportFragment, reportId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET doubleclicksearch/v2/reports/{reportId}/files/{reportFragment}
/// Downloads a report file encoded in UTF-8.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `doubleclicksearch_reports_get_file_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from doubleclicksearch_reports_get_file_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_reports_get_file_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET doubleclicksearch/v2/reports/{reportId}/files/{reportFragment}
/// Downloads a report file encoded in UTF-8.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `doubleclicksearch_reports_get_file_builder()` + `doubleclicksearch_reports_get_file_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_reports_get_file(
    client: &SimpleHttpClient,
    reportFragment: &str,
    reportId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = doubleclicksearch_reports_get_file_builder(client, reportFragment, reportId)?;
    doubleclicksearch_reports_get_file_execute(builder)
}

/// GET doubleclicksearch/v2/agency/{agencyId}/advertiser/{advertiserId}/idmapping
/// Downloads a csv file(encoded in UTF-8) that contains ID mappings between legacy SA360 and new SA360. The file includes all children entities of the given advertiser(e.g. engine accounts, campaigns, ad groups, etc.) that exist in both legacy SA360 and new SA360.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `doubleclicksearch_reports_get_id_mapping_file_execute()` to send, or `doubleclicksearch_reports_get_id_mapping_file` for simplest API.

pub fn doubleclicksearch_reports_get_id_mapping_file_builder(
    client: &SimpleHttpClient,
    advertiserId: &str,
    agencyId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comdoubleclicksearch/v2/agency/{}/advertiser/{}/idmapping",
        advertiserId, agencyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET doubleclicksearch/v2/agency/{agencyId}/advertiser/{advertiserId}/idmapping
/// Downloads a csv file(encoded in UTF-8) that contains ID mappings between legacy SA360 and new SA360. The file includes all children entities of the given advertiser(e.g. engine accounts, campaigns, ad groups, etc.) that exist in both legacy SA360 and new SA360.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `doubleclicksearch_reports_get_id_mapping_file_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from doubleclicksearch_reports_get_id_mapping_file_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_reports_get_id_mapping_file_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IdMappingFile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IdMappingFile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET doubleclicksearch/v2/agency/{agencyId}/advertiser/{advertiserId}/idmapping
/// Downloads a csv file(encoded in UTF-8) that contains ID mappings between legacy SA360 and new SA360. The file includes all children entities of the given advertiser(e.g. engine accounts, campaigns, ad groups, etc.) that exist in both legacy SA360 and new SA360.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `doubleclicksearch_reports_get_id_mapping_file_builder()` + `doubleclicksearch_reports_get_id_mapping_file_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_reports_get_id_mapping_file(
    client: &SimpleHttpClient,
    advertiserId: &str,
    agencyId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IdMappingFile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        doubleclicksearch_reports_get_id_mapping_file_builder(client, advertiserId, agencyId)?;
    doubleclicksearch_reports_get_id_mapping_file_execute(builder)
}

/// GET doubleclicksearch/v2/reports
/// Inserts a report request into the reporting system.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `doubleclicksearch_reports_request_execute()` to send, or `doubleclicksearch_reports_request` for simplest API.

pub fn doubleclicksearch_reports_request_builder(
    client: &SimpleHttpClient,
    body: &ReportRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comdoubleclicksearch/v2/reports",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET doubleclicksearch/v2/reports
/// Inserts a report request into the reporting system.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `doubleclicksearch_reports_request_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from doubleclicksearch_reports_request_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_reports_request_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Report>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Report = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET doubleclicksearch/v2/reports
/// Inserts a report request into the reporting system.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `doubleclicksearch_reports_request_builder()` + `doubleclicksearch_reports_request_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_reports_request(
    client: &SimpleHttpClient,
    body: &ReportRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Report>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = doubleclicksearch_reports_request_builder(client, body)?;
    doubleclicksearch_reports_request_execute(builder)
}

/// GET doubleclicksearch/v2/agency/{agencyId}/advertiser/{advertiserId}/savedcolumns
/// Retrieve the list of saved columns for a specified advertiser.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `doubleclicksearch_saved_columns_list_execute()` to send, or `doubleclicksearch_saved_columns_list` for simplest API.

pub fn doubleclicksearch_saved_columns_list_builder(
    client: &SimpleHttpClient,
    advertiserId: &str,
    agencyId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comdoubleclicksearch/v2/agency/{}/advertiser/{}/savedcolumns",
        advertiserId, agencyId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET doubleclicksearch/v2/agency/{agencyId}/advertiser/{advertiserId}/savedcolumns
/// Retrieve the list of saved columns for a specified advertiser.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `doubleclicksearch_saved_columns_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from doubleclicksearch_saved_columns_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_saved_columns_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedColumnList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SavedColumnList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET doubleclicksearch/v2/agency/{agencyId}/advertiser/{advertiserId}/savedcolumns
/// Retrieve the list of saved columns for a specified advertiser.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `doubleclicksearch_saved_columns_list_builder()` + `doubleclicksearch_saved_columns_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn doubleclicksearch_saved_columns_list(
    client: &SimpleHttpClient,
    advertiserId: &str,
    agencyId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedColumnList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = doubleclicksearch_saved_columns_list_builder(client, advertiserId, agencyId)?;
    doubleclicksearch_saved_columns_list_execute(builder)
}
