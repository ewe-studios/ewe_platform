//! Auto-generated API clients for gcp/appsmarket.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET appsmarket/v2/customerLicense/{applicationId}/{customerId}
/// Gets the customer''s licensing status to determine if they have access to a given app. For more information, see [Getting app installation and licensing details](<https://developers.google.`com/workspace/marketplace/example-calls-marketplace-api`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appsmarket_customer_license_get_execute()` to send, or `appsmarket_customer_license_get` for simplest API.

pub fn appsmarket_customer_license_get_builder<R>(
    client: &SimpleHttpClient<R>,
    applicationId: &String,
    customerId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://appsmarket.googleapis.com/appsmarket/v2/customerLicense/{}/{}",
        applicationId, customerId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET appsmarket/v2/customerLicense/{applicationId}/{customerId}
/// Gets the customer''s licensing status to determine if they have access to a given app. For more information, see [Getting app installation and licensing details](<https://developers.google.`com/workspace/marketplace/example-calls-marketplace-api`>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `appsmarket_customer_license_get_execute()` or `appsmarket_customer_license_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `appsmarket_customer_license_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appsmarket_customer_license_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CustomerLicense>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CustomerLicense = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET appsmarket/v2/customerLicense/{applicationId}/{customerId}
/// Gets the customer''s licensing status to determine if they have access to a given app. For more information, see [Getting app installation and licensing details](<https://developers.google.`com/workspace/marketplace/example-calls-marketplace-api`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `appsmarket_customer_license_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `appsmarket_customer_license_get_task()`.
/// For the simplest API, use `appsmarket_customer_license_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `appsmarket_customer_license_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn appsmarket_customer_license_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomerLicense>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = appsmarket_customer_license_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`appsmarket_customer_license_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AppsmarketCustomerLicenseGetArgs {
    /// Path parameter: applicationId
    pub applicationId: String,
    /// Path parameter: customerId
    pub customerId: String,
}

/// GET appsmarket/v2/customerLicense/{applicationId}/{customerId}
/// Gets the customer''s licensing status to determine if they have access to a given app. For more information, see [Getting app installation and licensing details](<https://developers.google.`com/workspace/marketplace/example-calls-marketplace-api`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appsmarket_customer_license_get_builder()` + `appsmarket_customer_license_get_execute()`.
/// For task-level control, use `appsmarket_customer_license_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appsmarket_customer_license_get(
    client: &SimpleHttpClient,
    args: &AppsmarketCustomerLicenseGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CustomerLicense>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        appsmarket_customer_license_get_builder(client, &args.applicationId, &args.customerId)?;
    appsmarket_customer_license_get_execute(builder)
}

/// GET appsmarket/v2/userLicense/{applicationId}/{userId}
/// Gets the user''s licensing status to determine if they have permission to use a given app. For more information, see [Getting app installation and licensing details](<https://developers.google.`com/workspace/marketplace/example-calls-marketplace-api`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `appsmarket_user_license_get_execute()` to send, or `appsmarket_user_license_get` for simplest API.

pub fn appsmarket_user_license_get_builder<R>(
    client: &SimpleHttpClient<R>,
    applicationId: &String,
    userId: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://appsmarket.googleapis.com/appsmarket/v2/userLicense/{}/{}",
        applicationId, userId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET appsmarket/v2/userLicense/{applicationId}/{userId}
/// Gets the user''s licensing status to determine if they have permission to use a given app. For more information, see [Getting app installation and licensing details](<https://developers.google.`com/workspace/marketplace/example-calls-marketplace-api`>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `appsmarket_user_license_get_execute()` or `appsmarket_user_license_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `appsmarket_user_license_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appsmarket_user_license_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UserLicense>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UserLicense = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET appsmarket/v2/userLicense/{applicationId}/{userId}
/// Gets the user''s licensing status to determine if they have permission to use a given app. For more information, see [Getting app installation and licensing details](<https://developers.google.`com/workspace/marketplace/example-calls-marketplace-api`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `appsmarket_user_license_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `appsmarket_user_license_get_task()`.
/// For the simplest API, use `appsmarket_user_license_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `appsmarket_user_license_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn appsmarket_user_license_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserLicense>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = appsmarket_user_license_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`appsmarket_user_license_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AppsmarketUserLicenseGetArgs {
    /// Path parameter: applicationId
    pub applicationId: String,
    /// Path parameter: userId
    pub userId: String,
}

/// GET appsmarket/v2/userLicense/{applicationId}/{userId}
/// Gets the user''s licensing status to determine if they have permission to use a given app. For more information, see [Getting app installation and licensing details](<https://developers.google.`com/workspace/marketplace/example-calls-marketplace-api`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `appsmarket_user_license_get_builder()` + `appsmarket_user_license_get_execute()`.
/// For task-level control, use `appsmarket_user_license_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn appsmarket_user_license_get(
    client: &SimpleHttpClient,
    args: &AppsmarketUserLicenseGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UserLicense>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = appsmarket_user_license_get_builder(client, &args.applicationId, &args.userId)?;
    appsmarket_user_license_get_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for CustomerLicense
// =============================================================================

/// ResourceIdentifier implementation for CustomerLicense with AppsmarketCustomerLicenseGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AppsmarketCustomerLicenseGetArgs> for CustomerLicense {
    fn generate_resource_id(&self, input: &AppsmarketCustomerLicenseGetArgs) -> String {
        format!(
            "gcp::appsmarket::CustomerLicense/{}/{}",
            input.applicationId, input.customerId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::appsmarket::CustomerLicense"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UserLicense
// =============================================================================

/// ResourceIdentifier implementation for UserLicense with AppsmarketUserLicenseGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<AppsmarketUserLicenseGetArgs> for UserLicense {
    fn generate_resource_id(&self, input: &AppsmarketUserLicenseGetArgs) -> String {
        format!(
            "gcp::appsmarket::UserLicense/{}/{}",
            input.applicationId, input.userId
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::appsmarket::UserLicense"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
