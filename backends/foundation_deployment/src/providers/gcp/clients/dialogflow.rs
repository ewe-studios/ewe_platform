//! Auto-generated API clients for gcp/dialogflow.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v3/projects/{projectsId}/locations/{locationsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_get_execute()` to send, or `dialogflow_projects_locations_get` for simplest API.

pub fn dialogflow_projects_locations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_get_execute()` or `dialogflow_projects_locations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudLocationLocation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudLocationLocation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_get_task()`.
/// For the simplest API, use `dialogflow_projects_locations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudLocationLocation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_get_builder()` + `dialogflow_projects_locations_get_execute()`.
/// For task-level control, use `dialogflow_projects_locations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_get(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudLocationLocation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_get_builder(client, &args.name)?;
    dialogflow_projects_locations_get_execute(builder)
}

/// GET v3/projects/{projectsId}/locations
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_list_execute()` to send, or `dialogflow_projects_locations_list` for simplest API.

pub fn dialogflow_projects_locations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    extraLocationTypes: &Option<String>,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes.as_ref() {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_list_execute()` or `dialogflow_projects_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudLocationListLocationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudLocationListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_list_task()`.
/// For the simplest API, use `dialogflow_projects_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudLocationListLocationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: extraLocationTypes
    pub extraLocationTypes: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v3/projects/{projectsId}/locations
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_list_builder()` + `dialogflow_projects_locations_list_execute()`.
/// For task-level control, use `dialogflow_projects_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_list(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudLocationListLocationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_list_builder(
        client,
        &args.name,
        &args.extraLocationTypes,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    dialogflow_projects_locations_list_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_create_execute()` to send, or `dialogflow_projects_locations_agents_create` for simplest API.

pub fn dialogflow_projects_locations_agents_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_create_execute()` or `dialogflow_projects_locations_agents_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Agent>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Agent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_create_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Agent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_create_builder()` + `dialogflow_projects_locations_agents_create_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_create(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Agent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_create_builder(client, &args.parent)?;
    dialogflow_projects_locations_agents_create_execute(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_delete_execute()` to send, or `dialogflow_projects_locations_agents_delete` for simplest API.

pub fn dialogflow_projects_locations_agents_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_delete_execute()` or `dialogflow_projects_locations_agents_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_delete_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_delete_builder()` + `dialogflow_projects_locations_agents_delete_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_delete(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_delete_builder(client, &args.name)?;
    dialogflow_projects_locations_agents_delete_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}:export
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_export_execute()` to send, or `dialogflow_projects_locations_agents_export` for simplest API.

pub fn dialogflow_projects_locations_agents_export_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}:export",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}:export
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_export_execute()` or `dialogflow_projects_locations_agents_export`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_export_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}:export
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_export_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_export_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_export()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_export_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_export_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_export`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsExportArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}:export
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_export_builder()` + `dialogflow_projects_locations_agents_export_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_export_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_export(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsExportArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_export_builder(client, &args.name)?;
    dialogflow_projects_locations_agents_export_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_get_execute()` to send, or `dialogflow_projects_locations_agents_get` for simplest API.

pub fn dialogflow_projects_locations_agents_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_get_execute()` or `dialogflow_projects_locations_agents_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Agent>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Agent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_get_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Agent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_get_builder()` + `dialogflow_projects_locations_agents_get_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_get(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Agent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_get_builder(client, &args.name)?;
    dialogflow_projects_locations_agents_get_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/generativeSettings
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_get_generative_settings_execute()` to send, or `dialogflow_projects_locations_agents_get_generative_settings` for simplest API.

pub fn dialogflow_projects_locations_agents_get_generative_settings_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    languageCode: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/generativeSettings",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/generativeSettings
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_get_generative_settings_execute()` or `dialogflow_projects_locations_agents_get_generative_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_get_generative_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_get_generative_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3GenerativeSettings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3GenerativeSettings =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/generativeSettings
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_get_generative_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_get_generative_settings_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_get_generative_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_get_generative_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_get_generative_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3GenerativeSettings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_get_generative_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_get_generative_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsGetGenerativeSettingsArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/generativeSettings
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_get_generative_settings_builder()` + `dialogflow_projects_locations_agents_get_generative_settings_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_get_generative_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_get_generative_settings(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsGetGenerativeSettingsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3GenerativeSettings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_get_generative_settings_builder(
        client,
        &args.name,
        &args.languageCode,
    )?;
    dialogflow_projects_locations_agents_get_generative_settings_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/validationResult
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_get_validation_result_execute()` to send, or `dialogflow_projects_locations_agents_get_validation_result` for simplest API.

pub fn dialogflow_projects_locations_agents_get_validation_result_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    languageCode: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/validationResult",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/validationResult
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_get_validation_result_execute()` or `dialogflow_projects_locations_agents_get_validation_result`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_get_validation_result_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_get_validation_result_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3AgentValidationResult>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3AgentValidationResult =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/validationResult
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_get_validation_result_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_get_validation_result_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_get_validation_result()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_get_validation_result_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_get_validation_result_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3AgentValidationResult>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_get_validation_result_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_get_validation_result`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsGetValidationResultArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/validationResult
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_get_validation_result_builder()` + `dialogflow_projects_locations_agents_get_validation_result_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_get_validation_result_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_get_validation_result(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsGetValidationResultArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3AgentValidationResult>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_get_validation_result_builder(
        client,
        &args.name,
        &args.languageCode,
    )?;
    dialogflow_projects_locations_agents_get_validation_result_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_list_execute()` to send, or `dialogflow_projects_locations_agents_list` for simplest API.

pub fn dialogflow_projects_locations_agents_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_list_execute()` or `dialogflow_projects_locations_agents_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3ListAgentsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListAgentsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_list_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListAgentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_list_builder()` + `dialogflow_projects_locations_agents_list_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_list(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListAgentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    dialogflow_projects_locations_agents_list_execute(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_patch_execute()` to send, or `dialogflow_projects_locations_agents_patch` for simplest API.

pub fn dialogflow_projects_locations_agents_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_patch_execute()` or `dialogflow_projects_locations_agents_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Agent>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Agent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_patch_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Agent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_patch_builder()` + `dialogflow_projects_locations_agents_patch_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_patch(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Agent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_patch_builder(client, &args.name, &args.updateMask)?;
    dialogflow_projects_locations_agents_patch_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}:restore
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_restore_execute()` to send, or `dialogflow_projects_locations_agents_restore` for simplest API.

pub fn dialogflow_projects_locations_agents_restore_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}:restore",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}:restore
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_restore_execute()` or `dialogflow_projects_locations_agents_restore`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_restore_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_restore_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}:restore
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_restore_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_restore_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_restore()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_restore_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_restore_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_restore_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_restore`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsRestoreArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}:restore
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_restore_builder()` + `dialogflow_projects_locations_agents_restore_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_restore_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_restore(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsRestoreArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_restore_builder(client, &args.name)?;
    dialogflow_projects_locations_agents_restore_execute(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/generativeSettings
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_update_generative_settings_execute()` to send, or `dialogflow_projects_locations_agents_update_generative_settings` for simplest API.

pub fn dialogflow_projects_locations_agents_update_generative_settings_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/generativeSettings",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/generativeSettings
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_update_generative_settings_execute()` or `dialogflow_projects_locations_agents_update_generative_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_update_generative_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_update_generative_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3GenerativeSettings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3GenerativeSettings =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/generativeSettings
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_update_generative_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_update_generative_settings_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_update_generative_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_update_generative_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_update_generative_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3GenerativeSettings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_update_generative_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_update_generative_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsUpdateGenerativeSettingsArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/generativeSettings
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_update_generative_settings_builder()` + `dialogflow_projects_locations_agents_update_generative_settings_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_update_generative_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_update_generative_settings(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsUpdateGenerativeSettingsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3GenerativeSettings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_update_generative_settings_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    dialogflow_projects_locations_agents_update_generative_settings_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}:validate
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_validate_execute()` to send, or `dialogflow_projects_locations_agents_validate` for simplest API.

pub fn dialogflow_projects_locations_agents_validate_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}:validate",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}:validate
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_validate_execute()` or `dialogflow_projects_locations_agents_validate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_validate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_validate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3AgentValidationResult>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3AgentValidationResult =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}:validate
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_validate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_validate_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_validate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_validate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_validate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3AgentValidationResult>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_validate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_validate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsValidateArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}:validate
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_validate_builder()` + `dialogflow_projects_locations_agents_validate_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_validate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_validate(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsValidateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3AgentValidationResult>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_validate_builder(client, &args.name)?;
    dialogflow_projects_locations_agents_validate_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/changelogs/{changelogsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_changelogs_get_execute()` to send, or `dialogflow_projects_locations_agents_changelogs_get` for simplest API.

pub fn dialogflow_projects_locations_agents_changelogs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/changelogs/{changelogsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/changelogs/{changelogsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_changelogs_get_execute()` or `dialogflow_projects_locations_agents_changelogs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_changelogs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_changelogs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Changelog>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Changelog = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/changelogs/{changelogsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_changelogs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_changelogs_get_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_changelogs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_changelogs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_changelogs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Changelog>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_changelogs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_changelogs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsChangelogsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/changelogs/{changelogsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_changelogs_get_builder()` + `dialogflow_projects_locations_agents_changelogs_get_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_changelogs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_changelogs_get(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsChangelogsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Changelog>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_changelogs_get_builder(client, &args.name)?;
    dialogflow_projects_locations_agents_changelogs_get_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/changelogs
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_changelogs_list_execute()` to send, or `dialogflow_projects_locations_agents_changelogs_list` for simplest API.

pub fn dialogflow_projects_locations_agents_changelogs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/changelogs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/changelogs
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_changelogs_list_execute()` or `dialogflow_projects_locations_agents_changelogs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_changelogs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_changelogs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3ListChangelogsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListChangelogsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/changelogs
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_changelogs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_changelogs_list_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_changelogs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_changelogs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_changelogs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListChangelogsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_changelogs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_changelogs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsChangelogsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/changelogs
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_changelogs_list_builder()` + `dialogflow_projects_locations_agents_changelogs_list_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_changelogs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_changelogs_list(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsChangelogsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListChangelogsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_changelogs_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    dialogflow_projects_locations_agents_changelogs_list_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/entityTypes
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_entity_types_create_execute()` to send, or `dialogflow_projects_locations_agents_entity_types_create` for simplest API.

pub fn dialogflow_projects_locations_agents_entity_types_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    languageCode: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/entityTypes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/entityTypes
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_entity_types_create_execute()` or `dialogflow_projects_locations_agents_entity_types_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_entity_types_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_entity_types_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3EntityType>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3EntityType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/entityTypes
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_entity_types_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_entity_types_create_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_entity_types_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_entity_types_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_entity_types_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3EntityType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_entity_types_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_entity_types_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEntityTypesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/entityTypes
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_entity_types_create_builder()` + `dialogflow_projects_locations_agents_entity_types_create_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_entity_types_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_entity_types_create(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsEntityTypesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3EntityType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_entity_types_create_builder(
        client,
        &args.parent,
        &args.languageCode,
    )?;
    dialogflow_projects_locations_agents_entity_types_create_execute(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/entityTypes/{entityTypesId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_entity_types_delete_execute()` to send, or `dialogflow_projects_locations_agents_entity_types_delete` for simplest API.

pub fn dialogflow_projects_locations_agents_entity_types_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    force: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/entityTypes/{entityTypesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force.as_ref() {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/entityTypes/{entityTypesId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_entity_types_delete_execute()` or `dialogflow_projects_locations_agents_entity_types_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_entity_types_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_entity_types_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/entityTypes/{entityTypesId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_entity_types_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_entity_types_delete_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_entity_types_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_entity_types_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_entity_types_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_entity_types_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_entity_types_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEntityTypesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<String>,
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/entityTypes/{entityTypesId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_entity_types_delete_builder()` + `dialogflow_projects_locations_agents_entity_types_delete_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_entity_types_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_entity_types_delete(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsEntityTypesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_entity_types_delete_builder(
        client,
        &args.name,
        &args.force,
    )?;
    dialogflow_projects_locations_agents_entity_types_delete_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/entityTypes:export
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_entity_types_export_execute()` to send, or `dialogflow_projects_locations_agents_entity_types_export` for simplest API.

pub fn dialogflow_projects_locations_agents_entity_types_export_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/entityTypes:export",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/entityTypes:export
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_entity_types_export_execute()` or `dialogflow_projects_locations_agents_entity_types_export`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_entity_types_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_entity_types_export_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/entityTypes:export
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_entity_types_export_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_entity_types_export_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_entity_types_export()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_entity_types_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_entity_types_export_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_entity_types_export_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_entity_types_export`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEntityTypesExportArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/entityTypes:export
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_entity_types_export_builder()` + `dialogflow_projects_locations_agents_entity_types_export_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_entity_types_export_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_entity_types_export(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsEntityTypesExportArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_entity_types_export_builder(client, &args.parent)?;
    dialogflow_projects_locations_agents_entity_types_export_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/entityTypes/{entityTypesId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_entity_types_get_execute()` to send, or `dialogflow_projects_locations_agents_entity_types_get` for simplest API.

pub fn dialogflow_projects_locations_agents_entity_types_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    languageCode: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/entityTypes/{entityTypesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/entityTypes/{entityTypesId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_entity_types_get_execute()` or `dialogflow_projects_locations_agents_entity_types_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_entity_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_entity_types_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3EntityType>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3EntityType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/entityTypes/{entityTypesId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_entity_types_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_entity_types_get_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_entity_types_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_entity_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_entity_types_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3EntityType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_entity_types_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_entity_types_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEntityTypesGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/entityTypes/{entityTypesId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_entity_types_get_builder()` + `dialogflow_projects_locations_agents_entity_types_get_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_entity_types_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_entity_types_get(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsEntityTypesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3EntityType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_entity_types_get_builder(
        client,
        &args.name,
        &args.languageCode,
    )?;
    dialogflow_projects_locations_agents_entity_types_get_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/entityTypes:import
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_entity_types_import_execute()` to send, or `dialogflow_projects_locations_agents_entity_types_import` for simplest API.

pub fn dialogflow_projects_locations_agents_entity_types_import_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/entityTypes:import",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/entityTypes:import
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_entity_types_import_execute()` or `dialogflow_projects_locations_agents_entity_types_import`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_entity_types_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_entity_types_import_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/entityTypes:import
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_entity_types_import_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_entity_types_import_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_entity_types_import()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_entity_types_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_entity_types_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_entity_types_import_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_entity_types_import`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEntityTypesImportArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/entityTypes:import
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_entity_types_import_builder()` + `dialogflow_projects_locations_agents_entity_types_import_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_entity_types_import_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_entity_types_import(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsEntityTypesImportArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_entity_types_import_builder(client, &args.parent)?;
    dialogflow_projects_locations_agents_entity_types_import_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/entityTypes
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_entity_types_list_execute()` to send, or `dialogflow_projects_locations_agents_entity_types_list` for simplest API.

pub fn dialogflow_projects_locations_agents_entity_types_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    languageCode: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/entityTypes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/entityTypes
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_entity_types_list_execute()` or `dialogflow_projects_locations_agents_entity_types_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_entity_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_entity_types_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3ListEntityTypesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListEntityTypesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/entityTypes
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_entity_types_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_entity_types_list_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_entity_types_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_entity_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_entity_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListEntityTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_entity_types_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_entity_types_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEntityTypesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/entityTypes
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_entity_types_list_builder()` + `dialogflow_projects_locations_agents_entity_types_list_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_entity_types_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_entity_types_list(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsEntityTypesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListEntityTypesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_entity_types_list_builder(
        client,
        &args.parent,
        &args.languageCode,
        &args.pageSize,
        &args.pageToken,
    )?;
    dialogflow_projects_locations_agents_entity_types_list_execute(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/entityTypes/{entityTypesId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_entity_types_patch_execute()` to send, or `dialogflow_projects_locations_agents_entity_types_patch` for simplest API.

pub fn dialogflow_projects_locations_agents_entity_types_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    languageCode: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/entityTypes/{entityTypesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/entityTypes/{entityTypesId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_entity_types_patch_execute()` or `dialogflow_projects_locations_agents_entity_types_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_entity_types_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_entity_types_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3EntityType>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3EntityType = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/entityTypes/{entityTypesId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_entity_types_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_entity_types_patch_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_entity_types_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_entity_types_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_entity_types_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3EntityType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_entity_types_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_entity_types_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEntityTypesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/entityTypes/{entityTypesId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_entity_types_patch_builder()` + `dialogflow_projects_locations_agents_entity_types_patch_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_entity_types_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_entity_types_patch(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsEntityTypesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3EntityType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_entity_types_patch_builder(
        client,
        &args.name,
        &args.languageCode,
        &args.updateMask,
    )?;
    dialogflow_projects_locations_agents_entity_types_patch_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_environments_create_execute()` to send, or `dialogflow_projects_locations_agents_environments_create` for simplest API.

pub fn dialogflow_projects_locations_agents_environments_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/environments",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_environments_create_execute()` or `dialogflow_projects_locations_agents_environments_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_environments_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_create_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_environments_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_environments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_environments_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_environments_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_environments_create_builder()` + `dialogflow_projects_locations_agents_environments_create_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_create(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_environments_create_builder(client, &args.parent)?;
    dialogflow_projects_locations_agents_environments_create_execute(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_environments_delete_execute()` to send, or `dialogflow_projects_locations_agents_environments_delete` for simplest API.

pub fn dialogflow_projects_locations_agents_environments_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_environments_delete_execute()` or `dialogflow_projects_locations_agents_environments_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_environments_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_delete_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_environments_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_environments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_environments_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_environments_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_environments_delete_builder()` + `dialogflow_projects_locations_agents_environments_delete_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_delete(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_environments_delete_builder(client, &args.name)?;
    dialogflow_projects_locations_agents_environments_delete_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}:deployFlow
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_environments_deploy_flow_execute()` to send, or `dialogflow_projects_locations_agents_environments_deploy_flow` for simplest API.

pub fn dialogflow_projects_locations_agents_environments_deploy_flow_builder<R>(
    client: &SimpleHttpClient<R>,
    environment: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}:deployFlow",
        environment,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}:deployFlow
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_environments_deploy_flow_execute()` or `dialogflow_projects_locations_agents_environments_deploy_flow`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_deploy_flow_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_deploy_flow_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}:deployFlow
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_environments_deploy_flow_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_deploy_flow_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_environments_deploy_flow()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_deploy_flow_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_environments_deploy_flow_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_environments_deploy_flow_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_environments_deploy_flow`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsDeployFlowArgs {
    /// Path parameter: environment
    pub environment: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}:deployFlow
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_environments_deploy_flow_builder()` + `dialogflow_projects_locations_agents_environments_deploy_flow_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_deploy_flow_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_deploy_flow(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsDeployFlowArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_environments_deploy_flow_builder(
        client,
        &args.environment,
    )?;
    dialogflow_projects_locations_agents_environments_deploy_flow_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_environments_get_execute()` to send, or `dialogflow_projects_locations_agents_environments_get` for simplest API.

pub fn dialogflow_projects_locations_agents_environments_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_environments_get_execute()` or `dialogflow_projects_locations_agents_environments_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Environment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Environment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_environments_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_get_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_environments_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_environments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Environment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_environments_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_environments_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_environments_get_builder()` + `dialogflow_projects_locations_agents_environments_get_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_get(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Environment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_environments_get_builder(client, &args.name)?;
    dialogflow_projects_locations_agents_environments_get_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_environments_list_execute()` to send, or `dialogflow_projects_locations_agents_environments_list` for simplest API.

pub fn dialogflow_projects_locations_agents_environments_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/environments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_environments_list_execute()` or `dialogflow_projects_locations_agents_environments_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListEnvironmentsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListEnvironmentsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_environments_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_list_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_environments_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_environments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListEnvironmentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_environments_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_environments_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_environments_list_builder()` + `dialogflow_projects_locations_agents_environments_list_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_list(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListEnvironmentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_environments_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    dialogflow_projects_locations_agents_environments_list_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}:lookupEnvironmentHistory
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_environments_lookup_environment_history_execute()` to send, or `dialogflow_projects_locations_agents_environments_lookup_environment_history` for simplest API.

pub fn dialogflow_projects_locations_agents_environments_lookup_environment_history_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}:lookupEnvironmentHistory",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}:lookupEnvironmentHistory
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_environments_lookup_environment_history_execute()` or `dialogflow_projects_locations_agents_environments_lookup_environment_history`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_lookup_environment_history_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_lookup_environment_history_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3LookupEnvironmentHistoryResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3LookupEnvironmentHistoryResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}:lookupEnvironmentHistory
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_environments_lookup_environment_history_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_lookup_environment_history_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_environments_lookup_environment_history()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_lookup_environment_history_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_environments_lookup_environment_history_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDialogflowCxV3LookupEnvironmentHistoryResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dialogflow_projects_locations_agents_environments_lookup_environment_history_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_environments_lookup_environment_history`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsLookupEnvironmentHistoryArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}:lookupEnvironmentHistory
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_environments_lookup_environment_history_builder()` + `dialogflow_projects_locations_agents_environments_lookup_environment_history_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_lookup_environment_history_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_lookup_environment_history(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsLookupEnvironmentHistoryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDialogflowCxV3LookupEnvironmentHistoryResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_environments_lookup_environment_history_builder(
            client,
            &args.name,
            &args.pageSize,
            &args.pageToken,
        )?;
    dialogflow_projects_locations_agents_environments_lookup_environment_history_execute(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_environments_patch_execute()` to send, or `dialogflow_projects_locations_agents_environments_patch` for simplest API.

pub fn dialogflow_projects_locations_agents_environments_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_environments_patch_execute()` or `dialogflow_projects_locations_agents_environments_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_environments_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_patch_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_environments_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_environments_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_environments_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_environments_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_environments_patch_builder()` + `dialogflow_projects_locations_agents_environments_patch_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_patch(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_environments_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    dialogflow_projects_locations_agents_environments_patch_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}:runContinuousTest
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_environments_run_continuous_test_execute()` to send, or `dialogflow_projects_locations_agents_environments_run_continuous_test` for simplest API.

pub fn dialogflow_projects_locations_agents_environments_run_continuous_test_builder<R>(
    client: &SimpleHttpClient<R>,
    environment: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}:runContinuousTest",
        environment,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}:runContinuousTest
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_environments_run_continuous_test_execute()` or `dialogflow_projects_locations_agents_environments_run_continuous_test`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_run_continuous_test_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_run_continuous_test_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}:runContinuousTest
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_environments_run_continuous_test_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_run_continuous_test_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_environments_run_continuous_test()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_run_continuous_test_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_environments_run_continuous_test_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_environments_run_continuous_test_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_environments_run_continuous_test`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsRunContinuousTestArgs {
    /// Path parameter: environment
    pub environment: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}:runContinuousTest
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_environments_run_continuous_test_builder()` + `dialogflow_projects_locations_agents_environments_run_continuous_test_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_run_continuous_test_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_run_continuous_test(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsRunContinuousTestArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_environments_run_continuous_test_builder(
        client,
        &args.environment,
    )?;
    dialogflow_projects_locations_agents_environments_run_continuous_test_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/continuousTestResults
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_environments_continuous_test_results_list_execute()` to send, or `dialogflow_projects_locations_agents_environments_continuous_test_results_list` for simplest API.

pub fn dialogflow_projects_locations_agents_environments_continuous_test_results_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/continuousTestResults",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/continuousTestResults
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_environments_continuous_test_results_list_execute()` or `dialogflow_projects_locations_agents_environments_continuous_test_results_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_continuous_test_results_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_continuous_test_results_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListContinuousTestResultsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListContinuousTestResultsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/continuousTestResults
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_environments_continuous_test_results_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_continuous_test_results_list_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_environments_continuous_test_results_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_continuous_test_results_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_environments_continuous_test_results_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListContinuousTestResultsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_environments_continuous_test_results_list_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_environments_continuous_test_results_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsContinuousTestResultsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/continuousTestResults
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_environments_continuous_test_results_list_builder()` + `dialogflow_projects_locations_agents_environments_continuous_test_results_list_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_continuous_test_results_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_continuous_test_results_list(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsContinuousTestResultsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListContinuousTestResultsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_environments_continuous_test_results_list_builder(
            client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )?;
    dialogflow_projects_locations_agents_environments_continuous_test_results_list_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/deployments/{deploymentsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_environments_deployments_get_execute()` to send, or `dialogflow_projects_locations_agents_environments_deployments_get` for simplest API.

pub fn dialogflow_projects_locations_agents_environments_deployments_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/deployments/{deploymentsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/deployments/{deploymentsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_environments_deployments_get_execute()` or `dialogflow_projects_locations_agents_environments_deployments_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_deployments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_deployments_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Deployment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Deployment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/deployments/{deploymentsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_environments_deployments_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_deployments_get_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_environments_deployments_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_deployments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_environments_deployments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Deployment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_environments_deployments_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_environments_deployments_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsDeploymentsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/deployments/{deploymentsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_environments_deployments_get_builder()` + `dialogflow_projects_locations_agents_environments_deployments_get_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_deployments_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_deployments_get(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsDeploymentsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Deployment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_environments_deployments_get_builder(
        client, &args.name,
    )?;
    dialogflow_projects_locations_agents_environments_deployments_get_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/deployments
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_environments_deployments_list_execute()` to send, or `dialogflow_projects_locations_agents_environments_deployments_list` for simplest API.

pub fn dialogflow_projects_locations_agents_environments_deployments_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/deployments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/deployments
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_environments_deployments_list_execute()` or `dialogflow_projects_locations_agents_environments_deployments_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_deployments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_deployments_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3ListDeploymentsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListDeploymentsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/deployments
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_environments_deployments_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_deployments_list_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_environments_deployments_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_deployments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_environments_deployments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_environments_deployments_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_environments_deployments_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsDeploymentsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/deployments
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_environments_deployments_list_builder()` + `dialogflow_projects_locations_agents_environments_deployments_list_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_deployments_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_deployments_list(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsDeploymentsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListDeploymentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_environments_deployments_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    dialogflow_projects_locations_agents_environments_deployments_list_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_environments_experiments_create_execute()` to send, or `dialogflow_projects_locations_agents_environments_experiments_create` for simplest API.

pub fn dialogflow_projects_locations_agents_environments_experiments_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_environments_experiments_create_execute()` or `dialogflow_projects_locations_agents_environments_experiments_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_experiments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_experiments_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Experiment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Experiment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_environments_experiments_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_experiments_create_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_environments_experiments_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_experiments_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_environments_experiments_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Experiment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_environments_experiments_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_environments_experiments_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsExperimentsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_environments_experiments_create_builder()` + `dialogflow_projects_locations_agents_environments_experiments_create_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_experiments_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_experiments_create(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsExperimentsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Experiment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_environments_experiments_create_builder(
        client,
        &args.parent,
    )?;
    dialogflow_projects_locations_agents_environments_experiments_create_execute(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments/{experimentsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_environments_experiments_delete_execute()` to send, or `dialogflow_projects_locations_agents_environments_experiments_delete` for simplest API.

pub fn dialogflow_projects_locations_agents_environments_experiments_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments/{experimentsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments/{experimentsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_environments_experiments_delete_execute()` or `dialogflow_projects_locations_agents_environments_experiments_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_experiments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_experiments_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments/{experimentsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_environments_experiments_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_experiments_delete_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_environments_experiments_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_experiments_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_environments_experiments_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_environments_experiments_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_environments_experiments_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsExperimentsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments/{experimentsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_environments_experiments_delete_builder()` + `dialogflow_projects_locations_agents_environments_experiments_delete_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_experiments_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_experiments_delete(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsExperimentsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_environments_experiments_delete_builder(
        client, &args.name,
    )?;
    dialogflow_projects_locations_agents_environments_experiments_delete_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments/{experimentsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_environments_experiments_get_execute()` to send, or `dialogflow_projects_locations_agents_environments_experiments_get` for simplest API.

pub fn dialogflow_projects_locations_agents_environments_experiments_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments/{experimentsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments/{experimentsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_environments_experiments_get_execute()` or `dialogflow_projects_locations_agents_environments_experiments_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_experiments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_experiments_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Experiment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Experiment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments/{experimentsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_environments_experiments_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_experiments_get_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_environments_experiments_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_experiments_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_environments_experiments_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Experiment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_environments_experiments_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_environments_experiments_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsExperimentsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments/{experimentsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_environments_experiments_get_builder()` + `dialogflow_projects_locations_agents_environments_experiments_get_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_experiments_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_experiments_get(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsExperimentsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Experiment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_environments_experiments_get_builder(
        client, &args.name,
    )?;
    dialogflow_projects_locations_agents_environments_experiments_get_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_environments_experiments_list_execute()` to send, or `dialogflow_projects_locations_agents_environments_experiments_list` for simplest API.

pub fn dialogflow_projects_locations_agents_environments_experiments_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_environments_experiments_list_execute()` or `dialogflow_projects_locations_agents_environments_experiments_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_experiments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_experiments_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3ListExperimentsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListExperimentsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_environments_experiments_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_experiments_list_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_environments_experiments_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_experiments_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_environments_experiments_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListExperimentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_environments_experiments_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_environments_experiments_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsExperimentsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_environments_experiments_list_builder()` + `dialogflow_projects_locations_agents_environments_experiments_list_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_experiments_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_experiments_list(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsExperimentsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListExperimentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_environments_experiments_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    dialogflow_projects_locations_agents_environments_experiments_list_execute(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments/{experimentsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_environments_experiments_patch_execute()` to send, or `dialogflow_projects_locations_agents_environments_experiments_patch` for simplest API.

pub fn dialogflow_projects_locations_agents_environments_experiments_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments/{experimentsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments/{experimentsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_environments_experiments_patch_execute()` or `dialogflow_projects_locations_agents_environments_experiments_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_experiments_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_experiments_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Experiment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Experiment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments/{experimentsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_environments_experiments_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_experiments_patch_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_environments_experiments_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_experiments_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_environments_experiments_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Experiment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_environments_experiments_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_environments_experiments_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsExperimentsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments/{experimentsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_environments_experiments_patch_builder()` + `dialogflow_projects_locations_agents_environments_experiments_patch_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_experiments_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_experiments_patch(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsExperimentsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Experiment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_environments_experiments_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    dialogflow_projects_locations_agents_environments_experiments_patch_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments/{experimentsId}:start
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_environments_experiments_start_execute()` to send, or `dialogflow_projects_locations_agents_environments_experiments_start` for simplest API.

pub fn dialogflow_projects_locations_agents_environments_experiments_start_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments/{experimentsId}:start",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments/{experimentsId}:start
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_environments_experiments_start_execute()` or `dialogflow_projects_locations_agents_environments_experiments_start`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_experiments_start_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_experiments_start_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Experiment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Experiment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments/{experimentsId}:start
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_environments_experiments_start_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_experiments_start_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_environments_experiments_start()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_experiments_start_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_environments_experiments_start_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Experiment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_environments_experiments_start_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_environments_experiments_start`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsExperimentsStartArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments/{experimentsId}:start
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_environments_experiments_start_builder()` + `dialogflow_projects_locations_agents_environments_experiments_start_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_experiments_start_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_experiments_start(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsExperimentsStartArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Experiment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_environments_experiments_start_builder(
        client, &args.name,
    )?;
    dialogflow_projects_locations_agents_environments_experiments_start_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments/{experimentsId}:stop
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_environments_experiments_stop_execute()` to send, or `dialogflow_projects_locations_agents_environments_experiments_stop` for simplest API.

pub fn dialogflow_projects_locations_agents_environments_experiments_stop_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments/{experimentsId}:stop",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments/{experimentsId}:stop
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_environments_experiments_stop_execute()` or `dialogflow_projects_locations_agents_environments_experiments_stop`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_experiments_stop_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_experiments_stop_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Experiment>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Experiment = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments/{experimentsId}:stop
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_environments_experiments_stop_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_experiments_stop_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_environments_experiments_stop()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_experiments_stop_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_environments_experiments_stop_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Experiment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_environments_experiments_stop_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_environments_experiments_stop`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsExperimentsStopArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/experiments/{experimentsId}:stop
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_environments_experiments_stop_builder()` + `dialogflow_projects_locations_agents_environments_experiments_stop_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_experiments_stop_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_experiments_stop(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsExperimentsStopArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Experiment>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_environments_experiments_stop_builder(
        client, &args.name,
    )?;
    dialogflow_projects_locations_agents_environments_experiments_stop_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}:detectIntent
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_environments_sessions_detect_intent_execute()` to send, or `dialogflow_projects_locations_agents_environments_sessions_detect_intent` for simplest API.

pub fn dialogflow_projects_locations_agents_environments_sessions_detect_intent_builder<R>(
    client: &SimpleHttpClient<R>,
    session: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}:detectIntent",
        session,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}:detectIntent
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_environments_sessions_detect_intent_execute()` or `dialogflow_projects_locations_agents_environments_sessions_detect_intent`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_sessions_detect_intent_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_sessions_detect_intent_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3DetectIntentResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3DetectIntentResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}:detectIntent
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_environments_sessions_detect_intent_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_sessions_detect_intent_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_environments_sessions_detect_intent()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_sessions_detect_intent_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_environments_sessions_detect_intent_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3DetectIntentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dialogflow_projects_locations_agents_environments_sessions_detect_intent_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_environments_sessions_detect_intent`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsSessionsDetectIntentArgs {
    /// Path parameter: session
    pub session: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}:detectIntent
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_environments_sessions_detect_intent_builder()` + `dialogflow_projects_locations_agents_environments_sessions_detect_intent_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_sessions_detect_intent_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_sessions_detect_intent(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsSessionsDetectIntentArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3DetectIntentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_environments_sessions_detect_intent_builder(
        client,
        &args.session,
    )?;
    dialogflow_projects_locations_agents_environments_sessions_detect_intent_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}:fulfillIntent
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_environments_sessions_fulfill_intent_execute()` to send, or `dialogflow_projects_locations_agents_environments_sessions_fulfill_intent` for simplest API.

pub fn dialogflow_projects_locations_agents_environments_sessions_fulfill_intent_builder<R>(
    client: &SimpleHttpClient<R>,
    session: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}:fulfillIntent",
        session,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}:fulfillIntent
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_environments_sessions_fulfill_intent_execute()` or `dialogflow_projects_locations_agents_environments_sessions_fulfill_intent`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_sessions_fulfill_intent_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_sessions_fulfill_intent_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3FulfillIntentResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3FulfillIntentResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}:fulfillIntent
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_environments_sessions_fulfill_intent_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_sessions_fulfill_intent_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_environments_sessions_fulfill_intent()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_sessions_fulfill_intent_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_environments_sessions_fulfill_intent_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3FulfillIntentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dialogflow_projects_locations_agents_environments_sessions_fulfill_intent_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_environments_sessions_fulfill_intent`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsSessionsFulfillIntentArgs {
    /// Path parameter: session
    pub session: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}:fulfillIntent
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_environments_sessions_fulfill_intent_builder()` + `dialogflow_projects_locations_agents_environments_sessions_fulfill_intent_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_sessions_fulfill_intent_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_sessions_fulfill_intent(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsSessionsFulfillIntentArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3FulfillIntentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_environments_sessions_fulfill_intent_builder(
            client,
            &args.session,
        )?;
    dialogflow_projects_locations_agents_environments_sessions_fulfill_intent_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}:matchIntent
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_environments_sessions_match_intent_execute()` to send, or `dialogflow_projects_locations_agents_environments_sessions_match_intent` for simplest API.

pub fn dialogflow_projects_locations_agents_environments_sessions_match_intent_builder<R>(
    client: &SimpleHttpClient<R>,
    session: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}:matchIntent",
        session,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}:matchIntent
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_environments_sessions_match_intent_execute()` or `dialogflow_projects_locations_agents_environments_sessions_match_intent`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_sessions_match_intent_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_sessions_match_intent_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3MatchIntentResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3MatchIntentResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}:matchIntent
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_environments_sessions_match_intent_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_sessions_match_intent_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_environments_sessions_match_intent()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_sessions_match_intent_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_environments_sessions_match_intent_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3MatchIntentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dialogflow_projects_locations_agents_environments_sessions_match_intent_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_environments_sessions_match_intent`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsSessionsMatchIntentArgs {
    /// Path parameter: session
    pub session: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}:matchIntent
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_environments_sessions_match_intent_builder()` + `dialogflow_projects_locations_agents_environments_sessions_match_intent_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_sessions_match_intent_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_sessions_match_intent(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsSessionsMatchIntentArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3MatchIntentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_environments_sessions_match_intent_builder(
        client,
        &args.session,
    )?;
    dialogflow_projects_locations_agents_environments_sessions_match_intent_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}:serverStreamingDetectIntent
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_environments_sessions_server_streaming_detect_intent_execute()` to send, or `dialogflow_projects_locations_agents_environments_sessions_server_streaming_detect_intent` for simplest API.

pub fn dialogflow_projects_locations_agents_environments_sessions_server_streaming_detect_intent_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    session: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}:serverStreamingDetectIntent",
        session,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}:serverStreamingDetectIntent
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_environments_sessions_server_streaming_detect_intent_execute()` or `dialogflow_projects_locations_agents_environments_sessions_server_streaming_detect_intent`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_sessions_server_streaming_detect_intent_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_sessions_server_streaming_detect_intent_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3DetectIntentResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3DetectIntentResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}:serverStreamingDetectIntent
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_environments_sessions_server_streaming_detect_intent_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_sessions_server_streaming_detect_intent_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_environments_sessions_server_streaming_detect_intent()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_sessions_server_streaming_detect_intent_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_environments_sessions_server_streaming_detect_intent_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3DetectIntentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_environments_sessions_server_streaming_detect_intent_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_environments_sessions_server_streaming_detect_intent`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsSessionsServerStreamingDetectIntentArgs {
    /// Path parameter: session
    pub session: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}:serverStreamingDetectIntent
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_environments_sessions_server_streaming_detect_intent_builder()` + `dialogflow_projects_locations_agents_environments_sessions_server_streaming_detect_intent_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_sessions_server_streaming_detect_intent_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_sessions_server_streaming_detect_intent(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsSessionsServerStreamingDetectIntentArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3DetectIntentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_environments_sessions_server_streaming_detect_intent_builder(client, &args.session)?;
    dialogflow_projects_locations_agents_environments_sessions_server_streaming_detect_intent_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}/entityTypes
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_environments_sessions_entity_types_create_execute()` to send, or `dialogflow_projects_locations_agents_environments_sessions_entity_types_create` for simplest API.

pub fn dialogflow_projects_locations_agents_environments_sessions_entity_types_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}/entityTypes",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}/entityTypes
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_environments_sessions_entity_types_create_execute()` or `dialogflow_projects_locations_agents_environments_sessions_entity_types_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_sessions_entity_types_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_sessions_entity_types_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3SessionEntityType>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3SessionEntityType =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}/entityTypes
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_environments_sessions_entity_types_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_sessions_entity_types_create_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_environments_sessions_entity_types_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_sessions_entity_types_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_environments_sessions_entity_types_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3SessionEntityType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_environments_sessions_entity_types_create_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_environments_sessions_entity_types_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}/entityTypes
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_environments_sessions_entity_types_create_builder()` + `dialogflow_projects_locations_agents_environments_sessions_entity_types_create_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_sessions_entity_types_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_sessions_entity_types_create(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3SessionEntityType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_environments_sessions_entity_types_create_builder(
            client,
            &args.parent,
        )?;
    dialogflow_projects_locations_agents_environments_sessions_entity_types_create_execute(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}/entityTypes/{entityTypesId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_environments_sessions_entity_types_delete_execute()` to send, or `dialogflow_projects_locations_agents_environments_sessions_entity_types_delete` for simplest API.

pub fn dialogflow_projects_locations_agents_environments_sessions_entity_types_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}/entityTypes/{entityTypesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}/entityTypes/{entityTypesId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_environments_sessions_entity_types_delete_execute()` or `dialogflow_projects_locations_agents_environments_sessions_entity_types_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_sessions_entity_types_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_sessions_entity_types_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}/entityTypes/{entityTypesId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_environments_sessions_entity_types_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_sessions_entity_types_delete_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_environments_sessions_entity_types_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_sessions_entity_types_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_environments_sessions_entity_types_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_environments_sessions_entity_types_delete_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_environments_sessions_entity_types_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}/entityTypes/{entityTypesId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_environments_sessions_entity_types_delete_builder()` + `dialogflow_projects_locations_agents_environments_sessions_entity_types_delete_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_sessions_entity_types_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_sessions_entity_types_delete(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_environments_sessions_entity_types_delete_builder(
            client, &args.name,
        )?;
    dialogflow_projects_locations_agents_environments_sessions_entity_types_delete_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}/entityTypes/{entityTypesId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_environments_sessions_entity_types_get_execute()` to send, or `dialogflow_projects_locations_agents_environments_sessions_entity_types_get` for simplest API.

pub fn dialogflow_projects_locations_agents_environments_sessions_entity_types_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}/entityTypes/{entityTypesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}/entityTypes/{entityTypesId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_environments_sessions_entity_types_get_execute()` or `dialogflow_projects_locations_agents_environments_sessions_entity_types_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_sessions_entity_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_sessions_entity_types_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3SessionEntityType>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3SessionEntityType =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}/entityTypes/{entityTypesId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_environments_sessions_entity_types_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_sessions_entity_types_get_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_environments_sessions_entity_types_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_sessions_entity_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_environments_sessions_entity_types_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3SessionEntityType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dialogflow_projects_locations_agents_environments_sessions_entity_types_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_environments_sessions_entity_types_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}/entityTypes/{entityTypesId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_environments_sessions_entity_types_get_builder()` + `dialogflow_projects_locations_agents_environments_sessions_entity_types_get_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_sessions_entity_types_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_sessions_entity_types_get(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3SessionEntityType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_environments_sessions_entity_types_get_builder(
            client, &args.name,
        )?;
    dialogflow_projects_locations_agents_environments_sessions_entity_types_get_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}/entityTypes
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_environments_sessions_entity_types_list_execute()` to send, or `dialogflow_projects_locations_agents_environments_sessions_entity_types_list` for simplest API.

pub fn dialogflow_projects_locations_agents_environments_sessions_entity_types_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}/entityTypes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}/entityTypes
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_environments_sessions_entity_types_list_execute()` or `dialogflow_projects_locations_agents_environments_sessions_entity_types_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_sessions_entity_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_sessions_entity_types_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListSessionEntityTypesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListSessionEntityTypesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}/entityTypes
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_environments_sessions_entity_types_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_sessions_entity_types_list_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_environments_sessions_entity_types_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_sessions_entity_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_environments_sessions_entity_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListSessionEntityTypesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dialogflow_projects_locations_agents_environments_sessions_entity_types_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_environments_sessions_entity_types_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}/entityTypes
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_environments_sessions_entity_types_list_builder()` + `dialogflow_projects_locations_agents_environments_sessions_entity_types_list_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_sessions_entity_types_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_sessions_entity_types_list(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListSessionEntityTypesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_environments_sessions_entity_types_list_builder(
            client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )?;
    dialogflow_projects_locations_agents_environments_sessions_entity_types_list_execute(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}/entityTypes/{entityTypesId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_environments_sessions_entity_types_patch_execute()` to send, or `dialogflow_projects_locations_agents_environments_sessions_entity_types_patch` for simplest API.

pub fn dialogflow_projects_locations_agents_environments_sessions_entity_types_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}/entityTypes/{entityTypesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}/entityTypes/{entityTypesId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_environments_sessions_entity_types_patch_execute()` or `dialogflow_projects_locations_agents_environments_sessions_entity_types_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_sessions_entity_types_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_sessions_entity_types_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3SessionEntityType>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3SessionEntityType =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}/entityTypes/{entityTypesId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_environments_sessions_entity_types_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_sessions_entity_types_patch_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_environments_sessions_entity_types_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_environments_sessions_entity_types_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_environments_sessions_entity_types_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3SessionEntityType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_environments_sessions_entity_types_patch_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_environments_sessions_entity_types_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/environments/{environmentsId}/sessions/{sessionsId}/entityTypes/{entityTypesId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_environments_sessions_entity_types_patch_builder()` + `dialogflow_projects_locations_agents_environments_sessions_entity_types_patch_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_environments_sessions_entity_types_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_environments_sessions_entity_types_patch(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3SessionEntityType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_environments_sessions_entity_types_patch_builder(
            client,
            &args.name,
            &args.updateMask,
        )?;
    dialogflow_projects_locations_agents_environments_sessions_entity_types_patch_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_flows_create_execute()` to send, or `dialogflow_projects_locations_agents_flows_create` for simplest API.

pub fn dialogflow_projects_locations_agents_flows_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    languageCode: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/flows",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_flows_create_execute()` or `dialogflow_projects_locations_agents_flows_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Flow>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Flow = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_flows_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_create_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_flows_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_flows_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Flow>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_flows_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_flows_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_flows_create_builder()` + `dialogflow_projects_locations_agents_flows_create_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_create(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsFlowsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Flow>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_flows_create_builder(
        client,
        &args.parent,
        &args.languageCode,
    )?;
    dialogflow_projects_locations_agents_flows_create_execute(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_flows_delete_execute()` to send, or `dialogflow_projects_locations_agents_flows_delete` for simplest API.

pub fn dialogflow_projects_locations_agents_flows_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    force: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force.as_ref() {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_flows_delete_execute()` or `dialogflow_projects_locations_agents_flows_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_flows_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_delete_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_flows_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_flows_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_flows_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_flows_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<String>,
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_flows_delete_builder()` + `dialogflow_projects_locations_agents_flows_delete_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_delete(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsFlowsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_flows_delete_builder(client, &args.name, &args.force)?;
    dialogflow_projects_locations_agents_flows_delete_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}:export
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_flows_export_execute()` to send, or `dialogflow_projects_locations_agents_flows_export` for simplest API.

pub fn dialogflow_projects_locations_agents_flows_export_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}:export",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}:export
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_flows_export_execute()` or `dialogflow_projects_locations_agents_flows_export`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_export_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}:export
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_flows_export_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_export_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_flows_export()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_flows_export_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_flows_export_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_flows_export`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsExportArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}:export
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_flows_export_builder()` + `dialogflow_projects_locations_agents_flows_export_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_export_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_export(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsFlowsExportArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_flows_export_builder(client, &args.name)?;
    dialogflow_projects_locations_agents_flows_export_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_flows_get_execute()` to send, or `dialogflow_projects_locations_agents_flows_get` for simplest API.

pub fn dialogflow_projects_locations_agents_flows_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    languageCode: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_flows_get_execute()` or `dialogflow_projects_locations_agents_flows_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Flow>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Flow = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_flows_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_get_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_flows_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_flows_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Flow>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_flows_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_flows_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_flows_get_builder()` + `dialogflow_projects_locations_agents_flows_get_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_get(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsFlowsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Flow>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_flows_get_builder(
        client,
        &args.name,
        &args.languageCode,
    )?;
    dialogflow_projects_locations_agents_flows_get_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/validationResult
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_flows_get_validation_result_execute()` to send, or `dialogflow_projects_locations_agents_flows_get_validation_result` for simplest API.

pub fn dialogflow_projects_locations_agents_flows_get_validation_result_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    languageCode: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/validationResult",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/validationResult
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_flows_get_validation_result_execute()` or `dialogflow_projects_locations_agents_flows_get_validation_result`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_get_validation_result_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_get_validation_result_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3FlowValidationResult>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3FlowValidationResult =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/validationResult
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_flows_get_validation_result_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_get_validation_result_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_flows_get_validation_result()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_get_validation_result_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_flows_get_validation_result_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3FlowValidationResult>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_flows_get_validation_result_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_flows_get_validation_result`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsGetValidationResultArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/validationResult
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_flows_get_validation_result_builder()` + `dialogflow_projects_locations_agents_flows_get_validation_result_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_get_validation_result_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_get_validation_result(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsFlowsGetValidationResultArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3FlowValidationResult>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_flows_get_validation_result_builder(
        client,
        &args.name,
        &args.languageCode,
    )?;
    dialogflow_projects_locations_agents_flows_get_validation_result_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows:import
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_flows_import_execute()` to send, or `dialogflow_projects_locations_agents_flows_import` for simplest API.

pub fn dialogflow_projects_locations_agents_flows_import_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/flows:import",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows:import
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_flows_import_execute()` or `dialogflow_projects_locations_agents_flows_import`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_import_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows:import
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_flows_import_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_import_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_flows_import()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_flows_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_flows_import_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_flows_import`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsImportArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows:import
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_flows_import_builder()` + `dialogflow_projects_locations_agents_flows_import_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_import_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_import(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsFlowsImportArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_flows_import_builder(client, &args.parent)?;
    dialogflow_projects_locations_agents_flows_import_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_flows_list_execute()` to send, or `dialogflow_projects_locations_agents_flows_list` for simplest API.

pub fn dialogflow_projects_locations_agents_flows_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    languageCode: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/flows",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_flows_list_execute()` or `dialogflow_projects_locations_agents_flows_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3ListFlowsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListFlowsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_flows_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_list_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_flows_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_flows_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListFlowsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_flows_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_flows_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_flows_list_builder()` + `dialogflow_projects_locations_agents_flows_list_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_list(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsFlowsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListFlowsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_flows_list_builder(
        client,
        &args.parent,
        &args.languageCode,
        &args.pageSize,
        &args.pageToken,
    )?;
    dialogflow_projects_locations_agents_flows_list_execute(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_flows_patch_execute()` to send, or `dialogflow_projects_locations_agents_flows_patch` for simplest API.

pub fn dialogflow_projects_locations_agents_flows_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    languageCode: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_flows_patch_execute()` or `dialogflow_projects_locations_agents_flows_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Flow>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Flow = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_flows_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_patch_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_flows_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_flows_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Flow>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_flows_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_flows_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_flows_patch_builder()` + `dialogflow_projects_locations_agents_flows_patch_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_patch(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsFlowsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Flow>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_flows_patch_builder(
        client,
        &args.name,
        &args.languageCode,
        &args.updateMask,
    )?;
    dialogflow_projects_locations_agents_flows_patch_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}:train
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_flows_train_execute()` to send, or `dialogflow_projects_locations_agents_flows_train` for simplest API.

pub fn dialogflow_projects_locations_agents_flows_train_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}:train",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}:train
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_flows_train_execute()` or `dialogflow_projects_locations_agents_flows_train`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_train_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_train_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}:train
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_flows_train_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_train_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_flows_train()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_train_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_flows_train_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_flows_train_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_flows_train`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsTrainArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}:train
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_flows_train_builder()` + `dialogflow_projects_locations_agents_flows_train_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_train_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_train(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsFlowsTrainArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_flows_train_builder(client, &args.name)?;
    dialogflow_projects_locations_agents_flows_train_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}:validate
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_flows_validate_execute()` to send, or `dialogflow_projects_locations_agents_flows_validate` for simplest API.

pub fn dialogflow_projects_locations_agents_flows_validate_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}:validate",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}:validate
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_flows_validate_execute()` or `dialogflow_projects_locations_agents_flows_validate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_validate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_validate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3FlowValidationResult>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3FlowValidationResult =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}:validate
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_flows_validate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_validate_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_flows_validate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_validate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_flows_validate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3FlowValidationResult>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_flows_validate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_flows_validate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsValidateArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}:validate
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_flows_validate_builder()` + `dialogflow_projects_locations_agents_flows_validate_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_validate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_validate(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsFlowsValidateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3FlowValidationResult>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_flows_validate_builder(client, &args.name)?;
    dialogflow_projects_locations_agents_flows_validate_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/pages
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_flows_pages_create_execute()` to send, or `dialogflow_projects_locations_agents_flows_pages_create` for simplest API.

pub fn dialogflow_projects_locations_agents_flows_pages_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    languageCode: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/pages",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/pages
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_flows_pages_create_execute()` or `dialogflow_projects_locations_agents_flows_pages_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_pages_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_pages_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Page>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Page = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/pages
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_flows_pages_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_pages_create_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_flows_pages_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_pages_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_flows_pages_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Page>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_flows_pages_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_flows_pages_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsPagesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/pages
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_flows_pages_create_builder()` + `dialogflow_projects_locations_agents_flows_pages_create_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_pages_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_pages_create(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsFlowsPagesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Page>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_flows_pages_create_builder(
        client,
        &args.parent,
        &args.languageCode,
    )?;
    dialogflow_projects_locations_agents_flows_pages_create_execute(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/pages/{pagesId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_flows_pages_delete_execute()` to send, or `dialogflow_projects_locations_agents_flows_pages_delete` for simplest API.

pub fn dialogflow_projects_locations_agents_flows_pages_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    force: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/pages/{pagesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force.as_ref() {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/pages/{pagesId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_flows_pages_delete_execute()` or `dialogflow_projects_locations_agents_flows_pages_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_pages_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_pages_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/pages/{pagesId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_flows_pages_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_pages_delete_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_flows_pages_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_pages_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_flows_pages_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_flows_pages_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_flows_pages_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsPagesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<String>,
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/pages/{pagesId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_flows_pages_delete_builder()` + `dialogflow_projects_locations_agents_flows_pages_delete_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_pages_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_pages_delete(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsFlowsPagesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_flows_pages_delete_builder(
        client,
        &args.name,
        &args.force,
    )?;
    dialogflow_projects_locations_agents_flows_pages_delete_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/pages/{pagesId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_flows_pages_get_execute()` to send, or `dialogflow_projects_locations_agents_flows_pages_get` for simplest API.

pub fn dialogflow_projects_locations_agents_flows_pages_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    languageCode: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/pages/{pagesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/pages/{pagesId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_flows_pages_get_execute()` or `dialogflow_projects_locations_agents_flows_pages_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_pages_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_pages_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Page>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Page = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/pages/{pagesId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_flows_pages_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_pages_get_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_flows_pages_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_pages_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_flows_pages_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Page>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_flows_pages_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_flows_pages_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsPagesGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/pages/{pagesId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_flows_pages_get_builder()` + `dialogflow_projects_locations_agents_flows_pages_get_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_pages_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_pages_get(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsFlowsPagesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Page>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_flows_pages_get_builder(
        client,
        &args.name,
        &args.languageCode,
    )?;
    dialogflow_projects_locations_agents_flows_pages_get_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/pages
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_flows_pages_list_execute()` to send, or `dialogflow_projects_locations_agents_flows_pages_list` for simplest API.

pub fn dialogflow_projects_locations_agents_flows_pages_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    languageCode: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/pages",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/pages
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_flows_pages_list_execute()` or `dialogflow_projects_locations_agents_flows_pages_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_pages_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_pages_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3ListPagesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListPagesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/pages
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_flows_pages_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_pages_list_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_flows_pages_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_pages_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_flows_pages_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListPagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_flows_pages_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_flows_pages_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsPagesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/pages
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_flows_pages_list_builder()` + `dialogflow_projects_locations_agents_flows_pages_list_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_pages_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_pages_list(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsFlowsPagesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListPagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_flows_pages_list_builder(
        client,
        &args.parent,
        &args.languageCode,
        &args.pageSize,
        &args.pageToken,
    )?;
    dialogflow_projects_locations_agents_flows_pages_list_execute(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/pages/{pagesId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_flows_pages_patch_execute()` to send, or `dialogflow_projects_locations_agents_flows_pages_patch` for simplest API.

pub fn dialogflow_projects_locations_agents_flows_pages_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    languageCode: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/pages/{pagesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/pages/{pagesId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_flows_pages_patch_execute()` or `dialogflow_projects_locations_agents_flows_pages_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_pages_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_pages_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Page>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Page = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/pages/{pagesId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_flows_pages_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_pages_patch_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_flows_pages_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_pages_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_flows_pages_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Page>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_flows_pages_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_flows_pages_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsPagesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/pages/{pagesId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_flows_pages_patch_builder()` + `dialogflow_projects_locations_agents_flows_pages_patch_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_pages_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_pages_patch(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsFlowsPagesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Page>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_flows_pages_patch_builder(
        client,
        &args.name,
        &args.languageCode,
        &args.updateMask,
    )?;
    dialogflow_projects_locations_agents_flows_pages_patch_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/transitionRouteGroups
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_flows_transition_route_groups_create_execute()` to send, or `dialogflow_projects_locations_agents_flows_transition_route_groups_create` for simplest API.

pub fn dialogflow_projects_locations_agents_flows_transition_route_groups_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    languageCode: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/transitionRouteGroups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/transitionRouteGroups
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_flows_transition_route_groups_create_execute()` or `dialogflow_projects_locations_agents_flows_transition_route_groups_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_transition_route_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_transition_route_groups_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3TransitionRouteGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3TransitionRouteGroup =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/transitionRouteGroups
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_flows_transition_route_groups_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_transition_route_groups_create_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_flows_transition_route_groups_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_transition_route_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_flows_transition_route_groups_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3TransitionRouteGroup>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dialogflow_projects_locations_agents_flows_transition_route_groups_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_flows_transition_route_groups_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/transitionRouteGroups
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_flows_transition_route_groups_create_builder()` + `dialogflow_projects_locations_agents_flows_transition_route_groups_create_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_transition_route_groups_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_transition_route_groups_create(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3TransitionRouteGroup>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_flows_transition_route_groups_create_builder(
            client,
            &args.parent,
            &args.languageCode,
        )?;
    dialogflow_projects_locations_agents_flows_transition_route_groups_create_execute(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/transitionRouteGroups/{transitionRouteGroupsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_flows_transition_route_groups_delete_execute()` to send, or `dialogflow_projects_locations_agents_flows_transition_route_groups_delete` for simplest API.

pub fn dialogflow_projects_locations_agents_flows_transition_route_groups_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    force: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/transitionRouteGroups/{transitionRouteGroupsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force.as_ref() {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/transitionRouteGroups/{transitionRouteGroupsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_flows_transition_route_groups_delete_execute()` or `dialogflow_projects_locations_agents_flows_transition_route_groups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_transition_route_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_transition_route_groups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/transitionRouteGroups/{transitionRouteGroupsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_flows_transition_route_groups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_transition_route_groups_delete_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_flows_transition_route_groups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_transition_route_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_flows_transition_route_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        dialogflow_projects_locations_agents_flows_transition_route_groups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_flows_transition_route_groups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<String>,
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/transitionRouteGroups/{transitionRouteGroupsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_flows_transition_route_groups_delete_builder()` + `dialogflow_projects_locations_agents_flows_transition_route_groups_delete_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_transition_route_groups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_transition_route_groups_delete(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_flows_transition_route_groups_delete_builder(
            client,
            &args.name,
            &args.force,
        )?;
    dialogflow_projects_locations_agents_flows_transition_route_groups_delete_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/transitionRouteGroups/{transitionRouteGroupsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_flows_transition_route_groups_get_execute()` to send, or `dialogflow_projects_locations_agents_flows_transition_route_groups_get` for simplest API.

pub fn dialogflow_projects_locations_agents_flows_transition_route_groups_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    languageCode: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/transitionRouteGroups/{transitionRouteGroupsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/transitionRouteGroups/{transitionRouteGroupsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_flows_transition_route_groups_get_execute()` or `dialogflow_projects_locations_agents_flows_transition_route_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_transition_route_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_transition_route_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3TransitionRouteGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3TransitionRouteGroup =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/transitionRouteGroups/{transitionRouteGroupsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_flows_transition_route_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_transition_route_groups_get_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_flows_transition_route_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_transition_route_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_flows_transition_route_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3TransitionRouteGroup>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dialogflow_projects_locations_agents_flows_transition_route_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_flows_transition_route_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/transitionRouteGroups/{transitionRouteGroupsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_flows_transition_route_groups_get_builder()` + `dialogflow_projects_locations_agents_flows_transition_route_groups_get_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_transition_route_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_transition_route_groups_get(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3TransitionRouteGroup>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_flows_transition_route_groups_get_builder(
        client,
        &args.name,
        &args.languageCode,
    )?;
    dialogflow_projects_locations_agents_flows_transition_route_groups_get_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/transitionRouteGroups
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_flows_transition_route_groups_list_execute()` to send, or `dialogflow_projects_locations_agents_flows_transition_route_groups_list` for simplest API.

pub fn dialogflow_projects_locations_agents_flows_transition_route_groups_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    languageCode: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/transitionRouteGroups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/transitionRouteGroups
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_flows_transition_route_groups_list_execute()` or `dialogflow_projects_locations_agents_flows_transition_route_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_transition_route_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_transition_route_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListTransitionRouteGroupsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListTransitionRouteGroupsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/transitionRouteGroups
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_flows_transition_route_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_transition_route_groups_list_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_flows_transition_route_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_transition_route_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_flows_transition_route_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListTransitionRouteGroupsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dialogflow_projects_locations_agents_flows_transition_route_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_flows_transition_route_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/transitionRouteGroups
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_flows_transition_route_groups_list_builder()` + `dialogflow_projects_locations_agents_flows_transition_route_groups_list_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_transition_route_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_transition_route_groups_list(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListTransitionRouteGroupsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_flows_transition_route_groups_list_builder(
        client,
        &args.parent,
        &args.languageCode,
        &args.pageSize,
        &args.pageToken,
    )?;
    dialogflow_projects_locations_agents_flows_transition_route_groups_list_execute(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/transitionRouteGroups/{transitionRouteGroupsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_flows_transition_route_groups_patch_execute()` to send, or `dialogflow_projects_locations_agents_flows_transition_route_groups_patch` for simplest API.

pub fn dialogflow_projects_locations_agents_flows_transition_route_groups_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    languageCode: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/transitionRouteGroups/{transitionRouteGroupsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/transitionRouteGroups/{transitionRouteGroupsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_flows_transition_route_groups_patch_execute()` or `dialogflow_projects_locations_agents_flows_transition_route_groups_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_transition_route_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_transition_route_groups_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3TransitionRouteGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3TransitionRouteGroup =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/transitionRouteGroups/{transitionRouteGroupsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_flows_transition_route_groups_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_transition_route_groups_patch_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_flows_transition_route_groups_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_transition_route_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_flows_transition_route_groups_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3TransitionRouteGroup>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dialogflow_projects_locations_agents_flows_transition_route_groups_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_flows_transition_route_groups_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/transitionRouteGroups/{transitionRouteGroupsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_flows_transition_route_groups_patch_builder()` + `dialogflow_projects_locations_agents_flows_transition_route_groups_patch_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_transition_route_groups_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_transition_route_groups_patch(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3TransitionRouteGroup>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_flows_transition_route_groups_patch_builder(
        client,
        &args.name,
        &args.languageCode,
        &args.updateMask,
    )?;
    dialogflow_projects_locations_agents_flows_transition_route_groups_patch_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions/{versionsId}:compareVersions
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_flows_versions_compare_versions_execute()` to send, or `dialogflow_projects_locations_agents_flows_versions_compare_versions` for simplest API.

pub fn dialogflow_projects_locations_agents_flows_versions_compare_versions_builder<R>(
    client: &SimpleHttpClient<R>,
    baseVersion: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions/{versionsId}:compareVersions",
        baseVersion,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions/{versionsId}:compareVersions
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_flows_versions_compare_versions_execute()` or `dialogflow_projects_locations_agents_flows_versions_compare_versions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_versions_compare_versions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_versions_compare_versions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3CompareVersionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3CompareVersionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions/{versionsId}:compareVersions
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_flows_versions_compare_versions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_versions_compare_versions_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_flows_versions_compare_versions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_versions_compare_versions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_flows_versions_compare_versions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3CompareVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_flows_versions_compare_versions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_flows_versions_compare_versions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsVersionsCompareVersionsArgs {
    /// Path parameter: baseVersion
    pub baseVersion: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions/{versionsId}:compareVersions
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_flows_versions_compare_versions_builder()` + `dialogflow_projects_locations_agents_flows_versions_compare_versions_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_versions_compare_versions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_versions_compare_versions(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsFlowsVersionsCompareVersionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3CompareVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_flows_versions_compare_versions_builder(
        client,
        &args.baseVersion,
    )?;
    dialogflow_projects_locations_agents_flows_versions_compare_versions_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_flows_versions_create_execute()` to send, or `dialogflow_projects_locations_agents_flows_versions_create` for simplest API.

pub fn dialogflow_projects_locations_agents_flows_versions_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_flows_versions_create_execute()` or `dialogflow_projects_locations_agents_flows_versions_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_versions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_versions_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_flows_versions_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_versions_create_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_flows_versions_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_versions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_flows_versions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_flows_versions_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_flows_versions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsVersionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_flows_versions_create_builder()` + `dialogflow_projects_locations_agents_flows_versions_create_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_versions_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_versions_create(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsFlowsVersionsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_flows_versions_create_builder(client, &args.parent)?;
    dialogflow_projects_locations_agents_flows_versions_create_execute(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions/{versionsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_flows_versions_delete_execute()` to send, or `dialogflow_projects_locations_agents_flows_versions_delete` for simplest API.

pub fn dialogflow_projects_locations_agents_flows_versions_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions/{versionsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions/{versionsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_flows_versions_delete_execute()` or `dialogflow_projects_locations_agents_flows_versions_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_versions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_versions_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions/{versionsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_flows_versions_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_versions_delete_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_flows_versions_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_versions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_flows_versions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_flows_versions_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_flows_versions_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsVersionsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions/{versionsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_flows_versions_delete_builder()` + `dialogflow_projects_locations_agents_flows_versions_delete_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_versions_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_versions_delete(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsFlowsVersionsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_flows_versions_delete_builder(client, &args.name)?;
    dialogflow_projects_locations_agents_flows_versions_delete_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions/{versionsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_flows_versions_get_execute()` to send, or `dialogflow_projects_locations_agents_flows_versions_get` for simplest API.

pub fn dialogflow_projects_locations_agents_flows_versions_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions/{versionsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions/{versionsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_flows_versions_get_execute()` or `dialogflow_projects_locations_agents_flows_versions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_versions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_versions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Version>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Version = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions/{versionsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_flows_versions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_versions_get_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_flows_versions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_versions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_flows_versions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Version>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_flows_versions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_flows_versions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsVersionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions/{versionsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_flows_versions_get_builder()` + `dialogflow_projects_locations_agents_flows_versions_get_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_versions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_versions_get(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsFlowsVersionsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Version>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_flows_versions_get_builder(client, &args.name)?;
    dialogflow_projects_locations_agents_flows_versions_get_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_flows_versions_list_execute()` to send, or `dialogflow_projects_locations_agents_flows_versions_list` for simplest API.

pub fn dialogflow_projects_locations_agents_flows_versions_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_flows_versions_list_execute()` or `dialogflow_projects_locations_agents_flows_versions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_versions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_versions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3ListVersionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListVersionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_flows_versions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_versions_list_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_flows_versions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_versions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_flows_versions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_flows_versions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_flows_versions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsVersionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_flows_versions_list_builder()` + `dialogflow_projects_locations_agents_flows_versions_list_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_versions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_versions_list(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsFlowsVersionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_flows_versions_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    dialogflow_projects_locations_agents_flows_versions_list_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions/{versionsId}:load
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_flows_versions_load_execute()` to send, or `dialogflow_projects_locations_agents_flows_versions_load` for simplest API.

pub fn dialogflow_projects_locations_agents_flows_versions_load_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions/{versionsId}:load",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions/{versionsId}:load
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_flows_versions_load_execute()` or `dialogflow_projects_locations_agents_flows_versions_load`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_versions_load_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_versions_load_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions/{versionsId}:load
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_flows_versions_load_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_versions_load_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_flows_versions_load()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_versions_load_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_flows_versions_load_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_flows_versions_load_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_flows_versions_load`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsVersionsLoadArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions/{versionsId}:load
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_flows_versions_load_builder()` + `dialogflow_projects_locations_agents_flows_versions_load_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_versions_load_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_versions_load(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsFlowsVersionsLoadArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_flows_versions_load_builder(client, &args.name)?;
    dialogflow_projects_locations_agents_flows_versions_load_execute(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions/{versionsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_flows_versions_patch_execute()` to send, or `dialogflow_projects_locations_agents_flows_versions_patch` for simplest API.

pub fn dialogflow_projects_locations_agents_flows_versions_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions/{versionsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions/{versionsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_flows_versions_patch_execute()` or `dialogflow_projects_locations_agents_flows_versions_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_versions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_versions_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Version>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Version = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions/{versionsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_flows_versions_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_versions_patch_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_flows_versions_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_flows_versions_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_flows_versions_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Version>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_flows_versions_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_flows_versions_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsFlowsVersionsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/flows/{flowsId}/versions/{versionsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_flows_versions_patch_builder()` + `dialogflow_projects_locations_agents_flows_versions_patch_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_flows_versions_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_flows_versions_patch(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsFlowsVersionsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Version>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_flows_versions_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    dialogflow_projects_locations_agents_flows_versions_patch_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/generators
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_generators_create_execute()` to send, or `dialogflow_projects_locations_agents_generators_create` for simplest API.

pub fn dialogflow_projects_locations_agents_generators_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    languageCode: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/generators",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/generators
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_generators_create_execute()` or `dialogflow_projects_locations_agents_generators_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_generators_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_generators_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Generator>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Generator = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/generators
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_generators_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_generators_create_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_generators_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_generators_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_generators_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Generator>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_generators_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_generators_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsGeneratorsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/generators
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_generators_create_builder()` + `dialogflow_projects_locations_agents_generators_create_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_generators_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_generators_create(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsGeneratorsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Generator>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_generators_create_builder(
        client,
        &args.parent,
        &args.languageCode,
    )?;
    dialogflow_projects_locations_agents_generators_create_execute(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/generators/{generatorsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_generators_delete_execute()` to send, or `dialogflow_projects_locations_agents_generators_delete` for simplest API.

pub fn dialogflow_projects_locations_agents_generators_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    force: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/generators/{generatorsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force.as_ref() {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/generators/{generatorsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_generators_delete_execute()` or `dialogflow_projects_locations_agents_generators_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_generators_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_generators_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/generators/{generatorsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_generators_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_generators_delete_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_generators_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_generators_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_generators_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_generators_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_generators_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsGeneratorsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<String>,
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/generators/{generatorsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_generators_delete_builder()` + `dialogflow_projects_locations_agents_generators_delete_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_generators_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_generators_delete(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsGeneratorsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_generators_delete_builder(
        client,
        &args.name,
        &args.force,
    )?;
    dialogflow_projects_locations_agents_generators_delete_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/generators/{generatorsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_generators_get_execute()` to send, or `dialogflow_projects_locations_agents_generators_get` for simplest API.

pub fn dialogflow_projects_locations_agents_generators_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    languageCode: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/generators/{generatorsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/generators/{generatorsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_generators_get_execute()` or `dialogflow_projects_locations_agents_generators_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_generators_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_generators_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Generator>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Generator = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/generators/{generatorsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_generators_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_generators_get_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_generators_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_generators_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_generators_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Generator>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_generators_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_generators_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsGeneratorsGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/generators/{generatorsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_generators_get_builder()` + `dialogflow_projects_locations_agents_generators_get_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_generators_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_generators_get(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsGeneratorsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Generator>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_generators_get_builder(
        client,
        &args.name,
        &args.languageCode,
    )?;
    dialogflow_projects_locations_agents_generators_get_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/generators
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_generators_list_execute()` to send, or `dialogflow_projects_locations_agents_generators_list` for simplest API.

pub fn dialogflow_projects_locations_agents_generators_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    languageCode: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/generators",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/generators
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_generators_list_execute()` or `dialogflow_projects_locations_agents_generators_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_generators_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_generators_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3ListGeneratorsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListGeneratorsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/generators
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_generators_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_generators_list_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_generators_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_generators_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_generators_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListGeneratorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_generators_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_generators_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsGeneratorsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/generators
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_generators_list_builder()` + `dialogflow_projects_locations_agents_generators_list_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_generators_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_generators_list(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsGeneratorsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListGeneratorsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_generators_list_builder(
        client,
        &args.parent,
        &args.languageCode,
        &args.pageSize,
        &args.pageToken,
    )?;
    dialogflow_projects_locations_agents_generators_list_execute(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/generators/{generatorsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_generators_patch_execute()` to send, or `dialogflow_projects_locations_agents_generators_patch` for simplest API.

pub fn dialogflow_projects_locations_agents_generators_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    languageCode: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/generators/{generatorsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/generators/{generatorsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_generators_patch_execute()` or `dialogflow_projects_locations_agents_generators_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_generators_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_generators_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Generator>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Generator = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/generators/{generatorsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_generators_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_generators_patch_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_generators_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_generators_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_generators_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Generator>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_generators_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_generators_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsGeneratorsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/generators/{generatorsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_generators_patch_builder()` + `dialogflow_projects_locations_agents_generators_patch_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_generators_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_generators_patch(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsGeneratorsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Generator>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_generators_patch_builder(
        client,
        &args.name,
        &args.languageCode,
        &args.updateMask,
    )?;
    dialogflow_projects_locations_agents_generators_patch_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/intents
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_intents_create_execute()` to send, or `dialogflow_projects_locations_agents_intents_create` for simplest API.

pub fn dialogflow_projects_locations_agents_intents_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    languageCode: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/intents",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/intents
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_intents_create_execute()` or `dialogflow_projects_locations_agents_intents_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_intents_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_intents_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Intent>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Intent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/intents
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_intents_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_intents_create_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_intents_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_intents_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_intents_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Intent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_intents_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_intents_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsIntentsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/intents
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_intents_create_builder()` + `dialogflow_projects_locations_agents_intents_create_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_intents_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_intents_create(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsIntentsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Intent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_intents_create_builder(
        client,
        &args.parent,
        &args.languageCode,
    )?;
    dialogflow_projects_locations_agents_intents_create_execute(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/intents/{intentsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_intents_delete_execute()` to send, or `dialogflow_projects_locations_agents_intents_delete` for simplest API.

pub fn dialogflow_projects_locations_agents_intents_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/intents/{intentsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/intents/{intentsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_intents_delete_execute()` or `dialogflow_projects_locations_agents_intents_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_intents_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_intents_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/intents/{intentsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_intents_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_intents_delete_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_intents_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_intents_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_intents_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_intents_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_intents_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsIntentsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/intents/{intentsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_intents_delete_builder()` + `dialogflow_projects_locations_agents_intents_delete_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_intents_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_intents_delete(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsIntentsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_intents_delete_builder(client, &args.name)?;
    dialogflow_projects_locations_agents_intents_delete_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/intents:export
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_intents_export_execute()` to send, or `dialogflow_projects_locations_agents_intents_export` for simplest API.

pub fn dialogflow_projects_locations_agents_intents_export_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/intents:export",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/intents:export
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_intents_export_execute()` or `dialogflow_projects_locations_agents_intents_export`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_intents_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_intents_export_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/intents:export
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_intents_export_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_intents_export_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_intents_export()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_intents_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_intents_export_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_intents_export_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_intents_export`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsIntentsExportArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/intents:export
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_intents_export_builder()` + `dialogflow_projects_locations_agents_intents_export_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_intents_export_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_intents_export(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsIntentsExportArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_intents_export_builder(client, &args.parent)?;
    dialogflow_projects_locations_agents_intents_export_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/intents/{intentsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_intents_get_execute()` to send, or `dialogflow_projects_locations_agents_intents_get` for simplest API.

pub fn dialogflow_projects_locations_agents_intents_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    languageCode: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/intents/{intentsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/intents/{intentsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_intents_get_execute()` or `dialogflow_projects_locations_agents_intents_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_intents_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_intents_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Intent>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Intent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/intents/{intentsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_intents_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_intents_get_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_intents_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_intents_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_intents_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Intent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_intents_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_intents_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsIntentsGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/intents/{intentsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_intents_get_builder()` + `dialogflow_projects_locations_agents_intents_get_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_intents_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_intents_get(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsIntentsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Intent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_intents_get_builder(
        client,
        &args.name,
        &args.languageCode,
    )?;
    dialogflow_projects_locations_agents_intents_get_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/intents:import
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_intents_import_execute()` to send, or `dialogflow_projects_locations_agents_intents_import` for simplest API.

pub fn dialogflow_projects_locations_agents_intents_import_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/intents:import",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/intents:import
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_intents_import_execute()` or `dialogflow_projects_locations_agents_intents_import`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_intents_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_intents_import_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/intents:import
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_intents_import_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_intents_import_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_intents_import()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_intents_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_intents_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_intents_import_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_intents_import`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsIntentsImportArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/intents:import
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_intents_import_builder()` + `dialogflow_projects_locations_agents_intents_import_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_intents_import_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_intents_import(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsIntentsImportArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_intents_import_builder(client, &args.parent)?;
    dialogflow_projects_locations_agents_intents_import_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/intents
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_intents_list_execute()` to send, or `dialogflow_projects_locations_agents_intents_list` for simplest API.

pub fn dialogflow_projects_locations_agents_intents_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    intentView: &Option<String>,
    languageCode: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/intents",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = intentView.as_ref() {
        query_parts.push(format!("intentView={}", val));
    }
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/intents
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_intents_list_execute()` or `dialogflow_projects_locations_agents_intents_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_intents_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_intents_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3ListIntentsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListIntentsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/intents
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_intents_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_intents_list_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_intents_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_intents_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_intents_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListIntentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_intents_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_intents_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsIntentsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: intentView
    pub intentView: Option<String>,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/intents
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_intents_list_builder()` + `dialogflow_projects_locations_agents_intents_list_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_intents_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_intents_list(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsIntentsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListIntentsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_intents_list_builder(
        client,
        &args.parent,
        &args.intentView,
        &args.languageCode,
        &args.pageSize,
        &args.pageToken,
    )?;
    dialogflow_projects_locations_agents_intents_list_execute(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/intents/{intentsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_intents_patch_execute()` to send, or `dialogflow_projects_locations_agents_intents_patch` for simplest API.

pub fn dialogflow_projects_locations_agents_intents_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    languageCode: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/intents/{intentsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/intents/{intentsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_intents_patch_execute()` or `dialogflow_projects_locations_agents_intents_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_intents_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_intents_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Intent>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Intent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/intents/{intentsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_intents_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_intents_patch_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_intents_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_intents_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_intents_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Intent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_intents_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_intents_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsIntentsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/intents/{intentsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_intents_patch_builder()` + `dialogflow_projects_locations_agents_intents_patch_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_intents_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_intents_patch(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsIntentsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Intent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_intents_patch_builder(
        client,
        &args.name,
        &args.languageCode,
        &args.updateMask,
    )?;
    dialogflow_projects_locations_agents_intents_patch_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_playbooks_create_execute()` to send, or `dialogflow_projects_locations_agents_playbooks_create` for simplest API.

pub fn dialogflow_projects_locations_agents_playbooks_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/playbooks",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_playbooks_create_execute()` or `dialogflow_projects_locations_agents_playbooks_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_playbooks_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_playbooks_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Playbook>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Playbook = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_playbooks_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_playbooks_create_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_playbooks_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_playbooks_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_playbooks_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Playbook>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_playbooks_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_playbooks_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPlaybooksCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_playbooks_create_builder()` + `dialogflow_projects_locations_agents_playbooks_create_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_playbooks_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_playbooks_create(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsPlaybooksCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Playbook>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_playbooks_create_builder(client, &args.parent)?;
    dialogflow_projects_locations_agents_playbooks_create_execute(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_playbooks_delete_execute()` to send, or `dialogflow_projects_locations_agents_playbooks_delete` for simplest API.

pub fn dialogflow_projects_locations_agents_playbooks_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_playbooks_delete_execute()` or `dialogflow_projects_locations_agents_playbooks_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_playbooks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_playbooks_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_playbooks_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_playbooks_delete_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_playbooks_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_playbooks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_playbooks_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_playbooks_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_playbooks_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPlaybooksDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_playbooks_delete_builder()` + `dialogflow_projects_locations_agents_playbooks_delete_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_playbooks_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_playbooks_delete(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsPlaybooksDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_playbooks_delete_builder(client, &args.name)?;
    dialogflow_projects_locations_agents_playbooks_delete_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}:export
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_playbooks_export_execute()` to send, or `dialogflow_projects_locations_agents_playbooks_export` for simplest API.

pub fn dialogflow_projects_locations_agents_playbooks_export_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}:export",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}:export
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_playbooks_export_execute()` or `dialogflow_projects_locations_agents_playbooks_export`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_playbooks_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_playbooks_export_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}:export
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_playbooks_export_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_playbooks_export_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_playbooks_export()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_playbooks_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_playbooks_export_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_playbooks_export_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_playbooks_export`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPlaybooksExportArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}:export
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_playbooks_export_builder()` + `dialogflow_projects_locations_agents_playbooks_export_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_playbooks_export_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_playbooks_export(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsPlaybooksExportArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_playbooks_export_builder(client, &args.name)?;
    dialogflow_projects_locations_agents_playbooks_export_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_playbooks_get_execute()` to send, or `dialogflow_projects_locations_agents_playbooks_get` for simplest API.

pub fn dialogflow_projects_locations_agents_playbooks_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_playbooks_get_execute()` or `dialogflow_projects_locations_agents_playbooks_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_playbooks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_playbooks_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Playbook>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Playbook = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_playbooks_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_playbooks_get_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_playbooks_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_playbooks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_playbooks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Playbook>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_playbooks_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_playbooks_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPlaybooksGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_playbooks_get_builder()` + `dialogflow_projects_locations_agents_playbooks_get_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_playbooks_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_playbooks_get(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsPlaybooksGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Playbook>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_playbooks_get_builder(client, &args.name)?;
    dialogflow_projects_locations_agents_playbooks_get_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks:import
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_playbooks_import_execute()` to send, or `dialogflow_projects_locations_agents_playbooks_import` for simplest API.

pub fn dialogflow_projects_locations_agents_playbooks_import_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/playbooks:import",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks:import
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_playbooks_import_execute()` or `dialogflow_projects_locations_agents_playbooks_import`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_playbooks_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_playbooks_import_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks:import
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_playbooks_import_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_playbooks_import_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_playbooks_import()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_playbooks_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_playbooks_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_playbooks_import_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_playbooks_import`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPlaybooksImportArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks:import
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_playbooks_import_builder()` + `dialogflow_projects_locations_agents_playbooks_import_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_playbooks_import_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_playbooks_import(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsPlaybooksImportArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_playbooks_import_builder(client, &args.parent)?;
    dialogflow_projects_locations_agents_playbooks_import_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_playbooks_list_execute()` to send, or `dialogflow_projects_locations_agents_playbooks_list` for simplest API.

pub fn dialogflow_projects_locations_agents_playbooks_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/playbooks",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_playbooks_list_execute()` or `dialogflow_projects_locations_agents_playbooks_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_playbooks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_playbooks_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3ListPlaybooksResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListPlaybooksResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_playbooks_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_playbooks_list_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_playbooks_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_playbooks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_playbooks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListPlaybooksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_playbooks_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_playbooks_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPlaybooksListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_playbooks_list_builder()` + `dialogflow_projects_locations_agents_playbooks_list_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_playbooks_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_playbooks_list(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsPlaybooksListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListPlaybooksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_playbooks_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    dialogflow_projects_locations_agents_playbooks_list_execute(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_playbooks_patch_execute()` to send, or `dialogflow_projects_locations_agents_playbooks_patch` for simplest API.

pub fn dialogflow_projects_locations_agents_playbooks_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_playbooks_patch_execute()` or `dialogflow_projects_locations_agents_playbooks_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_playbooks_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_playbooks_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Playbook>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Playbook = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_playbooks_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_playbooks_patch_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_playbooks_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_playbooks_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_playbooks_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Playbook>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_playbooks_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_playbooks_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPlaybooksPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_playbooks_patch_builder()` + `dialogflow_projects_locations_agents_playbooks_patch_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_playbooks_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_playbooks_patch(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsPlaybooksPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Playbook>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_playbooks_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    dialogflow_projects_locations_agents_playbooks_patch_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/examples
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_playbooks_examples_create_execute()` to send, or `dialogflow_projects_locations_agents_playbooks_examples_create` for simplest API.

pub fn dialogflow_projects_locations_agents_playbooks_examples_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/examples",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/examples
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_playbooks_examples_create_execute()` or `dialogflow_projects_locations_agents_playbooks_examples_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_playbooks_examples_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_playbooks_examples_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Example>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Example = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/examples
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_playbooks_examples_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_playbooks_examples_create_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_playbooks_examples_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_playbooks_examples_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_playbooks_examples_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Example>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_playbooks_examples_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_playbooks_examples_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPlaybooksExamplesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/examples
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_playbooks_examples_create_builder()` + `dialogflow_projects_locations_agents_playbooks_examples_create_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_playbooks_examples_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_playbooks_examples_create(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsPlaybooksExamplesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Example>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_playbooks_examples_create_builder(
        client,
        &args.parent,
    )?;
    dialogflow_projects_locations_agents_playbooks_examples_create_execute(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/examples/{examplesId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_playbooks_examples_delete_execute()` to send, or `dialogflow_projects_locations_agents_playbooks_examples_delete` for simplest API.

pub fn dialogflow_projects_locations_agents_playbooks_examples_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/examples/{examplesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/examples/{examplesId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_playbooks_examples_delete_execute()` or `dialogflow_projects_locations_agents_playbooks_examples_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_playbooks_examples_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_playbooks_examples_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/examples/{examplesId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_playbooks_examples_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_playbooks_examples_delete_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_playbooks_examples_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_playbooks_examples_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_playbooks_examples_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_playbooks_examples_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_playbooks_examples_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPlaybooksExamplesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/examples/{examplesId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_playbooks_examples_delete_builder()` + `dialogflow_projects_locations_agents_playbooks_examples_delete_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_playbooks_examples_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_playbooks_examples_delete(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsPlaybooksExamplesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_playbooks_examples_delete_builder(client, &args.name)?;
    dialogflow_projects_locations_agents_playbooks_examples_delete_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/examples/{examplesId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_playbooks_examples_get_execute()` to send, or `dialogflow_projects_locations_agents_playbooks_examples_get` for simplest API.

pub fn dialogflow_projects_locations_agents_playbooks_examples_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/examples/{examplesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/examples/{examplesId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_playbooks_examples_get_execute()` or `dialogflow_projects_locations_agents_playbooks_examples_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_playbooks_examples_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_playbooks_examples_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Example>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Example = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/examples/{examplesId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_playbooks_examples_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_playbooks_examples_get_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_playbooks_examples_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_playbooks_examples_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_playbooks_examples_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Example>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_playbooks_examples_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_playbooks_examples_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPlaybooksExamplesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/examples/{examplesId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_playbooks_examples_get_builder()` + `dialogflow_projects_locations_agents_playbooks_examples_get_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_playbooks_examples_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_playbooks_examples_get(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsPlaybooksExamplesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Example>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_playbooks_examples_get_builder(client, &args.name)?;
    dialogflow_projects_locations_agents_playbooks_examples_get_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/examples
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_playbooks_examples_list_execute()` to send, or `dialogflow_projects_locations_agents_playbooks_examples_list` for simplest API.

pub fn dialogflow_projects_locations_agents_playbooks_examples_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    languageCode: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/examples",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/examples
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_playbooks_examples_list_execute()` or `dialogflow_projects_locations_agents_playbooks_examples_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_playbooks_examples_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_playbooks_examples_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3ListExamplesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListExamplesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/examples
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_playbooks_examples_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_playbooks_examples_list_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_playbooks_examples_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_playbooks_examples_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_playbooks_examples_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListExamplesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_playbooks_examples_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_playbooks_examples_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPlaybooksExamplesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/examples
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_playbooks_examples_list_builder()` + `dialogflow_projects_locations_agents_playbooks_examples_list_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_playbooks_examples_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_playbooks_examples_list(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsPlaybooksExamplesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListExamplesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_playbooks_examples_list_builder(
        client,
        &args.parent,
        &args.languageCode,
        &args.pageSize,
        &args.pageToken,
    )?;
    dialogflow_projects_locations_agents_playbooks_examples_list_execute(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/examples/{examplesId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_playbooks_examples_patch_execute()` to send, or `dialogflow_projects_locations_agents_playbooks_examples_patch` for simplest API.

pub fn dialogflow_projects_locations_agents_playbooks_examples_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/examples/{examplesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/examples/{examplesId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_playbooks_examples_patch_execute()` or `dialogflow_projects_locations_agents_playbooks_examples_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_playbooks_examples_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_playbooks_examples_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Example>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Example = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/examples/{examplesId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_playbooks_examples_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_playbooks_examples_patch_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_playbooks_examples_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_playbooks_examples_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_playbooks_examples_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Example>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_playbooks_examples_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_playbooks_examples_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPlaybooksExamplesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/examples/{examplesId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_playbooks_examples_patch_builder()` + `dialogflow_projects_locations_agents_playbooks_examples_patch_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_playbooks_examples_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_playbooks_examples_patch(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsPlaybooksExamplesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Example>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_playbooks_examples_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    dialogflow_projects_locations_agents_playbooks_examples_patch_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/versions
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_playbooks_versions_create_execute()` to send, or `dialogflow_projects_locations_agents_playbooks_versions_create` for simplest API.

pub fn dialogflow_projects_locations_agents_playbooks_versions_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/versions",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/versions
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_playbooks_versions_create_execute()` or `dialogflow_projects_locations_agents_playbooks_versions_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_playbooks_versions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_playbooks_versions_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3PlaybookVersion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3PlaybookVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/versions
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_playbooks_versions_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_playbooks_versions_create_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_playbooks_versions_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_playbooks_versions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_playbooks_versions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3PlaybookVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_playbooks_versions_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_playbooks_versions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPlaybooksVersionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/versions
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_playbooks_versions_create_builder()` + `dialogflow_projects_locations_agents_playbooks_versions_create_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_playbooks_versions_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_playbooks_versions_create(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsPlaybooksVersionsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3PlaybookVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_playbooks_versions_create_builder(
        client,
        &args.parent,
    )?;
    dialogflow_projects_locations_agents_playbooks_versions_create_execute(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/versions/{versionsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_playbooks_versions_delete_execute()` to send, or `dialogflow_projects_locations_agents_playbooks_versions_delete` for simplest API.

pub fn dialogflow_projects_locations_agents_playbooks_versions_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/versions/{versionsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/versions/{versionsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_playbooks_versions_delete_execute()` or `dialogflow_projects_locations_agents_playbooks_versions_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_playbooks_versions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_playbooks_versions_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/versions/{versionsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_playbooks_versions_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_playbooks_versions_delete_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_playbooks_versions_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_playbooks_versions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_playbooks_versions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_playbooks_versions_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_playbooks_versions_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPlaybooksVersionsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/versions/{versionsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_playbooks_versions_delete_builder()` + `dialogflow_projects_locations_agents_playbooks_versions_delete_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_playbooks_versions_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_playbooks_versions_delete(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsPlaybooksVersionsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_playbooks_versions_delete_builder(client, &args.name)?;
    dialogflow_projects_locations_agents_playbooks_versions_delete_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/versions/{versionsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_playbooks_versions_get_execute()` to send, or `dialogflow_projects_locations_agents_playbooks_versions_get` for simplest API.

pub fn dialogflow_projects_locations_agents_playbooks_versions_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/versions/{versionsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/versions/{versionsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_playbooks_versions_get_execute()` or `dialogflow_projects_locations_agents_playbooks_versions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_playbooks_versions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_playbooks_versions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3PlaybookVersion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3PlaybookVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/versions/{versionsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_playbooks_versions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_playbooks_versions_get_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_playbooks_versions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_playbooks_versions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_playbooks_versions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3PlaybookVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_playbooks_versions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_playbooks_versions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPlaybooksVersionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/versions/{versionsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_playbooks_versions_get_builder()` + `dialogflow_projects_locations_agents_playbooks_versions_get_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_playbooks_versions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_playbooks_versions_get(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsPlaybooksVersionsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3PlaybookVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_playbooks_versions_get_builder(client, &args.name)?;
    dialogflow_projects_locations_agents_playbooks_versions_get_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/versions
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_playbooks_versions_list_execute()` to send, or `dialogflow_projects_locations_agents_playbooks_versions_list` for simplest API.

pub fn dialogflow_projects_locations_agents_playbooks_versions_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/versions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/versions
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_playbooks_versions_list_execute()` or `dialogflow_projects_locations_agents_playbooks_versions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_playbooks_versions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_playbooks_versions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListPlaybookVersionsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListPlaybookVersionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/versions
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_playbooks_versions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_playbooks_versions_list_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_playbooks_versions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_playbooks_versions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_playbooks_versions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListPlaybookVersionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_playbooks_versions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_playbooks_versions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPlaybooksVersionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/versions
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_playbooks_versions_list_builder()` + `dialogflow_projects_locations_agents_playbooks_versions_list_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_playbooks_versions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_playbooks_versions_list(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsPlaybooksVersionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListPlaybookVersionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_playbooks_versions_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    dialogflow_projects_locations_agents_playbooks_versions_list_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/versions/{versionsId}:restore
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_playbooks_versions_restore_execute()` to send, or `dialogflow_projects_locations_agents_playbooks_versions_restore` for simplest API.

pub fn dialogflow_projects_locations_agents_playbooks_versions_restore_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/versions/{versionsId}:restore",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/versions/{versionsId}:restore
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_playbooks_versions_restore_execute()` or `dialogflow_projects_locations_agents_playbooks_versions_restore`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_playbooks_versions_restore_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_playbooks_versions_restore_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3RestorePlaybookVersionResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3RestorePlaybookVersionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/versions/{versionsId}:restore
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_playbooks_versions_restore_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_playbooks_versions_restore_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_playbooks_versions_restore()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_playbooks_versions_restore_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_playbooks_versions_restore_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDialogflowCxV3RestorePlaybookVersionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_playbooks_versions_restore_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_playbooks_versions_restore`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsPlaybooksVersionsRestoreArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/playbooks/{playbooksId}/versions/{versionsId}:restore
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_playbooks_versions_restore_builder()` + `dialogflow_projects_locations_agents_playbooks_versions_restore_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_playbooks_versions_restore_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_playbooks_versions_restore(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsPlaybooksVersionsRestoreArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDialogflowCxV3RestorePlaybookVersionResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_playbooks_versions_restore_builder(
        client, &args.name,
    )?;
    dialogflow_projects_locations_agents_playbooks_versions_restore_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}:detectIntent
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_sessions_detect_intent_execute()` to send, or `dialogflow_projects_locations_agents_sessions_detect_intent` for simplest API.

pub fn dialogflow_projects_locations_agents_sessions_detect_intent_builder<R>(
    client: &SimpleHttpClient<R>,
    session: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}:detectIntent",
        session,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}:detectIntent
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_sessions_detect_intent_execute()` or `dialogflow_projects_locations_agents_sessions_detect_intent`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_sessions_detect_intent_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_sessions_detect_intent_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3DetectIntentResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3DetectIntentResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}:detectIntent
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_sessions_detect_intent_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_sessions_detect_intent_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_sessions_detect_intent()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_sessions_detect_intent_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_sessions_detect_intent_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3DetectIntentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_sessions_detect_intent_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_sessions_detect_intent`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsSessionsDetectIntentArgs {
    /// Path parameter: session
    pub session: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}:detectIntent
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_sessions_detect_intent_builder()` + `dialogflow_projects_locations_agents_sessions_detect_intent_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_sessions_detect_intent_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_sessions_detect_intent(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsSessionsDetectIntentArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3DetectIntentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_sessions_detect_intent_builder(client, &args.session)?;
    dialogflow_projects_locations_agents_sessions_detect_intent_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}:fulfillIntent
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_sessions_fulfill_intent_execute()` to send, or `dialogflow_projects_locations_agents_sessions_fulfill_intent` for simplest API.

pub fn dialogflow_projects_locations_agents_sessions_fulfill_intent_builder<R>(
    client: &SimpleHttpClient<R>,
    session: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}:fulfillIntent",
        session,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}:fulfillIntent
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_sessions_fulfill_intent_execute()` or `dialogflow_projects_locations_agents_sessions_fulfill_intent`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_sessions_fulfill_intent_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_sessions_fulfill_intent_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3FulfillIntentResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3FulfillIntentResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}:fulfillIntent
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_sessions_fulfill_intent_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_sessions_fulfill_intent_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_sessions_fulfill_intent()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_sessions_fulfill_intent_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_sessions_fulfill_intent_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3FulfillIntentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_sessions_fulfill_intent_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_sessions_fulfill_intent`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsSessionsFulfillIntentArgs {
    /// Path parameter: session
    pub session: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}:fulfillIntent
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_sessions_fulfill_intent_builder()` + `dialogflow_projects_locations_agents_sessions_fulfill_intent_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_sessions_fulfill_intent_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_sessions_fulfill_intent(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsSessionsFulfillIntentArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3FulfillIntentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_sessions_fulfill_intent_builder(
        client,
        &args.session,
    )?;
    dialogflow_projects_locations_agents_sessions_fulfill_intent_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}:matchIntent
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_sessions_match_intent_execute()` to send, or `dialogflow_projects_locations_agents_sessions_match_intent` for simplest API.

pub fn dialogflow_projects_locations_agents_sessions_match_intent_builder<R>(
    client: &SimpleHttpClient<R>,
    session: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}:matchIntent",
        session,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}:matchIntent
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_sessions_match_intent_execute()` or `dialogflow_projects_locations_agents_sessions_match_intent`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_sessions_match_intent_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_sessions_match_intent_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3MatchIntentResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3MatchIntentResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}:matchIntent
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_sessions_match_intent_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_sessions_match_intent_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_sessions_match_intent()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_sessions_match_intent_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_sessions_match_intent_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3MatchIntentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_sessions_match_intent_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_sessions_match_intent`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsSessionsMatchIntentArgs {
    /// Path parameter: session
    pub session: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}:matchIntent
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_sessions_match_intent_builder()` + `dialogflow_projects_locations_agents_sessions_match_intent_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_sessions_match_intent_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_sessions_match_intent(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsSessionsMatchIntentArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3MatchIntentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_sessions_match_intent_builder(client, &args.session)?;
    dialogflow_projects_locations_agents_sessions_match_intent_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}:serverStreamingDetectIntent
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_sessions_server_streaming_detect_intent_execute()` to send, or `dialogflow_projects_locations_agents_sessions_server_streaming_detect_intent` for simplest API.

pub fn dialogflow_projects_locations_agents_sessions_server_streaming_detect_intent_builder<R>(
    client: &SimpleHttpClient<R>,
    session: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}:serverStreamingDetectIntent",
        session,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}:serverStreamingDetectIntent
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_sessions_server_streaming_detect_intent_execute()` or `dialogflow_projects_locations_agents_sessions_server_streaming_detect_intent`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_sessions_server_streaming_detect_intent_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_sessions_server_streaming_detect_intent_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3DetectIntentResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3DetectIntentResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}:serverStreamingDetectIntent
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_sessions_server_streaming_detect_intent_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_sessions_server_streaming_detect_intent_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_sessions_server_streaming_detect_intent()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_sessions_server_streaming_detect_intent_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_sessions_server_streaming_detect_intent_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3DetectIntentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        dialogflow_projects_locations_agents_sessions_server_streaming_detect_intent_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_sessions_server_streaming_detect_intent`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsSessionsServerStreamingDetectIntentArgs {
    /// Path parameter: session
    pub session: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}:serverStreamingDetectIntent
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_sessions_server_streaming_detect_intent_builder()` + `dialogflow_projects_locations_agents_sessions_server_streaming_detect_intent_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_sessions_server_streaming_detect_intent_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_sessions_server_streaming_detect_intent(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsSessionsServerStreamingDetectIntentArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3DetectIntentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_sessions_server_streaming_detect_intent_builder(
            client,
            &args.session,
        )?;
    dialogflow_projects_locations_agents_sessions_server_streaming_detect_intent_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}:submitAnswerFeedback
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_sessions_submit_answer_feedback_execute()` to send, or `dialogflow_projects_locations_agents_sessions_submit_answer_feedback` for simplest API.

pub fn dialogflow_projects_locations_agents_sessions_submit_answer_feedback_builder<R>(
    client: &SimpleHttpClient<R>,
    session: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}:submitAnswerFeedback",
        session,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}:submitAnswerFeedback
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_sessions_submit_answer_feedback_execute()` or `dialogflow_projects_locations_agents_sessions_submit_answer_feedback`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_sessions_submit_answer_feedback_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_sessions_submit_answer_feedback_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3AnswerFeedback>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3AnswerFeedback =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}:submitAnswerFeedback
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_sessions_submit_answer_feedback_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_sessions_submit_answer_feedback_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_sessions_submit_answer_feedback()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_sessions_submit_answer_feedback_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_sessions_submit_answer_feedback_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3AnswerFeedback>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_sessions_submit_answer_feedback_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_sessions_submit_answer_feedback`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsSessionsSubmitAnswerFeedbackArgs {
    /// Path parameter: session
    pub session: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}:submitAnswerFeedback
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_sessions_submit_answer_feedback_builder()` + `dialogflow_projects_locations_agents_sessions_submit_answer_feedback_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_sessions_submit_answer_feedback_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_sessions_submit_answer_feedback(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsSessionsSubmitAnswerFeedbackArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3AnswerFeedback>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_sessions_submit_answer_feedback_builder(
        client,
        &args.session,
    )?;
    dialogflow_projects_locations_agents_sessions_submit_answer_feedback_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}/entityTypes
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_sessions_entity_types_create_execute()` to send, or `dialogflow_projects_locations_agents_sessions_entity_types_create` for simplest API.

pub fn dialogflow_projects_locations_agents_sessions_entity_types_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}/entityTypes",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}/entityTypes
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_sessions_entity_types_create_execute()` or `dialogflow_projects_locations_agents_sessions_entity_types_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_sessions_entity_types_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_sessions_entity_types_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3SessionEntityType>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3SessionEntityType =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}/entityTypes
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_sessions_entity_types_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_sessions_entity_types_create_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_sessions_entity_types_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_sessions_entity_types_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_sessions_entity_types_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3SessionEntityType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_sessions_entity_types_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_sessions_entity_types_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsSessionsEntityTypesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}/entityTypes
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_sessions_entity_types_create_builder()` + `dialogflow_projects_locations_agents_sessions_entity_types_create_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_sessions_entity_types_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_sessions_entity_types_create(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsSessionsEntityTypesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3SessionEntityType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_sessions_entity_types_create_builder(
        client,
        &args.parent,
    )?;
    dialogflow_projects_locations_agents_sessions_entity_types_create_execute(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}/entityTypes/{entityTypesId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_sessions_entity_types_delete_execute()` to send, or `dialogflow_projects_locations_agents_sessions_entity_types_delete` for simplest API.

pub fn dialogflow_projects_locations_agents_sessions_entity_types_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}/entityTypes/{entityTypesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}/entityTypes/{entityTypesId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_sessions_entity_types_delete_execute()` or `dialogflow_projects_locations_agents_sessions_entity_types_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_sessions_entity_types_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_sessions_entity_types_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}/entityTypes/{entityTypesId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_sessions_entity_types_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_sessions_entity_types_delete_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_sessions_entity_types_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_sessions_entity_types_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_sessions_entity_types_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_sessions_entity_types_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_sessions_entity_types_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsSessionsEntityTypesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}/entityTypes/{entityTypesId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_sessions_entity_types_delete_builder()` + `dialogflow_projects_locations_agents_sessions_entity_types_delete_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_sessions_entity_types_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_sessions_entity_types_delete(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsSessionsEntityTypesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_sessions_entity_types_delete_builder(
        client, &args.name,
    )?;
    dialogflow_projects_locations_agents_sessions_entity_types_delete_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}/entityTypes/{entityTypesId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_sessions_entity_types_get_execute()` to send, or `dialogflow_projects_locations_agents_sessions_entity_types_get` for simplest API.

pub fn dialogflow_projects_locations_agents_sessions_entity_types_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}/entityTypes/{entityTypesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}/entityTypes/{entityTypesId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_sessions_entity_types_get_execute()` or `dialogflow_projects_locations_agents_sessions_entity_types_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_sessions_entity_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_sessions_entity_types_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3SessionEntityType>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3SessionEntityType =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}/entityTypes/{entityTypesId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_sessions_entity_types_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_sessions_entity_types_get_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_sessions_entity_types_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_sessions_entity_types_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_sessions_entity_types_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3SessionEntityType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_sessions_entity_types_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_sessions_entity_types_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsSessionsEntityTypesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}/entityTypes/{entityTypesId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_sessions_entity_types_get_builder()` + `dialogflow_projects_locations_agents_sessions_entity_types_get_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_sessions_entity_types_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_sessions_entity_types_get(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsSessionsEntityTypesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3SessionEntityType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_sessions_entity_types_get_builder(client, &args.name)?;
    dialogflow_projects_locations_agents_sessions_entity_types_get_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}/entityTypes
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_sessions_entity_types_list_execute()` to send, or `dialogflow_projects_locations_agents_sessions_entity_types_list` for simplest API.

pub fn dialogflow_projects_locations_agents_sessions_entity_types_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}/entityTypes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}/entityTypes
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_sessions_entity_types_list_execute()` or `dialogflow_projects_locations_agents_sessions_entity_types_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_sessions_entity_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_sessions_entity_types_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListSessionEntityTypesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListSessionEntityTypesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}/entityTypes
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_sessions_entity_types_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_sessions_entity_types_list_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_sessions_entity_types_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_sessions_entity_types_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_sessions_entity_types_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListSessionEntityTypesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_sessions_entity_types_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_sessions_entity_types_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsSessionsEntityTypesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}/entityTypes
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_sessions_entity_types_list_builder()` + `dialogflow_projects_locations_agents_sessions_entity_types_list_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_sessions_entity_types_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_sessions_entity_types_list(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsSessionsEntityTypesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListSessionEntityTypesResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_sessions_entity_types_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    dialogflow_projects_locations_agents_sessions_entity_types_list_execute(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}/entityTypes/{entityTypesId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_sessions_entity_types_patch_execute()` to send, or `dialogflow_projects_locations_agents_sessions_entity_types_patch` for simplest API.

pub fn dialogflow_projects_locations_agents_sessions_entity_types_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}/entityTypes/{entityTypesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}/entityTypes/{entityTypesId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_sessions_entity_types_patch_execute()` or `dialogflow_projects_locations_agents_sessions_entity_types_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_sessions_entity_types_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_sessions_entity_types_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3SessionEntityType>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3SessionEntityType =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}/entityTypes/{entityTypesId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_sessions_entity_types_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_sessions_entity_types_patch_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_sessions_entity_types_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_sessions_entity_types_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_sessions_entity_types_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3SessionEntityType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_sessions_entity_types_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_sessions_entity_types_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsSessionsEntityTypesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/sessions/{sessionsId}/entityTypes/{entityTypesId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_sessions_entity_types_patch_builder()` + `dialogflow_projects_locations_agents_sessions_entity_types_patch_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_sessions_entity_types_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_sessions_entity_types_patch(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsSessionsEntityTypesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3SessionEntityType>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_sessions_entity_types_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    dialogflow_projects_locations_agents_sessions_entity_types_patch_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases:batchDelete
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_test_cases_batch_delete_execute()` to send, or `dialogflow_projects_locations_agents_test_cases_batch_delete` for simplest API.

pub fn dialogflow_projects_locations_agents_test_cases_batch_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/testCases:batchDelete",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases:batchDelete
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_test_cases_batch_delete_execute()` or `dialogflow_projects_locations_agents_test_cases_batch_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_test_cases_batch_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_test_cases_batch_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases:batchDelete
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_test_cases_batch_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_test_cases_batch_delete_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_test_cases_batch_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_test_cases_batch_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_test_cases_batch_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_test_cases_batch_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_test_cases_batch_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsTestCasesBatchDeleteArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases:batchDelete
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_test_cases_batch_delete_builder()` + `dialogflow_projects_locations_agents_test_cases_batch_delete_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_test_cases_batch_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_test_cases_batch_delete(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsTestCasesBatchDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_test_cases_batch_delete_builder(client, &args.parent)?;
    dialogflow_projects_locations_agents_test_cases_batch_delete_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases:batchRun
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_test_cases_batch_run_execute()` to send, or `dialogflow_projects_locations_agents_test_cases_batch_run` for simplest API.

pub fn dialogflow_projects_locations_agents_test_cases_batch_run_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/testCases:batchRun",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases:batchRun
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_test_cases_batch_run_execute()` or `dialogflow_projects_locations_agents_test_cases_batch_run`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_test_cases_batch_run_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_test_cases_batch_run_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases:batchRun
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_test_cases_batch_run_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_test_cases_batch_run_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_test_cases_batch_run()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_test_cases_batch_run_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_test_cases_batch_run_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_test_cases_batch_run_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_test_cases_batch_run`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsTestCasesBatchRunArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases:batchRun
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_test_cases_batch_run_builder()` + `dialogflow_projects_locations_agents_test_cases_batch_run_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_test_cases_batch_run_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_test_cases_batch_run(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsTestCasesBatchRunArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_test_cases_batch_run_builder(client, &args.parent)?;
    dialogflow_projects_locations_agents_test_cases_batch_run_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases:calculateCoverage
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_test_cases_calculate_coverage_execute()` to send, or `dialogflow_projects_locations_agents_test_cases_calculate_coverage` for simplest API.

pub fn dialogflow_projects_locations_agents_test_cases_calculate_coverage_builder<R>(
    client: &SimpleHttpClient<R>,
    agent: &String,
    type_rs: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/testCases:calculateCoverage",
        agent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = type_rs.as_ref() {
        query_parts.push(format!("type={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases:calculateCoverage
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_test_cases_calculate_coverage_execute()` or `dialogflow_projects_locations_agents_test_cases_calculate_coverage`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_test_cases_calculate_coverage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_test_cases_calculate_coverage_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3CalculateCoverageResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3CalculateCoverageResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases:calculateCoverage
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_test_cases_calculate_coverage_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_test_cases_calculate_coverage_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_test_cases_calculate_coverage()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_test_cases_calculate_coverage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_test_cases_calculate_coverage_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3CalculateCoverageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_test_cases_calculate_coverage_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_test_cases_calculate_coverage`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsTestCasesCalculateCoverageArgs {
    /// Path parameter: agent
    pub agent: String,
    /// Query parameter: type
    pub type_rs: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases:calculateCoverage
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_test_cases_calculate_coverage_builder()` + `dialogflow_projects_locations_agents_test_cases_calculate_coverage_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_test_cases_calculate_coverage_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_test_cases_calculate_coverage(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsTestCasesCalculateCoverageArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3CalculateCoverageResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_test_cases_calculate_coverage_builder(
        client,
        &args.agent,
        &args.type_rs,
    )?;
    dialogflow_projects_locations_agents_test_cases_calculate_coverage_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_test_cases_create_execute()` to send, or `dialogflow_projects_locations_agents_test_cases_create` for simplest API.

pub fn dialogflow_projects_locations_agents_test_cases_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/testCases",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_test_cases_create_execute()` or `dialogflow_projects_locations_agents_test_cases_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_test_cases_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_test_cases_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3TestCase>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3TestCase = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_test_cases_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_test_cases_create_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_test_cases_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_test_cases_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_test_cases_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3TestCase>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_test_cases_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_test_cases_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsTestCasesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_test_cases_create_builder()` + `dialogflow_projects_locations_agents_test_cases_create_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_test_cases_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_test_cases_create(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsTestCasesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3TestCase>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_test_cases_create_builder(client, &args.parent)?;
    dialogflow_projects_locations_agents_test_cases_create_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases:export
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_test_cases_export_execute()` to send, or `dialogflow_projects_locations_agents_test_cases_export` for simplest API.

pub fn dialogflow_projects_locations_agents_test_cases_export_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/testCases:export",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases:export
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_test_cases_export_execute()` or `dialogflow_projects_locations_agents_test_cases_export`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_test_cases_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_test_cases_export_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases:export
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_test_cases_export_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_test_cases_export_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_test_cases_export()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_test_cases_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_test_cases_export_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_test_cases_export_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_test_cases_export`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsTestCasesExportArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases:export
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_test_cases_export_builder()` + `dialogflow_projects_locations_agents_test_cases_export_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_test_cases_export_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_test_cases_export(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsTestCasesExportArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_test_cases_export_builder(client, &args.parent)?;
    dialogflow_projects_locations_agents_test_cases_export_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases/{testCasesId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_test_cases_get_execute()` to send, or `dialogflow_projects_locations_agents_test_cases_get` for simplest API.

pub fn dialogflow_projects_locations_agents_test_cases_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/testCases/{testCasesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases/{testCasesId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_test_cases_get_execute()` or `dialogflow_projects_locations_agents_test_cases_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_test_cases_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_test_cases_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3TestCase>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3TestCase = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases/{testCasesId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_test_cases_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_test_cases_get_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_test_cases_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_test_cases_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_test_cases_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3TestCase>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_test_cases_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_test_cases_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsTestCasesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases/{testCasesId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_test_cases_get_builder()` + `dialogflow_projects_locations_agents_test_cases_get_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_test_cases_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_test_cases_get(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsTestCasesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3TestCase>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_test_cases_get_builder(client, &args.name)?;
    dialogflow_projects_locations_agents_test_cases_get_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases:import
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_test_cases_import_execute()` to send, or `dialogflow_projects_locations_agents_test_cases_import` for simplest API.

pub fn dialogflow_projects_locations_agents_test_cases_import_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/testCases:import",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases:import
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_test_cases_import_execute()` or `dialogflow_projects_locations_agents_test_cases_import`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_test_cases_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_test_cases_import_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases:import
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_test_cases_import_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_test_cases_import_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_test_cases_import()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_test_cases_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_test_cases_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_test_cases_import_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_test_cases_import`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsTestCasesImportArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases:import
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_test_cases_import_builder()` + `dialogflow_projects_locations_agents_test_cases_import_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_test_cases_import_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_test_cases_import(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsTestCasesImportArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_test_cases_import_builder(client, &args.parent)?;
    dialogflow_projects_locations_agents_test_cases_import_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_test_cases_list_execute()` to send, or `dialogflow_projects_locations_agents_test_cases_list` for simplest API.

pub fn dialogflow_projects_locations_agents_test_cases_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    view: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/testCases",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_test_cases_list_execute()` or `dialogflow_projects_locations_agents_test_cases_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_test_cases_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_test_cases_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3ListTestCasesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListTestCasesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_test_cases_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_test_cases_list_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_test_cases_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_test_cases_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_test_cases_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListTestCasesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_test_cases_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_test_cases_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsTestCasesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_test_cases_list_builder()` + `dialogflow_projects_locations_agents_test_cases_list_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_test_cases_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_test_cases_list(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsTestCasesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListTestCasesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_test_cases_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
        &args.view,
    )?;
    dialogflow_projects_locations_agents_test_cases_list_execute(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases/{testCasesId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_test_cases_patch_execute()` to send, or `dialogflow_projects_locations_agents_test_cases_patch` for simplest API.

pub fn dialogflow_projects_locations_agents_test_cases_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/testCases/{testCasesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases/{testCasesId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_test_cases_patch_execute()` or `dialogflow_projects_locations_agents_test_cases_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_test_cases_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_test_cases_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3TestCase>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3TestCase = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases/{testCasesId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_test_cases_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_test_cases_patch_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_test_cases_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_test_cases_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_test_cases_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3TestCase>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_test_cases_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_test_cases_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsTestCasesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases/{testCasesId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_test_cases_patch_builder()` + `dialogflow_projects_locations_agents_test_cases_patch_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_test_cases_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_test_cases_patch(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsTestCasesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3TestCase>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_test_cases_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    dialogflow_projects_locations_agents_test_cases_patch_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases/{testCasesId}:run
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_test_cases_run_execute()` to send, or `dialogflow_projects_locations_agents_test_cases_run` for simplest API.

pub fn dialogflow_projects_locations_agents_test_cases_run_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/testCases/{testCasesId}:run",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases/{testCasesId}:run
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_test_cases_run_execute()` or `dialogflow_projects_locations_agents_test_cases_run`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_test_cases_run_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_test_cases_run_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases/{testCasesId}:run
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_test_cases_run_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_test_cases_run_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_test_cases_run()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_test_cases_run_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_test_cases_run_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_test_cases_run_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_test_cases_run`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsTestCasesRunArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases/{testCasesId}:run
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_test_cases_run_builder()` + `dialogflow_projects_locations_agents_test_cases_run_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_test_cases_run_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_test_cases_run(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsTestCasesRunArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_test_cases_run_builder(client, &args.name)?;
    dialogflow_projects_locations_agents_test_cases_run_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases/{testCasesId}/results/{resultsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_test_cases_results_get_execute()` to send, or `dialogflow_projects_locations_agents_test_cases_results_get` for simplest API.

pub fn dialogflow_projects_locations_agents_test_cases_results_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/testCases/{testCasesId}/results/{resultsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases/{testCasesId}/results/{resultsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_test_cases_results_get_execute()` or `dialogflow_projects_locations_agents_test_cases_results_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_test_cases_results_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_test_cases_results_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3TestCaseResult>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3TestCaseResult =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases/{testCasesId}/results/{resultsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_test_cases_results_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_test_cases_results_get_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_test_cases_results_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_test_cases_results_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_test_cases_results_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3TestCaseResult>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_test_cases_results_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_test_cases_results_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsTestCasesResultsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases/{testCasesId}/results/{resultsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_test_cases_results_get_builder()` + `dialogflow_projects_locations_agents_test_cases_results_get_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_test_cases_results_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_test_cases_results_get(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsTestCasesResultsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3TestCaseResult>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_test_cases_results_get_builder(client, &args.name)?;
    dialogflow_projects_locations_agents_test_cases_results_get_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases/{testCasesId}/results
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_test_cases_results_list_execute()` to send, or `dialogflow_projects_locations_agents_test_cases_results_list` for simplest API.

pub fn dialogflow_projects_locations_agents_test_cases_results_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/testCases/{testCasesId}/results",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases/{testCasesId}/results
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_test_cases_results_list_execute()` or `dialogflow_projects_locations_agents_test_cases_results_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_test_cases_results_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_test_cases_results_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListTestCaseResultsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListTestCaseResultsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases/{testCasesId}/results
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_test_cases_results_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_test_cases_results_list_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_test_cases_results_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_test_cases_results_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_test_cases_results_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListTestCaseResultsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_test_cases_results_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_test_cases_results_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsTestCasesResultsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/testCases/{testCasesId}/results
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_test_cases_results_list_builder()` + `dialogflow_projects_locations_agents_test_cases_results_list_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_test_cases_results_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_test_cases_results_list(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsTestCasesResultsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListTestCaseResultsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_test_cases_results_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    dialogflow_projects_locations_agents_test_cases_results_list_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_tools_create_execute()` to send, or `dialogflow_projects_locations_agents_tools_create` for simplest API.

pub fn dialogflow_projects_locations_agents_tools_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/tools",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_tools_create_execute()` or `dialogflow_projects_locations_agents_tools_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_tools_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_tools_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Tool>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Tool = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_tools_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_tools_create_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_tools_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_tools_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_tools_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Tool>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_tools_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_tools_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsToolsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_tools_create_builder()` + `dialogflow_projects_locations_agents_tools_create_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_tools_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_tools_create(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsToolsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Tool>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_tools_create_builder(client, &args.parent)?;
    dialogflow_projects_locations_agents_tools_create_execute(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_tools_delete_execute()` to send, or `dialogflow_projects_locations_agents_tools_delete` for simplest API.

pub fn dialogflow_projects_locations_agents_tools_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    force: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force.as_ref() {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_tools_delete_execute()` or `dialogflow_projects_locations_agents_tools_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_tools_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_tools_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_tools_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_tools_delete_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_tools_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_tools_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_tools_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_tools_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_tools_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsToolsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<String>,
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_tools_delete_builder()` + `dialogflow_projects_locations_agents_tools_delete_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_tools_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_tools_delete(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsToolsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_tools_delete_builder(client, &args.name, &args.force)?;
    dialogflow_projects_locations_agents_tools_delete_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_tools_get_execute()` to send, or `dialogflow_projects_locations_agents_tools_get` for simplest API.

pub fn dialogflow_projects_locations_agents_tools_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_tools_get_execute()` or `dialogflow_projects_locations_agents_tools_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_tools_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_tools_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Tool>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Tool = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_tools_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_tools_get_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_tools_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_tools_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_tools_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Tool>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_tools_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_tools_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsToolsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_tools_get_builder()` + `dialogflow_projects_locations_agents_tools_get_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_tools_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_tools_get(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsToolsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Tool>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_tools_get_builder(client, &args.name)?;
    dialogflow_projects_locations_agents_tools_get_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_tools_list_execute()` to send, or `dialogflow_projects_locations_agents_tools_list` for simplest API.

pub fn dialogflow_projects_locations_agents_tools_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/tools",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_tools_list_execute()` or `dialogflow_projects_locations_agents_tools_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_tools_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_tools_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3ListToolsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListToolsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_tools_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_tools_list_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_tools_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_tools_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_tools_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListToolsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_tools_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_tools_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsToolsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_tools_list_builder()` + `dialogflow_projects_locations_agents_tools_list_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_tools_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_tools_list(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsToolsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListToolsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_tools_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    dialogflow_projects_locations_agents_tools_list_execute(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_tools_patch_execute()` to send, or `dialogflow_projects_locations_agents_tools_patch` for simplest API.

pub fn dialogflow_projects_locations_agents_tools_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_tools_patch_execute()` or `dialogflow_projects_locations_agents_tools_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_tools_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_tools_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Tool>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Tool = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_tools_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_tools_patch_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_tools_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_tools_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_tools_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Tool>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_tools_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_tools_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsToolsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_tools_patch_builder()` + `dialogflow_projects_locations_agents_tools_patch_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_tools_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_tools_patch(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsToolsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Tool>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_tools_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    dialogflow_projects_locations_agents_tools_patch_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}/versions
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_tools_versions_create_execute()` to send, or `dialogflow_projects_locations_agents_tools_versions_create` for simplest API.

pub fn dialogflow_projects_locations_agents_tools_versions_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}/versions",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}/versions
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_tools_versions_create_execute()` or `dialogflow_projects_locations_agents_tools_versions_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_tools_versions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_tools_versions_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3ToolVersion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ToolVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}/versions
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_tools_versions_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_tools_versions_create_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_tools_versions_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_tools_versions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_tools_versions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ToolVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_tools_versions_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_tools_versions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsToolsVersionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}/versions
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_tools_versions_create_builder()` + `dialogflow_projects_locations_agents_tools_versions_create_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_tools_versions_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_tools_versions_create(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsToolsVersionsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ToolVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_tools_versions_create_builder(client, &args.parent)?;
    dialogflow_projects_locations_agents_tools_versions_create_execute(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}/versions/{versionsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_tools_versions_delete_execute()` to send, or `dialogflow_projects_locations_agents_tools_versions_delete` for simplest API.

pub fn dialogflow_projects_locations_agents_tools_versions_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    force: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}/versions/{versionsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force.as_ref() {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}/versions/{versionsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_tools_versions_delete_execute()` or `dialogflow_projects_locations_agents_tools_versions_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_tools_versions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_tools_versions_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}/versions/{versionsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_tools_versions_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_tools_versions_delete_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_tools_versions_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_tools_versions_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_tools_versions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_tools_versions_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_tools_versions_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsToolsVersionsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<String>,
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}/versions/{versionsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_tools_versions_delete_builder()` + `dialogflow_projects_locations_agents_tools_versions_delete_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_tools_versions_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_tools_versions_delete(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsToolsVersionsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_tools_versions_delete_builder(
        client,
        &args.name,
        &args.force,
    )?;
    dialogflow_projects_locations_agents_tools_versions_delete_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}/versions/{versionsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_tools_versions_get_execute()` to send, or `dialogflow_projects_locations_agents_tools_versions_get` for simplest API.

pub fn dialogflow_projects_locations_agents_tools_versions_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}/versions/{versionsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}/versions/{versionsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_tools_versions_get_execute()` or `dialogflow_projects_locations_agents_tools_versions_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_tools_versions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_tools_versions_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3ToolVersion>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ToolVersion = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}/versions/{versionsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_tools_versions_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_tools_versions_get_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_tools_versions_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_tools_versions_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_tools_versions_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ToolVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_tools_versions_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_tools_versions_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsToolsVersionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}/versions/{versionsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_tools_versions_get_builder()` + `dialogflow_projects_locations_agents_tools_versions_get_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_tools_versions_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_tools_versions_get(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsToolsVersionsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ToolVersion>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_tools_versions_get_builder(client, &args.name)?;
    dialogflow_projects_locations_agents_tools_versions_get_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}/versions
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_tools_versions_list_execute()` to send, or `dialogflow_projects_locations_agents_tools_versions_list` for simplest API.

pub fn dialogflow_projects_locations_agents_tools_versions_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}/versions",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}/versions
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_tools_versions_list_execute()` or `dialogflow_projects_locations_agents_tools_versions_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_tools_versions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_tools_versions_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListToolVersionsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListToolVersionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}/versions
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_tools_versions_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_tools_versions_list_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_tools_versions_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_tools_versions_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_tools_versions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListToolVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_tools_versions_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_tools_versions_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsToolsVersionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}/versions
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_tools_versions_list_builder()` + `dialogflow_projects_locations_agents_tools_versions_list_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_tools_versions_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_tools_versions_list(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsToolsVersionsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListToolVersionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_tools_versions_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    dialogflow_projects_locations_agents_tools_versions_list_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}/versions/{versionsId}:restore
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_tools_versions_restore_execute()` to send, or `dialogflow_projects_locations_agents_tools_versions_restore` for simplest API.

pub fn dialogflow_projects_locations_agents_tools_versions_restore_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}/versions/{versionsId}:restore",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}/versions/{versionsId}:restore
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_tools_versions_restore_execute()` or `dialogflow_projects_locations_agents_tools_versions_restore`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_tools_versions_restore_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_tools_versions_restore_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3RestoreToolVersionResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3RestoreToolVersionResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}/versions/{versionsId}:restore
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_tools_versions_restore_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_tools_versions_restore_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_tools_versions_restore()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_tools_versions_restore_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_tools_versions_restore_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3RestoreToolVersionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_tools_versions_restore_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_tools_versions_restore`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsToolsVersionsRestoreArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/tools/{toolsId}/versions/{versionsId}:restore
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_tools_versions_restore_builder()` + `dialogflow_projects_locations_agents_tools_versions_restore_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_tools_versions_restore_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_tools_versions_restore(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsToolsVersionsRestoreArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3RestoreToolVersionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_tools_versions_restore_builder(client, &args.name)?;
    dialogflow_projects_locations_agents_tools_versions_restore_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/transitionRouteGroups
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_transition_route_groups_create_execute()` to send, or `dialogflow_projects_locations_agents_transition_route_groups_create` for simplest API.

pub fn dialogflow_projects_locations_agents_transition_route_groups_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    languageCode: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/transitionRouteGroups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/transitionRouteGroups
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_transition_route_groups_create_execute()` or `dialogflow_projects_locations_agents_transition_route_groups_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_transition_route_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_transition_route_groups_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3TransitionRouteGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3TransitionRouteGroup =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/transitionRouteGroups
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_transition_route_groups_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_transition_route_groups_create_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_transition_route_groups_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_transition_route_groups_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_transition_route_groups_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3TransitionRouteGroup>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_transition_route_groups_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_transition_route_groups_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsTransitionRouteGroupsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/transitionRouteGroups
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_transition_route_groups_create_builder()` + `dialogflow_projects_locations_agents_transition_route_groups_create_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_transition_route_groups_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_transition_route_groups_create(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsTransitionRouteGroupsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3TransitionRouteGroup>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_transition_route_groups_create_builder(
        client,
        &args.parent,
        &args.languageCode,
    )?;
    dialogflow_projects_locations_agents_transition_route_groups_create_execute(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/transitionRouteGroups/{transitionRouteGroupsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_transition_route_groups_delete_execute()` to send, or `dialogflow_projects_locations_agents_transition_route_groups_delete` for simplest API.

pub fn dialogflow_projects_locations_agents_transition_route_groups_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    force: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/transitionRouteGroups/{transitionRouteGroupsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force.as_ref() {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/transitionRouteGroups/{transitionRouteGroupsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_transition_route_groups_delete_execute()` or `dialogflow_projects_locations_agents_transition_route_groups_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_transition_route_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_transition_route_groups_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/transitionRouteGroups/{transitionRouteGroupsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_transition_route_groups_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_transition_route_groups_delete_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_transition_route_groups_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_transition_route_groups_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_transition_route_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_transition_route_groups_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_transition_route_groups_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsTransitionRouteGroupsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<String>,
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/transitionRouteGroups/{transitionRouteGroupsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_transition_route_groups_delete_builder()` + `dialogflow_projects_locations_agents_transition_route_groups_delete_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_transition_route_groups_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_transition_route_groups_delete(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsTransitionRouteGroupsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_transition_route_groups_delete_builder(
        client,
        &args.name,
        &args.force,
    )?;
    dialogflow_projects_locations_agents_transition_route_groups_delete_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/transitionRouteGroups/{transitionRouteGroupsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_transition_route_groups_get_execute()` to send, or `dialogflow_projects_locations_agents_transition_route_groups_get` for simplest API.

pub fn dialogflow_projects_locations_agents_transition_route_groups_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    languageCode: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/transitionRouteGroups/{transitionRouteGroupsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/transitionRouteGroups/{transitionRouteGroupsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_transition_route_groups_get_execute()` or `dialogflow_projects_locations_agents_transition_route_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_transition_route_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_transition_route_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3TransitionRouteGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3TransitionRouteGroup =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/transitionRouteGroups/{transitionRouteGroupsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_transition_route_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_transition_route_groups_get_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_transition_route_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_transition_route_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_transition_route_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3TransitionRouteGroup>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_transition_route_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_transition_route_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsTransitionRouteGroupsGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/transitionRouteGroups/{transitionRouteGroupsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_transition_route_groups_get_builder()` + `dialogflow_projects_locations_agents_transition_route_groups_get_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_transition_route_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_transition_route_groups_get(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsTransitionRouteGroupsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3TransitionRouteGroup>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_transition_route_groups_get_builder(
        client,
        &args.name,
        &args.languageCode,
    )?;
    dialogflow_projects_locations_agents_transition_route_groups_get_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/transitionRouteGroups
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_transition_route_groups_list_execute()` to send, or `dialogflow_projects_locations_agents_transition_route_groups_list` for simplest API.

pub fn dialogflow_projects_locations_agents_transition_route_groups_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    languageCode: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/transitionRouteGroups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/transitionRouteGroups
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_transition_route_groups_list_execute()` or `dialogflow_projects_locations_agents_transition_route_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_transition_route_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_transition_route_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListTransitionRouteGroupsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListTransitionRouteGroupsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/transitionRouteGroups
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_transition_route_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_transition_route_groups_list_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_transition_route_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_transition_route_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_transition_route_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListTransitionRouteGroupsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_transition_route_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_transition_route_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsTransitionRouteGroupsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/transitionRouteGroups
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_transition_route_groups_list_builder()` + `dialogflow_projects_locations_agents_transition_route_groups_list_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_transition_route_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_transition_route_groups_list(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsTransitionRouteGroupsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListTransitionRouteGroupsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_transition_route_groups_list_builder(
        client,
        &args.parent,
        &args.languageCode,
        &args.pageSize,
        &args.pageToken,
    )?;
    dialogflow_projects_locations_agents_transition_route_groups_list_execute(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/transitionRouteGroups/{transitionRouteGroupsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_transition_route_groups_patch_execute()` to send, or `dialogflow_projects_locations_agents_transition_route_groups_patch` for simplest API.

pub fn dialogflow_projects_locations_agents_transition_route_groups_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    languageCode: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/transitionRouteGroups/{transitionRouteGroupsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = languageCode.as_ref() {
        query_parts.push(format!("languageCode={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/transitionRouteGroups/{transitionRouteGroupsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_transition_route_groups_patch_execute()` or `dialogflow_projects_locations_agents_transition_route_groups_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_transition_route_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_transition_route_groups_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3TransitionRouteGroup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3TransitionRouteGroup =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/transitionRouteGroups/{transitionRouteGroupsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_transition_route_groups_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_transition_route_groups_patch_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_transition_route_groups_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_transition_route_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_transition_route_groups_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3TransitionRouteGroup>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_transition_route_groups_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_transition_route_groups_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsTransitionRouteGroupsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: languageCode
    pub languageCode: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/transitionRouteGroups/{transitionRouteGroupsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_transition_route_groups_patch_builder()` + `dialogflow_projects_locations_agents_transition_route_groups_patch_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_transition_route_groups_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_transition_route_groups_patch(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsTransitionRouteGroupsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3TransitionRouteGroup>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_transition_route_groups_patch_builder(
        client,
        &args.name,
        &args.languageCode,
        &args.updateMask,
    )?;
    dialogflow_projects_locations_agents_transition_route_groups_patch_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/webhooks
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_webhooks_create_execute()` to send, or `dialogflow_projects_locations_agents_webhooks_create` for simplest API.

pub fn dialogflow_projects_locations_agents_webhooks_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/webhooks",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/webhooks
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_webhooks_create_execute()` or `dialogflow_projects_locations_agents_webhooks_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_webhooks_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_webhooks_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Webhook>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Webhook = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/webhooks
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_webhooks_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_webhooks_create_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_webhooks_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_webhooks_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_webhooks_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Webhook>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_webhooks_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_webhooks_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsWebhooksCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/webhooks
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_webhooks_create_builder()` + `dialogflow_projects_locations_agents_webhooks_create_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_webhooks_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_webhooks_create(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsWebhooksCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Webhook>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_agents_webhooks_create_builder(client, &args.parent)?;
    dialogflow_projects_locations_agents_webhooks_create_execute(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/webhooks/{webhooksId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_webhooks_delete_execute()` to send, or `dialogflow_projects_locations_agents_webhooks_delete` for simplest API.

pub fn dialogflow_projects_locations_agents_webhooks_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    force: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/webhooks/{webhooksId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = force.as_ref() {
        query_parts.push(format!("force={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/webhooks/{webhooksId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_webhooks_delete_execute()` or `dialogflow_projects_locations_agents_webhooks_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_webhooks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_webhooks_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/webhooks/{webhooksId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_webhooks_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_webhooks_delete_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_webhooks_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_webhooks_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_webhooks_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_webhooks_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_webhooks_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsWebhooksDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: force
    pub force: Option<String>,
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/webhooks/{webhooksId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_webhooks_delete_builder()` + `dialogflow_projects_locations_agents_webhooks_delete_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_webhooks_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_webhooks_delete(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsWebhooksDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_webhooks_delete_builder(
        client,
        &args.name,
        &args.force,
    )?;
    dialogflow_projects_locations_agents_webhooks_delete_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/webhooks/{webhooksId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_webhooks_get_execute()` to send, or `dialogflow_projects_locations_agents_webhooks_get` for simplest API.

pub fn dialogflow_projects_locations_agents_webhooks_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/webhooks/{webhooksId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/webhooks/{webhooksId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_webhooks_get_execute()` or `dialogflow_projects_locations_agents_webhooks_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_webhooks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_webhooks_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Webhook>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Webhook = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/webhooks/{webhooksId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_webhooks_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_webhooks_get_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_webhooks_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_webhooks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_webhooks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Webhook>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_webhooks_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_webhooks_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsWebhooksGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/webhooks/{webhooksId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_webhooks_get_builder()` + `dialogflow_projects_locations_agents_webhooks_get_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_webhooks_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_webhooks_get(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsWebhooksGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Webhook>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_webhooks_get_builder(client, &args.name)?;
    dialogflow_projects_locations_agents_webhooks_get_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/webhooks
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_webhooks_list_execute()` to send, or `dialogflow_projects_locations_agents_webhooks_list` for simplest API.

pub fn dialogflow_projects_locations_agents_webhooks_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/webhooks",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/webhooks
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_webhooks_list_execute()` or `dialogflow_projects_locations_agents_webhooks_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_webhooks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_webhooks_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3ListWebhooksResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListWebhooksResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/webhooks
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_webhooks_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_webhooks_list_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_webhooks_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_webhooks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_webhooks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListWebhooksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_webhooks_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_webhooks_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsWebhooksListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/webhooks
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_webhooks_list_builder()` + `dialogflow_projects_locations_agents_webhooks_list_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_webhooks_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_webhooks_list(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsWebhooksListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3ListWebhooksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_webhooks_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    dialogflow_projects_locations_agents_webhooks_list_execute(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/webhooks/{webhooksId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_agents_webhooks_patch_execute()` to send, or `dialogflow_projects_locations_agents_webhooks_patch` for simplest API.

pub fn dialogflow_projects_locations_agents_webhooks_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/agents/{agentsId}/webhooks/{webhooksId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/webhooks/{webhooksId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_agents_webhooks_patch_execute()` or `dialogflow_projects_locations_agents_webhooks_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_webhooks_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_webhooks_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3Webhook>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3Webhook = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/webhooks/{webhooksId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_agents_webhooks_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_agents_webhooks_patch_task()`.
/// For the simplest API, use `dialogflow_projects_locations_agents_webhooks_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_agents_webhooks_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_agents_webhooks_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Webhook>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_agents_webhooks_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_agents_webhooks_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsAgentsWebhooksPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/agents/{agentsId}/webhooks/{webhooksId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_agents_webhooks_patch_builder()` + `dialogflow_projects_locations_agents_webhooks_patch_execute()`.
/// For task-level control, use `dialogflow_projects_locations_agents_webhooks_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_agents_webhooks_patch(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsAgentsWebhooksPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3Webhook>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_agents_webhooks_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    dialogflow_projects_locations_agents_webhooks_patch_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_operations_cancel_execute()` to send, or `dialogflow_projects_locations_operations_cancel` for simplest API.

pub fn dialogflow_projects_locations_operations_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/operations/{operationsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_operations_cancel_execute()` or `dialogflow_projects_locations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_operations_cancel_task()`.
/// For the simplest API, use `dialogflow_projects_locations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_operations_cancel_builder()` + `dialogflow_projects_locations_operations_cancel_execute()`.
/// For task-level control, use `dialogflow_projects_locations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_operations_cancel_builder(client, &args.name)?;
    dialogflow_projects_locations_operations_cancel_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_operations_get_execute()` to send, or `dialogflow_projects_locations_operations_get` for simplest API.

pub fn dialogflow_projects_locations_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_operations_get_execute()` or `dialogflow_projects_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_operations_get_task()`.
/// For the simplest API, use `dialogflow_projects_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_operations_get_builder()` + `dialogflow_projects_locations_operations_get_execute()`.
/// For task-level control, use `dialogflow_projects_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_operations_get(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_operations_get_builder(client, &args.name)?;
    dialogflow_projects_locations_operations_get_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/operations
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_operations_list_execute()` to send, or `dialogflow_projects_locations_operations_list` for simplest API.

pub fn dialogflow_projects_locations_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    returnPartialSuccess: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/operations
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_operations_list_execute()` or `dialogflow_projects_locations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/operations
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_operations_list_task()`.
/// For the simplest API, use `dialogflow_projects_locations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/operations
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_operations_list_builder()` + `dialogflow_projects_locations_operations_list_execute()`.
/// For task-level control, use `dialogflow_projects_locations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_operations_list(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    dialogflow_projects_locations_operations_list_execute(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/securitySettings
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_security_settings_create_execute()` to send, or `dialogflow_projects_locations_security_settings_create` for simplest API.

pub fn dialogflow_projects_locations_security_settings_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/securitySettings",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/securitySettings
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_security_settings_create_execute()` or `dialogflow_projects_locations_security_settings_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_security_settings_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_security_settings_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3SecuritySettings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3SecuritySettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/securitySettings
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_security_settings_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_security_settings_create_task()`.
/// For the simplest API, use `dialogflow_projects_locations_security_settings_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_security_settings_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_security_settings_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3SecuritySettings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_security_settings_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_security_settings_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsSecuritySettingsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v3/projects/{projectsId}/locations/{locationsId}/securitySettings
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_security_settings_create_builder()` + `dialogflow_projects_locations_security_settings_create_execute()`.
/// For task-level control, use `dialogflow_projects_locations_security_settings_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_security_settings_create(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsSecuritySettingsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3SecuritySettings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_security_settings_create_builder(client, &args.parent)?;
    dialogflow_projects_locations_security_settings_create_execute(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/securitySettings/{securitySettingsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_security_settings_delete_execute()` to send, or `dialogflow_projects_locations_security_settings_delete` for simplest API.

pub fn dialogflow_projects_locations_security_settings_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/securitySettings/{securitySettingsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/securitySettings/{securitySettingsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_security_settings_delete_execute()` or `dialogflow_projects_locations_security_settings_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_security_settings_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_security_settings_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/securitySettings/{securitySettingsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_security_settings_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_security_settings_delete_task()`.
/// For the simplest API, use `dialogflow_projects_locations_security_settings_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_security_settings_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_security_settings_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_security_settings_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_security_settings_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsSecuritySettingsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v3/projects/{projectsId}/locations/{locationsId}/securitySettings/{securitySettingsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_security_settings_delete_builder()` + `dialogflow_projects_locations_security_settings_delete_execute()`.
/// For task-level control, use `dialogflow_projects_locations_security_settings_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_security_settings_delete(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsSecuritySettingsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dialogflow_projects_locations_security_settings_delete_builder(client, &args.name)?;
    dialogflow_projects_locations_security_settings_delete_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/securitySettings/{securitySettingsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_security_settings_get_execute()` to send, or `dialogflow_projects_locations_security_settings_get` for simplest API.

pub fn dialogflow_projects_locations_security_settings_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/securitySettings/{securitySettingsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/securitySettings/{securitySettingsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_security_settings_get_execute()` or `dialogflow_projects_locations_security_settings_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_security_settings_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_security_settings_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3SecuritySettings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3SecuritySettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/securitySettings/{securitySettingsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_security_settings_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_security_settings_get_task()`.
/// For the simplest API, use `dialogflow_projects_locations_security_settings_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_security_settings_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_security_settings_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3SecuritySettings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_security_settings_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_security_settings_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsSecuritySettingsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/securitySettings/{securitySettingsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_security_settings_get_builder()` + `dialogflow_projects_locations_security_settings_get_execute()`.
/// For task-level control, use `dialogflow_projects_locations_security_settings_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_security_settings_get(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsSecuritySettingsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3SecuritySettings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_security_settings_get_builder(client, &args.name)?;
    dialogflow_projects_locations_security_settings_get_execute(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/securitySettings
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_security_settings_list_execute()` to send, or `dialogflow_projects_locations_security_settings_list` for simplest API.

pub fn dialogflow_projects_locations_security_settings_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/securitySettings",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/securitySettings
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_security_settings_list_execute()` or `dialogflow_projects_locations_security_settings_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_security_settings_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_security_settings_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListSecuritySettingsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3ListSecuritySettingsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/securitySettings
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_security_settings_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_security_settings_list_task()`.
/// For the simplest API, use `dialogflow_projects_locations_security_settings_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_security_settings_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_security_settings_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListSecuritySettingsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_security_settings_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_security_settings_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsSecuritySettingsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v3/projects/{projectsId}/locations/{locationsId}/securitySettings
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_security_settings_list_builder()` + `dialogflow_projects_locations_security_settings_list_execute()`.
/// For task-level control, use `dialogflow_projects_locations_security_settings_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_security_settings_list(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsSecuritySettingsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDialogflowCxV3ListSecuritySettingsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_security_settings_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    dialogflow_projects_locations_security_settings_list_execute(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/securitySettings/{securitySettingsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_locations_security_settings_patch_execute()` to send, or `dialogflow_projects_locations_security_settings_patch` for simplest API.

pub fn dialogflow_projects_locations_security_settings_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/locations/{locationsId}/securitySettings/{securitySettingsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/securitySettings/{securitySettingsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_locations_security_settings_patch_execute()` or `dialogflow_projects_locations_security_settings_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_security_settings_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_security_settings_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDialogflowCxV3SecuritySettings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDialogflowCxV3SecuritySettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/securitySettings/{securitySettingsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_locations_security_settings_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_locations_security_settings_patch_task()`.
/// For the simplest API, use `dialogflow_projects_locations_security_settings_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_locations_security_settings_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_locations_security_settings_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3SecuritySettings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_locations_security_settings_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_locations_security_settings_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsLocationsSecuritySettingsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v3/projects/{projectsId}/locations/{locationsId}/securitySettings/{securitySettingsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_locations_security_settings_patch_builder()` + `dialogflow_projects_locations_security_settings_patch_execute()`.
/// For task-level control, use `dialogflow_projects_locations_security_settings_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_locations_security_settings_patch(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsLocationsSecuritySettingsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDialogflowCxV3SecuritySettings>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_locations_security_settings_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    dialogflow_projects_locations_security_settings_patch_execute(builder)
}

/// POST v3/projects/{projectsId}/operations/{operationsId}:cancel
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_operations_cancel_execute()` to send, or `dialogflow_projects_operations_cancel` for simplest API.

pub fn dialogflow_projects_operations_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/operations/{operationsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v3/projects/{projectsId}/operations/{operationsId}:cancel
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_operations_cancel_execute()` or `dialogflow_projects_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v3/projects/{projectsId}/operations/{operationsId}:cancel
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_operations_cancel_task()`.
/// For the simplest API, use `dialogflow_projects_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v3/projects/{projectsId}/operations/{operationsId}:cancel
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_operations_cancel_builder()` + `dialogflow_projects_operations_cancel_execute()`.
/// For task-level control, use `dialogflow_projects_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_operations_cancel(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_operations_cancel_builder(client, &args.name)?;
    dialogflow_projects_operations_cancel_execute(builder)
}

/// GET v3/projects/{projectsId}/operations/{operationsId}
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_operations_get_execute()` to send, or `dialogflow_projects_operations_get` for simplest API.

pub fn dialogflow_projects_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/operations/{operationsId}
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_operations_get_execute()` or `dialogflow_projects_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/operations/{operationsId}
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_operations_get_task()`.
/// For the simplest API, use `dialogflow_projects_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v3/projects/{projectsId}/operations/{operationsId}
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_operations_get_builder()` + `dialogflow_projects_operations_get_execute()`.
/// For task-level control, use `dialogflow_projects_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_operations_get(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_operations_get_builder(client, &args.name)?;
    dialogflow_projects_operations_get_execute(builder)
}

/// GET v3/projects/{projectsId}/operations
///
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dialogflow_projects_operations_list_execute()` to send, or `dialogflow_projects_operations_list` for simplest API.

pub fn dialogflow_projects_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    returnPartialSuccess: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://dialogflow.googleapis.com/v3/projects/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v3/projects/{projectsId}/operations
///
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dialogflow_projects_operations_list_execute()` or `dialogflow_projects_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v3/projects/{projectsId}/operations
///
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dialogflow_projects_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dialogflow_projects_operations_list_task()`.
/// For the simplest API, use `dialogflow_projects_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dialogflow_projects_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dialogflow_projects_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dialogflow_projects_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dialogflow_projects_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DialogflowProjectsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<String>,
}

/// GET v3/projects/{projectsId}/operations
///
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dialogflow_projects_operations_list_builder()` + `dialogflow_projects_operations_list_execute()`.
/// For task-level control, use `dialogflow_projects_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dialogflow_projects_operations_list(
    client: &SimpleHttpClient,
    args: &DialogflowProjectsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dialogflow_projects_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    dialogflow_projects_operations_list_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudLocationLocation
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudLocationLocation with DialogflowProjectsLocationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsGetArgs> for GoogleCloudLocationLocation {
    fn generate_resource_id(&self, input: &DialogflowProjectsLocationsGetArgs) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudLocationLocation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudLocationLocation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudLocationListLocationsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudLocationListLocationsResponse with DialogflowProjectsLocationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsListArgs>
    for GoogleCloudLocationListLocationsResponse
{
    fn generate_resource_id(&self, input: &DialogflowProjectsLocationsListArgs) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudLocationListLocationsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudLocationListLocationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Agent
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Agent with DialogflowProjectsLocationsAgentsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsCreateArgs>
    for GoogleCloudDialogflowCxV3Agent
{
    fn generate_resource_id(&self, input: &DialogflowProjectsLocationsAgentsCreateArgs) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Agent/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Agent"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DialogflowProjectsLocationsAgentsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsDeleteArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(&self, input: &DialogflowProjectsLocationsAgentsDeleteArgs) -> String {
        format!("gcp::dialogflow::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DialogflowProjectsLocationsAgentsExportArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsExportArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(&self, input: &DialogflowProjectsLocationsAgentsExportArgs) -> String {
        format!("gcp::dialogflow::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Agent
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Agent with DialogflowProjectsLocationsAgentsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsGetArgs>
    for GoogleCloudDialogflowCxV3Agent
{
    fn generate_resource_id(&self, input: &DialogflowProjectsLocationsAgentsGetArgs) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Agent/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Agent"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3GenerativeSettings
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3GenerativeSettings with DialogflowProjectsLocationsAgentsGetGenerativeSettingsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsGetGenerativeSettingsArgs>
    for GoogleCloudDialogflowCxV3GenerativeSettings
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsGetGenerativeSettingsArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3GenerativeSettings/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3GenerativeSettings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3AgentValidationResult
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3AgentValidationResult with DialogflowProjectsLocationsAgentsGetValidationResultArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsGetValidationResultArgs>
    for GoogleCloudDialogflowCxV3AgentValidationResult
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsGetValidationResultArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3AgentValidationResult/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3AgentValidationResult"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListAgentsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListAgentsResponse with DialogflowProjectsLocationsAgentsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsListArgs>
    for GoogleCloudDialogflowCxV3ListAgentsResponse
{
    fn generate_resource_id(&self, input: &DialogflowProjectsLocationsAgentsListArgs) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3ListAgentsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3ListAgentsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Agent
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Agent with DialogflowProjectsLocationsAgentsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsPatchArgs>
    for GoogleCloudDialogflowCxV3Agent
{
    fn generate_resource_id(&self, input: &DialogflowProjectsLocationsAgentsPatchArgs) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Agent/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Agent"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DialogflowProjectsLocationsAgentsRestoreArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsRestoreArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(&self, input: &DialogflowProjectsLocationsAgentsRestoreArgs) -> String {
        format!("gcp::dialogflow::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3GenerativeSettings
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3GenerativeSettings with DialogflowProjectsLocationsAgentsUpdateGenerativeSettingsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsUpdateGenerativeSettingsArgs>
    for GoogleCloudDialogflowCxV3GenerativeSettings
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsUpdateGenerativeSettingsArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3GenerativeSettings/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3GenerativeSettings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3AgentValidationResult
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3AgentValidationResult with DialogflowProjectsLocationsAgentsValidateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsValidateArgs>
    for GoogleCloudDialogflowCxV3AgentValidationResult
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsValidateArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3AgentValidationResult/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3AgentValidationResult"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Changelog
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Changelog with DialogflowProjectsLocationsAgentsChangelogsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsChangelogsGetArgs>
    for GoogleCloudDialogflowCxV3Changelog
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsChangelogsGetArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Changelog/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Changelog"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListChangelogsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListChangelogsResponse with DialogflowProjectsLocationsAgentsChangelogsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsChangelogsListArgs>
    for GoogleCloudDialogflowCxV3ListChangelogsResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsChangelogsListArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3ListChangelogsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3ListChangelogsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3EntityType
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3EntityType with DialogflowProjectsLocationsAgentsEntityTypesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsEntityTypesCreateArgs>
    for GoogleCloudDialogflowCxV3EntityType
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsEntityTypesCreateArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3EntityType/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3EntityType"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DialogflowProjectsLocationsAgentsEntityTypesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsEntityTypesDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsEntityTypesDeleteArgs,
    ) -> String {
        format!("gcp::dialogflow::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DialogflowProjectsLocationsAgentsEntityTypesExportArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsEntityTypesExportArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsEntityTypesExportArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleLongrunningOperation/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3EntityType
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3EntityType with DialogflowProjectsLocationsAgentsEntityTypesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsEntityTypesGetArgs>
    for GoogleCloudDialogflowCxV3EntityType
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsEntityTypesGetArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3EntityType/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3EntityType"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DialogflowProjectsLocationsAgentsEntityTypesImportArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsEntityTypesImportArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsEntityTypesImportArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleLongrunningOperation/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListEntityTypesResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListEntityTypesResponse with DialogflowProjectsLocationsAgentsEntityTypesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsEntityTypesListArgs>
    for GoogleCloudDialogflowCxV3ListEntityTypesResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsEntityTypesListArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3ListEntityTypesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3ListEntityTypesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3EntityType
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3EntityType with DialogflowProjectsLocationsAgentsEntityTypesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsEntityTypesPatchArgs>
    for GoogleCloudDialogflowCxV3EntityType
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsEntityTypesPatchArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3EntityType/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3EntityType"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DialogflowProjectsLocationsAgentsEnvironmentsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsEnvironmentsCreateArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsEnvironmentsCreateArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleLongrunningOperation/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DialogflowProjectsLocationsAgentsEnvironmentsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsEnvironmentsDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsEnvironmentsDeleteArgs,
    ) -> String {
        format!("gcp::dialogflow::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DialogflowProjectsLocationsAgentsEnvironmentsDeployFlowArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsEnvironmentsDeployFlowArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsEnvironmentsDeployFlowArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleLongrunningOperation/{}",
            input.environment
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Environment
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Environment with DialogflowProjectsLocationsAgentsEnvironmentsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsEnvironmentsGetArgs>
    for GoogleCloudDialogflowCxV3Environment
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsEnvironmentsGetArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Environment/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Environment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListEnvironmentsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListEnvironmentsResponse with DialogflowProjectsLocationsAgentsEnvironmentsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsEnvironmentsListArgs>
    for GoogleCloudDialogflowCxV3ListEnvironmentsResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsEnvironmentsListArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3ListEnvironmentsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3ListEnvironmentsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3LookupEnvironmentHistoryResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3LookupEnvironmentHistoryResponse with DialogflowProjectsLocationsAgentsEnvironmentsLookupEnvironmentHistoryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsEnvironmentsLookupEnvironmentHistoryArgs>
    for GoogleCloudDialogflowCxV3LookupEnvironmentHistoryResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsEnvironmentsLookupEnvironmentHistoryArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3LookupEnvironmentHistoryResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3LookupEnvironmentHistoryResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DialogflowProjectsLocationsAgentsEnvironmentsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsEnvironmentsPatchArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsEnvironmentsPatchArgs,
    ) -> String {
        format!("gcp::dialogflow::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DialogflowProjectsLocationsAgentsEnvironmentsRunContinuousTestArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsEnvironmentsRunContinuousTestArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsEnvironmentsRunContinuousTestArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleLongrunningOperation/{}",
            input.environment
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListContinuousTestResultsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListContinuousTestResultsResponse with DialogflowProjectsLocationsAgentsEnvironmentsContinuousTestResultsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsEnvironmentsContinuousTestResultsListArgs>
    for GoogleCloudDialogflowCxV3ListContinuousTestResultsResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsEnvironmentsContinuousTestResultsListArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3ListContinuousTestResultsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3ListContinuousTestResultsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Deployment
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Deployment with DialogflowProjectsLocationsAgentsEnvironmentsDeploymentsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsEnvironmentsDeploymentsGetArgs>
    for GoogleCloudDialogflowCxV3Deployment
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsEnvironmentsDeploymentsGetArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Deployment/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Deployment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListDeploymentsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListDeploymentsResponse with DialogflowProjectsLocationsAgentsEnvironmentsDeploymentsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsEnvironmentsDeploymentsListArgs>
    for GoogleCloudDialogflowCxV3ListDeploymentsResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsEnvironmentsDeploymentsListArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3ListDeploymentsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3ListDeploymentsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Experiment
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Experiment with DialogflowProjectsLocationsAgentsEnvironmentsExperimentsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsEnvironmentsExperimentsCreateArgs>
    for GoogleCloudDialogflowCxV3Experiment
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsEnvironmentsExperimentsCreateArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Experiment/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Experiment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DialogflowProjectsLocationsAgentsEnvironmentsExperimentsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsEnvironmentsExperimentsDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsEnvironmentsExperimentsDeleteArgs,
    ) -> String {
        format!("gcp::dialogflow::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Experiment
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Experiment with DialogflowProjectsLocationsAgentsEnvironmentsExperimentsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsEnvironmentsExperimentsGetArgs>
    for GoogleCloudDialogflowCxV3Experiment
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsEnvironmentsExperimentsGetArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Experiment/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Experiment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListExperimentsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListExperimentsResponse with DialogflowProjectsLocationsAgentsEnvironmentsExperimentsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsEnvironmentsExperimentsListArgs>
    for GoogleCloudDialogflowCxV3ListExperimentsResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsEnvironmentsExperimentsListArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3ListExperimentsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3ListExperimentsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Experiment
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Experiment with DialogflowProjectsLocationsAgentsEnvironmentsExperimentsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsEnvironmentsExperimentsPatchArgs>
    for GoogleCloudDialogflowCxV3Experiment
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsEnvironmentsExperimentsPatchArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Experiment/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Experiment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Experiment
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Experiment with DialogflowProjectsLocationsAgentsEnvironmentsExperimentsStartArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsEnvironmentsExperimentsStartArgs>
    for GoogleCloudDialogflowCxV3Experiment
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsEnvironmentsExperimentsStartArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Experiment/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Experiment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Experiment
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Experiment with DialogflowProjectsLocationsAgentsEnvironmentsExperimentsStopArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsEnvironmentsExperimentsStopArgs>
    for GoogleCloudDialogflowCxV3Experiment
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsEnvironmentsExperimentsStopArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Experiment/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Experiment"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3DetectIntentResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3DetectIntentResponse with DialogflowProjectsLocationsAgentsEnvironmentsSessionsDetectIntentArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsEnvironmentsSessionsDetectIntentArgs>
    for GoogleCloudDialogflowCxV3DetectIntentResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsEnvironmentsSessionsDetectIntentArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3DetectIntentResponse/{}",
            input.session
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3DetectIntentResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3FulfillIntentResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3FulfillIntentResponse with DialogflowProjectsLocationsAgentsEnvironmentsSessionsFulfillIntentArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsEnvironmentsSessionsFulfillIntentArgs>
    for GoogleCloudDialogflowCxV3FulfillIntentResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsEnvironmentsSessionsFulfillIntentArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3FulfillIntentResponse/{}",
            input.session
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3FulfillIntentResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3MatchIntentResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3MatchIntentResponse with DialogflowProjectsLocationsAgentsEnvironmentsSessionsMatchIntentArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsEnvironmentsSessionsMatchIntentArgs>
    for GoogleCloudDialogflowCxV3MatchIntentResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsEnvironmentsSessionsMatchIntentArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3MatchIntentResponse/{}",
            input.session
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3MatchIntentResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3DetectIntentResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3DetectIntentResponse with DialogflowProjectsLocationsAgentsEnvironmentsSessionsServerStreamingDetectIntentArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        DialogflowProjectsLocationsAgentsEnvironmentsSessionsServerStreamingDetectIntentArgs,
    > for GoogleCloudDialogflowCxV3DetectIntentResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsEnvironmentsSessionsServerStreamingDetectIntentArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3DetectIntentResponse/{}",
            input.session
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3DetectIntentResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3SessionEntityType
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3SessionEntityType with DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesCreateArgs>
    for GoogleCloudDialogflowCxV3SessionEntityType
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesCreateArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3SessionEntityType/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3SessionEntityType"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesDeleteArgs,
    ) -> String {
        format!("gcp::dialogflow::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3SessionEntityType
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3SessionEntityType with DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesGetArgs>
    for GoogleCloudDialogflowCxV3SessionEntityType
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesGetArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3SessionEntityType/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3SessionEntityType"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListSessionEntityTypesResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListSessionEntityTypesResponse with DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesListArgs>
    for GoogleCloudDialogflowCxV3ListSessionEntityTypesResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesListArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3ListSessionEntityTypesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3ListSessionEntityTypesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3SessionEntityType
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3SessionEntityType with DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesPatchArgs>
    for GoogleCloudDialogflowCxV3SessionEntityType
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsEnvironmentsSessionsEntityTypesPatchArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3SessionEntityType/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3SessionEntityType"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Flow
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Flow with DialogflowProjectsLocationsAgentsFlowsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsFlowsCreateArgs>
    for GoogleCloudDialogflowCxV3Flow
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsFlowsCreateArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Flow/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Flow"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DialogflowProjectsLocationsAgentsFlowsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsFlowsDeleteArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsFlowsDeleteArgs,
    ) -> String {
        format!("gcp::dialogflow::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DialogflowProjectsLocationsAgentsFlowsExportArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsFlowsExportArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsFlowsExportArgs,
    ) -> String {
        format!("gcp::dialogflow::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Flow
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Flow with DialogflowProjectsLocationsAgentsFlowsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsFlowsGetArgs>
    for GoogleCloudDialogflowCxV3Flow
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsFlowsGetArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Flow/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Flow"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3FlowValidationResult
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3FlowValidationResult with DialogflowProjectsLocationsAgentsFlowsGetValidationResultArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsFlowsGetValidationResultArgs>
    for GoogleCloudDialogflowCxV3FlowValidationResult
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsFlowsGetValidationResultArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3FlowValidationResult/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3FlowValidationResult"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DialogflowProjectsLocationsAgentsFlowsImportArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsFlowsImportArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsFlowsImportArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleLongrunningOperation/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListFlowsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListFlowsResponse with DialogflowProjectsLocationsAgentsFlowsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsFlowsListArgs>
    for GoogleCloudDialogflowCxV3ListFlowsResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsFlowsListArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3ListFlowsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3ListFlowsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Flow
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Flow with DialogflowProjectsLocationsAgentsFlowsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsFlowsPatchArgs>
    for GoogleCloudDialogflowCxV3Flow
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsFlowsPatchArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Flow/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Flow"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DialogflowProjectsLocationsAgentsFlowsTrainArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsFlowsTrainArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsFlowsTrainArgs,
    ) -> String {
        format!("gcp::dialogflow::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3FlowValidationResult
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3FlowValidationResult with DialogflowProjectsLocationsAgentsFlowsValidateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsFlowsValidateArgs>
    for GoogleCloudDialogflowCxV3FlowValidationResult
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsFlowsValidateArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3FlowValidationResult/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3FlowValidationResult"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Page
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Page with DialogflowProjectsLocationsAgentsFlowsPagesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsFlowsPagesCreateArgs>
    for GoogleCloudDialogflowCxV3Page
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsFlowsPagesCreateArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Page/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Page"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DialogflowProjectsLocationsAgentsFlowsPagesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsFlowsPagesDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsFlowsPagesDeleteArgs,
    ) -> String {
        format!("gcp::dialogflow::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Page
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Page with DialogflowProjectsLocationsAgentsFlowsPagesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsFlowsPagesGetArgs>
    for GoogleCloudDialogflowCxV3Page
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsFlowsPagesGetArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Page/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Page"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListPagesResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListPagesResponse with DialogflowProjectsLocationsAgentsFlowsPagesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsFlowsPagesListArgs>
    for GoogleCloudDialogflowCxV3ListPagesResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsFlowsPagesListArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3ListPagesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3ListPagesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Page
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Page with DialogflowProjectsLocationsAgentsFlowsPagesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsFlowsPagesPatchArgs>
    for GoogleCloudDialogflowCxV3Page
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsFlowsPagesPatchArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Page/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Page"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3TransitionRouteGroup
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3TransitionRouteGroup with DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsCreateArgs>
    for GoogleCloudDialogflowCxV3TransitionRouteGroup
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsCreateArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3TransitionRouteGroup/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3TransitionRouteGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsDeleteArgs,
    ) -> String {
        format!("gcp::dialogflow::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3TransitionRouteGroup
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3TransitionRouteGroup with DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsGetArgs>
    for GoogleCloudDialogflowCxV3TransitionRouteGroup
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsGetArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3TransitionRouteGroup/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3TransitionRouteGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListTransitionRouteGroupsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListTransitionRouteGroupsResponse with DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsListArgs>
    for GoogleCloudDialogflowCxV3ListTransitionRouteGroupsResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsListArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3ListTransitionRouteGroupsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3ListTransitionRouteGroupsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3TransitionRouteGroup
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3TransitionRouteGroup with DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsPatchArgs>
    for GoogleCloudDialogflowCxV3TransitionRouteGroup
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsFlowsTransitionRouteGroupsPatchArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3TransitionRouteGroup/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3TransitionRouteGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3CompareVersionsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3CompareVersionsResponse with DialogflowProjectsLocationsAgentsFlowsVersionsCompareVersionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsFlowsVersionsCompareVersionsArgs>
    for GoogleCloudDialogflowCxV3CompareVersionsResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsFlowsVersionsCompareVersionsArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3CompareVersionsResponse/{}",
            input.baseVersion
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3CompareVersionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DialogflowProjectsLocationsAgentsFlowsVersionsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsFlowsVersionsCreateArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsFlowsVersionsCreateArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleLongrunningOperation/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DialogflowProjectsLocationsAgentsFlowsVersionsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsFlowsVersionsDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsFlowsVersionsDeleteArgs,
    ) -> String {
        format!("gcp::dialogflow::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Version
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Version with DialogflowProjectsLocationsAgentsFlowsVersionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsFlowsVersionsGetArgs>
    for GoogleCloudDialogflowCxV3Version
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsFlowsVersionsGetArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Version/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Version"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListVersionsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListVersionsResponse with DialogflowProjectsLocationsAgentsFlowsVersionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsFlowsVersionsListArgs>
    for GoogleCloudDialogflowCxV3ListVersionsResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsFlowsVersionsListArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3ListVersionsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3ListVersionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DialogflowProjectsLocationsAgentsFlowsVersionsLoadArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsFlowsVersionsLoadArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsFlowsVersionsLoadArgs,
    ) -> String {
        format!("gcp::dialogflow::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Version
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Version with DialogflowProjectsLocationsAgentsFlowsVersionsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsFlowsVersionsPatchArgs>
    for GoogleCloudDialogflowCxV3Version
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsFlowsVersionsPatchArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Version/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Version"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Generator
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Generator with DialogflowProjectsLocationsAgentsGeneratorsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsGeneratorsCreateArgs>
    for GoogleCloudDialogflowCxV3Generator
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsGeneratorsCreateArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Generator/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Generator"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DialogflowProjectsLocationsAgentsGeneratorsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsGeneratorsDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsGeneratorsDeleteArgs,
    ) -> String {
        format!("gcp::dialogflow::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Generator
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Generator with DialogflowProjectsLocationsAgentsGeneratorsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsGeneratorsGetArgs>
    for GoogleCloudDialogflowCxV3Generator
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsGeneratorsGetArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Generator/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Generator"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListGeneratorsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListGeneratorsResponse with DialogflowProjectsLocationsAgentsGeneratorsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsGeneratorsListArgs>
    for GoogleCloudDialogflowCxV3ListGeneratorsResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsGeneratorsListArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3ListGeneratorsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3ListGeneratorsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Generator
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Generator with DialogflowProjectsLocationsAgentsGeneratorsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsGeneratorsPatchArgs>
    for GoogleCloudDialogflowCxV3Generator
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsGeneratorsPatchArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Generator/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Generator"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Intent
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Intent with DialogflowProjectsLocationsAgentsIntentsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsIntentsCreateArgs>
    for GoogleCloudDialogflowCxV3Intent
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsIntentsCreateArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Intent/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Intent"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DialogflowProjectsLocationsAgentsIntentsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsIntentsDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsIntentsDeleteArgs,
    ) -> String {
        format!("gcp::dialogflow::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DialogflowProjectsLocationsAgentsIntentsExportArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsIntentsExportArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsIntentsExportArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleLongrunningOperation/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Intent
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Intent with DialogflowProjectsLocationsAgentsIntentsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsIntentsGetArgs>
    for GoogleCloudDialogflowCxV3Intent
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsIntentsGetArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Intent/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Intent"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DialogflowProjectsLocationsAgentsIntentsImportArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsIntentsImportArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsIntentsImportArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleLongrunningOperation/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListIntentsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListIntentsResponse with DialogflowProjectsLocationsAgentsIntentsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsIntentsListArgs>
    for GoogleCloudDialogflowCxV3ListIntentsResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsIntentsListArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3ListIntentsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3ListIntentsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Intent
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Intent with DialogflowProjectsLocationsAgentsIntentsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsIntentsPatchArgs>
    for GoogleCloudDialogflowCxV3Intent
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsIntentsPatchArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Intent/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Intent"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Playbook
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Playbook with DialogflowProjectsLocationsAgentsPlaybooksCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsPlaybooksCreateArgs>
    for GoogleCloudDialogflowCxV3Playbook
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsPlaybooksCreateArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Playbook/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Playbook"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DialogflowProjectsLocationsAgentsPlaybooksDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsPlaybooksDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsPlaybooksDeleteArgs,
    ) -> String {
        format!("gcp::dialogflow::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DialogflowProjectsLocationsAgentsPlaybooksExportArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsPlaybooksExportArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsPlaybooksExportArgs,
    ) -> String {
        format!("gcp::dialogflow::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Playbook
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Playbook with DialogflowProjectsLocationsAgentsPlaybooksGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsPlaybooksGetArgs>
    for GoogleCloudDialogflowCxV3Playbook
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsPlaybooksGetArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Playbook/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Playbook"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DialogflowProjectsLocationsAgentsPlaybooksImportArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsPlaybooksImportArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsPlaybooksImportArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleLongrunningOperation/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListPlaybooksResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListPlaybooksResponse with DialogflowProjectsLocationsAgentsPlaybooksListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsPlaybooksListArgs>
    for GoogleCloudDialogflowCxV3ListPlaybooksResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsPlaybooksListArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3ListPlaybooksResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3ListPlaybooksResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Playbook
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Playbook with DialogflowProjectsLocationsAgentsPlaybooksPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsPlaybooksPatchArgs>
    for GoogleCloudDialogflowCxV3Playbook
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsPlaybooksPatchArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Playbook/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Playbook"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Example
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Example with DialogflowProjectsLocationsAgentsPlaybooksExamplesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsPlaybooksExamplesCreateArgs>
    for GoogleCloudDialogflowCxV3Example
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsPlaybooksExamplesCreateArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Example/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Example"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DialogflowProjectsLocationsAgentsPlaybooksExamplesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsPlaybooksExamplesDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsPlaybooksExamplesDeleteArgs,
    ) -> String {
        format!("gcp::dialogflow::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Example
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Example with DialogflowProjectsLocationsAgentsPlaybooksExamplesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsPlaybooksExamplesGetArgs>
    for GoogleCloudDialogflowCxV3Example
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsPlaybooksExamplesGetArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Example/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Example"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListExamplesResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListExamplesResponse with DialogflowProjectsLocationsAgentsPlaybooksExamplesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsPlaybooksExamplesListArgs>
    for GoogleCloudDialogflowCxV3ListExamplesResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsPlaybooksExamplesListArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3ListExamplesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3ListExamplesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Example
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Example with DialogflowProjectsLocationsAgentsPlaybooksExamplesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsPlaybooksExamplesPatchArgs>
    for GoogleCloudDialogflowCxV3Example
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsPlaybooksExamplesPatchArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Example/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Example"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3PlaybookVersion
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3PlaybookVersion with DialogflowProjectsLocationsAgentsPlaybooksVersionsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsPlaybooksVersionsCreateArgs>
    for GoogleCloudDialogflowCxV3PlaybookVersion
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsPlaybooksVersionsCreateArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3PlaybookVersion/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3PlaybookVersion"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DialogflowProjectsLocationsAgentsPlaybooksVersionsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsPlaybooksVersionsDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsPlaybooksVersionsDeleteArgs,
    ) -> String {
        format!("gcp::dialogflow::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3PlaybookVersion
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3PlaybookVersion with DialogflowProjectsLocationsAgentsPlaybooksVersionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsPlaybooksVersionsGetArgs>
    for GoogleCloudDialogflowCxV3PlaybookVersion
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsPlaybooksVersionsGetArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3PlaybookVersion/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3PlaybookVersion"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListPlaybookVersionsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListPlaybookVersionsResponse with DialogflowProjectsLocationsAgentsPlaybooksVersionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsPlaybooksVersionsListArgs>
    for GoogleCloudDialogflowCxV3ListPlaybookVersionsResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsPlaybooksVersionsListArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3ListPlaybookVersionsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3ListPlaybookVersionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3RestorePlaybookVersionResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3RestorePlaybookVersionResponse with DialogflowProjectsLocationsAgentsPlaybooksVersionsRestoreArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsPlaybooksVersionsRestoreArgs>
    for GoogleCloudDialogflowCxV3RestorePlaybookVersionResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsPlaybooksVersionsRestoreArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3RestorePlaybookVersionResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3RestorePlaybookVersionResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3DetectIntentResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3DetectIntentResponse with DialogflowProjectsLocationsAgentsSessionsDetectIntentArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsSessionsDetectIntentArgs>
    for GoogleCloudDialogflowCxV3DetectIntentResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsSessionsDetectIntentArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3DetectIntentResponse/{}",
            input.session
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3DetectIntentResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3FulfillIntentResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3FulfillIntentResponse with DialogflowProjectsLocationsAgentsSessionsFulfillIntentArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsSessionsFulfillIntentArgs>
    for GoogleCloudDialogflowCxV3FulfillIntentResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsSessionsFulfillIntentArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3FulfillIntentResponse/{}",
            input.session
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3FulfillIntentResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3MatchIntentResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3MatchIntentResponse with DialogflowProjectsLocationsAgentsSessionsMatchIntentArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsSessionsMatchIntentArgs>
    for GoogleCloudDialogflowCxV3MatchIntentResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsSessionsMatchIntentArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3MatchIntentResponse/{}",
            input.session
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3MatchIntentResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3DetectIntentResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3DetectIntentResponse with DialogflowProjectsLocationsAgentsSessionsServerStreamingDetectIntentArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsSessionsServerStreamingDetectIntentArgs>
    for GoogleCloudDialogflowCxV3DetectIntentResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsSessionsServerStreamingDetectIntentArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3DetectIntentResponse/{}",
            input.session
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3DetectIntentResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3AnswerFeedback
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3AnswerFeedback with DialogflowProjectsLocationsAgentsSessionsSubmitAnswerFeedbackArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsSessionsSubmitAnswerFeedbackArgs>
    for GoogleCloudDialogflowCxV3AnswerFeedback
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsSessionsSubmitAnswerFeedbackArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3AnswerFeedback/{}",
            input.session
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3AnswerFeedback"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3SessionEntityType
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3SessionEntityType with DialogflowProjectsLocationsAgentsSessionsEntityTypesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsSessionsEntityTypesCreateArgs>
    for GoogleCloudDialogflowCxV3SessionEntityType
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsSessionsEntityTypesCreateArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3SessionEntityType/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3SessionEntityType"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DialogflowProjectsLocationsAgentsSessionsEntityTypesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsSessionsEntityTypesDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsSessionsEntityTypesDeleteArgs,
    ) -> String {
        format!("gcp::dialogflow::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3SessionEntityType
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3SessionEntityType with DialogflowProjectsLocationsAgentsSessionsEntityTypesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsSessionsEntityTypesGetArgs>
    for GoogleCloudDialogflowCxV3SessionEntityType
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsSessionsEntityTypesGetArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3SessionEntityType/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3SessionEntityType"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListSessionEntityTypesResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListSessionEntityTypesResponse with DialogflowProjectsLocationsAgentsSessionsEntityTypesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsSessionsEntityTypesListArgs>
    for GoogleCloudDialogflowCxV3ListSessionEntityTypesResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsSessionsEntityTypesListArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3ListSessionEntityTypesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3ListSessionEntityTypesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3SessionEntityType
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3SessionEntityType with DialogflowProjectsLocationsAgentsSessionsEntityTypesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsSessionsEntityTypesPatchArgs>
    for GoogleCloudDialogflowCxV3SessionEntityType
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsSessionsEntityTypesPatchArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3SessionEntityType/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3SessionEntityType"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DialogflowProjectsLocationsAgentsTestCasesBatchDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsTestCasesBatchDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsTestCasesBatchDeleteArgs,
    ) -> String {
        format!("gcp::dialogflow::GoogleProtobufEmpty/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DialogflowProjectsLocationsAgentsTestCasesBatchRunArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsTestCasesBatchRunArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsTestCasesBatchRunArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleLongrunningOperation/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3CalculateCoverageResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3CalculateCoverageResponse with DialogflowProjectsLocationsAgentsTestCasesCalculateCoverageArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsTestCasesCalculateCoverageArgs>
    for GoogleCloudDialogflowCxV3CalculateCoverageResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsTestCasesCalculateCoverageArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3CalculateCoverageResponse/{}",
            input.agent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3CalculateCoverageResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3TestCase
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3TestCase with DialogflowProjectsLocationsAgentsTestCasesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsTestCasesCreateArgs>
    for GoogleCloudDialogflowCxV3TestCase
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsTestCasesCreateArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3TestCase/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3TestCase"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DialogflowProjectsLocationsAgentsTestCasesExportArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsTestCasesExportArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsTestCasesExportArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleLongrunningOperation/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3TestCase
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3TestCase with DialogflowProjectsLocationsAgentsTestCasesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsTestCasesGetArgs>
    for GoogleCloudDialogflowCxV3TestCase
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsTestCasesGetArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3TestCase/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3TestCase"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DialogflowProjectsLocationsAgentsTestCasesImportArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsTestCasesImportArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsTestCasesImportArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleLongrunningOperation/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListTestCasesResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListTestCasesResponse with DialogflowProjectsLocationsAgentsTestCasesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsTestCasesListArgs>
    for GoogleCloudDialogflowCxV3ListTestCasesResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsTestCasesListArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3ListTestCasesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3ListTestCasesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3TestCase
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3TestCase with DialogflowProjectsLocationsAgentsTestCasesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsTestCasesPatchArgs>
    for GoogleCloudDialogflowCxV3TestCase
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsTestCasesPatchArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3TestCase/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3TestCase"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DialogflowProjectsLocationsAgentsTestCasesRunArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsTestCasesRunArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsTestCasesRunArgs,
    ) -> String {
        format!("gcp::dialogflow::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3TestCaseResult
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3TestCaseResult with DialogflowProjectsLocationsAgentsTestCasesResultsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsTestCasesResultsGetArgs>
    for GoogleCloudDialogflowCxV3TestCaseResult
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsTestCasesResultsGetArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3TestCaseResult/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3TestCaseResult"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListTestCaseResultsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListTestCaseResultsResponse with DialogflowProjectsLocationsAgentsTestCasesResultsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsTestCasesResultsListArgs>
    for GoogleCloudDialogflowCxV3ListTestCaseResultsResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsTestCasesResultsListArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3ListTestCaseResultsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3ListTestCaseResultsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Tool
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Tool with DialogflowProjectsLocationsAgentsToolsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsToolsCreateArgs>
    for GoogleCloudDialogflowCxV3Tool
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsToolsCreateArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Tool/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Tool"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DialogflowProjectsLocationsAgentsToolsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsToolsDeleteArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsToolsDeleteArgs,
    ) -> String {
        format!("gcp::dialogflow::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Tool
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Tool with DialogflowProjectsLocationsAgentsToolsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsToolsGetArgs>
    for GoogleCloudDialogflowCxV3Tool
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsToolsGetArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Tool/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Tool"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListToolsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListToolsResponse with DialogflowProjectsLocationsAgentsToolsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsToolsListArgs>
    for GoogleCloudDialogflowCxV3ListToolsResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsToolsListArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3ListToolsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3ListToolsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Tool
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Tool with DialogflowProjectsLocationsAgentsToolsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsToolsPatchArgs>
    for GoogleCloudDialogflowCxV3Tool
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsToolsPatchArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Tool/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Tool"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ToolVersion
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ToolVersion with DialogflowProjectsLocationsAgentsToolsVersionsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsToolsVersionsCreateArgs>
    for GoogleCloudDialogflowCxV3ToolVersion
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsToolsVersionsCreateArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3ToolVersion/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3ToolVersion"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DialogflowProjectsLocationsAgentsToolsVersionsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsToolsVersionsDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsToolsVersionsDeleteArgs,
    ) -> String {
        format!("gcp::dialogflow::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ToolVersion
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ToolVersion with DialogflowProjectsLocationsAgentsToolsVersionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsToolsVersionsGetArgs>
    for GoogleCloudDialogflowCxV3ToolVersion
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsToolsVersionsGetArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3ToolVersion/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3ToolVersion"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListToolVersionsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListToolVersionsResponse with DialogflowProjectsLocationsAgentsToolsVersionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsToolsVersionsListArgs>
    for GoogleCloudDialogflowCxV3ListToolVersionsResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsToolsVersionsListArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3ListToolVersionsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3ListToolVersionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3RestoreToolVersionResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3RestoreToolVersionResponse with DialogflowProjectsLocationsAgentsToolsVersionsRestoreArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsToolsVersionsRestoreArgs>
    for GoogleCloudDialogflowCxV3RestoreToolVersionResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsToolsVersionsRestoreArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3RestoreToolVersionResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3RestoreToolVersionResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3TransitionRouteGroup
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3TransitionRouteGroup with DialogflowProjectsLocationsAgentsTransitionRouteGroupsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsTransitionRouteGroupsCreateArgs>
    for GoogleCloudDialogflowCxV3TransitionRouteGroup
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsTransitionRouteGroupsCreateArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3TransitionRouteGroup/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3TransitionRouteGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DialogflowProjectsLocationsAgentsTransitionRouteGroupsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsTransitionRouteGroupsDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsTransitionRouteGroupsDeleteArgs,
    ) -> String {
        format!("gcp::dialogflow::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3TransitionRouteGroup
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3TransitionRouteGroup with DialogflowProjectsLocationsAgentsTransitionRouteGroupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsTransitionRouteGroupsGetArgs>
    for GoogleCloudDialogflowCxV3TransitionRouteGroup
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsTransitionRouteGroupsGetArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3TransitionRouteGroup/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3TransitionRouteGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListTransitionRouteGroupsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListTransitionRouteGroupsResponse with DialogflowProjectsLocationsAgentsTransitionRouteGroupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsTransitionRouteGroupsListArgs>
    for GoogleCloudDialogflowCxV3ListTransitionRouteGroupsResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsTransitionRouteGroupsListArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3ListTransitionRouteGroupsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3ListTransitionRouteGroupsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3TransitionRouteGroup
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3TransitionRouteGroup with DialogflowProjectsLocationsAgentsTransitionRouteGroupsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsTransitionRouteGroupsPatchArgs>
    for GoogleCloudDialogflowCxV3TransitionRouteGroup
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsTransitionRouteGroupsPatchArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3TransitionRouteGroup/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3TransitionRouteGroup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Webhook
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Webhook with DialogflowProjectsLocationsAgentsWebhooksCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsWebhooksCreateArgs>
    for GoogleCloudDialogflowCxV3Webhook
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsWebhooksCreateArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Webhook/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Webhook"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DialogflowProjectsLocationsAgentsWebhooksDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsWebhooksDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsWebhooksDeleteArgs,
    ) -> String {
        format!("gcp::dialogflow::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Webhook
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Webhook with DialogflowProjectsLocationsAgentsWebhooksGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsWebhooksGetArgs>
    for GoogleCloudDialogflowCxV3Webhook
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsWebhooksGetArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Webhook/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Webhook"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListWebhooksResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListWebhooksResponse with DialogflowProjectsLocationsAgentsWebhooksListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsWebhooksListArgs>
    for GoogleCloudDialogflowCxV3ListWebhooksResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsWebhooksListArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3ListWebhooksResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3ListWebhooksResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Webhook
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3Webhook with DialogflowProjectsLocationsAgentsWebhooksPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsAgentsWebhooksPatchArgs>
    for GoogleCloudDialogflowCxV3Webhook
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsAgentsWebhooksPatchArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3Webhook/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3Webhook"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DialogflowProjectsLocationsOperationsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsOperationsCancelArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsOperationsCancelArgs,
    ) -> String {
        format!("gcp::dialogflow::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DialogflowProjectsLocationsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsOperationsGetArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(&self, input: &DialogflowProjectsLocationsOperationsGetArgs) -> String {
        format!("gcp::dialogflow::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningListOperationsResponse with DialogflowProjectsLocationsOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsOperationsListArgs>
    for GoogleLongrunningListOperationsResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsOperationsListArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleLongrunningListOperationsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleLongrunningListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3SecuritySettings
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3SecuritySettings with DialogflowProjectsLocationsSecuritySettingsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsSecuritySettingsCreateArgs>
    for GoogleCloudDialogflowCxV3SecuritySettings
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsSecuritySettingsCreateArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3SecuritySettings/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3SecuritySettings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DialogflowProjectsLocationsSecuritySettingsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsSecuritySettingsDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsSecuritySettingsDeleteArgs,
    ) -> String {
        format!("gcp::dialogflow::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3SecuritySettings
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3SecuritySettings with DialogflowProjectsLocationsSecuritySettingsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsSecuritySettingsGetArgs>
    for GoogleCloudDialogflowCxV3SecuritySettings
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsSecuritySettingsGetArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3SecuritySettings/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3SecuritySettings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListSecuritySettingsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3ListSecuritySettingsResponse with DialogflowProjectsLocationsSecuritySettingsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsSecuritySettingsListArgs>
    for GoogleCloudDialogflowCxV3ListSecuritySettingsResponse
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsSecuritySettingsListArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3ListSecuritySettingsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3ListSecuritySettingsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3SecuritySettings
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudDialogflowCxV3SecuritySettings with DialogflowProjectsLocationsSecuritySettingsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsLocationsSecuritySettingsPatchArgs>
    for GoogleCloudDialogflowCxV3SecuritySettings
{
    fn generate_resource_id(
        &self,
        input: &DialogflowProjectsLocationsSecuritySettingsPatchArgs,
    ) -> String {
        format!(
            "gcp::dialogflow::GoogleCloudDialogflowCxV3SecuritySettings/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleCloudDialogflowCxV3SecuritySettings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with DialogflowProjectsOperationsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsOperationsCancelArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(&self, input: &DialogflowProjectsOperationsCancelArgs) -> String {
        format!("gcp::dialogflow::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with DialogflowProjectsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsOperationsGetArgs> for GoogleLongrunningOperation {
    fn generate_resource_id(&self, input: &DialogflowProjectsOperationsGetArgs) -> String {
        format!("gcp::dialogflow::GoogleLongrunningOperation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningListOperationsResponse with DialogflowProjectsOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DialogflowProjectsOperationsListArgs>
    for GoogleLongrunningListOperationsResponse
{
    fn generate_resource_id(&self, input: &DialogflowProjectsOperationsListArgs) -> String {
        format!(
            "gcp::dialogflow::GoogleLongrunningListOperationsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::dialogflow::GoogleLongrunningListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
