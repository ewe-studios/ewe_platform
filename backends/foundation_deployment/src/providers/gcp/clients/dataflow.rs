//! Auto-generated API clients for gcp/dataflow.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/dataflow`

#![cfg(feature = "gcp/dataflow")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1b3/projects/{projectId}/snapshots
/// Deletes a snapshot.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_delete_snapshots_execute()` to send, or `dataflow_projects_delete_snapshots` for simplest API.

pub fn dataflow_projects_delete_snapshots_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    location: Option<&str>,
    snapshotId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/snapshots",
        projectId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = location {
        query_parts.push(format!("location={}", val));
    }
    if let Some(val) = snapshotId {
        query_parts.push(format!("snapshotId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/snapshots
/// Deletes a snapshot.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_delete_snapshots_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_delete_snapshots_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_delete_snapshots_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeleteSnapshotResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeleteSnapshotResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/snapshots
/// Deletes a snapshot.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_delete_snapshots_builder()` + `dataflow_projects_delete_snapshots_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_delete_snapshots(
    client: &SimpleHttpClient,
    projectId: &str,
    location: Option<&str>,
    snapshotId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeleteSnapshotResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataflow_projects_delete_snapshots_builder(client, projectId, location, snapshotId)?;
    dataflow_projects_delete_snapshots_execute(builder)
}

/// GET v1b3/projects/{projectId}/WorkerMessages
/// Send a worker_message to the service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_worker_messages_execute()` to send, or `dataflow_projects_worker_messages` for simplest API.

pub fn dataflow_projects_worker_messages_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    body: &SendWorkerMessagesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/WorkerMessages",
        projectId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/WorkerMessages
/// Send a worker_message to the service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_worker_messages_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_worker_messages_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_worker_messages_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SendWorkerMessagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SendWorkerMessagesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/WorkerMessages
/// Send a worker_message to the service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_worker_messages_builder()` + `dataflow_projects_worker_messages_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_worker_messages(
    client: &SimpleHttpClient,
    projectId: &str,
    body: &SendWorkerMessagesRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SendWorkerMessagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_worker_messages_builder(client, projectId, body)?;
    dataflow_projects_worker_messages_execute(builder)
}

/// GET v1b3/projects/{projectId}/jobs:aggregated
/// List the jobs of a project across all regions. **Note:** This method doesn't support filtering the list of jobs by name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_jobs_aggregated_execute()` to send, or `dataflow_projects_jobs_aggregated` for simplest API.

pub fn dataflow_projects_jobs_aggregated_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    filter: Option<&str>,
    location: Option<&str>,
    name: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/jobs:aggregated",
        projectId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = location {
        query_parts.push(format!("location={}", val));
    }
    if let Some(val) = name {
        query_parts.push(format!("name={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/jobs:aggregated
/// List the jobs of a project across all regions. **Note:** This method doesn't support filtering the list of jobs by name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_jobs_aggregated_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_jobs_aggregated_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_aggregated_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/jobs:aggregated
/// List the jobs of a project across all regions. **Note:** This method doesn't support filtering the list of jobs by name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_jobs_aggregated_builder()` + `dataflow_projects_jobs_aggregated_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_aggregated(
    client: &SimpleHttpClient,
    projectId: &str,
    filter: Option<&str>,
    location: Option<&str>,
    name: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_jobs_aggregated_builder(
        client, projectId, filter, location, name, pageSize, pageToken, view,
    )?;
    dataflow_projects_jobs_aggregated_execute(builder)
}

/// GET v1b3/projects/{projectId}/jobs
/// Creates a Dataflow job. To create a job, we recommend using projects.locations.jobs.create with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.create is not recommended, as your job will always start in us-central1. Do not enter confidential information when you supply string values using the API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_jobs_create_execute()` to send, or `dataflow_projects_jobs_create` for simplest API.

pub fn dataflow_projects_jobs_create_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    location: Option<&str>,
    replaceJobId: Option<&str>,
    view: Option<&str>,
    body: &Job,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/jobs",
        projectId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = location {
        query_parts.push(format!("location={}", val));
    }
    if let Some(val) = replaceJobId {
        query_parts.push(format!("replaceJobId={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/jobs
/// Creates a Dataflow job. To create a job, we recommend using projects.locations.jobs.create with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.create is not recommended, as your job will always start in us-central1. Do not enter confidential information when you supply string values using the API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_jobs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_jobs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/jobs
/// Creates a Dataflow job. To create a job, we recommend using projects.locations.jobs.create with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.create is not recommended, as your job will always start in us-central1. Do not enter confidential information when you supply string values using the API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_jobs_create_builder()` + `dataflow_projects_jobs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_create(
    client: &SimpleHttpClient,
    projectId: &str,
    location: Option<&str>,
    replaceJobId: Option<&str>,
    view: Option<&str>,
    body: &Job,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataflow_projects_jobs_create_builder(
        client,
        projectId,
        location,
        replaceJobId,
        view,
        body,
    )?;
    dataflow_projects_jobs_create_execute(builder)
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}
/// Gets the state of the specified Cloud Dataflow job. To get the state of a job, we recommend using projects.locations.jobs.get with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.get is not recommended, as you can only get the state of jobs that are running in us-central1.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_jobs_get_execute()` to send, or `dataflow_projects_jobs_get` for simplest API.

pub fn dataflow_projects_jobs_get_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    jobId: &str,
    location: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/jobs/{}",
        projectId, jobId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = location {
        query_parts.push(format!("location={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}
/// Gets the state of the specified Cloud Dataflow job. To get the state of a job, we recommend using projects.locations.jobs.get with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.get is not recommended, as you can only get the state of jobs that are running in us-central1.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_jobs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_jobs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}
/// Gets the state of the specified Cloud Dataflow job. To get the state of a job, we recommend using projects.locations.jobs.get with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.get is not recommended, as you can only get the state of jobs that are running in us-central1.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_jobs_get_builder()` + `dataflow_projects_jobs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_get(
    client: &SimpleHttpClient,
    projectId: &str,
    jobId: &str,
    location: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataflow_projects_jobs_get_builder(client, projectId, jobId, location, view)?;
    dataflow_projects_jobs_get_execute(builder)
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/metrics
/// Request the job status. To request the status of a job, we recommend using projects.locations.jobs.`getMetrics` with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.`getMetrics` is not recommended, as you can only request the status of jobs that are running in us-central1.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_jobs_get_metrics_execute()` to send, or `dataflow_projects_jobs_get_metrics` for simplest API.

pub fn dataflow_projects_jobs_get_metrics_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    jobId: &str,
    location: Option<&str>,
    startTime: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/jobs/{}/metrics",
        projectId, jobId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = location {
        query_parts.push(format!("location={}", val));
    }
    if let Some(val) = startTime {
        query_parts.push(format!("startTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/metrics
/// Request the job status. To request the status of a job, we recommend using projects.locations.jobs.`getMetrics` with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.`getMetrics` is not recommended, as you can only request the status of jobs that are running in us-central1.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_jobs_get_metrics_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_jobs_get_metrics_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_get_metrics_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JobMetrics>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: JobMetrics = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/metrics
/// Request the job status. To request the status of a job, we recommend using projects.locations.jobs.`getMetrics` with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.`getMetrics` is not recommended, as you can only request the status of jobs that are running in us-central1.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_jobs_get_metrics_builder()` + `dataflow_projects_jobs_get_metrics_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_get_metrics(
    client: &SimpleHttpClient,
    projectId: &str,
    jobId: &str,
    location: Option<&str>,
    startTime: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JobMetrics>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dataflow_projects_jobs_get_metrics_builder(client, projectId, jobId, location, startTime)?;
    dataflow_projects_jobs_get_metrics_execute(builder)
}

/// GET v1b3/projects/{projectId}/jobs
/// List the jobs of a project. To list the jobs of a project in a region, we recommend using projects.locations.jobs.list with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). To list the all jobs across all regions, use projects.jobs.aggregated. Using projects.jobs.list is not recommended, because you can only get the list of jobs that are running in us-central1. projects.locations.jobs.list and projects.jobs.list support filtering the list of jobs by name. Filtering by name isn't supported by projects.jobs.aggregated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_jobs_list_execute()` to send, or `dataflow_projects_jobs_list` for simplest API.

pub fn dataflow_projects_jobs_list_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    filter: Option<&str>,
    location: Option<&str>,
    name: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/jobs",
        projectId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = location {
        query_parts.push(format!("location={}", val));
    }
    if let Some(val) = name {
        query_parts.push(format!("name={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/jobs
/// List the jobs of a project. To list the jobs of a project in a region, we recommend using projects.locations.jobs.list with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). To list the all jobs across all regions, use projects.jobs.aggregated. Using projects.jobs.list is not recommended, because you can only get the list of jobs that are running in us-central1. projects.locations.jobs.list and projects.jobs.list support filtering the list of jobs by name. Filtering by name isn't supported by projects.jobs.aggregated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_jobs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_jobs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/jobs
/// List the jobs of a project. To list the jobs of a project in a region, we recommend using projects.locations.jobs.list with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). To list the all jobs across all regions, use projects.jobs.aggregated. Using projects.jobs.list is not recommended, because you can only get the list of jobs that are running in us-central1. projects.locations.jobs.list and projects.jobs.list support filtering the list of jobs by name. Filtering by name isn't supported by projects.jobs.aggregated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_jobs_list_builder()` + `dataflow_projects_jobs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_list(
    client: &SimpleHttpClient,
    projectId: &str,
    filter: Option<&str>,
    location: Option<&str>,
    name: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_jobs_list_builder(
        client, projectId, filter, location, name, pageSize, pageToken, view,
    )?;
    dataflow_projects_jobs_list_execute(builder)
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}:snapshot
/// Snapshot the state of a streaming job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_jobs_snapshot_execute()` to send, or `dataflow_projects_jobs_snapshot` for simplest API.

pub fn dataflow_projects_jobs_snapshot_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    jobId: &str,
    body: &SnapshotJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/jobs/{}:snapshot",
        projectId, jobId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}:snapshot
/// Snapshot the state of a streaming job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_jobs_snapshot_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_jobs_snapshot_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_snapshot_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snapshot>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Snapshot = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}:snapshot
/// Snapshot the state of a streaming job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_jobs_snapshot_builder()` + `dataflow_projects_jobs_snapshot_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_snapshot(
    client: &SimpleHttpClient,
    projectId: &str,
    jobId: &str,
    body: &SnapshotJobRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snapshot>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataflow_projects_jobs_snapshot_builder(client, projectId, jobId, body)?;
    dataflow_projects_jobs_snapshot_execute(builder)
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}
/// Updates the state of an existing Cloud Dataflow job. To update the state of an existing job, we recommend using projects.locations.jobs.update with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.update is not recommended, as you can only update the state of jobs that are running in us-central1.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_jobs_update_execute()` to send, or `dataflow_projects_jobs_update` for simplest API.

pub fn dataflow_projects_jobs_update_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    jobId: &str,
    location: Option<&str>,
    updateMask: Option<&str>,
    body: &Job,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/jobs/{}",
        projectId, jobId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = location {
        query_parts.push(format!("location={}", val));
    }
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}
/// Updates the state of an existing Cloud Dataflow job. To update the state of an existing job, we recommend using projects.locations.jobs.update with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.update is not recommended, as you can only update the state of jobs that are running in us-central1.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_jobs_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_jobs_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}
/// Updates the state of an existing Cloud Dataflow job. To update the state of an existing job, we recommend using projects.locations.jobs.update with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.update is not recommended, as you can only update the state of jobs that are running in us-central1.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_jobs_update_builder()` + `dataflow_projects_jobs_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_update(
    client: &SimpleHttpClient,
    projectId: &str,
    jobId: &str,
    location: Option<&str>,
    updateMask: Option<&str>,
    body: &Job,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataflow_projects_jobs_update_builder(
        client, projectId, jobId, location, updateMask, body,
    )?;
    dataflow_projects_jobs_update_execute(builder)
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/debug/getConfig
/// Get encoded debug configuration for component. Not cacheable.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_jobs_debug_get_config_execute()` to send, or `dataflow_projects_jobs_debug_get_config` for simplest API.

pub fn dataflow_projects_jobs_debug_get_config_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    jobId: &str,
    body: &GetDebugConfigRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/jobs/{}/debug/getConfig",
        projectId, jobId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/debug/getConfig
/// Get encoded debug configuration for component. Not cacheable.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_jobs_debug_get_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_jobs_debug_get_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_debug_get_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetDebugConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetDebugConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/debug/getConfig
/// Get encoded debug configuration for component. Not cacheable.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_jobs_debug_get_config_builder()` + `dataflow_projects_jobs_debug_get_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_debug_get_config(
    client: &SimpleHttpClient,
    projectId: &str,
    jobId: &str,
    body: &GetDebugConfigRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetDebugConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_jobs_debug_get_config_builder(client, projectId, jobId, body)?;
    dataflow_projects_jobs_debug_get_config_execute(builder)
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/debug/sendCapture
/// Send encoded debug capture data for component.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_jobs_debug_send_capture_execute()` to send, or `dataflow_projects_jobs_debug_send_capture` for simplest API.

pub fn dataflow_projects_jobs_debug_send_capture_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    jobId: &str,
    body: &SendDebugCaptureRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/jobs/{}/debug/sendCapture",
        projectId, jobId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/debug/sendCapture
/// Send encoded debug capture data for component.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_jobs_debug_send_capture_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_jobs_debug_send_capture_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_debug_send_capture_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SendDebugCaptureResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SendDebugCaptureResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/debug/sendCapture
/// Send encoded debug capture data for component.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_jobs_debug_send_capture_builder()` + `dataflow_projects_jobs_debug_send_capture_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_debug_send_capture(
    client: &SimpleHttpClient,
    projectId: &str,
    jobId: &str,
    body: &SendDebugCaptureRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SendDebugCaptureResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataflow_projects_jobs_debug_send_capture_builder(client, projectId, jobId, body)?;
    dataflow_projects_jobs_debug_send_capture_execute(builder)
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/messages
/// Request the job status. To request the status of a job, we recommend using projects.locations.jobs.messages.list with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.messages.list is not recommended, as you can only request the status of jobs that are running in us-central1.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_jobs_messages_list_execute()` to send, or `dataflow_projects_jobs_messages_list` for simplest API.

pub fn dataflow_projects_jobs_messages_list_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    jobId: &str,
    endTime: Option<&str>,
    location: Option<&str>,
    minimumImportance: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    startTime: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/jobs/{}/messages",
        projectId, jobId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = endTime {
        query_parts.push(format!("endTime={}", val));
    }
    if let Some(val) = location {
        query_parts.push(format!("location={}", val));
    }
    if let Some(val) = minimumImportance {
        query_parts.push(format!("minimumImportance={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = startTime {
        query_parts.push(format!("startTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/messages
/// Request the job status. To request the status of a job, we recommend using projects.locations.jobs.messages.list with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.messages.list is not recommended, as you can only request the status of jobs that are running in us-central1.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_jobs_messages_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_jobs_messages_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_messages_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListJobMessagesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListJobMessagesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/messages
/// Request the job status. To request the status of a job, we recommend using projects.locations.jobs.messages.list with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.messages.list is not recommended, as you can only request the status of jobs that are running in us-central1.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_jobs_messages_list_builder()` + `dataflow_projects_jobs_messages_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_messages_list(
    client: &SimpleHttpClient,
    projectId: &str,
    jobId: &str,
    endTime: Option<&str>,
    location: Option<&str>,
    minimumImportance: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    startTime: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListJobMessagesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_jobs_messages_list_builder(
        client,
        projectId,
        jobId,
        endTime,
        location,
        minimumImportance,
        pageSize,
        pageToken,
        startTime,
    )?;
    dataflow_projects_jobs_messages_list_execute(builder)
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/workItems:lease
/// Leases a dataflow WorkItem to run.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_jobs_work_items_lease_execute()` to send, or `dataflow_projects_jobs_work_items_lease` for simplest API.

pub fn dataflow_projects_jobs_work_items_lease_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    jobId: &str,
    body: &LeaseWorkItemRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/jobs/{}/workItems:lease",
        projectId, jobId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/workItems:lease
/// Leases a dataflow WorkItem to run.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_jobs_work_items_lease_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_jobs_work_items_lease_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_work_items_lease_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LeaseWorkItemResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LeaseWorkItemResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/workItems:lease
/// Leases a dataflow WorkItem to run.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_jobs_work_items_lease_builder()` + `dataflow_projects_jobs_work_items_lease_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_work_items_lease(
    client: &SimpleHttpClient,
    projectId: &str,
    jobId: &str,
    body: &LeaseWorkItemRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LeaseWorkItemResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_jobs_work_items_lease_builder(client, projectId, jobId, body)?;
    dataflow_projects_jobs_work_items_lease_execute(builder)
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/workItems:reportStatus
/// Reports the status of dataflow WorkItems leased by a worker.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_jobs_work_items_report_status_execute()` to send, or `dataflow_projects_jobs_work_items_report_status` for simplest API.

pub fn dataflow_projects_jobs_work_items_report_status_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    jobId: &str,
    body: &ReportWorkItemStatusRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/jobs/{}/workItems:reportStatus",
        projectId, jobId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/workItems:reportStatus
/// Reports the status of dataflow WorkItems leased by a worker.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_jobs_work_items_report_status_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_jobs_work_items_report_status_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_work_items_report_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReportWorkItemStatusResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReportWorkItemStatusResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/workItems:reportStatus
/// Reports the status of dataflow WorkItems leased by a worker.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_jobs_work_items_report_status_builder()` + `dataflow_projects_jobs_work_items_report_status_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_work_items_report_status(
    client: &SimpleHttpClient,
    projectId: &str,
    jobId: &str,
    body: &ReportWorkItemStatusRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReportWorkItemStatusResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataflow_projects_jobs_work_items_report_status_builder(client, projectId, jobId, body)?;
    dataflow_projects_jobs_work_items_report_status_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/WorkerMessages
/// Send a worker_message to the service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_worker_messages_execute()` to send, or `dataflow_projects_locations_worker_messages` for simplest API.

pub fn dataflow_projects_locations_worker_messages_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    body: &SendWorkerMessagesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/WorkerMessages",
        projectId, location,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/WorkerMessages
/// Send a worker_message to the service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_locations_worker_messages_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_locations_worker_messages_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_worker_messages_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SendWorkerMessagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SendWorkerMessagesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/WorkerMessages
/// Send a worker_message to the service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_worker_messages_builder()` + `dataflow_projects_locations_worker_messages_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_worker_messages(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    body: &SendWorkerMessagesRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SendWorkerMessagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataflow_projects_locations_worker_messages_builder(client, projectId, location, body)?;
    dataflow_projects_locations_worker_messages_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/flexTemplates:launch
/// Launch a job with a FlexTemplate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_flex_templates_launch_execute()` to send, or `dataflow_projects_locations_flex_templates_launch` for simplest API.

pub fn dataflow_projects_locations_flex_templates_launch_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    body: &LaunchFlexTemplateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/flexTemplates:launch",
        projectId, location,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/flexTemplates:launch
/// Launch a job with a FlexTemplate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_locations_flex_templates_launch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_locations_flex_templates_launch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_flex_templates_launch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LaunchFlexTemplateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LaunchFlexTemplateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/flexTemplates:launch
/// Launch a job with a FlexTemplate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_flex_templates_launch_builder()` + `dataflow_projects_locations_flex_templates_launch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_flex_templates_launch(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    body: &LaunchFlexTemplateRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LaunchFlexTemplateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_flex_templates_launch_builder(
        client, projectId, location, body,
    )?;
    dataflow_projects_locations_flex_templates_launch_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs
/// Creates a Dataflow job. To create a job, we recommend using projects.locations.jobs.create with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.create is not recommended, as your job will always start in us-central1. Do not enter confidential information when you supply string values using the API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_jobs_create_execute()` to send, or `dataflow_projects_locations_jobs_create` for simplest API.

pub fn dataflow_projects_locations_jobs_create_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    replaceJobId: Option<&str>,
    view: Option<&str>,
    body: &Job,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs",
        projectId, location,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = replaceJobId {
        query_parts.push(format!("replaceJobId={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs
/// Creates a Dataflow job. To create a job, we recommend using projects.locations.jobs.create with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.create is not recommended, as your job will always start in us-central1. Do not enter confidential information when you supply string values using the API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_locations_jobs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_locations_jobs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs
/// Creates a Dataflow job. To create a job, we recommend using projects.locations.jobs.create with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.create is not recommended, as your job will always start in us-central1. Do not enter confidential information when you supply string values using the API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_jobs_create_builder()` + `dataflow_projects_locations_jobs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_create(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    replaceJobId: Option<&str>,
    view: Option<&str>,
    body: &Job,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_jobs_create_builder(
        client,
        projectId,
        location,
        replaceJobId,
        view,
        body,
    )?;
    dataflow_projects_locations_jobs_create_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}
/// Gets the state of the specified Cloud Dataflow job. To get the state of a job, we recommend using projects.locations.jobs.get with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.get is not recommended, as you can only get the state of jobs that are running in us-central1.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_jobs_get_execute()` to send, or `dataflow_projects_locations_jobs_get` for simplest API.

pub fn dataflow_projects_locations_jobs_get_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    jobId: &str,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}",
        projectId, location, jobId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}
/// Gets the state of the specified Cloud Dataflow job. To get the state of a job, we recommend using projects.locations.jobs.get with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.get is not recommended, as you can only get the state of jobs that are running in us-central1.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_locations_jobs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_locations_jobs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}
/// Gets the state of the specified Cloud Dataflow job. To get the state of a job, we recommend using projects.locations.jobs.get with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.get is not recommended, as you can only get the state of jobs that are running in us-central1.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_jobs_get_builder()` + `dataflow_projects_locations_jobs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_get(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    jobId: &str,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dataflow_projects_locations_jobs_get_builder(client, projectId, location, jobId, view)?;
    dataflow_projects_locations_jobs_get_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/executionDetails
/// Request detailed information about the execution status of the job. EXPERIMENTAL. This API is subject to change or removal without notice.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_jobs_get_execution_details_execute()` to send, or `dataflow_projects_locations_jobs_get_execution_details` for simplest API.

pub fn dataflow_projects_locations_jobs_get_execution_details_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    jobId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}/executionDetails",
        projectId, location, jobId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/executionDetails
/// Request detailed information about the execution status of the job. EXPERIMENTAL. This API is subject to change or removal without notice.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_locations_jobs_get_execution_details_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_locations_jobs_get_execution_details_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_get_execution_details_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JobExecutionDetails>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: JobExecutionDetails = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/executionDetails
/// Request detailed information about the execution status of the job. EXPERIMENTAL. This API is subject to change or removal without notice.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_jobs_get_execution_details_builder()` + `dataflow_projects_locations_jobs_get_execution_details_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_get_execution_details(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    jobId: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JobExecutionDetails>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_jobs_get_execution_details_builder(
        client, projectId, location, jobId, pageSize, pageToken,
    )?;
    dataflow_projects_locations_jobs_get_execution_details_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/metrics
/// Request the job status. To request the status of a job, we recommend using projects.locations.jobs.`getMetrics` with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.`getMetrics` is not recommended, as you can only request the status of jobs that are running in us-central1.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_jobs_get_metrics_execute()` to send, or `dataflow_projects_locations_jobs_get_metrics` for simplest API.

pub fn dataflow_projects_locations_jobs_get_metrics_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    jobId: &str,
    startTime: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}/metrics",
        projectId, location, jobId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = startTime {
        query_parts.push(format!("startTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/metrics
/// Request the job status. To request the status of a job, we recommend using projects.locations.jobs.`getMetrics` with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.`getMetrics` is not recommended, as you can only request the status of jobs that are running in us-central1.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_locations_jobs_get_metrics_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_locations_jobs_get_metrics_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_get_metrics_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JobMetrics>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: JobMetrics = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/metrics
/// Request the job status. To request the status of a job, we recommend using projects.locations.jobs.`getMetrics` with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.`getMetrics` is not recommended, as you can only request the status of jobs that are running in us-central1.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_jobs_get_metrics_builder()` + `dataflow_projects_locations_jobs_get_metrics_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_get_metrics(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    jobId: &str,
    startTime: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JobMetrics>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_jobs_get_metrics_builder(
        client, projectId, location, jobId, startTime,
    )?;
    dataflow_projects_locations_jobs_get_metrics_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs
/// List the jobs of a project. To list the jobs of a project in a region, we recommend using projects.locations.jobs.list with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). To list the all jobs across all regions, use projects.jobs.aggregated. Using projects.jobs.list is not recommended, because you can only get the list of jobs that are running in us-central1. projects.locations.jobs.list and projects.jobs.list support filtering the list of jobs by name. Filtering by name isn't supported by projects.jobs.aggregated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_jobs_list_execute()` to send, or `dataflow_projects_locations_jobs_list` for simplest API.

pub fn dataflow_projects_locations_jobs_list_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    filter: Option<&str>,
    name: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs",
        projectId, location,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = name {
        query_parts.push(format!("name={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs
/// List the jobs of a project. To list the jobs of a project in a region, we recommend using projects.locations.jobs.list with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). To list the all jobs across all regions, use projects.jobs.aggregated. Using projects.jobs.list is not recommended, because you can only get the list of jobs that are running in us-central1. projects.locations.jobs.list and projects.jobs.list support filtering the list of jobs by name. Filtering by name isn't supported by projects.jobs.aggregated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_locations_jobs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_locations_jobs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs
/// List the jobs of a project. To list the jobs of a project in a region, we recommend using projects.locations.jobs.list with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). To list the all jobs across all regions, use projects.jobs.aggregated. Using projects.jobs.list is not recommended, because you can only get the list of jobs that are running in us-central1. projects.locations.jobs.list and projects.jobs.list support filtering the list of jobs by name. Filtering by name isn't supported by projects.jobs.aggregated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_jobs_list_builder()` + `dataflow_projects_locations_jobs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_list(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    filter: Option<&str>,
    name: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_jobs_list_builder(
        client, projectId, location, filter, name, pageSize, pageToken, view,
    )?;
    dataflow_projects_locations_jobs_list_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}:snapshot
/// Snapshot the state of a streaming job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_jobs_snapshot_execute()` to send, or `dataflow_projects_locations_jobs_snapshot` for simplest API.

pub fn dataflow_projects_locations_jobs_snapshot_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    jobId: &str,
    body: &SnapshotJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}:snapshot",
        projectId, location, jobId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}:snapshot
/// Snapshot the state of a streaming job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_locations_jobs_snapshot_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_locations_jobs_snapshot_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_snapshot_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snapshot>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Snapshot = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}:snapshot
/// Snapshot the state of a streaming job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_jobs_snapshot_builder()` + `dataflow_projects_locations_jobs_snapshot_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_snapshot(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    jobId: &str,
    body: &SnapshotJobRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snapshot>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_jobs_snapshot_builder(
        client, projectId, location, jobId, body,
    )?;
    dataflow_projects_locations_jobs_snapshot_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}
/// Updates the state of an existing Cloud Dataflow job. To update the state of an existing job, we recommend using projects.locations.jobs.update with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.update is not recommended, as you can only update the state of jobs that are running in us-central1.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_jobs_update_execute()` to send, or `dataflow_projects_locations_jobs_update` for simplest API.

pub fn dataflow_projects_locations_jobs_update_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    jobId: &str,
    updateMask: Option<&str>,
    body: &Job,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}",
        projectId, location, jobId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}
/// Updates the state of an existing Cloud Dataflow job. To update the state of an existing job, we recommend using projects.locations.jobs.update with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.update is not recommended, as you can only update the state of jobs that are running in us-central1.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_locations_jobs_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_locations_jobs_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}
/// Updates the state of an existing Cloud Dataflow job. To update the state of an existing job, we recommend using projects.locations.jobs.update with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.update is not recommended, as you can only update the state of jobs that are running in us-central1.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_jobs_update_builder()` + `dataflow_projects_locations_jobs_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_update(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    jobId: &str,
    updateMask: Option<&str>,
    body: &Job,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_jobs_update_builder(
        client, projectId, location, jobId, updateMask, body,
    )?;
    dataflow_projects_locations_jobs_update_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/debug/getConfig
/// Get encoded debug configuration for component. Not cacheable.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_jobs_debug_get_config_execute()` to send, or `dataflow_projects_locations_jobs_debug_get_config` for simplest API.

pub fn dataflow_projects_locations_jobs_debug_get_config_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    jobId: &str,
    body: &GetDebugConfigRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}/debug/getConfig",
        projectId, location, jobId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/debug/getConfig
/// Get encoded debug configuration for component. Not cacheable.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_locations_jobs_debug_get_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_locations_jobs_debug_get_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_debug_get_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetDebugConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetDebugConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/debug/getConfig
/// Get encoded debug configuration for component. Not cacheable.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_jobs_debug_get_config_builder()` + `dataflow_projects_locations_jobs_debug_get_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_debug_get_config(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    jobId: &str,
    body: &GetDebugConfigRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetDebugConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_jobs_debug_get_config_builder(
        client, projectId, location, jobId, body,
    )?;
    dataflow_projects_locations_jobs_debug_get_config_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/debug/getWorkerStacktraces
/// Get worker stacktraces from debug capture.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_jobs_debug_get_worker_stacktraces_execute()` to send, or `dataflow_projects_locations_jobs_debug_get_worker_stacktraces` for simplest API.

pub fn dataflow_projects_locations_jobs_debug_get_worker_stacktraces_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    jobId: &str,
    body: &GetWorkerStacktracesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}/debug/getWorkerStacktraces",
        projectId,
        location,
        jobId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/debug/getWorkerStacktraces
/// Get worker stacktraces from debug capture.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_locations_jobs_debug_get_worker_stacktraces_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_locations_jobs_debug_get_worker_stacktraces_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_debug_get_worker_stacktraces_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetWorkerStacktracesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetWorkerStacktracesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/debug/getWorkerStacktraces
/// Get worker stacktraces from debug capture.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_jobs_debug_get_worker_stacktraces_builder()` + `dataflow_projects_locations_jobs_debug_get_worker_stacktraces_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_debug_get_worker_stacktraces(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    jobId: &str,
    body: &GetWorkerStacktracesRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetWorkerStacktracesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_jobs_debug_get_worker_stacktraces_builder(
        client, projectId, location, jobId, body,
    )?;
    dataflow_projects_locations_jobs_debug_get_worker_stacktraces_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/debug/sendCapture
/// Send encoded debug capture data for component.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_jobs_debug_send_capture_execute()` to send, or `dataflow_projects_locations_jobs_debug_send_capture` for simplest API.

pub fn dataflow_projects_locations_jobs_debug_send_capture_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    jobId: &str,
    body: &SendDebugCaptureRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}/debug/sendCapture",
        projectId, location, jobId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/debug/sendCapture
/// Send encoded debug capture data for component.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_locations_jobs_debug_send_capture_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_locations_jobs_debug_send_capture_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_debug_send_capture_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SendDebugCaptureResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SendDebugCaptureResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/debug/sendCapture
/// Send encoded debug capture data for component.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_jobs_debug_send_capture_builder()` + `dataflow_projects_locations_jobs_debug_send_capture_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_debug_send_capture(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    jobId: &str,
    body: &SendDebugCaptureRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SendDebugCaptureResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_jobs_debug_send_capture_builder(
        client, projectId, location, jobId, body,
    )?;
    dataflow_projects_locations_jobs_debug_send_capture_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/messages
/// Request the job status. To request the status of a job, we recommend using projects.locations.jobs.messages.list with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.messages.list is not recommended, as you can only request the status of jobs that are running in us-central1.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_jobs_messages_list_execute()` to send, or `dataflow_projects_locations_jobs_messages_list` for simplest API.

pub fn dataflow_projects_locations_jobs_messages_list_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    jobId: &str,
    endTime: Option<&str>,
    minimumImportance: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    startTime: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}/messages",
        projectId, location, jobId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = endTime {
        query_parts.push(format!("endTime={}", val));
    }
    if let Some(val) = minimumImportance {
        query_parts.push(format!("minimumImportance={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = startTime {
        query_parts.push(format!("startTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/messages
/// Request the job status. To request the status of a job, we recommend using projects.locations.jobs.messages.list with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.messages.list is not recommended, as you can only request the status of jobs that are running in us-central1.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_locations_jobs_messages_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_locations_jobs_messages_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_messages_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListJobMessagesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListJobMessagesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/messages
/// Request the job status. To request the status of a job, we recommend using projects.locations.jobs.messages.list with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.messages.list is not recommended, as you can only request the status of jobs that are running in us-central1.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_jobs_messages_list_builder()` + `dataflow_projects_locations_jobs_messages_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_messages_list(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    jobId: &str,
    endTime: Option<&str>,
    minimumImportance: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    startTime: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListJobMessagesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_jobs_messages_list_builder(
        client,
        projectId,
        location,
        jobId,
        endTime,
        minimumImportance,
        pageSize,
        pageToken,
        startTime,
    )?;
    dataflow_projects_locations_jobs_messages_list_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/snapshots
/// Lists snapshots.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_jobs_snapshots_list_execute()` to send, or `dataflow_projects_locations_jobs_snapshots_list` for simplest API.

pub fn dataflow_projects_locations_jobs_snapshots_list_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    jobId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}/snapshots",
        projectId, location, jobId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/snapshots
/// Lists snapshots.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_locations_jobs_snapshots_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_locations_jobs_snapshots_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_snapshots_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSnapshotsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSnapshotsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/snapshots
/// Lists snapshots.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_jobs_snapshots_list_builder()` + `dataflow_projects_locations_jobs_snapshots_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_snapshots_list(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    jobId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSnapshotsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_jobs_snapshots_list_builder(
        client, projectId, location, jobId,
    )?;
    dataflow_projects_locations_jobs_snapshots_list_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/stages/{stageId}/executionDetails
/// Request detailed information about the execution status of a stage of the job. EXPERIMENTAL. This API is subject to change or removal without notice.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_jobs_stages_get_execution_details_execute()` to send, or `dataflow_projects_locations_jobs_stages_get_execution_details` for simplest API.

pub fn dataflow_projects_locations_jobs_stages_get_execution_details_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    jobId: &str,
    stageId: &str,
    endTime: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    startTime: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}/stages/{}/executionDetails",
        projectId,
        location,
        jobId,
        stageId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = endTime {
        query_parts.push(format!("endTime={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = startTime {
        query_parts.push(format!("startTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/stages/{stageId}/executionDetails
/// Request detailed information about the execution status of a stage of the job. EXPERIMENTAL. This API is subject to change or removal without notice.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_locations_jobs_stages_get_execution_details_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_locations_jobs_stages_get_execution_details_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_stages_get_execution_details_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StageExecutionDetails>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StageExecutionDetails = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/stages/{stageId}/executionDetails
/// Request detailed information about the execution status of a stage of the job. EXPERIMENTAL. This API is subject to change or removal without notice.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_jobs_stages_get_execution_details_builder()` + `dataflow_projects_locations_jobs_stages_get_execution_details_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_stages_get_execution_details(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    jobId: &str,
    stageId: &str,
    endTime: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    startTime: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StageExecutionDetails>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_jobs_stages_get_execution_details_builder(
        client, projectId, location, jobId, stageId, endTime, pageSize, pageToken, startTime,
    )?;
    dataflow_projects_locations_jobs_stages_get_execution_details_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/workItems:lease
/// Leases a dataflow WorkItem to run.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_jobs_work_items_lease_execute()` to send, or `dataflow_projects_locations_jobs_work_items_lease` for simplest API.

pub fn dataflow_projects_locations_jobs_work_items_lease_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    jobId: &str,
    body: &LeaseWorkItemRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}/workItems:lease",
        projectId, location, jobId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/workItems:lease
/// Leases a dataflow WorkItem to run.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_locations_jobs_work_items_lease_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_locations_jobs_work_items_lease_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_work_items_lease_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LeaseWorkItemResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LeaseWorkItemResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/workItems:lease
/// Leases a dataflow WorkItem to run.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_jobs_work_items_lease_builder()` + `dataflow_projects_locations_jobs_work_items_lease_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_work_items_lease(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    jobId: &str,
    body: &LeaseWorkItemRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LeaseWorkItemResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_jobs_work_items_lease_builder(
        client, projectId, location, jobId, body,
    )?;
    dataflow_projects_locations_jobs_work_items_lease_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/workItems:reportStatus
/// Reports the status of dataflow WorkItems leased by a worker.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_jobs_work_items_report_status_execute()` to send, or `dataflow_projects_locations_jobs_work_items_report_status` for simplest API.

pub fn dataflow_projects_locations_jobs_work_items_report_status_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    jobId: &str,
    body: &ReportWorkItemStatusRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}/workItems:reportStatus",
        projectId,
        location,
        jobId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/workItems:reportStatus
/// Reports the status of dataflow WorkItems leased by a worker.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_locations_jobs_work_items_report_status_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_locations_jobs_work_items_report_status_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_work_items_report_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReportWorkItemStatusResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReportWorkItemStatusResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/workItems:reportStatus
/// Reports the status of dataflow WorkItems leased by a worker.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_jobs_work_items_report_status_builder()` + `dataflow_projects_locations_jobs_work_items_report_status_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_work_items_report_status(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    jobId: &str,
    body: &ReportWorkItemStatusRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReportWorkItemStatusResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_jobs_work_items_report_status_builder(
        client, projectId, location, jobId, body,
    )?;
    dataflow_projects_locations_jobs_work_items_report_status_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/snapshots/{snapshotId}
/// Deletes a snapshot.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_snapshots_delete_execute()` to send, or `dataflow_projects_locations_snapshots_delete` for simplest API.

pub fn dataflow_projects_locations_snapshots_delete_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    snapshotId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/snapshots/{}",
        projectId, location, snapshotId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/snapshots/{snapshotId}
/// Deletes a snapshot.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_locations_snapshots_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_locations_snapshots_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_snapshots_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeleteSnapshotResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeleteSnapshotResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/snapshots/{snapshotId}
/// Deletes a snapshot.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_snapshots_delete_builder()` + `dataflow_projects_locations_snapshots_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_snapshots_delete(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    snapshotId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeleteSnapshotResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_snapshots_delete_builder(
        client, projectId, location, snapshotId,
    )?;
    dataflow_projects_locations_snapshots_delete_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/snapshots/{snapshotId}
/// Gets information about a snapshot.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_snapshots_get_execute()` to send, or `dataflow_projects_locations_snapshots_get` for simplest API.

pub fn dataflow_projects_locations_snapshots_get_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    snapshotId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/snapshots/{}",
        projectId, location, snapshotId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/snapshots/{snapshotId}
/// Gets information about a snapshot.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_locations_snapshots_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_locations_snapshots_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_snapshots_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snapshot>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Snapshot = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/snapshots/{snapshotId}
/// Gets information about a snapshot.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_snapshots_get_builder()` + `dataflow_projects_locations_snapshots_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_snapshots_get(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    snapshotId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snapshot>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dataflow_projects_locations_snapshots_get_builder(client, projectId, location, snapshotId)?;
    dataflow_projects_locations_snapshots_get_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/snapshots
/// Lists snapshots.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_snapshots_list_execute()` to send, or `dataflow_projects_locations_snapshots_list` for simplest API.

pub fn dataflow_projects_locations_snapshots_list_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    jobId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/snapshots",
        projectId, location,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = jobId {
        query_parts.push(format!("jobId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/snapshots
/// Lists snapshots.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_locations_snapshots_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_locations_snapshots_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_snapshots_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSnapshotsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSnapshotsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/snapshots
/// Lists snapshots.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_snapshots_list_builder()` + `dataflow_projects_locations_snapshots_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_snapshots_list(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    jobId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSnapshotsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataflow_projects_locations_snapshots_list_builder(client, projectId, location, jobId)?;
    dataflow_projects_locations_snapshots_list_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/templates
/// Creates a Cloud Dataflow job from a template. Do not enter confidential information when you supply string values using the API. To create a job, we recommend using projects.locations.templates.create with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.create is not recommended, because your job will always start in us-central1.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_templates_create_execute()` to send, or `dataflow_projects_locations_templates_create` for simplest API.

pub fn dataflow_projects_locations_templates_create_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    body: &CreateJobFromTemplateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/templates",
        projectId, location,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/templates
/// Creates a Cloud Dataflow job from a template. Do not enter confidential information when you supply string values using the API. To create a job, we recommend using projects.locations.templates.create with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.create is not recommended, because your job will always start in us-central1.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_locations_templates_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_locations_templates_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_templates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/templates
/// Creates a Cloud Dataflow job from a template. Do not enter confidential information when you supply string values using the API. To create a job, we recommend using projects.locations.templates.create with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.create is not recommended, because your job will always start in us-central1.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_templates_create_builder()` + `dataflow_projects_locations_templates_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_templates_create(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    body: &CreateJobFromTemplateRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dataflow_projects_locations_templates_create_builder(client, projectId, location, body)?;
    dataflow_projects_locations_templates_create_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/templates:get
/// Get the template associated with a template. To get the template, we recommend using projects.locations.templates.get with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.get is not recommended, because only templates that are running in us-central1 are retrieved.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_templates_get_execute()` to send, or `dataflow_projects_locations_templates_get` for simplest API.

pub fn dataflow_projects_locations_templates_get_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    gcsPath: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/templates:get",
        projectId, location,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = gcsPath {
        query_parts.push(format!("gcsPath={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/templates:get
/// Get the template associated with a template. To get the template, we recommend using projects.locations.templates.get with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.get is not recommended, because only templates that are running in us-central1 are retrieved.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_locations_templates_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_locations_templates_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetTemplateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetTemplateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/templates:get
/// Get the template associated with a template. To get the template, we recommend using projects.locations.templates.get with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.get is not recommended, because only templates that are running in us-central1 are retrieved.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_templates_get_builder()` + `dataflow_projects_locations_templates_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_templates_get(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    gcsPath: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetTemplateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_templates_get_builder(
        client, projectId, location, gcsPath, view,
    )?;
    dataflow_projects_locations_templates_get_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/templates:launch
/// Launches a template. To launch a template, we recommend using projects.locations.templates.launch with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.launch is not recommended, because jobs launched from the template will always start in us-central1.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_templates_launch_execute()` to send, or `dataflow_projects_locations_templates_launch` for simplest API.

pub fn dataflow_projects_locations_templates_launch_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    dynamicTemplate_gcsPath: Option<&str>,
    dynamicTemplate_stagingLocation: Option<&str>,
    gcsPath: Option<&str>,
    validateOnly: Option<bool>,
    body: &LaunchTemplateParameters,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/templates:launch",
        projectId, location,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = dynamicTemplate_gcsPath {
        query_parts.push(format!("dynamicTemplate_gcsPath={}", val));
    }
    if let Some(val) = dynamicTemplate_stagingLocation {
        query_parts.push(format!("dynamicTemplate_stagingLocation={}", val));
    }
    if let Some(val) = gcsPath {
        query_parts.push(format!("gcsPath={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/templates:launch
/// Launches a template. To launch a template, we recommend using projects.locations.templates.launch with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.launch is not recommended, because jobs launched from the template will always start in us-central1.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_locations_templates_launch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_locations_templates_launch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_templates_launch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LaunchTemplateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LaunchTemplateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/templates:launch
/// Launches a template. To launch a template, we recommend using projects.locations.templates.launch with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.launch is not recommended, because jobs launched from the template will always start in us-central1.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_templates_launch_builder()` + `dataflow_projects_locations_templates_launch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_templates_launch(
    client: &SimpleHttpClient,
    projectId: &str,
    location: &str,
    dynamicTemplate_gcsPath: Option<&str>,
    dynamicTemplate_stagingLocation: Option<&str>,
    gcsPath: Option<&str>,
    validateOnly: Option<bool>,
    body: &LaunchTemplateParameters,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LaunchTemplateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_templates_launch_builder(
        client,
        projectId,
        location,
        dynamicTemplate_gcsPath,
        dynamicTemplate_stagingLocation,
        gcsPath,
        validateOnly,
        body,
    )?;
    dataflow_projects_locations_templates_launch_execute(builder)
}

/// GET v1b3/projects/{projectId}/snapshots/{snapshotId}
/// Gets information about a snapshot.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_snapshots_get_execute()` to send, or `dataflow_projects_snapshots_get` for simplest API.

pub fn dataflow_projects_snapshots_get_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    snapshotId: &str,
    location: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/snapshots/{}",
        projectId, snapshotId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = location {
        query_parts.push(format!("location={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/snapshots/{snapshotId}
/// Gets information about a snapshot.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_snapshots_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_snapshots_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_snapshots_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snapshot>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Snapshot = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/snapshots/{snapshotId}
/// Gets information about a snapshot.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_snapshots_get_builder()` + `dataflow_projects_snapshots_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_snapshots_get(
    client: &SimpleHttpClient,
    projectId: &str,
    snapshotId: &str,
    location: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snapshot>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataflow_projects_snapshots_get_builder(client, projectId, snapshotId, location)?;
    dataflow_projects_snapshots_get_execute(builder)
}

/// GET v1b3/projects/{projectId}/snapshots
/// Lists snapshots.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_snapshots_list_execute()` to send, or `dataflow_projects_snapshots_list` for simplest API.

pub fn dataflow_projects_snapshots_list_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    jobId: Option<&str>,
    location: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/snapshots",
        projectId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = jobId {
        query_parts.push(format!("jobId={}", val));
    }
    if let Some(val) = location {
        query_parts.push(format!("location={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/snapshots
/// Lists snapshots.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_snapshots_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_snapshots_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_snapshots_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSnapshotsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSnapshotsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/snapshots
/// Lists snapshots.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_snapshots_list_builder()` + `dataflow_projects_snapshots_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_snapshots_list(
    client: &SimpleHttpClient,
    projectId: &str,
    jobId: Option<&str>,
    location: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSnapshotsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_snapshots_list_builder(client, projectId, jobId, location)?;
    dataflow_projects_snapshots_list_execute(builder)
}

/// GET v1b3/projects/{projectId}/templates
/// Creates a Cloud Dataflow job from a template. Do not enter confidential information when you supply string values using the API. To create a job, we recommend using projects.locations.templates.create with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.create is not recommended, because your job will always start in us-central1.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_templates_create_execute()` to send, or `dataflow_projects_templates_create` for simplest API.

pub fn dataflow_projects_templates_create_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    body: &CreateJobFromTemplateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/templates",
        projectId,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/templates
/// Creates a Cloud Dataflow job from a template. Do not enter confidential information when you supply string values using the API. To create a job, we recommend using projects.locations.templates.create with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.create is not recommended, because your job will always start in us-central1.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_templates_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_templates_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_templates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/templates
/// Creates a Cloud Dataflow job from a template. Do not enter confidential information when you supply string values using the API. To create a job, we recommend using projects.locations.templates.create with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.create is not recommended, because your job will always start in us-central1.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_templates_create_builder()` + `dataflow_projects_templates_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_templates_create(
    client: &SimpleHttpClient,
    projectId: &str,
    body: &CreateJobFromTemplateRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataflow_projects_templates_create_builder(client, projectId, body)?;
    dataflow_projects_templates_create_execute(builder)
}

/// GET v1b3/projects/{projectId}/templates:get
/// Get the template associated with a template. To get the template, we recommend using projects.locations.templates.get with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.get is not recommended, because only templates that are running in us-central1 are retrieved.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_templates_get_execute()` to send, or `dataflow_projects_templates_get` for simplest API.

pub fn dataflow_projects_templates_get_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    gcsPath: Option<&str>,
    location: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/templates:get",
        projectId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = gcsPath {
        query_parts.push(format!("gcsPath={}", val));
    }
    if let Some(val) = location {
        query_parts.push(format!("location={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/templates:get
/// Get the template associated with a template. To get the template, we recommend using projects.locations.templates.get with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.get is not recommended, because only templates that are running in us-central1 are retrieved.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_templates_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_templates_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetTemplateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetTemplateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/templates:get
/// Get the template associated with a template. To get the template, we recommend using projects.locations.templates.get with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.get is not recommended, because only templates that are running in us-central1 are retrieved.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_templates_get_builder()` + `dataflow_projects_templates_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_templates_get(
    client: &SimpleHttpClient,
    projectId: &str,
    gcsPath: Option<&str>,
    location: Option<&str>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetTemplateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataflow_projects_templates_get_builder(client, projectId, gcsPath, location, view)?;
    dataflow_projects_templates_get_execute(builder)
}

/// GET v1b3/projects/{projectId}/templates:launch
/// Launches a template. To launch a template, we recommend using projects.locations.templates.launch with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.launch is not recommended, because jobs launched from the template will always start in us-central1.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_templates_launch_execute()` to send, or `dataflow_projects_templates_launch` for simplest API.

pub fn dataflow_projects_templates_launch_builder(
    client: &SimpleHttpClient,
    projectId: &str,
    dynamicTemplate_gcsPath: Option<&str>,
    dynamicTemplate_stagingLocation: Option<&str>,
    gcsPath: Option<&str>,
    location: Option<&str>,
    validateOnly: Option<bool>,
    body: &LaunchTemplateParameters,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/templates:launch",
        projectId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = dynamicTemplate_gcsPath {
        query_parts.push(format!("dynamicTemplate_gcsPath={}", val));
    }
    if let Some(val) = dynamicTemplate_stagingLocation {
        query_parts.push(format!("dynamicTemplate_stagingLocation={}", val));
    }
    if let Some(val) = gcsPath {
        query_parts.push(format!("gcsPath={}", val));
    }
    if let Some(val) = location {
        query_parts.push(format!("location={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/templates:launch
/// Launches a template. To launch a template, we recommend using projects.locations.templates.launch with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.launch is not recommended, because jobs launched from the template will always start in us-central1.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dataflow_projects_templates_launch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dataflow_projects_templates_launch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_templates_launch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LaunchTemplateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LaunchTemplateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/templates:launch
/// Launches a template. To launch a template, we recommend using projects.locations.templates.launch with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.launch is not recommended, because jobs launched from the template will always start in us-central1.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_templates_launch_builder()` + `dataflow_projects_templates_launch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_templates_launch(
    client: &SimpleHttpClient,
    projectId: &str,
    dynamicTemplate_gcsPath: Option<&str>,
    dynamicTemplate_stagingLocation: Option<&str>,
    gcsPath: Option<&str>,
    location: Option<&str>,
    validateOnly: Option<bool>,
    body: &LaunchTemplateParameters,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LaunchTemplateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_templates_launch_builder(
        client,
        projectId,
        dynamicTemplate_gcsPath,
        dynamicTemplate_stagingLocation,
        gcsPath,
        location,
        validateOnly,
        body,
    )?;
    dataflow_projects_templates_launch_execute(builder)
}
