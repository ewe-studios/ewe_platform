//! Auto-generated API clients for gcp/dataflow.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1b3/projects/{projectId}/snapshots
/// Deletes a snapshot.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_delete_snapshots_execute()` to send, or `dataflow_projects_delete_snapshots` for simplest API.

pub fn dataflow_projects_delete_snapshots_builder(
    client: &SimpleHttpClient,
    projectId: String,
    location: Option<String>,
    snapshotId: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/snapshots",
        projectId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = location {
        query_parts.push(format!("location={}", val));
    }
    if let Some(val) = snapshotId {
        query_parts.push(format!("snapshotId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/snapshots
/// Deletes a snapshot.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_delete_snapshots_execute()` or `dataflow_projects_delete_snapshots`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_delete_snapshots_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_delete_snapshots_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeleteSnapshotResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeleteSnapshotResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/snapshots
/// Deletes a snapshot.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_delete_snapshots_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_delete_snapshots_task()`.
/// For the simplest API, use `dataflow_projects_delete_snapshots()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_delete_snapshots_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_delete_snapshots_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeleteSnapshotResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataflow_projects_delete_snapshots_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_delete_snapshots`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsDeleteSnapshotsArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Query parameter: location
    pub location: Option<String>,
    /// Query parameter: snapshotId
    pub snapshotId: Option<String>,
}

/// GET v1b3/projects/{projectId}/snapshots
/// Deletes a snapshot.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_delete_snapshots_builder()` + `dataflow_projects_delete_snapshots_execute()`.
/// For task-level control, use `dataflow_projects_delete_snapshots_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_delete_snapshots(
    client: &SimpleHttpClient,
    args: &DataflowProjectsDeleteSnapshotsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeleteSnapshotResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_delete_snapshots_builder(
        client,
        args.projectId.clone(),
        args.location.clone(),
        args.snapshotId.clone(),
    )?;
    dataflow_projects_delete_snapshots_execute(builder)
}

/// GET v1b3/projects/{projectId}/WorkerMessages
/// Send a worker_message to the service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_worker_messages_execute()` to send, or `dataflow_projects_worker_messages` for simplest API.

pub fn dataflow_projects_worker_messages_builder(
    client: &SimpleHttpClient,
    projectId: String,
    body: &SendWorkerMessagesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/WorkerMessages",
        projectId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/WorkerMessages
/// Send a worker_message to the service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_worker_messages_execute()` or `dataflow_projects_worker_messages`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_worker_messages_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_worker_messages_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SendWorkerMessagesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SendWorkerMessagesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/WorkerMessages
/// Send a worker_message to the service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_worker_messages_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_worker_messages_task()`.
/// For the simplest API, use `dataflow_projects_worker_messages()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_worker_messages_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_worker_messages_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SendWorkerMessagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataflow_projects_worker_messages_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_worker_messages`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsWorkerMessagesArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Request body.
    pub body: SendWorkerMessagesRequest,
}

/// GET v1b3/projects/{projectId}/WorkerMessages
/// Send a worker_message to the service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_worker_messages_builder()` + `dataflow_projects_worker_messages_execute()`.
/// For task-level control, use `dataflow_projects_worker_messages_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_worker_messages(
    client: &SimpleHttpClient,
    args: &DataflowProjectsWorkerMessagesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SendWorkerMessagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dataflow_projects_worker_messages_builder(client, args.projectId.clone(), &args.body)?;
    dataflow_projects_worker_messages_execute(builder)
}

/// GET v1b3/projects/{projectId}/jobs:aggregated
/// List the jobs of a project across all regions. **Note:** This method doesn't support filtering the list of jobs by name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_jobs_aggregated_execute()` to send, or `dataflow_projects_jobs_aggregated` for simplest API.

pub fn dataflow_projects_jobs_aggregated_builder(
    client: &SimpleHttpClient,
    projectId: String,
    filter: Option<String>,
    location: Option<String>,
    name: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
    view: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/jobs:aggregated",
        projectId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = location {
        query_parts.push(format!("location={}", val));
    }
    if let Some(val) = name {
        query_parts.push(format!("name={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/jobs:aggregated
/// List the jobs of a project across all regions. **Note:** This method doesn't support filtering the list of jobs by name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_jobs_aggregated_execute()` or `dataflow_projects_jobs_aggregated`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_jobs_aggregated_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_aggregated_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListJobsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/jobs:aggregated
/// List the jobs of a project across all regions. **Note:** This method doesn't support filtering the list of jobs by name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_jobs_aggregated_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_jobs_aggregated_task()`.
/// For the simplest API, use `dataflow_projects_jobs_aggregated()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_jobs_aggregated_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_jobs_aggregated_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataflow_projects_jobs_aggregated_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_jobs_aggregated`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsJobsAggregatedArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: location
    pub location: Option<String>,
    /// Query parameter: name
    pub name: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1b3/projects/{projectId}/jobs:aggregated
/// List the jobs of a project across all regions. **Note:** This method doesn't support filtering the list of jobs by name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_jobs_aggregated_builder()` + `dataflow_projects_jobs_aggregated_execute()`.
/// For task-level control, use `dataflow_projects_jobs_aggregated_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_aggregated(
    client: &SimpleHttpClient,
    args: &DataflowProjectsJobsAggregatedArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_jobs_aggregated_builder(
        client,
        args.projectId.clone(),
        args.filter.clone(),
        args.location.clone(),
        args.name.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
        args.view.clone(),
    )?;
    dataflow_projects_jobs_aggregated_execute(builder)
}

/// GET v1b3/projects/{projectId}/jobs
/// Creates a Dataflow job. To create a job, we recommend using projects.locations.jobs.create with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.create is not recommended, as your job will always start in us-central1. Do not enter confidential information when you supply string values using the API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_jobs_create_execute()` to send, or `dataflow_projects_jobs_create` for simplest API.

pub fn dataflow_projects_jobs_create_builder(
    client: &SimpleHttpClient,
    projectId: String,
    location: Option<String>,
    replaceJobId: Option<String>,
    view: Option<String>,
    body: &Job,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/jobs",
        projectId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = location {
        query_parts.push(format!("location={}", val));
    }
    if let Some(val) = replaceJobId {
        query_parts.push(format!("replaceJobId={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/jobs
/// Creates a Dataflow job. To create a job, we recommend using projects.locations.jobs.create with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.create is not recommended, as your job will always start in us-central1. Do not enter confidential information when you supply string values using the API.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_jobs_create_execute()` or `dataflow_projects_jobs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Job>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/jobs
/// Creates a Dataflow job. To create a job, we recommend using projects.locations.jobs.create with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.create is not recommended, as your job will always start in us-central1. Do not enter confidential information when you supply string values using the API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_jobs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_jobs_create_task()`.
/// For the simplest API, use `dataflow_projects_jobs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataflow_projects_jobs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_jobs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsJobsCreateArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Query parameter: location
    pub location: Option<String>,
    /// Query parameter: replaceJobId
    pub replaceJobId: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
    /// Request body.
    pub body: Job,
}

/// GET v1b3/projects/{projectId}/jobs
/// Creates a Dataflow job. To create a job, we recommend using projects.locations.jobs.create with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.create is not recommended, as your job will always start in us-central1. Do not enter confidential information when you supply string values using the API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_jobs_create_builder()` + `dataflow_projects_jobs_create_execute()`.
/// For task-level control, use `dataflow_projects_jobs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_create(
    client: &SimpleHttpClient,
    args: &DataflowProjectsJobsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataflow_projects_jobs_create_builder(
        client,
        args.projectId.clone(),
        args.location.clone(),
        args.replaceJobId.clone(),
        args.view.clone(),
        &args.body,
    )?;
    dataflow_projects_jobs_create_execute(builder)
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}
/// Gets the state of the specified Cloud Dataflow job. To get the state of a job, we recommend using projects.locations.jobs.get with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.get is not recommended, as you can only get the state of jobs that are running in us-central1.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_jobs_get_execute()` to send, or `dataflow_projects_jobs_get` for simplest API.

pub fn dataflow_projects_jobs_get_builder(
    client: &SimpleHttpClient,
    projectId: String,
    jobId: String,
    location: Option<String>,
    view: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/jobs/{}",
        projectId.as_str(),
        jobId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = location {
        query_parts.push(format!("location={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}
/// Gets the state of the specified Cloud Dataflow job. To get the state of a job, we recommend using projects.locations.jobs.get with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.get is not recommended, as you can only get the state of jobs that are running in us-central1.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_jobs_get_execute()` or `dataflow_projects_jobs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Job>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}
/// Gets the state of the specified Cloud Dataflow job. To get the state of a job, we recommend using projects.locations.jobs.get with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.get is not recommended, as you can only get the state of jobs that are running in us-central1.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_jobs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_jobs_get_task()`.
/// For the simplest API, use `dataflow_projects_jobs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataflow_projects_jobs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_jobs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsJobsGetArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: jobId
    pub jobId: String,
    /// Query parameter: location
    pub location: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}
/// Gets the state of the specified Cloud Dataflow job. To get the state of a job, we recommend using projects.locations.jobs.get with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.get is not recommended, as you can only get the state of jobs that are running in us-central1.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_jobs_get_builder()` + `dataflow_projects_jobs_get_execute()`.
/// For task-level control, use `dataflow_projects_jobs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_get(
    client: &SimpleHttpClient,
    args: &DataflowProjectsJobsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataflow_projects_jobs_get_builder(
        client,
        args.projectId.clone(),
        args.jobId.clone(),
        args.location.clone(),
        args.view.clone(),
    )?;
    dataflow_projects_jobs_get_execute(builder)
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/metrics
/// Request the job status. To request the status of a job, we recommend using projects.locations.jobs.`getMetrics` with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.`getMetrics` is not recommended, as you can only request the status of jobs that are running in us-central1.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_jobs_get_metrics_execute()` to send, or `dataflow_projects_jobs_get_metrics` for simplest API.

pub fn dataflow_projects_jobs_get_metrics_builder(
    client: &SimpleHttpClient,
    projectId: String,
    jobId: String,
    location: Option<String>,
    startTime: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/jobs/{}/metrics",
        projectId.as_str(),
        jobId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = location {
        query_parts.push(format!("location={}", val));
    }
    if let Some(val) = startTime {
        query_parts.push(format!("startTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/metrics
/// Request the job status. To request the status of a job, we recommend using projects.locations.jobs.`getMetrics` with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.`getMetrics` is not recommended, as you can only request the status of jobs that are running in us-central1.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_jobs_get_metrics_execute()` or `dataflow_projects_jobs_get_metrics`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_jobs_get_metrics_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_get_metrics_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<JobMetrics>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: JobMetrics = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/metrics
/// Request the job status. To request the status of a job, we recommend using projects.locations.jobs.`getMetrics` with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.`getMetrics` is not recommended, as you can only request the status of jobs that are running in us-central1.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_jobs_get_metrics_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_jobs_get_metrics_task()`.
/// For the simplest API, use `dataflow_projects_jobs_get_metrics()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_jobs_get_metrics_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_jobs_get_metrics_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JobMetrics>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataflow_projects_jobs_get_metrics_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_jobs_get_metrics`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsJobsGetMetricsArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: jobId
    pub jobId: String,
    /// Query parameter: location
    pub location: Option<String>,
    /// Query parameter: startTime
    pub startTime: Option<String>,
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/metrics
/// Request the job status. To request the status of a job, we recommend using projects.locations.jobs.`getMetrics` with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.`getMetrics` is not recommended, as you can only request the status of jobs that are running in us-central1.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_jobs_get_metrics_builder()` + `dataflow_projects_jobs_get_metrics_execute()`.
/// For task-level control, use `dataflow_projects_jobs_get_metrics_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_get_metrics(
    client: &SimpleHttpClient,
    args: &DataflowProjectsJobsGetMetricsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JobMetrics>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataflow_projects_jobs_get_metrics_builder(
        client,
        args.projectId.clone(),
        args.jobId.clone(),
        args.location.clone(),
        args.startTime.clone(),
    )?;
    dataflow_projects_jobs_get_metrics_execute(builder)
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}:snapshot
/// Snapshot the state of a streaming job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_jobs_snapshot_execute()` to send, or `dataflow_projects_jobs_snapshot` for simplest API.

pub fn dataflow_projects_jobs_snapshot_builder(
    client: &SimpleHttpClient,
    projectId: String,
    jobId: String,
    body: &SnapshotJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/jobs/{}:snapshot",
        projectId.as_str(),
        jobId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}:snapshot
/// Snapshot the state of a streaming job.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_jobs_snapshot_execute()` or `dataflow_projects_jobs_snapshot`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_jobs_snapshot_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_snapshot_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Snapshot>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Snapshot = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}:snapshot
/// Snapshot the state of a streaming job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_jobs_snapshot_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_jobs_snapshot_task()`.
/// For the simplest API, use `dataflow_projects_jobs_snapshot()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_jobs_snapshot_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_jobs_snapshot_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snapshot>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataflow_projects_jobs_snapshot_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_jobs_snapshot`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsJobsSnapshotArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: jobId
    pub jobId: String,
    /// Request body.
    pub body: SnapshotJobRequest,
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}:snapshot
/// Snapshot the state of a streaming job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_jobs_snapshot_builder()` + `dataflow_projects_jobs_snapshot_execute()`.
/// For task-level control, use `dataflow_projects_jobs_snapshot_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_snapshot(
    client: &SimpleHttpClient,
    args: &DataflowProjectsJobsSnapshotArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snapshot>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataflow_projects_jobs_snapshot_builder(
        client,
        args.projectId.clone(),
        args.jobId.clone(),
        &args.body,
    )?;
    dataflow_projects_jobs_snapshot_execute(builder)
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/debug/getConfig
/// Get encoded debug configuration for component. Not cacheable.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_jobs_debug_get_config_execute()` to send, or `dataflow_projects_jobs_debug_get_config` for simplest API.

pub fn dataflow_projects_jobs_debug_get_config_builder(
    client: &SimpleHttpClient,
    projectId: String,
    jobId: String,
    body: &GetDebugConfigRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/jobs/{}/debug/getConfig",
        projectId.as_str(),
        jobId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/debug/getConfig
/// Get encoded debug configuration for component. Not cacheable.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_jobs_debug_get_config_execute()` or `dataflow_projects_jobs_debug_get_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_jobs_debug_get_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_debug_get_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetDebugConfigResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetDebugConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/debug/getConfig
/// Get encoded debug configuration for component. Not cacheable.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_jobs_debug_get_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_jobs_debug_get_config_task()`.
/// For the simplest API, use `dataflow_projects_jobs_debug_get_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_jobs_debug_get_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_jobs_debug_get_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetDebugConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataflow_projects_jobs_debug_get_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_jobs_debug_get_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsJobsDebugGetConfigArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: jobId
    pub jobId: String,
    /// Request body.
    pub body: GetDebugConfigRequest,
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/debug/getConfig
/// Get encoded debug configuration for component. Not cacheable.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_jobs_debug_get_config_builder()` + `dataflow_projects_jobs_debug_get_config_execute()`.
/// For task-level control, use `dataflow_projects_jobs_debug_get_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_debug_get_config(
    client: &SimpleHttpClient,
    args: &DataflowProjectsJobsDebugGetConfigArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetDebugConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_jobs_debug_get_config_builder(
        client,
        args.projectId.clone(),
        args.jobId.clone(),
        &args.body,
    )?;
    dataflow_projects_jobs_debug_get_config_execute(builder)
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/debug/sendCapture
/// Send encoded debug capture data for component.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_jobs_debug_send_capture_execute()` to send, or `dataflow_projects_jobs_debug_send_capture` for simplest API.

pub fn dataflow_projects_jobs_debug_send_capture_builder(
    client: &SimpleHttpClient,
    projectId: String,
    jobId: String,
    body: &SendDebugCaptureRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/jobs/{}/debug/sendCapture",
        projectId.as_str(),
        jobId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/debug/sendCapture
/// Send encoded debug capture data for component.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_jobs_debug_send_capture_execute()` or `dataflow_projects_jobs_debug_send_capture`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_jobs_debug_send_capture_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_debug_send_capture_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SendDebugCaptureResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SendDebugCaptureResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/debug/sendCapture
/// Send encoded debug capture data for component.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_jobs_debug_send_capture_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_jobs_debug_send_capture_task()`.
/// For the simplest API, use `dataflow_projects_jobs_debug_send_capture()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_jobs_debug_send_capture_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_jobs_debug_send_capture_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SendDebugCaptureResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataflow_projects_jobs_debug_send_capture_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_jobs_debug_send_capture`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsJobsDebugSendCaptureArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: jobId
    pub jobId: String,
    /// Request body.
    pub body: SendDebugCaptureRequest,
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/debug/sendCapture
/// Send encoded debug capture data for component.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_jobs_debug_send_capture_builder()` + `dataflow_projects_jobs_debug_send_capture_execute()`.
/// For task-level control, use `dataflow_projects_jobs_debug_send_capture_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_debug_send_capture(
    client: &SimpleHttpClient,
    args: &DataflowProjectsJobsDebugSendCaptureArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SendDebugCaptureResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_jobs_debug_send_capture_builder(
        client,
        args.projectId.clone(),
        args.jobId.clone(),
        &args.body,
    )?;
    dataflow_projects_jobs_debug_send_capture_execute(builder)
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/messages
/// Request the job status. To request the status of a job, we recommend using projects.locations.jobs.messages.list with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.messages.list is not recommended, as you can only request the status of jobs that are running in us-central1.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_jobs_messages_list_execute()` to send, or `dataflow_projects_jobs_messages_list` for simplest API.

pub fn dataflow_projects_jobs_messages_list_builder(
    client: &SimpleHttpClient,
    projectId: String,
    jobId: String,
    endTime: Option<String>,
    location: Option<String>,
    minimumImportance: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
    startTime: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/jobs/{}/messages",
        projectId.as_str(),
        jobId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = endTime {
        query_parts.push(format!("endTime={}", val));
    }
    if let Some(val) = location {
        query_parts.push(format!("location={}", val));
    }
    if let Some(val) = minimumImportance {
        query_parts.push(format!("minimumImportance={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = startTime {
        query_parts.push(format!("startTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/messages
/// Request the job status. To request the status of a job, we recommend using projects.locations.jobs.messages.list with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.messages.list is not recommended, as you can only request the status of jobs that are running in us-central1.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_jobs_messages_list_execute()` or `dataflow_projects_jobs_messages_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_jobs_messages_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_messages_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListJobMessagesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListJobMessagesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/messages
/// Request the job status. To request the status of a job, we recommend using projects.locations.jobs.messages.list with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.messages.list is not recommended, as you can only request the status of jobs that are running in us-central1.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_jobs_messages_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_jobs_messages_list_task()`.
/// For the simplest API, use `dataflow_projects_jobs_messages_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_jobs_messages_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_jobs_messages_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListJobMessagesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataflow_projects_jobs_messages_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_jobs_messages_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsJobsMessagesListArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: jobId
    pub jobId: String,
    /// Query parameter: endTime
    pub endTime: Option<String>,
    /// Query parameter: location
    pub location: Option<String>,
    /// Query parameter: minimumImportance
    pub minimumImportance: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: startTime
    pub startTime: Option<String>,
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/messages
/// Request the job status. To request the status of a job, we recommend using projects.locations.jobs.messages.list with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.messages.list is not recommended, as you can only request the status of jobs that are running in us-central1.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_jobs_messages_list_builder()` + `dataflow_projects_jobs_messages_list_execute()`.
/// For task-level control, use `dataflow_projects_jobs_messages_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_messages_list(
    client: &SimpleHttpClient,
    args: &DataflowProjectsJobsMessagesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListJobMessagesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_jobs_messages_list_builder(
        client,
        args.projectId.clone(),
        args.jobId.clone(),
        args.endTime.clone(),
        args.location.clone(),
        args.minimumImportance.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
        args.startTime.clone(),
    )?;
    dataflow_projects_jobs_messages_list_execute(builder)
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/workItems:lease
/// Leases a dataflow WorkItem to run.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_jobs_work_items_lease_execute()` to send, or `dataflow_projects_jobs_work_items_lease` for simplest API.

pub fn dataflow_projects_jobs_work_items_lease_builder(
    client: &SimpleHttpClient,
    projectId: String,
    jobId: String,
    body: &LeaseWorkItemRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/jobs/{}/workItems:lease",
        projectId.as_str(),
        jobId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/workItems:lease
/// Leases a dataflow WorkItem to run.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_jobs_work_items_lease_execute()` or `dataflow_projects_jobs_work_items_lease`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_jobs_work_items_lease_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_work_items_lease_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LeaseWorkItemResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LeaseWorkItemResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/workItems:lease
/// Leases a dataflow WorkItem to run.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_jobs_work_items_lease_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_jobs_work_items_lease_task()`.
/// For the simplest API, use `dataflow_projects_jobs_work_items_lease()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_jobs_work_items_lease_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_jobs_work_items_lease_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LeaseWorkItemResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataflow_projects_jobs_work_items_lease_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_jobs_work_items_lease`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsJobsWorkItemsLeaseArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: jobId
    pub jobId: String,
    /// Request body.
    pub body: LeaseWorkItemRequest,
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/workItems:lease
/// Leases a dataflow WorkItem to run.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_jobs_work_items_lease_builder()` + `dataflow_projects_jobs_work_items_lease_execute()`.
/// For task-level control, use `dataflow_projects_jobs_work_items_lease_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_work_items_lease(
    client: &SimpleHttpClient,
    args: &DataflowProjectsJobsWorkItemsLeaseArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LeaseWorkItemResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_jobs_work_items_lease_builder(
        client,
        args.projectId.clone(),
        args.jobId.clone(),
        &args.body,
    )?;
    dataflow_projects_jobs_work_items_lease_execute(builder)
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/workItems:reportStatus
/// Reports the status of dataflow WorkItems leased by a worker.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_jobs_work_items_report_status_execute()` to send, or `dataflow_projects_jobs_work_items_report_status` for simplest API.

pub fn dataflow_projects_jobs_work_items_report_status_builder(
    client: &SimpleHttpClient,
    projectId: String,
    jobId: String,
    body: &ReportWorkItemStatusRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/jobs/{}/workItems:reportStatus",
        projectId.as_str(),
        jobId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/workItems:reportStatus
/// Reports the status of dataflow WorkItems leased by a worker.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_jobs_work_items_report_status_execute()` or `dataflow_projects_jobs_work_items_report_status`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_jobs_work_items_report_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_work_items_report_status_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReportWorkItemStatusResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReportWorkItemStatusResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/workItems:reportStatus
/// Reports the status of dataflow WorkItems leased by a worker.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_jobs_work_items_report_status_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_jobs_work_items_report_status_task()`.
/// For the simplest API, use `dataflow_projects_jobs_work_items_report_status()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_jobs_work_items_report_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_jobs_work_items_report_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReportWorkItemStatusResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataflow_projects_jobs_work_items_report_status_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_jobs_work_items_report_status`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsJobsWorkItemsReportStatusArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: jobId
    pub jobId: String,
    /// Request body.
    pub body: ReportWorkItemStatusRequest,
}

/// GET v1b3/projects/{projectId}/jobs/{jobId}/workItems:reportStatus
/// Reports the status of dataflow WorkItems leased by a worker.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_jobs_work_items_report_status_builder()` + `dataflow_projects_jobs_work_items_report_status_execute()`.
/// For task-level control, use `dataflow_projects_jobs_work_items_report_status_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_jobs_work_items_report_status(
    client: &SimpleHttpClient,
    args: &DataflowProjectsJobsWorkItemsReportStatusArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReportWorkItemStatusResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_jobs_work_items_report_status_builder(
        client,
        args.projectId.clone(),
        args.jobId.clone(),
        &args.body,
    )?;
    dataflow_projects_jobs_work_items_report_status_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/WorkerMessages
/// Send a worker_message to the service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_worker_messages_execute()` to send, or `dataflow_projects_locations_worker_messages` for simplest API.

pub fn dataflow_projects_locations_worker_messages_builder(
    client: &SimpleHttpClient,
    projectId: String,
    location: String,
    body: &SendWorkerMessagesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/WorkerMessages",
        projectId.as_str(),
        location.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/WorkerMessages
/// Send a worker_message to the service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_locations_worker_messages_execute()` or `dataflow_projects_locations_worker_messages`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_worker_messages_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_worker_messages_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SendWorkerMessagesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SendWorkerMessagesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/locations/{location}/WorkerMessages
/// Send a worker_message to the service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_locations_worker_messages_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_locations_worker_messages_task()`.
/// For the simplest API, use `dataflow_projects_locations_worker_messages()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_worker_messages_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_locations_worker_messages_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SendWorkerMessagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataflow_projects_locations_worker_messages_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_locations_worker_messages`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsWorkerMessagesArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: location
    pub location: String,
    /// Request body.
    pub body: SendWorkerMessagesRequest,
}

/// GET v1b3/projects/{projectId}/locations/{location}/WorkerMessages
/// Send a worker_message to the service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_worker_messages_builder()` + `dataflow_projects_locations_worker_messages_execute()`.
/// For task-level control, use `dataflow_projects_locations_worker_messages_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_worker_messages(
    client: &SimpleHttpClient,
    args: &DataflowProjectsLocationsWorkerMessagesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SendWorkerMessagesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_worker_messages_builder(
        client,
        args.projectId.clone(),
        args.location.clone(),
        &args.body,
    )?;
    dataflow_projects_locations_worker_messages_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/flexTemplates:launch
/// Launch a job with a FlexTemplate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_flex_templates_launch_execute()` to send, or `dataflow_projects_locations_flex_templates_launch` for simplest API.

pub fn dataflow_projects_locations_flex_templates_launch_builder(
    client: &SimpleHttpClient,
    projectId: String,
    location: String,
    body: &LaunchFlexTemplateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/flexTemplates:launch",
        projectId.as_str(),
        location.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/flexTemplates:launch
/// Launch a job with a FlexTemplate.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_locations_flex_templates_launch_execute()` or `dataflow_projects_locations_flex_templates_launch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_flex_templates_launch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_flex_templates_launch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LaunchFlexTemplateResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LaunchFlexTemplateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/locations/{location}/flexTemplates:launch
/// Launch a job with a FlexTemplate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_locations_flex_templates_launch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_locations_flex_templates_launch_task()`.
/// For the simplest API, use `dataflow_projects_locations_flex_templates_launch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_flex_templates_launch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_locations_flex_templates_launch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LaunchFlexTemplateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataflow_projects_locations_flex_templates_launch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_locations_flex_templates_launch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsFlexTemplatesLaunchArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: location
    pub location: String,
    /// Request body.
    pub body: LaunchFlexTemplateRequest,
}

/// GET v1b3/projects/{projectId}/locations/{location}/flexTemplates:launch
/// Launch a job with a FlexTemplate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_flex_templates_launch_builder()` + `dataflow_projects_locations_flex_templates_launch_execute()`.
/// For task-level control, use `dataflow_projects_locations_flex_templates_launch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_flex_templates_launch(
    client: &SimpleHttpClient,
    args: &DataflowProjectsLocationsFlexTemplatesLaunchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LaunchFlexTemplateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_flex_templates_launch_builder(
        client,
        args.projectId.clone(),
        args.location.clone(),
        &args.body,
    )?;
    dataflow_projects_locations_flex_templates_launch_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs
/// Creates a Dataflow job. To create a job, we recommend using projects.locations.jobs.create with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.create is not recommended, as your job will always start in us-central1. Do not enter confidential information when you supply string values using the API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_jobs_create_execute()` to send, or `dataflow_projects_locations_jobs_create` for simplest API.

pub fn dataflow_projects_locations_jobs_create_builder(
    client: &SimpleHttpClient,
    projectId: String,
    location: String,
    replaceJobId: Option<String>,
    view: Option<String>,
    body: &Job,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs",
        projectId.as_str(),
        location.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = replaceJobId {
        query_parts.push(format!("replaceJobId={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs
/// Creates a Dataflow job. To create a job, we recommend using projects.locations.jobs.create with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.create is not recommended, as your job will always start in us-central1. Do not enter confidential information when you supply string values using the API.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_locations_jobs_create_execute()` or `dataflow_projects_locations_jobs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Job>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs
/// Creates a Dataflow job. To create a job, we recommend using projects.locations.jobs.create with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.create is not recommended, as your job will always start in us-central1. Do not enter confidential information when you supply string values using the API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_locations_jobs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_locations_jobs_create_task()`.
/// For the simplest API, use `dataflow_projects_locations_jobs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_locations_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataflow_projects_locations_jobs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_locations_jobs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsJobsCreateArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: location
    pub location: String,
    /// Query parameter: replaceJobId
    pub replaceJobId: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
    /// Request body.
    pub body: Job,
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs
/// Creates a Dataflow job. To create a job, we recommend using projects.locations.jobs.create with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.create is not recommended, as your job will always start in us-central1. Do not enter confidential information when you supply string values using the API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_jobs_create_builder()` + `dataflow_projects_locations_jobs_create_execute()`.
/// For task-level control, use `dataflow_projects_locations_jobs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_create(
    client: &SimpleHttpClient,
    args: &DataflowProjectsLocationsJobsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_jobs_create_builder(
        client,
        args.projectId.clone(),
        args.location.clone(),
        args.replaceJobId.clone(),
        args.view.clone(),
        &args.body,
    )?;
    dataflow_projects_locations_jobs_create_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}
/// Gets the state of the specified Cloud Dataflow job. To get the state of a job, we recommend using projects.locations.jobs.get with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.get is not recommended, as you can only get the state of jobs that are running in us-central1.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_jobs_get_execute()` to send, or `dataflow_projects_locations_jobs_get` for simplest API.

pub fn dataflow_projects_locations_jobs_get_builder(
    client: &SimpleHttpClient,
    projectId: String,
    location: String,
    jobId: String,
    view: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}",
        projectId.as_str(),
        location.as_str(),
        jobId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}
/// Gets the state of the specified Cloud Dataflow job. To get the state of a job, we recommend using projects.locations.jobs.get with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.get is not recommended, as you can only get the state of jobs that are running in us-central1.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_locations_jobs_get_execute()` or `dataflow_projects_locations_jobs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Job>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}
/// Gets the state of the specified Cloud Dataflow job. To get the state of a job, we recommend using projects.locations.jobs.get with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.get is not recommended, as you can only get the state of jobs that are running in us-central1.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_locations_jobs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_locations_jobs_get_task()`.
/// For the simplest API, use `dataflow_projects_locations_jobs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_jobs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_locations_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataflow_projects_locations_jobs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_locations_jobs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsJobsGetArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: location
    pub location: String,
    /// Path parameter: jobId
    pub jobId: String,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}
/// Gets the state of the specified Cloud Dataflow job. To get the state of a job, we recommend using projects.locations.jobs.get with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.get is not recommended, as you can only get the state of jobs that are running in us-central1.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_jobs_get_builder()` + `dataflow_projects_locations_jobs_get_execute()`.
/// For task-level control, use `dataflow_projects_locations_jobs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_get(
    client: &SimpleHttpClient,
    args: &DataflowProjectsLocationsJobsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_jobs_get_builder(
        client,
        args.projectId.clone(),
        args.location.clone(),
        args.jobId.clone(),
        args.view.clone(),
    )?;
    dataflow_projects_locations_jobs_get_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/executionDetails
/// Request detailed information about the execution status of the job. EXPERIMENTAL. This API is subject to change or removal without notice.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_jobs_get_execution_details_execute()` to send, or `dataflow_projects_locations_jobs_get_execution_details` for simplest API.

pub fn dataflow_projects_locations_jobs_get_execution_details_builder(
    client: &SimpleHttpClient,
    projectId: String,
    location: String,
    jobId: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}/executionDetails",
        projectId.as_str(),
        location.as_str(),
        jobId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/executionDetails
/// Request detailed information about the execution status of the job. EXPERIMENTAL. This API is subject to change or removal without notice.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_locations_jobs_get_execution_details_execute()` or `dataflow_projects_locations_jobs_get_execution_details`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_jobs_get_execution_details_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_get_execution_details_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<JobExecutionDetails>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: JobExecutionDetails = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/executionDetails
/// Request detailed information about the execution status of the job. EXPERIMENTAL. This API is subject to change or removal without notice.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_locations_jobs_get_execution_details_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_locations_jobs_get_execution_details_task()`.
/// For the simplest API, use `dataflow_projects_locations_jobs_get_execution_details()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_jobs_get_execution_details_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_locations_jobs_get_execution_details_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JobExecutionDetails>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataflow_projects_locations_jobs_get_execution_details_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_locations_jobs_get_execution_details`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsJobsGetExecutionDetailsArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: location
    pub location: String,
    /// Path parameter: jobId
    pub jobId: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/executionDetails
/// Request detailed information about the execution status of the job. EXPERIMENTAL. This API is subject to change or removal without notice.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_jobs_get_execution_details_builder()` + `dataflow_projects_locations_jobs_get_execution_details_execute()`.
/// For task-level control, use `dataflow_projects_locations_jobs_get_execution_details_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_get_execution_details(
    client: &SimpleHttpClient,
    args: &DataflowProjectsLocationsJobsGetExecutionDetailsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JobExecutionDetails>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_jobs_get_execution_details_builder(
        client,
        args.projectId.clone(),
        args.location.clone(),
        args.jobId.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    dataflow_projects_locations_jobs_get_execution_details_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/metrics
/// Request the job status. To request the status of a job, we recommend using projects.locations.jobs.`getMetrics` with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.`getMetrics` is not recommended, as you can only request the status of jobs that are running in us-central1.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_jobs_get_metrics_execute()` to send, or `dataflow_projects_locations_jobs_get_metrics` for simplest API.

pub fn dataflow_projects_locations_jobs_get_metrics_builder(
    client: &SimpleHttpClient,
    projectId: String,
    location: String,
    jobId: String,
    startTime: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}/metrics",
        projectId.as_str(),
        location.as_str(),
        jobId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = startTime {
        query_parts.push(format!("startTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/metrics
/// Request the job status. To request the status of a job, we recommend using projects.locations.jobs.`getMetrics` with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.`getMetrics` is not recommended, as you can only request the status of jobs that are running in us-central1.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_locations_jobs_get_metrics_execute()` or `dataflow_projects_locations_jobs_get_metrics`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_jobs_get_metrics_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_get_metrics_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<JobMetrics>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: JobMetrics = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/metrics
/// Request the job status. To request the status of a job, we recommend using projects.locations.jobs.`getMetrics` with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.`getMetrics` is not recommended, as you can only request the status of jobs that are running in us-central1.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_locations_jobs_get_metrics_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_locations_jobs_get_metrics_task()`.
/// For the simplest API, use `dataflow_projects_locations_jobs_get_metrics()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_jobs_get_metrics_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_locations_jobs_get_metrics_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JobMetrics>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataflow_projects_locations_jobs_get_metrics_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_locations_jobs_get_metrics`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsJobsGetMetricsArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: location
    pub location: String,
    /// Path parameter: jobId
    pub jobId: String,
    /// Query parameter: startTime
    pub startTime: Option<String>,
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/metrics
/// Request the job status. To request the status of a job, we recommend using projects.locations.jobs.`getMetrics` with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.`getMetrics` is not recommended, as you can only request the status of jobs that are running in us-central1.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_jobs_get_metrics_builder()` + `dataflow_projects_locations_jobs_get_metrics_execute()`.
/// For task-level control, use `dataflow_projects_locations_jobs_get_metrics_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_get_metrics(
    client: &SimpleHttpClient,
    args: &DataflowProjectsLocationsJobsGetMetricsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<JobMetrics>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_jobs_get_metrics_builder(
        client,
        args.projectId.clone(),
        args.location.clone(),
        args.jobId.clone(),
        args.startTime.clone(),
    )?;
    dataflow_projects_locations_jobs_get_metrics_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}:snapshot
/// Snapshot the state of a streaming job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_jobs_snapshot_execute()` to send, or `dataflow_projects_locations_jobs_snapshot` for simplest API.

pub fn dataflow_projects_locations_jobs_snapshot_builder(
    client: &SimpleHttpClient,
    projectId: String,
    location: String,
    jobId: String,
    body: &SnapshotJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}:snapshot",
        projectId.as_str(),
        location.as_str(),
        jobId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}:snapshot
/// Snapshot the state of a streaming job.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_locations_jobs_snapshot_execute()` or `dataflow_projects_locations_jobs_snapshot`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_jobs_snapshot_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_snapshot_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Snapshot>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Snapshot = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}:snapshot
/// Snapshot the state of a streaming job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_locations_jobs_snapshot_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_locations_jobs_snapshot_task()`.
/// For the simplest API, use `dataflow_projects_locations_jobs_snapshot()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_jobs_snapshot_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_locations_jobs_snapshot_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snapshot>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataflow_projects_locations_jobs_snapshot_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_locations_jobs_snapshot`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsJobsSnapshotArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: location
    pub location: String,
    /// Path parameter: jobId
    pub jobId: String,
    /// Request body.
    pub body: SnapshotJobRequest,
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}:snapshot
/// Snapshot the state of a streaming job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_jobs_snapshot_builder()` + `dataflow_projects_locations_jobs_snapshot_execute()`.
/// For task-level control, use `dataflow_projects_locations_jobs_snapshot_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_snapshot(
    client: &SimpleHttpClient,
    args: &DataflowProjectsLocationsJobsSnapshotArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snapshot>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_jobs_snapshot_builder(
        client,
        args.projectId.clone(),
        args.location.clone(),
        args.jobId.clone(),
        &args.body,
    )?;
    dataflow_projects_locations_jobs_snapshot_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/debug/getConfig
/// Get encoded debug configuration for component. Not cacheable.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_jobs_debug_get_config_execute()` to send, or `dataflow_projects_locations_jobs_debug_get_config` for simplest API.

pub fn dataflow_projects_locations_jobs_debug_get_config_builder(
    client: &SimpleHttpClient,
    projectId: String,
    location: String,
    jobId: String,
    body: &GetDebugConfigRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}/debug/getConfig",
        projectId.as_str(),
        location.as_str(),
        jobId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/debug/getConfig
/// Get encoded debug configuration for component. Not cacheable.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_locations_jobs_debug_get_config_execute()` or `dataflow_projects_locations_jobs_debug_get_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_jobs_debug_get_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_debug_get_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetDebugConfigResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetDebugConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/debug/getConfig
/// Get encoded debug configuration for component. Not cacheable.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_locations_jobs_debug_get_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_locations_jobs_debug_get_config_task()`.
/// For the simplest API, use `dataflow_projects_locations_jobs_debug_get_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_jobs_debug_get_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_locations_jobs_debug_get_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetDebugConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataflow_projects_locations_jobs_debug_get_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_locations_jobs_debug_get_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsJobsDebugGetConfigArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: location
    pub location: String,
    /// Path parameter: jobId
    pub jobId: String,
    /// Request body.
    pub body: GetDebugConfigRequest,
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/debug/getConfig
/// Get encoded debug configuration for component. Not cacheable.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_jobs_debug_get_config_builder()` + `dataflow_projects_locations_jobs_debug_get_config_execute()`.
/// For task-level control, use `dataflow_projects_locations_jobs_debug_get_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_debug_get_config(
    client: &SimpleHttpClient,
    args: &DataflowProjectsLocationsJobsDebugGetConfigArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetDebugConfigResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_jobs_debug_get_config_builder(
        client,
        args.projectId.clone(),
        args.location.clone(),
        args.jobId.clone(),
        &args.body,
    )?;
    dataflow_projects_locations_jobs_debug_get_config_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/debug/getWorkerStacktraces
/// Get worker stacktraces from debug capture.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_jobs_debug_get_worker_stacktraces_execute()` to send, or `dataflow_projects_locations_jobs_debug_get_worker_stacktraces` for simplest API.

pub fn dataflow_projects_locations_jobs_debug_get_worker_stacktraces_builder(
    client: &SimpleHttpClient,
    projectId: String,
    location: String,
    jobId: String,
    body: &GetWorkerStacktracesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}/debug/getWorkerStacktraces",
        projectId.as_str(),
        location.as_str(),
        jobId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/debug/getWorkerStacktraces
/// Get worker stacktraces from debug capture.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_locations_jobs_debug_get_worker_stacktraces_execute()` or `dataflow_projects_locations_jobs_debug_get_worker_stacktraces`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_jobs_debug_get_worker_stacktraces_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_debug_get_worker_stacktraces_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetWorkerStacktracesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetWorkerStacktracesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/debug/getWorkerStacktraces
/// Get worker stacktraces from debug capture.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_locations_jobs_debug_get_worker_stacktraces_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_locations_jobs_debug_get_worker_stacktraces_task()`.
/// For the simplest API, use `dataflow_projects_locations_jobs_debug_get_worker_stacktraces()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_jobs_debug_get_worker_stacktraces_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_locations_jobs_debug_get_worker_stacktraces_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetWorkerStacktracesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataflow_projects_locations_jobs_debug_get_worker_stacktraces_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_locations_jobs_debug_get_worker_stacktraces`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsJobsDebugGetWorkerStacktracesArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: location
    pub location: String,
    /// Path parameter: jobId
    pub jobId: String,
    /// Request body.
    pub body: GetWorkerStacktracesRequest,
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/debug/getWorkerStacktraces
/// Get worker stacktraces from debug capture.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_jobs_debug_get_worker_stacktraces_builder()` + `dataflow_projects_locations_jobs_debug_get_worker_stacktraces_execute()`.
/// For task-level control, use `dataflow_projects_locations_jobs_debug_get_worker_stacktraces_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_debug_get_worker_stacktraces(
    client: &SimpleHttpClient,
    args: &DataflowProjectsLocationsJobsDebugGetWorkerStacktracesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GetWorkerStacktracesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_jobs_debug_get_worker_stacktraces_builder(
        client,
        args.projectId.clone(),
        args.location.clone(),
        args.jobId.clone(),
        &args.body,
    )?;
    dataflow_projects_locations_jobs_debug_get_worker_stacktraces_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/debug/sendCapture
/// Send encoded debug capture data for component.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_jobs_debug_send_capture_execute()` to send, or `dataflow_projects_locations_jobs_debug_send_capture` for simplest API.

pub fn dataflow_projects_locations_jobs_debug_send_capture_builder(
    client: &SimpleHttpClient,
    projectId: String,
    location: String,
    jobId: String,
    body: &SendDebugCaptureRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}/debug/sendCapture",
        projectId.as_str(),
        location.as_str(),
        jobId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/debug/sendCapture
/// Send encoded debug capture data for component.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_locations_jobs_debug_send_capture_execute()` or `dataflow_projects_locations_jobs_debug_send_capture`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_jobs_debug_send_capture_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_debug_send_capture_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SendDebugCaptureResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SendDebugCaptureResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/debug/sendCapture
/// Send encoded debug capture data for component.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_locations_jobs_debug_send_capture_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_locations_jobs_debug_send_capture_task()`.
/// For the simplest API, use `dataflow_projects_locations_jobs_debug_send_capture()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_jobs_debug_send_capture_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_locations_jobs_debug_send_capture_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SendDebugCaptureResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataflow_projects_locations_jobs_debug_send_capture_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_locations_jobs_debug_send_capture`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsJobsDebugSendCaptureArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: location
    pub location: String,
    /// Path parameter: jobId
    pub jobId: String,
    /// Request body.
    pub body: SendDebugCaptureRequest,
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/debug/sendCapture
/// Send encoded debug capture data for component.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_jobs_debug_send_capture_builder()` + `dataflow_projects_locations_jobs_debug_send_capture_execute()`.
/// For task-level control, use `dataflow_projects_locations_jobs_debug_send_capture_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_debug_send_capture(
    client: &SimpleHttpClient,
    args: &DataflowProjectsLocationsJobsDebugSendCaptureArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SendDebugCaptureResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_jobs_debug_send_capture_builder(
        client,
        args.projectId.clone(),
        args.location.clone(),
        args.jobId.clone(),
        &args.body,
    )?;
    dataflow_projects_locations_jobs_debug_send_capture_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/messages
/// Request the job status. To request the status of a job, we recommend using projects.locations.jobs.messages.list with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.messages.list is not recommended, as you can only request the status of jobs that are running in us-central1.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_jobs_messages_list_execute()` to send, or `dataflow_projects_locations_jobs_messages_list` for simplest API.

pub fn dataflow_projects_locations_jobs_messages_list_builder(
    client: &SimpleHttpClient,
    projectId: String,
    location: String,
    jobId: String,
    endTime: Option<String>,
    minimumImportance: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
    startTime: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}/messages",
        projectId.as_str(),
        location.as_str(),
        jobId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = endTime {
        query_parts.push(format!("endTime={}", val));
    }
    if let Some(val) = minimumImportance {
        query_parts.push(format!("minimumImportance={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = startTime {
        query_parts.push(format!("startTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/messages
/// Request the job status. To request the status of a job, we recommend using projects.locations.jobs.messages.list with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.messages.list is not recommended, as you can only request the status of jobs that are running in us-central1.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_locations_jobs_messages_list_execute()` or `dataflow_projects_locations_jobs_messages_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_jobs_messages_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_messages_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListJobMessagesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListJobMessagesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/messages
/// Request the job status. To request the status of a job, we recommend using projects.locations.jobs.messages.list with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.messages.list is not recommended, as you can only request the status of jobs that are running in us-central1.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_locations_jobs_messages_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_locations_jobs_messages_list_task()`.
/// For the simplest API, use `dataflow_projects_locations_jobs_messages_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_jobs_messages_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_locations_jobs_messages_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListJobMessagesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataflow_projects_locations_jobs_messages_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_locations_jobs_messages_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsJobsMessagesListArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: location
    pub location: String,
    /// Path parameter: jobId
    pub jobId: String,
    /// Query parameter: endTime
    pub endTime: Option<String>,
    /// Query parameter: minimumImportance
    pub minimumImportance: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: startTime
    pub startTime: Option<String>,
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/messages
/// Request the job status. To request the status of a job, we recommend using projects.locations.jobs.messages.list with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.jobs.messages.list is not recommended, as you can only request the status of jobs that are running in us-central1.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_jobs_messages_list_builder()` + `dataflow_projects_locations_jobs_messages_list_execute()`.
/// For task-level control, use `dataflow_projects_locations_jobs_messages_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_messages_list(
    client: &SimpleHttpClient,
    args: &DataflowProjectsLocationsJobsMessagesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListJobMessagesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_jobs_messages_list_builder(
        client,
        args.projectId.clone(),
        args.location.clone(),
        args.jobId.clone(),
        args.endTime.clone(),
        args.minimumImportance.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
        args.startTime.clone(),
    )?;
    dataflow_projects_locations_jobs_messages_list_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/snapshots
/// Lists snapshots.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_jobs_snapshots_list_execute()` to send, or `dataflow_projects_locations_jobs_snapshots_list` for simplest API.

pub fn dataflow_projects_locations_jobs_snapshots_list_builder(
    client: &SimpleHttpClient,
    projectId: String,
    location: String,
    jobId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}/snapshots",
        projectId.as_str(),
        location.as_str(),
        jobId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/snapshots
/// Lists snapshots.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_locations_jobs_snapshots_list_execute()` or `dataflow_projects_locations_jobs_snapshots_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_jobs_snapshots_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_snapshots_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSnapshotsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSnapshotsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/snapshots
/// Lists snapshots.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_locations_jobs_snapshots_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_locations_jobs_snapshots_list_task()`.
/// For the simplest API, use `dataflow_projects_locations_jobs_snapshots_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_jobs_snapshots_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_locations_jobs_snapshots_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSnapshotsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataflow_projects_locations_jobs_snapshots_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_locations_jobs_snapshots_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsJobsSnapshotsListArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: location
    pub location: String,
    /// Path parameter: jobId
    pub jobId: String,
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/snapshots
/// Lists snapshots.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_jobs_snapshots_list_builder()` + `dataflow_projects_locations_jobs_snapshots_list_execute()`.
/// For task-level control, use `dataflow_projects_locations_jobs_snapshots_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_snapshots_list(
    client: &SimpleHttpClient,
    args: &DataflowProjectsLocationsJobsSnapshotsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSnapshotsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_jobs_snapshots_list_builder(
        client,
        args.projectId.clone(),
        args.location.clone(),
        args.jobId.clone(),
    )?;
    dataflow_projects_locations_jobs_snapshots_list_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/stages/{stageId}/executionDetails
/// Request detailed information about the execution status of a stage of the job. EXPERIMENTAL. This API is subject to change or removal without notice.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_jobs_stages_get_execution_details_execute()` to send, or `dataflow_projects_locations_jobs_stages_get_execution_details` for simplest API.

pub fn dataflow_projects_locations_jobs_stages_get_execution_details_builder(
    client: &SimpleHttpClient,
    projectId: String,
    location: String,
    jobId: String,
    stageId: String,
    endTime: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
    startTime: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}/stages/{}/executionDetails",
        projectId.as_str(),
        location.as_str(),
        jobId.as_str(),
        stageId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = endTime {
        query_parts.push(format!("endTime={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = startTime {
        query_parts.push(format!("startTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/stages/{stageId}/executionDetails
/// Request detailed information about the execution status of a stage of the job. EXPERIMENTAL. This API is subject to change or removal without notice.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_locations_jobs_stages_get_execution_details_execute()` or `dataflow_projects_locations_jobs_stages_get_execution_details`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_jobs_stages_get_execution_details_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_stages_get_execution_details_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StageExecutionDetails>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: StageExecutionDetails = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/stages/{stageId}/executionDetails
/// Request detailed information about the execution status of a stage of the job. EXPERIMENTAL. This API is subject to change or removal without notice.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_locations_jobs_stages_get_execution_details_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_locations_jobs_stages_get_execution_details_task()`.
/// For the simplest API, use `dataflow_projects_locations_jobs_stages_get_execution_details()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_jobs_stages_get_execution_details_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_locations_jobs_stages_get_execution_details_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StageExecutionDetails>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataflow_projects_locations_jobs_stages_get_execution_details_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_locations_jobs_stages_get_execution_details`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsJobsStagesGetExecutionDetailsArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: location
    pub location: String,
    /// Path parameter: jobId
    pub jobId: String,
    /// Path parameter: stageId
    pub stageId: String,
    /// Query parameter: endTime
    pub endTime: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: startTime
    pub startTime: Option<String>,
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/stages/{stageId}/executionDetails
/// Request detailed information about the execution status of a stage of the job. EXPERIMENTAL. This API is subject to change or removal without notice.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_jobs_stages_get_execution_details_builder()` + `dataflow_projects_locations_jobs_stages_get_execution_details_execute()`.
/// For task-level control, use `dataflow_projects_locations_jobs_stages_get_execution_details_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_stages_get_execution_details(
    client: &SimpleHttpClient,
    args: &DataflowProjectsLocationsJobsStagesGetExecutionDetailsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<StageExecutionDetails>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_jobs_stages_get_execution_details_builder(
        client,
        args.projectId.clone(),
        args.location.clone(),
        args.jobId.clone(),
        args.stageId.clone(),
        args.endTime.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
        args.startTime.clone(),
    )?;
    dataflow_projects_locations_jobs_stages_get_execution_details_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/workItems:lease
/// Leases a dataflow WorkItem to run.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_jobs_work_items_lease_execute()` to send, or `dataflow_projects_locations_jobs_work_items_lease` for simplest API.

pub fn dataflow_projects_locations_jobs_work_items_lease_builder(
    client: &SimpleHttpClient,
    projectId: String,
    location: String,
    jobId: String,
    body: &LeaseWorkItemRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}/workItems:lease",
        projectId.as_str(),
        location.as_str(),
        jobId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/workItems:lease
/// Leases a dataflow WorkItem to run.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_locations_jobs_work_items_lease_execute()` or `dataflow_projects_locations_jobs_work_items_lease`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_jobs_work_items_lease_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_work_items_lease_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LeaseWorkItemResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LeaseWorkItemResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/workItems:lease
/// Leases a dataflow WorkItem to run.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_locations_jobs_work_items_lease_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_locations_jobs_work_items_lease_task()`.
/// For the simplest API, use `dataflow_projects_locations_jobs_work_items_lease()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_jobs_work_items_lease_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_locations_jobs_work_items_lease_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LeaseWorkItemResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataflow_projects_locations_jobs_work_items_lease_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_locations_jobs_work_items_lease`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsJobsWorkItemsLeaseArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: location
    pub location: String,
    /// Path parameter: jobId
    pub jobId: String,
    /// Request body.
    pub body: LeaseWorkItemRequest,
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/workItems:lease
/// Leases a dataflow WorkItem to run.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_jobs_work_items_lease_builder()` + `dataflow_projects_locations_jobs_work_items_lease_execute()`.
/// For task-level control, use `dataflow_projects_locations_jobs_work_items_lease_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_work_items_lease(
    client: &SimpleHttpClient,
    args: &DataflowProjectsLocationsJobsWorkItemsLeaseArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LeaseWorkItemResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_jobs_work_items_lease_builder(
        client,
        args.projectId.clone(),
        args.location.clone(),
        args.jobId.clone(),
        &args.body,
    )?;
    dataflow_projects_locations_jobs_work_items_lease_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/workItems:reportStatus
/// Reports the status of dataflow WorkItems leased by a worker.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_jobs_work_items_report_status_execute()` to send, or `dataflow_projects_locations_jobs_work_items_report_status` for simplest API.

pub fn dataflow_projects_locations_jobs_work_items_report_status_builder(
    client: &SimpleHttpClient,
    projectId: String,
    location: String,
    jobId: String,
    body: &ReportWorkItemStatusRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/jobs/{}/workItems:reportStatus",
        projectId.as_str(),
        location.as_str(),
        jobId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/workItems:reportStatus
/// Reports the status of dataflow WorkItems leased by a worker.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_locations_jobs_work_items_report_status_execute()` or `dataflow_projects_locations_jobs_work_items_report_status`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_jobs_work_items_report_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_work_items_report_status_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReportWorkItemStatusResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ReportWorkItemStatusResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/workItems:reportStatus
/// Reports the status of dataflow WorkItems leased by a worker.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_locations_jobs_work_items_report_status_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_locations_jobs_work_items_report_status_task()`.
/// For the simplest API, use `dataflow_projects_locations_jobs_work_items_report_status()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_jobs_work_items_report_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_locations_jobs_work_items_report_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReportWorkItemStatusResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = dataflow_projects_locations_jobs_work_items_report_status_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_locations_jobs_work_items_report_status`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsJobsWorkItemsReportStatusArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: location
    pub location: String,
    /// Path parameter: jobId
    pub jobId: String,
    /// Request body.
    pub body: ReportWorkItemStatusRequest,
}

/// GET v1b3/projects/{projectId}/locations/{location}/jobs/{jobId}/workItems:reportStatus
/// Reports the status of dataflow WorkItems leased by a worker.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_jobs_work_items_report_status_builder()` + `dataflow_projects_locations_jobs_work_items_report_status_execute()`.
/// For task-level control, use `dataflow_projects_locations_jobs_work_items_report_status_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_jobs_work_items_report_status(
    client: &SimpleHttpClient,
    args: &DataflowProjectsLocationsJobsWorkItemsReportStatusArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ReportWorkItemStatusResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_jobs_work_items_report_status_builder(
        client,
        args.projectId.clone(),
        args.location.clone(),
        args.jobId.clone(),
        &args.body,
    )?;
    dataflow_projects_locations_jobs_work_items_report_status_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/snapshots/{snapshotId}
/// Deletes a snapshot.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_snapshots_delete_execute()` to send, or `dataflow_projects_locations_snapshots_delete` for simplest API.

pub fn dataflow_projects_locations_snapshots_delete_builder(
    client: &SimpleHttpClient,
    projectId: String,
    location: String,
    snapshotId: String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/snapshots/{}",
        projectId.as_str(),
        location.as_str(),
        snapshotId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/snapshots/{snapshotId}
/// Deletes a snapshot.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_locations_snapshots_delete_execute()` or `dataflow_projects_locations_snapshots_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_snapshots_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_snapshots_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeleteSnapshotResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeleteSnapshotResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/locations/{location}/snapshots/{snapshotId}
/// Deletes a snapshot.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_locations_snapshots_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_locations_snapshots_delete_task()`.
/// For the simplest API, use `dataflow_projects_locations_snapshots_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_snapshots_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_locations_snapshots_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeleteSnapshotResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataflow_projects_locations_snapshots_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_locations_snapshots_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsSnapshotsDeleteArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: location
    pub location: String,
    /// Path parameter: snapshotId
    pub snapshotId: String,
}

/// GET v1b3/projects/{projectId}/locations/{location}/snapshots/{snapshotId}
/// Deletes a snapshot.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_snapshots_delete_builder()` + `dataflow_projects_locations_snapshots_delete_execute()`.
/// For task-level control, use `dataflow_projects_locations_snapshots_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_snapshots_delete(
    client: &SimpleHttpClient,
    args: &DataflowProjectsLocationsSnapshotsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeleteSnapshotResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_snapshots_delete_builder(
        client,
        args.projectId.clone(),
        args.location.clone(),
        args.snapshotId.clone(),
    )?;
    dataflow_projects_locations_snapshots_delete_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/snapshots
/// Lists snapshots.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_snapshots_list_execute()` to send, or `dataflow_projects_locations_snapshots_list` for simplest API.

pub fn dataflow_projects_locations_snapshots_list_builder(
    client: &SimpleHttpClient,
    projectId: String,
    location: String,
    jobId: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/snapshots",
        projectId.as_str(),
        location.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = jobId {
        query_parts.push(format!("jobId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/snapshots
/// Lists snapshots.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_locations_snapshots_list_execute()` or `dataflow_projects_locations_snapshots_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_snapshots_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_snapshots_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSnapshotsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSnapshotsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/locations/{location}/snapshots
/// Lists snapshots.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_locations_snapshots_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_locations_snapshots_list_task()`.
/// For the simplest API, use `dataflow_projects_locations_snapshots_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_snapshots_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_locations_snapshots_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSnapshotsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataflow_projects_locations_snapshots_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_locations_snapshots_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsSnapshotsListArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: location
    pub location: String,
    /// Query parameter: jobId
    pub jobId: Option<String>,
}

/// GET v1b3/projects/{projectId}/locations/{location}/snapshots
/// Lists snapshots.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_snapshots_list_builder()` + `dataflow_projects_locations_snapshots_list_execute()`.
/// For task-level control, use `dataflow_projects_locations_snapshots_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_snapshots_list(
    client: &SimpleHttpClient,
    args: &DataflowProjectsLocationsSnapshotsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSnapshotsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_snapshots_list_builder(
        client,
        args.projectId.clone(),
        args.location.clone(),
        args.jobId.clone(),
    )?;
    dataflow_projects_locations_snapshots_list_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/templates
/// Creates a Cloud Dataflow job from a template. Do not enter confidential information when you supply string values using the API. To create a job, we recommend using projects.locations.templates.create with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.create is not recommended, because your job will always start in us-central1.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_templates_create_execute()` to send, or `dataflow_projects_locations_templates_create` for simplest API.

pub fn dataflow_projects_locations_templates_create_builder(
    client: &SimpleHttpClient,
    projectId: String,
    location: String,
    body: &CreateJobFromTemplateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/templates",
        projectId.as_str(),
        location.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/templates
/// Creates a Cloud Dataflow job from a template. Do not enter confidential information when you supply string values using the API. To create a job, we recommend using projects.locations.templates.create with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.create is not recommended, because your job will always start in us-central1.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_locations_templates_create_execute()` or `dataflow_projects_locations_templates_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_templates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_templates_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Job>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/locations/{location}/templates
/// Creates a Cloud Dataflow job from a template. Do not enter confidential information when you supply string values using the API. To create a job, we recommend using projects.locations.templates.create with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.create is not recommended, because your job will always start in us-central1.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_locations_templates_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_locations_templates_create_task()`.
/// For the simplest API, use `dataflow_projects_locations_templates_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_templates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_locations_templates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataflow_projects_locations_templates_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_locations_templates_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsTemplatesCreateArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: location
    pub location: String,
    /// Request body.
    pub body: CreateJobFromTemplateRequest,
}

/// GET v1b3/projects/{projectId}/locations/{location}/templates
/// Creates a Cloud Dataflow job from a template. Do not enter confidential information when you supply string values using the API. To create a job, we recommend using projects.locations.templates.create with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.create is not recommended, because your job will always start in us-central1.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_templates_create_builder()` + `dataflow_projects_locations_templates_create_execute()`.
/// For task-level control, use `dataflow_projects_locations_templates_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_templates_create(
    client: &SimpleHttpClient,
    args: &DataflowProjectsLocationsTemplatesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_templates_create_builder(
        client,
        args.projectId.clone(),
        args.location.clone(),
        &args.body,
    )?;
    dataflow_projects_locations_templates_create_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/templates:get
/// Get the template associated with a template. To get the template, we recommend using projects.locations.templates.get with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.get is not recommended, because only templates that are running in us-central1 are retrieved.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_templates_get_execute()` to send, or `dataflow_projects_locations_templates_get` for simplest API.

pub fn dataflow_projects_locations_templates_get_builder(
    client: &SimpleHttpClient,
    projectId: String,
    location: String,
    gcsPath: Option<String>,
    view: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/templates:get",
        projectId.as_str(),
        location.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = gcsPath {
        query_parts.push(format!("gcsPath={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/templates:get
/// Get the template associated with a template. To get the template, we recommend using projects.locations.templates.get with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.get is not recommended, because only templates that are running in us-central1 are retrieved.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_locations_templates_get_execute()` or `dataflow_projects_locations_templates_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_templates_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetTemplateResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetTemplateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/locations/{location}/templates:get
/// Get the template associated with a template. To get the template, we recommend using projects.locations.templates.get with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.get is not recommended, because only templates that are running in us-central1 are retrieved.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_locations_templates_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_locations_templates_get_task()`.
/// For the simplest API, use `dataflow_projects_locations_templates_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_locations_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetTemplateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataflow_projects_locations_templates_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_locations_templates_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsTemplatesGetArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: location
    pub location: String,
    /// Query parameter: gcsPath
    pub gcsPath: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1b3/projects/{projectId}/locations/{location}/templates:get
/// Get the template associated with a template. To get the template, we recommend using projects.locations.templates.get with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.get is not recommended, because only templates that are running in us-central1 are retrieved.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_templates_get_builder()` + `dataflow_projects_locations_templates_get_execute()`.
/// For task-level control, use `dataflow_projects_locations_templates_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_templates_get(
    client: &SimpleHttpClient,
    args: &DataflowProjectsLocationsTemplatesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetTemplateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_templates_get_builder(
        client,
        args.projectId.clone(),
        args.location.clone(),
        args.gcsPath.clone(),
        args.view.clone(),
    )?;
    dataflow_projects_locations_templates_get_execute(builder)
}

/// GET v1b3/projects/{projectId}/locations/{location}/templates:launch
/// Launches a template. To launch a template, we recommend using projects.locations.templates.launch with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.launch is not recommended, because jobs launched from the template will always start in us-central1.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_locations_templates_launch_execute()` to send, or `dataflow_projects_locations_templates_launch` for simplest API.

pub fn dataflow_projects_locations_templates_launch_builder(
    client: &SimpleHttpClient,
    projectId: String,
    location: String,
    dynamicTemplate_gcsPath: Option<String>,
    dynamicTemplate_stagingLocation: Option<String>,
    gcsPath: Option<String>,
    validateOnly: Option<bool>,
    body: &LaunchTemplateParameters,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/locations/{}/templates:launch",
        projectId.as_str(),
        location.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = dynamicTemplate_gcsPath {
        query_parts.push(format!("dynamicTemplate.gcsPath={}", val));
    }
    if let Some(val) = dynamicTemplate_stagingLocation {
        query_parts.push(format!("dynamicTemplate.stagingLocation={}", val));
    }
    if let Some(val) = gcsPath {
        query_parts.push(format!("gcsPath={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/locations/{location}/templates:launch
/// Launches a template. To launch a template, we recommend using projects.locations.templates.launch with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.launch is not recommended, because jobs launched from the template will always start in us-central1.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_locations_templates_launch_execute()` or `dataflow_projects_locations_templates_launch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_templates_launch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_templates_launch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LaunchTemplateResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LaunchTemplateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/locations/{location}/templates:launch
/// Launches a template. To launch a template, we recommend using projects.locations.templates.launch with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.launch is not recommended, because jobs launched from the template will always start in us-central1.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_locations_templates_launch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_locations_templates_launch_task()`.
/// For the simplest API, use `dataflow_projects_locations_templates_launch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_locations_templates_launch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_locations_templates_launch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LaunchTemplateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataflow_projects_locations_templates_launch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_locations_templates_launch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsLocationsTemplatesLaunchArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: location
    pub location: String,
    /// Query parameter: dynamicTemplate_gcsPath
    pub dynamicTemplate_gcsPath: Option<String>,
    /// Query parameter: dynamicTemplate_stagingLocation
    pub dynamicTemplate_stagingLocation: Option<String>,
    /// Query parameter: gcsPath
    pub gcsPath: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
    /// Request body.
    pub body: LaunchTemplateParameters,
}

/// GET v1b3/projects/{projectId}/locations/{location}/templates:launch
/// Launches a template. To launch a template, we recommend using projects.locations.templates.launch with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.launch is not recommended, because jobs launched from the template will always start in us-central1.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_locations_templates_launch_builder()` + `dataflow_projects_locations_templates_launch_execute()`.
/// For task-level control, use `dataflow_projects_locations_templates_launch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_locations_templates_launch(
    client: &SimpleHttpClient,
    args: &DataflowProjectsLocationsTemplatesLaunchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LaunchTemplateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_locations_templates_launch_builder(
        client,
        args.projectId.clone(),
        args.location.clone(),
        args.dynamicTemplate_gcsPath.clone(),
        args.dynamicTemplate_stagingLocation.clone(),
        args.gcsPath.clone(),
        args.validateOnly.clone(),
        &args.body,
    )?;
    dataflow_projects_locations_templates_launch_execute(builder)
}

/// GET v1b3/projects/{projectId}/snapshots/{snapshotId}
/// Gets information about a snapshot.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_snapshots_get_execute()` to send, or `dataflow_projects_snapshots_get` for simplest API.

pub fn dataflow_projects_snapshots_get_builder(
    client: &SimpleHttpClient,
    projectId: String,
    snapshotId: String,
    location: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/snapshots/{}",
        projectId.as_str(),
        snapshotId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = location {
        query_parts.push(format!("location={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/snapshots/{snapshotId}
/// Gets information about a snapshot.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_snapshots_get_execute()` or `dataflow_projects_snapshots_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_snapshots_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_snapshots_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Snapshot>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Snapshot = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/snapshots/{snapshotId}
/// Gets information about a snapshot.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_snapshots_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_snapshots_get_task()`.
/// For the simplest API, use `dataflow_projects_snapshots_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_snapshots_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_snapshots_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snapshot>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataflow_projects_snapshots_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_snapshots_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsSnapshotsGetArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Path parameter: snapshotId
    pub snapshotId: String,
    /// Query parameter: location
    pub location: Option<String>,
}

/// GET v1b3/projects/{projectId}/snapshots/{snapshotId}
/// Gets information about a snapshot.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_snapshots_get_builder()` + `dataflow_projects_snapshots_get_execute()`.
/// For task-level control, use `dataflow_projects_snapshots_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_snapshots_get(
    client: &SimpleHttpClient,
    args: &DataflowProjectsSnapshotsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Snapshot>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dataflow_projects_snapshots_get_builder(
        client,
        args.projectId.clone(),
        args.snapshotId.clone(),
        args.location.clone(),
    )?;
    dataflow_projects_snapshots_get_execute(builder)
}

/// GET v1b3/projects/{projectId}/templates
/// Creates a Cloud Dataflow job from a template. Do not enter confidential information when you supply string values using the API. To create a job, we recommend using projects.locations.templates.create with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.create is not recommended, because your job will always start in us-central1.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_templates_create_execute()` to send, or `dataflow_projects_templates_create` for simplest API.

pub fn dataflow_projects_templates_create_builder(
    client: &SimpleHttpClient,
    projectId: String,
    body: &CreateJobFromTemplateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/templates",
        projectId.as_str(),
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/templates
/// Creates a Cloud Dataflow job from a template. Do not enter confidential information when you supply string values using the API. To create a job, we recommend using projects.locations.templates.create with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.create is not recommended, because your job will always start in us-central1.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_templates_create_execute()` or `dataflow_projects_templates_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_templates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_templates_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Job>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/templates
/// Creates a Cloud Dataflow job from a template. Do not enter confidential information when you supply string values using the API. To create a job, we recommend using projects.locations.templates.create with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.create is not recommended, because your job will always start in us-central1.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_templates_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_templates_create_task()`.
/// For the simplest API, use `dataflow_projects_templates_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_templates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_templates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = dataflow_projects_templates_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_templates_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsTemplatesCreateArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Request body.
    pub body: CreateJobFromTemplateRequest,
}

/// GET v1b3/projects/{projectId}/templates
/// Creates a Cloud Dataflow job from a template. Do not enter confidential information when you supply string values using the API. To create a job, we recommend using projects.locations.templates.create with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.create is not recommended, because your job will always start in us-central1.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_templates_create_builder()` + `dataflow_projects_templates_create_execute()`.
/// For task-level control, use `dataflow_projects_templates_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_templates_create(
    client: &SimpleHttpClient,
    args: &DataflowProjectsTemplatesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dataflow_projects_templates_create_builder(client, args.projectId.clone(), &args.body)?;
    dataflow_projects_templates_create_execute(builder)
}

/// GET v1b3/projects/{projectId}/templates:get
/// Get the template associated with a template. To get the template, we recommend using projects.locations.templates.get with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.get is not recommended, because only templates that are running in us-central1 are retrieved.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_templates_get_execute()` to send, or `dataflow_projects_templates_get` for simplest API.

pub fn dataflow_projects_templates_get_builder(
    client: &SimpleHttpClient,
    projectId: String,
    gcsPath: Option<String>,
    location: Option<String>,
    view: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/templates:get",
        projectId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = gcsPath {
        query_parts.push(format!("gcsPath={}", val));
    }
    if let Some(val) = location {
        query_parts.push(format!("location={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1b3/projects/{projectId}/templates:get
/// Get the template associated with a template. To get the template, we recommend using projects.locations.templates.get with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.get is not recommended, because only templates that are running in us-central1 are retrieved.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_templates_get_execute()` or `dataflow_projects_templates_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_templates_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GetTemplateResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GetTemplateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/templates:get
/// Get the template associated with a template. To get the template, we recommend using projects.locations.templates.get with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.get is not recommended, because only templates that are running in us-central1 are retrieved.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_templates_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_templates_get_task()`.
/// For the simplest API, use `dataflow_projects_templates_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetTemplateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataflow_projects_templates_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_templates_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsTemplatesGetArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Query parameter: gcsPath
    pub gcsPath: Option<String>,
    /// Query parameter: location
    pub location: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1b3/projects/{projectId}/templates:get
/// Get the template associated with a template. To get the template, we recommend using projects.locations.templates.get with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.get is not recommended, because only templates that are running in us-central1 are retrieved.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_templates_get_builder()` + `dataflow_projects_templates_get_execute()`.
/// For task-level control, use `dataflow_projects_templates_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_templates_get(
    client: &SimpleHttpClient,
    args: &DataflowProjectsTemplatesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GetTemplateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_templates_get_builder(
        client,
        args.projectId.clone(),
        args.gcsPath.clone(),
        args.location.clone(),
        args.view.clone(),
    )?;
    dataflow_projects_templates_get_execute(builder)
}

/// GET v1b3/projects/{projectId}/templates:launch
/// Launches a template. To launch a template, we recommend using projects.locations.templates.launch with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.launch is not recommended, because jobs launched from the template will always start in us-central1.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dataflow_projects_templates_launch_execute()` to send, or `dataflow_projects_templates_launch` for simplest API.

pub fn dataflow_projects_templates_launch_builder(
    client: &SimpleHttpClient,
    projectId: String,
    dynamicTemplate_gcsPath: Option<String>,
    dynamicTemplate_stagingLocation: Option<String>,
    gcsPath: Option<String>,
    location: Option<String>,
    validateOnly: Option<bool>,
    body: &LaunchTemplateParameters,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://dataflow.googleapis.com/v1b3/projects/{}/templates:launch",
        projectId.as_str(),
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = dynamicTemplate_gcsPath {
        query_parts.push(format!("dynamicTemplate.gcsPath={}", val));
    }
    if let Some(val) = dynamicTemplate_stagingLocation {
        query_parts.push(format!("dynamicTemplate.stagingLocation={}", val));
    }
    if let Some(val) = gcsPath {
        query_parts.push(format!("gcsPath={}", val));
    }
    if let Some(val) = location {
        query_parts.push(format!("location={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1b3/projects/{projectId}/templates:launch
/// Launches a template. To launch a template, we recommend using projects.locations.templates.launch with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.launch is not recommended, because jobs launched from the template will always start in us-central1.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `dataflow_projects_templates_launch_execute()` or `dataflow_projects_templates_launch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_templates_launch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_templates_launch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LaunchTemplateResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LaunchTemplateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1b3/projects/{projectId}/templates:launch
/// Launches a template. To launch a template, we recommend using projects.locations.templates.launch with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.launch is not recommended, because jobs launched from the template will always start in us-central1.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `dataflow_projects_templates_launch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `dataflow_projects_templates_launch_task()`.
/// For the simplest API, use `dataflow_projects_templates_launch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `dataflow_projects_templates_launch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn dataflow_projects_templates_launch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LaunchTemplateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = dataflow_projects_templates_launch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dataflow_projects_templates_launch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataflowProjectsTemplatesLaunchArgs {
    /// Path parameter: projectId
    pub projectId: String,
    /// Query parameter: dynamicTemplate_gcsPath
    pub dynamicTemplate_gcsPath: Option<String>,
    /// Query parameter: dynamicTemplate_stagingLocation
    pub dynamicTemplate_stagingLocation: Option<String>,
    /// Query parameter: gcsPath
    pub gcsPath: Option<String>,
    /// Query parameter: location
    pub location: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
    /// Request body.
    pub body: LaunchTemplateParameters,
}

/// GET v1b3/projects/{projectId}/templates:launch
/// Launches a template. To launch a template, we recommend using projects.locations.templates.launch with a [regional endpoint] (<https://cloud.google.`com/dataflow/docs/concepts/regional-endpoints`>). Using projects.templates.launch is not recommended, because jobs launched from the template will always start in us-central1.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dataflow_projects_templates_launch_builder()` + `dataflow_projects_templates_launch_execute()`.
/// For task-level control, use `dataflow_projects_templates_launch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dataflow_projects_templates_launch(
    client: &SimpleHttpClient,
    args: &DataflowProjectsTemplatesLaunchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LaunchTemplateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dataflow_projects_templates_launch_builder(
        client,
        args.projectId.clone(),
        args.dynamicTemplate_gcsPath.clone(),
        args.dynamicTemplate_stagingLocation.clone(),
        args.gcsPath.clone(),
        args.location.clone(),
        args.validateOnly.clone(),
        &args.body,
    )?;
    dataflow_projects_templates_launch_execute(builder)
}
