//! Auto-generated API clients for gcp/gmailpostmastertools.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST v2/domainStats:batchQuery
/// Executes a batch of QueryDomainStats requests for multiple domains. Returns PERMISSION_DENIED if you don't have permission to access DomainStats for any of the requested domains.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gmailpostmastertools_domain_stats_batch_query_execute()` to send, or `gmailpostmastertools_domain_stats_batch_query` for simplest API.

pub fn gmailpostmastertools_domain_stats_batch_query_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://gmailpostmastertools.googleapis.com/v2/domainStats:batchQuery",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/domainStats:batchQuery
/// Executes a batch of QueryDomainStats requests for multiple domains. Returns PERMISSION_DENIED if you don't have permission to access DomainStats for any of the requested domains.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gmailpostmastertools_domain_stats_batch_query_execute()` or `gmailpostmastertools_domain_stats_batch_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gmailpostmastertools_domain_stats_batch_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gmailpostmastertools_domain_stats_batch_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchQueryDomainStatsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchQueryDomainStatsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/domainStats:batchQuery
/// Executes a batch of QueryDomainStats requests for multiple domains. Returns PERMISSION_DENIED if you don't have permission to access DomainStats for any of the requested domains.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gmailpostmastertools_domain_stats_batch_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gmailpostmastertools_domain_stats_batch_query_task()`.
/// For the simplest API, use `gmailpostmastertools_domain_stats_batch_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gmailpostmastertools_domain_stats_batch_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gmailpostmastertools_domain_stats_batch_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchQueryDomainStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = gmailpostmastertools_domain_stats_batch_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v2/domainStats:batchQuery
/// Executes a batch of QueryDomainStats requests for multiple domains. Returns PERMISSION_DENIED if you don't have permission to access DomainStats for any of the requested domains.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gmailpostmastertools_domain_stats_batch_query_builder()` + `gmailpostmastertools_domain_stats_batch_query_execute()`.
/// For task-level control, use `gmailpostmastertools_domain_stats_batch_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gmailpostmastertools_domain_stats_batch_query(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchQueryDomainStatsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = gmailpostmastertools_domain_stats_batch_query_builder(client)?;
    gmailpostmastertools_domain_stats_batch_query_execute(builder)
}

/// GET v2/domains/{domainsId}
/// Retrieves detailed information about a domain registered by you. Returns NOT_FOUND if the domain is not registered by you. Domain represents the metadata of a domain that has been registered within the system and linked to a user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gmailpostmastertools_domains_get_execute()` to send, or `gmailpostmastertools_domains_get` for simplest API.

pub fn gmailpostmastertools_domains_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gmailpostmastertools.googleapis.com/v2/domains/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/domains/{domainsId}
/// Retrieves detailed information about a domain registered by you. Returns NOT_FOUND if the domain is not registered by you. Domain represents the metadata of a domain that has been registered within the system and linked to a user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gmailpostmastertools_domains_get_execute()` or `gmailpostmastertools_domains_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gmailpostmastertools_domains_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gmailpostmastertools_domains_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Domain>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Domain = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/domains/{domainsId}
/// Retrieves detailed information about a domain registered by you. Returns NOT_FOUND if the domain is not registered by you. Domain represents the metadata of a domain that has been registered within the system and linked to a user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gmailpostmastertools_domains_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gmailpostmastertools_domains_get_task()`.
/// For the simplest API, use `gmailpostmastertools_domains_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gmailpostmastertools_domains_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gmailpostmastertools_domains_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Domain>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = gmailpostmastertools_domains_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gmailpostmastertools_domains_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GmailpostmastertoolsDomainsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/domains/{domainsId}
/// Retrieves detailed information about a domain registered by you. Returns NOT_FOUND if the domain is not registered by you. Domain represents the metadata of a domain that has been registered within the system and linked to a user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gmailpostmastertools_domains_get_builder()` + `gmailpostmastertools_domains_get_execute()`.
/// For task-level control, use `gmailpostmastertools_domains_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gmailpostmastertools_domains_get(
    client: &SimpleHttpClient,
    args: &GmailpostmastertoolsDomainsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Domain>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = gmailpostmastertools_domains_get_builder(client, &args.name)?;
    gmailpostmastertools_domains_get_execute(builder)
}

/// GET v2/domains/{domainsId}/complianceStatus
/// Retrieves the compliance status for a given domain. Returns PERMISSION_DENIED if you don't have permission to access compliance status for the domain.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gmailpostmastertools_domains_get_compliance_status_execute()` to send, or `gmailpostmastertools_domains_get_compliance_status` for simplest API.

pub fn gmailpostmastertools_domains_get_compliance_status_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gmailpostmastertools.googleapis.com/v2/domains/{}/complianceStatus",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/domains/{domainsId}/complianceStatus
/// Retrieves the compliance status for a given domain. Returns PERMISSION_DENIED if you don't have permission to access compliance status for the domain.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gmailpostmastertools_domains_get_compliance_status_execute()` or `gmailpostmastertools_domains_get_compliance_status`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gmailpostmastertools_domains_get_compliance_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gmailpostmastertools_domains_get_compliance_status_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DomainComplianceStatus>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DomainComplianceStatus = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/domains/{domainsId}/complianceStatus
/// Retrieves the compliance status for a given domain. Returns PERMISSION_DENIED if you don't have permission to access compliance status for the domain.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gmailpostmastertools_domains_get_compliance_status_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gmailpostmastertools_domains_get_compliance_status_task()`.
/// For the simplest API, use `gmailpostmastertools_domains_get_compliance_status()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gmailpostmastertools_domains_get_compliance_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gmailpostmastertools_domains_get_compliance_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DomainComplianceStatus>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = gmailpostmastertools_domains_get_compliance_status_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gmailpostmastertools_domains_get_compliance_status`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GmailpostmastertoolsDomainsGetComplianceStatusArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/domains/{domainsId}/complianceStatus
/// Retrieves the compliance status for a given domain. Returns PERMISSION_DENIED if you don't have permission to access compliance status for the domain.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gmailpostmastertools_domains_get_compliance_status_builder()` + `gmailpostmastertools_domains_get_compliance_status_execute()`.
/// For task-level control, use `gmailpostmastertools_domains_get_compliance_status_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gmailpostmastertools_domains_get_compliance_status(
    client: &SimpleHttpClient,
    args: &GmailpostmastertoolsDomainsGetComplianceStatusArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DomainComplianceStatus>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = gmailpostmastertools_domains_get_compliance_status_builder(client, &args.name)?;
    gmailpostmastertools_domains_get_compliance_status_execute(builder)
}

/// GET v2/domains
/// Retrieves a list of all domains registered by you, along with their corresponding metadata. The order of domains in the response is unspecified and non-deterministic. Newly registered domains will not necessarily be added to the end of this list.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gmailpostmastertools_domains_list_execute()` to send, or `gmailpostmastertools_domains_list` for simplest API.

pub fn gmailpostmastertools_domains_list_builder<R>(
    client: &SimpleHttpClient<R>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://gmailpostmastertools.googleapis.com/v2/domains",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/domains
/// Retrieves a list of all domains registered by you, along with their corresponding metadata. The order of domains in the response is unspecified and non-deterministic. Newly registered domains will not necessarily be added to the end of this list.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gmailpostmastertools_domains_list_execute()` or `gmailpostmastertools_domains_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gmailpostmastertools_domains_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gmailpostmastertools_domains_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDomainsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDomainsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/domains
/// Retrieves a list of all domains registered by you, along with their corresponding metadata. The order of domains in the response is unspecified and non-deterministic. Newly registered domains will not necessarily be added to the end of this list.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gmailpostmastertools_domains_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gmailpostmastertools_domains_list_task()`.
/// For the simplest API, use `gmailpostmastertools_domains_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gmailpostmastertools_domains_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gmailpostmastertools_domains_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDomainsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = gmailpostmastertools_domains_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gmailpostmastertools_domains_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GmailpostmastertoolsDomainsListArgs {
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v2/domains
/// Retrieves a list of all domains registered by you, along with their corresponding metadata. The order of domains in the response is unspecified and non-deterministic. Newly registered domains will not necessarily be added to the end of this list.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gmailpostmastertools_domains_list_builder()` + `gmailpostmastertools_domains_list_execute()`.
/// For task-level control, use `gmailpostmastertools_domains_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gmailpostmastertools_domains_list(
    client: &SimpleHttpClient,
    args: &GmailpostmastertoolsDomainsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDomainsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        gmailpostmastertools_domains_list_builder(client, &args.pageSize, &args.pageToken)?;
    gmailpostmastertools_domains_list_execute(builder)
}

/// POST v2/domains/{domainsId}/domainStats:query
/// Retrieves a list of domain statistics for a given domain and time period. Returns statistics only for dates where data is available. Returns PERMISSION_DENIED if you don't have permission to access DomainStats for the domain.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `gmailpostmastertools_domains_domain_stats_query_execute()` to send, or `gmailpostmastertools_domains_domain_stats_query` for simplest API.

pub fn gmailpostmastertools_domains_domain_stats_query_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://gmailpostmastertools.googleapis.com/v2/domains/{}/domainStats:query",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/domains/{domainsId}/domainStats:query
/// Retrieves a list of domain statistics for a given domain and time period. Returns statistics only for dates where data is available. Returns PERMISSION_DENIED if you don't have permission to access DomainStats for the domain.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `gmailpostmastertools_domains_domain_stats_query_execute()` or `gmailpostmastertools_domains_domain_stats_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gmailpostmastertools_domains_domain_stats_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gmailpostmastertools_domains_domain_stats_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<QueryDomainStatsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: QueryDomainStatsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/domains/{domainsId}/domainStats:query
/// Retrieves a list of domain statistics for a given domain and time period. Returns statistics only for dates where data is available. Returns PERMISSION_DENIED if you don't have permission to access DomainStats for the domain.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `gmailpostmastertools_domains_domain_stats_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `gmailpostmastertools_domains_domain_stats_query_task()`.
/// For the simplest API, use `gmailpostmastertools_domains_domain_stats_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `gmailpostmastertools_domains_domain_stats_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn gmailpostmastertools_domains_domain_stats_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<QueryDomainStatsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = gmailpostmastertools_domains_domain_stats_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`gmailpostmastertools_domains_domain_stats_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GmailpostmastertoolsDomainsDomainStatsQueryArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/domains/{domainsId}/domainStats:query
/// Retrieves a list of domain statistics for a given domain and time period. Returns statistics only for dates where data is available. Returns PERMISSION_DENIED if you don't have permission to access DomainStats for the domain.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `gmailpostmastertools_domains_domain_stats_query_builder()` + `gmailpostmastertools_domains_domain_stats_query_execute()`.
/// For task-level control, use `gmailpostmastertools_domains_domain_stats_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn gmailpostmastertools_domains_domain_stats_query(
    client: &SimpleHttpClient,
    args: &GmailpostmastertoolsDomainsDomainStatsQueryArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<QueryDomainStatsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = gmailpostmastertools_domains_domain_stats_query_builder(client, &args.parent)?;
    gmailpostmastertools_domains_domain_stats_query_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for BatchQueryDomainStatsResponse
// =============================================================================

/// ResourceIdentifier implementation for BatchQueryDomainStatsResponse with GmailpostmastertoolsDomainStatsBatchQueryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GmailpostmastertoolsDomainStatsBatchQueryArgs>
    for BatchQueryDomainStatsResponse
{
    fn generate_resource_id(
        &self,
        input: &GmailpostmastertoolsDomainStatsBatchQueryArgs,
    ) -> String {
        "gcp::gmailpostmastertools::BatchQueryDomainStatsResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gmailpostmastertools::BatchQueryDomainStatsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Domain
// =============================================================================

/// ResourceIdentifier implementation for Domain with GmailpostmastertoolsDomainsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GmailpostmastertoolsDomainsGetArgs> for Domain {
    fn generate_resource_id(&self, input: &GmailpostmastertoolsDomainsGetArgs) -> String {
        format!("gcp::gmailpostmastertools::Domain/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gmailpostmastertools::Domain"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DomainComplianceStatus
// =============================================================================

/// ResourceIdentifier implementation for DomainComplianceStatus with GmailpostmastertoolsDomainsGetComplianceStatusArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GmailpostmastertoolsDomainsGetComplianceStatusArgs>
    for DomainComplianceStatus
{
    fn generate_resource_id(
        &self,
        input: &GmailpostmastertoolsDomainsGetComplianceStatusArgs,
    ) -> String {
        format!(
            "gcp::gmailpostmastertools::DomainComplianceStatus/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gmailpostmastertools::DomainComplianceStatus"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListDomainsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListDomainsResponse with GmailpostmastertoolsDomainsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GmailpostmastertoolsDomainsListArgs> for ListDomainsResponse {
    fn generate_resource_id(&self, input: &GmailpostmastertoolsDomainsListArgs) -> String {
        "gcp::gmailpostmastertools::ListDomainsResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gmailpostmastertools::ListDomainsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for QueryDomainStatsResponse
// =============================================================================

/// ResourceIdentifier implementation for QueryDomainStatsResponse with GmailpostmastertoolsDomainsDomainStatsQueryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<GmailpostmastertoolsDomainsDomainStatsQueryArgs>
    for QueryDomainStatsResponse
{
    fn generate_resource_id(
        &self,
        input: &GmailpostmastertoolsDomainsDomainStatsQueryArgs,
    ) -> String {
        format!(
            "gcp::gmailpostmastertools::QueryDomainStatsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::gmailpostmastertools::QueryDomainStatsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
