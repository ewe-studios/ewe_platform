//! Auto-generated API clients for gcp/webfonts.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/webfonts
/// Retrieves the list of fonts currently served by the Google Fonts Developer API.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `webfonts_webfonts_list_execute()` to send, or `webfonts_webfonts_list` for simplest API.

pub fn webfonts_webfonts_list_builder(
    client: &SimpleHttpClient,
    capability: Option<&str>,
    category: Option<&str>,
    family: Option<&str>,
    sort: Option<&str>,
    subset: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/webfonts",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "capability",
            capability
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "category",
            category
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "family",
            family
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "sort",
            sort.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "subset",
            subset
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/webfonts
/// Retrieves the list of fonts currently served by the Google Fonts Developer API.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `webfonts_webfonts_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from webfonts_webfonts_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webfonts_webfonts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebfontList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WebfontList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/webfonts
/// Retrieves the list of fonts currently served by the Google Fonts Developer API.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `webfonts_webfonts_list_builder()` + `webfonts_webfonts_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webfonts_webfonts_list(
    client: &SimpleHttpClient,
    capability: Option<&str>,
    category: Option<&str>,
    family: Option<&str>,
    sort: Option<&str>,
    subset: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WebfontList>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        webfonts_webfonts_list_builder(client, capability, category, family, sort, subset)?;
    webfonts_webfonts_list_execute(builder)
}
