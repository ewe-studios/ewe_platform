//! Auto-generated API clients for gcp/recommendationengine.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs
/// Lists all the catalog configurations associated with the project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommendationengine_projects_locations_catalogs_list_execute()` to send, or `recommendationengine_projects_locations_catalogs_list` for simplest API.

pub fn recommendationengine_projects_locations_catalogs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommendationengine.googleapis.com/v1beta1/projects/{}/locations/{locationsId}/catalogs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs
/// Lists all the catalog configurations associated with the project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommendationengine_projects_locations_catalogs_list_execute()` or `recommendationengine_projects_locations_catalogs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRecommendationengineV1beta1ListCatalogsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommendationengineV1beta1ListCatalogsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs
/// Lists all the catalog configurations associated with the project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommendationengine_projects_locations_catalogs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_list_task()`.
/// For the simplest API, use `recommendationengine_projects_locations_catalogs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommendationengine_projects_locations_catalogs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRecommendationengineV1beta1ListCatalogsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommendationengine_projects_locations_catalogs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommendationengine_projects_locations_catalogs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommendationengineProjectsLocationsCatalogsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs
/// Lists all the catalog configurations associated with the project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommendationengine_projects_locations_catalogs_list_builder()` + `recommendationengine_projects_locations_catalogs_list_execute()`.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_list(
    client: &SimpleHttpClient,
    args: &RecommendationengineProjectsLocationsCatalogsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRecommendationengineV1beta1ListCatalogsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommendationengine_projects_locations_catalogs_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    recommendationengine_projects_locations_catalogs_list_execute(builder)
}

/// PATCH v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}
/// Updates the catalog configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommendationengine_projects_locations_catalogs_patch_execute()` to send, or `recommendationengine_projects_locations_catalogs_patch` for simplest API.

pub fn recommendationengine_projects_locations_catalogs_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommendationengine.googleapis.com/v1beta1/projects/{}/locations/{locationsId}/catalogs/{catalogsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}
/// Updates the catalog configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommendationengine_projects_locations_catalogs_patch_execute()` or `recommendationengine_projects_locations_catalogs_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommendationengineV1beta1Catalog>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommendationengineV1beta1Catalog =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}
/// Updates the catalog configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommendationengine_projects_locations_catalogs_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_patch_task()`.
/// For the simplest API, use `recommendationengine_projects_locations_catalogs_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommendationengine_projects_locations_catalogs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommendationengineV1beta1Catalog>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommendationengine_projects_locations_catalogs_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommendationengine_projects_locations_catalogs_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommendationengineProjectsLocationsCatalogsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}
/// Updates the catalog configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommendationengine_projects_locations_catalogs_patch_builder()` + `recommendationengine_projects_locations_catalogs_patch_execute()`.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_patch(
    client: &SimpleHttpClient,
    args: &RecommendationengineProjectsLocationsCatalogsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommendationengineV1beta1Catalog>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommendationengine_projects_locations_catalogs_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    recommendationengine_projects_locations_catalogs_patch_execute(builder)
}

/// POST v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/catalogItems
/// Creates a catalog item.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommendationengine_projects_locations_catalogs_catalog_items_create_execute()` to send, or `recommendationengine_projects_locations_catalogs_catalog_items_create` for simplest API.

pub fn recommendationengine_projects_locations_catalogs_catalog_items_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommendationengine.googleapis.com/v1beta1/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/catalogItems",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/catalogItems
/// Creates a catalog item.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommendationengine_projects_locations_catalogs_catalog_items_create_execute()` or `recommendationengine_projects_locations_catalogs_catalog_items_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_catalog_items_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_catalog_items_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRecommendationengineV1beta1CatalogItem>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommendationengineV1beta1CatalogItem =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/catalogItems
/// Creates a catalog item.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommendationengine_projects_locations_catalogs_catalog_items_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_catalog_items_create_task()`.
/// For the simplest API, use `recommendationengine_projects_locations_catalogs_catalog_items_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_catalog_items_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommendationengine_projects_locations_catalogs_catalog_items_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommendationengineV1beta1CatalogItem>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommendationengine_projects_locations_catalogs_catalog_items_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommendationengine_projects_locations_catalogs_catalog_items_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommendationengineProjectsLocationsCatalogsCatalogItemsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/catalogItems
/// Creates a catalog item.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommendationengine_projects_locations_catalogs_catalog_items_create_builder()` + `recommendationengine_projects_locations_catalogs_catalog_items_create_execute()`.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_catalog_items_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_catalog_items_create(
    client: &SimpleHttpClient,
    args: &RecommendationengineProjectsLocationsCatalogsCatalogItemsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommendationengineV1beta1CatalogItem>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommendationengine_projects_locations_catalogs_catalog_items_create_builder(
        client,
        &args.parent,
    )?;
    recommendationengine_projects_locations_catalogs_catalog_items_create_execute(builder)
}

/// DELETE v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/catalogItems/{catalogItemsId}
/// Deletes a catalog item.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommendationengine_projects_locations_catalogs_catalog_items_delete_execute()` to send, or `recommendationengine_projects_locations_catalogs_catalog_items_delete` for simplest API.

pub fn recommendationengine_projects_locations_catalogs_catalog_items_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommendationengine.googleapis.com/v1beta1/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/catalogItems/{catalogItemsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/catalogItems/{catalogItemsId}
/// Deletes a catalog item.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommendationengine_projects_locations_catalogs_catalog_items_delete_execute()` or `recommendationengine_projects_locations_catalogs_catalog_items_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_catalog_items_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_catalog_items_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/catalogItems/{catalogItemsId}
/// Deletes a catalog item.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommendationengine_projects_locations_catalogs_catalog_items_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_catalog_items_delete_task()`.
/// For the simplest API, use `recommendationengine_projects_locations_catalogs_catalog_items_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_catalog_items_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommendationengine_projects_locations_catalogs_catalog_items_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = recommendationengine_projects_locations_catalogs_catalog_items_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommendationengine_projects_locations_catalogs_catalog_items_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommendationengineProjectsLocationsCatalogsCatalogItemsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/catalogItems/{catalogItemsId}
/// Deletes a catalog item.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommendationengine_projects_locations_catalogs_catalog_items_delete_builder()` + `recommendationengine_projects_locations_catalogs_catalog_items_delete_execute()`.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_catalog_items_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_catalog_items_delete(
    client: &SimpleHttpClient,
    args: &RecommendationengineProjectsLocationsCatalogsCatalogItemsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = recommendationengine_projects_locations_catalogs_catalog_items_delete_builder(
        client, &args.name,
    )?;
    recommendationengine_projects_locations_catalogs_catalog_items_delete_execute(builder)
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/catalogItems/{catalogItemsId}
/// Gets a specific catalog item.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommendationengine_projects_locations_catalogs_catalog_items_get_execute()` to send, or `recommendationengine_projects_locations_catalogs_catalog_items_get` for simplest API.

pub fn recommendationengine_projects_locations_catalogs_catalog_items_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommendationengine.googleapis.com/v1beta1/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/catalogItems/{catalogItemsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/catalogItems/{catalogItemsId}
/// Gets a specific catalog item.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommendationengine_projects_locations_catalogs_catalog_items_get_execute()` or `recommendationengine_projects_locations_catalogs_catalog_items_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_catalog_items_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_catalog_items_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRecommendationengineV1beta1CatalogItem>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommendationengineV1beta1CatalogItem =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/catalogItems/{catalogItemsId}
/// Gets a specific catalog item.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommendationengine_projects_locations_catalogs_catalog_items_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_catalog_items_get_task()`.
/// For the simplest API, use `recommendationengine_projects_locations_catalogs_catalog_items_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_catalog_items_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommendationengine_projects_locations_catalogs_catalog_items_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommendationengineV1beta1CatalogItem>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommendationengine_projects_locations_catalogs_catalog_items_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommendationengine_projects_locations_catalogs_catalog_items_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommendationengineProjectsLocationsCatalogsCatalogItemsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/catalogItems/{catalogItemsId}
/// Gets a specific catalog item.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommendationengine_projects_locations_catalogs_catalog_items_get_builder()` + `recommendationengine_projects_locations_catalogs_catalog_items_get_execute()`.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_catalog_items_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_catalog_items_get(
    client: &SimpleHttpClient,
    args: &RecommendationengineProjectsLocationsCatalogsCatalogItemsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommendationengineV1beta1CatalogItem>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommendationengine_projects_locations_catalogs_catalog_items_get_builder(
        client, &args.name,
    )?;
    recommendationengine_projects_locations_catalogs_catalog_items_get_execute(builder)
}

/// POST v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/catalogItems:import
/// Bulk import of multiple catalog items. Request processing may be synchronous. No partial updating supported. Non-existing items will be created. Operation.response is of type ImportResponse. Note that it is possible for a subset of the items to be successfully updated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommendationengine_projects_locations_catalogs_catalog_items_import_execute()` to send, or `recommendationengine_projects_locations_catalogs_catalog_items_import` for simplest API.

pub fn recommendationengine_projects_locations_catalogs_catalog_items_import_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommendationengine.googleapis.com/v1beta1/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/catalogItems:import",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/catalogItems:import
/// Bulk import of multiple catalog items. Request processing may be synchronous. No partial updating supported. Non-existing items will be created. Operation.response is of type ImportResponse. Note that it is possible for a subset of the items to be successfully updated.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommendationengine_projects_locations_catalogs_catalog_items_import_execute()` or `recommendationengine_projects_locations_catalogs_catalog_items_import`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_catalog_items_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_catalog_items_import_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/catalogItems:import
/// Bulk import of multiple catalog items. Request processing may be synchronous. No partial updating supported. Non-existing items will be created. Operation.response is of type ImportResponse. Note that it is possible for a subset of the items to be successfully updated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommendationengine_projects_locations_catalogs_catalog_items_import_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_catalog_items_import_task()`.
/// For the simplest API, use `recommendationengine_projects_locations_catalogs_catalog_items_import()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_catalog_items_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommendationengine_projects_locations_catalogs_catalog_items_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommendationengine_projects_locations_catalogs_catalog_items_import_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommendationengine_projects_locations_catalogs_catalog_items_import`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommendationengineProjectsLocationsCatalogsCatalogItemsImportArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/catalogItems:import
/// Bulk import of multiple catalog items. Request processing may be synchronous. No partial updating supported. Non-existing items will be created. Operation.response is of type ImportResponse. Note that it is possible for a subset of the items to be successfully updated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommendationengine_projects_locations_catalogs_catalog_items_import_builder()` + `recommendationengine_projects_locations_catalogs_catalog_items_import_execute()`.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_catalog_items_import_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_catalog_items_import(
    client: &SimpleHttpClient,
    args: &RecommendationengineProjectsLocationsCatalogsCatalogItemsImportArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommendationengine_projects_locations_catalogs_catalog_items_import_builder(
        client,
        &args.parent,
    )?;
    recommendationengine_projects_locations_catalogs_catalog_items_import_execute(builder)
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/catalogItems
/// Gets a list of catalog items.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommendationengine_projects_locations_catalogs_catalog_items_list_execute()` to send, or `recommendationengine_projects_locations_catalogs_catalog_items_list` for simplest API.

pub fn recommendationengine_projects_locations_catalogs_catalog_items_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommendationengine.googleapis.com/v1beta1/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/catalogItems",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/catalogItems
/// Gets a list of catalog items.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommendationengine_projects_locations_catalogs_catalog_items_list_execute()` or `recommendationengine_projects_locations_catalogs_catalog_items_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_catalog_items_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_catalog_items_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRecommendationengineV1beta1ListCatalogItemsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommendationengineV1beta1ListCatalogItemsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/catalogItems
/// Gets a list of catalog items.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommendationengine_projects_locations_catalogs_catalog_items_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_catalog_items_list_task()`.
/// For the simplest API, use `recommendationengine_projects_locations_catalogs_catalog_items_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_catalog_items_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommendationengine_projects_locations_catalogs_catalog_items_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRecommendationengineV1beta1ListCatalogItemsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommendationengine_projects_locations_catalogs_catalog_items_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommendationengine_projects_locations_catalogs_catalog_items_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommendationengineProjectsLocationsCatalogsCatalogItemsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/catalogItems
/// Gets a list of catalog items.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommendationengine_projects_locations_catalogs_catalog_items_list_builder()` + `recommendationengine_projects_locations_catalogs_catalog_items_list_execute()`.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_catalog_items_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_catalog_items_list(
    client: &SimpleHttpClient,
    args: &RecommendationengineProjectsLocationsCatalogsCatalogItemsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRecommendationengineV1beta1ListCatalogItemsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommendationengine_projects_locations_catalogs_catalog_items_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    recommendationengine_projects_locations_catalogs_catalog_items_list_execute(builder)
}

/// PATCH v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/catalogItems/{catalogItemsId}
/// Updates a catalog item. Partial updating is supported. Non-existing items will be created.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommendationengine_projects_locations_catalogs_catalog_items_patch_execute()` to send, or `recommendationengine_projects_locations_catalogs_catalog_items_patch` for simplest API.

pub fn recommendationengine_projects_locations_catalogs_catalog_items_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommendationengine.googleapis.com/v1beta1/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/catalogItems/{catalogItemsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/catalogItems/{catalogItemsId}
/// Updates a catalog item. Partial updating is supported. Non-existing items will be created.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommendationengine_projects_locations_catalogs_catalog_items_patch_execute()` or `recommendationengine_projects_locations_catalogs_catalog_items_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_catalog_items_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_catalog_items_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRecommendationengineV1beta1CatalogItem>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommendationengineV1beta1CatalogItem =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/catalogItems/{catalogItemsId}
/// Updates a catalog item. Partial updating is supported. Non-existing items will be created.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommendationengine_projects_locations_catalogs_catalog_items_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_catalog_items_patch_task()`.
/// For the simplest API, use `recommendationengine_projects_locations_catalogs_catalog_items_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_catalog_items_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommendationengine_projects_locations_catalogs_catalog_items_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommendationengineV1beta1CatalogItem>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommendationengine_projects_locations_catalogs_catalog_items_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommendationengine_projects_locations_catalogs_catalog_items_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommendationengineProjectsLocationsCatalogsCatalogItemsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/catalogItems/{catalogItemsId}
/// Updates a catalog item. Partial updating is supported. Non-existing items will be created.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommendationengine_projects_locations_catalogs_catalog_items_patch_builder()` + `recommendationengine_projects_locations_catalogs_catalog_items_patch_execute()`.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_catalog_items_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_catalog_items_patch(
    client: &SimpleHttpClient,
    args: &RecommendationengineProjectsLocationsCatalogsCatalogItemsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommendationengineV1beta1CatalogItem>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommendationengine_projects_locations_catalogs_catalog_items_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    recommendationengine_projects_locations_catalogs_catalog_items_patch_execute(builder)
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommendationengine_projects_locations_catalogs_event_stores_operations_get_execute()` to send, or `recommendationengine_projects_locations_catalogs_event_stores_operations_get` for simplest API.

pub fn recommendationengine_projects_locations_catalogs_event_stores_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommendationengine.googleapis.com/v1beta1/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommendationengine_projects_locations_catalogs_event_stores_operations_get_execute()` or `recommendationengine_projects_locations_catalogs_event_stores_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_event_stores_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_event_stores_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommendationengine_projects_locations_catalogs_event_stores_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_event_stores_operations_get_task()`.
/// For the simplest API, use `recommendationengine_projects_locations_catalogs_event_stores_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_event_stores_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommendationengine_projects_locations_catalogs_event_stores_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        recommendationengine_projects_locations_catalogs_event_stores_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommendationengine_projects_locations_catalogs_event_stores_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommendationengineProjectsLocationsCatalogsEventStoresOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommendationengine_projects_locations_catalogs_event_stores_operations_get_builder()` + `recommendationengine_projects_locations_catalogs_event_stores_operations_get_execute()`.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_event_stores_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_event_stores_operations_get(
    client: &SimpleHttpClient,
    args: &RecommendationengineProjectsLocationsCatalogsEventStoresOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recommendationengine_projects_locations_catalogs_event_stores_operations_get_builder(
            client, &args.name,
        )?;
    recommendationengine_projects_locations_catalogs_event_stores_operations_get_execute(builder)
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommendationengine_projects_locations_catalogs_event_stores_operations_list_execute()` to send, or `recommendationengine_projects_locations_catalogs_event_stores_operations_list` for simplest API.

pub fn recommendationengine_projects_locations_catalogs_event_stores_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    returnPartialSuccess: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommendationengine.googleapis.com/v1beta1/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommendationengine_projects_locations_catalogs_event_stores_operations_list_execute()` or `recommendationengine_projects_locations_catalogs_event_stores_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_event_stores_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_event_stores_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommendationengine_projects_locations_catalogs_event_stores_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_event_stores_operations_list_task()`.
/// For the simplest API, use `recommendationengine_projects_locations_catalogs_event_stores_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_event_stores_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommendationengine_projects_locations_catalogs_event_stores_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommendationengine_projects_locations_catalogs_event_stores_operations_list_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommendationengine_projects_locations_catalogs_event_stores_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommendationengineProjectsLocationsCatalogsEventStoresOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<String>,
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommendationengine_projects_locations_catalogs_event_stores_operations_list_builder()` + `recommendationengine_projects_locations_catalogs_event_stores_operations_list_execute()`.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_event_stores_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_event_stores_operations_list(
    client: &SimpleHttpClient,
    args: &RecommendationengineProjectsLocationsCatalogsEventStoresOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recommendationengine_projects_locations_catalogs_event_stores_operations_list_builder(
            client,
            &args.name,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
            &args.returnPartialSuccess,
        )?;
    recommendationengine_projects_locations_catalogs_event_stores_operations_list_execute(builder)
}

/// POST v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/placements/{placementsId}:predict
/// Makes a recommendation prediction. If using API Key based authentication, the API Key must be registered using the PredictionApiKeyRegistry service. [Learn more](<https://cloud.google.`com/recommendations-ai/docs/setting-up`#register-key>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommendationengine_projects_locations_catalogs_event_stores_placements_predict_execute()` to send, or `recommendationengine_projects_locations_catalogs_event_stores_placements_predict` for simplest API.

pub fn recommendationengine_projects_locations_catalogs_event_stores_placements_predict_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommendationengine.googleapis.com/v1beta1/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/placements/{placementsId}:predict",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/placements/{placementsId}:predict
/// Makes a recommendation prediction. If using API Key based authentication, the API Key must be registered using the PredictionApiKeyRegistry service. [Learn more](<https://cloud.google.`com/recommendations-ai/docs/setting-up`#register-key>).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommendationengine_projects_locations_catalogs_event_stores_placements_predict_execute()` or `recommendationengine_projects_locations_catalogs_event_stores_placements_predict`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_event_stores_placements_predict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_event_stores_placements_predict_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRecommendationengineV1beta1PredictResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommendationengineV1beta1PredictResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/placements/{placementsId}:predict
/// Makes a recommendation prediction. If using API Key based authentication, the API Key must be registered using the PredictionApiKeyRegistry service. [Learn more](<https://cloud.google.`com/recommendations-ai/docs/setting-up`#register-key>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommendationengine_projects_locations_catalogs_event_stores_placements_predict_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_event_stores_placements_predict_task()`.
/// For the simplest API, use `recommendationengine_projects_locations_catalogs_event_stores_placements_predict()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_event_stores_placements_predict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommendationengine_projects_locations_catalogs_event_stores_placements_predict_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRecommendationengineV1beta1PredictResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        recommendationengine_projects_locations_catalogs_event_stores_placements_predict_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommendationengine_projects_locations_catalogs_event_stores_placements_predict`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommendationengineProjectsLocationsCatalogsEventStoresPlacementsPredictArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/placements/{placementsId}:predict
/// Makes a recommendation prediction. If using API Key based authentication, the API Key must be registered using the PredictionApiKeyRegistry service. [Learn more](<https://cloud.google.`com/recommendations-ai/docs/setting-up`#register-key>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommendationengine_projects_locations_catalogs_event_stores_placements_predict_builder()` + `recommendationengine_projects_locations_catalogs_event_stores_placements_predict_execute()`.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_event_stores_placements_predict_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_event_stores_placements_predict(
    client: &SimpleHttpClient,
    args: &RecommendationengineProjectsLocationsCatalogsEventStoresPlacementsPredictArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRecommendationengineV1beta1PredictResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recommendationengine_projects_locations_catalogs_event_stores_placements_predict_builder(
            client, &args.name,
        )?;
    recommendationengine_projects_locations_catalogs_event_stores_placements_predict_execute(
        builder,
    )
}

/// POST v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/predictionApiKeyRegistrations
/// Register an API key for use with predict method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_create_execute()` to send, or `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_create` for simplest API.

pub fn recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_create_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommendationengine.googleapis.com/v1beta1/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/predictionApiKeyRegistrations",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/predictionApiKeyRegistrations
/// Register an API key for use with predict method.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_create_execute()` or `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRecommendationengineV1beta1PredictionApiKeyRegistration>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommendationengineV1beta1PredictionApiKeyRegistration =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/predictionApiKeyRegistrations
/// Register an API key for use with predict method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_create_task()`.
/// For the simplest API, use `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRecommendationengineV1beta1PredictionApiKeyRegistration>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommendationengineProjectsLocationsCatalogsEventStoresPredictionApiKeyRegistrationsCreateArgs
{
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/predictionApiKeyRegistrations
/// Register an API key for use with predict method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_create_builder()` + `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_create_execute()`.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_create(
    client: &SimpleHttpClient,
    args: &RecommendationengineProjectsLocationsCatalogsEventStoresPredictionApiKeyRegistrationsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRecommendationengineV1beta1PredictionApiKeyRegistration>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_create_builder(client, &args.parent)?;
    recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_create_execute(builder)
}

/// DELETE v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/predictionApiKeyRegistrations/{predictionApiKeyRegistrationsId}
/// Unregister an `apiKey` from using for predict method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_delete_execute()` to send, or `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_delete` for simplest API.

pub fn recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_delete_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommendationengine.googleapis.com/v1beta1/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/predictionApiKeyRegistrations/{predictionApiKeyRegistrationsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/predictionApiKeyRegistrations/{predictionApiKeyRegistrationsId}
/// Unregister an `apiKey` from using for predict method.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_delete_execute()` or `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/predictionApiKeyRegistrations/{predictionApiKeyRegistrationsId}
/// Unregister an `apiKey` from using for predict method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_delete_task()`.
/// For the simplest API, use `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommendationengineProjectsLocationsCatalogsEventStoresPredictionApiKeyRegistrationsDeleteArgs
{
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/predictionApiKeyRegistrations/{predictionApiKeyRegistrationsId}
/// Unregister an `apiKey` from using for predict method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_delete_builder()` + `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_delete_execute()`.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_delete(
    client: &SimpleHttpClient,
    args: &RecommendationengineProjectsLocationsCatalogsEventStoresPredictionApiKeyRegistrationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_delete_builder(client, &args.name)?;
    recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_delete_execute(builder)
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/predictionApiKeyRegistrations
/// List the registered `apiKeys` for use with predict method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_list_execute()` to send, or `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_list` for simplest API.

pub fn recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_list_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommendationengine.googleapis.com/v1beta1/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/predictionApiKeyRegistrations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/predictionApiKeyRegistrations
/// List the registered `apiKeys` for use with predict method.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_list_execute()` or `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<
                    GoogleCloudRecommendationengineV1beta1ListPredictionApiKeyRegistrationsResponse,
                >,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, intro, headers, .. } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommendationengineV1beta1ListPredictionApiKeyRegistrationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/predictionApiKeyRegistrations
/// List the registered `apiKeys` for use with predict method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_list_task()`.
/// For the simplest API, use `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudRecommendationengineV1beta1ListPredictionApiKeyRegistrationsResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommendationengineProjectsLocationsCatalogsEventStoresPredictionApiKeyRegistrationsListArgs
{
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/predictionApiKeyRegistrations
/// List the registered `apiKeys` for use with predict method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_list_builder()` + `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_list_execute()`.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_list(
    client: &SimpleHttpClient,
    args: &RecommendationengineProjectsLocationsCatalogsEventStoresPredictionApiKeyRegistrationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<
                    GoogleCloudRecommendationengineV1beta1ListPredictionApiKeyRegistrationsResponse,
                >,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_list_builder(client, &args.parent, &args.pageSize, &args.pageToken)?;
    recommendationengine_projects_locations_catalogs_event_stores_prediction_api_key_registrations_list_execute(builder)
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/userEvents:collect
/// Writes a single user event from the browser. This uses a GET request to due to browser restriction of POST-ing to a 3rd party domain. This method is used only by the Recommendations AI JavaScript pixel. Users should not call this method directly.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommendationengine_projects_locations_catalogs_event_stores_user_events_collect_execute()` to send, or `recommendationengine_projects_locations_catalogs_event_stores_user_events_collect` for simplest API.

pub fn recommendationengine_projects_locations_catalogs_event_stores_user_events_collect_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    ets: &Option<String>,
    uri: &Option<String>,
    userEvent: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommendationengine.googleapis.com/v1beta1/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/userEvents:collect",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ets.as_ref() {
        query_parts.push(format!("ets={}", val));
    }
    if let Some(val) = uri.as_ref() {
        query_parts.push(format!("uri={}", val));
    }
    if let Some(val) = userEvent.as_ref() {
        query_parts.push(format!("userEvent={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/userEvents:collect
/// Writes a single user event from the browser. This uses a GET request to due to browser restriction of POST-ing to a 3rd party domain. This method is used only by the Recommendations AI JavaScript pixel. Users should not call this method directly.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_collect_execute()` or `recommendationengine_projects_locations_catalogs_event_stores_user_events_collect`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_event_stores_user_events_collect_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_event_stores_user_events_collect_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleApiHttpBody>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleApiHttpBody = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/userEvents:collect
/// Writes a single user event from the browser. This uses a GET request to due to browser restriction of POST-ing to a 3rd party domain. This method is used only by the Recommendations AI JavaScript pixel. Users should not call this method directly.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_collect_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_collect_task()`.
/// For the simplest API, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_collect()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_event_stores_user_events_collect_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommendationengine_projects_locations_catalogs_event_stores_user_events_collect_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        recommendationengine_projects_locations_catalogs_event_stores_user_events_collect_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommendationengine_projects_locations_catalogs_event_stores_user_events_collect`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommendationengineProjectsLocationsCatalogsEventStoresUserEventsCollectArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: ets
    pub ets: Option<String>,
    /// Query parameter: uri
    pub uri: Option<String>,
    /// Query parameter: userEvent
    pub userEvent: Option<String>,
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/userEvents:collect
/// Writes a single user event from the browser. This uses a GET request to due to browser restriction of POST-ing to a 3rd party domain. This method is used only by the Recommendations AI JavaScript pixel. Users should not call this method directly.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_collect_builder()` + `recommendationengine_projects_locations_catalogs_event_stores_user_events_collect_execute()`.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_collect_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_event_stores_user_events_collect(
    client: &SimpleHttpClient,
    args: &RecommendationengineProjectsLocationsCatalogsEventStoresUserEventsCollectArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleApiHttpBody>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        recommendationengine_projects_locations_catalogs_event_stores_user_events_collect_builder(
            client,
            &args.parent,
            &args.ets,
            &args.uri,
            &args.userEvent,
        )?;
    recommendationengine_projects_locations_catalogs_event_stores_user_events_collect_execute(
        builder,
    )
}

/// POST v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/userEvents:import
/// Bulk import of User events. Request processing might be synchronous. Events that already exist are skipped. Use this method for backfilling historical user events. Operation.response is of type ImportResponse. Note that it is possible for a subset of the items to be successfully inserted. Operation.metadata is of type ImportMetadata.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommendationengine_projects_locations_catalogs_event_stores_user_events_import_execute()` to send, or `recommendationengine_projects_locations_catalogs_event_stores_user_events_import` for simplest API.

pub fn recommendationengine_projects_locations_catalogs_event_stores_user_events_import_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommendationengine.googleapis.com/v1beta1/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/userEvents:import",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/userEvents:import
/// Bulk import of User events. Request processing might be synchronous. Events that already exist are skipped. Use this method for backfilling historical user events. Operation.response is of type ImportResponse. Note that it is possible for a subset of the items to be successfully inserted. Operation.metadata is of type ImportMetadata.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_import_execute()` or `recommendationengine_projects_locations_catalogs_event_stores_user_events_import`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_event_stores_user_events_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_event_stores_user_events_import_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/userEvents:import
/// Bulk import of User events. Request processing might be synchronous. Events that already exist are skipped. Use this method for backfilling historical user events. Operation.response is of type ImportResponse. Note that it is possible for a subset of the items to be successfully inserted. Operation.metadata is of type ImportMetadata.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_import_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_import_task()`.
/// For the simplest API, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_import()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_event_stores_user_events_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommendationengine_projects_locations_catalogs_event_stores_user_events_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        recommendationengine_projects_locations_catalogs_event_stores_user_events_import_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommendationengine_projects_locations_catalogs_event_stores_user_events_import`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommendationengineProjectsLocationsCatalogsEventStoresUserEventsImportArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/userEvents:import
/// Bulk import of User events. Request processing might be synchronous. Events that already exist are skipped. Use this method for backfilling historical user events. Operation.response is of type ImportResponse. Note that it is possible for a subset of the items to be successfully inserted. Operation.metadata is of type ImportMetadata.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_import_builder()` + `recommendationengine_projects_locations_catalogs_event_stores_user_events_import_execute()`.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_import_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_event_stores_user_events_import(
    client: &SimpleHttpClient,
    args: &RecommendationengineProjectsLocationsCatalogsEventStoresUserEventsImportArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recommendationengine_projects_locations_catalogs_event_stores_user_events_import_builder(
            client,
            &args.parent,
        )?;
    recommendationengine_projects_locations_catalogs_event_stores_user_events_import_execute(
        builder,
    )
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/userEvents
/// Gets a list of user events within a time range, with potential filtering. The method does not list unjoined user events. Unjoined user event definition: when a user event is ingested from Recommendations AI User Event APIs, the catalog item included in the user event is connected with the current catalog. If a catalog item of the ingested event is not in the current catalog, it could lead to degraded model quality. This is called an unjoined event.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommendationengine_projects_locations_catalogs_event_stores_user_events_list_execute()` to send, or `recommendationengine_projects_locations_catalogs_event_stores_user_events_list` for simplest API.

pub fn recommendationengine_projects_locations_catalogs_event_stores_user_events_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommendationengine.googleapis.com/v1beta1/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/userEvents",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/userEvents
/// Gets a list of user events within a time range, with potential filtering. The method does not list unjoined user events. Unjoined user event definition: when a user event is ingested from Recommendations AI User Event APIs, the catalog item included in the user event is connected with the current catalog. If a catalog item of the ingested event is not in the current catalog, it could lead to degraded model quality. This is called an unjoined event.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_list_execute()` or `recommendationengine_projects_locations_catalogs_event_stores_user_events_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_event_stores_user_events_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_event_stores_user_events_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudRecommendationengineV1beta1ListUserEventsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommendationengineV1beta1ListUserEventsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/userEvents
/// Gets a list of user events within a time range, with potential filtering. The method does not list unjoined user events. Unjoined user event definition: when a user event is ingested from Recommendations AI User Event APIs, the catalog item included in the user event is connected with the current catalog. If a catalog item of the ingested event is not in the current catalog, it could lead to degraded model quality. This is called an unjoined event.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_list_task()`.
/// For the simplest API, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_event_stores_user_events_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommendationengine_projects_locations_catalogs_event_stores_user_events_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRecommendationengineV1beta1ListUserEventsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommendationengine_projects_locations_catalogs_event_stores_user_events_list_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommendationengine_projects_locations_catalogs_event_stores_user_events_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommendationengineProjectsLocationsCatalogsEventStoresUserEventsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/userEvents
/// Gets a list of user events within a time range, with potential filtering. The method does not list unjoined user events. Unjoined user event definition: when a user event is ingested from Recommendations AI User Event APIs, the catalog item included in the user event is connected with the current catalog. If a catalog item of the ingested event is not in the current catalog, it could lead to degraded model quality. This is called an unjoined event.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_list_builder()` + `recommendationengine_projects_locations_catalogs_event_stores_user_events_list_execute()`.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_event_stores_user_events_list(
    client: &SimpleHttpClient,
    args: &RecommendationengineProjectsLocationsCatalogsEventStoresUserEventsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudRecommendationengineV1beta1ListUserEventsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recommendationengine_projects_locations_catalogs_event_stores_user_events_list_builder(
            client,
            &args.parent,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
        )?;
    recommendationengine_projects_locations_catalogs_event_stores_user_events_list_execute(builder)
}

/// POST v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/userEvents:purge
/// Deletes permanently all user events specified by the filter provided. Depending on the number of events specified by the filter, this operation could take hours or days to complete. To test a filter, use the list command first.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommendationengine_projects_locations_catalogs_event_stores_user_events_purge_execute()` to send, or `recommendationengine_projects_locations_catalogs_event_stores_user_events_purge` for simplest API.

pub fn recommendationengine_projects_locations_catalogs_event_stores_user_events_purge_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommendationengine.googleapis.com/v1beta1/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/userEvents:purge",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/userEvents:purge
/// Deletes permanently all user events specified by the filter provided. Depending on the number of events specified by the filter, this operation could take hours or days to complete. To test a filter, use the list command first.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_purge_execute()` or `recommendationengine_projects_locations_catalogs_event_stores_user_events_purge`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_event_stores_user_events_purge_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_event_stores_user_events_purge_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/userEvents:purge
/// Deletes permanently all user events specified by the filter provided. Depending on the number of events specified by the filter, this operation could take hours or days to complete. To test a filter, use the list command first.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_purge_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_purge_task()`.
/// For the simplest API, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_purge()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_event_stores_user_events_purge_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommendationengine_projects_locations_catalogs_event_stores_user_events_purge_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        recommendationengine_projects_locations_catalogs_event_stores_user_events_purge_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommendationengine_projects_locations_catalogs_event_stores_user_events_purge`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommendationengineProjectsLocationsCatalogsEventStoresUserEventsPurgeArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/userEvents:purge
/// Deletes permanently all user events specified by the filter provided. Depending on the number of events specified by the filter, this operation could take hours or days to complete. To test a filter, use the list command first.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_purge_builder()` + `recommendationengine_projects_locations_catalogs_event_stores_user_events_purge_execute()`.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_purge_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_event_stores_user_events_purge(
    client: &SimpleHttpClient,
    args: &RecommendationengineProjectsLocationsCatalogsEventStoresUserEventsPurgeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recommendationengine_projects_locations_catalogs_event_stores_user_events_purge_builder(
            client,
            &args.parent,
        )?;
    recommendationengine_projects_locations_catalogs_event_stores_user_events_purge_execute(builder)
}

/// POST v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/userEvents:rejoin
/// Triggers a user event rejoin operation with latest catalog data. Events will not be annotated with detailed catalog information if catalog item is missing at the time the user event is ingested, and these events are stored as unjoined events with a limited usage on training and serving. This API can be used to trigger a ''join'' operation on specified events with latest version of catalog items. It can also be used to correct events joined with wrong catalog items.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommendationengine_projects_locations_catalogs_event_stores_user_events_rejoin_execute()` to send, or `recommendationengine_projects_locations_catalogs_event_stores_user_events_rejoin` for simplest API.

pub fn recommendationengine_projects_locations_catalogs_event_stores_user_events_rejoin_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommendationengine.googleapis.com/v1beta1/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/userEvents:rejoin",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/userEvents:rejoin
/// Triggers a user event rejoin operation with latest catalog data. Events will not be annotated with detailed catalog information if catalog item is missing at the time the user event is ingested, and these events are stored as unjoined events with a limited usage on training and serving. This API can be used to trigger a ''join'' operation on specified events with latest version of catalog items. It can also be used to correct events joined with wrong catalog items.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_rejoin_execute()` or `recommendationengine_projects_locations_catalogs_event_stores_user_events_rejoin`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_event_stores_user_events_rejoin_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_event_stores_user_events_rejoin_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/userEvents:rejoin
/// Triggers a user event rejoin operation with latest catalog data. Events will not be annotated with detailed catalog information if catalog item is missing at the time the user event is ingested, and these events are stored as unjoined events with a limited usage on training and serving. This API can be used to trigger a ''join'' operation on specified events with latest version of catalog items. It can also be used to correct events joined with wrong catalog items.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_rejoin_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_rejoin_task()`.
/// For the simplest API, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_rejoin()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_event_stores_user_events_rejoin_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommendationengine_projects_locations_catalogs_event_stores_user_events_rejoin_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        recommendationengine_projects_locations_catalogs_event_stores_user_events_rejoin_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommendationengine_projects_locations_catalogs_event_stores_user_events_rejoin`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommendationengineProjectsLocationsCatalogsEventStoresUserEventsRejoinArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/userEvents:rejoin
/// Triggers a user event rejoin operation with latest catalog data. Events will not be annotated with detailed catalog information if catalog item is missing at the time the user event is ingested, and these events are stored as unjoined events with a limited usage on training and serving. This API can be used to trigger a ''join'' operation on specified events with latest version of catalog items. It can also be used to correct events joined with wrong catalog items.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_rejoin_builder()` + `recommendationengine_projects_locations_catalogs_event_stores_user_events_rejoin_execute()`.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_rejoin_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_event_stores_user_events_rejoin(
    client: &SimpleHttpClient,
    args: &RecommendationengineProjectsLocationsCatalogsEventStoresUserEventsRejoinArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recommendationengine_projects_locations_catalogs_event_stores_user_events_rejoin_builder(
            client,
            &args.parent,
        )?;
    recommendationengine_projects_locations_catalogs_event_stores_user_events_rejoin_execute(
        builder,
    )
}

/// POST v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/userEvents:write
/// Writes a single user event.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommendationengine_projects_locations_catalogs_event_stores_user_events_write_execute()` to send, or `recommendationengine_projects_locations_catalogs_event_stores_user_events_write` for simplest API.

pub fn recommendationengine_projects_locations_catalogs_event_stores_user_events_write_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommendationengine.googleapis.com/v1beta1/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/userEvents:write",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/userEvents:write
/// Writes a single user event.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_write_execute()` or `recommendationengine_projects_locations_catalogs_event_stores_user_events_write`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_event_stores_user_events_write_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_event_stores_user_events_write_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudRecommendationengineV1beta1UserEvent>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudRecommendationengineV1beta1UserEvent =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/userEvents:write
/// Writes a single user event.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_write_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_write_task()`.
/// For the simplest API, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_write()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_event_stores_user_events_write_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommendationengine_projects_locations_catalogs_event_stores_user_events_write_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommendationengineV1beta1UserEvent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        recommendationengine_projects_locations_catalogs_event_stores_user_events_write_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommendationengine_projects_locations_catalogs_event_stores_user_events_write`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommendationengineProjectsLocationsCatalogsEventStoresUserEventsWriteArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/eventStores/{eventStoresId}/userEvents:write
/// Writes a single user event.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_write_builder()` + `recommendationengine_projects_locations_catalogs_event_stores_user_events_write_execute()`.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_event_stores_user_events_write_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_event_stores_user_events_write(
    client: &SimpleHttpClient,
    args: &RecommendationengineProjectsLocationsCatalogsEventStoresUserEventsWriteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudRecommendationengineV1beta1UserEvent>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        recommendationengine_projects_locations_catalogs_event_stores_user_events_write_builder(
            client,
            &args.parent,
        )?;
    recommendationengine_projects_locations_catalogs_event_stores_user_events_write_execute(builder)
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommendationengine_projects_locations_catalogs_operations_get_execute()` to send, or `recommendationengine_projects_locations_catalogs_operations_get` for simplest API.

pub fn recommendationengine_projects_locations_catalogs_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommendationengine.googleapis.com/v1beta1/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommendationengine_projects_locations_catalogs_operations_get_execute()` or `recommendationengine_projects_locations_catalogs_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommendationengine_projects_locations_catalogs_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_operations_get_task()`.
/// For the simplest API, use `recommendationengine_projects_locations_catalogs_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommendationengine_projects_locations_catalogs_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommendationengine_projects_locations_catalogs_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommendationengine_projects_locations_catalogs_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommendationengineProjectsLocationsCatalogsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommendationengine_projects_locations_catalogs_operations_get_builder()` + `recommendationengine_projects_locations_catalogs_operations_get_execute()`.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_operations_get(
    client: &SimpleHttpClient,
    args: &RecommendationengineProjectsLocationsCatalogsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommendationengine_projects_locations_catalogs_operations_get_builder(
        client, &args.name,
    )?;
    recommendationengine_projects_locations_catalogs_operations_get_execute(builder)
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `recommendationengine_projects_locations_catalogs_operations_list_execute()` to send, or `recommendationengine_projects_locations_catalogs_operations_list` for simplest API.

pub fn recommendationengine_projects_locations_catalogs_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    returnPartialSuccess: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://recommendationengine.googleapis.com/v1beta1/projects/{}/locations/{locationsId}/catalogs/{catalogsId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `recommendationengine_projects_locations_catalogs_operations_list_execute()` or `recommendationengine_projects_locations_catalogs_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `recommendationengine_projects_locations_catalogs_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_operations_list_task()`.
/// For the simplest API, use `recommendationengine_projects_locations_catalogs_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `recommendationengine_projects_locations_catalogs_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn recommendationengine_projects_locations_catalogs_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = recommendationengine_projects_locations_catalogs_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`recommendationengine_projects_locations_catalogs_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct RecommendationengineProjectsLocationsCatalogsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<String>,
}

/// GET v1beta1/projects/{projectsId}/locations/{locationsId}/catalogs/{catalogsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `recommendationengine_projects_locations_catalogs_operations_list_builder()` + `recommendationengine_projects_locations_catalogs_operations_list_execute()`.
/// For task-level control, use `recommendationengine_projects_locations_catalogs_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn recommendationengine_projects_locations_catalogs_operations_list(
    client: &SimpleHttpClient,
    args: &RecommendationengineProjectsLocationsCatalogsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = recommendationengine_projects_locations_catalogs_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    recommendationengine_projects_locations_catalogs_operations_list_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommendationengineV1beta1ListCatalogsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommendationengineV1beta1ListCatalogsResponse with RecommendationengineProjectsLocationsCatalogsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommendationengineProjectsLocationsCatalogsListArgs>
    for GoogleCloudRecommendationengineV1beta1ListCatalogsResponse
{
    fn generate_resource_id(
        &self,
        input: &RecommendationengineProjectsLocationsCatalogsListArgs,
    ) -> String {
        format!("gcp::recommendationengine::GoogleCloudRecommendationengineV1beta1ListCatalogsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommendationengine::GoogleCloudRecommendationengineV1beta1ListCatalogsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommendationengineV1beta1Catalog
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommendationengineV1beta1Catalog with RecommendationengineProjectsLocationsCatalogsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommendationengineProjectsLocationsCatalogsPatchArgs>
    for GoogleCloudRecommendationengineV1beta1Catalog
{
    fn generate_resource_id(
        &self,
        input: &RecommendationengineProjectsLocationsCatalogsPatchArgs,
    ) -> String {
        format!(
            "gcp::recommendationengine::GoogleCloudRecommendationengineV1beta1Catalog/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommendationengine::GoogleCloudRecommendationengineV1beta1Catalog"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommendationengineV1beta1CatalogItem
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommendationengineV1beta1CatalogItem with RecommendationengineProjectsLocationsCatalogsCatalogItemsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommendationengineProjectsLocationsCatalogsCatalogItemsCreateArgs>
    for GoogleCloudRecommendationengineV1beta1CatalogItem
{
    fn generate_resource_id(
        &self,
        input: &RecommendationengineProjectsLocationsCatalogsCatalogItemsCreateArgs,
    ) -> String {
        format!(
            "gcp::recommendationengine::GoogleCloudRecommendationengineV1beta1CatalogItem/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommendationengine::GoogleCloudRecommendationengineV1beta1CatalogItem"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with RecommendationengineProjectsLocationsCatalogsCatalogItemsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommendationengineProjectsLocationsCatalogsCatalogItemsDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &RecommendationengineProjectsLocationsCatalogsCatalogItemsDeleteArgs,
    ) -> String {
        format!(
            "gcp::recommendationengine::GoogleProtobufEmpty/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommendationengine::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommendationengineV1beta1CatalogItem
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommendationengineV1beta1CatalogItem with RecommendationengineProjectsLocationsCatalogsCatalogItemsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommendationengineProjectsLocationsCatalogsCatalogItemsGetArgs>
    for GoogleCloudRecommendationengineV1beta1CatalogItem
{
    fn generate_resource_id(
        &self,
        input: &RecommendationengineProjectsLocationsCatalogsCatalogItemsGetArgs,
    ) -> String {
        format!(
            "gcp::recommendationengine::GoogleCloudRecommendationengineV1beta1CatalogItem/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommendationengine::GoogleCloudRecommendationengineV1beta1CatalogItem"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with RecommendationengineProjectsLocationsCatalogsCatalogItemsImportArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommendationengineProjectsLocationsCatalogsCatalogItemsImportArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &RecommendationengineProjectsLocationsCatalogsCatalogItemsImportArgs,
    ) -> String {
        format!(
            "gcp::recommendationengine::GoogleLongrunningOperation/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommendationengine::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommendationengineV1beta1ListCatalogItemsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommendationengineV1beta1ListCatalogItemsResponse with RecommendationengineProjectsLocationsCatalogsCatalogItemsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommendationengineProjectsLocationsCatalogsCatalogItemsListArgs>
    for GoogleCloudRecommendationengineV1beta1ListCatalogItemsResponse
{
    fn generate_resource_id(
        &self,
        input: &RecommendationengineProjectsLocationsCatalogsCatalogItemsListArgs,
    ) -> String {
        format!("gcp::recommendationengine::GoogleCloudRecommendationengineV1beta1ListCatalogItemsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommendationengine::GoogleCloudRecommendationengineV1beta1ListCatalogItemsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommendationengineV1beta1CatalogItem
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommendationengineV1beta1CatalogItem with RecommendationengineProjectsLocationsCatalogsCatalogItemsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommendationengineProjectsLocationsCatalogsCatalogItemsPatchArgs>
    for GoogleCloudRecommendationengineV1beta1CatalogItem
{
    fn generate_resource_id(
        &self,
        input: &RecommendationengineProjectsLocationsCatalogsCatalogItemsPatchArgs,
    ) -> String {
        format!(
            "gcp::recommendationengine::GoogleCloudRecommendationengineV1beta1CatalogItem/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommendationengine::GoogleCloudRecommendationengineV1beta1CatalogItem"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with RecommendationengineProjectsLocationsCatalogsEventStoresOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommendationengineProjectsLocationsCatalogsEventStoresOperationsGetArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &RecommendationengineProjectsLocationsCatalogsEventStoresOperationsGetArgs,
    ) -> String {
        format!(
            "gcp::recommendationengine::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommendationengine::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningListOperationsResponse with RecommendationengineProjectsLocationsCatalogsEventStoresOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommendationengineProjectsLocationsCatalogsEventStoresOperationsListArgs>
    for GoogleLongrunningListOperationsResponse
{
    fn generate_resource_id(
        &self,
        input: &RecommendationengineProjectsLocationsCatalogsEventStoresOperationsListArgs,
    ) -> String {
        format!(
            "gcp::recommendationengine::GoogleLongrunningListOperationsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommendationengine::GoogleLongrunningListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommendationengineV1beta1PredictResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommendationengineV1beta1PredictResponse with RecommendationengineProjectsLocationsCatalogsEventStoresPlacementsPredictArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        RecommendationengineProjectsLocationsCatalogsEventStoresPlacementsPredictArgs,
    > for GoogleCloudRecommendationengineV1beta1PredictResponse
{
    fn generate_resource_id(
        &self,
        input: &RecommendationengineProjectsLocationsCatalogsEventStoresPlacementsPredictArgs,
    ) -> String {
        format!(
            "gcp::recommendationengine::GoogleCloudRecommendationengineV1beta1PredictResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommendationengine::GoogleCloudRecommendationengineV1beta1PredictResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommendationengineV1beta1PredictionApiKeyRegistration
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommendationengineV1beta1PredictionApiKeyRegistration with RecommendationengineProjectsLocationsCatalogsEventStoresPredictionApiKeyRegistrationsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommendationengineProjectsLocationsCatalogsEventStoresPredictionApiKeyRegistrationsCreateArgs> for GoogleCloudRecommendationengineV1beta1PredictionApiKeyRegistration {
    fn generate_resource_id(&self, input: &RecommendationengineProjectsLocationsCatalogsEventStoresPredictionApiKeyRegistrationsCreateArgs) -> String {
        format!("gcp::recommendationengine::GoogleCloudRecommendationengineV1beta1PredictionApiKeyRegistration/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommendationengine::GoogleCloudRecommendationengineV1beta1PredictionApiKeyRegistration"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with RecommendationengineProjectsLocationsCatalogsEventStoresPredictionApiKeyRegistrationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommendationengineProjectsLocationsCatalogsEventStoresPredictionApiKeyRegistrationsDeleteArgs> for GoogleProtobufEmpty {
    fn generate_resource_id(&self, input: &RecommendationengineProjectsLocationsCatalogsEventStoresPredictionApiKeyRegistrationsDeleteArgs) -> String {
        format!("gcp::recommendationengine::GoogleProtobufEmpty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommendationengine::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommendationengineV1beta1ListPredictionApiKeyRegistrationsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommendationengineV1beta1ListPredictionApiKeyRegistrationsResponse with RecommendationengineProjectsLocationsCatalogsEventStoresPredictionApiKeyRegistrationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommendationengineProjectsLocationsCatalogsEventStoresPredictionApiKeyRegistrationsListArgs> for GoogleCloudRecommendationengineV1beta1ListPredictionApiKeyRegistrationsResponse {
    fn generate_resource_id(&self, input: &RecommendationengineProjectsLocationsCatalogsEventStoresPredictionApiKeyRegistrationsListArgs) -> String {
        format!("gcp::recommendationengine::GoogleCloudRecommendationengineV1beta1ListPredictionApiKeyRegistrationsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommendationengine::GoogleCloudRecommendationengineV1beta1ListPredictionApiKeyRegistrationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleApiHttpBody
// =============================================================================

/// ResourceIdentifier implementation for GoogleApiHttpBody with RecommendationengineProjectsLocationsCatalogsEventStoresUserEventsCollectArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        RecommendationengineProjectsLocationsCatalogsEventStoresUserEventsCollectArgs,
    > for GoogleApiHttpBody
{
    fn generate_resource_id(
        &self,
        input: &RecommendationengineProjectsLocationsCatalogsEventStoresUserEventsCollectArgs,
    ) -> String {
        format!(
            "gcp::recommendationengine::GoogleApiHttpBody/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommendationengine::GoogleApiHttpBody"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with RecommendationengineProjectsLocationsCatalogsEventStoresUserEventsImportArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<RecommendationengineProjectsLocationsCatalogsEventStoresUserEventsImportArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &RecommendationengineProjectsLocationsCatalogsEventStoresUserEventsImportArgs,
    ) -> String {
        format!(
            "gcp::recommendationengine::GoogleLongrunningOperation/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommendationengine::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommendationengineV1beta1ListUserEventsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommendationengineV1beta1ListUserEventsResponse with RecommendationengineProjectsLocationsCatalogsEventStoresUserEventsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommendationengineProjectsLocationsCatalogsEventStoresUserEventsListArgs>
    for GoogleCloudRecommendationengineV1beta1ListUserEventsResponse
{
    fn generate_resource_id(
        &self,
        input: &RecommendationengineProjectsLocationsCatalogsEventStoresUserEventsListArgs,
    ) -> String {
        format!("gcp::recommendationengine::GoogleCloudRecommendationengineV1beta1ListUserEventsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommendationengine::GoogleCloudRecommendationengineV1beta1ListUserEventsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with RecommendationengineProjectsLocationsCatalogsEventStoresUserEventsPurgeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommendationengineProjectsLocationsCatalogsEventStoresUserEventsPurgeArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &RecommendationengineProjectsLocationsCatalogsEventStoresUserEventsPurgeArgs,
    ) -> String {
        format!(
            "gcp::recommendationengine::GoogleLongrunningOperation/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommendationengine::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with RecommendationengineProjectsLocationsCatalogsEventStoresUserEventsRejoinArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<RecommendationengineProjectsLocationsCatalogsEventStoresUserEventsRejoinArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &RecommendationengineProjectsLocationsCatalogsEventStoresUserEventsRejoinArgs,
    ) -> String {
        format!(
            "gcp::recommendationengine::GoogleLongrunningOperation/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommendationengine::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudRecommendationengineV1beta1UserEvent
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudRecommendationengineV1beta1UserEvent with RecommendationengineProjectsLocationsCatalogsEventStoresUserEventsWriteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommendationengineProjectsLocationsCatalogsEventStoresUserEventsWriteArgs>
    for GoogleCloudRecommendationengineV1beta1UserEvent
{
    fn generate_resource_id(
        &self,
        input: &RecommendationengineProjectsLocationsCatalogsEventStoresUserEventsWriteArgs,
    ) -> String {
        format!(
            "gcp::recommendationengine::GoogleCloudRecommendationengineV1beta1UserEvent/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommendationengine::GoogleCloudRecommendationengineV1beta1UserEvent"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with RecommendationengineProjectsLocationsCatalogsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommendationengineProjectsLocationsCatalogsOperationsGetArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &RecommendationengineProjectsLocationsCatalogsOperationsGetArgs,
    ) -> String {
        format!(
            "gcp::recommendationengine::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommendationengine::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningListOperationsResponse with RecommendationengineProjectsLocationsCatalogsOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<RecommendationengineProjectsLocationsCatalogsOperationsListArgs>
    for GoogleLongrunningListOperationsResponse
{
    fn generate_resource_id(
        &self,
        input: &RecommendationengineProjectsLocationsCatalogsOperationsListArgs,
    ) -> String {
        format!(
            "gcp::recommendationengine::GoogleLongrunningListOperationsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::recommendationengine::GoogleLongrunningListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
