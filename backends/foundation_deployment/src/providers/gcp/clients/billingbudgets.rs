//! Auto-generated API clients for gcp/billingbudgets.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/billingbudgets`

#![cfg(feature = "gcp/billingbudgets")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/billingAccounts/{billingAccountsId}/budgets
/// Creates a new budget. See [Quotas and limits](<https://cloud.google.`com/billing/quotas`>) for more information on the limits of the number of budgets you can create.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `billingbudgets_billing_accounts_budgets_create_execute()` to send, or `billingbudgets_billing_accounts_budgets_create` for simplest API.

pub fn billingbudgets_billing_accounts_budgets_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudBillingBudgetsV1Budget,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://billingbudgets.googleapis.com/v1/billingAccounts/{}/budgets",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/billingAccounts/{billingAccountsId}/budgets
/// Creates a new budget. See [Quotas and limits](<https://cloud.google.`com/billing/quotas`>) for more information on the limits of the number of budgets you can create.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `billingbudgets_billing_accounts_budgets_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from billingbudgets_billing_accounts_budgets_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn billingbudgets_billing_accounts_budgets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudBillingBudgetsV1Budget>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudBillingBudgetsV1Budget = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/billingAccounts/{billingAccountsId}/budgets
/// Creates a new budget. See [Quotas and limits](<https://cloud.google.`com/billing/quotas`>) for more information on the limits of the number of budgets you can create.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `billingbudgets_billing_accounts_budgets_create_builder()` + `billingbudgets_billing_accounts_budgets_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn billingbudgets_billing_accounts_budgets_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudBillingBudgetsV1Budget,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudBillingBudgetsV1Budget>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = billingbudgets_billing_accounts_budgets_create_builder(client, parent, body)?;
    billingbudgets_billing_accounts_budgets_create_execute(builder)
}

/// GET v1/billingAccounts/{billingAccountsId}/budgets/{budgetsId}
/// Deletes a budget. Returns successfully if already deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `billingbudgets_billing_accounts_budgets_delete_execute()` to send, or `billingbudgets_billing_accounts_budgets_delete` for simplest API.

pub fn billingbudgets_billing_accounts_budgets_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://billingbudgets.googleapis.com/v1/billingAccounts/{}/budgets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/billingAccounts/{billingAccountsId}/budgets/{budgetsId}
/// Deletes a budget. Returns successfully if already deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `billingbudgets_billing_accounts_budgets_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from billingbudgets_billing_accounts_budgets_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn billingbudgets_billing_accounts_budgets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/billingAccounts/{billingAccountsId}/budgets/{budgetsId}
/// Deletes a budget. Returns successfully if already deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `billingbudgets_billing_accounts_budgets_delete_builder()` + `billingbudgets_billing_accounts_budgets_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn billingbudgets_billing_accounts_budgets_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = billingbudgets_billing_accounts_budgets_delete_builder(client, name)?;
    billingbudgets_billing_accounts_budgets_delete_execute(builder)
}

/// GET v1/billingAccounts/{billingAccountsId}/budgets/{budgetsId}
/// Returns a budget. WARNING: There are some fields exposed on the Google Cloud Console that aren't available on this API. When reading from the API, you will not see these fields in the return value, though they may have been set in the Cloud Console.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `billingbudgets_billing_accounts_budgets_get_execute()` to send, or `billingbudgets_billing_accounts_budgets_get` for simplest API.

pub fn billingbudgets_billing_accounts_budgets_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://billingbudgets.googleapis.com/v1/billingAccounts/{}/budgets/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/billingAccounts/{billingAccountsId}/budgets/{budgetsId}
/// Returns a budget. WARNING: There are some fields exposed on the Google Cloud Console that aren't available on this API. When reading from the API, you will not see these fields in the return value, though they may have been set in the Cloud Console.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `billingbudgets_billing_accounts_budgets_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from billingbudgets_billing_accounts_budgets_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn billingbudgets_billing_accounts_budgets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudBillingBudgetsV1Budget>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudBillingBudgetsV1Budget = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/billingAccounts/{billingAccountsId}/budgets/{budgetsId}
/// Returns a budget. WARNING: There are some fields exposed on the Google Cloud Console that aren't available on this API. When reading from the API, you will not see these fields in the return value, though they may have been set in the Cloud Console.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `billingbudgets_billing_accounts_budgets_get_builder()` + `billingbudgets_billing_accounts_budgets_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn billingbudgets_billing_accounts_budgets_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudBillingBudgetsV1Budget>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = billingbudgets_billing_accounts_budgets_get_builder(client, name)?;
    billingbudgets_billing_accounts_budgets_get_execute(builder)
}

/// GET v1/billingAccounts/{billingAccountsId}/budgets
/// Returns a list of budgets for a billing account. WARNING: There are some fields exposed on the Google Cloud Console that aren't available on this API. When reading from the API, you will not see these fields in the return value, though they may have been set in the Cloud Console.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `billingbudgets_billing_accounts_budgets_list_execute()` to send, or `billingbudgets_billing_accounts_budgets_list` for simplest API.

pub fn billingbudgets_billing_accounts_budgets_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    scope: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://billingbudgets.googleapis.com/v1/billingAccounts/{}/budgets",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = scope {
        query_parts.push(format!("scope={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/billingAccounts/{billingAccountsId}/budgets
/// Returns a list of budgets for a billing account. WARNING: There are some fields exposed on the Google Cloud Console that aren't available on this API. When reading from the API, you will not see these fields in the return value, though they may have been set in the Cloud Console.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `billingbudgets_billing_accounts_budgets_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from billingbudgets_billing_accounts_budgets_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn billingbudgets_billing_accounts_budgets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudBillingBudgetsV1ListBudgetsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudBillingBudgetsV1ListBudgetsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/billingAccounts/{billingAccountsId}/budgets
/// Returns a list of budgets for a billing account. WARNING: There are some fields exposed on the Google Cloud Console that aren't available on this API. When reading from the API, you will not see these fields in the return value, though they may have been set in the Cloud Console.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `billingbudgets_billing_accounts_budgets_list_builder()` + `billingbudgets_billing_accounts_budgets_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn billingbudgets_billing_accounts_budgets_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    scope: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudBillingBudgetsV1ListBudgetsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = billingbudgets_billing_accounts_budgets_list_builder(
        client, parent, pageSize, pageToken, scope,
    )?;
    billingbudgets_billing_accounts_budgets_list_execute(builder)
}

/// GET v1/billingAccounts/{billingAccountsId}/budgets/{budgetsId}
/// Updates a budget and returns the updated budget. WARNING: There are some fields exposed on the Google Cloud Console that aren't available on this API. Budget fields that are not exposed in this API will not be changed by this method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `billingbudgets_billing_accounts_budgets_patch_execute()` to send, or `billingbudgets_billing_accounts_budgets_patch` for simplest API.

pub fn billingbudgets_billing_accounts_budgets_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudBillingBudgetsV1Budget,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://billingbudgets.googleapis.com/v1/billingAccounts/{}/budgets/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/billingAccounts/{billingAccountsId}/budgets/{budgetsId}
/// Updates a budget and returns the updated budget. WARNING: There are some fields exposed on the Google Cloud Console that aren't available on this API. Budget fields that are not exposed in this API will not be changed by this method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `billingbudgets_billing_accounts_budgets_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from billingbudgets_billing_accounts_budgets_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn billingbudgets_billing_accounts_budgets_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudBillingBudgetsV1Budget>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudBillingBudgetsV1Budget = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/billingAccounts/{billingAccountsId}/budgets/{budgetsId}
/// Updates a budget and returns the updated budget. WARNING: There are some fields exposed on the Google Cloud Console that aren't available on this API. Budget fields that are not exposed in this API will not be changed by this method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `billingbudgets_billing_accounts_budgets_patch_builder()` + `billingbudgets_billing_accounts_budgets_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn billingbudgets_billing_accounts_budgets_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &GoogleCloudBillingBudgetsV1Budget,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudBillingBudgetsV1Budget>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        billingbudgets_billing_accounts_budgets_patch_builder(client, name, updateMask, body)?;
    billingbudgets_billing_accounts_budgets_patch_execute(builder)
}
