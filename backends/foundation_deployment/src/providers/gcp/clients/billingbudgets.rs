//! Auto-generated API clients for gcp/billingbudgets.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/billingAccounts/{billingAccountsId}/budgets
/// Creates a new budget. See [Quotas and limits](<https://cloud.google.`com/billing/quotas`>) for more information on the limits of the number of budgets you can create.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `billingbudgets_billing_accounts_budgets_create_execute()` to send, or `billingbudgets_billing_accounts_budgets_create` for simplest API.

pub fn billingbudgets_billing_accounts_budgets_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &GoogleCloudBillingBudgetsV1Budget,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://billingbudgets.googleapis.com/v1/billingAccounts/{}/budgets",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/billingAccounts/{billingAccountsId}/budgets
/// Creates a new budget. See [Quotas and limits](<https://cloud.google.`com/billing/quotas`>) for more information on the limits of the number of budgets you can create.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `billingbudgets_billing_accounts_budgets_create_execute()` or `billingbudgets_billing_accounts_budgets_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `billingbudgets_billing_accounts_budgets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn billingbudgets_billing_accounts_budgets_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudBillingBudgetsV1Budget>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudBillingBudgetsV1Budget = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/billingAccounts/{billingAccountsId}/budgets
/// Creates a new budget. See [Quotas and limits](<https://cloud.google.`com/billing/quotas`>) for more information on the limits of the number of budgets you can create.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `billingbudgets_billing_accounts_budgets_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `billingbudgets_billing_accounts_budgets_create_task()`.
/// For the simplest API, use `billingbudgets_billing_accounts_budgets_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `billingbudgets_billing_accounts_budgets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn billingbudgets_billing_accounts_budgets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudBillingBudgetsV1Budget>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = billingbudgets_billing_accounts_budgets_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`billingbudgets_billing_accounts_budgets_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BillingbudgetsBillingAccountsBudgetsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudBillingBudgetsV1Budget,
}

/// GET v1/billingAccounts/{billingAccountsId}/budgets
/// Creates a new budget. See [Quotas and limits](<https://cloud.google.`com/billing/quotas`>) for more information on the limits of the number of budgets you can create.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `billingbudgets_billing_accounts_budgets_create_builder()` + `billingbudgets_billing_accounts_budgets_create_execute()`.
/// For task-level control, use `billingbudgets_billing_accounts_budgets_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn billingbudgets_billing_accounts_budgets_create(
    client: &SimpleHttpClient,
    args: &BillingbudgetsBillingAccountsBudgetsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudBillingBudgetsV1Budget>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        billingbudgets_billing_accounts_budgets_create_builder(client, &args.parent, &args.body)?;
    billingbudgets_billing_accounts_budgets_create_execute(builder)
}
