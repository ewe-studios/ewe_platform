//! Auto-generated API clients for gcp/cloudcontrolspartner.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, StreamIterator, StreamIteratorExt, TaskIterator, TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/partner
/// Get details of a Partner.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudcontrolspartner_organizations_locations_get_partner_execute()` to send, or `cloudcontrolspartner_organizations_locations_get_partner` for simplest API.

pub fn cloudcontrolspartner_organizations_locations_get_partner_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudcontrolspartner.googleapis.com/v1/organizations/{}/locations/{}/partner",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/partner
/// Get details of a Partner.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudcontrolspartner_organizations_locations_get_partner_execute()` or `cloudcontrolspartner_organizations_locations_get_partner`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudcontrolspartner_organizations_locations_get_partner_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcontrolspartner_organizations_locations_get_partner_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Partner>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Partner = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/partner
/// Get details of a Partner.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudcontrolspartner_organizations_locations_get_partner_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudcontrolspartner_organizations_locations_get_partner_task()`.
/// For the simplest API, use `cloudcontrolspartner_organizations_locations_get_partner()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudcontrolspartner_organizations_locations_get_partner_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudcontrolspartner_organizations_locations_get_partner_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Partner>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudcontrolspartner_organizations_locations_get_partner_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudcontrolspartner_organizations_locations_get_partner`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudcontrolspartnerOrganizationsLocationsGetPartnerArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/partner
/// Get details of a Partner.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudcontrolspartner_organizations_locations_get_partner_builder()` + `cloudcontrolspartner_organizations_locations_get_partner_execute()`.
/// For task-level control, use `cloudcontrolspartner_organizations_locations_get_partner_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcontrolspartner_organizations_locations_get_partner(
    client: &SimpleHttpClient,
    args: &CloudcontrolspartnerOrganizationsLocationsGetPartnerArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Partner>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudcontrolspartner_organizations_locations_get_partner_builder(client, &args.name)?;
    cloudcontrolspartner_organizations_locations_get_partner_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers
/// Creates a new customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudcontrolspartner_organizations_locations_customers_create_execute()` to send, or `cloudcontrolspartner_organizations_locations_customers_create` for simplest API.

pub fn cloudcontrolspartner_organizations_locations_customers_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    customerId: Option<&str>,
    body: &Customer,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudcontrolspartner.googleapis.com/v1/organizations/{}/locations/{}/customers",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = customerId {
        query_parts.push(format!("customerId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers
/// Creates a new customer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudcontrolspartner_organizations_locations_customers_create_execute()` or `cloudcontrolspartner_organizations_locations_customers_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudcontrolspartner_organizations_locations_customers_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcontrolspartner_organizations_locations_customers_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Customer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Customer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers
/// Creates a new customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudcontrolspartner_organizations_locations_customers_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudcontrolspartner_organizations_locations_customers_create_task()`.
/// For the simplest API, use `cloudcontrolspartner_organizations_locations_customers_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudcontrolspartner_organizations_locations_customers_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudcontrolspartner_organizations_locations_customers_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Customer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudcontrolspartner_organizations_locations_customers_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudcontrolspartner_organizations_locations_customers_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudcontrolspartnerOrganizationsLocationsCustomersCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: customerId
    pub customerId: Option<String>,
    /// Request body.
    pub body: Customer,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers
/// Creates a new customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudcontrolspartner_organizations_locations_customers_create_builder()` + `cloudcontrolspartner_organizations_locations_customers_create_execute()`.
/// For task-level control, use `cloudcontrolspartner_organizations_locations_customers_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcontrolspartner_organizations_locations_customers_create(
    client: &SimpleHttpClient,
    args: &CloudcontrolspartnerOrganizationsLocationsCustomersCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Customer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudcontrolspartner_organizations_locations_customers_create_builder(
        client,
        &args.parent,
        args.customerId.as_deref(),
        &args.body,
    )?;
    cloudcontrolspartner_organizations_locations_customers_create_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}
/// Delete details of a single customer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudcontrolspartner_organizations_locations_customers_delete_execute()` to send, or `cloudcontrolspartner_organizations_locations_customers_delete` for simplest API.

pub fn cloudcontrolspartner_organizations_locations_customers_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudcontrolspartner.googleapis.com/v1/organizations/{}/locations/{}/customers/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}
/// Delete details of a single customer
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudcontrolspartner_organizations_locations_customers_delete_execute()` or `cloudcontrolspartner_organizations_locations_customers_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudcontrolspartner_organizations_locations_customers_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcontrolspartner_organizations_locations_customers_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}
/// Delete details of a single customer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudcontrolspartner_organizations_locations_customers_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudcontrolspartner_organizations_locations_customers_delete_task()`.
/// For the simplest API, use `cloudcontrolspartner_organizations_locations_customers_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudcontrolspartner_organizations_locations_customers_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudcontrolspartner_organizations_locations_customers_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudcontrolspartner_organizations_locations_customers_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudcontrolspartner_organizations_locations_customers_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudcontrolspartnerOrganizationsLocationsCustomersDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}
/// Delete details of a single customer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudcontrolspartner_organizations_locations_customers_delete_builder()` + `cloudcontrolspartner_organizations_locations_customers_delete_execute()`.
/// For task-level control, use `cloudcontrolspartner_organizations_locations_customers_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcontrolspartner_organizations_locations_customers_delete(
    client: &SimpleHttpClient,
    args: &CloudcontrolspartnerOrganizationsLocationsCustomersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudcontrolspartner_organizations_locations_customers_delete_builder(client, &args.name)?;
    cloudcontrolspartner_organizations_locations_customers_delete_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}
/// Gets details of a single customer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudcontrolspartner_organizations_locations_customers_get_execute()` to send, or `cloudcontrolspartner_organizations_locations_customers_get` for simplest API.

pub fn cloudcontrolspartner_organizations_locations_customers_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudcontrolspartner.googleapis.com/v1/organizations/{}/locations/{}/customers/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}
/// Gets details of a single customer
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudcontrolspartner_organizations_locations_customers_get_execute()` or `cloudcontrolspartner_organizations_locations_customers_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudcontrolspartner_organizations_locations_customers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcontrolspartner_organizations_locations_customers_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Customer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Customer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}
/// Gets details of a single customer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudcontrolspartner_organizations_locations_customers_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudcontrolspartner_organizations_locations_customers_get_task()`.
/// For the simplest API, use `cloudcontrolspartner_organizations_locations_customers_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudcontrolspartner_organizations_locations_customers_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudcontrolspartner_organizations_locations_customers_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Customer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudcontrolspartner_organizations_locations_customers_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudcontrolspartner_organizations_locations_customers_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudcontrolspartnerOrganizationsLocationsCustomersGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}
/// Gets details of a single customer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudcontrolspartner_organizations_locations_customers_get_builder()` + `cloudcontrolspartner_organizations_locations_customers_get_execute()`.
/// For task-level control, use `cloudcontrolspartner_organizations_locations_customers_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcontrolspartner_organizations_locations_customers_get(
    client: &SimpleHttpClient,
    args: &CloudcontrolspartnerOrganizationsLocationsCustomersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Customer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudcontrolspartner_organizations_locations_customers_get_builder(client, &args.name)?;
    cloudcontrolspartner_organizations_locations_customers_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers
/// Lists customers of a partner identified by its Google Cloud organization ID
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudcontrolspartner_organizations_locations_customers_list_execute()` to send, or `cloudcontrolspartner_organizations_locations_customers_list` for simplest API.

pub fn cloudcontrolspartner_organizations_locations_customers_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudcontrolspartner.googleapis.com/v1/organizations/{}/locations/{}/customers",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers
/// Lists customers of a partner identified by its Google Cloud organization ID
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudcontrolspartner_organizations_locations_customers_list_execute()` or `cloudcontrolspartner_organizations_locations_customers_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudcontrolspartner_organizations_locations_customers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcontrolspartner_organizations_locations_customers_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListCustomersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCustomersResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers
/// Lists customers of a partner identified by its Google Cloud organization ID
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudcontrolspartner_organizations_locations_customers_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudcontrolspartner_organizations_locations_customers_list_task()`.
/// For the simplest API, use `cloudcontrolspartner_organizations_locations_customers_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudcontrolspartner_organizations_locations_customers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudcontrolspartner_organizations_locations_customers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCustomersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = cloudcontrolspartner_organizations_locations_customers_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudcontrolspartner_organizations_locations_customers_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudcontrolspartnerOrganizationsLocationsCustomersListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers
/// Lists customers of a partner identified by its Google Cloud organization ID
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudcontrolspartner_organizations_locations_customers_list_builder()` + `cloudcontrolspartner_organizations_locations_customers_list_execute()`.
/// For task-level control, use `cloudcontrolspartner_organizations_locations_customers_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcontrolspartner_organizations_locations_customers_list(
    client: &SimpleHttpClient,
    args: &CloudcontrolspartnerOrganizationsLocationsCustomersListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCustomersResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudcontrolspartner_organizations_locations_customers_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    cloudcontrolspartner_organizations_locations_customers_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}
/// Update details of a single customer
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudcontrolspartner_organizations_locations_customers_patch_execute()` to send, or `cloudcontrolspartner_organizations_locations_customers_patch` for simplest API.

pub fn cloudcontrolspartner_organizations_locations_customers_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Customer,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudcontrolspartner.googleapis.com/v1/organizations/{}/locations/{}/customers/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}
/// Update details of a single customer
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudcontrolspartner_organizations_locations_customers_patch_execute()` or `cloudcontrolspartner_organizations_locations_customers_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudcontrolspartner_organizations_locations_customers_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcontrolspartner_organizations_locations_customers_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Customer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Customer = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}
/// Update details of a single customer
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudcontrolspartner_organizations_locations_customers_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudcontrolspartner_organizations_locations_customers_patch_task()`.
/// For the simplest API, use `cloudcontrolspartner_organizations_locations_customers_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudcontrolspartner_organizations_locations_customers_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudcontrolspartner_organizations_locations_customers_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Customer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudcontrolspartner_organizations_locations_customers_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudcontrolspartner_organizations_locations_customers_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudcontrolspartnerOrganizationsLocationsCustomersPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: Customer,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}
/// Update details of a single customer
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudcontrolspartner_organizations_locations_customers_patch_builder()` + `cloudcontrolspartner_organizations_locations_customers_patch_execute()`.
/// For task-level control, use `cloudcontrolspartner_organizations_locations_customers_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcontrolspartner_organizations_locations_customers_patch(
    client: &SimpleHttpClient,
    args: &CloudcontrolspartnerOrganizationsLocationsCustomersPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Customer>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudcontrolspartner_organizations_locations_customers_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    cloudcontrolspartner_organizations_locations_customers_patch_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}/workloads/{workloadsId}
/// Gets details of a single workload
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudcontrolspartner_organizations_locations_customers_workloads_get_execute()` to send, or `cloudcontrolspartner_organizations_locations_customers_workloads_get` for simplest API.

pub fn cloudcontrolspartner_organizations_locations_customers_workloads_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudcontrolspartner.googleapis.com/v1/organizations/{}/locations/{}/customers/{}/workloads/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}/workloads/{workloadsId}
/// Gets details of a single workload
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudcontrolspartner_organizations_locations_customers_workloads_get_execute()` or `cloudcontrolspartner_organizations_locations_customers_workloads_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudcontrolspartner_organizations_locations_customers_workloads_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcontrolspartner_organizations_locations_customers_workloads_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Workload>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Workload = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}/workloads/{workloadsId}
/// Gets details of a single workload
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudcontrolspartner_organizations_locations_customers_workloads_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudcontrolspartner_organizations_locations_customers_workloads_get_task()`.
/// For the simplest API, use `cloudcontrolspartner_organizations_locations_customers_workloads_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudcontrolspartner_organizations_locations_customers_workloads_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudcontrolspartner_organizations_locations_customers_workloads_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Workload>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudcontrolspartner_organizations_locations_customers_workloads_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudcontrolspartner_organizations_locations_customers_workloads_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudcontrolspartnerOrganizationsLocationsCustomersWorkloadsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}/workloads/{workloadsId}
/// Gets details of a single workload
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudcontrolspartner_organizations_locations_customers_workloads_get_builder()` + `cloudcontrolspartner_organizations_locations_customers_workloads_get_execute()`.
/// For task-level control, use `cloudcontrolspartner_organizations_locations_customers_workloads_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcontrolspartner_organizations_locations_customers_workloads_get(
    client: &SimpleHttpClient,
    args: &CloudcontrolspartnerOrganizationsLocationsCustomersWorkloadsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Workload>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudcontrolspartner_organizations_locations_customers_workloads_get_builder(
        client, &args.name,
    )?;
    cloudcontrolspartner_organizations_locations_customers_workloads_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}/workloads/{workloadsId}/ekmConnections
/// Gets the EKM connections associated with a workload
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudcontrolspartner_organizations_locations_customers_workloads_get_ekm_connections_execute()` to send, or `cloudcontrolspartner_organizations_locations_customers_workloads_get_ekm_connections` for simplest API.

pub fn cloudcontrolspartner_organizations_locations_customers_workloads_get_ekm_connections_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudcontrolspartner.googleapis.com/v1/organizations/{}/locations/{}/customers/{}/workloads/{}/ekmConnections",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}/workloads/{workloadsId}/ekmConnections
/// Gets the EKM connections associated with a workload
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudcontrolspartner_organizations_locations_customers_workloads_get_ekm_connections_execute()` or `cloudcontrolspartner_organizations_locations_customers_workloads_get_ekm_connections`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudcontrolspartner_organizations_locations_customers_workloads_get_ekm_connections_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcontrolspartner_organizations_locations_customers_workloads_get_ekm_connections_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<EkmConnections>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: EkmConnections = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}/workloads/{workloadsId}/ekmConnections
/// Gets the EKM connections associated with a workload
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudcontrolspartner_organizations_locations_customers_workloads_get_ekm_connections_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudcontrolspartner_organizations_locations_customers_workloads_get_ekm_connections_task()`.
/// For the simplest API, use `cloudcontrolspartner_organizations_locations_customers_workloads_get_ekm_connections()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudcontrolspartner_organizations_locations_customers_workloads_get_ekm_connections_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudcontrolspartner_organizations_locations_customers_workloads_get_ekm_connections_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EkmConnections>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        cloudcontrolspartner_organizations_locations_customers_workloads_get_ekm_connections_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudcontrolspartner_organizations_locations_customers_workloads_get_ekm_connections`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudcontrolspartnerOrganizationsLocationsCustomersWorkloadsGetEkmConnectionsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}/workloads/{workloadsId}/ekmConnections
/// Gets the EKM connections associated with a workload
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudcontrolspartner_organizations_locations_customers_workloads_get_ekm_connections_builder()` + `cloudcontrolspartner_organizations_locations_customers_workloads_get_ekm_connections_execute()`.
/// For task-level control, use `cloudcontrolspartner_organizations_locations_customers_workloads_get_ekm_connections_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcontrolspartner_organizations_locations_customers_workloads_get_ekm_connections(
    client: &SimpleHttpClient,
    args: &CloudcontrolspartnerOrganizationsLocationsCustomersWorkloadsGetEkmConnectionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<EkmConnections>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudcontrolspartner_organizations_locations_customers_workloads_get_ekm_connections_builder(client, &args.name)?;
    cloudcontrolspartner_organizations_locations_customers_workloads_get_ekm_connections_execute(
        builder,
    )
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}/workloads/{workloadsId}/partnerPermissions
/// Gets the partner permissions granted for a workload
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudcontrolspartner_organizations_locations_customers_workloads_get_partner_permissions_execute()` to send, or `cloudcontrolspartner_organizations_locations_customers_workloads_get_partner_permissions` for simplest API.

pub fn cloudcontrolspartner_organizations_locations_customers_workloads_get_partner_permissions_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudcontrolspartner.googleapis.com/v1/organizations/{}/locations/{}/customers/{}/workloads/{}/partnerPermissions",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}/workloads/{workloadsId}/partnerPermissions
/// Gets the partner permissions granted for a workload
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudcontrolspartner_organizations_locations_customers_workloads_get_partner_permissions_execute()` or `cloudcontrolspartner_organizations_locations_customers_workloads_get_partner_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudcontrolspartner_organizations_locations_customers_workloads_get_partner_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcontrolspartner_organizations_locations_customers_workloads_get_partner_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<PartnerPermissions>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PartnerPermissions = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}/workloads/{workloadsId}/partnerPermissions
/// Gets the partner permissions granted for a workload
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudcontrolspartner_organizations_locations_customers_workloads_get_partner_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudcontrolspartner_organizations_locations_customers_workloads_get_partner_permissions_task()`.
/// For the simplest API, use `cloudcontrolspartner_organizations_locations_customers_workloads_get_partner_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudcontrolspartner_organizations_locations_customers_workloads_get_partner_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudcontrolspartner_organizations_locations_customers_workloads_get_partner_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PartnerPermissions>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = cloudcontrolspartner_organizations_locations_customers_workloads_get_partner_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudcontrolspartner_organizations_locations_customers_workloads_get_partner_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudcontrolspartnerOrganizationsLocationsCustomersWorkloadsGetPartnerPermissionsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}/workloads/{workloadsId}/partnerPermissions
/// Gets the partner permissions granted for a workload
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudcontrolspartner_organizations_locations_customers_workloads_get_partner_permissions_builder()` + `cloudcontrolspartner_organizations_locations_customers_workloads_get_partner_permissions_execute()`.
/// For task-level control, use `cloudcontrolspartner_organizations_locations_customers_workloads_get_partner_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcontrolspartner_organizations_locations_customers_workloads_get_partner_permissions(
    client: &SimpleHttpClient,
    args: &CloudcontrolspartnerOrganizationsLocationsCustomersWorkloadsGetPartnerPermissionsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PartnerPermissions>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudcontrolspartner_organizations_locations_customers_workloads_get_partner_permissions_builder(client, &args.name)?;
    cloudcontrolspartner_organizations_locations_customers_workloads_get_partner_permissions_execute(
        builder,
    )
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}/workloads
/// Lists customer workloads for a given customer org id
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudcontrolspartner_organizations_locations_customers_workloads_list_execute()` to send, or `cloudcontrolspartner_organizations_locations_customers_workloads_list` for simplest API.

pub fn cloudcontrolspartner_organizations_locations_customers_workloads_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudcontrolspartner.googleapis.com/v1/organizations/{}/locations/{}/customers/{}/workloads",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}/workloads
/// Lists customer workloads for a given customer org id
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudcontrolspartner_organizations_locations_customers_workloads_list_execute()` or `cloudcontrolspartner_organizations_locations_customers_workloads_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudcontrolspartner_organizations_locations_customers_workloads_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcontrolspartner_organizations_locations_customers_workloads_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListWorkloadsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListWorkloadsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}/workloads
/// Lists customer workloads for a given customer org id
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudcontrolspartner_organizations_locations_customers_workloads_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudcontrolspartner_organizations_locations_customers_workloads_list_task()`.
/// For the simplest API, use `cloudcontrolspartner_organizations_locations_customers_workloads_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudcontrolspartner_organizations_locations_customers_workloads_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudcontrolspartner_organizations_locations_customers_workloads_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListWorkloadsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = cloudcontrolspartner_organizations_locations_customers_workloads_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudcontrolspartner_organizations_locations_customers_workloads_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudcontrolspartnerOrganizationsLocationsCustomersWorkloadsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}/workloads
/// Lists customer workloads for a given customer org id
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudcontrolspartner_organizations_locations_customers_workloads_list_builder()` + `cloudcontrolspartner_organizations_locations_customers_workloads_list_execute()`.
/// For task-level control, use `cloudcontrolspartner_organizations_locations_customers_workloads_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcontrolspartner_organizations_locations_customers_workloads_list(
    client: &SimpleHttpClient,
    args: &CloudcontrolspartnerOrganizationsLocationsCustomersWorkloadsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListWorkloadsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudcontrolspartner_organizations_locations_customers_workloads_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    cloudcontrolspartner_organizations_locations_customers_workloads_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}/workloads/{workloadsId}/accessApprovalRequests
/// Deprecated: Only returns access approval requests directly associated with an assured workload folder.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudcontrolspartner_organizations_locations_customers_workloads_access_approval_requests_list_execute()` to send, or `cloudcontrolspartner_organizations_locations_customers_workloads_access_approval_requests_list` for simplest API.

pub fn cloudcontrolspartner_organizations_locations_customers_workloads_access_approval_requests_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudcontrolspartner.googleapis.com/v1/organizations/{}/locations/{}/customers/{}/workloads/{}/accessApprovalRequests",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}/workloads/{workloadsId}/accessApprovalRequests
/// Deprecated: Only returns access approval requests directly associated with an assured workload folder.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudcontrolspartner_organizations_locations_customers_workloads_access_approval_requests_list_execute()` or `cloudcontrolspartner_organizations_locations_customers_workloads_access_approval_requests_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudcontrolspartner_organizations_locations_customers_workloads_access_approval_requests_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcontrolspartner_organizations_locations_customers_workloads_access_approval_requests_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<ListAccessApprovalRequestsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAccessApprovalRequestsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}/workloads/{workloadsId}/accessApprovalRequests
/// Deprecated: Only returns access approval requests directly associated with an assured workload folder.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudcontrolspartner_organizations_locations_customers_workloads_access_approval_requests_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudcontrolspartner_organizations_locations_customers_workloads_access_approval_requests_list_task()`.
/// For the simplest API, use `cloudcontrolspartner_organizations_locations_customers_workloads_access_approval_requests_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudcontrolspartner_organizations_locations_customers_workloads_access_approval_requests_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudcontrolspartner_organizations_locations_customers_workloads_access_approval_requests_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAccessApprovalRequestsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudcontrolspartner_organizations_locations_customers_workloads_access_approval_requests_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudcontrolspartner_organizations_locations_customers_workloads_access_approval_requests_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudcontrolspartnerOrganizationsLocationsCustomersWorkloadsAccessApprovalRequestsListArgs
{
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}/workloads/{workloadsId}/accessApprovalRequests
/// Deprecated: Only returns access approval requests directly associated with an assured workload folder.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudcontrolspartner_organizations_locations_customers_workloads_access_approval_requests_list_builder()` + `cloudcontrolspartner_organizations_locations_customers_workloads_access_approval_requests_list_execute()`.
/// For task-level control, use `cloudcontrolspartner_organizations_locations_customers_workloads_access_approval_requests_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcontrolspartner_organizations_locations_customers_workloads_access_approval_requests_list(
    client: &SimpleHttpClient,
    args: &CloudcontrolspartnerOrganizationsLocationsCustomersWorkloadsAccessApprovalRequestsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAccessApprovalRequestsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudcontrolspartner_organizations_locations_customers_workloads_access_approval_requests_list_builder(client, &args.parent, args.filter.as_deref(), args.orderBy.as_deref(), args.pageSize, args.pageToken.as_deref())?;
    cloudcontrolspartner_organizations_locations_customers_workloads_access_approval_requests_list_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}/workloads/{workloadsId}/violations/{violationsId}
/// Gets details of a single Violation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudcontrolspartner_organizations_locations_customers_workloads_violations_get_execute()` to send, or `cloudcontrolspartner_organizations_locations_customers_workloads_violations_get` for simplest API.

pub fn cloudcontrolspartner_organizations_locations_customers_workloads_violations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudcontrolspartner.googleapis.com/v1/organizations/{}/locations/{}/customers/{}/workloads/{}/violations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}/workloads/{workloadsId}/violations/{violationsId}
/// Gets details of a single Violation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudcontrolspartner_organizations_locations_customers_workloads_violations_get_execute()` or `cloudcontrolspartner_organizations_locations_customers_workloads_violations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudcontrolspartner_organizations_locations_customers_workloads_violations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcontrolspartner_organizations_locations_customers_workloads_violations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Violation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Violation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}/workloads/{workloadsId}/violations/{violationsId}
/// Gets details of a single Violation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudcontrolspartner_organizations_locations_customers_workloads_violations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudcontrolspartner_organizations_locations_customers_workloads_violations_get_task()`.
/// For the simplest API, use `cloudcontrolspartner_organizations_locations_customers_workloads_violations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudcontrolspartner_organizations_locations_customers_workloads_violations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudcontrolspartner_organizations_locations_customers_workloads_violations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Violation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        cloudcontrolspartner_organizations_locations_customers_workloads_violations_get_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudcontrolspartner_organizations_locations_customers_workloads_violations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudcontrolspartnerOrganizationsLocationsCustomersWorkloadsViolationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}/workloads/{workloadsId}/violations/{violationsId}
/// Gets details of a single Violation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudcontrolspartner_organizations_locations_customers_workloads_violations_get_builder()` + `cloudcontrolspartner_organizations_locations_customers_workloads_violations_get_execute()`.
/// For task-level control, use `cloudcontrolspartner_organizations_locations_customers_workloads_violations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcontrolspartner_organizations_locations_customers_workloads_violations_get(
    client: &SimpleHttpClient,
    args: &CloudcontrolspartnerOrganizationsLocationsCustomersWorkloadsViolationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Violation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudcontrolspartner_organizations_locations_customers_workloads_violations_get_builder(
            client, &args.name,
        )?;
    cloudcontrolspartner_organizations_locations_customers_workloads_violations_get_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}/workloads/{workloadsId}/violations
/// Lists Violations for a workload Callers may also choose to read across multiple Customers or for a single customer as per [AIP-159](<https://google.aip.`dev/159`>) by using '-' (the hyphen or dash character) as a wildcard character instead of {customer} & {workload}. Format: `organizations/{organization}/locations/{location}/customers/{customer}/workloads/{workload}`
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudcontrolspartner_organizations_locations_customers_workloads_violations_list_execute()` to send, or `cloudcontrolspartner_organizations_locations_customers_workloads_violations_list` for simplest API.

pub fn cloudcontrolspartner_organizations_locations_customers_workloads_violations_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    interval_endTime: Option<&str>,
    interval_startTime: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudcontrolspartner.googleapis.com/v1/organizations/{}/locations/{}/customers/{}/workloads/{}/violations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = interval_endTime {
        query_parts.push(format!("interval_endTime={}", val));
    }
    if let Some(val) = interval_startTime {
        query_parts.push(format!("interval_startTime={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}/workloads/{workloadsId}/violations
/// Lists Violations for a workload Callers may also choose to read across multiple Customers or for a single customer as per [AIP-159](<https://google.aip.`dev/159`>) by using '-' (the hyphen or dash character) as a wildcard character instead of {customer} & {workload}. Format: `organizations/{organization}/locations/{location}/customers/{customer}/workloads/{workload}`
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudcontrolspartner_organizations_locations_customers_workloads_violations_list_execute()` or `cloudcontrolspartner_organizations_locations_customers_workloads_violations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudcontrolspartner_organizations_locations_customers_workloads_violations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcontrolspartner_organizations_locations_customers_workloads_violations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListViolationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListViolationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}/workloads/{workloadsId}/violations
/// Lists Violations for a workload Callers may also choose to read across multiple Customers or for a single customer as per [AIP-159](<https://google.aip.`dev/159`>) by using '-' (the hyphen or dash character) as a wildcard character instead of {customer} & {workload}. Format: `organizations/{organization}/locations/{location}/customers/{customer}/workloads/{workload}`
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudcontrolspartner_organizations_locations_customers_workloads_violations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudcontrolspartner_organizations_locations_customers_workloads_violations_list_task()`.
/// For the simplest API, use `cloudcontrolspartner_organizations_locations_customers_workloads_violations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudcontrolspartner_organizations_locations_customers_workloads_violations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudcontrolspartner_organizations_locations_customers_workloads_violations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListViolationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        cloudcontrolspartner_organizations_locations_customers_workloads_violations_list_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudcontrolspartner_organizations_locations_customers_workloads_violations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudcontrolspartnerOrganizationsLocationsCustomersWorkloadsViolationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: interval_endTime
    pub interval_endTime: Option<String>,
    /// Query parameter: interval_startTime
    pub interval_startTime: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/customers/{customersId}/workloads/{workloadsId}/violations
/// Lists Violations for a workload Callers may also choose to read across multiple Customers or for a single customer as per [AIP-159](<https://google.aip.`dev/159`>) by using '-' (the hyphen or dash character) as a wildcard character instead of {customer} & {workload}. Format: `organizations/{organization}/locations/{location}/customers/{customer}/workloads/{workload}`
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudcontrolspartner_organizations_locations_customers_workloads_violations_list_builder()` + `cloudcontrolspartner_organizations_locations_customers_workloads_violations_list_execute()`.
/// For task-level control, use `cloudcontrolspartner_organizations_locations_customers_workloads_violations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcontrolspartner_organizations_locations_customers_workloads_violations_list(
    client: &SimpleHttpClient,
    args: &CloudcontrolspartnerOrganizationsLocationsCustomersWorkloadsViolationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListViolationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        cloudcontrolspartner_organizations_locations_customers_workloads_violations_list_builder(
            client,
            &args.parent,
            args.filter.as_deref(),
            args.interval_endTime.as_deref(),
            args.interval_startTime.as_deref(),
            args.orderBy.as_deref(),
            args.pageSize,
            args.pageToken.as_deref(),
        )?;
    cloudcontrolspartner_organizations_locations_customers_workloads_violations_list_execute(
        builder,
    )
}
