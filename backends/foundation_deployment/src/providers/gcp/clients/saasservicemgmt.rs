//! Auto-generated API clients for Google Cloud Platform - saasservicemgmt.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/{+name}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_get_execute()` to send, or `saasservicemgmt_projects_locations_get` for simplest API.

pub fn saasservicemgmt_projects_locations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudLocationLocation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudLocationLocation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_get_builder()` + `saasservicemgmt_projects_locations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudLocationLocation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_get_builder(client, name)?;
    saasservicemgmt_projects_locations_get_execute(builder)
}

/// GET v1/{+name}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_list_execute()` to send, or `saasservicemgmt_projects_locations_list` for simplest API.

pub fn saasservicemgmt_projects_locations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}/locations", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "extraLocationTypes",
            extraLocationTypes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_list_builder()` + `saasservicemgmt_projects_locations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_list(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_list_builder(
        client,
        name,
        extraLocationTypes,
        filter,
        pageSize,
        pageToken,
    )?;
    saasservicemgmt_projects_locations_list_execute(builder)
}

/// GET v1/{+parent}/releases
/// Create a new release.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_releases_create_execute()` to send, or `saasservicemgmt_projects_locations_releases_create` for simplest API.

pub fn saasservicemgmt_projects_locations_releases_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    releaseId: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
    body: &Release,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/releases", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "releaseId",
            releaseId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/releases
/// Create a new release.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_releases_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_releases_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_releases_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Release>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Release = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/releases
/// Create a new release.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_releases_create_builder()` + `saasservicemgmt_projects_locations_releases_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_releases_create(
    client: &SimpleHttpClient,
    parent: &str,
    releaseId: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
    body: &Release,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Release>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_releases_create_builder(
        client,
        parent,
        releaseId,
        requestId,
        validateOnly,
        body,
    )?;
    saasservicemgmt_projects_locations_releases_create_execute(builder)
}

/// GET v1/{+name}
/// Delete a single release.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_releases_delete_execute()` to send, or `saasservicemgmt_projects_locations_releases_delete` for simplest API.

pub fn saasservicemgmt_projects_locations_releases_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "etag",
            etag.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Delete a single release.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_releases_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_releases_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_releases_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Delete a single release.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_releases_delete_builder()` + `saasservicemgmt_projects_locations_releases_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_releases_delete(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_releases_delete_builder(
        client,
        name,
        etag,
        requestId,
        validateOnly,
    )?;
    saasservicemgmt_projects_locations_releases_delete_execute(builder)
}

/// GET v1/{+name}
/// Retrieve a single release.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_releases_get_execute()` to send, or `saasservicemgmt_projects_locations_releases_get` for simplest API.

pub fn saasservicemgmt_projects_locations_releases_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Retrieve a single release.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_releases_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_releases_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_releases_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Release>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Release = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Retrieve a single release.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_releases_get_builder()` + `saasservicemgmt_projects_locations_releases_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_releases_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Release>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_releases_get_builder(client, name)?;
    saasservicemgmt_projects_locations_releases_get_execute(builder)
}

/// GET v1/{+parent}/releases
/// Retrieve a collection of releases.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_releases_list_execute()` to send, or `saasservicemgmt_projects_locations_releases_list` for simplest API.

pub fn saasservicemgmt_projects_locations_releases_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/releases", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/releases
/// Retrieve a collection of releases.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_releases_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_releases_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_releases_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListReleasesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListReleasesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/releases
/// Retrieve a collection of releases.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_releases_list_builder()` + `saasservicemgmt_projects_locations_releases_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_releases_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListReleasesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_releases_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    saasservicemgmt_projects_locations_releases_list_execute(builder)
}

/// GET v1/{+name}
/// Update a single release.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_releases_patch_execute()` to send, or `saasservicemgmt_projects_locations_releases_patch` for simplest API.

pub fn saasservicemgmt_projects_locations_releases_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &Release,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Update a single release.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_releases_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_releases_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_releases_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Release>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Release = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Update a single release.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_releases_patch_builder()` + `saasservicemgmt_projects_locations_releases_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_releases_patch(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &Release,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Release>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_releases_patch_builder(
        client,
        name,
        requestId,
        updateMask,
        validateOnly,
        body,
    )?;
    saasservicemgmt_projects_locations_releases_patch_execute(builder)
}

/// GET v1/{+parent}/rolloutKinds
/// Create a new rollout kind.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_rollout_kinds_create_execute()` to send, or `saasservicemgmt_projects_locations_rollout_kinds_create` for simplest API.

pub fn saasservicemgmt_projects_locations_rollout_kinds_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    rolloutKindId: Option<&str>,
    validateOnly: Option<bool>,
    body: &RolloutKind,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/rolloutKinds", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "rolloutKindId",
            rolloutKindId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/rolloutKinds
/// Create a new rollout kind.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_rollout_kinds_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_rollout_kinds_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_rollout_kinds_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RolloutKind>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RolloutKind = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/rolloutKinds
/// Create a new rollout kind.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_rollout_kinds_create_builder()` + `saasservicemgmt_projects_locations_rollout_kinds_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_rollout_kinds_create(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    rolloutKindId: Option<&str>,
    validateOnly: Option<bool>,
    body: &RolloutKind,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RolloutKind>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_rollout_kinds_create_builder(
        client,
        parent,
        requestId,
        rolloutKindId,
        validateOnly,
        body,
    )?;
    saasservicemgmt_projects_locations_rollout_kinds_create_execute(builder)
}

/// GET v1/{+name}
/// Delete a single rollout kind.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_rollout_kinds_delete_execute()` to send, or `saasservicemgmt_projects_locations_rollout_kinds_delete` for simplest API.

pub fn saasservicemgmt_projects_locations_rollout_kinds_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "etag",
            etag.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Delete a single rollout kind.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_rollout_kinds_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_rollout_kinds_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_rollout_kinds_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Delete a single rollout kind.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_rollout_kinds_delete_builder()` + `saasservicemgmt_projects_locations_rollout_kinds_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_rollout_kinds_delete(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_rollout_kinds_delete_builder(
        client,
        name,
        etag,
        requestId,
        validateOnly,
    )?;
    saasservicemgmt_projects_locations_rollout_kinds_delete_execute(builder)
}

/// GET v1/{+name}
/// Retrieve a single rollout kind.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_rollout_kinds_get_execute()` to send, or `saasservicemgmt_projects_locations_rollout_kinds_get` for simplest API.

pub fn saasservicemgmt_projects_locations_rollout_kinds_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Retrieve a single rollout kind.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_rollout_kinds_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_rollout_kinds_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_rollout_kinds_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RolloutKind>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RolloutKind = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Retrieve a single rollout kind.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_rollout_kinds_get_builder()` + `saasservicemgmt_projects_locations_rollout_kinds_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_rollout_kinds_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RolloutKind>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_rollout_kinds_get_builder(client, name)?;
    saasservicemgmt_projects_locations_rollout_kinds_get_execute(builder)
}

/// GET v1/{+parent}/rolloutKinds
/// Retrieve a collection of rollout kinds.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_rollout_kinds_list_execute()` to send, or `saasservicemgmt_projects_locations_rollout_kinds_list` for simplest API.

pub fn saasservicemgmt_projects_locations_rollout_kinds_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/rolloutKinds", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/rolloutKinds
/// Retrieve a collection of rollout kinds.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_rollout_kinds_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_rollout_kinds_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_rollout_kinds_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRolloutKindsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListRolloutKindsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/rolloutKinds
/// Retrieve a collection of rollout kinds.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_rollout_kinds_list_builder()` + `saasservicemgmt_projects_locations_rollout_kinds_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_rollout_kinds_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRolloutKindsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_rollout_kinds_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    saasservicemgmt_projects_locations_rollout_kinds_list_execute(builder)
}

/// GET v1/{+name}
/// Update a single rollout kind.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_rollout_kinds_patch_execute()` to send, or `saasservicemgmt_projects_locations_rollout_kinds_patch` for simplest API.

pub fn saasservicemgmt_projects_locations_rollout_kinds_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &RolloutKind,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Update a single rollout kind.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_rollout_kinds_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_rollout_kinds_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_rollout_kinds_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RolloutKind>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RolloutKind = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Update a single rollout kind.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_rollout_kinds_patch_builder()` + `saasservicemgmt_projects_locations_rollout_kinds_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_rollout_kinds_patch(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &RolloutKind,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RolloutKind>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_rollout_kinds_patch_builder(
        client,
        name,
        requestId,
        updateMask,
        validateOnly,
        body,
    )?;
    saasservicemgmt_projects_locations_rollout_kinds_patch_execute(builder)
}

/// GET v1/{+parent}/rollouts
/// Create a new rollout.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_rollouts_create_execute()` to send, or `saasservicemgmt_projects_locations_rollouts_create` for simplest API.

pub fn saasservicemgmt_projects_locations_rollouts_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    rolloutId: Option<&str>,
    validateOnly: Option<bool>,
    body: &Rollout,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/rollouts", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "rolloutId",
            rolloutId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/rollouts
/// Create a new rollout.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_rollouts_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_rollouts_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_rollouts_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Rollout>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Rollout = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/rollouts
/// Create a new rollout.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_rollouts_create_builder()` + `saasservicemgmt_projects_locations_rollouts_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_rollouts_create(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    rolloutId: Option<&str>,
    validateOnly: Option<bool>,
    body: &Rollout,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Rollout>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_rollouts_create_builder(
        client,
        parent,
        requestId,
        rolloutId,
        validateOnly,
        body,
    )?;
    saasservicemgmt_projects_locations_rollouts_create_execute(builder)
}

/// GET v1/{+name}
/// Delete a single rollout.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_rollouts_delete_execute()` to send, or `saasservicemgmt_projects_locations_rollouts_delete` for simplest API.

pub fn saasservicemgmt_projects_locations_rollouts_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "etag",
            etag.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Delete a single rollout.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_rollouts_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_rollouts_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_rollouts_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Delete a single rollout.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_rollouts_delete_builder()` + `saasservicemgmt_projects_locations_rollouts_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_rollouts_delete(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_rollouts_delete_builder(
        client,
        name,
        etag,
        requestId,
        validateOnly,
    )?;
    saasservicemgmt_projects_locations_rollouts_delete_execute(builder)
}

/// GET v1/{+name}
/// Retrieve a single rollout.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_rollouts_get_execute()` to send, or `saasservicemgmt_projects_locations_rollouts_get` for simplest API.

pub fn saasservicemgmt_projects_locations_rollouts_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Retrieve a single rollout.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_rollouts_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_rollouts_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_rollouts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Rollout>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Rollout = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Retrieve a single rollout.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_rollouts_get_builder()` + `saasservicemgmt_projects_locations_rollouts_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_rollouts_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Rollout>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_rollouts_get_builder(client, name)?;
    saasservicemgmt_projects_locations_rollouts_get_execute(builder)
}

/// GET v1/{+parent}/rollouts
/// Retrieve a collection of rollouts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_rollouts_list_execute()` to send, or `saasservicemgmt_projects_locations_rollouts_list` for simplest API.

pub fn saasservicemgmt_projects_locations_rollouts_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/rollouts", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/rollouts
/// Retrieve a collection of rollouts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_rollouts_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_rollouts_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_rollouts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRolloutsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListRolloutsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/rollouts
/// Retrieve a collection of rollouts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_rollouts_list_builder()` + `saasservicemgmt_projects_locations_rollouts_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_rollouts_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRolloutsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_rollouts_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    saasservicemgmt_projects_locations_rollouts_list_execute(builder)
}

/// GET v1/{+name}
/// Update a single rollout.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_rollouts_patch_execute()` to send, or `saasservicemgmt_projects_locations_rollouts_patch` for simplest API.

pub fn saasservicemgmt_projects_locations_rollouts_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &Rollout,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Update a single rollout.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_rollouts_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_rollouts_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_rollouts_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Rollout>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Rollout = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Update a single rollout.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_rollouts_patch_builder()` + `saasservicemgmt_projects_locations_rollouts_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_rollouts_patch(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &Rollout,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Rollout>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_rollouts_patch_builder(
        client,
        name,
        requestId,
        updateMask,
        validateOnly,
        body,
    )?;
    saasservicemgmt_projects_locations_rollouts_patch_execute(builder)
}

/// GET v1/{+parent}/saas
/// Create a new saas.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_saas_create_execute()` to send, or `saasservicemgmt_projects_locations_saas_create` for simplest API.

pub fn saasservicemgmt_projects_locations_saas_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    saasId: Option<&str>,
    validateOnly: Option<bool>,
    body: &Saas,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/saas", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "saasId",
            saasId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/saas
/// Create a new saas.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_saas_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_saas_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_saas_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Saas>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Saas = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/saas
/// Create a new saas.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_saas_create_builder()` + `saasservicemgmt_projects_locations_saas_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_saas_create(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    saasId: Option<&str>,
    validateOnly: Option<bool>,
    body: &Saas,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Saas>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_saas_create_builder(
        client,
        parent,
        requestId,
        saasId,
        validateOnly,
        body,
    )?;
    saasservicemgmt_projects_locations_saas_create_execute(builder)
}

/// GET v1/{+name}
/// Delete a single saas.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_saas_delete_execute()` to send, or `saasservicemgmt_projects_locations_saas_delete` for simplest API.

pub fn saasservicemgmt_projects_locations_saas_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "etag",
            etag.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Delete a single saas.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_saas_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_saas_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_saas_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Delete a single saas.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_saas_delete_builder()` + `saasservicemgmt_projects_locations_saas_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_saas_delete(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_saas_delete_builder(
        client,
        name,
        etag,
        requestId,
        validateOnly,
    )?;
    saasservicemgmt_projects_locations_saas_delete_execute(builder)
}

/// GET v1/{+name}
/// Retrieve a single saas.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_saas_get_execute()` to send, or `saasservicemgmt_projects_locations_saas_get` for simplest API.

pub fn saasservicemgmt_projects_locations_saas_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Retrieve a single saas.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_saas_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_saas_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_saas_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Saas>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Saas = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Retrieve a single saas.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_saas_get_builder()` + `saasservicemgmt_projects_locations_saas_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_saas_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Saas>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_saas_get_builder(client, name)?;
    saasservicemgmt_projects_locations_saas_get_execute(builder)
}

/// GET v1/{+parent}/saas
/// Retrieve a collection of saas.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_saas_list_execute()` to send, or `saasservicemgmt_projects_locations_saas_list` for simplest API.

pub fn saasservicemgmt_projects_locations_saas_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/saas", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/saas
/// Retrieve a collection of saas.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_saas_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_saas_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_saas_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSaasResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSaasResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/saas
/// Retrieve a collection of saas.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_saas_list_builder()` + `saasservicemgmt_projects_locations_saas_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_saas_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSaasResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_saas_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    saasservicemgmt_projects_locations_saas_list_execute(builder)
}

/// GET v1/{+name}
/// Update a single saas.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_saas_patch_execute()` to send, or `saasservicemgmt_projects_locations_saas_patch` for simplest API.

pub fn saasservicemgmt_projects_locations_saas_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &Saas,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Update a single saas.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_saas_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_saas_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_saas_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Saas>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Saas = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Update a single saas.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_saas_patch_builder()` + `saasservicemgmt_projects_locations_saas_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_saas_patch(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &Saas,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Saas>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_saas_patch_builder(
        client,
        name,
        requestId,
        updateMask,
        validateOnly,
        body,
    )?;
    saasservicemgmt_projects_locations_saas_patch_execute(builder)
}

/// GET v1/{+parent}/tenants
/// Create a new tenant.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_tenants_create_execute()` to send, or `saasservicemgmt_projects_locations_tenants_create` for simplest API.

pub fn saasservicemgmt_projects_locations_tenants_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    tenantId: Option<&str>,
    validateOnly: Option<bool>,
    body: &Tenant,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/tenants", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "tenantId",
            tenantId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/tenants
/// Create a new tenant.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_tenants_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_tenants_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_tenants_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Tenant>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Tenant = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/tenants
/// Create a new tenant.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_tenants_create_builder()` + `saasservicemgmt_projects_locations_tenants_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_tenants_create(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    tenantId: Option<&str>,
    validateOnly: Option<bool>,
    body: &Tenant,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Tenant>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_tenants_create_builder(
        client,
        parent,
        requestId,
        tenantId,
        validateOnly,
        body,
    )?;
    saasservicemgmt_projects_locations_tenants_create_execute(builder)
}

/// GET v1/{+name}
/// Delete a single tenant.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_tenants_delete_execute()` to send, or `saasservicemgmt_projects_locations_tenants_delete` for simplest API.

pub fn saasservicemgmt_projects_locations_tenants_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "etag",
            etag.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Delete a single tenant.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_tenants_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_tenants_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_tenants_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Delete a single tenant.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_tenants_delete_builder()` + `saasservicemgmt_projects_locations_tenants_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_tenants_delete(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_tenants_delete_builder(
        client,
        name,
        etag,
        requestId,
        validateOnly,
    )?;
    saasservicemgmt_projects_locations_tenants_delete_execute(builder)
}

/// GET v1/{+name}
/// Retrieve a single tenant.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_tenants_get_execute()` to send, or `saasservicemgmt_projects_locations_tenants_get` for simplest API.

pub fn saasservicemgmt_projects_locations_tenants_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Retrieve a single tenant.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_tenants_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_tenants_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_tenants_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Tenant>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Tenant = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Retrieve a single tenant.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_tenants_get_builder()` + `saasservicemgmt_projects_locations_tenants_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_tenants_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Tenant>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_tenants_get_builder(client, name)?;
    saasservicemgmt_projects_locations_tenants_get_execute(builder)
}

/// GET v1/{+parent}/tenants
/// Retrieve a collection of tenants.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_tenants_list_execute()` to send, or `saasservicemgmt_projects_locations_tenants_list` for simplest API.

pub fn saasservicemgmt_projects_locations_tenants_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/tenants", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/tenants
/// Retrieve a collection of tenants.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_tenants_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_tenants_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_tenants_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTenantsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTenantsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/tenants
/// Retrieve a collection of tenants.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_tenants_list_builder()` + `saasservicemgmt_projects_locations_tenants_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_tenants_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTenantsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_tenants_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    saasservicemgmt_projects_locations_tenants_list_execute(builder)
}

/// GET v1/{+name}
/// Update a single tenant.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_tenants_patch_execute()` to send, or `saasservicemgmt_projects_locations_tenants_patch` for simplest API.

pub fn saasservicemgmt_projects_locations_tenants_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &Tenant,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Update a single tenant.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_tenants_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_tenants_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_tenants_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Tenant>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Tenant = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Update a single tenant.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_tenants_patch_builder()` + `saasservicemgmt_projects_locations_tenants_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_tenants_patch(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &Tenant,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Tenant>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_tenants_patch_builder(
        client,
        name,
        requestId,
        updateMask,
        validateOnly,
        body,
    )?;
    saasservicemgmt_projects_locations_tenants_patch_execute(builder)
}

/// GET v1/{+parent}/unitKinds
/// Create a new unit kind.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_unit_kinds_create_execute()` to send, or `saasservicemgmt_projects_locations_unit_kinds_create` for simplest API.

pub fn saasservicemgmt_projects_locations_unit_kinds_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    unitKindId: Option<&str>,
    validateOnly: Option<bool>,
    body: &UnitKind,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/unitKinds", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "unitKindId",
            unitKindId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/unitKinds
/// Create a new unit kind.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_unit_kinds_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_unit_kinds_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_unit_kinds_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UnitKind>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UnitKind = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/unitKinds
/// Create a new unit kind.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_unit_kinds_create_builder()` + `saasservicemgmt_projects_locations_unit_kinds_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_unit_kinds_create(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    unitKindId: Option<&str>,
    validateOnly: Option<bool>,
    body: &UnitKind,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UnitKind>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_unit_kinds_create_builder(
        client,
        parent,
        requestId,
        unitKindId,
        validateOnly,
        body,
    )?;
    saasservicemgmt_projects_locations_unit_kinds_create_execute(builder)
}

/// GET v1/{+name}
/// Delete a single unit kind.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_unit_kinds_delete_execute()` to send, or `saasservicemgmt_projects_locations_unit_kinds_delete` for simplest API.

pub fn saasservicemgmt_projects_locations_unit_kinds_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "etag",
            etag.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Delete a single unit kind.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_unit_kinds_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_unit_kinds_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_unit_kinds_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Delete a single unit kind.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_unit_kinds_delete_builder()` + `saasservicemgmt_projects_locations_unit_kinds_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_unit_kinds_delete(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_unit_kinds_delete_builder(
        client,
        name,
        etag,
        requestId,
        validateOnly,
    )?;
    saasservicemgmt_projects_locations_unit_kinds_delete_execute(builder)
}

/// GET v1/{+name}
/// Retrieve a single unit kind.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_unit_kinds_get_execute()` to send, or `saasservicemgmt_projects_locations_unit_kinds_get` for simplest API.

pub fn saasservicemgmt_projects_locations_unit_kinds_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Retrieve a single unit kind.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_unit_kinds_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_unit_kinds_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_unit_kinds_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UnitKind>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UnitKind = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Retrieve a single unit kind.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_unit_kinds_get_builder()` + `saasservicemgmt_projects_locations_unit_kinds_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_unit_kinds_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UnitKind>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_unit_kinds_get_builder(client, name)?;
    saasservicemgmt_projects_locations_unit_kinds_get_execute(builder)
}

/// GET v1/{+parent}/unitKinds
/// Retrieve a collection of unit kinds.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_unit_kinds_list_execute()` to send, or `saasservicemgmt_projects_locations_unit_kinds_list` for simplest API.

pub fn saasservicemgmt_projects_locations_unit_kinds_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/unitKinds", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/unitKinds
/// Retrieve a collection of unit kinds.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_unit_kinds_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_unit_kinds_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_unit_kinds_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListUnitKindsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListUnitKindsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/unitKinds
/// Retrieve a collection of unit kinds.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_unit_kinds_list_builder()` + `saasservicemgmt_projects_locations_unit_kinds_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_unit_kinds_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListUnitKindsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_unit_kinds_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    saasservicemgmt_projects_locations_unit_kinds_list_execute(builder)
}

/// GET v1/{+name}
/// Update a single unit kind.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_unit_kinds_patch_execute()` to send, or `saasservicemgmt_projects_locations_unit_kinds_patch` for simplest API.

pub fn saasservicemgmt_projects_locations_unit_kinds_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &UnitKind,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Update a single unit kind.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_unit_kinds_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_unit_kinds_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_unit_kinds_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UnitKind>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UnitKind = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Update a single unit kind.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_unit_kinds_patch_builder()` + `saasservicemgmt_projects_locations_unit_kinds_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_unit_kinds_patch(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &UnitKind,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UnitKind>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_unit_kinds_patch_builder(
        client,
        name,
        requestId,
        updateMask,
        validateOnly,
        body,
    )?;
    saasservicemgmt_projects_locations_unit_kinds_patch_execute(builder)
}

/// GET v1/{+parent}/unitOperations
/// Create a new unit operation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_unit_operations_create_execute()` to send, or `saasservicemgmt_projects_locations_unit_operations_create` for simplest API.

pub fn saasservicemgmt_projects_locations_unit_operations_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    unitOperationId: Option<&str>,
    validateOnly: Option<bool>,
    body: &UnitOperation,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/unitOperations", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "unitOperationId",
            unitOperationId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/unitOperations
/// Create a new unit operation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_unit_operations_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_unit_operations_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_unit_operations_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UnitOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UnitOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/unitOperations
/// Create a new unit operation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_unit_operations_create_builder()` + `saasservicemgmt_projects_locations_unit_operations_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_unit_operations_create(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    unitOperationId: Option<&str>,
    validateOnly: Option<bool>,
    body: &UnitOperation,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UnitOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_unit_operations_create_builder(
        client,
        parent,
        requestId,
        unitOperationId,
        validateOnly,
        body,
    )?;
    saasservicemgmt_projects_locations_unit_operations_create_execute(builder)
}

/// GET v1/{+name}
/// Delete a single unit operation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_unit_operations_delete_execute()` to send, or `saasservicemgmt_projects_locations_unit_operations_delete` for simplest API.

pub fn saasservicemgmt_projects_locations_unit_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "etag",
            etag.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Delete a single unit operation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_unit_operations_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_unit_operations_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_unit_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Delete a single unit operation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_unit_operations_delete_builder()` + `saasservicemgmt_projects_locations_unit_operations_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_unit_operations_delete(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_unit_operations_delete_builder(
        client,
        name,
        etag,
        requestId,
        validateOnly,
    )?;
    saasservicemgmt_projects_locations_unit_operations_delete_execute(builder)
}

/// GET v1/{+name}
/// Retrieve a single unit operation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_unit_operations_get_execute()` to send, or `saasservicemgmt_projects_locations_unit_operations_get` for simplest API.

pub fn saasservicemgmt_projects_locations_unit_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Retrieve a single unit operation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_unit_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_unit_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_unit_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UnitOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UnitOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Retrieve a single unit operation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_unit_operations_get_builder()` + `saasservicemgmt_projects_locations_unit_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_unit_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UnitOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_unit_operations_get_builder(client, name)?;
    saasservicemgmt_projects_locations_unit_operations_get_execute(builder)
}

/// GET v1/{+parent}/unitOperations
/// Retrieve a collection of unit operations.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_unit_operations_list_execute()` to send, or `saasservicemgmt_projects_locations_unit_operations_list` for simplest API.

pub fn saasservicemgmt_projects_locations_unit_operations_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/unitOperations", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/unitOperations
/// Retrieve a collection of unit operations.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_unit_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_unit_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_unit_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUnitOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListUnitOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/unitOperations
/// Retrieve a collection of unit operations.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_unit_operations_list_builder()` + `saasservicemgmt_projects_locations_unit_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_unit_operations_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListUnitOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_unit_operations_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    saasservicemgmt_projects_locations_unit_operations_list_execute(builder)
}

/// GET v1/{+name}
/// Update a single unit operation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_unit_operations_patch_execute()` to send, or `saasservicemgmt_projects_locations_unit_operations_patch` for simplest API.

pub fn saasservicemgmt_projects_locations_unit_operations_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &UnitOperation,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Update a single unit operation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_unit_operations_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_unit_operations_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_unit_operations_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UnitOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UnitOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Update a single unit operation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_unit_operations_patch_builder()` + `saasservicemgmt_projects_locations_unit_operations_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_unit_operations_patch(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &UnitOperation,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UnitOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_unit_operations_patch_builder(
        client,
        name,
        requestId,
        updateMask,
        validateOnly,
        body,
    )?;
    saasservicemgmt_projects_locations_unit_operations_patch_execute(builder)
}

/// GET v1/{+parent}/units
/// Create a new unit.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_units_create_execute()` to send, or `saasservicemgmt_projects_locations_units_create` for simplest API.

pub fn saasservicemgmt_projects_locations_units_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    unitId: Option<&str>,
    validateOnly: Option<bool>,
    body: &Unit,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/units", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "unitId",
            unitId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+parent}/units
/// Create a new unit.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_units_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_units_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_units_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Unit>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Unit = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/units
/// Create a new unit.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_units_create_builder()` + `saasservicemgmt_projects_locations_units_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_units_create(
    client: &SimpleHttpClient,
    parent: &str,
    requestId: Option<&str>,
    unitId: Option<&str>,
    validateOnly: Option<bool>,
    body: &Unit,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Unit>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_units_create_builder(
        client,
        parent,
        requestId,
        unitId,
        validateOnly,
        body,
    )?;
    saasservicemgmt_projects_locations_units_create_execute(builder)
}

/// GET v1/{+name}
/// Delete a single unit.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_units_delete_execute()` to send, or `saasservicemgmt_projects_locations_units_delete` for simplest API.

pub fn saasservicemgmt_projects_locations_units_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "etag",
            etag.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Delete a single unit.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_units_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_units_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_units_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Delete a single unit.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_units_delete_builder()` + `saasservicemgmt_projects_locations_units_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_units_delete(
    client: &SimpleHttpClient,
    name: &str,
    etag: Option<&str>,
    requestId: Option<&str>,
    validateOnly: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_units_delete_builder(
        client,
        name,
        etag,
        requestId,
        validateOnly,
    )?;
    saasservicemgmt_projects_locations_units_delete_execute(builder)
}

/// GET v1/{+name}
/// Retrieve a single unit.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_units_get_execute()` to send, or `saasservicemgmt_projects_locations_units_get` for simplest API.

pub fn saasservicemgmt_projects_locations_units_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Retrieve a single unit.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_units_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_units_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_units_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Unit>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Unit = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Retrieve a single unit.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_units_get_builder()` + `saasservicemgmt_projects_locations_units_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_units_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Unit>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_units_get_builder(client, name)?;
    saasservicemgmt_projects_locations_units_get_execute(builder)
}

/// GET v1/{+parent}/units
/// Retrieve a collection of units.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_units_list_execute()` to send, or `saasservicemgmt_projects_locations_units_list` for simplest API.

pub fn saasservicemgmt_projects_locations_units_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/units", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "orderBy",
            orderBy
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/units
/// Retrieve a collection of units.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_units_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_units_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_units_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListUnitsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListUnitsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/units
/// Retrieve a collection of units.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_units_list_builder()` + `saasservicemgmt_projects_locations_units_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_units_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListUnitsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_units_list_builder(
        client, parent, filter, orderBy, pageSize, pageToken,
    )?;
    saasservicemgmt_projects_locations_units_list_execute(builder)
}

/// GET v1/{+name}
/// Update a single unit.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `saasservicemgmt_projects_locations_units_patch_execute()` to send, or `saasservicemgmt_projects_locations_units_patch` for simplest API.

pub fn saasservicemgmt_projects_locations_units_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &Unit,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "requestId",
            requestId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "validateOnly",
            validateOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Update a single unit.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `saasservicemgmt_projects_locations_units_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from saasservicemgmt_projects_locations_units_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_units_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Unit>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Unit = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Update a single unit.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `saasservicemgmt_projects_locations_units_patch_builder()` + `saasservicemgmt_projects_locations_units_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn saasservicemgmt_projects_locations_units_patch(
    client: &SimpleHttpClient,
    name: &str,
    requestId: Option<&str>,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &Unit,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Unit>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = saasservicemgmt_projects_locations_units_patch_builder(
        client,
        name,
        requestId,
        updateMask,
        validateOnly,
        body,
    )?;
    saasservicemgmt_projects_locations_units_patch_execute(builder)
}
