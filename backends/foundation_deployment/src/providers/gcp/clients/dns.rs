//! Auto-generated API clients for gcp/dns.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/dns`

#![cfg(feature = "gcp/dns")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/changes
/// Atomically updates the ResourceRecordSet collection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_changes_create_execute()` to send, or `dns_changes_create` for simplest API.

pub fn dns_changes_create_builder(
    client: &SimpleHttpClient,
    project: &str,
    managedZone: &str,
    clientOperationId: Option<&str>,
    body: &Change,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/managedZones/{}/changes",
        project, managedZone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clientOperationId {
        query_parts.push(format!("clientOperationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/changes
/// Atomically updates the ResourceRecordSet collection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_changes_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_changes_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_changes_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Change>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Change = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_changes_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsChangesCreateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: managedZone
    pub managedZone: String,
    /// Query parameter: clientOperationId
    pub clientOperationId: Option<String>,
    /// Request body.
    pub body: Change,
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/changes
/// Atomically updates the ResourceRecordSet collection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_changes_create_builder()` + `dns_changes_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_changes_create(
    client: &SimpleHttpClient,
    args: &DnsChangesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Change>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dns_changes_create_builder(
        client,
        &args.project,
        &args.managedZone,
        args.clientOperationId.as_deref(),
        &args.body,
    )?;
    dns_changes_create_execute(builder)
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/changes/{changeId}
/// Fetches the representation of an existing Change.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_changes_get_execute()` to send, or `dns_changes_get` for simplest API.

pub fn dns_changes_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    managedZone: &str,
    changeId: &str,
    clientOperationId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/managedZones/{}/changes/{}",
        project, managedZone, changeId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clientOperationId {
        query_parts.push(format!("clientOperationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/changes/{changeId}
/// Fetches the representation of an existing Change.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_changes_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_changes_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_changes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Change>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Change = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_changes_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsChangesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: managedZone
    pub managedZone: String,
    /// Path parameter: changeId
    pub changeId: String,
    /// Query parameter: clientOperationId
    pub clientOperationId: Option<String>,
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/changes/{changeId}
/// Fetches the representation of an existing Change.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_changes_get_builder()` + `dns_changes_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_changes_get(
    client: &SimpleHttpClient,
    args: &DnsChangesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Change>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dns_changes_get_builder(
        client,
        &args.project,
        &args.managedZone,
        &args.changeId,
        args.clientOperationId.as_deref(),
    )?;
    dns_changes_get_execute(builder)
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/changes
/// Enumerates Changes to a ResourceRecordSet collection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_changes_list_execute()` to send, or `dns_changes_list` for simplest API.

pub fn dns_changes_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    managedZone: &str,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    sortBy: Option<&str>,
    sortOrder: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/managedZones/{}/changes",
        project, managedZone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = sortBy {
        query_parts.push(format!("sortBy={}", val));
    }
    if let Some(val) = sortOrder {
        query_parts.push(format!("sortOrder={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/changes
/// Enumerates Changes to a ResourceRecordSet collection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_changes_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_changes_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_changes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ChangesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ChangesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_changes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsChangesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: managedZone
    pub managedZone: String,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: sortBy
    pub sortBy: Option<String>,
    /// Query parameter: sortOrder
    pub sortOrder: Option<String>,
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/changes
/// Enumerates Changes to a ResourceRecordSet collection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_changes_list_builder()` + `dns_changes_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_changes_list(
    client: &SimpleHttpClient,
    args: &DnsChangesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ChangesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dns_changes_list_builder(
        client,
        &args.project,
        &args.managedZone,
        args.maxResults,
        args.pageToken.as_deref(),
        args.sortBy.as_deref(),
        args.sortOrder.as_deref(),
    )?;
    dns_changes_list_execute(builder)
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/dnsKeys/{dnsKeyId}
/// Fetches the representation of an existing DnsKey.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_dns_keys_get_execute()` to send, or `dns_dns_keys_get` for simplest API.

pub fn dns_dns_keys_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    managedZone: &str,
    dnsKeyId: &str,
    clientOperationId: Option<&str>,
    digestType: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/managedZones/{}/dnsKeys/{}",
        project, managedZone, dnsKeyId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clientOperationId {
        query_parts.push(format!("clientOperationId={}", val));
    }
    if let Some(val) = digestType {
        query_parts.push(format!("digestType={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/dnsKeys/{dnsKeyId}
/// Fetches the representation of an existing DnsKey.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_dns_keys_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_dns_keys_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_dns_keys_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DnsKey>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DnsKey = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_dns_keys_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsDnsKeysGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: managedZone
    pub managedZone: String,
    /// Path parameter: dnsKeyId
    pub dnsKeyId: String,
    /// Query parameter: clientOperationId
    pub clientOperationId: Option<String>,
    /// Query parameter: digestType
    pub digestType: Option<String>,
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/dnsKeys/{dnsKeyId}
/// Fetches the representation of an existing DnsKey.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_dns_keys_get_builder()` + `dns_dns_keys_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_dns_keys_get(
    client: &SimpleHttpClient,
    args: &DnsDnsKeysGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DnsKey>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dns_dns_keys_get_builder(
        client,
        &args.project,
        &args.managedZone,
        &args.dnsKeyId,
        args.clientOperationId.as_deref(),
        args.digestType.as_deref(),
    )?;
    dns_dns_keys_get_execute(builder)
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/dnsKeys
/// Enumerates DnsKeys to a ResourceRecordSet collection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_dns_keys_list_execute()` to send, or `dns_dns_keys_list` for simplest API.

pub fn dns_dns_keys_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    managedZone: &str,
    digestType: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/managedZones/{}/dnsKeys",
        project, managedZone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = digestType {
        query_parts.push(format!("digestType={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/dnsKeys
/// Enumerates DnsKeys to a ResourceRecordSet collection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_dns_keys_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_dns_keys_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_dns_keys_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DnsKeysListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DnsKeysListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_dns_keys_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsDnsKeysListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: managedZone
    pub managedZone: String,
    /// Query parameter: digestType
    pub digestType: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/dnsKeys
/// Enumerates DnsKeys to a ResourceRecordSet collection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_dns_keys_list_builder()` + `dns_dns_keys_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_dns_keys_list(
    client: &SimpleHttpClient,
    args: &DnsDnsKeysListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DnsKeysListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dns_dns_keys_list_builder(
        client,
        &args.project,
        &args.managedZone,
        args.digestType.as_deref(),
        args.maxResults,
        args.pageToken.as_deref(),
    )?;
    dns_dns_keys_list_execute(builder)
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/operations/{operation}
/// Fetches the representation of an existing Operation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_managed_zone_operations_get_execute()` to send, or `dns_managed_zone_operations_get` for simplest API.

pub fn dns_managed_zone_operations_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    managedZone: &str,
    operation: &str,
    clientOperationId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/managedZones/{}/operations/{}",
        project, managedZone, operation,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clientOperationId {
        query_parts.push(format!("clientOperationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/operations/{operation}
/// Fetches the representation of an existing Operation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_managed_zone_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_managed_zone_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_managed_zone_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_managed_zone_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsManagedZoneOperationsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: managedZone
    pub managedZone: String,
    /// Path parameter: operation
    pub operation: String,
    /// Query parameter: clientOperationId
    pub clientOperationId: Option<String>,
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/operations/{operation}
/// Fetches the representation of an existing Operation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_managed_zone_operations_get_builder()` + `dns_managed_zone_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_managed_zone_operations_get(
    client: &SimpleHttpClient,
    args: &DnsManagedZoneOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dns_managed_zone_operations_get_builder(
        client,
        &args.project,
        &args.managedZone,
        &args.operation,
        args.clientOperationId.as_deref(),
    )?;
    dns_managed_zone_operations_get_execute(builder)
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/operations
/// Enumerates Operations for the given ManagedZone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_managed_zone_operations_list_execute()` to send, or `dns_managed_zone_operations_list` for simplest API.

pub fn dns_managed_zone_operations_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    managedZone: &str,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
    sortBy: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/managedZones/{}/operations",
        project, managedZone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = sortBy {
        query_parts.push(format!("sortBy={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/operations
/// Enumerates Operations for the given ManagedZone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_managed_zone_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_managed_zone_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_managed_zone_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ManagedZoneOperationsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ManagedZoneOperationsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_managed_zone_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsManagedZoneOperationsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: managedZone
    pub managedZone: String,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: sortBy
    pub sortBy: Option<String>,
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/operations
/// Enumerates Operations for the given ManagedZone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_managed_zone_operations_list_builder()` + `dns_managed_zone_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_managed_zone_operations_list(
    client: &SimpleHttpClient,
    args: &DnsManagedZoneOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ManagedZoneOperationsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dns_managed_zone_operations_list_builder(
        client,
        &args.project,
        &args.managedZone,
        args.maxResults,
        args.pageToken.as_deref(),
        args.sortBy.as_deref(),
    )?;
    dns_managed_zone_operations_list_execute(builder)
}

/// GET dns/v1/projects/{project}/managedZones
/// Creates a new ManagedZone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_managed_zones_create_execute()` to send, or `dns_managed_zones_create` for simplest API.

pub fn dns_managed_zones_create_builder(
    client: &SimpleHttpClient,
    project: &str,
    clientOperationId: Option<&str>,
    body: &ManagedZone,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/managedZones",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clientOperationId {
        query_parts.push(format!("clientOperationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET dns/v1/projects/{project}/managedZones
/// Creates a new ManagedZone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_managed_zones_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_managed_zones_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_managed_zones_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ManagedZone>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ManagedZone = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_managed_zones_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsManagedZonesCreateArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: clientOperationId
    pub clientOperationId: Option<String>,
    /// Request body.
    pub body: ManagedZone,
}

/// GET dns/v1/projects/{project}/managedZones
/// Creates a new ManagedZone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_managed_zones_create_builder()` + `dns_managed_zones_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_managed_zones_create(
    client: &SimpleHttpClient,
    args: &DnsManagedZonesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ManagedZone>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dns_managed_zones_create_builder(
        client,
        &args.project,
        args.clientOperationId.as_deref(),
        &args.body,
    )?;
    dns_managed_zones_create_execute(builder)
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}
/// Deletes a previously created ManagedZone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_managed_zones_delete_execute()` to send, or `dns_managed_zones_delete` for simplest API.

pub fn dns_managed_zones_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    managedZone: &str,
    clientOperationId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/managedZones/{}",
        project, managedZone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clientOperationId {
        query_parts.push(format!("clientOperationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}
/// Deletes a previously created ManagedZone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_managed_zones_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_managed_zones_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_managed_zones_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_managed_zones_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsManagedZonesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: managedZone
    pub managedZone: String,
    /// Query parameter: clientOperationId
    pub clientOperationId: Option<String>,
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}
/// Deletes a previously created ManagedZone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_managed_zones_delete_builder()` + `dns_managed_zones_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_managed_zones_delete(
    client: &SimpleHttpClient,
    args: &DnsManagedZonesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dns_managed_zones_delete_builder(
        client,
        &args.project,
        &args.managedZone,
        args.clientOperationId.as_deref(),
    )?;
    dns_managed_zones_delete_execute(builder)
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}
/// Fetches the representation of an existing ManagedZone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_managed_zones_get_execute()` to send, or `dns_managed_zones_get` for simplest API.

pub fn dns_managed_zones_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    managedZone: &str,
    clientOperationId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/managedZones/{}",
        project, managedZone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clientOperationId {
        query_parts.push(format!("clientOperationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}
/// Fetches the representation of an existing ManagedZone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_managed_zones_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_managed_zones_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_managed_zones_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ManagedZone>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ManagedZone = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_managed_zones_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsManagedZonesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: managedZone
    pub managedZone: String,
    /// Query parameter: clientOperationId
    pub clientOperationId: Option<String>,
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}
/// Fetches the representation of an existing ManagedZone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_managed_zones_get_builder()` + `dns_managed_zones_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_managed_zones_get(
    client: &SimpleHttpClient,
    args: &DnsManagedZonesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ManagedZone>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dns_managed_zones_get_builder(
        client,
        &args.project,
        &args.managedZone,
        args.clientOperationId.as_deref(),
    )?;
    dns_managed_zones_get_execute(builder)
}

/// GET dns/v1/projects/{projectsId}/managedZones/{managedZonesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_managed_zones_get_iam_policy_execute()` to send, or `dns_managed_zones_get_iam_policy` for simplest API.

pub fn dns_managed_zones_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleIamV1GetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/managedZones/{}:getIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET dns/v1/projects/{projectsId}/managedZones/{managedZonesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_managed_zones_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_managed_zones_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_managed_zones_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_managed_zones_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsManagedZonesGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GoogleIamV1GetIamPolicyRequest,
}

/// GET dns/v1/projects/{projectsId}/managedZones/{managedZonesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_managed_zones_get_iam_policy_builder()` + `dns_managed_zones_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_managed_zones_get_iam_policy(
    client: &SimpleHttpClient,
    args: &DnsManagedZonesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dns_managed_zones_get_iam_policy_builder(client, &args.resource, &args.body)?;
    dns_managed_zones_get_iam_policy_execute(builder)
}

/// GET dns/v1/projects/{project}/managedZones
/// Enumerates ManagedZones that have been created but not yet deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_managed_zones_list_execute()` to send, or `dns_managed_zones_list` for simplest API.

pub fn dns_managed_zones_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    dnsName: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/managedZones",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = dnsName {
        query_parts.push(format!("dnsName={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET dns/v1/projects/{project}/managedZones
/// Enumerates ManagedZones that have been created but not yet deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_managed_zones_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_managed_zones_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_managed_zones_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ManagedZonesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ManagedZonesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_managed_zones_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsManagedZonesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: dnsName
    pub dnsName: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET dns/v1/projects/{project}/managedZones
/// Enumerates ManagedZones that have been created but not yet deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_managed_zones_list_builder()` + `dns_managed_zones_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_managed_zones_list(
    client: &SimpleHttpClient,
    args: &DnsManagedZonesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ManagedZonesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dns_managed_zones_list_builder(
        client,
        &args.project,
        args.dnsName.as_deref(),
        args.maxResults,
        args.pageToken.as_deref(),
    )?;
    dns_managed_zones_list_execute(builder)
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}
/// Applies a partial update to an existing ManagedZone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_managed_zones_patch_execute()` to send, or `dns_managed_zones_patch` for simplest API.

pub fn dns_managed_zones_patch_builder(
    client: &SimpleHttpClient,
    project: &str,
    managedZone: &str,
    clientOperationId: Option<&str>,
    body: &ManagedZone,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/managedZones/{}",
        project, managedZone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clientOperationId {
        query_parts.push(format!("clientOperationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}
/// Applies a partial update to an existing ManagedZone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_managed_zones_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_managed_zones_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_managed_zones_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_managed_zones_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsManagedZonesPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: managedZone
    pub managedZone: String,
    /// Query parameter: clientOperationId
    pub clientOperationId: Option<String>,
    /// Request body.
    pub body: ManagedZone,
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}
/// Applies a partial update to an existing ManagedZone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_managed_zones_patch_builder()` + `dns_managed_zones_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_managed_zones_patch(
    client: &SimpleHttpClient,
    args: &DnsManagedZonesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dns_managed_zones_patch_builder(
        client,
        &args.project,
        &args.managedZone,
        args.clientOperationId.as_deref(),
        &args.body,
    )?;
    dns_managed_zones_patch_execute(builder)
}

/// GET dns/v1/projects/{projectsId}/managedZones/{managedZonesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_managed_zones_set_iam_policy_execute()` to send, or `dns_managed_zones_set_iam_policy` for simplest API.

pub fn dns_managed_zones_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleIamV1SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/managedZones/{}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET dns/v1/projects/{projectsId}/managedZones/{managedZonesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_managed_zones_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_managed_zones_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_managed_zones_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_managed_zones_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsManagedZonesSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GoogleIamV1SetIamPolicyRequest,
}

/// GET dns/v1/projects/{projectsId}/managedZones/{managedZonesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_managed_zones_set_iam_policy_builder()` + `dns_managed_zones_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_managed_zones_set_iam_policy(
    client: &SimpleHttpClient,
    args: &DnsManagedZonesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleIamV1Policy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dns_managed_zones_set_iam_policy_builder(client, &args.resource, &args.body)?;
    dns_managed_zones_set_iam_policy_execute(builder)
}

/// GET dns/v1/projects/{projectsId}/managedZones/{managedZonesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this returns an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_managed_zones_test_iam_permissions_execute()` to send, or `dns_managed_zones_test_iam_permissions` for simplest API.

pub fn dns_managed_zones_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GoogleIamV1TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/managedZones/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET dns/v1/projects/{projectsId}/managedZones/{managedZonesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this returns an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_managed_zones_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_managed_zones_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_managed_zones_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleIamV1TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_managed_zones_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsManagedZonesTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GoogleIamV1TestIamPermissionsRequest,
}

/// GET dns/v1/projects/{projectsId}/managedZones/{managedZonesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this returns an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_managed_zones_test_iam_permissions_builder()` + `dns_managed_zones_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_managed_zones_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &DnsManagedZonesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleIamV1TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        dns_managed_zones_test_iam_permissions_builder(client, &args.resource, &args.body)?;
    dns_managed_zones_test_iam_permissions_execute(builder)
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}
/// Updates an existing ManagedZone.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_managed_zones_update_execute()` to send, or `dns_managed_zones_update` for simplest API.

pub fn dns_managed_zones_update_builder(
    client: &SimpleHttpClient,
    project: &str,
    managedZone: &str,
    clientOperationId: Option<&str>,
    body: &ManagedZone,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/managedZones/{}",
        project, managedZone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clientOperationId {
        query_parts.push(format!("clientOperationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}
/// Updates an existing ManagedZone.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_managed_zones_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_managed_zones_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_managed_zones_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_managed_zones_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsManagedZonesUpdateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: managedZone
    pub managedZone: String,
    /// Query parameter: clientOperationId
    pub clientOperationId: Option<String>,
    /// Request body.
    pub body: ManagedZone,
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}
/// Updates an existing ManagedZone.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_managed_zones_update_builder()` + `dns_managed_zones_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_managed_zones_update(
    client: &SimpleHttpClient,
    args: &DnsManagedZonesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dns_managed_zones_update_builder(
        client,
        &args.project,
        &args.managedZone,
        args.clientOperationId.as_deref(),
        &args.body,
    )?;
    dns_managed_zones_update_execute(builder)
}

/// GET dns/v1/projects/{project}/policies
/// Creates a new policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_policies_create_execute()` to send, or `dns_policies_create` for simplest API.

pub fn dns_policies_create_builder(
    client: &SimpleHttpClient,
    project: &str,
    clientOperationId: Option<&str>,
    body: &Policy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/policies",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clientOperationId {
        query_parts.push(format!("clientOperationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET dns/v1/projects/{project}/policies
/// Creates a new policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_policies_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_policies_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_policies_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_policies_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsPoliciesCreateArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: clientOperationId
    pub clientOperationId: Option<String>,
    /// Request body.
    pub body: Policy,
}

/// GET dns/v1/projects/{project}/policies
/// Creates a new policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_policies_create_builder()` + `dns_policies_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_policies_create(
    client: &SimpleHttpClient,
    args: &DnsPoliciesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dns_policies_create_builder(
        client,
        &args.project,
        args.clientOperationId.as_deref(),
        &args.body,
    )?;
    dns_policies_create_execute(builder)
}

/// GET dns/v1/projects/{project}/policies/{policy}
/// Deletes a previously created policy. Fails if the policy is still being referenced by a network.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_policies_delete_execute()` to send, or `dns_policies_delete` for simplest API.

pub fn dns_policies_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    policy: &str,
    clientOperationId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/policies/{}",
        project, policy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clientOperationId {
        query_parts.push(format!("clientOperationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET dns/v1/projects/{project}/policies/{policy}
/// Deletes a previously created policy. Fails if the policy is still being referenced by a network.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_policies_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_policies_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_policies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsPoliciesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: policy
    pub policy: String,
    /// Query parameter: clientOperationId
    pub clientOperationId: Option<String>,
}

/// GET dns/v1/projects/{project}/policies/{policy}
/// Deletes a previously created policy. Fails if the policy is still being referenced by a network.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_policies_delete_builder()` + `dns_policies_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_policies_delete(
    client: &SimpleHttpClient,
    args: &DnsPoliciesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dns_policies_delete_builder(
        client,
        &args.project,
        &args.policy,
        args.clientOperationId.as_deref(),
    )?;
    dns_policies_delete_execute(builder)
}

/// GET dns/v1/projects/{project}/policies/{policy}
/// Fetches the representation of an existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_policies_get_execute()` to send, or `dns_policies_get` for simplest API.

pub fn dns_policies_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    policy: &str,
    clientOperationId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/policies/{}",
        project, policy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clientOperationId {
        query_parts.push(format!("clientOperationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET dns/v1/projects/{project}/policies/{policy}
/// Fetches the representation of an existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_policies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_policies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_policies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsPoliciesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: policy
    pub policy: String,
    /// Query parameter: clientOperationId
    pub clientOperationId: Option<String>,
}

/// GET dns/v1/projects/{project}/policies/{policy}
/// Fetches the representation of an existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_policies_get_builder()` + `dns_policies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_policies_get(
    client: &SimpleHttpClient,
    args: &DnsPoliciesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dns_policies_get_builder(
        client,
        &args.project,
        &args.policy,
        args.clientOperationId.as_deref(),
    )?;
    dns_policies_get_execute(builder)
}

/// GET dns/v1/projects/{project}/policies
/// Enumerates all policies associated with a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_policies_list_execute()` to send, or `dns_policies_list` for simplest API.

pub fn dns_policies_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/policies",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET dns/v1/projects/{project}/policies
/// Enumerates all policies associated with a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_policies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_policies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PoliciesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PoliciesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_policies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsPoliciesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET dns/v1/projects/{project}/policies
/// Enumerates all policies associated with a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_policies_list_builder()` + `dns_policies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_policies_list(
    client: &SimpleHttpClient,
    args: &DnsPoliciesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PoliciesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dns_policies_list_builder(
        client,
        &args.project,
        args.maxResults,
        args.pageToken.as_deref(),
    )?;
    dns_policies_list_execute(builder)
}

/// GET dns/v1/projects/{project}/policies/{policy}
/// Applies a partial update to an existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_policies_patch_execute()` to send, or `dns_policies_patch` for simplest API.

pub fn dns_policies_patch_builder(
    client: &SimpleHttpClient,
    project: &str,
    policy: &str,
    clientOperationId: Option<&str>,
    body: &Policy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/policies/{}",
        project, policy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clientOperationId {
        query_parts.push(format!("clientOperationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET dns/v1/projects/{project}/policies/{policy}
/// Applies a partial update to an existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_policies_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_policies_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PoliciesPatchResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PoliciesPatchResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_policies_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsPoliciesPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: policy
    pub policy: String,
    /// Query parameter: clientOperationId
    pub clientOperationId: Option<String>,
    /// Request body.
    pub body: Policy,
}

/// GET dns/v1/projects/{project}/policies/{policy}
/// Applies a partial update to an existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_policies_patch_builder()` + `dns_policies_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_policies_patch(
    client: &SimpleHttpClient,
    args: &DnsPoliciesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PoliciesPatchResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dns_policies_patch_builder(
        client,
        &args.project,
        &args.policy,
        args.clientOperationId.as_deref(),
        &args.body,
    )?;
    dns_policies_patch_execute(builder)
}

/// GET dns/v1/projects/{project}/policies/{policy}
/// Updates an existing policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_policies_update_execute()` to send, or `dns_policies_update` for simplest API.

pub fn dns_policies_update_builder(
    client: &SimpleHttpClient,
    project: &str,
    policy: &str,
    clientOperationId: Option<&str>,
    body: &Policy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/policies/{}",
        project, policy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clientOperationId {
        query_parts.push(format!("clientOperationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET dns/v1/projects/{project}/policies/{policy}
/// Updates an existing policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_policies_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_policies_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_policies_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PoliciesUpdateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PoliciesUpdateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_policies_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsPoliciesUpdateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: policy
    pub policy: String,
    /// Query parameter: clientOperationId
    pub clientOperationId: Option<String>,
    /// Request body.
    pub body: Policy,
}

/// GET dns/v1/projects/{project}/policies/{policy}
/// Updates an existing policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_policies_update_builder()` + `dns_policies_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_policies_update(
    client: &SimpleHttpClient,
    args: &DnsPoliciesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PoliciesUpdateResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dns_policies_update_builder(
        client,
        &args.project,
        &args.policy,
        args.clientOperationId.as_deref(),
        &args.body,
    )?;
    dns_policies_update_execute(builder)
}

/// GET dns/v1/projects/{project}
/// Fetches the representation of an existing Project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_projects_get_execute()` to send, or `dns_projects_get` for simplest API.

pub fn dns_projects_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    clientOperationId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://dns.googleapis.com/dns/v1/projects/{}", project,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clientOperationId {
        query_parts.push(format!("clientOperationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET dns/v1/projects/{project}
/// Fetches the representation of an existing Project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_projects_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_projects_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_projects_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Project>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Project = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_projects_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsProjectsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: clientOperationId
    pub clientOperationId: Option<String>,
}

/// GET dns/v1/projects/{project}
/// Fetches the representation of an existing Project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_projects_get_builder()` + `dns_projects_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_projects_get(
    client: &SimpleHttpClient,
    args: &DnsProjectsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Project>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        dns_projects_get_builder(client, &args.project, args.clientOperationId.as_deref())?;
    dns_projects_get_execute(builder)
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/rrsets
/// Creates a new ResourceRecordSet.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_resource_record_sets_create_execute()` to send, or `dns_resource_record_sets_create` for simplest API.

pub fn dns_resource_record_sets_create_builder(
    client: &SimpleHttpClient,
    project: &str,
    managedZone: &str,
    clientOperationId: Option<&str>,
    body: &ResourceRecordSet,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/managedZones/{}/rrsets",
        project, managedZone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clientOperationId {
        query_parts.push(format!("clientOperationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/rrsets
/// Creates a new ResourceRecordSet.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_resource_record_sets_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_resource_record_sets_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_resource_record_sets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResourceRecordSet>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ResourceRecordSet = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_resource_record_sets_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsResourceRecordSetsCreateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: managedZone
    pub managedZone: String,
    /// Query parameter: clientOperationId
    pub clientOperationId: Option<String>,
    /// Request body.
    pub body: ResourceRecordSet,
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/rrsets
/// Creates a new ResourceRecordSet.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_resource_record_sets_create_builder()` + `dns_resource_record_sets_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_resource_record_sets_create(
    client: &SimpleHttpClient,
    args: &DnsResourceRecordSetsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResourceRecordSet>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dns_resource_record_sets_create_builder(
        client,
        &args.project,
        &args.managedZone,
        args.clientOperationId.as_deref(),
        &args.body,
    )?;
    dns_resource_record_sets_create_execute(builder)
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/rrsets/{name}/{type}
/// Deletes a previously created ResourceRecordSet.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_resource_record_sets_delete_execute()` to send, or `dns_resource_record_sets_delete` for simplest API.

pub fn dns_resource_record_sets_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    managedZone: &str,
    name: &str,
    type_rs: &str,
    clientOperationId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/managedZones/{}/rrsets/{}/{}",
        project, managedZone, name, type_rs,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clientOperationId {
        query_parts.push(format!("clientOperationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/rrsets/{name}/{type}
/// Deletes a previously created ResourceRecordSet.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_resource_record_sets_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_resource_record_sets_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_resource_record_sets_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ResourceRecordSetsDeleteResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ResourceRecordSetsDeleteResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_resource_record_sets_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsResourceRecordSetsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: managedZone
    pub managedZone: String,
    /// Path parameter: name
    pub name: String,
    /// Path parameter: type
    pub type_rs: String,
    /// Query parameter: clientOperationId
    pub clientOperationId: Option<String>,
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/rrsets/{name}/{type}
/// Deletes a previously created ResourceRecordSet.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_resource_record_sets_delete_builder()` + `dns_resource_record_sets_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_resource_record_sets_delete(
    client: &SimpleHttpClient,
    args: &DnsResourceRecordSetsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ResourceRecordSetsDeleteResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dns_resource_record_sets_delete_builder(
        client,
        &args.project,
        &args.managedZone,
        &args.name,
        &args.type_rs,
        args.clientOperationId.as_deref(),
    )?;
    dns_resource_record_sets_delete_execute(builder)
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/rrsets/{name}/{type}
/// Fetches the representation of an existing ResourceRecordSet.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_resource_record_sets_get_execute()` to send, or `dns_resource_record_sets_get` for simplest API.

pub fn dns_resource_record_sets_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    managedZone: &str,
    name: &str,
    type_rs: &str,
    clientOperationId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/managedZones/{}/rrsets/{}/{}",
        project, managedZone, name, type_rs,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clientOperationId {
        query_parts.push(format!("clientOperationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/rrsets/{name}/{type}
/// Fetches the representation of an existing ResourceRecordSet.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_resource_record_sets_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_resource_record_sets_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_resource_record_sets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResourceRecordSet>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ResourceRecordSet = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_resource_record_sets_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsResourceRecordSetsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: managedZone
    pub managedZone: String,
    /// Path parameter: name
    pub name: String,
    /// Path parameter: type
    pub type_rs: String,
    /// Query parameter: clientOperationId
    pub clientOperationId: Option<String>,
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/rrsets/{name}/{type}
/// Fetches the representation of an existing ResourceRecordSet.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_resource_record_sets_get_builder()` + `dns_resource_record_sets_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_resource_record_sets_get(
    client: &SimpleHttpClient,
    args: &DnsResourceRecordSetsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResourceRecordSet>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dns_resource_record_sets_get_builder(
        client,
        &args.project,
        &args.managedZone,
        &args.name,
        &args.type_rs,
        args.clientOperationId.as_deref(),
    )?;
    dns_resource_record_sets_get_execute(builder)
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/rrsets
/// Enumerates ResourceRecordSets that you have created but not yet deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_resource_record_sets_list_execute()` to send, or `dns_resource_record_sets_list` for simplest API.

pub fn dns_resource_record_sets_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    managedZone: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    name: Option<&str>,
    pageToken: Option<&str>,
    type_rs: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/managedZones/{}/rrsets",
        project, managedZone,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = name {
        query_parts.push(format!("name={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = type_rs {
        query_parts.push(format!("type={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/rrsets
/// Enumerates ResourceRecordSets that you have created but not yet deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_resource_record_sets_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_resource_record_sets_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_resource_record_sets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ResourceRecordSetsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ResourceRecordSetsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_resource_record_sets_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsResourceRecordSetsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: managedZone
    pub managedZone: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: name
    pub name: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: type
    pub type_rs: Option<String>,
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/rrsets
/// Enumerates ResourceRecordSets that you have created but not yet deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_resource_record_sets_list_builder()` + `dns_resource_record_sets_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_resource_record_sets_list(
    client: &SimpleHttpClient,
    args: &DnsResourceRecordSetsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ResourceRecordSetsListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dns_resource_record_sets_list_builder(
        client,
        &args.project,
        &args.managedZone,
        args.filter.as_deref(),
        args.maxResults,
        args.name.as_deref(),
        args.pageToken.as_deref(),
        args.type_rs.as_deref(),
    )?;
    dns_resource_record_sets_list_execute(builder)
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/rrsets/{name}/{type}
/// Applies a partial update to an existing ResourceRecordSet.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_resource_record_sets_patch_execute()` to send, or `dns_resource_record_sets_patch` for simplest API.

pub fn dns_resource_record_sets_patch_builder(
    client: &SimpleHttpClient,
    project: &str,
    managedZone: &str,
    name: &str,
    type_rs: &str,
    clientOperationId: Option<&str>,
    body: &ResourceRecordSet,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/managedZones/{}/rrsets/{}/{}",
        project, managedZone, name, type_rs,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clientOperationId {
        query_parts.push(format!("clientOperationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/rrsets/{name}/{type}
/// Applies a partial update to an existing ResourceRecordSet.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_resource_record_sets_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_resource_record_sets_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_resource_record_sets_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResourceRecordSet>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ResourceRecordSet = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_resource_record_sets_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsResourceRecordSetsPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: managedZone
    pub managedZone: String,
    /// Path parameter: name
    pub name: String,
    /// Path parameter: type
    pub type_rs: String,
    /// Query parameter: clientOperationId
    pub clientOperationId: Option<String>,
    /// Request body.
    pub body: ResourceRecordSet,
}

/// GET dns/v1/projects/{project}/managedZones/{managedZone}/rrsets/{name}/{type}
/// Applies a partial update to an existing ResourceRecordSet.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_resource_record_sets_patch_builder()` + `dns_resource_record_sets_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_resource_record_sets_patch(
    client: &SimpleHttpClient,
    args: &DnsResourceRecordSetsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResourceRecordSet>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dns_resource_record_sets_patch_builder(
        client,
        &args.project,
        &args.managedZone,
        &args.name,
        &args.type_rs,
        args.clientOperationId.as_deref(),
        &args.body,
    )?;
    dns_resource_record_sets_patch_execute(builder)
}

/// GET dns/v1/projects/{project}/responsePolicies
/// Creates a new Response Policy
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_response_policies_create_execute()` to send, or `dns_response_policies_create` for simplest API.

pub fn dns_response_policies_create_builder(
    client: &SimpleHttpClient,
    project: &str,
    clientOperationId: Option<&str>,
    body: &ResponsePolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/responsePolicies",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clientOperationId {
        query_parts.push(format!("clientOperationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET dns/v1/projects/{project}/responsePolicies
/// Creates a new Response Policy
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_response_policies_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_response_policies_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_response_policies_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResponsePolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ResponsePolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_response_policies_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsResponsePoliciesCreateArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: clientOperationId
    pub clientOperationId: Option<String>,
    /// Request body.
    pub body: ResponsePolicy,
}

/// GET dns/v1/projects/{project}/responsePolicies
/// Creates a new Response Policy
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_response_policies_create_builder()` + `dns_response_policies_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_response_policies_create(
    client: &SimpleHttpClient,
    args: &DnsResponsePoliciesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResponsePolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dns_response_policies_create_builder(
        client,
        &args.project,
        args.clientOperationId.as_deref(),
        &args.body,
    )?;
    dns_response_policies_create_execute(builder)
}

/// GET dns/v1/projects/{project}/responsePolicies/{responsePolicy}
/// Deletes a previously created Response Policy. Fails if the response policy is non-empty or still being referenced by a network.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_response_policies_delete_execute()` to send, or `dns_response_policies_delete` for simplest API.

pub fn dns_response_policies_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    responsePolicy: &str,
    clientOperationId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/responsePolicies/{}",
        project, responsePolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clientOperationId {
        query_parts.push(format!("clientOperationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET dns/v1/projects/{project}/responsePolicies/{responsePolicy}
/// Deletes a previously created Response Policy. Fails if the response policy is non-empty or still being referenced by a network.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_response_policies_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_response_policies_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_response_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_response_policies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsResponsePoliciesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: responsePolicy
    pub responsePolicy: String,
    /// Query parameter: clientOperationId
    pub clientOperationId: Option<String>,
}

/// GET dns/v1/projects/{project}/responsePolicies/{responsePolicy}
/// Deletes a previously created Response Policy. Fails if the response policy is non-empty or still being referenced by a network.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_response_policies_delete_builder()` + `dns_response_policies_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_response_policies_delete(
    client: &SimpleHttpClient,
    args: &DnsResponsePoliciesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dns_response_policies_delete_builder(
        client,
        &args.project,
        &args.responsePolicy,
        args.clientOperationId.as_deref(),
    )?;
    dns_response_policies_delete_execute(builder)
}

/// GET dns/v1/projects/{project}/responsePolicies/{responsePolicy}
/// Fetches the representation of an existing Response Policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_response_policies_get_execute()` to send, or `dns_response_policies_get` for simplest API.

pub fn dns_response_policies_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    responsePolicy: &str,
    clientOperationId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/responsePolicies/{}",
        project, responsePolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clientOperationId {
        query_parts.push(format!("clientOperationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET dns/v1/projects/{project}/responsePolicies/{responsePolicy}
/// Fetches the representation of an existing Response Policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_response_policies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_response_policies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_response_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResponsePolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ResponsePolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_response_policies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsResponsePoliciesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: responsePolicy
    pub responsePolicy: String,
    /// Query parameter: clientOperationId
    pub clientOperationId: Option<String>,
}

/// GET dns/v1/projects/{project}/responsePolicies/{responsePolicy}
/// Fetches the representation of an existing Response Policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_response_policies_get_builder()` + `dns_response_policies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_response_policies_get(
    client: &SimpleHttpClient,
    args: &DnsResponsePoliciesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResponsePolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dns_response_policies_get_builder(
        client,
        &args.project,
        &args.responsePolicy,
        args.clientOperationId.as_deref(),
    )?;
    dns_response_policies_get_execute(builder)
}

/// GET dns/v1/projects/{project}/responsePolicies
/// Enumerates all Response Policies associated with a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_response_policies_list_execute()` to send, or `dns_response_policies_list` for simplest API.

pub fn dns_response_policies_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/responsePolicies",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET dns/v1/projects/{project}/responsePolicies
/// Enumerates all Response Policies associated with a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_response_policies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_response_policies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_response_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ResponsePoliciesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ResponsePoliciesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_response_policies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsResponsePoliciesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET dns/v1/projects/{project}/responsePolicies
/// Enumerates all Response Policies associated with a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_response_policies_list_builder()` + `dns_response_policies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_response_policies_list(
    client: &SimpleHttpClient,
    args: &DnsResponsePoliciesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ResponsePoliciesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dns_response_policies_list_builder(
        client,
        &args.project,
        args.maxResults,
        args.pageToken.as_deref(),
    )?;
    dns_response_policies_list_execute(builder)
}

/// GET dns/v1/projects/{project}/responsePolicies/{responsePolicy}
/// Applies a partial update to an existing Response Policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_response_policies_patch_execute()` to send, or `dns_response_policies_patch` for simplest API.

pub fn dns_response_policies_patch_builder(
    client: &SimpleHttpClient,
    project: &str,
    responsePolicy: &str,
    clientOperationId: Option<&str>,
    body: &ResponsePolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/responsePolicies/{}",
        project, responsePolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clientOperationId {
        query_parts.push(format!("clientOperationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET dns/v1/projects/{project}/responsePolicies/{responsePolicy}
/// Applies a partial update to an existing Response Policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_response_policies_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_response_policies_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_response_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ResponsePoliciesPatchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ResponsePoliciesPatchResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_response_policies_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsResponsePoliciesPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: responsePolicy
    pub responsePolicy: String,
    /// Query parameter: clientOperationId
    pub clientOperationId: Option<String>,
    /// Request body.
    pub body: ResponsePolicy,
}

/// GET dns/v1/projects/{project}/responsePolicies/{responsePolicy}
/// Applies a partial update to an existing Response Policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_response_policies_patch_builder()` + `dns_response_policies_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_response_policies_patch(
    client: &SimpleHttpClient,
    args: &DnsResponsePoliciesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ResponsePoliciesPatchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dns_response_policies_patch_builder(
        client,
        &args.project,
        &args.responsePolicy,
        args.clientOperationId.as_deref(),
        &args.body,
    )?;
    dns_response_policies_patch_execute(builder)
}

/// GET dns/v1/projects/{project}/responsePolicies/{responsePolicy}
/// Updates an existing Response Policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_response_policies_update_execute()` to send, or `dns_response_policies_update` for simplest API.

pub fn dns_response_policies_update_builder(
    client: &SimpleHttpClient,
    project: &str,
    responsePolicy: &str,
    clientOperationId: Option<&str>,
    body: &ResponsePolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/responsePolicies/{}",
        project, responsePolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clientOperationId {
        query_parts.push(format!("clientOperationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET dns/v1/projects/{project}/responsePolicies/{responsePolicy}
/// Updates an existing Response Policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_response_policies_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_response_policies_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_response_policies_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ResponsePoliciesUpdateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ResponsePoliciesUpdateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_response_policies_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsResponsePoliciesUpdateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: responsePolicy
    pub responsePolicy: String,
    /// Query parameter: clientOperationId
    pub clientOperationId: Option<String>,
    /// Request body.
    pub body: ResponsePolicy,
}

/// GET dns/v1/projects/{project}/responsePolicies/{responsePolicy}
/// Updates an existing Response Policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_response_policies_update_builder()` + `dns_response_policies_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_response_policies_update(
    client: &SimpleHttpClient,
    args: &DnsResponsePoliciesUpdateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ResponsePoliciesUpdateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dns_response_policies_update_builder(
        client,
        &args.project,
        &args.responsePolicy,
        args.clientOperationId.as_deref(),
        &args.body,
    )?;
    dns_response_policies_update_execute(builder)
}

/// GET dns/v1/projects/{project}/responsePolicies/{responsePolicy}/rules
/// Creates a new Response Policy Rule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_response_policy_rules_create_execute()` to send, or `dns_response_policy_rules_create` for simplest API.

pub fn dns_response_policy_rules_create_builder(
    client: &SimpleHttpClient,
    project: &str,
    responsePolicy: &str,
    clientOperationId: Option<&str>,
    body: &ResponsePolicyRule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/responsePolicies/{}/rules",
        project, responsePolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clientOperationId {
        query_parts.push(format!("clientOperationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET dns/v1/projects/{project}/responsePolicies/{responsePolicy}/rules
/// Creates a new Response Policy Rule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_response_policy_rules_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_response_policy_rules_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_response_policy_rules_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResponsePolicyRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ResponsePolicyRule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_response_policy_rules_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsResponsePolicyRulesCreateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: responsePolicy
    pub responsePolicy: String,
    /// Query parameter: clientOperationId
    pub clientOperationId: Option<String>,
    /// Request body.
    pub body: ResponsePolicyRule,
}

/// GET dns/v1/projects/{project}/responsePolicies/{responsePolicy}/rules
/// Creates a new Response Policy Rule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_response_policy_rules_create_builder()` + `dns_response_policy_rules_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_response_policy_rules_create(
    client: &SimpleHttpClient,
    args: &DnsResponsePolicyRulesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResponsePolicyRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dns_response_policy_rules_create_builder(
        client,
        &args.project,
        &args.responsePolicy,
        args.clientOperationId.as_deref(),
        &args.body,
    )?;
    dns_response_policy_rules_create_execute(builder)
}

/// GET dns/v1/projects/{project}/responsePolicies/{responsePolicy}/rules/{responsePolicyRule}
/// Deletes a previously created Response Policy Rule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_response_policy_rules_delete_execute()` to send, or `dns_response_policy_rules_delete` for simplest API.

pub fn dns_response_policy_rules_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    responsePolicy: &str,
    responsePolicyRule: &str,
    clientOperationId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/responsePolicies/{}/rules/{}",
        project, responsePolicy, responsePolicyRule,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clientOperationId {
        query_parts.push(format!("clientOperationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET dns/v1/projects/{project}/responsePolicies/{responsePolicy}/rules/{responsePolicyRule}
/// Deletes a previously created Response Policy Rule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_response_policy_rules_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_response_policy_rules_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_response_policy_rules_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_response_policy_rules_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsResponsePolicyRulesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: responsePolicy
    pub responsePolicy: String,
    /// Path parameter: responsePolicyRule
    pub responsePolicyRule: String,
    /// Query parameter: clientOperationId
    pub clientOperationId: Option<String>,
}

/// GET dns/v1/projects/{project}/responsePolicies/{responsePolicy}/rules/{responsePolicyRule}
/// Deletes a previously created Response Policy Rule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_response_policy_rules_delete_builder()` + `dns_response_policy_rules_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_response_policy_rules_delete(
    client: &SimpleHttpClient,
    args: &DnsResponsePolicyRulesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = dns_response_policy_rules_delete_builder(
        client,
        &args.project,
        &args.responsePolicy,
        &args.responsePolicyRule,
        args.clientOperationId.as_deref(),
    )?;
    dns_response_policy_rules_delete_execute(builder)
}

/// GET dns/v1/projects/{project}/responsePolicies/{responsePolicy}/rules/{responsePolicyRule}
/// Fetches the representation of an existing Response Policy Rule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_response_policy_rules_get_execute()` to send, or `dns_response_policy_rules_get` for simplest API.

pub fn dns_response_policy_rules_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    responsePolicy: &str,
    responsePolicyRule: &str,
    clientOperationId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/responsePolicies/{}/rules/{}",
        project, responsePolicy, responsePolicyRule,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clientOperationId {
        query_parts.push(format!("clientOperationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET dns/v1/projects/{project}/responsePolicies/{responsePolicy}/rules/{responsePolicyRule}
/// Fetches the representation of an existing Response Policy Rule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_response_policy_rules_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_response_policy_rules_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_response_policy_rules_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResponsePolicyRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ResponsePolicyRule = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_response_policy_rules_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsResponsePolicyRulesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: responsePolicy
    pub responsePolicy: String,
    /// Path parameter: responsePolicyRule
    pub responsePolicyRule: String,
    /// Query parameter: clientOperationId
    pub clientOperationId: Option<String>,
}

/// GET dns/v1/projects/{project}/responsePolicies/{responsePolicy}/rules/{responsePolicyRule}
/// Fetches the representation of an existing Response Policy Rule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_response_policy_rules_get_builder()` + `dns_response_policy_rules_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_response_policy_rules_get(
    client: &SimpleHttpClient,
    args: &DnsResponsePolicyRulesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ResponsePolicyRule>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = dns_response_policy_rules_get_builder(
        client,
        &args.project,
        &args.responsePolicy,
        &args.responsePolicyRule,
        args.clientOperationId.as_deref(),
    )?;
    dns_response_policy_rules_get_execute(builder)
}

/// GET dns/v1/projects/{project}/responsePolicies/{responsePolicy}/rules
/// Enumerates all Response Policy Rules associated with a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_response_policy_rules_list_execute()` to send, or `dns_response_policy_rules_list` for simplest API.

pub fn dns_response_policy_rules_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    responsePolicy: &str,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/responsePolicies/{}/rules",
        project, responsePolicy,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET dns/v1/projects/{project}/responsePolicies/{responsePolicy}/rules
/// Enumerates all Response Policy Rules associated with a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_response_policy_rules_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_response_policy_rules_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_response_policy_rules_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ResponsePolicyRulesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ResponsePolicyRulesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_response_policy_rules_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsResponsePolicyRulesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: responsePolicy
    pub responsePolicy: String,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET dns/v1/projects/{project}/responsePolicies/{responsePolicy}/rules
/// Enumerates all Response Policy Rules associated with a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_response_policy_rules_list_builder()` + `dns_response_policy_rules_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_response_policy_rules_list(
    client: &SimpleHttpClient,
    args: &DnsResponsePolicyRulesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ResponsePolicyRulesListResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dns_response_policy_rules_list_builder(
        client,
        &args.project,
        &args.responsePolicy,
        args.maxResults,
        args.pageToken.as_deref(),
    )?;
    dns_response_policy_rules_list_execute(builder)
}

/// GET dns/v1/projects/{project}/responsePolicies/{responsePolicy}/rules/{responsePolicyRule}
/// Applies a partial update to an existing Response Policy Rule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_response_policy_rules_patch_execute()` to send, or `dns_response_policy_rules_patch` for simplest API.

pub fn dns_response_policy_rules_patch_builder(
    client: &SimpleHttpClient,
    project: &str,
    responsePolicy: &str,
    responsePolicyRule: &str,
    clientOperationId: Option<&str>,
    body: &ResponsePolicyRule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/responsePolicies/{}/rules/{}",
        project, responsePolicy, responsePolicyRule,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clientOperationId {
        query_parts.push(format!("clientOperationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET dns/v1/projects/{project}/responsePolicies/{responsePolicy}/rules/{responsePolicyRule}
/// Applies a partial update to an existing Response Policy Rule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_response_policy_rules_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_response_policy_rules_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_response_policy_rules_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ResponsePolicyRulesPatchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ResponsePolicyRulesPatchResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_response_policy_rules_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsResponsePolicyRulesPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: responsePolicy
    pub responsePolicy: String,
    /// Path parameter: responsePolicyRule
    pub responsePolicyRule: String,
    /// Query parameter: clientOperationId
    pub clientOperationId: Option<String>,
    /// Request body.
    pub body: ResponsePolicyRule,
}

/// GET dns/v1/projects/{project}/responsePolicies/{responsePolicy}/rules/{responsePolicyRule}
/// Applies a partial update to an existing Response Policy Rule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_response_policy_rules_patch_builder()` + `dns_response_policy_rules_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_response_policy_rules_patch(
    client: &SimpleHttpClient,
    args: &DnsResponsePolicyRulesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ResponsePolicyRulesPatchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dns_response_policy_rules_patch_builder(
        client,
        &args.project,
        &args.responsePolicy,
        &args.responsePolicyRule,
        args.clientOperationId.as_deref(),
        &args.body,
    )?;
    dns_response_policy_rules_patch_execute(builder)
}

/// GET dns/v1/projects/{project}/responsePolicies/{responsePolicy}/rules/{responsePolicyRule}
/// Updates an existing Response Policy Rule.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `dns_response_policy_rules_update_execute()` to send, or `dns_response_policy_rules_update` for simplest API.

pub fn dns_response_policy_rules_update_builder(
    client: &SimpleHttpClient,
    project: &str,
    responsePolicy: &str,
    responsePolicyRule: &str,
    clientOperationId: Option<&str>,
    body: &ResponsePolicyRule,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://dns.googleapis.com/dns/v1/projects/{}/responsePolicies/{}/rules/{}",
        project, responsePolicy, responsePolicyRule,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = clientOperationId {
        query_parts.push(format!("clientOperationId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET dns/v1/projects/{project}/responsePolicies/{responsePolicy}/rules/{responsePolicyRule}
/// Updates an existing Response Policy Rule.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `dns_response_policy_rules_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from dns_response_policy_rules_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_response_policy_rules_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ResponsePolicyRulesUpdateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ResponsePolicyRulesUpdateResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`dns_response_policy_rules_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DnsResponsePolicyRulesUpdateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: responsePolicy
    pub responsePolicy: String,
    /// Path parameter: responsePolicyRule
    pub responsePolicyRule: String,
    /// Query parameter: clientOperationId
    pub clientOperationId: Option<String>,
    /// Request body.
    pub body: ResponsePolicyRule,
}

/// GET dns/v1/projects/{project}/responsePolicies/{responsePolicy}/rules/{responsePolicyRule}
/// Updates an existing Response Policy Rule.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `dns_response_policy_rules_update_builder()` + `dns_response_policy_rules_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn dns_response_policy_rules_update(
    client: &SimpleHttpClient,
    args: &DnsResponsePolicyRulesUpdateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ResponsePolicyRulesUpdateResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = dns_response_policy_rules_update_builder(
        client,
        &args.project,
        &args.responsePolicy,
        &args.responsePolicyRule,
        args.clientOperationId.as_deref(),
        &args.body,
    )?;
    dns_response_policy_rules_update_execute(builder)
}
