//! Auto-generated API clients for gcp/webrisk.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, StreamIterator, StreamIteratorExt, TaskIterator, TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/hashes:search
/// Gets the full hashes that match the requested hash prefix. This is used after a hash prefix is looked up in a `threatList` and there is a match. The client side `threatList` only holds partial hashes so the client must query this method to determine if there is a full hash match of a threat.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `webrisk_hashes_search_execute()` to send, or `webrisk_hashes_search` for simplest API.

pub fn webrisk_hashes_search_builder(
    client: &SimpleHttpClient,
    hashPrefix: Option<&str>,
    threatTypes: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://webrisk.googleapis.com/v1/hashes:search",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = hashPrefix {
        query_parts.push(format!("hashPrefix={}", val));
    }
    if let Some(val) = threatTypes {
        query_parts.push(format!("threatTypes={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/hashes:search
/// Gets the full hashes that match the requested hash prefix. This is used after a hash prefix is looked up in a `threatList` and there is a match. The client side `threatList` only holds partial hashes so the client must query this method to determine if there is a full hash match of a threat.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `webrisk_hashes_search_execute()` or `webrisk_hashes_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `webrisk_hashes_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webrisk_hashes_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudWebriskV1SearchHashesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudWebriskV1SearchHashesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/hashes:search
/// Gets the full hashes that match the requested hash prefix. This is used after a hash prefix is looked up in a `threatList` and there is a match. The client side `threatList` only holds partial hashes so the client must query this method to determine if there is a full hash match of a threat.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `webrisk_hashes_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `webrisk_hashes_search_task()`.
/// For the simplest API, use `webrisk_hashes_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `webrisk_hashes_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn webrisk_hashes_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudWebriskV1SearchHashesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = webrisk_hashes_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`webrisk_hashes_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WebriskHashesSearchArgs {
    /// Query parameter: hashPrefix
    pub hashPrefix: Option<String>,
    /// Query parameter: threatTypes
    pub threatTypes: Option<String>,
}

/// GET v1/hashes:search
/// Gets the full hashes that match the requested hash prefix. This is used after a hash prefix is looked up in a `threatList` and there is a match. The client side `threatList` only holds partial hashes so the client must query this method to determine if there is a full hash match of a threat.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `webrisk_hashes_search_builder()` + `webrisk_hashes_search_execute()`.
/// For task-level control, use `webrisk_hashes_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webrisk_hashes_search(
    client: &SimpleHttpClient,
    args: &WebriskHashesSearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudWebriskV1SearchHashesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = webrisk_hashes_search_builder(
        client,
        args.hashPrefix.as_deref(),
        args.threatTypes.as_deref(),
    )?;
    webrisk_hashes_search_execute(builder)
}

/// GET v1/projects/{projectsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `webrisk_projects_operations_cancel_execute()` to send, or `webrisk_projects_operations_cancel` for simplest API.

pub fn webrisk_projects_operations_cancel_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleLongrunningCancelOperationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://webrisk.googleapis.com/v1/projects/{}/operations/{}:cancel",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `webrisk_projects_operations_cancel_execute()` or `webrisk_projects_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `webrisk_projects_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webrisk_projects_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `webrisk_projects_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `webrisk_projects_operations_cancel_task()`.
/// For the simplest API, use `webrisk_projects_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `webrisk_projects_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn webrisk_projects_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = webrisk_projects_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`webrisk_projects_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WebriskProjectsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: GoogleLongrunningCancelOperationRequest,
}

/// GET v1/projects/{projectsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `webrisk_projects_operations_cancel_builder()` + `webrisk_projects_operations_cancel_execute()`.
/// For task-level control, use `webrisk_projects_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webrisk_projects_operations_cancel(
    client: &SimpleHttpClient,
    args: &WebriskProjectsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = webrisk_projects_operations_cancel_builder(client, &args.name, &args.body)?;
    webrisk_projects_operations_cancel_execute(builder)
}

/// GET v1/projects/{projectsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `webrisk_projects_operations_delete_execute()` to send, or `webrisk_projects_operations_delete` for simplest API.

pub fn webrisk_projects_operations_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://webrisk.googleapis.com/v1/projects/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `webrisk_projects_operations_delete_execute()` or `webrisk_projects_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `webrisk_projects_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webrisk_projects_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `webrisk_projects_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `webrisk_projects_operations_delete_task()`.
/// For the simplest API, use `webrisk_projects_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `webrisk_projects_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn webrisk_projects_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = webrisk_projects_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`webrisk_projects_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WebriskProjectsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn't support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `webrisk_projects_operations_delete_builder()` + `webrisk_projects_operations_delete_execute()`.
/// For task-level control, use `webrisk_projects_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webrisk_projects_operations_delete(
    client: &SimpleHttpClient,
    args: &WebriskProjectsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = webrisk_projects_operations_delete_builder(client, &args.name)?;
    webrisk_projects_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `webrisk_projects_operations_get_execute()` to send, or `webrisk_projects_operations_get` for simplest API.

pub fn webrisk_projects_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://webrisk.googleapis.com/v1/projects/{}/operations/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `webrisk_projects_operations_get_execute()` or `webrisk_projects_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `webrisk_projects_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webrisk_projects_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `webrisk_projects_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `webrisk_projects_operations_get_task()`.
/// For the simplest API, use `webrisk_projects_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `webrisk_projects_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn webrisk_projects_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = webrisk_projects_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`webrisk_projects_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WebriskProjectsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `webrisk_projects_operations_get_builder()` + `webrisk_projects_operations_get_execute()`.
/// For task-level control, use `webrisk_projects_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webrisk_projects_operations_get(
    client: &SimpleHttpClient,
    args: &WebriskProjectsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = webrisk_projects_operations_get_builder(client, &args.name)?;
    webrisk_projects_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `webrisk_projects_operations_list_execute()` to send, or `webrisk_projects_operations_list` for simplest API.

pub fn webrisk_projects_operations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://webrisk.googleapis.com/v1/projects/{}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `webrisk_projects_operations_list_execute()` or `webrisk_projects_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `webrisk_projects_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webrisk_projects_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `webrisk_projects_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `webrisk_projects_operations_list_task()`.
/// For the simplest API, use `webrisk_projects_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `webrisk_projects_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn webrisk_projects_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = webrisk_projects_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`webrisk_projects_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WebriskProjectsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1/projects/{projectsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `webrisk_projects_operations_list_builder()` + `webrisk_projects_operations_list_execute()`.
/// For task-level control, use `webrisk_projects_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webrisk_projects_operations_list(
    client: &SimpleHttpClient,
    args: &WebriskProjectsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = webrisk_projects_operations_list_builder(
        client,
        &args.name,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.returnPartialSuccess,
    )?;
    webrisk_projects_operations_list_execute(builder)
}

/// GET v1/projects/{projectsId}/submissions
/// Creates a Submission of a URI suspected of containing phishing content to be reviewed. If the result verifies the existence of malicious phishing content, the site will be added to the [Google's Social Engineering lists](<https://support.google.`com/webmasters/answer/6350487/`>) in order to protect users that could get exposed to this threat in the future. Only allowlisted projects can use this method during Early Access. Please reach out to Sales or your customer engineer to obtain access.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `webrisk_projects_submissions_create_execute()` to send, or `webrisk_projects_submissions_create` for simplest API.

pub fn webrisk_projects_submissions_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleCloudWebriskV1Submission,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://webrisk.googleapis.com/v1/projects/{}/submissions",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/submissions
/// Creates a Submission of a URI suspected of containing phishing content to be reviewed. If the result verifies the existence of malicious phishing content, the site will be added to the [Google's Social Engineering lists](<https://support.google.`com/webmasters/answer/6350487/`>) in order to protect users that could get exposed to this threat in the future. Only allowlisted projects can use this method during Early Access. Please reach out to Sales or your customer engineer to obtain access.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `webrisk_projects_submissions_create_execute()` or `webrisk_projects_submissions_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `webrisk_projects_submissions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webrisk_projects_submissions_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudWebriskV1Submission>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudWebriskV1Submission = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/submissions
/// Creates a Submission of a URI suspected of containing phishing content to be reviewed. If the result verifies the existence of malicious phishing content, the site will be added to the [Google's Social Engineering lists](<https://support.google.`com/webmasters/answer/6350487/`>) in order to protect users that could get exposed to this threat in the future. Only allowlisted projects can use this method during Early Access. Please reach out to Sales or your customer engineer to obtain access.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `webrisk_projects_submissions_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `webrisk_projects_submissions_create_task()`.
/// For the simplest API, use `webrisk_projects_submissions_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `webrisk_projects_submissions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn webrisk_projects_submissions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudWebriskV1Submission>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = webrisk_projects_submissions_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`webrisk_projects_submissions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WebriskProjectsSubmissionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudWebriskV1Submission,
}

/// GET v1/projects/{projectsId}/submissions
/// Creates a Submission of a URI suspected of containing phishing content to be reviewed. If the result verifies the existence of malicious phishing content, the site will be added to the [Google's Social Engineering lists](<https://support.google.`com/webmasters/answer/6350487/`>) in order to protect users that could get exposed to this threat in the future. Only allowlisted projects can use this method during Early Access. Please reach out to Sales or your customer engineer to obtain access.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `webrisk_projects_submissions_create_builder()` + `webrisk_projects_submissions_create_execute()`.
/// For task-level control, use `webrisk_projects_submissions_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webrisk_projects_submissions_create(
    client: &SimpleHttpClient,
    args: &WebriskProjectsSubmissionsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudWebriskV1Submission>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = webrisk_projects_submissions_create_builder(client, &args.parent, &args.body)?;
    webrisk_projects_submissions_create_execute(builder)
}

/// GET v1/threatLists:computeDiff
/// Gets the most recent threat list diffs. These diffs should be applied to a local database of hashes to keep it up-to-date. If the local database is empty or excessively out-of-date, a complete snapshot of the database will be returned. This Method only updates a single ThreatList at a time. To update multiple ThreatList databases, this method needs to be called once for each list.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `webrisk_threat_lists_compute_diff_execute()` to send, or `webrisk_threat_lists_compute_diff` for simplest API.

pub fn webrisk_threat_lists_compute_diff_builder(
    client: &SimpleHttpClient,
    constraints_maxDatabaseEntries: Option<i32>,
    constraints_maxDiffEntries: Option<i32>,
    constraints_supportedCompressions: Option<&str>,
    threatType: Option<&str>,
    versionToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://webrisk.googleapis.com/v1/threatLists:computeDiff",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = constraints_maxDatabaseEntries {
        query_parts.push(format!("constraints_maxDatabaseEntries={}", val));
    }
    if let Some(val) = constraints_maxDiffEntries {
        query_parts.push(format!("constraints_maxDiffEntries={}", val));
    }
    if let Some(val) = constraints_supportedCompressions {
        query_parts.push(format!("constraints_supportedCompressions={}", val));
    }
    if let Some(val) = threatType {
        query_parts.push(format!("threatType={}", val));
    }
    if let Some(val) = versionToken {
        query_parts.push(format!("versionToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/threatLists:computeDiff
/// Gets the most recent threat list diffs. These diffs should be applied to a local database of hashes to keep it up-to-date. If the local database is empty or excessively out-of-date, a complete snapshot of the database will be returned. This Method only updates a single ThreatList at a time. To update multiple ThreatList databases, this method needs to be called once for each list.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `webrisk_threat_lists_compute_diff_execute()` or `webrisk_threat_lists_compute_diff`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `webrisk_threat_lists_compute_diff_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webrisk_threat_lists_compute_diff_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudWebriskV1ComputeThreatListDiffResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudWebriskV1ComputeThreatListDiffResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/threatLists:computeDiff
/// Gets the most recent threat list diffs. These diffs should be applied to a local database of hashes to keep it up-to-date. If the local database is empty or excessively out-of-date, a complete snapshot of the database will be returned. This Method only updates a single ThreatList at a time. To update multiple ThreatList databases, this method needs to be called once for each list.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `webrisk_threat_lists_compute_diff_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `webrisk_threat_lists_compute_diff_task()`.
/// For the simplest API, use `webrisk_threat_lists_compute_diff()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `webrisk_threat_lists_compute_diff_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn webrisk_threat_lists_compute_diff_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudWebriskV1ComputeThreatListDiffResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = webrisk_threat_lists_compute_diff_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`webrisk_threat_lists_compute_diff`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WebriskThreatListsComputeDiffArgs {
    /// Query parameter: constraints_maxDatabaseEntries
    pub constraints_maxDatabaseEntries: Option<i32>,
    /// Query parameter: constraints_maxDiffEntries
    pub constraints_maxDiffEntries: Option<i32>,
    /// Query parameter: constraints_supportedCompressions
    pub constraints_supportedCompressions: Option<String>,
    /// Query parameter: threatType
    pub threatType: Option<String>,
    /// Query parameter: versionToken
    pub versionToken: Option<String>,
}

/// GET v1/threatLists:computeDiff
/// Gets the most recent threat list diffs. These diffs should be applied to a local database of hashes to keep it up-to-date. If the local database is empty or excessively out-of-date, a complete snapshot of the database will be returned. This Method only updates a single ThreatList at a time. To update multiple ThreatList databases, this method needs to be called once for each list.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `webrisk_threat_lists_compute_diff_builder()` + `webrisk_threat_lists_compute_diff_execute()`.
/// For task-level control, use `webrisk_threat_lists_compute_diff_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webrisk_threat_lists_compute_diff(
    client: &SimpleHttpClient,
    args: &WebriskThreatListsComputeDiffArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudWebriskV1ComputeThreatListDiffResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = webrisk_threat_lists_compute_diff_builder(
        client,
        args.constraints_maxDatabaseEntries,
        args.constraints_maxDiffEntries,
        args.constraints_supportedCompressions.as_deref(),
        args.threatType.as_deref(),
        args.versionToken.as_deref(),
    )?;
    webrisk_threat_lists_compute_diff_execute(builder)
}

/// GET v1/uris:search
/// This method is used to check whether a URI is on a given `threatList`. Multiple `threatLists` may be searched in a single query. The response will list all requested `threatLists` the URI was found to match. If the URI is not found on any of the requested ThreatList an empty response will be returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `webrisk_uris_search_execute()` to send, or `webrisk_uris_search` for simplest API.

pub fn webrisk_uris_search_builder(
    client: &SimpleHttpClient,
    threatTypes: Option<&str>,
    uri: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://webrisk.googleapis.com/v1/uris:search",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = threatTypes {
        query_parts.push(format!("threatTypes={}", val));
    }
    if let Some(val) = uri {
        query_parts.push(format!("uri={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/uris:search
/// This method is used to check whether a URI is on a given `threatList`. Multiple `threatLists` may be searched in a single query. The response will list all requested `threatLists` the URI was found to match. If the URI is not found on any of the requested ThreatList an empty response will be returned.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `webrisk_uris_search_execute()` or `webrisk_uris_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `webrisk_uris_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webrisk_uris_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<GoogleCloudWebriskV1SearchUrisResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudWebriskV1SearchUrisResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/uris:search
/// This method is used to check whether a URI is on a given `threatList`. Multiple `threatLists` may be searched in a single query. The response will list all requested `threatLists` the URI was found to match. If the URI is not found on any of the requested ThreatList an empty response will be returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `webrisk_uris_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `webrisk_uris_search_task()`.
/// For the simplest API, use `webrisk_uris_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `webrisk_uris_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn webrisk_uris_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudWebriskV1SearchUrisResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = webrisk_uris_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`webrisk_uris_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WebriskUrisSearchArgs {
    /// Query parameter: threatTypes
    pub threatTypes: Option<String>,
    /// Query parameter: uri
    pub uri: Option<String>,
}

/// GET v1/uris:search
/// This method is used to check whether a URI is on a given `threatList`. Multiple `threatLists` may be searched in a single query. The response will list all requested `threatLists` the URI was found to match. If the URI is not found on any of the requested ThreatList an empty response will be returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `webrisk_uris_search_builder()` + `webrisk_uris_search_execute()`.
/// For task-level control, use `webrisk_uris_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn webrisk_uris_search(
    client: &SimpleHttpClient,
    args: &WebriskUrisSearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudWebriskV1SearchUrisResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        webrisk_uris_search_builder(client, args.threatTypes.as_deref(), args.uri.as_deref())?;
    webrisk_uris_search_execute(builder)
}
