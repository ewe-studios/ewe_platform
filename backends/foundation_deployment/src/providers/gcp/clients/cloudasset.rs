//! Auto-generated API clients for gcp/cloudasset.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, StreamIterator, StreamIteratorExt, TaskIterator, TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/{v1Id}/{v1Id1}/assets
/// Lists assets with time and resource types and returns paged results in response.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudasset_assets_list_execute()` to send, or `cloudasset_assets_list` for simplest API.

pub fn cloudasset_assets_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    assetTypes: Option<&str>,
    contentType: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    readTime: Option<&str>,
    relationshipTypes: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudasset.googleapis.com/v1/{}/{}/assets", parent,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = assetTypes {
        query_parts.push(format!("assetTypes={}", val));
    }
    if let Some(val) = contentType {
        query_parts.push(format!("contentType={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = readTime {
        query_parts.push(format!("readTime={}", val));
    }
    if let Some(val) = relationshipTypes {
        query_parts.push(format!("relationshipTypes={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/{v1Id}/{v1Id1}/assets
/// Lists assets with time and resource types and returns paged results in response.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudasset_assets_list_execute()` or `cloudasset_assets_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_assets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_assets_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAssetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}/{v1Id1}/assets
/// Lists assets with time and resource types and returns paged results in response.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudasset_assets_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudasset_assets_list_task()`.
/// For the simplest API, use `cloudasset_assets_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_assets_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudasset_assets_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = cloudasset_assets_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudasset_assets_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudassetAssetsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: assetTypes
    pub assetTypes: Option<String>,
    /// Query parameter: contentType
    pub contentType: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: readTime
    pub readTime: Option<String>,
    /// Query parameter: relationshipTypes
    pub relationshipTypes: Option<String>,
}

/// GET v1/{v1Id}/{v1Id1}/assets
/// Lists assets with time and resource types and returns paged results in response.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudasset_assets_list_builder()` + `cloudasset_assets_list_execute()`.
/// For task-level control, use `cloudasset_assets_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_assets_list(
    client: &SimpleHttpClient,
    args: &CloudassetAssetsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudasset_assets_list_builder(
        client,
        &args.parent,
        args.assetTypes.as_deref(),
        args.contentType.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.readTime.as_deref(),
        args.relationshipTypes.as_deref(),
    )?;
    cloudasset_assets_list_execute(builder)
}

/// GET v1/{v1Id}/{v1Id1}/effectiveIamPolicies:batchGet
/// Gets effective IAM policies for a batch of resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudasset_effective_iam_policies_batch_get_execute()` to send, or `cloudasset_effective_iam_policies_batch_get` for simplest API.

pub fn cloudasset_effective_iam_policies_batch_get_builder(
    client: &SimpleHttpClient,
    scope: &str,
    names: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudasset.googleapis.com/v1/{}/{}/effectiveIamPolicies:batchGet",
        scope,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = names {
        query_parts.push(format!("names={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/{v1Id}/{v1Id1}/effectiveIamPolicies:batchGet
/// Gets effective IAM policies for a batch of resources.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudasset_effective_iam_policies_batch_get_execute()` or `cloudasset_effective_iam_policies_batch_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_effective_iam_policies_batch_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_effective_iam_policies_batch_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<BatchGetEffectiveIamPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchGetEffectiveIamPoliciesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}/{v1Id1}/effectiveIamPolicies:batchGet
/// Gets effective IAM policies for a batch of resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudasset_effective_iam_policies_batch_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudasset_effective_iam_policies_batch_get_task()`.
/// For the simplest API, use `cloudasset_effective_iam_policies_batch_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_effective_iam_policies_batch_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudasset_effective_iam_policies_batch_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchGetEffectiveIamPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudasset_effective_iam_policies_batch_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudasset_effective_iam_policies_batch_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudassetEffectiveIamPoliciesBatchGetArgs {
    /// Path parameter: scope
    pub scope: String,
    /// Query parameter: names
    pub names: Option<String>,
}

/// GET v1/{v1Id}/{v1Id1}/effectiveIamPolicies:batchGet
/// Gets effective IAM policies for a batch of resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudasset_effective_iam_policies_batch_get_builder()` + `cloudasset_effective_iam_policies_batch_get_execute()`.
/// For task-level control, use `cloudasset_effective_iam_policies_batch_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_effective_iam_policies_batch_get(
    client: &SimpleHttpClient,
    args: &CloudassetEffectiveIamPoliciesBatchGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchGetEffectiveIamPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudasset_effective_iam_policies_batch_get_builder(
        client,
        &args.scope,
        args.names.as_deref(),
    )?;
    cloudasset_effective_iam_policies_batch_get_execute(builder)
}

/// GET v1/{v1Id}/{v1Id1}/feeds
/// Creates a feed in a parent `project/folder/organization` to listen to its asset updates.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudasset_feeds_create_execute()` to send, or `cloudasset_feeds_create` for simplest API.

pub fn cloudasset_feeds_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &CreateFeedRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudasset.googleapis.com/v1/{}/{}/feeds", parent,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{v1Id}/{v1Id1}/feeds
/// Creates a feed in a parent `project/folder/organization` to listen to its asset updates.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudasset_feeds_create_execute()` or `cloudasset_feeds_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_feeds_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_feeds_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Feed>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Feed = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}/{v1Id1}/feeds
/// Creates a feed in a parent `project/folder/organization` to listen to its asset updates.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudasset_feeds_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudasset_feeds_create_task()`.
/// For the simplest API, use `cloudasset_feeds_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_feeds_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudasset_feeds_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Feed>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudasset_feeds_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudasset_feeds_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudassetFeedsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: CreateFeedRequest,
}

/// GET v1/{v1Id}/{v1Id1}/feeds
/// Creates a feed in a parent `project/folder/organization` to listen to its asset updates.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudasset_feeds_create_builder()` + `cloudasset_feeds_create_execute()`.
/// For task-level control, use `cloudasset_feeds_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_feeds_create(
    client: &SimpleHttpClient,
    args: &CloudassetFeedsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Feed>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudasset_feeds_create_builder(client, &args.parent, &args.body)?;
    cloudasset_feeds_create_execute(builder)
}

/// GET v1/{v1Id}/{v1Id1}/feeds/{feedsId}
/// Deletes an asset feed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudasset_feeds_delete_execute()` to send, or `cloudasset_feeds_delete` for simplest API.

pub fn cloudasset_feeds_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudasset.googleapis.com/v1/{}/{}/feeds/{}", name,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/{v1Id}/{v1Id1}/feeds/{feedsId}
/// Deletes an asset feed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudasset_feeds_delete_execute()` or `cloudasset_feeds_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_feeds_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_feeds_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}/{v1Id1}/feeds/{feedsId}
/// Deletes an asset feed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudasset_feeds_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudasset_feeds_delete_task()`.
/// For the simplest API, use `cloudasset_feeds_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_feeds_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudasset_feeds_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudasset_feeds_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudasset_feeds_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudassetFeedsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/{v1Id}/{v1Id1}/feeds/{feedsId}
/// Deletes an asset feed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudasset_feeds_delete_builder()` + `cloudasset_feeds_delete_execute()`.
/// For task-level control, use `cloudasset_feeds_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_feeds_delete(
    client: &SimpleHttpClient,
    args: &CloudassetFeedsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudasset_feeds_delete_builder(client, &args.name)?;
    cloudasset_feeds_delete_execute(builder)
}

/// GET v1/{v1Id}/{v1Id1}/feeds/{feedsId}
/// Gets details about an asset feed.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudasset_feeds_get_execute()` to send, or `cloudasset_feeds_get` for simplest API.

pub fn cloudasset_feeds_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudasset.googleapis.com/v1/{}/{}/feeds/{}", name,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/{v1Id}/{v1Id1}/feeds/{feedsId}
/// Gets details about an asset feed.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudasset_feeds_get_execute()` or `cloudasset_feeds_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_feeds_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_feeds_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Feed>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Feed = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}/{v1Id1}/feeds/{feedsId}
/// Gets details about an asset feed.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudasset_feeds_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudasset_feeds_get_task()`.
/// For the simplest API, use `cloudasset_feeds_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_feeds_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudasset_feeds_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Feed>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudasset_feeds_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudasset_feeds_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudassetFeedsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/{v1Id}/{v1Id1}/feeds/{feedsId}
/// Gets details about an asset feed.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudasset_feeds_get_builder()` + `cloudasset_feeds_get_execute()`.
/// For task-level control, use `cloudasset_feeds_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_feeds_get(
    client: &SimpleHttpClient,
    args: &CloudassetFeedsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Feed>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudasset_feeds_get_builder(client, &args.name)?;
    cloudasset_feeds_get_execute(builder)
}

/// GET v1/{v1Id}/{v1Id1}/feeds
/// Lists all asset feeds in a parent `project/folder/organization`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudasset_feeds_list_execute()` to send, or `cloudasset_feeds_list` for simplest API.

pub fn cloudasset_feeds_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudasset.googleapis.com/v1/{}/{}/feeds", parent,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/{v1Id}/{v1Id1}/feeds
/// Lists all asset feeds in a parent `project/folder/organization`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudasset_feeds_list_execute()` or `cloudasset_feeds_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_feeds_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_feeds_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListFeedsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListFeedsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}/{v1Id1}/feeds
/// Lists all asset feeds in a parent `project/folder/organization`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudasset_feeds_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudasset_feeds_list_task()`.
/// For the simplest API, use `cloudasset_feeds_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_feeds_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudasset_feeds_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFeedsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = cloudasset_feeds_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudasset_feeds_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudassetFeedsListArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// GET v1/{v1Id}/{v1Id1}/feeds
/// Lists all asset feeds in a parent `project/folder/organization`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudasset_feeds_list_builder()` + `cloudasset_feeds_list_execute()`.
/// For task-level control, use `cloudasset_feeds_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_feeds_list(
    client: &SimpleHttpClient,
    args: &CloudassetFeedsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListFeedsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudasset_feeds_list_builder(client, &args.parent)?;
    cloudasset_feeds_list_execute(builder)
}

/// GET v1/{v1Id}/{v1Id1}/feeds/{feedsId}
/// Updates an asset feed configuration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudasset_feeds_patch_execute()` to send, or `cloudasset_feeds_patch` for simplest API.

pub fn cloudasset_feeds_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &UpdateFeedRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://cloudasset.googleapis.com/v1/{}/{}/feeds/{}", name,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{v1Id}/{v1Id1}/feeds/{feedsId}
/// Updates an asset feed configuration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudasset_feeds_patch_execute()` or `cloudasset_feeds_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_feeds_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_feeds_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Feed>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Feed = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}/{v1Id1}/feeds/{feedsId}
/// Updates an asset feed configuration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudasset_feeds_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudasset_feeds_patch_task()`.
/// For the simplest API, use `cloudasset_feeds_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_feeds_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudasset_feeds_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Feed>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudasset_feeds_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudasset_feeds_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudassetFeedsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: UpdateFeedRequest,
}

/// GET v1/{v1Id}/{v1Id1}/feeds/{feedsId}
/// Updates an asset feed configuration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudasset_feeds_patch_builder()` + `cloudasset_feeds_patch_execute()`.
/// For task-level control, use `cloudasset_feeds_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_feeds_patch(
    client: &SimpleHttpClient,
    args: &CloudassetFeedsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Feed>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudasset_feeds_patch_builder(client, &args.name, &args.body)?;
    cloudasset_feeds_patch_execute(builder)
}

/// GET v1/{v1Id}/{v1Id1}/operations/{operationsId}/{operationsId1}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudasset_operations_get_execute()` to send, or `cloudasset_operations_get` for simplest API.

pub fn cloudasset_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudasset.googleapis.com/v1/{}/{}/operations/{}/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/{v1Id}/{v1Id1}/operations/{operationsId}/{operationsId1}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudasset_operations_get_execute()` or `cloudasset_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}/{v1Id1}/operations/{operationsId}/{operationsId1}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudasset_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudasset_operations_get_task()`.
/// For the simplest API, use `cloudasset_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudasset_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudasset_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudasset_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudassetOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/{v1Id}/{v1Id1}/operations/{operationsId}/{operationsId1}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudasset_operations_get_builder()` + `cloudasset_operations_get_execute()`.
/// For task-level control, use `cloudasset_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_operations_get(
    client: &SimpleHttpClient,
    args: &CloudassetOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudasset_operations_get_builder(client, &args.name)?;
    cloudasset_operations_get_execute(builder)
}

/// GET v1/{v1Id}/{v1Id1}/savedQueries
/// Creates a saved query in a parent `project/folder/organization`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudasset_saved_queries_create_execute()` to send, or `cloudasset_saved_queries_create` for simplest API.

pub fn cloudasset_saved_queries_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    savedQueryId: Option<&str>,
    body: &SavedQuery,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudasset.googleapis.com/v1/{}/{}/savedQueries",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = savedQueryId {
        query_parts.push(format!("savedQueryId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{v1Id}/{v1Id1}/savedQueries
/// Creates a saved query in a parent `project/folder/organization`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudasset_saved_queries_create_execute()` or `cloudasset_saved_queries_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_saved_queries_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_saved_queries_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SavedQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}/{v1Id1}/savedQueries
/// Creates a saved query in a parent `project/folder/organization`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudasset_saved_queries_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudasset_saved_queries_create_task()`.
/// For the simplest API, use `cloudasset_saved_queries_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_saved_queries_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudasset_saved_queries_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudasset_saved_queries_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudasset_saved_queries_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudassetSavedQueriesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: savedQueryId
    pub savedQueryId: Option<String>,
    /// Request body.
    pub body: SavedQuery,
}

/// GET v1/{v1Id}/{v1Id1}/savedQueries
/// Creates a saved query in a parent `project/folder/organization`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudasset_saved_queries_create_builder()` + `cloudasset_saved_queries_create_execute()`.
/// For task-level control, use `cloudasset_saved_queries_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_saved_queries_create(
    client: &SimpleHttpClient,
    args: &CloudassetSavedQueriesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudasset_saved_queries_create_builder(
        client,
        &args.parent,
        args.savedQueryId.as_deref(),
        &args.body,
    )?;
    cloudasset_saved_queries_create_execute(builder)
}

/// GET v1/{v1Id}/{v1Id1}/savedQueries/{savedQueriesId}
/// Deletes a saved query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudasset_saved_queries_delete_execute()` to send, or `cloudasset_saved_queries_delete` for simplest API.

pub fn cloudasset_saved_queries_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudasset.googleapis.com/v1/{}/{}/savedQueries/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/{v1Id}/{v1Id1}/savedQueries/{savedQueriesId}
/// Deletes a saved query.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudasset_saved_queries_delete_execute()` or `cloudasset_saved_queries_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_saved_queries_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_saved_queries_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}/{v1Id1}/savedQueries/{savedQueriesId}
/// Deletes a saved query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudasset_saved_queries_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudasset_saved_queries_delete_task()`.
/// For the simplest API, use `cloudasset_saved_queries_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_saved_queries_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudasset_saved_queries_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudasset_saved_queries_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudasset_saved_queries_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudassetSavedQueriesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/{v1Id}/{v1Id1}/savedQueries/{savedQueriesId}
/// Deletes a saved query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudasset_saved_queries_delete_builder()` + `cloudasset_saved_queries_delete_execute()`.
/// For task-level control, use `cloudasset_saved_queries_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_saved_queries_delete(
    client: &SimpleHttpClient,
    args: &CloudassetSavedQueriesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudasset_saved_queries_delete_builder(client, &args.name)?;
    cloudasset_saved_queries_delete_execute(builder)
}

/// GET v1/{v1Id}/{v1Id1}/savedQueries/{savedQueriesId}
/// Gets details about a saved query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudasset_saved_queries_get_execute()` to send, or `cloudasset_saved_queries_get` for simplest API.

pub fn cloudasset_saved_queries_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudasset.googleapis.com/v1/{}/{}/savedQueries/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/{v1Id}/{v1Id1}/savedQueries/{savedQueriesId}
/// Gets details about a saved query.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudasset_saved_queries_get_execute()` or `cloudasset_saved_queries_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_saved_queries_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_saved_queries_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SavedQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}/{v1Id1}/savedQueries/{savedQueriesId}
/// Gets details about a saved query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudasset_saved_queries_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudasset_saved_queries_get_task()`.
/// For the simplest API, use `cloudasset_saved_queries_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_saved_queries_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudasset_saved_queries_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudasset_saved_queries_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudasset_saved_queries_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudassetSavedQueriesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/{v1Id}/{v1Id1}/savedQueries/{savedQueriesId}
/// Gets details about a saved query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudasset_saved_queries_get_builder()` + `cloudasset_saved_queries_get_execute()`.
/// For task-level control, use `cloudasset_saved_queries_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_saved_queries_get(
    client: &SimpleHttpClient,
    args: &CloudassetSavedQueriesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudasset_saved_queries_get_builder(client, &args.name)?;
    cloudasset_saved_queries_get_execute(builder)
}

/// GET v1/{v1Id}/{v1Id1}/savedQueries
/// Lists all saved queries in a parent `project/folder/organization`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudasset_saved_queries_list_execute()` to send, or `cloudasset_saved_queries_list` for simplest API.

pub fn cloudasset_saved_queries_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudasset.googleapis.com/v1/{}/{}/savedQueries",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/{v1Id}/{v1Id1}/savedQueries
/// Lists all saved queries in a parent `project/folder/organization`.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudasset_saved_queries_list_execute()` or `cloudasset_saved_queries_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_saved_queries_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_saved_queries_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<ListSavedQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSavedQueriesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}/{v1Id1}/savedQueries
/// Lists all saved queries in a parent `project/folder/organization`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudasset_saved_queries_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudasset_saved_queries_list_task()`.
/// For the simplest API, use `cloudasset_saved_queries_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_saved_queries_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudasset_saved_queries_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSavedQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = cloudasset_saved_queries_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudasset_saved_queries_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudassetSavedQueriesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/{v1Id}/{v1Id1}/savedQueries
/// Lists all saved queries in a parent `project/folder/organization`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudasset_saved_queries_list_builder()` + `cloudasset_saved_queries_list_execute()`.
/// For task-level control, use `cloudasset_saved_queries_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_saved_queries_list(
    client: &SimpleHttpClient,
    args: &CloudassetSavedQueriesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSavedQueriesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudasset_saved_queries_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    cloudasset_saved_queries_list_execute(builder)
}

/// GET v1/{v1Id}/{v1Id1}/savedQueries/{savedQueriesId}
/// Updates a saved query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudasset_saved_queries_patch_execute()` to send, or `cloudasset_saved_queries_patch` for simplest API.

pub fn cloudasset_saved_queries_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &SavedQuery,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudasset.googleapis.com/v1/{}/{}/savedQueries/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{v1Id}/{v1Id1}/savedQueries/{savedQueriesId}
/// Updates a saved query.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudasset_saved_queries_patch_execute()` or `cloudasset_saved_queries_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_saved_queries_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_saved_queries_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SavedQuery = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}/{v1Id1}/savedQueries/{savedQueriesId}
/// Updates a saved query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudasset_saved_queries_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudasset_saved_queries_patch_task()`.
/// For the simplest API, use `cloudasset_saved_queries_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_saved_queries_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudasset_saved_queries_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudasset_saved_queries_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudasset_saved_queries_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudassetSavedQueriesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: SavedQuery,
}

/// GET v1/{v1Id}/{v1Id1}/savedQueries/{savedQueriesId}
/// Updates a saved query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudasset_saved_queries_patch_builder()` + `cloudasset_saved_queries_patch_execute()`.
/// For task-level control, use `cloudasset_saved_queries_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_saved_queries_patch(
    client: &SimpleHttpClient,
    args: &CloudassetSavedQueriesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SavedQuery>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudasset_saved_queries_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    cloudasset_saved_queries_patch_execute(builder)
}

/// GET v1/{v1Id}/{v1Id1}:analyzeIamPolicy
/// Analyzes IAM policies to answer which identities have what accesses on which resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudasset_analyze_iam_policy_execute()` to send, or `cloudasset_analyze_iam_policy` for simplest API.

pub fn cloudasset_analyze_iam_policy_builder(
    client: &SimpleHttpClient,
    scope: &str,
    analysisQuery_accessSelector_permissions: Option<&str>,
    analysisQuery_accessSelector_roles: Option<&str>,
    analysisQuery_conditionContext_accessTime: Option<&str>,
    analysisQuery_identitySelector_identity: Option<&str>,
    analysisQuery_options_analyzeServiceAccountImpersonation: Option<bool>,
    analysisQuery_options_expandGroups: Option<bool>,
    analysisQuery_options_expandResources: Option<bool>,
    analysisQuery_options_expandRoles: Option<bool>,
    analysisQuery_options_outputGroupEdges: Option<bool>,
    analysisQuery_options_outputResourceEdges: Option<bool>,
    analysisQuery_resourceSelector_fullResourceName: Option<&str>,
    executionTimeout: Option<&str>,
    savedAnalysisQuery: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudasset.googleapis.com/v1/{}/{}:analyzeIamPolicy",
        scope,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = analysisQuery_accessSelector_permissions {
        query_parts.push(format!("analysisQuery_accessSelector_permissions={}", val));
    }
    if let Some(val) = analysisQuery_accessSelector_roles {
        query_parts.push(format!("analysisQuery_accessSelector_roles={}", val));
    }
    if let Some(val) = analysisQuery_conditionContext_accessTime {
        query_parts.push(format!("analysisQuery_conditionContext_accessTime={}", val));
    }
    if let Some(val) = analysisQuery_identitySelector_identity {
        query_parts.push(format!("analysisQuery_identitySelector_identity={}", val));
    }
    if let Some(val) = analysisQuery_options_analyzeServiceAccountImpersonation {
        query_parts.push(format!(
            "analysisQuery_options_analyzeServiceAccountImpersonation={}",
            val
        ));
    }
    if let Some(val) = analysisQuery_options_expandGroups {
        query_parts.push(format!("analysisQuery_options_expandGroups={}", val));
    }
    if let Some(val) = analysisQuery_options_expandResources {
        query_parts.push(format!("analysisQuery_options_expandResources={}", val));
    }
    if let Some(val) = analysisQuery_options_expandRoles {
        query_parts.push(format!("analysisQuery_options_expandRoles={}", val));
    }
    if let Some(val) = analysisQuery_options_outputGroupEdges {
        query_parts.push(format!("analysisQuery_options_outputGroupEdges={}", val));
    }
    if let Some(val) = analysisQuery_options_outputResourceEdges {
        query_parts.push(format!("analysisQuery_options_outputResourceEdges={}", val));
    }
    if let Some(val) = analysisQuery_resourceSelector_fullResourceName {
        query_parts.push(format!(
            "analysisQuery_resourceSelector_fullResourceName={}",
            val
        ));
    }
    if let Some(val) = executionTimeout {
        query_parts.push(format!("executionTimeout={}", val));
    }
    if let Some(val) = savedAnalysisQuery {
        query_parts.push(format!("savedAnalysisQuery={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/{v1Id}/{v1Id1}:analyzeIamPolicy
/// Analyzes IAM policies to answer which identities have what accesses on which resources.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudasset_analyze_iam_policy_execute()` or `cloudasset_analyze_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_analyze_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_analyze_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<AnalyzeIamPolicyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AnalyzeIamPolicyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}/{v1Id1}:analyzeIamPolicy
/// Analyzes IAM policies to answer which identities have what accesses on which resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudasset_analyze_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudasset_analyze_iam_policy_task()`.
/// For the simplest API, use `cloudasset_analyze_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_analyze_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudasset_analyze_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AnalyzeIamPolicyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = cloudasset_analyze_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudasset_analyze_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudassetAnalyzeIamPolicyArgs {
    /// Path parameter: scope
    pub scope: String,
    /// Query parameter: analysisQuery_accessSelector_permissions
    pub analysisQuery_accessSelector_permissions: Option<String>,
    /// Query parameter: analysisQuery_accessSelector_roles
    pub analysisQuery_accessSelector_roles: Option<String>,
    /// Query parameter: analysisQuery_conditionContext_accessTime
    pub analysisQuery_conditionContext_accessTime: Option<String>,
    /// Query parameter: analysisQuery_identitySelector_identity
    pub analysisQuery_identitySelector_identity: Option<String>,
    /// Query parameter: analysisQuery_options_analyzeServiceAccountImpersonation
    pub analysisQuery_options_analyzeServiceAccountImpersonation: Option<bool>,
    /// Query parameter: analysisQuery_options_expandGroups
    pub analysisQuery_options_expandGroups: Option<bool>,
    /// Query parameter: analysisQuery_options_expandResources
    pub analysisQuery_options_expandResources: Option<bool>,
    /// Query parameter: analysisQuery_options_expandRoles
    pub analysisQuery_options_expandRoles: Option<bool>,
    /// Query parameter: analysisQuery_options_outputGroupEdges
    pub analysisQuery_options_outputGroupEdges: Option<bool>,
    /// Query parameter: analysisQuery_options_outputResourceEdges
    pub analysisQuery_options_outputResourceEdges: Option<bool>,
    /// Query parameter: analysisQuery_resourceSelector_fullResourceName
    pub analysisQuery_resourceSelector_fullResourceName: Option<String>,
    /// Query parameter: executionTimeout
    pub executionTimeout: Option<String>,
    /// Query parameter: savedAnalysisQuery
    pub savedAnalysisQuery: Option<String>,
}

/// GET v1/{v1Id}/{v1Id1}:analyzeIamPolicy
/// Analyzes IAM policies to answer which identities have what accesses on which resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudasset_analyze_iam_policy_builder()` + `cloudasset_analyze_iam_policy_execute()`.
/// For task-level control, use `cloudasset_analyze_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_analyze_iam_policy(
    client: &SimpleHttpClient,
    args: &CloudassetAnalyzeIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AnalyzeIamPolicyResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudasset_analyze_iam_policy_builder(
        client,
        &args.scope,
        args.analysisQuery_accessSelector_permissions.as_deref(),
        args.analysisQuery_accessSelector_roles.as_deref(),
        args.analysisQuery_conditionContext_accessTime.as_deref(),
        args.analysisQuery_identitySelector_identity.as_deref(),
        args.analysisQuery_options_analyzeServiceAccountImpersonation,
        args.analysisQuery_options_expandGroups,
        args.analysisQuery_options_expandResources,
        args.analysisQuery_options_expandRoles,
        args.analysisQuery_options_outputGroupEdges,
        args.analysisQuery_options_outputResourceEdges,
        args.analysisQuery_resourceSelector_fullResourceName
            .as_deref(),
        args.executionTimeout.as_deref(),
        args.savedAnalysisQuery.as_deref(),
    )?;
    cloudasset_analyze_iam_policy_execute(builder)
}

/// GET v1/{v1Id}/{v1Id1}:analyzeIamPolicyLongrunning
/// Analyzes IAM policies asynchronously to answer which identities have what accesses on which resources, and writes the analysis results to a Google Cloud Storage or a BigQuery destination. For Cloud Storage destination, the output format is the JSON format that represents a AnalyzeIamPolicyResponse. This method implements the google.longrunning.Operation, which allows you to track the operation status. We recommend intervals of at least 2 seconds with exponential backoff retry to poll the operation result. The metadata contains the metadata for the long-running operation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudasset_analyze_iam_policy_longrunning_execute()` to send, or `cloudasset_analyze_iam_policy_longrunning` for simplest API.

pub fn cloudasset_analyze_iam_policy_longrunning_builder(
    client: &SimpleHttpClient,
    scope: &str,
    body: &AnalyzeIamPolicyLongrunningRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudasset.googleapis.com/v1/{}/{}:analyzeIamPolicyLongrunning",
        scope,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{v1Id}/{v1Id1}:analyzeIamPolicyLongrunning
/// Analyzes IAM policies asynchronously to answer which identities have what accesses on which resources, and writes the analysis results to a Google Cloud Storage or a BigQuery destination. For Cloud Storage destination, the output format is the JSON format that represents a AnalyzeIamPolicyResponse. This method implements the google.longrunning.Operation, which allows you to track the operation status. We recommend intervals of at least 2 seconds with exponential backoff retry to poll the operation result. The metadata contains the metadata for the long-running operation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudasset_analyze_iam_policy_longrunning_execute()` or `cloudasset_analyze_iam_policy_longrunning`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_analyze_iam_policy_longrunning_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_analyze_iam_policy_longrunning_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}/{v1Id1}:analyzeIamPolicyLongrunning
/// Analyzes IAM policies asynchronously to answer which identities have what accesses on which resources, and writes the analysis results to a Google Cloud Storage or a BigQuery destination. For Cloud Storage destination, the output format is the JSON format that represents a AnalyzeIamPolicyResponse. This method implements the google.longrunning.Operation, which allows you to track the operation status. We recommend intervals of at least 2 seconds with exponential backoff retry to poll the operation result. The metadata contains the metadata for the long-running operation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudasset_analyze_iam_policy_longrunning_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudasset_analyze_iam_policy_longrunning_task()`.
/// For the simplest API, use `cloudasset_analyze_iam_policy_longrunning()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_analyze_iam_policy_longrunning_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudasset_analyze_iam_policy_longrunning_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudasset_analyze_iam_policy_longrunning_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudasset_analyze_iam_policy_longrunning`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudassetAnalyzeIamPolicyLongrunningArgs {
    /// Path parameter: scope
    pub scope: String,
    /// Request body.
    pub body: AnalyzeIamPolicyLongrunningRequest,
}

/// GET v1/{v1Id}/{v1Id1}:analyzeIamPolicyLongrunning
/// Analyzes IAM policies asynchronously to answer which identities have what accesses on which resources, and writes the analysis results to a Google Cloud Storage or a BigQuery destination. For Cloud Storage destination, the output format is the JSON format that represents a AnalyzeIamPolicyResponse. This method implements the google.longrunning.Operation, which allows you to track the operation status. We recommend intervals of at least 2 seconds with exponential backoff retry to poll the operation result. The metadata contains the metadata for the long-running operation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudasset_analyze_iam_policy_longrunning_builder()` + `cloudasset_analyze_iam_policy_longrunning_execute()`.
/// For task-level control, use `cloudasset_analyze_iam_policy_longrunning_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_analyze_iam_policy_longrunning(
    client: &SimpleHttpClient,
    args: &CloudassetAnalyzeIamPolicyLongrunningArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudasset_analyze_iam_policy_longrunning_builder(client, &args.scope, &args.body)?;
    cloudasset_analyze_iam_policy_longrunning_execute(builder)
}

/// GET v1/{v1Id}/{v1Id1}:analyzeMove
/// Analyze moving a resource to a specified destination without kicking off the actual move. The analysis is best effort depending on the user's permissions of viewing different hierarchical policies and configurations. The policies and configuration are subject to change before the actual resource migration takes place.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudasset_analyze_move_execute()` to send, or `cloudasset_analyze_move` for simplest API.

pub fn cloudasset_analyze_move_builder(
    client: &SimpleHttpClient,
    resource: &str,
    destinationParent: Option<&str>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudasset.googleapis.com/v1/{}/{}:analyzeMove",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = destinationParent {
        query_parts.push(format!("destinationParent={}", val));
    }
    if let Some(val) = view {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/{v1Id}/{v1Id1}:analyzeMove
/// Analyze moving a resource to a specified destination without kicking off the actual move. The analysis is best effort depending on the user's permissions of viewing different hierarchical policies and configurations. The policies and configuration are subject to change before the actual resource migration takes place.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudasset_analyze_move_execute()` or `cloudasset_analyze_move`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_analyze_move_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_analyze_move_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<AnalyzeMoveResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AnalyzeMoveResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}/{v1Id1}:analyzeMove
/// Analyze moving a resource to a specified destination without kicking off the actual move. The analysis is best effort depending on the user's permissions of viewing different hierarchical policies and configurations. The policies and configuration are subject to change before the actual resource migration takes place.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudasset_analyze_move_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudasset_analyze_move_task()`.
/// For the simplest API, use `cloudasset_analyze_move()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_analyze_move_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudasset_analyze_move_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AnalyzeMoveResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = cloudasset_analyze_move_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudasset_analyze_move`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudassetAnalyzeMoveArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: destinationParent
    pub destinationParent: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/{v1Id}/{v1Id1}:analyzeMove
/// Analyze moving a resource to a specified destination without kicking off the actual move. The analysis is best effort depending on the user's permissions of viewing different hierarchical policies and configurations. The policies and configuration are subject to change before the actual resource migration takes place.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudasset_analyze_move_builder()` + `cloudasset_analyze_move_execute()`.
/// For task-level control, use `cloudasset_analyze_move_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_analyze_move(
    client: &SimpleHttpClient,
    args: &CloudassetAnalyzeMoveArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AnalyzeMoveResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudasset_analyze_move_builder(
        client,
        &args.resource,
        args.destinationParent.as_deref(),
        args.view.as_deref(),
    )?;
    cloudasset_analyze_move_execute(builder)
}

/// GET v1/{v1Id}/{v1Id1}:analyzeOrgPolicies
/// Analyzes organization policies under a scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudasset_analyze_org_policies_execute()` to send, or `cloudasset_analyze_org_policies` for simplest API.

pub fn cloudasset_analyze_org_policies_builder(
    client: &SimpleHttpClient,
    scope: &str,
    constraint: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudasset.googleapis.com/v1/{}/{}:analyzeOrgPolicies",
        scope,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = constraint {
        query_parts.push(format!("constraint={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/{v1Id}/{v1Id1}:analyzeOrgPolicies
/// Analyzes organization policies under a scope.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudasset_analyze_org_policies_execute()` or `cloudasset_analyze_org_policies`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_analyze_org_policies_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_analyze_org_policies_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<AnalyzeOrgPoliciesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AnalyzeOrgPoliciesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}/{v1Id1}:analyzeOrgPolicies
/// Analyzes organization policies under a scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudasset_analyze_org_policies_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudasset_analyze_org_policies_task()`.
/// For the simplest API, use `cloudasset_analyze_org_policies()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_analyze_org_policies_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudasset_analyze_org_policies_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AnalyzeOrgPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudasset_analyze_org_policies_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudasset_analyze_org_policies`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudassetAnalyzeOrgPoliciesArgs {
    /// Path parameter: scope
    pub scope: String,
    /// Query parameter: constraint
    pub constraint: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/{v1Id}/{v1Id1}:analyzeOrgPolicies
/// Analyzes organization policies under a scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudasset_analyze_org_policies_builder()` + `cloudasset_analyze_org_policies_execute()`.
/// For task-level control, use `cloudasset_analyze_org_policies_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_analyze_org_policies(
    client: &SimpleHttpClient,
    args: &CloudassetAnalyzeOrgPoliciesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AnalyzeOrgPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudasset_analyze_org_policies_builder(
        client,
        &args.scope,
        args.constraint.as_deref(),
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    cloudasset_analyze_org_policies_execute(builder)
}

/// GET v1/{v1Id}/{v1Id1}:analyzeOrgPolicyGovernedAssets
/// Analyzes organization policies governed assets (Google Cloud resources or policies) under a scope. This RPC supports custom constraints and the following canned constraints: * `constraints/ainotebooks`.`accessMode` * `constraints/ainotebooks`.`disableFileDownloads` * `constraints/ainotebooks`.`disableRootAccess` * `constraints/ainotebooks`.`disableTerminal` * `constraints/ainotebooks`.`environmentOptions` * `constraints/ainotebooks`.`requireAutoUpgradeSchedule` * `constraints/ainotebooks`.`restrictVpcNetworks` * `constraints/compute`.`disableGuestAttributesAccess` * `constraints/compute`.`disableInstanceDataAccessApis` * `constraints/compute`.`disableNestedVirtualization` * `constraints/compute`.`disableSerialPortAccess` * `constraints/compute`.`disableSerialPortLogging` * `constraints/compute`.`disableVpcExternalIpv6` * `constraints/compute`.`requireOsLogin` * `constraints/compute`.`requireShieldedVm` * `constraints/compute`.`restrictLoadBalancerCreationForTypes` * `constraints/compute`.`restrictProtocolForwardingCreationForTypes` * `constraints/compute`.`restrictXpnProjectLienRemoval` * `constraints/compute`.`setNewProjectDefaultToZonalDNSOnly` * `constraints/compute`.`skipDefaultNetworkCreation` * `constraints/compute`.`trustedImageProjects` * `constraints/compute`.`vmCanIpForward` * `constraints/compute`.`vmExternalIpAccess` * `constraints/gcp`.`detailedAuditLoggingMode` * `constraints/gcp`.`resourceLocations` * `constraints/iam`.`allowedPolicyMemberDomains` * `constraints/iam`.`automaticIamGrantsForDefaultServiceAccounts` * `constraints/iam`.`disableServiceAccountCreation` * `constraints/iam`.`disableServiceAccountKeyCreation` * `constraints/iam`.`disableServiceAccountKeyUpload` * `constraints/iam`.`restrictCrossProjectServiceAccountLienRemoval` * `constraints/iam`.`serviceAccountKeyExpiryHours` * `constraints/resourcemanager`.`accessBoundaries` * `constraints/resourcemanager`.`allowedExportDestinations` * `constraints/sql`.`restrictAuthorizedNetworks` * `constraints/sql`.`restrictNoncompliantDiagnosticDataAccess` * `constraints/sql`.`restrictNoncompliantResourceCreation` * `constraints/sql`.`restrictPublicIp` * `constraints/storage`.`publicAccessPrevention` * `constraints/storage`.`restrictAuthTypes` * `constraints/storage`.`uniformBucketLevelAccess` This RPC only returns either resources of types [supported by search APIs](<https://cloud.google.`com/asset-inventory/docs/supported-asset-types`>) or IAM policies.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudasset_analyze_org_policy_governed_assets_execute()` to send, or `cloudasset_analyze_org_policy_governed_assets` for simplest API.

pub fn cloudasset_analyze_org_policy_governed_assets_builder(
    client: &SimpleHttpClient,
    scope: &str,
    constraint: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudasset.googleapis.com/v1/{}/{}:analyzeOrgPolicyGovernedAssets",
        scope,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = constraint {
        query_parts.push(format!("constraint={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/{v1Id}/{v1Id1}:analyzeOrgPolicyGovernedAssets
/// Analyzes organization policies governed assets (Google Cloud resources or policies) under a scope. This RPC supports custom constraints and the following canned constraints: * `constraints/ainotebooks`.`accessMode` * `constraints/ainotebooks`.`disableFileDownloads` * `constraints/ainotebooks`.`disableRootAccess` * `constraints/ainotebooks`.`disableTerminal` * `constraints/ainotebooks`.`environmentOptions` * `constraints/ainotebooks`.`requireAutoUpgradeSchedule` * `constraints/ainotebooks`.`restrictVpcNetworks` * `constraints/compute`.`disableGuestAttributesAccess` * `constraints/compute`.`disableInstanceDataAccessApis` * `constraints/compute`.`disableNestedVirtualization` * `constraints/compute`.`disableSerialPortAccess` * `constraints/compute`.`disableSerialPortLogging` * `constraints/compute`.`disableVpcExternalIpv6` * `constraints/compute`.`requireOsLogin` * `constraints/compute`.`requireShieldedVm` * `constraints/compute`.`restrictLoadBalancerCreationForTypes` * `constraints/compute`.`restrictProtocolForwardingCreationForTypes` * `constraints/compute`.`restrictXpnProjectLienRemoval` * `constraints/compute`.`setNewProjectDefaultToZonalDNSOnly` * `constraints/compute`.`skipDefaultNetworkCreation` * `constraints/compute`.`trustedImageProjects` * `constraints/compute`.`vmCanIpForward` * `constraints/compute`.`vmExternalIpAccess` * `constraints/gcp`.`detailedAuditLoggingMode` * `constraints/gcp`.`resourceLocations` * `constraints/iam`.`allowedPolicyMemberDomains` * `constraints/iam`.`automaticIamGrantsForDefaultServiceAccounts` * `constraints/iam`.`disableServiceAccountCreation` * `constraints/iam`.`disableServiceAccountKeyCreation` * `constraints/iam`.`disableServiceAccountKeyUpload` * `constraints/iam`.`restrictCrossProjectServiceAccountLienRemoval` * `constraints/iam`.`serviceAccountKeyExpiryHours` * `constraints/resourcemanager`.`accessBoundaries` * `constraints/resourcemanager`.`allowedExportDestinations` * `constraints/sql`.`restrictAuthorizedNetworks` * `constraints/sql`.`restrictNoncompliantDiagnosticDataAccess` * `constraints/sql`.`restrictNoncompliantResourceCreation` * `constraints/sql`.`restrictPublicIp` * `constraints/storage`.`publicAccessPrevention` * `constraints/storage`.`restrictAuthTypes` * `constraints/storage`.`uniformBucketLevelAccess` This RPC only returns either resources of types [supported by search APIs](<https://cloud.google.`com/asset-inventory/docs/supported-asset-types`>) or IAM policies.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudasset_analyze_org_policy_governed_assets_execute()` or `cloudasset_analyze_org_policy_governed_assets`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_analyze_org_policy_governed_assets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_analyze_org_policy_governed_assets_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<AnalyzeOrgPolicyGovernedAssetsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AnalyzeOrgPolicyGovernedAssetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}/{v1Id1}:analyzeOrgPolicyGovernedAssets
/// Analyzes organization policies governed assets (Google Cloud resources or policies) under a scope. This RPC supports custom constraints and the following canned constraints: * `constraints/ainotebooks`.`accessMode` * `constraints/ainotebooks`.`disableFileDownloads` * `constraints/ainotebooks`.`disableRootAccess` * `constraints/ainotebooks`.`disableTerminal` * `constraints/ainotebooks`.`environmentOptions` * `constraints/ainotebooks`.`requireAutoUpgradeSchedule` * `constraints/ainotebooks`.`restrictVpcNetworks` * `constraints/compute`.`disableGuestAttributesAccess` * `constraints/compute`.`disableInstanceDataAccessApis` * `constraints/compute`.`disableNestedVirtualization` * `constraints/compute`.`disableSerialPortAccess` * `constraints/compute`.`disableSerialPortLogging` * `constraints/compute`.`disableVpcExternalIpv6` * `constraints/compute`.`requireOsLogin` * `constraints/compute`.`requireShieldedVm` * `constraints/compute`.`restrictLoadBalancerCreationForTypes` * `constraints/compute`.`restrictProtocolForwardingCreationForTypes` * `constraints/compute`.`restrictXpnProjectLienRemoval` * `constraints/compute`.`setNewProjectDefaultToZonalDNSOnly` * `constraints/compute`.`skipDefaultNetworkCreation` * `constraints/compute`.`trustedImageProjects` * `constraints/compute`.`vmCanIpForward` * `constraints/compute`.`vmExternalIpAccess` * `constraints/gcp`.`detailedAuditLoggingMode` * `constraints/gcp`.`resourceLocations` * `constraints/iam`.`allowedPolicyMemberDomains` * `constraints/iam`.`automaticIamGrantsForDefaultServiceAccounts` * `constraints/iam`.`disableServiceAccountCreation` * `constraints/iam`.`disableServiceAccountKeyCreation` * `constraints/iam`.`disableServiceAccountKeyUpload` * `constraints/iam`.`restrictCrossProjectServiceAccountLienRemoval` * `constraints/iam`.`serviceAccountKeyExpiryHours` * `constraints/resourcemanager`.`accessBoundaries` * `constraints/resourcemanager`.`allowedExportDestinations` * `constraints/sql`.`restrictAuthorizedNetworks` * `constraints/sql`.`restrictNoncompliantDiagnosticDataAccess` * `constraints/sql`.`restrictNoncompliantResourceCreation` * `constraints/sql`.`restrictPublicIp` * `constraints/storage`.`publicAccessPrevention` * `constraints/storage`.`restrictAuthTypes` * `constraints/storage`.`uniformBucketLevelAccess` This RPC only returns either resources of types [supported by search APIs](<https://cloud.google.`com/asset-inventory/docs/supported-asset-types`>) or IAM policies.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudasset_analyze_org_policy_governed_assets_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudasset_analyze_org_policy_governed_assets_task()`.
/// For the simplest API, use `cloudasset_analyze_org_policy_governed_assets()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_analyze_org_policy_governed_assets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudasset_analyze_org_policy_governed_assets_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AnalyzeOrgPolicyGovernedAssetsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudasset_analyze_org_policy_governed_assets_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudasset_analyze_org_policy_governed_assets`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudassetAnalyzeOrgPolicyGovernedAssetsArgs {
    /// Path parameter: scope
    pub scope: String,
    /// Query parameter: constraint
    pub constraint: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/{v1Id}/{v1Id1}:analyzeOrgPolicyGovernedAssets
/// Analyzes organization policies governed assets (Google Cloud resources or policies) under a scope. This RPC supports custom constraints and the following canned constraints: * `constraints/ainotebooks`.`accessMode` * `constraints/ainotebooks`.`disableFileDownloads` * `constraints/ainotebooks`.`disableRootAccess` * `constraints/ainotebooks`.`disableTerminal` * `constraints/ainotebooks`.`environmentOptions` * `constraints/ainotebooks`.`requireAutoUpgradeSchedule` * `constraints/ainotebooks`.`restrictVpcNetworks` * `constraints/compute`.`disableGuestAttributesAccess` * `constraints/compute`.`disableInstanceDataAccessApis` * `constraints/compute`.`disableNestedVirtualization` * `constraints/compute`.`disableSerialPortAccess` * `constraints/compute`.`disableSerialPortLogging` * `constraints/compute`.`disableVpcExternalIpv6` * `constraints/compute`.`requireOsLogin` * `constraints/compute`.`requireShieldedVm` * `constraints/compute`.`restrictLoadBalancerCreationForTypes` * `constraints/compute`.`restrictProtocolForwardingCreationForTypes` * `constraints/compute`.`restrictXpnProjectLienRemoval` * `constraints/compute`.`setNewProjectDefaultToZonalDNSOnly` * `constraints/compute`.`skipDefaultNetworkCreation` * `constraints/compute`.`trustedImageProjects` * `constraints/compute`.`vmCanIpForward` * `constraints/compute`.`vmExternalIpAccess` * `constraints/gcp`.`detailedAuditLoggingMode` * `constraints/gcp`.`resourceLocations` * `constraints/iam`.`allowedPolicyMemberDomains` * `constraints/iam`.`automaticIamGrantsForDefaultServiceAccounts` * `constraints/iam`.`disableServiceAccountCreation` * `constraints/iam`.`disableServiceAccountKeyCreation` * `constraints/iam`.`disableServiceAccountKeyUpload` * `constraints/iam`.`restrictCrossProjectServiceAccountLienRemoval` * `constraints/iam`.`serviceAccountKeyExpiryHours` * `constraints/resourcemanager`.`accessBoundaries` * `constraints/resourcemanager`.`allowedExportDestinations` * `constraints/sql`.`restrictAuthorizedNetworks` * `constraints/sql`.`restrictNoncompliantDiagnosticDataAccess` * `constraints/sql`.`restrictNoncompliantResourceCreation` * `constraints/sql`.`restrictPublicIp` * `constraints/storage`.`publicAccessPrevention` * `constraints/storage`.`restrictAuthTypes` * `constraints/storage`.`uniformBucketLevelAccess` This RPC only returns either resources of types [supported by search APIs](<https://cloud.google.`com/asset-inventory/docs/supported-asset-types`>) or IAM policies.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudasset_analyze_org_policy_governed_assets_builder()` + `cloudasset_analyze_org_policy_governed_assets_execute()`.
/// For task-level control, use `cloudasset_analyze_org_policy_governed_assets_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_analyze_org_policy_governed_assets(
    client: &SimpleHttpClient,
    args: &CloudassetAnalyzeOrgPolicyGovernedAssetsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AnalyzeOrgPolicyGovernedAssetsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudasset_analyze_org_policy_governed_assets_builder(
        client,
        &args.scope,
        args.constraint.as_deref(),
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    cloudasset_analyze_org_policy_governed_assets_execute(builder)
}

/// GET v1/{v1Id}/{v1Id1}:analyzeOrgPolicyGovernedContainers
/// Analyzes organization policies governed containers (projects, folders or organization) under a scope.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudasset_analyze_org_policy_governed_containers_execute()` to send, or `cloudasset_analyze_org_policy_governed_containers` for simplest API.

pub fn cloudasset_analyze_org_policy_governed_containers_builder(
    client: &SimpleHttpClient,
    scope: &str,
    constraint: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudasset.googleapis.com/v1/{}/{}:analyzeOrgPolicyGovernedContainers",
        scope,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = constraint {
        query_parts.push(format!("constraint={}", val));
    }
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/{v1Id}/{v1Id1}:analyzeOrgPolicyGovernedContainers
/// Analyzes organization policies governed containers (projects, folders or organization) under a scope.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudasset_analyze_org_policy_governed_containers_execute()` or `cloudasset_analyze_org_policy_governed_containers`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_analyze_org_policy_governed_containers_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_analyze_org_policy_governed_containers_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<AnalyzeOrgPolicyGovernedContainersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AnalyzeOrgPolicyGovernedContainersResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}/{v1Id1}:analyzeOrgPolicyGovernedContainers
/// Analyzes organization policies governed containers (projects, folders or organization) under a scope.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudasset_analyze_org_policy_governed_containers_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudasset_analyze_org_policy_governed_containers_task()`.
/// For the simplest API, use `cloudasset_analyze_org_policy_governed_containers()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_analyze_org_policy_governed_containers_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudasset_analyze_org_policy_governed_containers_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AnalyzeOrgPolicyGovernedContainersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudasset_analyze_org_policy_governed_containers_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudasset_analyze_org_policy_governed_containers`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudassetAnalyzeOrgPolicyGovernedContainersArgs {
    /// Path parameter: scope
    pub scope: String,
    /// Query parameter: constraint
    pub constraint: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/{v1Id}/{v1Id1}:analyzeOrgPolicyGovernedContainers
/// Analyzes organization policies governed containers (projects, folders or organization) under a scope.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudasset_analyze_org_policy_governed_containers_builder()` + `cloudasset_analyze_org_policy_governed_containers_execute()`.
/// For task-level control, use `cloudasset_analyze_org_policy_governed_containers_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_analyze_org_policy_governed_containers(
    client: &SimpleHttpClient,
    args: &CloudassetAnalyzeOrgPolicyGovernedContainersArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AnalyzeOrgPolicyGovernedContainersResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudasset_analyze_org_policy_governed_containers_builder(
        client,
        &args.scope,
        args.constraint.as_deref(),
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    cloudasset_analyze_org_policy_governed_containers_execute(builder)
}

/// GET v1/{v1Id}/{v1Id1}:batchGetAssetsHistory
/// Batch gets the update history of assets that overlap a time window. For IAM_POLICY content, this API outputs history when the asset and its attached IAM POLICY both exist. This can create gaps in the output history. Otherwise, this API outputs history with asset in both non-delete or deleted status. If a specified asset does not exist, this API returns an INVALID_ARGUMENT error.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudasset_batch_get_assets_history_execute()` to send, or `cloudasset_batch_get_assets_history` for simplest API.

pub fn cloudasset_batch_get_assets_history_builder(
    client: &SimpleHttpClient,
    parent: &str,
    assetNames: Option<&str>,
    contentType: Option<&str>,
    readTimeWindow_endTime: Option<&str>,
    readTimeWindow_startTime: Option<&str>,
    relationshipTypes: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudasset.googleapis.com/v1/{}/{}:batchGetAssetsHistory",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = assetNames {
        query_parts.push(format!("assetNames={}", val));
    }
    if let Some(val) = contentType {
        query_parts.push(format!("contentType={}", val));
    }
    if let Some(val) = readTimeWindow_endTime {
        query_parts.push(format!("readTimeWindow_endTime={}", val));
    }
    if let Some(val) = readTimeWindow_startTime {
        query_parts.push(format!("readTimeWindow_startTime={}", val));
    }
    if let Some(val) = relationshipTypes {
        query_parts.push(format!("relationshipTypes={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/{v1Id}/{v1Id1}:batchGetAssetsHistory
/// Batch gets the update history of assets that overlap a time window. For IAM_POLICY content, this API outputs history when the asset and its attached IAM POLICY both exist. This can create gaps in the output history. Otherwise, this API outputs history with asset in both non-delete or deleted status. If a specified asset does not exist, this API returns an INVALID_ARGUMENT error.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudasset_batch_get_assets_history_execute()` or `cloudasset_batch_get_assets_history`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_batch_get_assets_history_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_batch_get_assets_history_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<BatchGetAssetsHistoryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchGetAssetsHistoryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}/{v1Id1}:batchGetAssetsHistory
/// Batch gets the update history of assets that overlap a time window. For IAM_POLICY content, this API outputs history when the asset and its attached IAM POLICY both exist. This can create gaps in the output history. Otherwise, this API outputs history with asset in both non-delete or deleted status. If a specified asset does not exist, this API returns an INVALID_ARGUMENT error.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudasset_batch_get_assets_history_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudasset_batch_get_assets_history_task()`.
/// For the simplest API, use `cloudasset_batch_get_assets_history()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_batch_get_assets_history_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudasset_batch_get_assets_history_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchGetAssetsHistoryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudasset_batch_get_assets_history_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudasset_batch_get_assets_history`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudassetBatchGetAssetsHistoryArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: assetNames
    pub assetNames: Option<String>,
    /// Query parameter: contentType
    pub contentType: Option<String>,
    /// Query parameter: readTimeWindow_endTime
    pub readTimeWindow_endTime: Option<String>,
    /// Query parameter: readTimeWindow_startTime
    pub readTimeWindow_startTime: Option<String>,
    /// Query parameter: relationshipTypes
    pub relationshipTypes: Option<String>,
}

/// GET v1/{v1Id}/{v1Id1}:batchGetAssetsHistory
/// Batch gets the update history of assets that overlap a time window. For IAM_POLICY content, this API outputs history when the asset and its attached IAM POLICY both exist. This can create gaps in the output history. Otherwise, this API outputs history with asset in both non-delete or deleted status. If a specified asset does not exist, this API returns an INVALID_ARGUMENT error.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudasset_batch_get_assets_history_builder()` + `cloudasset_batch_get_assets_history_execute()`.
/// For task-level control, use `cloudasset_batch_get_assets_history_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_batch_get_assets_history(
    client: &SimpleHttpClient,
    args: &CloudassetBatchGetAssetsHistoryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchGetAssetsHistoryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudasset_batch_get_assets_history_builder(
        client,
        &args.parent,
        args.assetNames.as_deref(),
        args.contentType.as_deref(),
        args.readTimeWindow_endTime.as_deref(),
        args.readTimeWindow_startTime.as_deref(),
        args.relationshipTypes.as_deref(),
    )?;
    cloudasset_batch_get_assets_history_execute(builder)
}

/// GET v1/{v1Id}/{v1Id1}:exportAssets
/// Exports assets with time and resource types to a given Cloud Storage `location/BigQuery` table. For Cloud Storage location destinations, the output format is newline-delimited JSON. Each line represents a google.cloud.asset.v1.Asset in the JSON format; for BigQuery table destinations, the output table stores the fields in asset Protobuf as columns. This API implements the google.longrunning.Operation API, which allows you to keep track of the export. We recommend intervals of at least 2 seconds with exponential retry to poll the export operation result. For regular-size resource parent, the export operation usually finishes within 5 minutes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudasset_export_assets_execute()` to send, or `cloudasset_export_assets` for simplest API.

pub fn cloudasset_export_assets_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &ExportAssetsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudasset.googleapis.com/v1/{}/{}:exportAssets",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{v1Id}/{v1Id1}:exportAssets
/// Exports assets with time and resource types to a given Cloud Storage `location/BigQuery` table. For Cloud Storage location destinations, the output format is newline-delimited JSON. Each line represents a google.cloud.asset.v1.Asset in the JSON format; for BigQuery table destinations, the output table stores the fields in asset Protobuf as columns. This API implements the google.longrunning.Operation API, which allows you to keep track of the export. We recommend intervals of at least 2 seconds with exponential retry to poll the export operation result. For regular-size resource parent, the export operation usually finishes within 5 minutes.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudasset_export_assets_execute()` or `cloudasset_export_assets`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_export_assets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_export_assets_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}/{v1Id1}:exportAssets
/// Exports assets with time and resource types to a given Cloud Storage `location/BigQuery` table. For Cloud Storage location destinations, the output format is newline-delimited JSON. Each line represents a google.cloud.asset.v1.Asset in the JSON format; for BigQuery table destinations, the output table stores the fields in asset Protobuf as columns. This API implements the google.longrunning.Operation API, which allows you to keep track of the export. We recommend intervals of at least 2 seconds with exponential retry to poll the export operation result. For regular-size resource parent, the export operation usually finishes within 5 minutes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudasset_export_assets_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudasset_export_assets_task()`.
/// For the simplest API, use `cloudasset_export_assets()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_export_assets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudasset_export_assets_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = cloudasset_export_assets_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudasset_export_assets`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudassetExportAssetsArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: ExportAssetsRequest,
}

/// GET v1/{v1Id}/{v1Id1}:exportAssets
/// Exports assets with time and resource types to a given Cloud Storage `location/BigQuery` table. For Cloud Storage location destinations, the output format is newline-delimited JSON. Each line represents a google.cloud.asset.v1.Asset in the JSON format; for BigQuery table destinations, the output table stores the fields in asset Protobuf as columns. This API implements the google.longrunning.Operation API, which allows you to keep track of the export. We recommend intervals of at least 2 seconds with exponential retry to poll the export operation result. For regular-size resource parent, the export operation usually finishes within 5 minutes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudasset_export_assets_builder()` + `cloudasset_export_assets_execute()`.
/// For task-level control, use `cloudasset_export_assets_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_export_assets(
    client: &SimpleHttpClient,
    args: &CloudassetExportAssetsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudasset_export_assets_builder(client, &args.parent, &args.body)?;
    cloudasset_export_assets_execute(builder)
}

/// GET v1/{v1Id}/{v1Id1}:queryAssets
/// Issue a job that queries assets using a SQL statement compatible with [BigQuery SQL](<https://cloud.google.`com/bigquery/docs/introduction-sql`>). If the query execution finishes within timeout and there's no pagination, the full query results will be returned in the QueryAssetsResponse. Otherwise, full query results can be obtained by issuing extra requests with the job_reference from the a previous QueryAssets call. Note, the query result has approximately 10 GB limitation enforced by [BigQuery](<https://cloud.google.`com/bigquery/docs/best-practices-performance-output`>). Queries return larger results will result in errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudasset_query_assets_execute()` to send, or `cloudasset_query_assets` for simplest API.

pub fn cloudasset_query_assets_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &QueryAssetsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudasset.googleapis.com/v1/{}/{}:queryAssets",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{v1Id}/{v1Id1}:queryAssets
/// Issue a job that queries assets using a SQL statement compatible with [BigQuery SQL](<https://cloud.google.`com/bigquery/docs/introduction-sql`>). If the query execution finishes within timeout and there's no pagination, the full query results will be returned in the QueryAssetsResponse. Otherwise, full query results can be obtained by issuing extra requests with the job_reference from the a previous QueryAssets call. Note, the query result has approximately 10 GB limitation enforced by [BigQuery](<https://cloud.google.`com/bigquery/docs/best-practices-performance-output`>). Queries return larger results will result in errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudasset_query_assets_execute()` or `cloudasset_query_assets`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_query_assets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_query_assets_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<QueryAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: QueryAssetsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}/{v1Id1}:queryAssets
/// Issue a job that queries assets using a SQL statement compatible with [BigQuery SQL](<https://cloud.google.`com/bigquery/docs/introduction-sql`>). If the query execution finishes within timeout and there's no pagination, the full query results will be returned in the QueryAssetsResponse. Otherwise, full query results can be obtained by issuing extra requests with the job_reference from the a previous QueryAssets call. Note, the query result has approximately 10 GB limitation enforced by [BigQuery](<https://cloud.google.`com/bigquery/docs/best-practices-performance-output`>). Queries return larger results will result in errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudasset_query_assets_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudasset_query_assets_task()`.
/// For the simplest API, use `cloudasset_query_assets()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_query_assets_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudasset_query_assets_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<QueryAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = cloudasset_query_assets_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudasset_query_assets`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudassetQueryAssetsArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: QueryAssetsRequest,
}

/// GET v1/{v1Id}/{v1Id1}:queryAssets
/// Issue a job that queries assets using a SQL statement compatible with [BigQuery SQL](<https://cloud.google.`com/bigquery/docs/introduction-sql`>). If the query execution finishes within timeout and there's no pagination, the full query results will be returned in the QueryAssetsResponse. Otherwise, full query results can be obtained by issuing extra requests with the job_reference from the a previous QueryAssets call. Note, the query result has approximately 10 GB limitation enforced by [BigQuery](<https://cloud.google.`com/bigquery/docs/best-practices-performance-output`>). Queries return larger results will result in errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudasset_query_assets_builder()` + `cloudasset_query_assets_execute()`.
/// For task-level control, use `cloudasset_query_assets_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_query_assets(
    client: &SimpleHttpClient,
    args: &CloudassetQueryAssetsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<QueryAssetsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudasset_query_assets_builder(client, &args.parent, &args.body)?;
    cloudasset_query_assets_execute(builder)
}

/// GET v1/{v1Id}/{v1Id1}:searchAllIamPolicies
/// Searches all IAM policies within the specified scope, such as a project, folder, or organization. The caller must be granted the cloudasset.assets.`searchAllIamPolicies` permission on the desired scope, otherwise the request will be rejected.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudasset_search_all_iam_policies_execute()` to send, or `cloudasset_search_all_iam_policies` for simplest API.

pub fn cloudasset_search_all_iam_policies_builder(
    client: &SimpleHttpClient,
    scope: &str,
    assetTypes: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    query: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudasset.googleapis.com/v1/{}/{}:searchAllIamPolicies",
        scope,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = assetTypes {
        query_parts.push(format!("assetTypes={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = query {
        query_parts.push(format!("query={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/{v1Id}/{v1Id1}:searchAllIamPolicies
/// Searches all IAM policies within the specified scope, such as a project, folder, or organization. The caller must be granted the cloudasset.assets.`searchAllIamPolicies` permission on the desired scope, otherwise the request will be rejected.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudasset_search_all_iam_policies_execute()` or `cloudasset_search_all_iam_policies`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_search_all_iam_policies_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_search_all_iam_policies_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            D = Result<ApiResponse<SearchAllIamPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchAllIamPoliciesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}/{v1Id1}:searchAllIamPolicies
/// Searches all IAM policies within the specified scope, such as a project, folder, or organization. The caller must be granted the cloudasset.assets.`searchAllIamPolicies` permission on the desired scope, otherwise the request will be rejected.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudasset_search_all_iam_policies_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudasset_search_all_iam_policies_task()`.
/// For the simplest API, use `cloudasset_search_all_iam_policies()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_search_all_iam_policies_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudasset_search_all_iam_policies_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchAllIamPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudasset_search_all_iam_policies_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudasset_search_all_iam_policies`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudassetSearchAllIamPoliciesArgs {
    /// Path parameter: scope
    pub scope: String,
    /// Query parameter: assetTypes
    pub assetTypes: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: query
    pub query: Option<String>,
}

/// GET v1/{v1Id}/{v1Id1}:searchAllIamPolicies
/// Searches all IAM policies within the specified scope, such as a project, folder, or organization. The caller must be granted the cloudasset.assets.`searchAllIamPolicies` permission on the desired scope, otherwise the request will be rejected.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudasset_search_all_iam_policies_builder()` + `cloudasset_search_all_iam_policies_execute()`.
/// For task-level control, use `cloudasset_search_all_iam_policies_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_search_all_iam_policies(
    client: &SimpleHttpClient,
    args: &CloudassetSearchAllIamPoliciesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchAllIamPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudasset_search_all_iam_policies_builder(
        client,
        &args.scope,
        args.assetTypes.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.query.as_deref(),
    )?;
    cloudasset_search_all_iam_policies_execute(builder)
}

/// GET v1/{v1Id}/{v1Id1}:searchAllResources
/// Searches all Google Cloud resources within the specified scope, such as a project, folder, or organization. The caller must be granted the cloudasset.assets.`searchAllResources` permission on the desired scope, otherwise the request will be rejected.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudasset_search_all_resources_execute()` to send, or `cloudasset_search_all_resources` for simplest API.

pub fn cloudasset_search_all_resources_builder(
    client: &SimpleHttpClient,
    scope: &str,
    assetTypes: Option<&str>,
    orderBy: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    query: Option<&str>,
    readMask: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://cloudasset.googleapis.com/v1/{}/{}:searchAllResources",
        scope,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = assetTypes {
        query_parts.push(format!("assetTypes={}", val));
    }
    if let Some(val) = orderBy {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = query {
        query_parts.push(format!("query={}", val));
    }
    if let Some(val) = readMask {
        query_parts.push(format!("readMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/{v1Id}/{v1Id1}:searchAllResources
/// Searches all Google Cloud resources within the specified scope, such as a project, folder, or organization. The caller must be granted the cloudasset.assets.`searchAllResources` permission on the desired scope, otherwise the request will be rejected.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `cloudasset_search_all_resources_execute()` or `cloudasset_search_all_resources`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_search_all_resources_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_search_all_resources_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<D = Result<ApiResponse<SearchAllResourcesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchAllResourcesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}/{v1Id1}:searchAllResources
/// Searches all Google Cloud resources within the specified scope, such as a project, folder, or organization. The caller must be granted the cloudasset.assets.`searchAllResources` permission on the desired scope, otherwise the request will be rejected.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `cloudasset_search_all_resources_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `cloudasset_search_all_resources_task()`.
/// For the simplest API, use `cloudasset_search_all_resources()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `cloudasset_search_all_resources_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn cloudasset_search_all_resources_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchAllResourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = cloudasset_search_all_resources_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`cloudasset_search_all_resources`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudassetSearchAllResourcesArgs {
    /// Path parameter: scope
    pub scope: String,
    /// Query parameter: assetTypes
    pub assetTypes: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: query
    pub query: Option<String>,
    /// Query parameter: readMask
    pub readMask: Option<String>,
}

/// GET v1/{v1Id}/{v1Id1}:searchAllResources
/// Searches all Google Cloud resources within the specified scope, such as a project, folder, or organization. The caller must be granted the cloudasset.assets.`searchAllResources` permission on the desired scope, otherwise the request will be rejected.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudasset_search_all_resources_builder()` + `cloudasset_search_all_resources_execute()`.
/// For task-level control, use `cloudasset_search_all_resources_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudasset_search_all_resources(
    client: &SimpleHttpClient,
    args: &CloudassetSearchAllResourcesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchAllResourcesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = cloudasset_search_all_resources_builder(
        client,
        &args.scope,
        args.assetTypes.as_deref(),
        args.orderBy.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.query.as_deref(),
        args.readMask.as_deref(),
    )?;
    cloudasset_search_all_resources_execute(builder)
}
