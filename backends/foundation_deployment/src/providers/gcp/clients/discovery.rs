//! Auto-generated API clients for gcp/discovery.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET apis/{api}/{version}/rest
/// Retrieve the description of a particular version of an api.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discovery_apis_get_rest_execute()` to send, or `discovery_apis_get_rest` for simplest API.

pub fn discovery_apis_get_rest_builder(
    client: &SimpleHttpClient,
    api: &String,
    version: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://www.googleapis.com/discovery/v1/apis/{}/{}/rest",
        api, version,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET apis/{api}/{version}/rest
/// Retrieve the description of a particular version of an api.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `discovery_apis_get_rest_execute()` or `discovery_apis_get_rest`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `discovery_apis_get_rest_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discovery_apis_get_rest_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RestDescription>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RestDescription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET apis/{api}/{version}/rest
/// Retrieve the description of a particular version of an api.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `discovery_apis_get_rest_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `discovery_apis_get_rest_task()`.
/// For the simplest API, use `discovery_apis_get_rest()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `discovery_apis_get_rest_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn discovery_apis_get_rest_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RestDescription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = discovery_apis_get_rest_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`discovery_apis_get_rest`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DiscoveryApisGetRestArgs {
    /// Path parameter: api
    pub api: String,
    /// Path parameter: version
    pub version: String,
}

/// GET apis/{api}/{version}/rest
/// Retrieve the description of a particular version of an api.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discovery_apis_get_rest_builder()` + `discovery_apis_get_rest_execute()`.
/// For task-level control, use `discovery_apis_get_rest_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discovery_apis_get_rest(
    client: &SimpleHttpClient,
    args: &DiscoveryApisGetRestArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RestDescription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = discovery_apis_get_rest_builder(client, &args.api, &args.version)?;
    discovery_apis_get_rest_execute(builder)
}

/// GET apis
/// Retrieve the list of APIs supported at this endpoint.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discovery_apis_list_execute()` to send, or `discovery_apis_list` for simplest API.

pub fn discovery_apis_list_builder(
    client: &SimpleHttpClient,
    name: &Option<Option<String>>,
    preferred: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://www.googleapis.com/discovery/v1/apis",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = name.as_ref() {
        query_parts.push(format!("name={}", val));
    }
    if let Some(val) = preferred.as_ref() {
        query_parts.push(format!("preferred={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET apis
/// Retrieve the list of APIs supported at this endpoint.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `discovery_apis_list_execute()` or `discovery_apis_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `discovery_apis_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discovery_apis_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DirectoryList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DirectoryList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET apis
/// Retrieve the list of APIs supported at this endpoint.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `discovery_apis_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `discovery_apis_list_task()`.
/// For the simplest API, use `discovery_apis_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `discovery_apis_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn discovery_apis_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DirectoryList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = discovery_apis_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`discovery_apis_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DiscoveryApisListArgs {
    /// Query parameter: name
    pub name: Option<Option<String>>,
    /// Query parameter: preferred
    pub preferred: Option<Option<String>>,
}

/// GET apis
/// Retrieve the list of APIs supported at this endpoint.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discovery_apis_list_builder()` + `discovery_apis_list_execute()`.
/// For task-level control, use `discovery_apis_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discovery_apis_list(
    client: &SimpleHttpClient,
    args: &DiscoveryApisListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DirectoryList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = discovery_apis_list_builder(client, &args.name, &args.preferred)?;
    discovery_apis_list_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for RestDescription
// =============================================================================

/// ResourceIdentifier implementation for RestDescription with DiscoveryApisGetRestArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DiscoveryApisGetRestArgs> for RestDescription {
    fn generate_resource_id(&self, input: &DiscoveryApisGetRestArgs) -> String {
        format!(
            "gcp::discovery::RestDescription/{}/{}",
            input.api, input.version
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::discovery::RestDescription"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DirectoryList
// =============================================================================

/// ResourceIdentifier implementation for DirectoryList with DiscoveryApisListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<DiscoveryApisListArgs> for DirectoryList {
    fn generate_resource_id(&self, input: &DiscoveryApisListArgs) -> String {
        "gcp::discovery::DirectoryList".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::discovery::DirectoryList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
