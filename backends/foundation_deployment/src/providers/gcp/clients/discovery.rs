//! Auto-generated API clients for Google Cloud Platform - discovery.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET apis/{api}/{version}/rest
/// Retrieve the description of a particular version of an api.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discovery_apis_get_rest_execute()` to send, or `discovery_apis_get_rest` for simplest API.

pub fn discovery_apis_get_rest_builder(
    client: &SimpleHttpClient,
    api: &str,
    version: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comapis/{}/{}/rest", api, version,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET apis/{api}/{version}/rest
/// Retrieve the description of a particular version of an api.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discovery_apis_get_rest_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discovery_apis_get_rest_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discovery_apis_get_rest_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RestDescription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RestDescription = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET apis/{api}/{version}/rest
/// Retrieve the description of a particular version of an api.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discovery_apis_get_rest_builder()` + `discovery_apis_get_rest_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discovery_apis_get_rest(
    client: &SimpleHttpClient,
    api: &str,
    version: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RestDescription>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = discovery_apis_get_rest_builder(client, api, version)?;
    discovery_apis_get_rest_execute(builder)
}

/// GET apis
/// Retrieve the list of APIs supported at this endpoint.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `discovery_apis_list_execute()` to send, or `discovery_apis_list` for simplest API.

pub fn discovery_apis_list_builder(
    client: &SimpleHttpClient,
    name: Option<&str>,
    preferred: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comapis",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "name",
            name.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "preferred",
            preferred
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET apis
/// Retrieve the list of APIs supported at this endpoint.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `discovery_apis_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from discovery_apis_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discovery_apis_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DirectoryList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DirectoryList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET apis
/// Retrieve the list of APIs supported at this endpoint.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `discovery_apis_list_builder()` + `discovery_apis_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn discovery_apis_list(
    client: &SimpleHttpClient,
    name: Option<&str>,
    preferred: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DirectoryList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = discovery_apis_list_builder(client, name, preferred)?;
    discovery_apis_list_execute(builder)
}
