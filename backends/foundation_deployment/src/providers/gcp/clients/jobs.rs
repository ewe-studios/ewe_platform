//! Auto-generated API clients for Google Cloud Platform - jobs.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v4/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `jobs_projects_operations_get_execute()` to send, or `jobs_projects_operations_get` for simplest API.

pub fn jobs_projects_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv4/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `jobs_projects_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from jobs_projects_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/{+name}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `jobs_projects_operations_get_builder()` + `jobs_projects_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = jobs_projects_operations_get_builder(client, name)?;
    jobs_projects_operations_get_execute(builder)
}

/// GET v4/{+tenant}:completeQuery
/// Completes the specified prefix with keyword suggestions. Intended for use by a job search auto-complete search box.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `jobs_projects_tenants_complete_query_execute()` to send, or `jobs_projects_tenants_complete_query` for simplest API.

pub fn jobs_projects_tenants_complete_query_builder(
    client: &SimpleHttpClient,
    tenant: &str,
    company: Option<&str>,
    languageCodes: Option<&str>,
    pageSize: Option<i32>,
    query: Option<&str>,
    scope: Option<&str>,
    type_rs: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv4/{+tenant}:completeQuery", tenant,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "company",
            company
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "languageCodes",
            languageCodes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "query",
            query
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "scope",
            scope
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "type",
            type_rs
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v4/{+tenant}:completeQuery
/// Completes the specified prefix with keyword suggestions. Intended for use by a job search auto-complete search box.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `jobs_projects_tenants_complete_query_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from jobs_projects_tenants_complete_query_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_complete_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CompleteQueryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CompleteQueryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/{+tenant}:completeQuery
/// Completes the specified prefix with keyword suggestions. Intended for use by a job search auto-complete search box.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `jobs_projects_tenants_complete_query_builder()` + `jobs_projects_tenants_complete_query_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_complete_query(
    client: &SimpleHttpClient,
    tenant: &str,
    company: Option<&str>,
    languageCodes: Option<&str>,
    pageSize: Option<i32>,
    query: Option<&str>,
    scope: Option<&str>,
    type_rs: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CompleteQueryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = jobs_projects_tenants_complete_query_builder(
        client,
        tenant,
        company,
        languageCodes,
        pageSize,
        query,
        scope,
        type_rs,
    )?;
    jobs_projects_tenants_complete_query_execute(builder)
}

/// GET v4/{+parent}/tenants
/// Creates a new tenant entity.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `jobs_projects_tenants_create_execute()` to send, or `jobs_projects_tenants_create` for simplest API.

pub fn jobs_projects_tenants_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Tenant,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv4/{+parent}/tenants", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v4/{+parent}/tenants
/// Creates a new tenant entity.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `jobs_projects_tenants_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from jobs_projects_tenants_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Tenant>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Tenant = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/{+parent}/tenants
/// Creates a new tenant entity.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `jobs_projects_tenants_create_builder()` + `jobs_projects_tenants_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Tenant,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Tenant>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = jobs_projects_tenants_create_builder(client, parent, body)?;
    jobs_projects_tenants_create_execute(builder)
}

/// GET v4/{+name}
/// Deletes specified tenant.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `jobs_projects_tenants_delete_execute()` to send, or `jobs_projects_tenants_delete` for simplest API.

pub fn jobs_projects_tenants_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv4/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/{+name}
/// Deletes specified tenant.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `jobs_projects_tenants_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from jobs_projects_tenants_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/{+name}
/// Deletes specified tenant.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `jobs_projects_tenants_delete_builder()` + `jobs_projects_tenants_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = jobs_projects_tenants_delete_builder(client, name)?;
    jobs_projects_tenants_delete_execute(builder)
}

/// GET v4/{+name}
/// Retrieves specified tenant.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `jobs_projects_tenants_get_execute()` to send, or `jobs_projects_tenants_get` for simplest API.

pub fn jobs_projects_tenants_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv4/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/{+name}
/// Retrieves specified tenant.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `jobs_projects_tenants_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from jobs_projects_tenants_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Tenant>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Tenant = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/{+name}
/// Retrieves specified tenant.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `jobs_projects_tenants_get_builder()` + `jobs_projects_tenants_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Tenant>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = jobs_projects_tenants_get_builder(client, name)?;
    jobs_projects_tenants_get_execute(builder)
}

/// GET v4/{+parent}/tenants
/// Lists all tenants associated with the project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `jobs_projects_tenants_list_execute()` to send, or `jobs_projects_tenants_list` for simplest API.

pub fn jobs_projects_tenants_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv4/{+parent}/tenants", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v4/{+parent}/tenants
/// Lists all tenants associated with the project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `jobs_projects_tenants_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from jobs_projects_tenants_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTenantsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTenantsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/{+parent}/tenants
/// Lists all tenants associated with the project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `jobs_projects_tenants_list_builder()` + `jobs_projects_tenants_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTenantsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = jobs_projects_tenants_list_builder(client, parent, pageSize, pageToken)?;
    jobs_projects_tenants_list_execute(builder)
}

/// GET v4/{+name}
/// Updates specified tenant.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `jobs_projects_tenants_patch_execute()` to send, or `jobs_projects_tenants_patch` for simplest API.

pub fn jobs_projects_tenants_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Tenant,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv4/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v4/{+name}
/// Updates specified tenant.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `jobs_projects_tenants_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from jobs_projects_tenants_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Tenant>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Tenant = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/{+name}
/// Updates specified tenant.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `jobs_projects_tenants_patch_builder()` + `jobs_projects_tenants_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Tenant,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Tenant>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = jobs_projects_tenants_patch_builder(client, name, updateMask, body)?;
    jobs_projects_tenants_patch_execute(builder)
}

/// GET v4/{+parent}/clientEvents
/// Report events issued when end user interacts with customer's application that uses Cloud Talent Solution. You may inspect the created events in [self service tools](<https://console.cloud.google.`com/talent-solution/overview`>). [Learn more](<https://cloud.google.`com/talent-solution/docs/management-tools`>) about self service tools.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `jobs_projects_tenants_client_events_create_execute()` to send, or `jobs_projects_tenants_client_events_create` for simplest API.

pub fn jobs_projects_tenants_client_events_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &ClientEvent,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv4/{+parent}/clientEvents", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v4/{+parent}/clientEvents
/// Report events issued when end user interacts with customer's application that uses Cloud Talent Solution. You may inspect the created events in [self service tools](<https://console.cloud.google.`com/talent-solution/overview`>). [Learn more](<https://cloud.google.`com/talent-solution/docs/management-tools`>) about self service tools.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `jobs_projects_tenants_client_events_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from jobs_projects_tenants_client_events_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_client_events_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientEvent>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ClientEvent = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/{+parent}/clientEvents
/// Report events issued when end user interacts with customer's application that uses Cloud Talent Solution. You may inspect the created events in [self service tools](<https://console.cloud.google.`com/talent-solution/overview`>). [Learn more](<https://cloud.google.`com/talent-solution/docs/management-tools`>) about self service tools.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `jobs_projects_tenants_client_events_create_builder()` + `jobs_projects_tenants_client_events_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_client_events_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &ClientEvent,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClientEvent>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = jobs_projects_tenants_client_events_create_builder(client, parent, body)?;
    jobs_projects_tenants_client_events_create_execute(builder)
}

/// GET v4/{+parent}/companies
/// Creates a new company entity.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `jobs_projects_tenants_companies_create_execute()` to send, or `jobs_projects_tenants_companies_create` for simplest API.

pub fn jobs_projects_tenants_companies_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Company,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv4/{+parent}/companies", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v4/{+parent}/companies
/// Creates a new company entity.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `jobs_projects_tenants_companies_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from jobs_projects_tenants_companies_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_companies_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Company>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Company = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/{+parent}/companies
/// Creates a new company entity.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `jobs_projects_tenants_companies_create_builder()` + `jobs_projects_tenants_companies_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_companies_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Company,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Company>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = jobs_projects_tenants_companies_create_builder(client, parent, body)?;
    jobs_projects_tenants_companies_create_execute(builder)
}

/// GET v4/{+name}
/// Deletes specified company. Prerequisite: The company has no jobs associated with it.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `jobs_projects_tenants_companies_delete_execute()` to send, or `jobs_projects_tenants_companies_delete` for simplest API.

pub fn jobs_projects_tenants_companies_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv4/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/{+name}
/// Deletes specified company. Prerequisite: The company has no jobs associated with it.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `jobs_projects_tenants_companies_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from jobs_projects_tenants_companies_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_companies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/{+name}
/// Deletes specified company. Prerequisite: The company has no jobs associated with it.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `jobs_projects_tenants_companies_delete_builder()` + `jobs_projects_tenants_companies_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_companies_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = jobs_projects_tenants_companies_delete_builder(client, name)?;
    jobs_projects_tenants_companies_delete_execute(builder)
}

/// GET v4/{+name}
/// Retrieves specified company.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `jobs_projects_tenants_companies_get_execute()` to send, or `jobs_projects_tenants_companies_get` for simplest API.

pub fn jobs_projects_tenants_companies_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv4/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/{+name}
/// Retrieves specified company.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `jobs_projects_tenants_companies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from jobs_projects_tenants_companies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_companies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Company>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Company = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/{+name}
/// Retrieves specified company.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `jobs_projects_tenants_companies_get_builder()` + `jobs_projects_tenants_companies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_companies_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Company>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = jobs_projects_tenants_companies_get_builder(client, name)?;
    jobs_projects_tenants_companies_get_execute(builder)
}

/// GET v4/{+parent}/companies
/// Lists all companies associated with the project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `jobs_projects_tenants_companies_list_execute()` to send, or `jobs_projects_tenants_companies_list` for simplest API.

pub fn jobs_projects_tenants_companies_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    requireOpenJobs: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv4/{+parent}/companies", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "requireOpenJobs",
            requireOpenJobs
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v4/{+parent}/companies
/// Lists all companies associated with the project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `jobs_projects_tenants_companies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from jobs_projects_tenants_companies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_companies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCompaniesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCompaniesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/{+parent}/companies
/// Lists all companies associated with the project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `jobs_projects_tenants_companies_list_builder()` + `jobs_projects_tenants_companies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_companies_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    requireOpenJobs: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCompaniesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = jobs_projects_tenants_companies_list_builder(
        client,
        parent,
        pageSize,
        pageToken,
        requireOpenJobs,
    )?;
    jobs_projects_tenants_companies_list_execute(builder)
}

/// GET v4/{+name}
/// Updates specified company.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `jobs_projects_tenants_companies_patch_execute()` to send, or `jobs_projects_tenants_companies_patch` for simplest API.

pub fn jobs_projects_tenants_companies_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Company,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv4/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v4/{+name}
/// Updates specified company.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `jobs_projects_tenants_companies_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from jobs_projects_tenants_companies_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_companies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Company>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Company = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/{+name}
/// Updates specified company.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `jobs_projects_tenants_companies_patch_builder()` + `jobs_projects_tenants_companies_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_companies_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Company,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Company>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = jobs_projects_tenants_companies_patch_builder(client, name, updateMask, body)?;
    jobs_projects_tenants_companies_patch_execute(builder)
}

/// GET v4/{+parent}/jobs:batchCreate
/// Begins executing a batch create jobs operation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `jobs_projects_tenants_jobs_batch_create_execute()` to send, or `jobs_projects_tenants_jobs_batch_create` for simplest API.

pub fn jobs_projects_tenants_jobs_batch_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &BatchCreateJobsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv4/{+parent}/jobs:batchCreate",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v4/{+parent}/jobs:batchCreate
/// Begins executing a batch create jobs operation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `jobs_projects_tenants_jobs_batch_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from jobs_projects_tenants_jobs_batch_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_jobs_batch_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/{+parent}/jobs:batchCreate
/// Begins executing a batch create jobs operation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `jobs_projects_tenants_jobs_batch_create_builder()` + `jobs_projects_tenants_jobs_batch_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_jobs_batch_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &BatchCreateJobsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = jobs_projects_tenants_jobs_batch_create_builder(client, parent, body)?;
    jobs_projects_tenants_jobs_batch_create_execute(builder)
}

/// GET v4/{+parent}/jobs:batchDelete
/// Begins executing a batch delete jobs operation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `jobs_projects_tenants_jobs_batch_delete_execute()` to send, or `jobs_projects_tenants_jobs_batch_delete` for simplest API.

pub fn jobs_projects_tenants_jobs_batch_delete_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &BatchDeleteJobsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv4/{+parent}/jobs:batchDelete",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v4/{+parent}/jobs:batchDelete
/// Begins executing a batch delete jobs operation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `jobs_projects_tenants_jobs_batch_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from jobs_projects_tenants_jobs_batch_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_jobs_batch_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/{+parent}/jobs:batchDelete
/// Begins executing a batch delete jobs operation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `jobs_projects_tenants_jobs_batch_delete_builder()` + `jobs_projects_tenants_jobs_batch_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_jobs_batch_delete(
    client: &SimpleHttpClient,
    parent: &str,
    body: &BatchDeleteJobsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = jobs_projects_tenants_jobs_batch_delete_builder(client, parent, body)?;
    jobs_projects_tenants_jobs_batch_delete_execute(builder)
}

/// GET v4/{+parent}/jobs:batchUpdate
/// Begins executing a batch update jobs operation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `jobs_projects_tenants_jobs_batch_update_execute()` to send, or `jobs_projects_tenants_jobs_batch_update` for simplest API.

pub fn jobs_projects_tenants_jobs_batch_update_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &BatchUpdateJobsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv4/{+parent}/jobs:batchUpdate",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v4/{+parent}/jobs:batchUpdate
/// Begins executing a batch update jobs operation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `jobs_projects_tenants_jobs_batch_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from jobs_projects_tenants_jobs_batch_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_jobs_batch_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/{+parent}/jobs:batchUpdate
/// Begins executing a batch update jobs operation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `jobs_projects_tenants_jobs_batch_update_builder()` + `jobs_projects_tenants_jobs_batch_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_jobs_batch_update(
    client: &SimpleHttpClient,
    parent: &str,
    body: &BatchUpdateJobsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = jobs_projects_tenants_jobs_batch_update_builder(client, parent, body)?;
    jobs_projects_tenants_jobs_batch_update_execute(builder)
}

/// GET v4/{+parent}/jobs
/// Creates a new job. Typically, the job becomes searchable within 10 seconds, but it may take up to 5 minutes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `jobs_projects_tenants_jobs_create_execute()` to send, or `jobs_projects_tenants_jobs_create` for simplest API.

pub fn jobs_projects_tenants_jobs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Job,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv4/{+parent}/jobs", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v4/{+parent}/jobs
/// Creates a new job. Typically, the job becomes searchable within 10 seconds, but it may take up to 5 minutes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `jobs_projects_tenants_jobs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from jobs_projects_tenants_jobs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/{+parent}/jobs
/// Creates a new job. Typically, the job becomes searchable within 10 seconds, but it may take up to 5 minutes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `jobs_projects_tenants_jobs_create_builder()` + `jobs_projects_tenants_jobs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_jobs_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Job,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = jobs_projects_tenants_jobs_create_builder(client, parent, body)?;
    jobs_projects_tenants_jobs_create_execute(builder)
}

/// GET v4/{+name}
/// Deletes the specified job. Typically, the job becomes unsearchable within 10 seconds, but it may take up to 5 minutes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `jobs_projects_tenants_jobs_delete_execute()` to send, or `jobs_projects_tenants_jobs_delete` for simplest API.

pub fn jobs_projects_tenants_jobs_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv4/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/{+name}
/// Deletes the specified job. Typically, the job becomes unsearchable within 10 seconds, but it may take up to 5 minutes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `jobs_projects_tenants_jobs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from jobs_projects_tenants_jobs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_jobs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/{+name}
/// Deletes the specified job. Typically, the job becomes unsearchable within 10 seconds, but it may take up to 5 minutes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `jobs_projects_tenants_jobs_delete_builder()` + `jobs_projects_tenants_jobs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_jobs_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = jobs_projects_tenants_jobs_delete_builder(client, name)?;
    jobs_projects_tenants_jobs_delete_execute(builder)
}

/// GET v4/{+name}
/// Retrieves the specified job, whose status is OPEN or recently EXPIRED within the last 90 days.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `jobs_projects_tenants_jobs_get_execute()` to send, or `jobs_projects_tenants_jobs_get` for simplest API.

pub fn jobs_projects_tenants_jobs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv4/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/{+name}
/// Retrieves the specified job, whose status is OPEN or recently EXPIRED within the last 90 days.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `jobs_projects_tenants_jobs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from jobs_projects_tenants_jobs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_jobs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/{+name}
/// Retrieves the specified job, whose status is OPEN or recently EXPIRED within the last 90 days.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `jobs_projects_tenants_jobs_get_builder()` + `jobs_projects_tenants_jobs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_jobs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = jobs_projects_tenants_jobs_get_builder(client, name)?;
    jobs_projects_tenants_jobs_get_execute(builder)
}

/// GET v4/{+parent}/jobs
/// Lists jobs by filter.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `jobs_projects_tenants_jobs_list_execute()` to send, or `jobs_projects_tenants_jobs_list` for simplest API.

pub fn jobs_projects_tenants_jobs_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    jobView: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv4/{+parent}/jobs", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "jobView",
            jobView
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v4/{+parent}/jobs
/// Lists jobs by filter.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `jobs_projects_tenants_jobs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from jobs_projects_tenants_jobs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_jobs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/{+parent}/jobs
/// Lists jobs by filter.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `jobs_projects_tenants_jobs_list_builder()` + `jobs_projects_tenants_jobs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_jobs_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    jobView: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = jobs_projects_tenants_jobs_list_builder(
        client, parent, filter, jobView, pageSize, pageToken,
    )?;
    jobs_projects_tenants_jobs_list_execute(builder)
}

/// GET v4/{+name}
/// Updates specified job. Typically, updated contents become visible in search results within 10 seconds, but it may take up to 5 minutes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `jobs_projects_tenants_jobs_patch_execute()` to send, or `jobs_projects_tenants_jobs_patch` for simplest API.

pub fn jobs_projects_tenants_jobs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Job,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv4/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v4/{+name}
/// Updates specified job. Typically, updated contents become visible in search results within 10 seconds, but it may take up to 5 minutes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `jobs_projects_tenants_jobs_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from jobs_projects_tenants_jobs_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_jobs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/{+name}
/// Updates specified job. Typically, updated contents become visible in search results within 10 seconds, but it may take up to 5 minutes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `jobs_projects_tenants_jobs_patch_builder()` + `jobs_projects_tenants_jobs_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_jobs_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Job,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Job>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = jobs_projects_tenants_jobs_patch_builder(client, name, updateMask, body)?;
    jobs_projects_tenants_jobs_patch_execute(builder)
}

/// GET v4/{+parent}/jobs:search
/// Searches for jobs using the provided SearchJobsRequest. This call constrains the visibility of jobs present in the database, and only returns jobs that the caller has permission to search against.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `jobs_projects_tenants_jobs_search_execute()` to send, or `jobs_projects_tenants_jobs_search` for simplest API.

pub fn jobs_projects_tenants_jobs_search_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SearchJobsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv4/{+parent}/jobs:search", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v4/{+parent}/jobs:search
/// Searches for jobs using the provided SearchJobsRequest. This call constrains the visibility of jobs present in the database, and only returns jobs that the caller has permission to search against.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `jobs_projects_tenants_jobs_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from jobs_projects_tenants_jobs_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_jobs_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/{+parent}/jobs:search
/// Searches for jobs using the provided SearchJobsRequest. This call constrains the visibility of jobs present in the database, and only returns jobs that the caller has permission to search against.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `jobs_projects_tenants_jobs_search_builder()` + `jobs_projects_tenants_jobs_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_jobs_search(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SearchJobsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = jobs_projects_tenants_jobs_search_builder(client, parent, body)?;
    jobs_projects_tenants_jobs_search_execute(builder)
}

/// GET v4/{+parent}/jobs:searchForAlert
/// Searches for jobs using the provided SearchJobsRequest. This API call is intended for the use case of targeting passive job seekers (for example, job seekers who have signed up to receive email alerts about potential job opportunities), it has different algorithmic adjustments that are designed to specifically target passive job seekers. This call constrains the visibility of jobs present in the database, and only returns jobs the caller has permission to search against.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `jobs_projects_tenants_jobs_search_for_alert_execute()` to send, or `jobs_projects_tenants_jobs_search_for_alert` for simplest API.

pub fn jobs_projects_tenants_jobs_search_for_alert_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SearchJobsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv4/{+parent}/jobs:searchForAlert",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v4/{+parent}/jobs:searchForAlert
/// Searches for jobs using the provided SearchJobsRequest. This API call is intended for the use case of targeting passive job seekers (for example, job seekers who have signed up to receive email alerts about potential job opportunities), it has different algorithmic adjustments that are designed to specifically target passive job seekers. This call constrains the visibility of jobs present in the database, and only returns jobs the caller has permission to search against.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `jobs_projects_tenants_jobs_search_for_alert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from jobs_projects_tenants_jobs_search_for_alert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_jobs_search_for_alert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchJobsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/{+parent}/jobs:searchForAlert
/// Searches for jobs using the provided SearchJobsRequest. This API call is intended for the use case of targeting passive job seekers (for example, job seekers who have signed up to receive email alerts about potential job opportunities), it has different algorithmic adjustments that are designed to specifically target passive job seekers. This call constrains the visibility of jobs present in the database, and only returns jobs the caller has permission to search against.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `jobs_projects_tenants_jobs_search_for_alert_builder()` + `jobs_projects_tenants_jobs_search_for_alert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn jobs_projects_tenants_jobs_search_for_alert(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SearchJobsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SearchJobsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = jobs_projects_tenants_jobs_search_for_alert_builder(client, parent, body)?;
    jobs_projects_tenants_jobs_search_for_alert_execute(builder)
}
