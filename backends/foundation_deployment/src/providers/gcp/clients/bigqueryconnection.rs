//! Auto-generated API clients for gcp/bigqueryconnection.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST v1/projects/{projectsId}/locations/{locationsId}/connections
/// Creates a new connection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryconnection_projects_locations_connections_create_execute()` to send, or `bigqueryconnection_projects_locations_connections_create` for simplest API.

pub fn bigqueryconnection_projects_locations_connections_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    connectionId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryconnection.googleapis.com/v1/projects/{}/locations/{locationsId}/connections",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = connectionId.as_ref() {
        query_parts.push(format!("connectionId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/connections
/// Creates a new connection.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryconnection_projects_locations_connections_create_execute()` or `bigqueryconnection_projects_locations_connections_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryconnection_projects_locations_connections_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryconnection_projects_locations_connections_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Connection>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Connection = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/connections
/// Creates a new connection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryconnection_projects_locations_connections_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryconnection_projects_locations_connections_create_task()`.
/// For the simplest API, use `bigqueryconnection_projects_locations_connections_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryconnection_projects_locations_connections_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryconnection_projects_locations_connections_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Connection>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigqueryconnection_projects_locations_connections_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryconnection_projects_locations_connections_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryconnectionProjectsLocationsConnectionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: connectionId
    pub connectionId: Option<Option<String>>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/connections
/// Creates a new connection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryconnection_projects_locations_connections_create_builder()` + `bigqueryconnection_projects_locations_connections_create_execute()`.
/// For task-level control, use `bigqueryconnection_projects_locations_connections_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryconnection_projects_locations_connections_create(
    client: &SimpleHttpClient,
    args: &BigqueryconnectionProjectsLocationsConnectionsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Connection>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigqueryconnection_projects_locations_connections_create_builder(
        client,
        &args.parent,
        &args.connectionId,
    )?;
    bigqueryconnection_projects_locations_connections_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}
/// Deletes connection and associated credential.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryconnection_projects_locations_connections_delete_execute()` to send, or `bigqueryconnection_projects_locations_connections_delete` for simplest API.

pub fn bigqueryconnection_projects_locations_connections_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryconnection.googleapis.com/v1/projects/{}/locations/{locationsId}/connections/{connectionsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}
/// Deletes connection and associated credential.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryconnection_projects_locations_connections_delete_execute()` or `bigqueryconnection_projects_locations_connections_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryconnection_projects_locations_connections_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryconnection_projects_locations_connections_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}
/// Deletes connection and associated credential.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryconnection_projects_locations_connections_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryconnection_projects_locations_connections_delete_task()`.
/// For the simplest API, use `bigqueryconnection_projects_locations_connections_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryconnection_projects_locations_connections_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryconnection_projects_locations_connections_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigqueryconnection_projects_locations_connections_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryconnection_projects_locations_connections_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryconnectionProjectsLocationsConnectionsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}
/// Deletes connection and associated credential.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryconnection_projects_locations_connections_delete_builder()` + `bigqueryconnection_projects_locations_connections_delete_execute()`.
/// For task-level control, use `bigqueryconnection_projects_locations_connections_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryconnection_projects_locations_connections_delete(
    client: &SimpleHttpClient,
    args: &BigqueryconnectionProjectsLocationsConnectionsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        bigqueryconnection_projects_locations_connections_delete_builder(client, &args.name)?;
    bigqueryconnection_projects_locations_connections_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}
/// Returns specified connection.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryconnection_projects_locations_connections_get_execute()` to send, or `bigqueryconnection_projects_locations_connections_get` for simplest API.

pub fn bigqueryconnection_projects_locations_connections_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryconnection.googleapis.com/v1/projects/{}/locations/{locationsId}/connections/{connectionsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}
/// Returns specified connection.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryconnection_projects_locations_connections_get_execute()` or `bigqueryconnection_projects_locations_connections_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryconnection_projects_locations_connections_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryconnection_projects_locations_connections_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Connection>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Connection = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}
/// Returns specified connection.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryconnection_projects_locations_connections_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryconnection_projects_locations_connections_get_task()`.
/// For the simplest API, use `bigqueryconnection_projects_locations_connections_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryconnection_projects_locations_connections_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryconnection_projects_locations_connections_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Connection>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigqueryconnection_projects_locations_connections_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryconnection_projects_locations_connections_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryconnectionProjectsLocationsConnectionsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}
/// Returns specified connection.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryconnection_projects_locations_connections_get_builder()` + `bigqueryconnection_projects_locations_connections_get_execute()`.
/// For task-level control, use `bigqueryconnection_projects_locations_connections_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryconnection_projects_locations_connections_get(
    client: &SimpleHttpClient,
    args: &BigqueryconnectionProjectsLocationsConnectionsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Connection>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        bigqueryconnection_projects_locations_connections_get_builder(client, &args.name)?;
    bigqueryconnection_projects_locations_connections_get_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryconnection_projects_locations_connections_get_iam_policy_execute()` to send, or `bigqueryconnection_projects_locations_connections_get_iam_policy` for simplest API.

pub fn bigqueryconnection_projects_locations_connections_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryconnection.googleapis.com/v1/projects/{}/locations/{locationsId}/connections/{connectionsId}:getIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryconnection_projects_locations_connections_get_iam_policy_execute()` or `bigqueryconnection_projects_locations_connections_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryconnection_projects_locations_connections_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryconnection_projects_locations_connections_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryconnection_projects_locations_connections_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryconnection_projects_locations_connections_get_iam_policy_task()`.
/// For the simplest API, use `bigqueryconnection_projects_locations_connections_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryconnection_projects_locations_connections_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryconnection_projects_locations_connections_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigqueryconnection_projects_locations_connections_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryconnection_projects_locations_connections_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryconnectionProjectsLocationsConnectionsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryconnection_projects_locations_connections_get_iam_policy_builder()` + `bigqueryconnection_projects_locations_connections_get_iam_policy_execute()`.
/// For task-level control, use `bigqueryconnection_projects_locations_connections_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryconnection_projects_locations_connections_get_iam_policy(
    client: &SimpleHttpClient,
    args: &BigqueryconnectionProjectsLocationsConnectionsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigqueryconnection_projects_locations_connections_get_iam_policy_builder(
        client,
        &args.resource,
    )?;
    bigqueryconnection_projects_locations_connections_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/connections
/// Returns a list of connections in the given project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryconnection_projects_locations_connections_list_execute()` to send, or `bigqueryconnection_projects_locations_connections_list` for simplest API.

pub fn bigqueryconnection_projects_locations_connections_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryconnection.googleapis.com/v1/projects/{}/locations/{locationsId}/connections",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/connections
/// Returns a list of connections in the given project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryconnection_projects_locations_connections_list_execute()` or `bigqueryconnection_projects_locations_connections_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryconnection_projects_locations_connections_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryconnection_projects_locations_connections_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListConnectionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListConnectionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/connections
/// Returns a list of connections in the given project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryconnection_projects_locations_connections_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryconnection_projects_locations_connections_list_task()`.
/// For the simplest API, use `bigqueryconnection_projects_locations_connections_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryconnection_projects_locations_connections_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryconnection_projects_locations_connections_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListConnectionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = bigqueryconnection_projects_locations_connections_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryconnection_projects_locations_connections_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryconnectionProjectsLocationsConnectionsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/connections
/// Returns a list of connections in the given project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryconnection_projects_locations_connections_list_builder()` + `bigqueryconnection_projects_locations_connections_list_execute()`.
/// For task-level control, use `bigqueryconnection_projects_locations_connections_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryconnection_projects_locations_connections_list(
    client: &SimpleHttpClient,
    args: &BigqueryconnectionProjectsLocationsConnectionsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListConnectionsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = bigqueryconnection_projects_locations_connections_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    bigqueryconnection_projects_locations_connections_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}
/// Updates the specified connection. For security reasons, also resets credential if connection properties are in the update field mask.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryconnection_projects_locations_connections_patch_execute()` to send, or `bigqueryconnection_projects_locations_connections_patch` for simplest API.

pub fn bigqueryconnection_projects_locations_connections_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryconnection.googleapis.com/v1/projects/{}/locations/{locationsId}/connections/{connectionsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}
/// Updates the specified connection. For security reasons, also resets credential if connection properties are in the update field mask.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryconnection_projects_locations_connections_patch_execute()` or `bigqueryconnection_projects_locations_connections_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryconnection_projects_locations_connections_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryconnection_projects_locations_connections_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Connection>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Connection = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}
/// Updates the specified connection. For security reasons, also resets credential if connection properties are in the update field mask.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryconnection_projects_locations_connections_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryconnection_projects_locations_connections_patch_task()`.
/// For the simplest API, use `bigqueryconnection_projects_locations_connections_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryconnection_projects_locations_connections_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryconnection_projects_locations_connections_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Connection>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigqueryconnection_projects_locations_connections_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryconnection_projects_locations_connections_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryconnectionProjectsLocationsConnectionsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}
/// Updates the specified connection. For security reasons, also resets credential if connection properties are in the update field mask.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryconnection_projects_locations_connections_patch_builder()` + `bigqueryconnection_projects_locations_connections_patch_execute()`.
/// For task-level control, use `bigqueryconnection_projects_locations_connections_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryconnection_projects_locations_connections_patch(
    client: &SimpleHttpClient,
    args: &BigqueryconnectionProjectsLocationsConnectionsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Connection>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigqueryconnection_projects_locations_connections_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    bigqueryconnection_projects_locations_connections_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryconnection_projects_locations_connections_set_iam_policy_execute()` to send, or `bigqueryconnection_projects_locations_connections_set_iam_policy` for simplest API.

pub fn bigqueryconnection_projects_locations_connections_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryconnection.googleapis.com/v1/projects/{}/locations/{locationsId}/connections/{connectionsId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryconnection_projects_locations_connections_set_iam_policy_execute()` or `bigqueryconnection_projects_locations_connections_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryconnection_projects_locations_connections_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryconnection_projects_locations_connections_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryconnection_projects_locations_connections_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryconnection_projects_locations_connections_set_iam_policy_task()`.
/// For the simplest API, use `bigqueryconnection_projects_locations_connections_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryconnection_projects_locations_connections_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryconnection_projects_locations_connections_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigqueryconnection_projects_locations_connections_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryconnection_projects_locations_connections_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryconnectionProjectsLocationsConnectionsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryconnection_projects_locations_connections_set_iam_policy_builder()` + `bigqueryconnection_projects_locations_connections_set_iam_policy_execute()`.
/// For task-level control, use `bigqueryconnection_projects_locations_connections_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryconnection_projects_locations_connections_set_iam_policy(
    client: &SimpleHttpClient,
    args: &BigqueryconnectionProjectsLocationsConnectionsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigqueryconnection_projects_locations_connections_set_iam_policy_builder(
        client,
        &args.resource,
    )?;
    bigqueryconnection_projects_locations_connections_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigqueryconnection_projects_locations_connections_test_iam_permissions_execute()` to send, or `bigqueryconnection_projects_locations_connections_test_iam_permissions` for simplest API.

pub fn bigqueryconnection_projects_locations_connections_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigqueryconnection.googleapis.com/v1/projects/{}/locations/{locationsId}/connections/{connectionsId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigqueryconnection_projects_locations_connections_test_iam_permissions_execute()` or `bigqueryconnection_projects_locations_connections_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryconnection_projects_locations_connections_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryconnection_projects_locations_connections_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigqueryconnection_projects_locations_connections_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigqueryconnection_projects_locations_connections_test_iam_permissions_task()`.
/// For the simplest API, use `bigqueryconnection_projects_locations_connections_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigqueryconnection_projects_locations_connections_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigqueryconnection_projects_locations_connections_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        bigqueryconnection_projects_locations_connections_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigqueryconnection_projects_locations_connections_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryconnectionProjectsLocationsConnectionsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigqueryconnection_projects_locations_connections_test_iam_permissions_builder()` + `bigqueryconnection_projects_locations_connections_test_iam_permissions_execute()`.
/// For task-level control, use `bigqueryconnection_projects_locations_connections_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigqueryconnection_projects_locations_connections_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &BigqueryconnectionProjectsLocationsConnectionsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = bigqueryconnection_projects_locations_connections_test_iam_permissions_builder(
        client,
        &args.resource,
    )?;
    bigqueryconnection_projects_locations_connections_test_iam_permissions_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for Connection
// =============================================================================

/// ResourceIdentifier implementation for Connection with BigqueryconnectionProjectsLocationsConnectionsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryconnectionProjectsLocationsConnectionsCreateArgs> for Connection {
    fn generate_resource_id(
        &self,
        input: &BigqueryconnectionProjectsLocationsConnectionsCreateArgs,
    ) -> String {
        format!("gcp::bigqueryconnection::Connection/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryconnection::Connection"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with BigqueryconnectionProjectsLocationsConnectionsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryconnectionProjectsLocationsConnectionsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &BigqueryconnectionProjectsLocationsConnectionsDeleteArgs,
    ) -> String {
        format!("gcp::bigqueryconnection::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryconnection::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Connection
// =============================================================================

/// ResourceIdentifier implementation for Connection with BigqueryconnectionProjectsLocationsConnectionsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryconnectionProjectsLocationsConnectionsGetArgs> for Connection {
    fn generate_resource_id(
        &self,
        input: &BigqueryconnectionProjectsLocationsConnectionsGetArgs,
    ) -> String {
        format!("gcp::bigqueryconnection::Connection/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryconnection::Connection"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with BigqueryconnectionProjectsLocationsConnectionsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryconnectionProjectsLocationsConnectionsGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &BigqueryconnectionProjectsLocationsConnectionsGetIamPolicyArgs,
    ) -> String {
        format!("gcp::bigqueryconnection::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryconnection::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListConnectionsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListConnectionsResponse with BigqueryconnectionProjectsLocationsConnectionsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryconnectionProjectsLocationsConnectionsListArgs>
    for ListConnectionsResponse
{
    fn generate_resource_id(
        &self,
        input: &BigqueryconnectionProjectsLocationsConnectionsListArgs,
    ) -> String {
        format!(
            "gcp::bigqueryconnection::ListConnectionsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryconnection::ListConnectionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Connection
// =============================================================================

/// ResourceIdentifier implementation for Connection with BigqueryconnectionProjectsLocationsConnectionsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryconnectionProjectsLocationsConnectionsPatchArgs> for Connection {
    fn generate_resource_id(
        &self,
        input: &BigqueryconnectionProjectsLocationsConnectionsPatchArgs,
    ) -> String {
        format!("gcp::bigqueryconnection::Connection/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryconnection::Connection"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with BigqueryconnectionProjectsLocationsConnectionsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryconnectionProjectsLocationsConnectionsSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &BigqueryconnectionProjectsLocationsConnectionsSetIamPolicyArgs,
    ) -> String {
        format!("gcp::bigqueryconnection::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryconnection::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with BigqueryconnectionProjectsLocationsConnectionsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigqueryconnectionProjectsLocationsConnectionsTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &BigqueryconnectionProjectsLocationsConnectionsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::bigqueryconnection::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigqueryconnection::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
