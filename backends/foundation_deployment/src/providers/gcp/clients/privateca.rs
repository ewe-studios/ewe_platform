//! Auto-generated API clients for gcp/privateca.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_get_execute()` to send, or `privateca_projects_locations_get` for simplest API.

pub fn privateca_projects_locations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_get_execute()` or `privateca_projects_locations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Location>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_get_task()`.
/// For the simplest API, use `privateca_projects_locations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_get_builder()` + `privateca_projects_locations_get_execute()`.
/// For task-level control, use `privateca_projects_locations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_get(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_get_builder(client, &args.name)?;
    privateca_projects_locations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_list_execute()` to send, or `privateca_projects_locations_list` for simplest API.

pub fn privateca_projects_locations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    extraLocationTypes: &Option<String>,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes.as_ref() {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_list_execute()` or `privateca_projects_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLocationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_list_task()`.
/// For the simplest API, use `privateca_projects_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: extraLocationTypes
    pub extraLocationTypes: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_list_builder()` + `privateca_projects_locations_list_execute()`.
/// For task-level control, use `privateca_projects_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_list(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_list_builder(
        client,
        &args.name,
        &args.extraLocationTypes,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    privateca_projects_locations_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools
/// Create a CaPool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_ca_pools_create_execute()` to send, or `privateca_projects_locations_ca_pools_create` for simplest API.

pub fn privateca_projects_locations_ca_pools_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    caPoolId: &Option<String>,
    requestId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/caPools",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = caPoolId.as_ref() {
        query_parts.push(format!("caPoolId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools
/// Create a CaPool.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_ca_pools_create_execute()` or `privateca_projects_locations_ca_pools_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools
/// Create a CaPool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_ca_pools_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_ca_pools_create_task()`.
/// For the simplest API, use `privateca_projects_locations_ca_pools_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_ca_pools_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_ca_pools_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_ca_pools_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: caPoolId
    pub caPoolId: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools
/// Create a CaPool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_ca_pools_create_builder()` + `privateca_projects_locations_ca_pools_create_execute()`.
/// For task-level control, use `privateca_projects_locations_ca_pools_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_create(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCaPoolsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_ca_pools_create_builder(
        client,
        &args.parent,
        &args.caPoolId,
        &args.requestId,
    )?;
    privateca_projects_locations_ca_pools_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}
/// Delete a CaPool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_ca_pools_delete_execute()` to send, or `privateca_projects_locations_ca_pools_delete` for simplest API.

pub fn privateca_projects_locations_ca_pools_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    ignoreDependentResources: &Option<String>,
    requestId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/caPools/{caPoolsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ignoreDependentResources.as_ref() {
        query_parts.push(format!("ignoreDependentResources={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}
/// Delete a CaPool.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_ca_pools_delete_execute()` or `privateca_projects_locations_ca_pools_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}
/// Delete a CaPool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_ca_pools_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_ca_pools_delete_task()`.
/// For the simplest API, use `privateca_projects_locations_ca_pools_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_ca_pools_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_ca_pools_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_ca_pools_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: ignoreDependentResources
    pub ignoreDependentResources: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}
/// Delete a CaPool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_ca_pools_delete_builder()` + `privateca_projects_locations_ca_pools_delete_execute()`.
/// For task-level control, use `privateca_projects_locations_ca_pools_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_delete(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCaPoolsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_ca_pools_delete_builder(
        client,
        &args.name,
        &args.ignoreDependentResources,
        &args.requestId,
    )?;
    privateca_projects_locations_ca_pools_delete_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}:fetchCaCerts
/// FetchCaCerts returns the current trust anchor for the CaPool. This will include CA certificate chains for all certificate authorities in the ENABLED, DISABLED, or STAGED states.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_ca_pools_fetch_ca_certs_execute()` to send, or `privateca_projects_locations_ca_pools_fetch_ca_certs` for simplest API.

pub fn privateca_projects_locations_ca_pools_fetch_ca_certs_builder<R>(
    client: &SimpleHttpClient<R>,
    caPool: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/caPools/{caPoolsId}:fetchCaCerts",
        caPool,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}:fetchCaCerts
/// FetchCaCerts returns the current trust anchor for the CaPool. This will include CA certificate chains for all certificate authorities in the ENABLED, DISABLED, or STAGED states.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_ca_pools_fetch_ca_certs_execute()` or `privateca_projects_locations_ca_pools_fetch_ca_certs`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_fetch_ca_certs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_fetch_ca_certs_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FetchCaCertsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FetchCaCertsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}:fetchCaCerts
/// FetchCaCerts returns the current trust anchor for the CaPool. This will include CA certificate chains for all certificate authorities in the ENABLED, DISABLED, or STAGED states.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_ca_pools_fetch_ca_certs_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_ca_pools_fetch_ca_certs_task()`.
/// For the simplest API, use `privateca_projects_locations_ca_pools_fetch_ca_certs()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_fetch_ca_certs_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_ca_pools_fetch_ca_certs_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FetchCaCertsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_ca_pools_fetch_ca_certs_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_ca_pools_fetch_ca_certs`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsFetchCaCertsArgs {
    /// Path parameter: caPool
    pub caPool: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}:fetchCaCerts
/// FetchCaCerts returns the current trust anchor for the CaPool. This will include CA certificate chains for all certificate authorities in the ENABLED, DISABLED, or STAGED states.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_ca_pools_fetch_ca_certs_builder()` + `privateca_projects_locations_ca_pools_fetch_ca_certs_execute()`.
/// For task-level control, use `privateca_projects_locations_ca_pools_fetch_ca_certs_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_fetch_ca_certs(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCaPoolsFetchCaCertsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FetchCaCertsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        privateca_projects_locations_ca_pools_fetch_ca_certs_builder(client, &args.caPool)?;
    privateca_projects_locations_ca_pools_fetch_ca_certs_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}
/// Returns a CaPool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_ca_pools_get_execute()` to send, or `privateca_projects_locations_ca_pools_get` for simplest API.

pub fn privateca_projects_locations_ca_pools_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/caPools/{caPoolsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}
/// Returns a CaPool.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_ca_pools_get_execute()` or `privateca_projects_locations_ca_pools_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CaPool>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CaPool = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}
/// Returns a CaPool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_ca_pools_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_ca_pools_get_task()`.
/// For the simplest API, use `privateca_projects_locations_ca_pools_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_ca_pools_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CaPool>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_ca_pools_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_ca_pools_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}
/// Returns a CaPool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_ca_pools_get_builder()` + `privateca_projects_locations_ca_pools_get_execute()`.
/// For task-level control, use `privateca_projects_locations_ca_pools_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_get(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCaPoolsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CaPool>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_ca_pools_get_builder(client, &args.name)?;
    privateca_projects_locations_ca_pools_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_ca_pools_get_iam_policy_execute()` to send, or `privateca_projects_locations_ca_pools_get_iam_policy` for simplest API.

pub fn privateca_projects_locations_ca_pools_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/caPools/{caPoolsId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_ca_pools_get_iam_policy_execute()` or `privateca_projects_locations_ca_pools_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_ca_pools_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_ca_pools_get_iam_policy_task()`.
/// For the simplest API, use `privateca_projects_locations_ca_pools_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_ca_pools_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_ca_pools_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_ca_pools_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_ca_pools_get_iam_policy_builder()` + `privateca_projects_locations_ca_pools_get_iam_policy_execute()`.
/// For task-level control, use `privateca_projects_locations_ca_pools_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_get_iam_policy(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCaPoolsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_ca_pools_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    privateca_projects_locations_ca_pools_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools
/// Lists CaPools.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_ca_pools_list_execute()` to send, or `privateca_projects_locations_ca_pools_list` for simplest API.

pub fn privateca_projects_locations_ca_pools_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/caPools",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools
/// Lists CaPools.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_ca_pools_list_execute()` or `privateca_projects_locations_ca_pools_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCaPoolsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCaPoolsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools
/// Lists CaPools.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_ca_pools_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_ca_pools_list_task()`.
/// For the simplest API, use `privateca_projects_locations_ca_pools_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_ca_pools_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCaPoolsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_ca_pools_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_ca_pools_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools
/// Lists CaPools.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_ca_pools_list_builder()` + `privateca_projects_locations_ca_pools_list_execute()`.
/// For task-level control, use `privateca_projects_locations_ca_pools_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_list(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCaPoolsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCaPoolsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_ca_pools_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    privateca_projects_locations_ca_pools_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}
/// Update a CaPool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_ca_pools_patch_execute()` to send, or `privateca_projects_locations_ca_pools_patch` for simplest API.

pub fn privateca_projects_locations_ca_pools_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/caPools/{caPoolsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}
/// Update a CaPool.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_ca_pools_patch_execute()` or `privateca_projects_locations_ca_pools_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}
/// Update a CaPool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_ca_pools_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_ca_pools_patch_task()`.
/// For the simplest API, use `privateca_projects_locations_ca_pools_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_ca_pools_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_ca_pools_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_ca_pools_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}
/// Update a CaPool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_ca_pools_patch_builder()` + `privateca_projects_locations_ca_pools_patch_execute()`.
/// For task-level control, use `privateca_projects_locations_ca_pools_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_patch(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCaPoolsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_ca_pools_patch_builder(
        client,
        &args.name,
        &args.requestId,
        &args.updateMask,
    )?;
    privateca_projects_locations_ca_pools_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_ca_pools_set_iam_policy_execute()` to send, or `privateca_projects_locations_ca_pools_set_iam_policy` for simplest API.

pub fn privateca_projects_locations_ca_pools_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/caPools/{caPoolsId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_ca_pools_set_iam_policy_execute()` or `privateca_projects_locations_ca_pools_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_ca_pools_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_ca_pools_set_iam_policy_task()`.
/// For the simplest API, use `privateca_projects_locations_ca_pools_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_ca_pools_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_ca_pools_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_ca_pools_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_ca_pools_set_iam_policy_builder()` + `privateca_projects_locations_ca_pools_set_iam_policy_execute()`.
/// For task-level control, use `privateca_projects_locations_ca_pools_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_set_iam_policy(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCaPoolsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        privateca_projects_locations_ca_pools_set_iam_policy_builder(client, &args.resource)?;
    privateca_projects_locations_ca_pools_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_ca_pools_test_iam_permissions_execute()` to send, or `privateca_projects_locations_ca_pools_test_iam_permissions` for simplest API.

pub fn privateca_projects_locations_ca_pools_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/caPools/{caPoolsId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_ca_pools_test_iam_permissions_execute()` or `privateca_projects_locations_ca_pools_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_ca_pools_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_ca_pools_test_iam_permissions_task()`.
/// For the simplest API, use `privateca_projects_locations_ca_pools_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_ca_pools_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_ca_pools_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_ca_pools_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_ca_pools_test_iam_permissions_builder()` + `privateca_projects_locations_ca_pools_test_iam_permissions_execute()`.
/// For task-level control, use `privateca_projects_locations_ca_pools_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCaPoolsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        privateca_projects_locations_ca_pools_test_iam_permissions_builder(client, &args.resource)?;
    privateca_projects_locations_ca_pools_test_iam_permissions_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}:activate
/// Activate a CertificateAuthority that is in state AWAITING_USER_ACTIVATION and is of type SUBORDINATE. After the parent Certificate Authority signs a certificate signing request from FetchCertificateAuthorityCsr, this method can complete the activation process.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_ca_pools_certificate_authorities_activate_execute()` to send, or `privateca_projects_locations_ca_pools_certificate_authorities_activate` for simplest API.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_activate_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}:activate",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}:activate
/// Activate a CertificateAuthority that is in state AWAITING_USER_ACTIVATION and is of type SUBORDINATE. After the parent Certificate Authority signs a certificate signing request from FetchCertificateAuthorityCsr, this method can complete the activation process.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_ca_pools_certificate_authorities_activate_execute()` or `privateca_projects_locations_ca_pools_certificate_authorities_activate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificate_authorities_activate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_activate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}:activate
/// Activate a CertificateAuthority that is in state AWAITING_USER_ACTIVATION and is of type SUBORDINATE. After the parent Certificate Authority signs a certificate signing request from FetchCertificateAuthorityCsr, this method can complete the activation process.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_ca_pools_certificate_authorities_activate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificate_authorities_activate_task()`.
/// For the simplest API, use `privateca_projects_locations_ca_pools_certificate_authorities_activate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificate_authorities_activate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_activate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        privateca_projects_locations_ca_pools_certificate_authorities_activate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_ca_pools_certificate_authorities_activate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesActivateArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}:activate
/// Activate a CertificateAuthority that is in state AWAITING_USER_ACTIVATION and is of type SUBORDINATE. After the parent Certificate Authority signs a certificate signing request from FetchCertificateAuthorityCsr, this method can complete the activation process.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_ca_pools_certificate_authorities_activate_builder()` + `privateca_projects_locations_ca_pools_certificate_authorities_activate_execute()`.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificate_authorities_activate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_activate(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesActivateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_ca_pools_certificate_authorities_activate_builder(
        client, &args.name,
    )?;
    privateca_projects_locations_ca_pools_certificate_authorities_activate_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities
/// Create a new CertificateAuthority in a given Project and Location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_ca_pools_certificate_authorities_create_execute()` to send, or `privateca_projects_locations_ca_pools_certificate_authorities_create` for simplest API.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    certificateAuthorityId: &Option<String>,
    requestId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = certificateAuthorityId.as_ref() {
        query_parts.push(format!("certificateAuthorityId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities
/// Create a new CertificateAuthority in a given Project and Location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_ca_pools_certificate_authorities_create_execute()` or `privateca_projects_locations_ca_pools_certificate_authorities_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificate_authorities_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities
/// Create a new CertificateAuthority in a given Project and Location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_ca_pools_certificate_authorities_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificate_authorities_create_task()`.
/// For the simplest API, use `privateca_projects_locations_ca_pools_certificate_authorities_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificate_authorities_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_ca_pools_certificate_authorities_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_ca_pools_certificate_authorities_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: certificateAuthorityId
    pub certificateAuthorityId: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities
/// Create a new CertificateAuthority in a given Project and Location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_ca_pools_certificate_authorities_create_builder()` + `privateca_projects_locations_ca_pools_certificate_authorities_create_execute()`.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificate_authorities_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_create(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_ca_pools_certificate_authorities_create_builder(
        client,
        &args.parent,
        &args.certificateAuthorityId,
        &args.requestId,
    )?;
    privateca_projects_locations_ca_pools_certificate_authorities_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}
/// Delete a CertificateAuthority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_ca_pools_certificate_authorities_delete_execute()` to send, or `privateca_projects_locations_ca_pools_certificate_authorities_delete` for simplest API.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    ignoreActiveCertificates: &Option<String>,
    ignoreDependentResources: &Option<String>,
    requestId: &Option<String>,
    skipGracePeriod: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = ignoreActiveCertificates.as_ref() {
        query_parts.push(format!("ignoreActiveCertificates={}", val));
    }
    if let Some(val) = ignoreDependentResources.as_ref() {
        query_parts.push(format!("ignoreDependentResources={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = skipGracePeriod.as_ref() {
        query_parts.push(format!("skipGracePeriod={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}
/// Delete a CertificateAuthority.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_ca_pools_certificate_authorities_delete_execute()` or `privateca_projects_locations_ca_pools_certificate_authorities_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificate_authorities_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}
/// Delete a CertificateAuthority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_ca_pools_certificate_authorities_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificate_authorities_delete_task()`.
/// For the simplest API, use `privateca_projects_locations_ca_pools_certificate_authorities_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificate_authorities_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_ca_pools_certificate_authorities_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_ca_pools_certificate_authorities_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: ignoreActiveCertificates
    pub ignoreActiveCertificates: Option<String>,
    /// Query parameter: ignoreDependentResources
    pub ignoreDependentResources: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: skipGracePeriod
    pub skipGracePeriod: Option<String>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}
/// Delete a CertificateAuthority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_ca_pools_certificate_authorities_delete_builder()` + `privateca_projects_locations_ca_pools_certificate_authorities_delete_execute()`.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificate_authorities_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_delete(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_ca_pools_certificate_authorities_delete_builder(
        client,
        &args.name,
        &args.ignoreActiveCertificates,
        &args.ignoreDependentResources,
        &args.requestId,
        &args.skipGracePeriod,
    )?;
    privateca_projects_locations_ca_pools_certificate_authorities_delete_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}:disable
/// Disable a CertificateAuthority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_ca_pools_certificate_authorities_disable_execute()` to send, or `privateca_projects_locations_ca_pools_certificate_authorities_disable` for simplest API.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_disable_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}:disable",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}:disable
/// Disable a CertificateAuthority.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_ca_pools_certificate_authorities_disable_execute()` or `privateca_projects_locations_ca_pools_certificate_authorities_disable`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificate_authorities_disable_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_disable_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}:disable
/// Disable a CertificateAuthority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_ca_pools_certificate_authorities_disable_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificate_authorities_disable_task()`.
/// For the simplest API, use `privateca_projects_locations_ca_pools_certificate_authorities_disable()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificate_authorities_disable_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_disable_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_ca_pools_certificate_authorities_disable_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_ca_pools_certificate_authorities_disable`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesDisableArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}:disable
/// Disable a CertificateAuthority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_ca_pools_certificate_authorities_disable_builder()` + `privateca_projects_locations_ca_pools_certificate_authorities_disable_execute()`.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificate_authorities_disable_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_disable(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesDisableArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_ca_pools_certificate_authorities_disable_builder(
        client, &args.name,
    )?;
    privateca_projects_locations_ca_pools_certificate_authorities_disable_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}:enable
/// Enable a CertificateAuthority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_ca_pools_certificate_authorities_enable_execute()` to send, or `privateca_projects_locations_ca_pools_certificate_authorities_enable` for simplest API.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_enable_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}:enable",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}:enable
/// Enable a CertificateAuthority.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_ca_pools_certificate_authorities_enable_execute()` or `privateca_projects_locations_ca_pools_certificate_authorities_enable`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificate_authorities_enable_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_enable_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}:enable
/// Enable a CertificateAuthority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_ca_pools_certificate_authorities_enable_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificate_authorities_enable_task()`.
/// For the simplest API, use `privateca_projects_locations_ca_pools_certificate_authorities_enable()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificate_authorities_enable_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_enable_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_ca_pools_certificate_authorities_enable_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_ca_pools_certificate_authorities_enable`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesEnableArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}:enable
/// Enable a CertificateAuthority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_ca_pools_certificate_authorities_enable_builder()` + `privateca_projects_locations_ca_pools_certificate_authorities_enable_execute()`.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificate_authorities_enable_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_enable(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesEnableArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_ca_pools_certificate_authorities_enable_builder(
        client, &args.name,
    )?;
    privateca_projects_locations_ca_pools_certificate_authorities_enable_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}:fetch
/// Fetch a certificate signing request (CSR) from a CertificateAuthority that is in state AWAITING_USER_ACTIVATION and is of type SUBORDINATE. The CSR must then be signed by the desired parent Certificate Authority, which could be another CertificateAuthority resource, or could be an on-prem certificate authority. See also ActivateCertificateAuthority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_ca_pools_certificate_authorities_fetch_execute()` to send, or `privateca_projects_locations_ca_pools_certificate_authorities_fetch` for simplest API.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_fetch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}:fetch",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}:fetch
/// Fetch a certificate signing request (CSR) from a CertificateAuthority that is in state AWAITING_USER_ACTIVATION and is of type SUBORDINATE. The CSR must then be signed by the desired parent Certificate Authority, which could be another CertificateAuthority resource, or could be an on-prem certificate authority. See also ActivateCertificateAuthority.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_ca_pools_certificate_authorities_fetch_execute()` or `privateca_projects_locations_ca_pools_certificate_authorities_fetch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificate_authorities_fetch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_fetch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FetchCertificateAuthorityCsrResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FetchCertificateAuthorityCsrResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}:fetch
/// Fetch a certificate signing request (CSR) from a CertificateAuthority that is in state AWAITING_USER_ACTIVATION and is of type SUBORDINATE. The CSR must then be signed by the desired parent Certificate Authority, which could be another CertificateAuthority resource, or could be an on-prem certificate authority. See also ActivateCertificateAuthority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_ca_pools_certificate_authorities_fetch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificate_authorities_fetch_task()`.
/// For the simplest API, use `privateca_projects_locations_ca_pools_certificate_authorities_fetch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificate_authorities_fetch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_fetch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FetchCertificateAuthorityCsrResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_ca_pools_certificate_authorities_fetch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_ca_pools_certificate_authorities_fetch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesFetchArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}:fetch
/// Fetch a certificate signing request (CSR) from a CertificateAuthority that is in state AWAITING_USER_ACTIVATION and is of type SUBORDINATE. The CSR must then be signed by the desired parent Certificate Authority, which could be another CertificateAuthority resource, or could be an on-prem certificate authority. See also ActivateCertificateAuthority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_ca_pools_certificate_authorities_fetch_builder()` + `privateca_projects_locations_ca_pools_certificate_authorities_fetch_execute()`.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificate_authorities_fetch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_fetch(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesFetchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<FetchCertificateAuthorityCsrResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_ca_pools_certificate_authorities_fetch_builder(
        client, &args.name,
    )?;
    privateca_projects_locations_ca_pools_certificate_authorities_fetch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}
/// Returns a CertificateAuthority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_ca_pools_certificate_authorities_get_execute()` to send, or `privateca_projects_locations_ca_pools_certificate_authorities_get` for simplest API.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}
/// Returns a CertificateAuthority.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_ca_pools_certificate_authorities_get_execute()` or `privateca_projects_locations_ca_pools_certificate_authorities_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificate_authorities_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CertificateAuthority>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CertificateAuthority = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}
/// Returns a CertificateAuthority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_ca_pools_certificate_authorities_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificate_authorities_get_task()`.
/// For the simplest API, use `privateca_projects_locations_ca_pools_certificate_authorities_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificate_authorities_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CertificateAuthority>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_ca_pools_certificate_authorities_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_ca_pools_certificate_authorities_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}
/// Returns a CertificateAuthority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_ca_pools_certificate_authorities_get_builder()` + `privateca_projects_locations_ca_pools_certificate_authorities_get_execute()`.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificate_authorities_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_get(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CertificateAuthority>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_ca_pools_certificate_authorities_get_builder(
        client, &args.name,
    )?;
    privateca_projects_locations_ca_pools_certificate_authorities_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities
/// Lists CertificateAuthorities.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_ca_pools_certificate_authorities_list_execute()` to send, or `privateca_projects_locations_ca_pools_certificate_authorities_list` for simplest API.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities
/// Lists CertificateAuthorities.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_ca_pools_certificate_authorities_list_execute()` or `privateca_projects_locations_ca_pools_certificate_authorities_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificate_authorities_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCertificateAuthoritiesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCertificateAuthoritiesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities
/// Lists CertificateAuthorities.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_ca_pools_certificate_authorities_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificate_authorities_list_task()`.
/// For the simplest API, use `privateca_projects_locations_ca_pools_certificate_authorities_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificate_authorities_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCertificateAuthoritiesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_ca_pools_certificate_authorities_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_ca_pools_certificate_authorities_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities
/// Lists CertificateAuthorities.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_ca_pools_certificate_authorities_list_builder()` + `privateca_projects_locations_ca_pools_certificate_authorities_list_execute()`.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificate_authorities_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_list(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCertificateAuthoritiesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_ca_pools_certificate_authorities_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    privateca_projects_locations_ca_pools_certificate_authorities_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}
/// Update a CertificateAuthority.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_ca_pools_certificate_authorities_patch_execute()` to send, or `privateca_projects_locations_ca_pools_certificate_authorities_patch` for simplest API.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}
/// Update a CertificateAuthority.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_ca_pools_certificate_authorities_patch_execute()` or `privateca_projects_locations_ca_pools_certificate_authorities_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificate_authorities_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}
/// Update a CertificateAuthority.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_ca_pools_certificate_authorities_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificate_authorities_patch_task()`.
/// For the simplest API, use `privateca_projects_locations_ca_pools_certificate_authorities_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificate_authorities_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_ca_pools_certificate_authorities_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_ca_pools_certificate_authorities_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}
/// Update a CertificateAuthority.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_ca_pools_certificate_authorities_patch_builder()` + `privateca_projects_locations_ca_pools_certificate_authorities_patch_execute()`.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificate_authorities_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_patch(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_ca_pools_certificate_authorities_patch_builder(
        client,
        &args.name,
        &args.requestId,
        &args.updateMask,
    )?;
    privateca_projects_locations_ca_pools_certificate_authorities_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}:undelete
/// Undelete a CertificateAuthority that has been deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_ca_pools_certificate_authorities_undelete_execute()` to send, or `privateca_projects_locations_ca_pools_certificate_authorities_undelete` for simplest API.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_undelete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}:undelete",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}:undelete
/// Undelete a CertificateAuthority that has been deleted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_ca_pools_certificate_authorities_undelete_execute()` or `privateca_projects_locations_ca_pools_certificate_authorities_undelete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificate_authorities_undelete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_undelete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}:undelete
/// Undelete a CertificateAuthority that has been deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_ca_pools_certificate_authorities_undelete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificate_authorities_undelete_task()`.
/// For the simplest API, use `privateca_projects_locations_ca_pools_certificate_authorities_undelete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificate_authorities_undelete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_undelete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        privateca_projects_locations_ca_pools_certificate_authorities_undelete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_ca_pools_certificate_authorities_undelete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesUndeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}:undelete
/// Undelete a CertificateAuthority that has been deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_ca_pools_certificate_authorities_undelete_builder()` + `privateca_projects_locations_ca_pools_certificate_authorities_undelete_execute()`.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificate_authorities_undelete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_undelete(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesUndeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_ca_pools_certificate_authorities_undelete_builder(
        client, &args.name,
    )?;
    privateca_projects_locations_ca_pools_certificate_authorities_undelete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}/certificateRevocationLists/{certificateRevocationListsId}
/// Returns a CertificateRevocationList.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_execute()` to send, or `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get` for simplest API.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}/certificateRevocationLists/{certificateRevocationListsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}/certificateRevocationLists/{certificateRevocationListsId}
/// Returns a CertificateRevocationList.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_execute()` or `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CertificateRevocationList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CertificateRevocationList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}/certificateRevocationLists/{certificateRevocationListsId}
/// Returns a CertificateRevocationList.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_task()`.
/// For the simplest API, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CertificateRevocationList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCertificateRevocationListsGetArgs
{
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}/certificateRevocationLists/{certificateRevocationListsId}
/// Returns a CertificateRevocationList.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_builder()` + `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_execute()`.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCertificateRevocationListsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CertificateRevocationList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_builder(client, &args.name)?;
    privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}/certificateRevocationLists/{certificateRevocationListsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_iam_policy_execute()` to send, or `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_iam_policy` for simplest API.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_iam_policy_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}/certificateRevocationLists/{certificateRevocationListsId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}/certificateRevocationLists/{certificateRevocationListsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_iam_policy_execute()` or `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}/certificateRevocationLists/{certificateRevocationListsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_iam_policy_task()`.
/// For the simplest API, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCertificateRevocationListsGetIamPolicyArgs
{
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}/certificateRevocationLists/{certificateRevocationListsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_iam_policy_builder()` + `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_iam_policy_execute()`.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_iam_policy(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCertificateRevocationListsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_iam_policy_builder(client, &args.resource, &args.options_requestedPolicyVersion)?;
    privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}/certificateRevocationLists
/// Lists CertificateRevocationLists.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_list_execute()` to send, or `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_list` for simplest API.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_list_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}/certificateRevocationLists",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}/certificateRevocationLists
/// Lists CertificateRevocationLists.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_list_execute()` or `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCertificateRevocationListsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCertificateRevocationListsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}/certificateRevocationLists
/// Lists CertificateRevocationLists.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_list_task()`.
/// For the simplest API, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCertificateRevocationListsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCertificateRevocationListsListArgs
{
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}/certificateRevocationLists
/// Lists CertificateRevocationLists.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_list_builder()` + `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_list_execute()`.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_list(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCertificateRevocationListsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCertificateRevocationListsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_list_builder(client, &args.parent, &args.filter, &args.orderBy, &args.pageSize, &args.pageToken)?;
    privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}/certificateRevocationLists/{certificateRevocationListsId}
/// Update a CertificateRevocationList.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_patch_execute()` to send, or `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_patch` for simplest API.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_patch_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}/certificateRevocationLists/{certificateRevocationListsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}/certificateRevocationLists/{certificateRevocationListsId}
/// Update a CertificateRevocationList.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_patch_execute()` or `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}/certificateRevocationLists/{certificateRevocationListsId}
/// Update a CertificateRevocationList.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_patch_task()`.
/// For the simplest API, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCertificateRevocationListsPatchArgs
{
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}/certificateRevocationLists/{certificateRevocationListsId}
/// Update a CertificateRevocationList.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_patch_builder()` + `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_patch_execute()`.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_patch(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCertificateRevocationListsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_patch_builder(client, &args.name, &args.requestId, &args.updateMask)?;
    privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}/certificateRevocationLists/{certificateRevocationListsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_set_iam_policy_execute()` to send, or `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_set_iam_policy` for simplest API.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_set_iam_policy_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}/certificateRevocationLists/{certificateRevocationListsId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}/certificateRevocationLists/{certificateRevocationListsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_set_iam_policy_execute()` or `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}/certificateRevocationLists/{certificateRevocationListsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_set_iam_policy_task()`.
/// For the simplest API, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCertificateRevocationListsSetIamPolicyArgs
{
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}/certificateRevocationLists/{certificateRevocationListsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_set_iam_policy_builder()` + `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_set_iam_policy_execute()`.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_set_iam_policy(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCertificateRevocationListsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_set_iam_policy_builder(client, &args.resource)?;
    privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}/certificateRevocationLists/{certificateRevocationListsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_test_iam_permissions_execute()` to send, or `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_test_iam_permissions` for simplest API.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_test_iam_permissions_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}/certificateRevocationLists/{certificateRevocationListsId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}/certificateRevocationLists/{certificateRevocationListsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_test_iam_permissions_execute()` or `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}/certificateRevocationLists/{certificateRevocationListsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_test_iam_permissions_task()`.
/// For the simplest API, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCertificateRevocationListsTestIamPermissionsArgs
{
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificateAuthorities/{certificateAuthoritiesId}/certificateRevocationLists/{certificateRevocationListsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_test_iam_permissions_builder()` + `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_test_iam_permissions_execute()`.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCertificateRevocationListsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_test_iam_permissions_builder(client, &args.resource)?;
    privateca_projects_locations_ca_pools_certificate_authorities_certificate_revocation_lists_test_iam_permissions_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificates
/// Create a new Certificate in a given Project, Location from a particular CaPool.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_ca_pools_certificates_create_execute()` to send, or `privateca_projects_locations_ca_pools_certificates_create` for simplest API.

pub fn privateca_projects_locations_ca_pools_certificates_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    certificateId: &Option<String>,
    issuingCertificateAuthorityId: &Option<String>,
    requestId: &Option<String>,
    validateOnly: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/caPools/{caPoolsId}/certificates",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = certificateId.as_ref() {
        query_parts.push(format!("certificateId={}", val));
    }
    if let Some(val) = issuingCertificateAuthorityId.as_ref() {
        query_parts.push(format!("issuingCertificateAuthorityId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = validateOnly.as_ref() {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificates
/// Create a new Certificate in a given Project, Location from a particular CaPool.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_ca_pools_certificates_create_execute()` or `privateca_projects_locations_ca_pools_certificates_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificates_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Certificate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Certificate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificates
/// Create a new Certificate in a given Project, Location from a particular CaPool.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_ca_pools_certificates_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificates_create_task()`.
/// For the simplest API, use `privateca_projects_locations_ca_pools_certificates_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_ca_pools_certificates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Certificate>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_ca_pools_certificates_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_ca_pools_certificates_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsCertificatesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: certificateId
    pub certificateId: Option<String>,
    /// Query parameter: issuingCertificateAuthorityId
    pub issuingCertificateAuthorityId: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificates
/// Create a new Certificate in a given Project, Location from a particular CaPool.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_ca_pools_certificates_create_builder()` + `privateca_projects_locations_ca_pools_certificates_create_execute()`.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificates_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificates_create(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCaPoolsCertificatesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Certificate>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_ca_pools_certificates_create_builder(
        client,
        &args.parent,
        &args.certificateId,
        &args.issuingCertificateAuthorityId,
        &args.requestId,
        &args.validateOnly,
    )?;
    privateca_projects_locations_ca_pools_certificates_create_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificates/{certificatesId}
/// Returns a Certificate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_ca_pools_certificates_get_execute()` to send, or `privateca_projects_locations_ca_pools_certificates_get` for simplest API.

pub fn privateca_projects_locations_ca_pools_certificates_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/caPools/{caPoolsId}/certificates/{certificatesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificates/{certificatesId}
/// Returns a Certificate.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_ca_pools_certificates_get_execute()` or `privateca_projects_locations_ca_pools_certificates_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificates_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Certificate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Certificate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificates/{certificatesId}
/// Returns a Certificate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_ca_pools_certificates_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificates_get_task()`.
/// For the simplest API, use `privateca_projects_locations_ca_pools_certificates_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_ca_pools_certificates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Certificate>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_ca_pools_certificates_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_ca_pools_certificates_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsCertificatesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificates/{certificatesId}
/// Returns a Certificate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_ca_pools_certificates_get_builder()` + `privateca_projects_locations_ca_pools_certificates_get_execute()`.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificates_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificates_get(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCaPoolsCertificatesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Certificate>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        privateca_projects_locations_ca_pools_certificates_get_builder(client, &args.name)?;
    privateca_projects_locations_ca_pools_certificates_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificates
/// Lists Certificates.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_ca_pools_certificates_list_execute()` to send, or `privateca_projects_locations_ca_pools_certificates_list` for simplest API.

pub fn privateca_projects_locations_ca_pools_certificates_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/caPools/{caPoolsId}/certificates",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificates
/// Lists Certificates.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_ca_pools_certificates_list_execute()` or `privateca_projects_locations_ca_pools_certificates_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificates_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCertificatesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCertificatesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificates
/// Lists Certificates.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_ca_pools_certificates_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificates_list_task()`.
/// For the simplest API, use `privateca_projects_locations_ca_pools_certificates_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_ca_pools_certificates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCertificatesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_ca_pools_certificates_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_ca_pools_certificates_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsCertificatesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificates
/// Lists Certificates.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_ca_pools_certificates_list_builder()` + `privateca_projects_locations_ca_pools_certificates_list_execute()`.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificates_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificates_list(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCaPoolsCertificatesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListCertificatesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_ca_pools_certificates_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    privateca_projects_locations_ca_pools_certificates_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificates/{certificatesId}
/// Update a Certificate. Currently, the only field you can update is the labels field.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_ca_pools_certificates_patch_execute()` to send, or `privateca_projects_locations_ca_pools_certificates_patch` for simplest API.

pub fn privateca_projects_locations_ca_pools_certificates_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/caPools/{caPoolsId}/certificates/{certificatesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificates/{certificatesId}
/// Update a Certificate. Currently, the only field you can update is the labels field.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_ca_pools_certificates_patch_execute()` or `privateca_projects_locations_ca_pools_certificates_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificates_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificates_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Certificate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Certificate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificates/{certificatesId}
/// Update a Certificate. Currently, the only field you can update is the labels field.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_ca_pools_certificates_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificates_patch_task()`.
/// For the simplest API, use `privateca_projects_locations_ca_pools_certificates_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificates_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_ca_pools_certificates_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Certificate>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_ca_pools_certificates_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_ca_pools_certificates_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsCertificatesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificates/{certificatesId}
/// Update a Certificate. Currently, the only field you can update is the labels field.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_ca_pools_certificates_patch_builder()` + `privateca_projects_locations_ca_pools_certificates_patch_execute()`.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificates_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificates_patch(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCaPoolsCertificatesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Certificate>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_ca_pools_certificates_patch_builder(
        client,
        &args.name,
        &args.requestId,
        &args.updateMask,
    )?;
    privateca_projects_locations_ca_pools_certificates_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificates/{certificatesId}:revoke
/// Revoke a Certificate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_ca_pools_certificates_revoke_execute()` to send, or `privateca_projects_locations_ca_pools_certificates_revoke` for simplest API.

pub fn privateca_projects_locations_ca_pools_certificates_revoke_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/caPools/{caPoolsId}/certificates/{certificatesId}:revoke",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificates/{certificatesId}:revoke
/// Revoke a Certificate.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_ca_pools_certificates_revoke_execute()` or `privateca_projects_locations_ca_pools_certificates_revoke`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificates_revoke_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificates_revoke_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Certificate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Certificate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificates/{certificatesId}:revoke
/// Revoke a Certificate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_ca_pools_certificates_revoke_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificates_revoke_task()`.
/// For the simplest API, use `privateca_projects_locations_ca_pools_certificates_revoke()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_ca_pools_certificates_revoke_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_ca_pools_certificates_revoke_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Certificate>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_ca_pools_certificates_revoke_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_ca_pools_certificates_revoke`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCaPoolsCertificatesRevokeArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/caPools/{caPoolsId}/certificates/{certificatesId}:revoke
/// Revoke a Certificate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_ca_pools_certificates_revoke_builder()` + `privateca_projects_locations_ca_pools_certificates_revoke_execute()`.
/// For task-level control, use `privateca_projects_locations_ca_pools_certificates_revoke_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_ca_pools_certificates_revoke(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCaPoolsCertificatesRevokeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Certificate>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        privateca_projects_locations_ca_pools_certificates_revoke_builder(client, &args.name)?;
    privateca_projects_locations_ca_pools_certificates_revoke_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/certificateTemplates
/// Create a new CertificateTemplate in a given Project and Location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_certificate_templates_create_execute()` to send, or `privateca_projects_locations_certificate_templates_create` for simplest API.

pub fn privateca_projects_locations_certificate_templates_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    certificateTemplateId: &Option<String>,
    requestId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/certificateTemplates",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = certificateTemplateId.as_ref() {
        query_parts.push(format!("certificateTemplateId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/certificateTemplates
/// Create a new CertificateTemplate in a given Project and Location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_certificate_templates_create_execute()` or `privateca_projects_locations_certificate_templates_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_certificate_templates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_certificate_templates_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/certificateTemplates
/// Create a new CertificateTemplate in a given Project and Location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_certificate_templates_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_certificate_templates_create_task()`.
/// For the simplest API, use `privateca_projects_locations_certificate_templates_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_certificate_templates_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_certificate_templates_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_certificate_templates_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_certificate_templates_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCertificateTemplatesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: certificateTemplateId
    pub certificateTemplateId: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/certificateTemplates
/// Create a new CertificateTemplate in a given Project and Location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_certificate_templates_create_builder()` + `privateca_projects_locations_certificate_templates_create_execute()`.
/// For task-level control, use `privateca_projects_locations_certificate_templates_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_certificate_templates_create(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCertificateTemplatesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_certificate_templates_create_builder(
        client,
        &args.parent,
        &args.certificateTemplateId,
        &args.requestId,
    )?;
    privateca_projects_locations_certificate_templates_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/certificateTemplates/{certificateTemplatesId}
/// DeleteCertificateTemplate deletes a CertificateTemplate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_certificate_templates_delete_execute()` to send, or `privateca_projects_locations_certificate_templates_delete` for simplest API.

pub fn privateca_projects_locations_certificate_templates_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/certificateTemplates/{certificateTemplatesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/certificateTemplates/{certificateTemplatesId}
/// DeleteCertificateTemplate deletes a CertificateTemplate.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_certificate_templates_delete_execute()` or `privateca_projects_locations_certificate_templates_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_certificate_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_certificate_templates_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/certificateTemplates/{certificateTemplatesId}
/// DeleteCertificateTemplate deletes a CertificateTemplate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_certificate_templates_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_certificate_templates_delete_task()`.
/// For the simplest API, use `privateca_projects_locations_certificate_templates_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_certificate_templates_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_certificate_templates_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_certificate_templates_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_certificate_templates_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCertificateTemplatesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/certificateTemplates/{certificateTemplatesId}
/// DeleteCertificateTemplate deletes a CertificateTemplate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_certificate_templates_delete_builder()` + `privateca_projects_locations_certificate_templates_delete_execute()`.
/// For task-level control, use `privateca_projects_locations_certificate_templates_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_certificate_templates_delete(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCertificateTemplatesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_certificate_templates_delete_builder(
        client,
        &args.name,
        &args.requestId,
    )?;
    privateca_projects_locations_certificate_templates_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateTemplates/{certificateTemplatesId}
/// Returns a CertificateTemplate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_certificate_templates_get_execute()` to send, or `privateca_projects_locations_certificate_templates_get` for simplest API.

pub fn privateca_projects_locations_certificate_templates_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/certificateTemplates/{certificateTemplatesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateTemplates/{certificateTemplatesId}
/// Returns a CertificateTemplate.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_certificate_templates_get_execute()` or `privateca_projects_locations_certificate_templates_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_certificate_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_certificate_templates_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CertificateTemplate>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CertificateTemplate = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateTemplates/{certificateTemplatesId}
/// Returns a CertificateTemplate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_certificate_templates_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_certificate_templates_get_task()`.
/// For the simplest API, use `privateca_projects_locations_certificate_templates_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_certificate_templates_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_certificate_templates_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CertificateTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_certificate_templates_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_certificate_templates_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCertificateTemplatesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateTemplates/{certificateTemplatesId}
/// Returns a CertificateTemplate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_certificate_templates_get_builder()` + `privateca_projects_locations_certificate_templates_get_execute()`.
/// For task-level control, use `privateca_projects_locations_certificate_templates_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_certificate_templates_get(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCertificateTemplatesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<CertificateTemplate>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        privateca_projects_locations_certificate_templates_get_builder(client, &args.name)?;
    privateca_projects_locations_certificate_templates_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateTemplates/{certificateTemplatesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_certificate_templates_get_iam_policy_execute()` to send, or `privateca_projects_locations_certificate_templates_get_iam_policy` for simplest API.

pub fn privateca_projects_locations_certificate_templates_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/certificateTemplates/{certificateTemplatesId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateTemplates/{certificateTemplatesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_certificate_templates_get_iam_policy_execute()` or `privateca_projects_locations_certificate_templates_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_certificate_templates_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_certificate_templates_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateTemplates/{certificateTemplatesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_certificate_templates_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_certificate_templates_get_iam_policy_task()`.
/// For the simplest API, use `privateca_projects_locations_certificate_templates_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_certificate_templates_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_certificate_templates_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_certificate_templates_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_certificate_templates_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCertificateTemplatesGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateTemplates/{certificateTemplatesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_certificate_templates_get_iam_policy_builder()` + `privateca_projects_locations_certificate_templates_get_iam_policy_execute()`.
/// For task-level control, use `privateca_projects_locations_certificate_templates_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_certificate_templates_get_iam_policy(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCertificateTemplatesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_certificate_templates_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    privateca_projects_locations_certificate_templates_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateTemplates
/// Lists CertificateTemplates.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_certificate_templates_list_execute()` to send, or `privateca_projects_locations_certificate_templates_list` for simplest API.

pub fn privateca_projects_locations_certificate_templates_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/certificateTemplates",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateTemplates
/// Lists CertificateTemplates.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_certificate_templates_list_execute()` or `privateca_projects_locations_certificate_templates_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_certificate_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_certificate_templates_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCertificateTemplatesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListCertificateTemplatesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateTemplates
/// Lists CertificateTemplates.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_certificate_templates_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_certificate_templates_list_task()`.
/// For the simplest API, use `privateca_projects_locations_certificate_templates_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_certificate_templates_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_certificate_templates_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCertificateTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_certificate_templates_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_certificate_templates_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCertificateTemplatesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/certificateTemplates
/// Lists CertificateTemplates.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_certificate_templates_list_builder()` + `privateca_projects_locations_certificate_templates_list_execute()`.
/// For task-level control, use `privateca_projects_locations_certificate_templates_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_certificate_templates_list(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCertificateTemplatesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListCertificateTemplatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_certificate_templates_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    privateca_projects_locations_certificate_templates_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/certificateTemplates/{certificateTemplatesId}
/// Update a CertificateTemplate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_certificate_templates_patch_execute()` to send, or `privateca_projects_locations_certificate_templates_patch` for simplest API.

pub fn privateca_projects_locations_certificate_templates_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/certificateTemplates/{certificateTemplatesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/certificateTemplates/{certificateTemplatesId}
/// Update a CertificateTemplate.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_certificate_templates_patch_execute()` or `privateca_projects_locations_certificate_templates_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_certificate_templates_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_certificate_templates_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/certificateTemplates/{certificateTemplatesId}
/// Update a CertificateTemplate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_certificate_templates_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_certificate_templates_patch_task()`.
/// For the simplest API, use `privateca_projects_locations_certificate_templates_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_certificate_templates_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_certificate_templates_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_certificate_templates_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_certificate_templates_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCertificateTemplatesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/certificateTemplates/{certificateTemplatesId}
/// Update a CertificateTemplate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_certificate_templates_patch_builder()` + `privateca_projects_locations_certificate_templates_patch_execute()`.
/// For task-level control, use `privateca_projects_locations_certificate_templates_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_certificate_templates_patch(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCertificateTemplatesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_certificate_templates_patch_builder(
        client,
        &args.name,
        &args.requestId,
        &args.updateMask,
    )?;
    privateca_projects_locations_certificate_templates_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/certificateTemplates/{certificateTemplatesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_certificate_templates_set_iam_policy_execute()` to send, or `privateca_projects_locations_certificate_templates_set_iam_policy` for simplest API.

pub fn privateca_projects_locations_certificate_templates_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/certificateTemplates/{certificateTemplatesId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/certificateTemplates/{certificateTemplatesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_certificate_templates_set_iam_policy_execute()` or `privateca_projects_locations_certificate_templates_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_certificate_templates_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_certificate_templates_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/certificateTemplates/{certificateTemplatesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_certificate_templates_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_certificate_templates_set_iam_policy_task()`.
/// For the simplest API, use `privateca_projects_locations_certificate_templates_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_certificate_templates_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_certificate_templates_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_certificate_templates_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_certificate_templates_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCertificateTemplatesSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/certificateTemplates/{certificateTemplatesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_certificate_templates_set_iam_policy_builder()` + `privateca_projects_locations_certificate_templates_set_iam_policy_execute()`.
/// For task-level control, use `privateca_projects_locations_certificate_templates_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_certificate_templates_set_iam_policy(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCertificateTemplatesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_certificate_templates_set_iam_policy_builder(
        client,
        &args.resource,
    )?;
    privateca_projects_locations_certificate_templates_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/certificateTemplates/{certificateTemplatesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_certificate_templates_test_iam_permissions_execute()` to send, or `privateca_projects_locations_certificate_templates_test_iam_permissions` for simplest API.

pub fn privateca_projects_locations_certificate_templates_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/certificateTemplates/{certificateTemplatesId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/certificateTemplates/{certificateTemplatesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_certificate_templates_test_iam_permissions_execute()` or `privateca_projects_locations_certificate_templates_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_certificate_templates_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_certificate_templates_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/certificateTemplates/{certificateTemplatesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_certificate_templates_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_certificate_templates_test_iam_permissions_task()`.
/// For the simplest API, use `privateca_projects_locations_certificate_templates_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_certificate_templates_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_certificate_templates_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        privateca_projects_locations_certificate_templates_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_certificate_templates_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsCertificateTemplatesTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/certificateTemplates/{certificateTemplatesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_certificate_templates_test_iam_permissions_builder()` + `privateca_projects_locations_certificate_templates_test_iam_permissions_execute()`.
/// For task-level control, use `privateca_projects_locations_certificate_templates_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_certificate_templates_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsCertificateTemplatesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_certificate_templates_test_iam_permissions_builder(
        client,
        &args.resource,
    )?;
    privateca_projects_locations_certificate_templates_test_iam_permissions_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_operations_cancel_execute()` to send, or `privateca_projects_locations_operations_cancel` for simplest API.

pub fn privateca_projects_locations_operations_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_operations_cancel_execute()` or `privateca_projects_locations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_operations_cancel_task()`.
/// For the simplest API, use `privateca_projects_locations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_operations_cancel_builder()` + `privateca_projects_locations_operations_cancel_execute()`.
/// For task-level control, use `privateca_projects_locations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_operations_cancel_builder(client, &args.name)?;
    privateca_projects_locations_operations_cancel_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_operations_delete_execute()` to send, or `privateca_projects_locations_operations_delete` for simplest API.

pub fn privateca_projects_locations_operations_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_operations_delete_execute()` or `privateca_projects_locations_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_operations_delete_task()`.
/// For the simplest API, use `privateca_projects_locations_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_operations_delete_builder()` + `privateca_projects_locations_operations_delete_execute()`.
/// For task-level control, use `privateca_projects_locations_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_operations_delete(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_operations_delete_builder(client, &args.name)?;
    privateca_projects_locations_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_operations_get_execute()` to send, or `privateca_projects_locations_operations_get` for simplest API.

pub fn privateca_projects_locations_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_operations_get_execute()` or `privateca_projects_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_operations_get_task()`.
/// For the simplest API, use `privateca_projects_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_operations_get_builder()` + `privateca_projects_locations_operations_get_execute()`.
/// For task-level control, use `privateca_projects_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_operations_get(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_operations_get_builder(client, &args.name)?;
    privateca_projects_locations_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `privateca_projects_locations_operations_list_execute()` to send, or `privateca_projects_locations_operations_list` for simplest API.

pub fn privateca_projects_locations_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    returnPartialSuccess: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://privateca.googleapis.com/v1/projects/{}/locations/{locationsId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `privateca_projects_locations_operations_list_execute()` or `privateca_projects_locations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `privateca_projects_locations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `privateca_projects_locations_operations_list_task()`.
/// For the simplest API, use `privateca_projects_locations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `privateca_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn privateca_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = privateca_projects_locations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`privateca_projects_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PrivatecaProjectsLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `privateca_projects_locations_operations_list_builder()` + `privateca_projects_locations_operations_list_execute()`.
/// For task-level control, use `privateca_projects_locations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn privateca_projects_locations_operations_list(
    client: &SimpleHttpClient,
    args: &PrivatecaProjectsLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = privateca_projects_locations_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    privateca_projects_locations_operations_list_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for Location
// =============================================================================

/// ResourceIdentifier implementation for Location with PrivatecaProjectsLocationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsGetArgs> for Location {
    fn generate_resource_id(&self, input: &PrivatecaProjectsLocationsGetArgs) -> String {
        format!("gcp::privateca::Location/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::Location"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLocationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLocationsResponse with PrivatecaProjectsLocationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsListArgs> for ListLocationsResponse {
    fn generate_resource_id(&self, input: &PrivatecaProjectsLocationsListArgs) -> String {
        format!("gcp::privateca::ListLocationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::ListLocationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with PrivatecaProjectsLocationsCaPoolsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCaPoolsCreateArgs> for Operation {
    fn generate_resource_id(&self, input: &PrivatecaProjectsLocationsCaPoolsCreateArgs) -> String {
        format!("gcp::privateca::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with PrivatecaProjectsLocationsCaPoolsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCaPoolsDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &PrivatecaProjectsLocationsCaPoolsDeleteArgs) -> String {
        format!("gcp::privateca::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FetchCaCertsResponse
// =============================================================================

/// ResourceIdentifier implementation for FetchCaCertsResponse with PrivatecaProjectsLocationsCaPoolsFetchCaCertsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCaPoolsFetchCaCertsArgs>
    for FetchCaCertsResponse
{
    fn generate_resource_id(
        &self,
        input: &PrivatecaProjectsLocationsCaPoolsFetchCaCertsArgs,
    ) -> String {
        format!("gcp::privateca::FetchCaCertsResponse/{}", input.caPool)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::FetchCaCertsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CaPool
// =============================================================================

/// ResourceIdentifier implementation for CaPool with PrivatecaProjectsLocationsCaPoolsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCaPoolsGetArgs> for CaPool {
    fn generate_resource_id(&self, input: &PrivatecaProjectsLocationsCaPoolsGetArgs) -> String {
        format!("gcp::privateca::CaPool/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::CaPool"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with PrivatecaProjectsLocationsCaPoolsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCaPoolsGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &PrivatecaProjectsLocationsCaPoolsGetIamPolicyArgs,
    ) -> String {
        format!("gcp::privateca::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListCaPoolsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListCaPoolsResponse with PrivatecaProjectsLocationsCaPoolsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCaPoolsListArgs> for ListCaPoolsResponse {
    fn generate_resource_id(&self, input: &PrivatecaProjectsLocationsCaPoolsListArgs) -> String {
        format!("gcp::privateca::ListCaPoolsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::ListCaPoolsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with PrivatecaProjectsLocationsCaPoolsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCaPoolsPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &PrivatecaProjectsLocationsCaPoolsPatchArgs) -> String {
        format!("gcp::privateca::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with PrivatecaProjectsLocationsCaPoolsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCaPoolsSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &PrivatecaProjectsLocationsCaPoolsSetIamPolicyArgs,
    ) -> String {
        format!("gcp::privateca::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with PrivatecaProjectsLocationsCaPoolsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCaPoolsTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &PrivatecaProjectsLocationsCaPoolsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::privateca::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesActivateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesActivateArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesActivateArgs,
    ) -> String {
        format!("gcp::privateca::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCreateArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCreateArgs,
    ) -> String {
        format!("gcp::privateca::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesDeleteArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesDeleteArgs,
    ) -> String {
        format!("gcp::privateca::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesDisableArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesDisableArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesDisableArgs,
    ) -> String {
        format!("gcp::privateca::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesEnableArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesEnableArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesEnableArgs,
    ) -> String {
        format!("gcp::privateca::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FetchCertificateAuthorityCsrResponse
// =============================================================================

/// ResourceIdentifier implementation for FetchCertificateAuthorityCsrResponse with PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesFetchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesFetchArgs>
    for FetchCertificateAuthorityCsrResponse
{
    fn generate_resource_id(
        &self,
        input: &PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesFetchArgs,
    ) -> String {
        format!(
            "gcp::privateca::FetchCertificateAuthorityCsrResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::FetchCertificateAuthorityCsrResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CertificateAuthority
// =============================================================================

/// ResourceIdentifier implementation for CertificateAuthority with PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesGetArgs>
    for CertificateAuthority
{
    fn generate_resource_id(
        &self,
        input: &PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesGetArgs,
    ) -> String {
        format!("gcp::privateca::CertificateAuthority/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::CertificateAuthority"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListCertificateAuthoritiesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListCertificateAuthoritiesResponse with PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesListArgs>
    for ListCertificateAuthoritiesResponse
{
    fn generate_resource_id(
        &self,
        input: &PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesListArgs,
    ) -> String {
        format!(
            "gcp::privateca::ListCertificateAuthoritiesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::ListCertificateAuthoritiesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesPatchArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesPatchArgs,
    ) -> String {
        format!("gcp::privateca::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesUndeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesUndeleteArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesUndeleteArgs,
    ) -> String {
        format!("gcp::privateca::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CertificateRevocationList
// =============================================================================

/// ResourceIdentifier implementation for CertificateRevocationList with PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCertificateRevocationListsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCertificateRevocationListsGetArgs,
    > for CertificateRevocationList
{
    fn generate_resource_id(
        &self,
        input: &PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCertificateRevocationListsGetArgs,
    ) -> String {
        format!("gcp::privateca::CertificateRevocationList/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::CertificateRevocationList"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCertificateRevocationListsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCertificateRevocationListsGetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCertificateRevocationListsGetIamPolicyArgs) -> String {
        format!("gcp::privateca::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListCertificateRevocationListsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListCertificateRevocationListsResponse with PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCertificateRevocationListsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCertificateRevocationListsListArgs,
    > for ListCertificateRevocationListsResponse
{
    fn generate_resource_id(
        &self,
        input: &PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCertificateRevocationListsListArgs,
    ) -> String {
        format!(
            "gcp::privateca::ListCertificateRevocationListsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::ListCertificateRevocationListsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCertificateRevocationListsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCertificateRevocationListsPatchArgs,
    > for Operation
{
    fn generate_resource_id(
        &self,
        input: &PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCertificateRevocationListsPatchArgs,
    ) -> String {
        format!("gcp::privateca::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCertificateRevocationListsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCertificateRevocationListsSetIamPolicyArgs> for Policy {
    fn generate_resource_id(&self, input: &PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCertificateRevocationListsSetIamPolicyArgs) -> String {
        format!("gcp::privateca::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCertificateRevocationListsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCertificateRevocationListsTestIamPermissionsArgs> for TestIamPermissionsResponse {
    fn generate_resource_id(&self, input: &PrivatecaProjectsLocationsCaPoolsCertificateAuthoritiesCertificateRevocationListsTestIamPermissionsArgs) -> String {
        format!("gcp::privateca::TestIamPermissionsResponse/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Certificate
// =============================================================================

/// ResourceIdentifier implementation for Certificate with PrivatecaProjectsLocationsCaPoolsCertificatesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCaPoolsCertificatesCreateArgs> for Certificate {
    fn generate_resource_id(
        &self,
        input: &PrivatecaProjectsLocationsCaPoolsCertificatesCreateArgs,
    ) -> String {
        format!("gcp::privateca::Certificate/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::Certificate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Certificate
// =============================================================================

/// ResourceIdentifier implementation for Certificate with PrivatecaProjectsLocationsCaPoolsCertificatesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCaPoolsCertificatesGetArgs> for Certificate {
    fn generate_resource_id(
        &self,
        input: &PrivatecaProjectsLocationsCaPoolsCertificatesGetArgs,
    ) -> String {
        format!("gcp::privateca::Certificate/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::Certificate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListCertificatesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListCertificatesResponse with PrivatecaProjectsLocationsCaPoolsCertificatesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCaPoolsCertificatesListArgs>
    for ListCertificatesResponse
{
    fn generate_resource_id(
        &self,
        input: &PrivatecaProjectsLocationsCaPoolsCertificatesListArgs,
    ) -> String {
        format!("gcp::privateca::ListCertificatesResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::ListCertificatesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Certificate
// =============================================================================

/// ResourceIdentifier implementation for Certificate with PrivatecaProjectsLocationsCaPoolsCertificatesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCaPoolsCertificatesPatchArgs> for Certificate {
    fn generate_resource_id(
        &self,
        input: &PrivatecaProjectsLocationsCaPoolsCertificatesPatchArgs,
    ) -> String {
        format!("gcp::privateca::Certificate/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::Certificate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Certificate
// =============================================================================

/// ResourceIdentifier implementation for Certificate with PrivatecaProjectsLocationsCaPoolsCertificatesRevokeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCaPoolsCertificatesRevokeArgs> for Certificate {
    fn generate_resource_id(
        &self,
        input: &PrivatecaProjectsLocationsCaPoolsCertificatesRevokeArgs,
    ) -> String {
        format!("gcp::privateca::Certificate/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::Certificate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with PrivatecaProjectsLocationsCertificateTemplatesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCertificateTemplatesCreateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &PrivatecaProjectsLocationsCertificateTemplatesCreateArgs,
    ) -> String {
        format!("gcp::privateca::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with PrivatecaProjectsLocationsCertificateTemplatesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCertificateTemplatesDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &PrivatecaProjectsLocationsCertificateTemplatesDeleteArgs,
    ) -> String {
        format!("gcp::privateca::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CertificateTemplate
// =============================================================================

/// ResourceIdentifier implementation for CertificateTemplate with PrivatecaProjectsLocationsCertificateTemplatesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCertificateTemplatesGetArgs>
    for CertificateTemplate
{
    fn generate_resource_id(
        &self,
        input: &PrivatecaProjectsLocationsCertificateTemplatesGetArgs,
    ) -> String {
        format!("gcp::privateca::CertificateTemplate/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::CertificateTemplate"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with PrivatecaProjectsLocationsCertificateTemplatesGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCertificateTemplatesGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &PrivatecaProjectsLocationsCertificateTemplatesGetIamPolicyArgs,
    ) -> String {
        format!("gcp::privateca::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListCertificateTemplatesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListCertificateTemplatesResponse with PrivatecaProjectsLocationsCertificateTemplatesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCertificateTemplatesListArgs>
    for ListCertificateTemplatesResponse
{
    fn generate_resource_id(
        &self,
        input: &PrivatecaProjectsLocationsCertificateTemplatesListArgs,
    ) -> String {
        format!(
            "gcp::privateca::ListCertificateTemplatesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::ListCertificateTemplatesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with PrivatecaProjectsLocationsCertificateTemplatesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCertificateTemplatesPatchArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &PrivatecaProjectsLocationsCertificateTemplatesPatchArgs,
    ) -> String {
        format!("gcp::privateca::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with PrivatecaProjectsLocationsCertificateTemplatesSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCertificateTemplatesSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &PrivatecaProjectsLocationsCertificateTemplatesSetIamPolicyArgs,
    ) -> String {
        format!("gcp::privateca::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with PrivatecaProjectsLocationsCertificateTemplatesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsCertificateTemplatesTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &PrivatecaProjectsLocationsCertificateTemplatesTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::privateca::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with PrivatecaProjectsLocationsOperationsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsOperationsCancelArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &PrivatecaProjectsLocationsOperationsCancelArgs,
    ) -> String {
        format!("gcp::privateca::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with PrivatecaProjectsLocationsOperationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsOperationsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &PrivatecaProjectsLocationsOperationsDeleteArgs,
    ) -> String {
        format!("gcp::privateca::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with PrivatecaProjectsLocationsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsOperationsGetArgs> for Operation {
    fn generate_resource_id(&self, input: &PrivatecaProjectsLocationsOperationsGetArgs) -> String {
        format!("gcp::privateca::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListOperationsResponse with PrivatecaProjectsLocationsOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PrivatecaProjectsLocationsOperationsListArgs> for ListOperationsResponse {
    fn generate_resource_id(&self, input: &PrivatecaProjectsLocationsOperationsListArgs) -> String {
        format!("gcp::privateca::ListOperationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::privateca::ListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
