//! Auto-generated API clients for gcp/datalabeling.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1beta1/projects/{projectsId}/annotationSpecSets
/// Creates an annotation spec set by providing a set of labels.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalabeling_projects_annotation_spec_sets_create_execute()` to send, or `datalabeling_projects_annotation_spec_sets_create` for simplest API.

pub fn datalabeling_projects_annotation_spec_sets_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &GoogleCloudDatalabelingV1beta1CreateAnnotationSpecSetRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://datalabeling.googleapis.com/v1beta1/projects/{}/annotationSpecSets",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/projects/{projectsId}/annotationSpecSets
/// Creates an annotation spec set by providing a set of labels.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datalabeling_projects_annotation_spec_sets_create_execute()` or `datalabeling_projects_annotation_spec_sets_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalabeling_projects_annotation_spec_sets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalabeling_projects_annotation_spec_sets_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDatalabelingV1beta1AnnotationSpecSet>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatalabelingV1beta1AnnotationSpecSet =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}/annotationSpecSets
/// Creates an annotation spec set by providing a set of labels.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datalabeling_projects_annotation_spec_sets_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datalabeling_projects_annotation_spec_sets_create_task()`.
/// For the simplest API, use `datalabeling_projects_annotation_spec_sets_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalabeling_projects_annotation_spec_sets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datalabeling_projects_annotation_spec_sets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatalabelingV1beta1AnnotationSpecSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datalabeling_projects_annotation_spec_sets_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datalabeling_projects_annotation_spec_sets_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatalabelingProjectsAnnotationSpecSetsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudDatalabelingV1beta1CreateAnnotationSpecSetRequest,
}

/// GET v1beta1/projects/{projectsId}/annotationSpecSets
/// Creates an annotation spec set by providing a set of labels.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalabeling_projects_annotation_spec_sets_create_builder()` + `datalabeling_projects_annotation_spec_sets_create_execute()`.
/// For task-level control, use `datalabeling_projects_annotation_spec_sets_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalabeling_projects_annotation_spec_sets_create(
    client: &SimpleHttpClient,
    args: &DatalabelingProjectsAnnotationSpecSetsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatalabelingV1beta1AnnotationSpecSet>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalabeling_projects_annotation_spec_sets_create_builder(
        client,
        args.parent.clone(),
        &args.body,
    )?;
    datalabeling_projects_annotation_spec_sets_create_execute(builder)
}

/// GET v1beta1/projects/{projectsId}/datasets
/// Creates dataset. If success return a Dataset resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalabeling_projects_datasets_create_execute()` to send, or `datalabeling_projects_datasets_create` for simplest API.

pub fn datalabeling_projects_datasets_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &GoogleCloudDatalabelingV1beta1CreateDatasetRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://datalabeling.googleapis.com/v1beta1/projects/{}/datasets",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/projects/{projectsId}/datasets
/// Creates dataset. If success return a Dataset resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datalabeling_projects_datasets_create_execute()` or `datalabeling_projects_datasets_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalabeling_projects_datasets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalabeling_projects_datasets_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDatalabelingV1beta1Dataset>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatalabelingV1beta1Dataset = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}/datasets
/// Creates dataset. If success return a Dataset resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datalabeling_projects_datasets_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datalabeling_projects_datasets_create_task()`.
/// For the simplest API, use `datalabeling_projects_datasets_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalabeling_projects_datasets_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datalabeling_projects_datasets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatalabelingV1beta1Dataset>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datalabeling_projects_datasets_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datalabeling_projects_datasets_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatalabelingProjectsDatasetsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudDatalabelingV1beta1CreateDatasetRequest,
}

/// GET v1beta1/projects/{projectsId}/datasets
/// Creates dataset. If success return a Dataset resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalabeling_projects_datasets_create_builder()` + `datalabeling_projects_datasets_create_execute()`.
/// For task-level control, use `datalabeling_projects_datasets_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalabeling_projects_datasets_create(
    client: &SimpleHttpClient,
    args: &DatalabelingProjectsDatasetsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatalabelingV1beta1Dataset>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        datalabeling_projects_datasets_create_builder(client, args.parent.clone(), &args.body)?;
    datalabeling_projects_datasets_create_execute(builder)
}

/// GET v1beta1/projects/{projectsId}/evaluationJobs
/// Creates an evaluation job.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalabeling_projects_evaluation_jobs_create_execute()` to send, or `datalabeling_projects_evaluation_jobs_create` for simplest API.

pub fn datalabeling_projects_evaluation_jobs_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &GoogleCloudDatalabelingV1beta1CreateEvaluationJobRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://datalabeling.googleapis.com/v1beta1/projects/{}/evaluationJobs",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/projects/{projectsId}/evaluationJobs
/// Creates an evaluation job.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datalabeling_projects_evaluation_jobs_create_execute()` or `datalabeling_projects_evaluation_jobs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalabeling_projects_evaluation_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalabeling_projects_evaluation_jobs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudDatalabelingV1beta1EvaluationJob>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatalabelingV1beta1EvaluationJob =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}/evaluationJobs
/// Creates an evaluation job.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datalabeling_projects_evaluation_jobs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datalabeling_projects_evaluation_jobs_create_task()`.
/// For the simplest API, use `datalabeling_projects_evaluation_jobs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalabeling_projects_evaluation_jobs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datalabeling_projects_evaluation_jobs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatalabelingV1beta1EvaluationJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datalabeling_projects_evaluation_jobs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datalabeling_projects_evaluation_jobs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatalabelingProjectsEvaluationJobsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudDatalabelingV1beta1CreateEvaluationJobRequest,
}

/// GET v1beta1/projects/{projectsId}/evaluationJobs
/// Creates an evaluation job.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalabeling_projects_evaluation_jobs_create_builder()` + `datalabeling_projects_evaluation_jobs_create_execute()`.
/// For task-level control, use `datalabeling_projects_evaluation_jobs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalabeling_projects_evaluation_jobs_create(
    client: &SimpleHttpClient,
    args: &DatalabelingProjectsEvaluationJobsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudDatalabelingV1beta1EvaluationJob>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalabeling_projects_evaluation_jobs_create_builder(
        client,
        args.parent.clone(),
        &args.body,
    )?;
    datalabeling_projects_evaluation_jobs_create_execute(builder)
}

/// GET v1beta1/projects/{projectsId}/evaluations:search
/// Searches evaluations within a project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalabeling_projects_evaluations_search_execute()` to send, or `datalabeling_projects_evaluations_search` for simplest API.

pub fn datalabeling_projects_evaluations_search_builder(
    client: &SimpleHttpClient,
    parent: String,
    filter: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://datalabeling.googleapis.com/v1beta1/projects/{}/evaluations:search",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/projects/{projectsId}/evaluations:search
/// Searches evaluations within a project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datalabeling_projects_evaluations_search_execute()` or `datalabeling_projects_evaluations_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalabeling_projects_evaluations_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalabeling_projects_evaluations_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleCloudDatalabelingV1beta1SearchEvaluationsResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudDatalabelingV1beta1SearchEvaluationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}/evaluations:search
/// Searches evaluations within a project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datalabeling_projects_evaluations_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datalabeling_projects_evaluations_search_task()`.
/// For the simplest API, use `datalabeling_projects_evaluations_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalabeling_projects_evaluations_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datalabeling_projects_evaluations_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatalabelingV1beta1SearchEvaluationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datalabeling_projects_evaluations_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datalabeling_projects_evaluations_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatalabelingProjectsEvaluationsSearchArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1beta1/projects/{projectsId}/evaluations:search
/// Searches evaluations within a project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalabeling_projects_evaluations_search_builder()` + `datalabeling_projects_evaluations_search_execute()`.
/// For task-level control, use `datalabeling_projects_evaluations_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalabeling_projects_evaluations_search(
    client: &SimpleHttpClient,
    args: &DatalabelingProjectsEvaluationsSearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleCloudDatalabelingV1beta1SearchEvaluationsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalabeling_projects_evaluations_search_builder(
        client,
        args.parent.clone(),
        args.filter.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    datalabeling_projects_evaluations_search_execute(builder)
}

/// GET v1beta1/projects/{projectsId}/instructions
/// Creates an instruction for how data should be labeled.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalabeling_projects_instructions_create_execute()` to send, or `datalabeling_projects_instructions_create` for simplest API.

pub fn datalabeling_projects_instructions_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &GoogleCloudDatalabelingV1beta1CreateInstructionRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://datalabeling.googleapis.com/v1beta1/projects/{}/instructions",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1beta1/projects/{projectsId}/instructions
/// Creates an instruction for how data should be labeled.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datalabeling_projects_instructions_create_execute()` or `datalabeling_projects_instructions_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalabeling_projects_instructions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalabeling_projects_instructions_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}/instructions
/// Creates an instruction for how data should be labeled.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datalabeling_projects_instructions_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datalabeling_projects_instructions_create_task()`.
/// For the simplest API, use `datalabeling_projects_instructions_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalabeling_projects_instructions_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datalabeling_projects_instructions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datalabeling_projects_instructions_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datalabeling_projects_instructions_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatalabelingProjectsInstructionsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: GoogleCloudDatalabelingV1beta1CreateInstructionRequest,
}

/// GET v1beta1/projects/{projectsId}/instructions
/// Creates an instruction for how data should be labeled.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalabeling_projects_instructions_create_builder()` + `datalabeling_projects_instructions_create_execute()`.
/// For task-level control, use `datalabeling_projects_instructions_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalabeling_projects_instructions_create(
    client: &SimpleHttpClient,
    args: &DatalabelingProjectsInstructionsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        datalabeling_projects_instructions_create_builder(client, args.parent.clone(), &args.body)?;
    datalabeling_projects_instructions_create_execute(builder)
}

/// GET v1beta1/projects/{projectsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `datalabeling_projects_operations_list_execute()` to send, or `datalabeling_projects_operations_list` for simplest API.

pub fn datalabeling_projects_operations_list_builder(
    client: &SimpleHttpClient,
    name: String,
    filter: Option<String>,
    pageSize: Option<i32>,
    pageToken: Option<String>,
    returnPartialSuccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://datalabeling.googleapis.com/v1beta1/projects/{}/operations",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1beta1/projects/{projectsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `datalabeling_projects_operations_list_execute()` or `datalabeling_projects_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalabeling_projects_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalabeling_projects_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1beta1/projects/{projectsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `datalabeling_projects_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `datalabeling_projects_operations_list_task()`.
/// For the simplest API, use `datalabeling_projects_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `datalabeling_projects_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn datalabeling_projects_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = datalabeling_projects_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`datalabeling_projects_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatalabelingProjectsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<bool>,
}

/// GET v1beta1/projects/{projectsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn't support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `datalabeling_projects_operations_list_builder()` + `datalabeling_projects_operations_list_execute()`.
/// For task-level control, use `datalabeling_projects_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn datalabeling_projects_operations_list(
    client: &SimpleHttpClient,
    args: &DatalabelingProjectsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = datalabeling_projects_operations_list_builder(
        client,
        args.name.clone(),
        args.filter.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
        args.returnPartialSuccess.clone(),
    )?;
    datalabeling_projects_operations_list_execute(builder)
}
