//! Auto-generated API clients for gcp/cloudcommerceprocurement.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/{+name}:approve
/// Grants an approval on an Account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudcommerceprocurement_providers_accounts_approve_execute()` to send, or `cloudcommerceprocurement_providers_accounts_approve` for simplest API.

pub fn cloudcommerceprocurement_providers_accounts_approve_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ApproveAccountRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:approve", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:approve
/// Grants an approval on an Account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudcommerceprocurement_providers_accounts_approve_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudcommerceprocurement_providers_accounts_approve_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcommerceprocurement_providers_accounts_approve_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:approve
/// Grants an approval on an Account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudcommerceprocurement_providers_accounts_approve_builder()` + `cloudcommerceprocurement_providers_accounts_approve_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcommerceprocurement_providers_accounts_approve(
    client: &SimpleHttpClient,
    name: &str,
    body: &ApproveAccountRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudcommerceprocurement_providers_accounts_approve_builder(client, name, body)?;
    cloudcommerceprocurement_providers_accounts_approve_execute(builder)
}

/// GET v1/{+name}
/// Gets a requested Account resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudcommerceprocurement_providers_accounts_get_execute()` to send, or `cloudcommerceprocurement_providers_accounts_get` for simplest API.

pub fn cloudcommerceprocurement_providers_accounts_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "view",
            view.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+name}
/// Gets a requested Account resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudcommerceprocurement_providers_accounts_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudcommerceprocurement_providers_accounts_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcommerceprocurement_providers_accounts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a requested Account resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudcommerceprocurement_providers_accounts_get_builder()` + `cloudcommerceprocurement_providers_accounts_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcommerceprocurement_providers_accounts_get(
    client: &SimpleHttpClient,
    name: &str,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudcommerceprocurement_providers_accounts_get_builder(client, name, view)?;
    cloudcommerceprocurement_providers_accounts_get_execute(builder)
}

/// GET v1/{+parent}/accounts
/// Lists Accounts that the provider has access to.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudcommerceprocurement_providers_accounts_list_execute()` to send, or `cloudcommerceprocurement_providers_accounts_list` for simplest API.

pub fn cloudcommerceprocurement_providers_accounts_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/accounts", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/accounts
/// Lists Accounts that the provider has access to.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudcommerceprocurement_providers_accounts_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudcommerceprocurement_providers_accounts_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcommerceprocurement_providers_accounts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAccountsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAccountsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/accounts
/// Lists Accounts that the provider has access to.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudcommerceprocurement_providers_accounts_list_builder()` + `cloudcommerceprocurement_providers_accounts_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcommerceprocurement_providers_accounts_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAccountsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudcommerceprocurement_providers_accounts_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    cloudcommerceprocurement_providers_accounts_list_execute(builder)
}

/// GET v1/{+name}:reject
/// Rejects an approval on an Account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudcommerceprocurement_providers_accounts_reject_execute()` to send, or `cloudcommerceprocurement_providers_accounts_reject` for simplest API.

pub fn cloudcommerceprocurement_providers_accounts_reject_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &RejectAccountRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:reject", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:reject
/// Rejects an approval on an Account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudcommerceprocurement_providers_accounts_reject_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudcommerceprocurement_providers_accounts_reject_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcommerceprocurement_providers_accounts_reject_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:reject
/// Rejects an approval on an Account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudcommerceprocurement_providers_accounts_reject_builder()` + `cloudcommerceprocurement_providers_accounts_reject_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcommerceprocurement_providers_accounts_reject(
    client: &SimpleHttpClient,
    name: &str,
    body: &RejectAccountRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudcommerceprocurement_providers_accounts_reject_builder(client, name, body)?;
    cloudcommerceprocurement_providers_accounts_reject_execute(builder)
}

/// GET v1/{+name}:reset
/// Resets an Account and cancels all associated Entitlements. Partner can only reset accounts they own rather than customer accounts.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudcommerceprocurement_providers_accounts_reset_execute()` to send, or `cloudcommerceprocurement_providers_accounts_reset` for simplest API.

pub fn cloudcommerceprocurement_providers_accounts_reset_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ResetAccountRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:reset", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:reset
/// Resets an Account and cancels all associated Entitlements. Partner can only reset accounts they own rather than customer accounts.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudcommerceprocurement_providers_accounts_reset_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudcommerceprocurement_providers_accounts_reset_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcommerceprocurement_providers_accounts_reset_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:reset
/// Resets an Account and cancels all associated Entitlements. Partner can only reset accounts they own rather than customer accounts.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudcommerceprocurement_providers_accounts_reset_builder()` + `cloudcommerceprocurement_providers_accounts_reset_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcommerceprocurement_providers_accounts_reset(
    client: &SimpleHttpClient,
    name: &str,
    body: &ResetAccountRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudcommerceprocurement_providers_accounts_reset_builder(client, name, body)?;
    cloudcommerceprocurement_providers_accounts_reset_execute(builder)
}

/// GET v1/{+name}:approve
/// Approves an entitlement that is in the EntitlementState.ENTITLEMENT_ACTIVATION_REQUESTED state. This method is invoked by the provider to approve the creation of the entitlement resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudcommerceprocurement_providers_entitlements_approve_execute()` to send, or `cloudcommerceprocurement_providers_entitlements_approve` for simplest API.

pub fn cloudcommerceprocurement_providers_entitlements_approve_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ApproveEntitlementRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:approve", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:approve
/// Approves an entitlement that is in the EntitlementState.ENTITLEMENT_ACTIVATION_REQUESTED state. This method is invoked by the provider to approve the creation of the entitlement resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudcommerceprocurement_providers_entitlements_approve_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudcommerceprocurement_providers_entitlements_approve_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcommerceprocurement_providers_entitlements_approve_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:approve
/// Approves an entitlement that is in the EntitlementState.ENTITLEMENT_ACTIVATION_REQUESTED state. This method is invoked by the provider to approve the creation of the entitlement resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudcommerceprocurement_providers_entitlements_approve_builder()` + `cloudcommerceprocurement_providers_entitlements_approve_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcommerceprocurement_providers_entitlements_approve(
    client: &SimpleHttpClient,
    name: &str,
    body: &ApproveEntitlementRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudcommerceprocurement_providers_entitlements_approve_builder(client, name, body)?;
    cloudcommerceprocurement_providers_entitlements_approve_execute(builder)
}

/// GET v1/{+name}:approvePlanChange
/// Approves an entitlement plan change that is in the EntitlementState.ENTITLEMENT_PENDING_PLAN_CHANGE_APPROVAL state. This method is invoked by the provider to approve the plan change on the entitlement resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudcommerceprocurement_providers_entitlements_approve_plan_change_execute()` to send, or `cloudcommerceprocurement_providers_entitlements_approve_plan_change` for simplest API.

pub fn cloudcommerceprocurement_providers_entitlements_approve_plan_change_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ApproveEntitlementPlanChangeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:approvePlanChange", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:approvePlanChange
/// Approves an entitlement plan change that is in the EntitlementState.ENTITLEMENT_PENDING_PLAN_CHANGE_APPROVAL state. This method is invoked by the provider to approve the plan change on the entitlement resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudcommerceprocurement_providers_entitlements_approve_plan_change_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudcommerceprocurement_providers_entitlements_approve_plan_change_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcommerceprocurement_providers_entitlements_approve_plan_change_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:approvePlanChange
/// Approves an entitlement plan change that is in the EntitlementState.ENTITLEMENT_PENDING_PLAN_CHANGE_APPROVAL state. This method is invoked by the provider to approve the plan change on the entitlement resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudcommerceprocurement_providers_entitlements_approve_plan_change_builder()` + `cloudcommerceprocurement_providers_entitlements_approve_plan_change_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcommerceprocurement_providers_entitlements_approve_plan_change(
    client: &SimpleHttpClient,
    name: &str,
    body: &ApproveEntitlementPlanChangeRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudcommerceprocurement_providers_entitlements_approve_plan_change_builder(
        client, name, body,
    )?;
    cloudcommerceprocurement_providers_entitlements_approve_plan_change_execute(builder)
}

/// GET v1/{+name}
/// Gets a requested Entitlement resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudcommerceprocurement_providers_entitlements_get_execute()` to send, or `cloudcommerceprocurement_providers_entitlements_get` for simplest API.

pub fn cloudcommerceprocurement_providers_entitlements_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a requested Entitlement resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudcommerceprocurement_providers_entitlements_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudcommerceprocurement_providers_entitlements_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcommerceprocurement_providers_entitlements_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Entitlement>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Entitlement = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Gets a requested Entitlement resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudcommerceprocurement_providers_entitlements_get_builder()` + `cloudcommerceprocurement_providers_entitlements_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcommerceprocurement_providers_entitlements_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Entitlement>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudcommerceprocurement_providers_entitlements_get_builder(client, name)?;
    cloudcommerceprocurement_providers_entitlements_get_execute(builder)
}

/// GET v1/{+parent}/entitlements
/// Lists Entitlements for which the provider has read access.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudcommerceprocurement_providers_entitlements_list_execute()` to send, or `cloudcommerceprocurement_providers_entitlements_list` for simplest API.

pub fn cloudcommerceprocurement_providers_entitlements_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/entitlements", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/entitlements
/// Lists Entitlements for which the provider has read access.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudcommerceprocurement_providers_entitlements_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudcommerceprocurement_providers_entitlements_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcommerceprocurement_providers_entitlements_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListEntitlementsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListEntitlementsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/entitlements
/// Lists Entitlements for which the provider has read access.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudcommerceprocurement_providers_entitlements_list_builder()` + `cloudcommerceprocurement_providers_entitlements_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcommerceprocurement_providers_entitlements_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListEntitlementsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = cloudcommerceprocurement_providers_entitlements_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    cloudcommerceprocurement_providers_entitlements_list_execute(builder)
}

/// GET v1/{+name}
/// Updates an existing Entitlement.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudcommerceprocurement_providers_entitlements_patch_execute()` to send, or `cloudcommerceprocurement_providers_entitlements_patch` for simplest API.

pub fn cloudcommerceprocurement_providers_entitlements_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Entitlement,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}
/// Updates an existing Entitlement.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudcommerceprocurement_providers_entitlements_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudcommerceprocurement_providers_entitlements_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcommerceprocurement_providers_entitlements_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Entitlement>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Entitlement = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Updates an existing Entitlement.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudcommerceprocurement_providers_entitlements_patch_builder()` + `cloudcommerceprocurement_providers_entitlements_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcommerceprocurement_providers_entitlements_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Entitlement,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Entitlement>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudcommerceprocurement_providers_entitlements_patch_builder(
        client, name, updateMask, body,
    )?;
    cloudcommerceprocurement_providers_entitlements_patch_execute(builder)
}

/// GET v1/{+name}:reject
/// Rejects an entitlement that is in the EntitlementState.ENTITLEMENT_ACTIVATION_REQUESTED state. This method is invoked by the provider to reject the creation of the entitlement resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudcommerceprocurement_providers_entitlements_reject_execute()` to send, or `cloudcommerceprocurement_providers_entitlements_reject` for simplest API.

pub fn cloudcommerceprocurement_providers_entitlements_reject_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &RejectEntitlementRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:reject", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:reject
/// Rejects an entitlement that is in the EntitlementState.ENTITLEMENT_ACTIVATION_REQUESTED state. This method is invoked by the provider to reject the creation of the entitlement resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudcommerceprocurement_providers_entitlements_reject_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudcommerceprocurement_providers_entitlements_reject_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcommerceprocurement_providers_entitlements_reject_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:reject
/// Rejects an entitlement that is in the EntitlementState.ENTITLEMENT_ACTIVATION_REQUESTED state. This method is invoked by the provider to reject the creation of the entitlement resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudcommerceprocurement_providers_entitlements_reject_builder()` + `cloudcommerceprocurement_providers_entitlements_reject_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcommerceprocurement_providers_entitlements_reject(
    client: &SimpleHttpClient,
    name: &str,
    body: &RejectEntitlementRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudcommerceprocurement_providers_entitlements_reject_builder(client, name, body)?;
    cloudcommerceprocurement_providers_entitlements_reject_execute(builder)
}

/// GET v1/{+name}:rejectPlanChange
/// Rejects an entitlement plan change that is in the EntitlementState.ENTITLEMENT_PENDING_PLAN_CHANGE_APPROVAL state. This method is invoked by the provider to reject the plan change on the entitlement resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudcommerceprocurement_providers_entitlements_reject_plan_change_execute()` to send, or `cloudcommerceprocurement_providers_entitlements_reject_plan_change` for simplest API.

pub fn cloudcommerceprocurement_providers_entitlements_reject_plan_change_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &RejectEntitlementPlanChangeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:rejectPlanChange", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:rejectPlanChange
/// Rejects an entitlement plan change that is in the EntitlementState.ENTITLEMENT_PENDING_PLAN_CHANGE_APPROVAL state. This method is invoked by the provider to reject the plan change on the entitlement resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudcommerceprocurement_providers_entitlements_reject_plan_change_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudcommerceprocurement_providers_entitlements_reject_plan_change_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcommerceprocurement_providers_entitlements_reject_plan_change_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:rejectPlanChange
/// Rejects an entitlement plan change that is in the EntitlementState.ENTITLEMENT_PENDING_PLAN_CHANGE_APPROVAL state. This method is invoked by the provider to reject the plan change on the entitlement resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudcommerceprocurement_providers_entitlements_reject_plan_change_builder()` + `cloudcommerceprocurement_providers_entitlements_reject_plan_change_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcommerceprocurement_providers_entitlements_reject_plan_change(
    client: &SimpleHttpClient,
    name: &str,
    body: &RejectEntitlementPlanChangeRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = cloudcommerceprocurement_providers_entitlements_reject_plan_change_builder(
        client, name, body,
    )?;
    cloudcommerceprocurement_providers_entitlements_reject_plan_change_execute(builder)
}

/// GET v1/{+name}:suspend
/// Requests suspension of an active Entitlement. This is not yet supported.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `cloudcommerceprocurement_providers_entitlements_suspend_execute()` to send, or `cloudcommerceprocurement_providers_entitlements_suspend` for simplest API.

pub fn cloudcommerceprocurement_providers_entitlements_suspend_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &SuspendEntitlementRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}:suspend", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+name}:suspend
/// Requests suspension of an active Entitlement. This is not yet supported.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `cloudcommerceprocurement_providers_entitlements_suspend_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from cloudcommerceprocurement_providers_entitlements_suspend_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcommerceprocurement_providers_entitlements_suspend_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}:suspend
/// Requests suspension of an active Entitlement. This is not yet supported.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `cloudcommerceprocurement_providers_entitlements_suspend_builder()` + `cloudcommerceprocurement_providers_entitlements_suspend_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn cloudcommerceprocurement_providers_entitlements_suspend(
    client: &SimpleHttpClient,
    name: &str,
    body: &SuspendEntitlementRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        cloudcommerceprocurement_providers_entitlements_suspend_builder(client, name, body)?;
    cloudcommerceprocurement_providers_entitlements_suspend_execute(builder)
}
