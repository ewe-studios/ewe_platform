//! Auto-generated API clients for gcp/baremetalsolution.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v2/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_get_execute()` to send, or `baremetalsolution_projects_locations_get` for simplest API.

pub fn baremetalsolution_projects_locations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_get_execute()` or `baremetalsolution_projects_locations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Location>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_get_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_get_builder()` + `baremetalsolution_projects_locations_get_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_get(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_get_builder(client, &args.name)?;
    baremetalsolution_projects_locations_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_list_execute()` to send, or `baremetalsolution_projects_locations_list` for simplest API.

pub fn baremetalsolution_projects_locations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    extraLocationTypes: &Option<Option<String>>,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes.as_ref() {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_list_execute()` or `baremetalsolution_projects_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLocationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_list_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: extraLocationTypes
    pub extraLocationTypes: Option<Option<String>>,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v2/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_list_builder()` + `baremetalsolution_projects_locations_list_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_list(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_list_builder(
        client,
        &args.name,
        &args.extraLocationTypes,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    baremetalsolution_projects_locations_list_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:detachLun
/// Detach LUN from Instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_instances_detach_lun_execute()` to send, or `baremetalsolution_projects_locations_instances_detach_lun` for simplest API.

pub fn baremetalsolution_projects_locations_instances_detach_lun_builder<R>(
    client: &SimpleHttpClient<R>,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/instances/{instancesId}:detachLun",
        instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:detachLun
/// Detach LUN from Instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_instances_detach_lun_execute()` or `baremetalsolution_projects_locations_instances_detach_lun`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_instances_detach_lun_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_detach_lun_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:detachLun
/// Detach LUN from Instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_instances_detach_lun_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_instances_detach_lun_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_instances_detach_lun()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_instances_detach_lun_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_instances_detach_lun_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_instances_detach_lun_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_instances_detach_lun`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsInstancesDetachLunArgs {
    /// Path parameter: instance
    pub instance: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:detachLun
/// Detach LUN from Instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_instances_detach_lun_builder()` + `baremetalsolution_projects_locations_instances_detach_lun_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_instances_detach_lun_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_detach_lun(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsInstancesDetachLunArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        baremetalsolution_projects_locations_instances_detach_lun_builder(client, &args.instance)?;
    baremetalsolution_projects_locations_instances_detach_lun_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:disableHyperthreading
/// Perform disable hyperthreading operation on a single server.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_instances_disable_hyperthreading_execute()` to send, or `baremetalsolution_projects_locations_instances_disable_hyperthreading` for simplest API.

pub fn baremetalsolution_projects_locations_instances_disable_hyperthreading_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/instances/{instancesId}:disableHyperthreading",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:disableHyperthreading
/// Perform disable hyperthreading operation on a single server.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_instances_disable_hyperthreading_execute()` or `baremetalsolution_projects_locations_instances_disable_hyperthreading`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_instances_disable_hyperthreading_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_disable_hyperthreading_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:disableHyperthreading
/// Perform disable hyperthreading operation on a single server.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_instances_disable_hyperthreading_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_instances_disable_hyperthreading_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_instances_disable_hyperthreading()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_instances_disable_hyperthreading_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_instances_disable_hyperthreading_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_instances_disable_hyperthreading_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_instances_disable_hyperthreading`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsInstancesDisableHyperthreadingArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:disableHyperthreading
/// Perform disable hyperthreading operation on a single server.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_instances_disable_hyperthreading_builder()` + `baremetalsolution_projects_locations_instances_disable_hyperthreading_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_instances_disable_hyperthreading_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_disable_hyperthreading(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsInstancesDisableHyperthreadingArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_instances_disable_hyperthreading_builder(
        client, &args.name,
    )?;
    baremetalsolution_projects_locations_instances_disable_hyperthreading_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:disableInteractiveSerialConsole
/// Disable the interactive serial console feature on an instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_instances_disable_interactive_serial_console_execute()` to send, or `baremetalsolution_projects_locations_instances_disable_interactive_serial_console` for simplest API.

pub fn baremetalsolution_projects_locations_instances_disable_interactive_serial_console_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/instances/{instancesId}:disableInteractiveSerialConsole",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:disableInteractiveSerialConsole
/// Disable the interactive serial console feature on an instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_instances_disable_interactive_serial_console_execute()` or `baremetalsolution_projects_locations_instances_disable_interactive_serial_console`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_instances_disable_interactive_serial_console_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_disable_interactive_serial_console_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:disableInteractiveSerialConsole
/// Disable the interactive serial console feature on an instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_instances_disable_interactive_serial_console_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_instances_disable_interactive_serial_console_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_instances_disable_interactive_serial_console()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_instances_disable_interactive_serial_console_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_instances_disable_interactive_serial_console_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        baremetalsolution_projects_locations_instances_disable_interactive_serial_console_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_instances_disable_interactive_serial_console`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsInstancesDisableInteractiveSerialConsoleArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:disableInteractiveSerialConsole
/// Disable the interactive serial console feature on an instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_instances_disable_interactive_serial_console_builder()` + `baremetalsolution_projects_locations_instances_disable_interactive_serial_console_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_instances_disable_interactive_serial_console_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_disable_interactive_serial_console(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsInstancesDisableInteractiveSerialConsoleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        baremetalsolution_projects_locations_instances_disable_interactive_serial_console_builder(
            client, &args.name,
        )?;
    baremetalsolution_projects_locations_instances_disable_interactive_serial_console_execute(
        builder,
    )
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:enableHyperthreading
/// Perform enable hyperthreading operation on a single server.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_instances_enable_hyperthreading_execute()` to send, or `baremetalsolution_projects_locations_instances_enable_hyperthreading` for simplest API.

pub fn baremetalsolution_projects_locations_instances_enable_hyperthreading_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/instances/{instancesId}:enableHyperthreading",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:enableHyperthreading
/// Perform enable hyperthreading operation on a single server.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_instances_enable_hyperthreading_execute()` or `baremetalsolution_projects_locations_instances_enable_hyperthreading`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_instances_enable_hyperthreading_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_enable_hyperthreading_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:enableHyperthreading
/// Perform enable hyperthreading operation on a single server.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_instances_enable_hyperthreading_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_instances_enable_hyperthreading_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_instances_enable_hyperthreading()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_instances_enable_hyperthreading_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_instances_enable_hyperthreading_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_instances_enable_hyperthreading_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_instances_enable_hyperthreading`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsInstancesEnableHyperthreadingArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:enableHyperthreading
/// Perform enable hyperthreading operation on a single server.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_instances_enable_hyperthreading_builder()` + `baremetalsolution_projects_locations_instances_enable_hyperthreading_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_instances_enable_hyperthreading_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_enable_hyperthreading(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsInstancesEnableHyperthreadingArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_instances_enable_hyperthreading_builder(
        client, &args.name,
    )?;
    baremetalsolution_projects_locations_instances_enable_hyperthreading_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:enableInteractiveSerialConsole
/// Enable the interactive serial console feature on an instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_instances_enable_interactive_serial_console_execute()` to send, or `baremetalsolution_projects_locations_instances_enable_interactive_serial_console` for simplest API.

pub fn baremetalsolution_projects_locations_instances_enable_interactive_serial_console_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/instances/{instancesId}:enableInteractiveSerialConsole",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:enableInteractiveSerialConsole
/// Enable the interactive serial console feature on an instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_instances_enable_interactive_serial_console_execute()` or `baremetalsolution_projects_locations_instances_enable_interactive_serial_console`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_instances_enable_interactive_serial_console_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_enable_interactive_serial_console_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:enableInteractiveSerialConsole
/// Enable the interactive serial console feature on an instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_instances_enable_interactive_serial_console_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_instances_enable_interactive_serial_console_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_instances_enable_interactive_serial_console()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_instances_enable_interactive_serial_console_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_instances_enable_interactive_serial_console_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        baremetalsolution_projects_locations_instances_enable_interactive_serial_console_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_instances_enable_interactive_serial_console`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsInstancesEnableInteractiveSerialConsoleArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:enableInteractiveSerialConsole
/// Enable the interactive serial console feature on an instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_instances_enable_interactive_serial_console_builder()` + `baremetalsolution_projects_locations_instances_enable_interactive_serial_console_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_instances_enable_interactive_serial_console_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_enable_interactive_serial_console(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsInstancesEnableInteractiveSerialConsoleArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        baremetalsolution_projects_locations_instances_enable_interactive_serial_console_builder(
            client, &args.name,
        )?;
    baremetalsolution_projects_locations_instances_enable_interactive_serial_console_execute(
        builder,
    )
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
/// Get details about a single server.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_instances_get_execute()` to send, or `baremetalsolution_projects_locations_instances_get` for simplest API.

pub fn baremetalsolution_projects_locations_instances_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/instances/{instancesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
/// Get details about a single server.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_instances_get_execute()` or `baremetalsolution_projects_locations_instances_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_instances_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Instance>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Instance = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
/// Get details about a single server.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_instances_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_instances_get_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_instances_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_instances_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_instances_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Instance>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_instances_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_instances_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsInstancesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
/// Get details about a single server.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_instances_get_builder()` + `baremetalsolution_projects_locations_instances_get_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_instances_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_get(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsInstancesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Instance>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_instances_get_builder(client, &args.name)?;
    baremetalsolution_projects_locations_instances_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/instances
/// List servers in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_instances_list_execute()` to send, or `baremetalsolution_projects_locations_instances_list` for simplest API.

pub fn baremetalsolution_projects_locations_instances_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/instances",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/instances
/// List servers in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_instances_list_execute()` or `baremetalsolution_projects_locations_instances_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_instances_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListInstancesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListInstancesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/instances
/// List servers in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_instances_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_instances_list_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_instances_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_instances_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_instances_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListInstancesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_instances_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_instances_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsInstancesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/instances
/// List servers in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_instances_list_builder()` + `baremetalsolution_projects_locations_instances_list_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_instances_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_list(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsInstancesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListInstancesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_instances_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    baremetalsolution_projects_locations_instances_list_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:loadAuthInfo
/// Load auth info for a server.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_instances_load_auth_info_execute()` to send, or `baremetalsolution_projects_locations_instances_load_auth_info` for simplest API.

pub fn baremetalsolution_projects_locations_instances_load_auth_info_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/instances/{instancesId}:loadAuthInfo",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:loadAuthInfo
/// Load auth info for a server.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_instances_load_auth_info_execute()` or `baremetalsolution_projects_locations_instances_load_auth_info`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_instances_load_auth_info_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_load_auth_info_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LoadInstanceAuthInfoResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LoadInstanceAuthInfoResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:loadAuthInfo
/// Load auth info for a server.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_instances_load_auth_info_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_instances_load_auth_info_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_instances_load_auth_info()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_instances_load_auth_info_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_instances_load_auth_info_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LoadInstanceAuthInfoResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_instances_load_auth_info_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_instances_load_auth_info`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsInstancesLoadAuthInfoArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:loadAuthInfo
/// Load auth info for a server.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_instances_load_auth_info_builder()` + `baremetalsolution_projects_locations_instances_load_auth_info_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_instances_load_auth_info_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_load_auth_info(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsInstancesLoadAuthInfoArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LoadInstanceAuthInfoResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        baremetalsolution_projects_locations_instances_load_auth_info_builder(client, &args.name)?;
    baremetalsolution_projects_locations_instances_load_auth_info_execute(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
/// Update details of a single server.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_instances_patch_execute()` to send, or `baremetalsolution_projects_locations_instances_patch` for simplest API.

pub fn baremetalsolution_projects_locations_instances_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/instances/{instancesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
/// Update details of a single server.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_instances_patch_execute()` or `baremetalsolution_projects_locations_instances_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_instances_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
/// Update details of a single server.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_instances_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_instances_patch_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_instances_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_instances_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_instances_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_instances_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_instances_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsInstancesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}
/// Update details of a single server.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_instances_patch_builder()` + `baremetalsolution_projects_locations_instances_patch_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_instances_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_patch(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsInstancesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_instances_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    baremetalsolution_projects_locations_instances_patch_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:reimage
/// Perform reimage operation on a single server.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_instances_reimage_execute()` to send, or `baremetalsolution_projects_locations_instances_reimage` for simplest API.

pub fn baremetalsolution_projects_locations_instances_reimage_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/instances/{instancesId}:reimage",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:reimage
/// Perform reimage operation on a single server.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_instances_reimage_execute()` or `baremetalsolution_projects_locations_instances_reimage`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_instances_reimage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_reimage_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:reimage
/// Perform reimage operation on a single server.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_instances_reimage_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_instances_reimage_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_instances_reimage()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_instances_reimage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_instances_reimage_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_instances_reimage_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_instances_reimage`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsInstancesReimageArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:reimage
/// Perform reimage operation on a single server.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_instances_reimage_builder()` + `baremetalsolution_projects_locations_instances_reimage_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_instances_reimage_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_reimage(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsInstancesReimageArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        baremetalsolution_projects_locations_instances_reimage_builder(client, &args.name)?;
    baremetalsolution_projects_locations_instances_reimage_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:rename
/// RenameInstance sets a new name for an instance. Use with caution, previous names become immediately invalidated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_instances_rename_execute()` to send, or `baremetalsolution_projects_locations_instances_rename` for simplest API.

pub fn baremetalsolution_projects_locations_instances_rename_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/instances/{instancesId}:rename",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:rename
/// RenameInstance sets a new name for an instance. Use with caution, previous names become immediately invalidated.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_instances_rename_execute()` or `baremetalsolution_projects_locations_instances_rename`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_instances_rename_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_rename_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Instance>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Instance = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:rename
/// RenameInstance sets a new name for an instance. Use with caution, previous names become immediately invalidated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_instances_rename_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_instances_rename_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_instances_rename()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_instances_rename_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_instances_rename_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Instance>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_instances_rename_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_instances_rename`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsInstancesRenameArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:rename
/// RenameInstance sets a new name for an instance. Use with caution, previous names become immediately invalidated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_instances_rename_builder()` + `baremetalsolution_projects_locations_instances_rename_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_instances_rename_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_rename(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsInstancesRenameArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Instance>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        baremetalsolution_projects_locations_instances_rename_builder(client, &args.name)?;
    baremetalsolution_projects_locations_instances_rename_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:reset
/// Perform an ungraceful, hard reset on a server. Equivalent to shutting the power off and then turning it back on.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_instances_reset_execute()` to send, or `baremetalsolution_projects_locations_instances_reset` for simplest API.

pub fn baremetalsolution_projects_locations_instances_reset_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/instances/{instancesId}:reset",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:reset
/// Perform an ungraceful, hard reset on a server. Equivalent to shutting the power off and then turning it back on.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_instances_reset_execute()` or `baremetalsolution_projects_locations_instances_reset`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_instances_reset_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_reset_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:reset
/// Perform an ungraceful, hard reset on a server. Equivalent to shutting the power off and then turning it back on.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_instances_reset_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_instances_reset_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_instances_reset()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_instances_reset_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_instances_reset_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_instances_reset_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_instances_reset`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsInstancesResetArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:reset
/// Perform an ungraceful, hard reset on a server. Equivalent to shutting the power off and then turning it back on.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_instances_reset_builder()` + `baremetalsolution_projects_locations_instances_reset_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_instances_reset_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_reset(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsInstancesResetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_instances_reset_builder(client, &args.name)?;
    baremetalsolution_projects_locations_instances_reset_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:start
/// Starts a server that was shutdown.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_instances_start_execute()` to send, or `baremetalsolution_projects_locations_instances_start` for simplest API.

pub fn baremetalsolution_projects_locations_instances_start_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/instances/{instancesId}:start",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:start
/// Starts a server that was shutdown.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_instances_start_execute()` or `baremetalsolution_projects_locations_instances_start`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_instances_start_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_start_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:start
/// Starts a server that was shutdown.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_instances_start_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_instances_start_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_instances_start()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_instances_start_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_instances_start_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_instances_start_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_instances_start`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsInstancesStartArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:start
/// Starts a server that was shutdown.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_instances_start_builder()` + `baremetalsolution_projects_locations_instances_start_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_instances_start_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_start(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsInstancesStartArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_instances_start_builder(client, &args.name)?;
    baremetalsolution_projects_locations_instances_start_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:stop
/// Stop a running server.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_instances_stop_execute()` to send, or `baremetalsolution_projects_locations_instances_stop` for simplest API.

pub fn baremetalsolution_projects_locations_instances_stop_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/instances/{instancesId}:stop",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:stop
/// Stop a running server.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_instances_stop_execute()` or `baremetalsolution_projects_locations_instances_stop`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_instances_stop_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_stop_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:stop
/// Stop a running server.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_instances_stop_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_instances_stop_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_instances_stop()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_instances_stop_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_instances_stop_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_instances_stop_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_instances_stop`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsInstancesStopArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/instances/{instancesId}:stop
/// Stop a running server.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_instances_stop_builder()` + `baremetalsolution_projects_locations_instances_stop_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_instances_stop_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_stop(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsInstancesStopArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_instances_stop_builder(client, &args.name)?;
    baremetalsolution_projects_locations_instances_stop_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/networks/{networksId}
/// Get details of a single network.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_networks_get_execute()` to send, or `baremetalsolution_projects_locations_networks_get` for simplest API.

pub fn baremetalsolution_projects_locations_networks_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/networks/{networksId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/networks/{networksId}
/// Get details of a single network.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_networks_get_execute()` or `baremetalsolution_projects_locations_networks_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_networks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_networks_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Network>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Network = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/networks/{networksId}
/// Get details of a single network.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_networks_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_networks_get_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_networks_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_networks_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_networks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Network>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_networks_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_networks_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsNetworksGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/networks/{networksId}
/// Get details of a single network.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_networks_get_builder()` + `baremetalsolution_projects_locations_networks_get_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_networks_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_networks_get(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsNetworksGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Network>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_networks_get_builder(client, &args.name)?;
    baremetalsolution_projects_locations_networks_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/networks
/// List network in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_networks_list_execute()` to send, or `baremetalsolution_projects_locations_networks_list` for simplest API.

pub fn baremetalsolution_projects_locations_networks_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/networks",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/networks
/// List network in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_networks_list_execute()` or `baremetalsolution_projects_locations_networks_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_networks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_networks_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListNetworksResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListNetworksResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/networks
/// List network in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_networks_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_networks_list_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_networks_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_networks_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_networks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListNetworksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_networks_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_networks_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsNetworksListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/networks
/// List network in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_networks_list_builder()` + `baremetalsolution_projects_locations_networks_list_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_networks_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_networks_list(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsNetworksListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListNetworksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_networks_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    baremetalsolution_projects_locations_networks_list_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/networks:listNetworkUsage
/// List all Networks (and used IPs for each Network) in the vendor account associated with the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_networks_list_network_usage_execute()` to send, or `baremetalsolution_projects_locations_networks_list_network_usage` for simplest API.

pub fn baremetalsolution_projects_locations_networks_list_network_usage_builder<R>(
    client: &SimpleHttpClient<R>,
    location: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/networks:listNetworkUsage",
        location,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/networks:listNetworkUsage
/// List all Networks (and used IPs for each Network) in the vendor account associated with the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_networks_list_network_usage_execute()` or `baremetalsolution_projects_locations_networks_list_network_usage`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_networks_list_network_usage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_networks_list_network_usage_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListNetworkUsageResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListNetworkUsageResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/networks:listNetworkUsage
/// List all Networks (and used IPs for each Network) in the vendor account associated with the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_networks_list_network_usage_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_networks_list_network_usage_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_networks_list_network_usage()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_networks_list_network_usage_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_networks_list_network_usage_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListNetworkUsageResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_networks_list_network_usage_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_networks_list_network_usage`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsNetworksListNetworkUsageArgs {
    /// Path parameter: location
    pub location: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/networks:listNetworkUsage
/// List all Networks (and used IPs for each Network) in the vendor account associated with the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_networks_list_network_usage_builder()` + `baremetalsolution_projects_locations_networks_list_network_usage_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_networks_list_network_usage_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_networks_list_network_usage(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsNetworksListNetworkUsageArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListNetworkUsageResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_networks_list_network_usage_builder(
        client,
        &args.location,
    )?;
    baremetalsolution_projects_locations_networks_list_network_usage_execute(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/networks/{networksId}
/// Update details of a single network.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_networks_patch_execute()` to send, or `baremetalsolution_projects_locations_networks_patch` for simplest API.

pub fn baremetalsolution_projects_locations_networks_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/networks/{networksId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/networks/{networksId}
/// Update details of a single network.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_networks_patch_execute()` or `baremetalsolution_projects_locations_networks_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_networks_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_networks_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/networks/{networksId}
/// Update details of a single network.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_networks_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_networks_patch_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_networks_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_networks_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_networks_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_networks_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_networks_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsNetworksPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/networks/{networksId}
/// Update details of a single network.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_networks_patch_builder()` + `baremetalsolution_projects_locations_networks_patch_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_networks_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_networks_patch(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsNetworksPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_networks_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    baremetalsolution_projects_locations_networks_patch_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/networks/{networksId}:rename
/// RenameNetwork sets a new name for a network. Use with caution, previous names become immediately invalidated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_networks_rename_execute()` to send, or `baremetalsolution_projects_locations_networks_rename` for simplest API.

pub fn baremetalsolution_projects_locations_networks_rename_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/networks/{networksId}:rename",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/networks/{networksId}:rename
/// RenameNetwork sets a new name for a network. Use with caution, previous names become immediately invalidated.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_networks_rename_execute()` or `baremetalsolution_projects_locations_networks_rename`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_networks_rename_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_networks_rename_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Network>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Network = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/networks/{networksId}:rename
/// RenameNetwork sets a new name for a network. Use with caution, previous names become immediately invalidated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_networks_rename_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_networks_rename_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_networks_rename()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_networks_rename_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_networks_rename_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Network>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_networks_rename_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_networks_rename`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsNetworksRenameArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/networks/{networksId}:rename
/// RenameNetwork sets a new name for a network. Use with caution, previous names become immediately invalidated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_networks_rename_builder()` + `baremetalsolution_projects_locations_networks_rename_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_networks_rename_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_networks_rename(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsNetworksRenameArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Network>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_networks_rename_builder(client, &args.name)?;
    baremetalsolution_projects_locations_networks_rename_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/nfsShares
/// Create an NFS share.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_nfs_shares_create_execute()` to send, or `baremetalsolution_projects_locations_nfs_shares_create` for simplest API.

pub fn baremetalsolution_projects_locations_nfs_shares_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/nfsShares",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/nfsShares
/// Create an NFS share.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_nfs_shares_create_execute()` or `baremetalsolution_projects_locations_nfs_shares_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_nfs_shares_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_nfs_shares_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/nfsShares
/// Create an NFS share.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_nfs_shares_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_nfs_shares_create_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_nfs_shares_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_nfs_shares_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_nfs_shares_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_nfs_shares_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_nfs_shares_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsNfsSharesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/nfsShares
/// Create an NFS share.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_nfs_shares_create_builder()` + `baremetalsolution_projects_locations_nfs_shares_create_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_nfs_shares_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_nfs_shares_create(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsNfsSharesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        baremetalsolution_projects_locations_nfs_shares_create_builder(client, &args.parent)?;
    baremetalsolution_projects_locations_nfs_shares_create_execute(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/nfsShares/{nfsSharesId}
/// Delete an NFS share. The underlying volume is automatically deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_nfs_shares_delete_execute()` to send, or `baremetalsolution_projects_locations_nfs_shares_delete` for simplest API.

pub fn baremetalsolution_projects_locations_nfs_shares_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/nfsShares/{nfsSharesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/nfsShares/{nfsSharesId}
/// Delete an NFS share. The underlying volume is automatically deleted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_nfs_shares_delete_execute()` or `baremetalsolution_projects_locations_nfs_shares_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_nfs_shares_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_nfs_shares_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/nfsShares/{nfsSharesId}
/// Delete an NFS share. The underlying volume is automatically deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_nfs_shares_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_nfs_shares_delete_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_nfs_shares_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_nfs_shares_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_nfs_shares_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_nfs_shares_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_nfs_shares_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsNfsSharesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/nfsShares/{nfsSharesId}
/// Delete an NFS share. The underlying volume is automatically deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_nfs_shares_delete_builder()` + `baremetalsolution_projects_locations_nfs_shares_delete_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_nfs_shares_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_nfs_shares_delete(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsNfsSharesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        baremetalsolution_projects_locations_nfs_shares_delete_builder(client, &args.name)?;
    baremetalsolution_projects_locations_nfs_shares_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/nfsShares/{nfsSharesId}
/// Get details of a single NFS share.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_nfs_shares_get_execute()` to send, or `baremetalsolution_projects_locations_nfs_shares_get` for simplest API.

pub fn baremetalsolution_projects_locations_nfs_shares_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/nfsShares/{nfsSharesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/nfsShares/{nfsSharesId}
/// Get details of a single NFS share.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_nfs_shares_get_execute()` or `baremetalsolution_projects_locations_nfs_shares_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_nfs_shares_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_nfs_shares_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NfsShare>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NfsShare = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/nfsShares/{nfsSharesId}
/// Get details of a single NFS share.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_nfs_shares_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_nfs_shares_get_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_nfs_shares_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_nfs_shares_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_nfs_shares_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NfsShare>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_nfs_shares_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_nfs_shares_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsNfsSharesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/nfsShares/{nfsSharesId}
/// Get details of a single NFS share.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_nfs_shares_get_builder()` + `baremetalsolution_projects_locations_nfs_shares_get_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_nfs_shares_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_nfs_shares_get(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsNfsSharesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NfsShare>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_nfs_shares_get_builder(client, &args.name)?;
    baremetalsolution_projects_locations_nfs_shares_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/nfsShares
/// List NFS shares.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_nfs_shares_list_execute()` to send, or `baremetalsolution_projects_locations_nfs_shares_list` for simplest API.

pub fn baremetalsolution_projects_locations_nfs_shares_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/nfsShares",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/nfsShares
/// List NFS shares.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_nfs_shares_list_execute()` or `baremetalsolution_projects_locations_nfs_shares_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_nfs_shares_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_nfs_shares_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListNfsSharesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListNfsSharesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/nfsShares
/// List NFS shares.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_nfs_shares_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_nfs_shares_list_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_nfs_shares_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_nfs_shares_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_nfs_shares_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListNfsSharesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_nfs_shares_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_nfs_shares_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsNfsSharesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/nfsShares
/// List NFS shares.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_nfs_shares_list_builder()` + `baremetalsolution_projects_locations_nfs_shares_list_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_nfs_shares_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_nfs_shares_list(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsNfsSharesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListNfsSharesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_nfs_shares_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    baremetalsolution_projects_locations_nfs_shares_list_execute(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/nfsShares/{nfsSharesId}
/// Update details of a single NFS share.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_nfs_shares_patch_execute()` to send, or `baremetalsolution_projects_locations_nfs_shares_patch` for simplest API.

pub fn baremetalsolution_projects_locations_nfs_shares_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/nfsShares/{nfsSharesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/nfsShares/{nfsSharesId}
/// Update details of a single NFS share.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_nfs_shares_patch_execute()` or `baremetalsolution_projects_locations_nfs_shares_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_nfs_shares_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_nfs_shares_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/nfsShares/{nfsSharesId}
/// Update details of a single NFS share.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_nfs_shares_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_nfs_shares_patch_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_nfs_shares_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_nfs_shares_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_nfs_shares_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_nfs_shares_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_nfs_shares_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsNfsSharesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/nfsShares/{nfsSharesId}
/// Update details of a single NFS share.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_nfs_shares_patch_builder()` + `baremetalsolution_projects_locations_nfs_shares_patch_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_nfs_shares_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_nfs_shares_patch(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsNfsSharesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_nfs_shares_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    baremetalsolution_projects_locations_nfs_shares_patch_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/nfsShares/{nfsSharesId}:rename
/// RenameNfsShare sets a new name for an nfsshare. Use with caution, previous names become immediately invalidated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_nfs_shares_rename_execute()` to send, or `baremetalsolution_projects_locations_nfs_shares_rename` for simplest API.

pub fn baremetalsolution_projects_locations_nfs_shares_rename_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/nfsShares/{nfsSharesId}:rename",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/nfsShares/{nfsSharesId}:rename
/// RenameNfsShare sets a new name for an nfsshare. Use with caution, previous names become immediately invalidated.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_nfs_shares_rename_execute()` or `baremetalsolution_projects_locations_nfs_shares_rename`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_nfs_shares_rename_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_nfs_shares_rename_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<NfsShare>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NfsShare = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/nfsShares/{nfsSharesId}:rename
/// RenameNfsShare sets a new name for an nfsshare. Use with caution, previous names become immediately invalidated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_nfs_shares_rename_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_nfs_shares_rename_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_nfs_shares_rename()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_nfs_shares_rename_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_nfs_shares_rename_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NfsShare>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_nfs_shares_rename_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_nfs_shares_rename`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsNfsSharesRenameArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/nfsShares/{nfsSharesId}:rename
/// RenameNfsShare sets a new name for an nfsshare. Use with caution, previous names become immediately invalidated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_nfs_shares_rename_builder()` + `baremetalsolution_projects_locations_nfs_shares_rename_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_nfs_shares_rename_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_nfs_shares_rename(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsNfsSharesRenameArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NfsShare>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        baremetalsolution_projects_locations_nfs_shares_rename_builder(client, &args.name)?;
    baremetalsolution_projects_locations_nfs_shares_rename_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Get details about an operation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_operations_get_execute()` to send, or `baremetalsolution_projects_locations_operations_get` for simplest API.

pub fn baremetalsolution_projects_locations_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Get details about an operation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_operations_get_execute()` or `baremetalsolution_projects_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Get details about an operation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_operations_get_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Get details about an operation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_operations_get_builder()` + `baremetalsolution_projects_locations_operations_get_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_operations_get(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_operations_get_builder(client, &args.name)?;
    baremetalsolution_projects_locations_operations_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/osImages/{osImagesId}
/// Get details of a single OS image.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_os_images_get_execute()` to send, or `baremetalsolution_projects_locations_os_images_get` for simplest API.

pub fn baremetalsolution_projects_locations_os_images_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/osImages/{osImagesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/osImages/{osImagesId}
/// Get details of a single OS image.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_os_images_get_execute()` or `baremetalsolution_projects_locations_os_images_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_os_images_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_os_images_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OSImage>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OSImage = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/osImages/{osImagesId}
/// Get details of a single OS image.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_os_images_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_os_images_get_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_os_images_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_os_images_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_os_images_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OSImage>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_os_images_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_os_images_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsOsImagesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/osImages/{osImagesId}
/// Get details of a single OS image.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_os_images_get_builder()` + `baremetalsolution_projects_locations_os_images_get_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_os_images_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_os_images_get(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsOsImagesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OSImage>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_os_images_get_builder(client, &args.name)?;
    baremetalsolution_projects_locations_os_images_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/osImages
/// Retrieves the list of OS images which are currently approved.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_os_images_list_execute()` to send, or `baremetalsolution_projects_locations_os_images_list` for simplest API.

pub fn baremetalsolution_projects_locations_os_images_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/osImages",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/osImages
/// Retrieves the list of OS images which are currently approved.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_os_images_list_execute()` or `baremetalsolution_projects_locations_os_images_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_os_images_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_os_images_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOSImagesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOSImagesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/osImages
/// Retrieves the list of OS images which are currently approved.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_os_images_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_os_images_list_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_os_images_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_os_images_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_os_images_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOSImagesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_os_images_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_os_images_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsOsImagesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/osImages
/// Retrieves the list of OS images which are currently approved.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_os_images_list_builder()` + `baremetalsolution_projects_locations_os_images_list_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_os_images_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_os_images_list(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsOsImagesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOSImagesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_os_images_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    baremetalsolution_projects_locations_os_images_list_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/provisioningConfigs
/// Create new ProvisioningConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_provisioning_configs_create_execute()` to send, or `baremetalsolution_projects_locations_provisioning_configs_create` for simplest API.

pub fn baremetalsolution_projects_locations_provisioning_configs_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    email: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/provisioningConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = email.as_ref() {
        query_parts.push(format!("email={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/provisioningConfigs
/// Create new ProvisioningConfig.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_provisioning_configs_create_execute()` or `baremetalsolution_projects_locations_provisioning_configs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_provisioning_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_provisioning_configs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProvisioningConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProvisioningConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/provisioningConfigs
/// Create new ProvisioningConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_provisioning_configs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_provisioning_configs_create_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_provisioning_configs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_provisioning_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_provisioning_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProvisioningConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_provisioning_configs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_provisioning_configs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsProvisioningConfigsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: email
    pub email: Option<Option<String>>,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/provisioningConfigs
/// Create new ProvisioningConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_provisioning_configs_create_builder()` + `baremetalsolution_projects_locations_provisioning_configs_create_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_provisioning_configs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_provisioning_configs_create(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsProvisioningConfigsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProvisioningConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_provisioning_configs_create_builder(
        client,
        &args.parent,
        &args.email,
    )?;
    baremetalsolution_projects_locations_provisioning_configs_create_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/provisioningConfigs/{provisioningConfigsId}
/// Get ProvisioningConfig by name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_provisioning_configs_get_execute()` to send, or `baremetalsolution_projects_locations_provisioning_configs_get` for simplest API.

pub fn baremetalsolution_projects_locations_provisioning_configs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/provisioningConfigs/{provisioningConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/provisioningConfigs/{provisioningConfigsId}
/// Get ProvisioningConfig by name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_provisioning_configs_get_execute()` or `baremetalsolution_projects_locations_provisioning_configs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_provisioning_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_provisioning_configs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProvisioningConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProvisioningConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/provisioningConfigs/{provisioningConfigsId}
/// Get ProvisioningConfig by name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_provisioning_configs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_provisioning_configs_get_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_provisioning_configs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_provisioning_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_provisioning_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProvisioningConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_provisioning_configs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_provisioning_configs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsProvisioningConfigsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/provisioningConfigs/{provisioningConfigsId}
/// Get ProvisioningConfig by name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_provisioning_configs_get_builder()` + `baremetalsolution_projects_locations_provisioning_configs_get_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_provisioning_configs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_provisioning_configs_get(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsProvisioningConfigsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProvisioningConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        baremetalsolution_projects_locations_provisioning_configs_get_builder(client, &args.name)?;
    baremetalsolution_projects_locations_provisioning_configs_get_execute(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/provisioningConfigs/{provisioningConfigsId}
/// Update existing ProvisioningConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_provisioning_configs_patch_execute()` to send, or `baremetalsolution_projects_locations_provisioning_configs_patch` for simplest API.

pub fn baremetalsolution_projects_locations_provisioning_configs_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    email: &Option<Option<String>>,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/provisioningConfigs/{provisioningConfigsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = email.as_ref() {
        query_parts.push(format!("email={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/provisioningConfigs/{provisioningConfigsId}
/// Update existing ProvisioningConfig.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_provisioning_configs_patch_execute()` or `baremetalsolution_projects_locations_provisioning_configs_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_provisioning_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_provisioning_configs_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ProvisioningConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProvisioningConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/provisioningConfigs/{provisioningConfigsId}
/// Update existing ProvisioningConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_provisioning_configs_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_provisioning_configs_patch_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_provisioning_configs_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_provisioning_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_provisioning_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProvisioningConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_provisioning_configs_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_provisioning_configs_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsProvisioningConfigsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: email
    pub email: Option<Option<String>>,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/provisioningConfigs/{provisioningConfigsId}
/// Update existing ProvisioningConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_provisioning_configs_patch_builder()` + `baremetalsolution_projects_locations_provisioning_configs_patch_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_provisioning_configs_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_provisioning_configs_patch(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsProvisioningConfigsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProvisioningConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_provisioning_configs_patch_builder(
        client,
        &args.name,
        &args.email,
        &args.updateMask,
    )?;
    baremetalsolution_projects_locations_provisioning_configs_patch_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/provisioningConfigs:submit
/// Submit a provisioning configuration for a given project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_provisioning_configs_submit_execute()` to send, or `baremetalsolution_projects_locations_provisioning_configs_submit` for simplest API.

pub fn baremetalsolution_projects_locations_provisioning_configs_submit_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/provisioningConfigs:submit",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/provisioningConfigs:submit
/// Submit a provisioning configuration for a given project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_provisioning_configs_submit_execute()` or `baremetalsolution_projects_locations_provisioning_configs_submit`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_provisioning_configs_submit_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_provisioning_configs_submit_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SubmitProvisioningConfigResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SubmitProvisioningConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/provisioningConfigs:submit
/// Submit a provisioning configuration for a given project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_provisioning_configs_submit_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_provisioning_configs_submit_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_provisioning_configs_submit()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_provisioning_configs_submit_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_provisioning_configs_submit_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SubmitProvisioningConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_provisioning_configs_submit_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_provisioning_configs_submit`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsProvisioningConfigsSubmitArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/provisioningConfigs:submit
/// Submit a provisioning configuration for a given project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_provisioning_configs_submit_builder()` + `baremetalsolution_projects_locations_provisioning_configs_submit_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_provisioning_configs_submit_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_provisioning_configs_submit(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsProvisioningConfigsSubmitArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SubmitProvisioningConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_provisioning_configs_submit_builder(
        client,
        &args.parent,
    )?;
    baremetalsolution_projects_locations_provisioning_configs_submit_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/provisioningQuotas
/// List the budget details to provision resources on a given project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_provisioning_quotas_list_execute()` to send, or `baremetalsolution_projects_locations_provisioning_quotas_list` for simplest API.

pub fn baremetalsolution_projects_locations_provisioning_quotas_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/provisioningQuotas",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/provisioningQuotas
/// List the budget details to provision resources on a given project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_provisioning_quotas_list_execute()` or `baremetalsolution_projects_locations_provisioning_quotas_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_provisioning_quotas_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_provisioning_quotas_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListProvisioningQuotasResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListProvisioningQuotasResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/provisioningQuotas
/// List the budget details to provision resources on a given project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_provisioning_quotas_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_provisioning_quotas_list_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_provisioning_quotas_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_provisioning_quotas_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_provisioning_quotas_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListProvisioningQuotasResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_provisioning_quotas_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_provisioning_quotas_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsProvisioningQuotasListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/provisioningQuotas
/// List the budget details to provision resources on a given project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_provisioning_quotas_list_builder()` + `baremetalsolution_projects_locations_provisioning_quotas_list_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_provisioning_quotas_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_provisioning_quotas_list(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsProvisioningQuotasListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListProvisioningQuotasResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_provisioning_quotas_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    baremetalsolution_projects_locations_provisioning_quotas_list_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/sshKeys
/// Register a public SSH key in the specified project for use with the interactive serial console feature.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_ssh_keys_create_execute()` to send, or `baremetalsolution_projects_locations_ssh_keys_create` for simplest API.

pub fn baremetalsolution_projects_locations_ssh_keys_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    sshKeyId: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/sshKeys",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = sshKeyId.as_ref() {
        query_parts.push(format!("sshKeyId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/sshKeys
/// Register a public SSH key in the specified project for use with the interactive serial console feature.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_ssh_keys_create_execute()` or `baremetalsolution_projects_locations_ssh_keys_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_ssh_keys_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_ssh_keys_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SSHKey>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SSHKey = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/sshKeys
/// Register a public SSH key in the specified project for use with the interactive serial console feature.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_ssh_keys_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_ssh_keys_create_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_ssh_keys_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_ssh_keys_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_ssh_keys_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SSHKey>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_ssh_keys_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_ssh_keys_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsSshKeysCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: sshKeyId
    pub sshKeyId: Option<Option<String>>,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/sshKeys
/// Register a public SSH key in the specified project for use with the interactive serial console feature.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_ssh_keys_create_builder()` + `baremetalsolution_projects_locations_ssh_keys_create_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_ssh_keys_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_ssh_keys_create(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsSshKeysCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SSHKey>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_ssh_keys_create_builder(
        client,
        &args.parent,
        &args.sshKeyId,
    )?;
    baremetalsolution_projects_locations_ssh_keys_create_execute(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/sshKeys/{sshKeysId}
/// Deletes a public SSH key registered in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_ssh_keys_delete_execute()` to send, or `baremetalsolution_projects_locations_ssh_keys_delete` for simplest API.

pub fn baremetalsolution_projects_locations_ssh_keys_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/sshKeys/{sshKeysId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/sshKeys/{sshKeysId}
/// Deletes a public SSH key registered in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_ssh_keys_delete_execute()` or `baremetalsolution_projects_locations_ssh_keys_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_ssh_keys_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_ssh_keys_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/sshKeys/{sshKeysId}
/// Deletes a public SSH key registered in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_ssh_keys_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_ssh_keys_delete_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_ssh_keys_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_ssh_keys_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_ssh_keys_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_ssh_keys_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_ssh_keys_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsSshKeysDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/sshKeys/{sshKeysId}
/// Deletes a public SSH key registered in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_ssh_keys_delete_builder()` + `baremetalsolution_projects_locations_ssh_keys_delete_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_ssh_keys_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_ssh_keys_delete(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsSshKeysDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_ssh_keys_delete_builder(client, &args.name)?;
    baremetalsolution_projects_locations_ssh_keys_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/sshKeys
/// Lists the public SSH keys registered for the specified project. These SSH keys are used only for the interactive serial console feature.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_ssh_keys_list_execute()` to send, or `baremetalsolution_projects_locations_ssh_keys_list` for simplest API.

pub fn baremetalsolution_projects_locations_ssh_keys_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/sshKeys",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/sshKeys
/// Lists the public SSH keys registered for the specified project. These SSH keys are used only for the interactive serial console feature.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_ssh_keys_list_execute()` or `baremetalsolution_projects_locations_ssh_keys_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_ssh_keys_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_ssh_keys_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSSHKeysResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSSHKeysResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/sshKeys
/// Lists the public SSH keys registered for the specified project. These SSH keys are used only for the interactive serial console feature.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_ssh_keys_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_ssh_keys_list_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_ssh_keys_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_ssh_keys_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_ssh_keys_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSSHKeysResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_ssh_keys_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_ssh_keys_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsSshKeysListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/sshKeys
/// Lists the public SSH keys registered for the specified project. These SSH keys are used only for the interactive serial console feature.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_ssh_keys_list_builder()` + `baremetalsolution_projects_locations_ssh_keys_list_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_ssh_keys_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_ssh_keys_list(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsSshKeysListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSSHKeysResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_ssh_keys_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    baremetalsolution_projects_locations_ssh_keys_list_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}:evict
/// Skips volume's cooloff and deletes it now. Volume must be in cooloff state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_volumes_evict_execute()` to send, or `baremetalsolution_projects_locations_volumes_evict` for simplest API.

pub fn baremetalsolution_projects_locations_volumes_evict_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/volumes/{volumesId}:evict",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}:evict
/// Skips volume's cooloff and deletes it now. Volume must be in cooloff state.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_volumes_evict_execute()` or `baremetalsolution_projects_locations_volumes_evict`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_volumes_evict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_evict_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}:evict
/// Skips volume's cooloff and deletes it now. Volume must be in cooloff state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_volumes_evict_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_volumes_evict_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_volumes_evict()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_volumes_evict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_volumes_evict_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_volumes_evict_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_volumes_evict`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsVolumesEvictArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}:evict
/// Skips volume's cooloff and deletes it now. Volume must be in cooloff state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_volumes_evict_builder()` + `baremetalsolution_projects_locations_volumes_evict_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_volumes_evict_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_evict(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsVolumesEvictArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_volumes_evict_builder(client, &args.name)?;
    baremetalsolution_projects_locations_volumes_evict_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}
/// Get details of a single storage volume.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_volumes_get_execute()` to send, or `baremetalsolution_projects_locations_volumes_get` for simplest API.

pub fn baremetalsolution_projects_locations_volumes_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/volumes/{volumesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}
/// Get details of a single storage volume.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_volumes_get_execute()` or `baremetalsolution_projects_locations_volumes_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_volumes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Volume>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Volume = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}
/// Get details of a single storage volume.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_volumes_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_volumes_get_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_volumes_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_volumes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_volumes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volume>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_volumes_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_volumes_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsVolumesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}
/// Get details of a single storage volume.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_volumes_get_builder()` + `baremetalsolution_projects_locations_volumes_get_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_volumes_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_get(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsVolumesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volume>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_volumes_get_builder(client, &args.name)?;
    baremetalsolution_projects_locations_volumes_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/volumes
/// List storage volumes in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_volumes_list_execute()` to send, or `baremetalsolution_projects_locations_volumes_list` for simplest API.

pub fn baremetalsolution_projects_locations_volumes_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/volumes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/volumes
/// List storage volumes in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_volumes_list_execute()` or `baremetalsolution_projects_locations_volumes_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_volumes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListVolumesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListVolumesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/volumes
/// List storage volumes in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_volumes_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_volumes_list_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_volumes_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_volumes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_volumes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListVolumesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_volumes_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_volumes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsVolumesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/volumes
/// List storage volumes in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_volumes_list_builder()` + `baremetalsolution_projects_locations_volumes_list_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_volumes_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_list(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsVolumesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListVolumesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_volumes_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    baremetalsolution_projects_locations_volumes_list_execute(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}
/// Update details of a single storage volume.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_volumes_patch_execute()` to send, or `baremetalsolution_projects_locations_volumes_patch` for simplest API.

pub fn baremetalsolution_projects_locations_volumes_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/volumes/{volumesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}
/// Update details of a single storage volume.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_volumes_patch_execute()` or `baremetalsolution_projects_locations_volumes_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_volumes_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}
/// Update details of a single storage volume.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_volumes_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_volumes_patch_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_volumes_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_volumes_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_volumes_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_volumes_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_volumes_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsVolumesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<Option<String>>,
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}
/// Update details of a single storage volume.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_volumes_patch_builder()` + `baremetalsolution_projects_locations_volumes_patch_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_volumes_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_patch(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsVolumesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_volumes_patch_builder(
        client,
        &args.name,
        &args.updateMask,
    )?;
    baremetalsolution_projects_locations_volumes_patch_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}:rename
/// RenameVolume sets a new name for a volume. Use with caution, previous names become immediately invalidated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_volumes_rename_execute()` to send, or `baremetalsolution_projects_locations_volumes_rename` for simplest API.

pub fn baremetalsolution_projects_locations_volumes_rename_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/volumes/{volumesId}:rename",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}:rename
/// RenameVolume sets a new name for a volume. Use with caution, previous names become immediately invalidated.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_volumes_rename_execute()` or `baremetalsolution_projects_locations_volumes_rename`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_volumes_rename_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_rename_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Volume>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Volume = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}:rename
/// RenameVolume sets a new name for a volume. Use with caution, previous names become immediately invalidated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_volumes_rename_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_volumes_rename_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_volumes_rename()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_volumes_rename_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_volumes_rename_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volume>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_volumes_rename_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_volumes_rename`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsVolumesRenameArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}:rename
/// RenameVolume sets a new name for a volume. Use with caution, previous names become immediately invalidated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_volumes_rename_builder()` + `baremetalsolution_projects_locations_volumes_rename_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_volumes_rename_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_rename(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsVolumesRenameArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volume>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_volumes_rename_builder(client, &args.name)?;
    baremetalsolution_projects_locations_volumes_rename_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}:resize
/// Emergency Volume resize.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_volumes_resize_execute()` to send, or `baremetalsolution_projects_locations_volumes_resize` for simplest API.

pub fn baremetalsolution_projects_locations_volumes_resize_builder<R>(
    client: &SimpleHttpClient<R>,
    volume: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/volumes/{volumesId}:resize",
        volume,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}:resize
/// Emergency Volume resize.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_volumes_resize_execute()` or `baremetalsolution_projects_locations_volumes_resize`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_volumes_resize_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_resize_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}:resize
/// Emergency Volume resize.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_volumes_resize_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_volumes_resize_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_volumes_resize()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_volumes_resize_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_volumes_resize_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_volumes_resize_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_volumes_resize`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsVolumesResizeArgs {
    /// Path parameter: volume
    pub volume: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}:resize
/// Emergency Volume resize.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_volumes_resize_builder()` + `baremetalsolution_projects_locations_volumes_resize_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_volumes_resize_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_resize(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsVolumesResizeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        baremetalsolution_projects_locations_volumes_resize_builder(client, &args.volume)?;
    baremetalsolution_projects_locations_volumes_resize_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}/luns/{lunsId}:evict
/// Skips lun's cooloff and deletes it now. Lun must be in cooloff state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_volumes_luns_evict_execute()` to send, or `baremetalsolution_projects_locations_volumes_luns_evict` for simplest API.

pub fn baremetalsolution_projects_locations_volumes_luns_evict_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/volumes/{volumesId}/luns/{lunsId}:evict",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}/luns/{lunsId}:evict
/// Skips lun's cooloff and deletes it now. Lun must be in cooloff state.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_volumes_luns_evict_execute()` or `baremetalsolution_projects_locations_volumes_luns_evict`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_volumes_luns_evict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_luns_evict_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}/luns/{lunsId}:evict
/// Skips lun's cooloff and deletes it now. Lun must be in cooloff state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_volumes_luns_evict_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_volumes_luns_evict_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_volumes_luns_evict()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_volumes_luns_evict_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_volumes_luns_evict_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_volumes_luns_evict_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_volumes_luns_evict`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsVolumesLunsEvictArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}/luns/{lunsId}:evict
/// Skips lun's cooloff and deletes it now. Lun must be in cooloff state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_volumes_luns_evict_builder()` + `baremetalsolution_projects_locations_volumes_luns_evict_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_volumes_luns_evict_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_luns_evict(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsVolumesLunsEvictArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        baremetalsolution_projects_locations_volumes_luns_evict_builder(client, &args.name)?;
    baremetalsolution_projects_locations_volumes_luns_evict_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}/luns/{lunsId}
/// Get details of a single storage logical unit number(LUN).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_volumes_luns_get_execute()` to send, or `baremetalsolution_projects_locations_volumes_luns_get` for simplest API.

pub fn baremetalsolution_projects_locations_volumes_luns_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/volumes/{volumesId}/luns/{lunsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}/luns/{lunsId}
/// Get details of a single storage logical unit number(LUN).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_volumes_luns_get_execute()` or `baremetalsolution_projects_locations_volumes_luns_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_volumes_luns_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_luns_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Lun>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Lun = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}/luns/{lunsId}
/// Get details of a single storage logical unit number(LUN).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_volumes_luns_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_volumes_luns_get_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_volumes_luns_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_volumes_luns_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_volumes_luns_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Lun>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_volumes_luns_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_volumes_luns_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsVolumesLunsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}/luns/{lunsId}
/// Get details of a single storage logical unit number(LUN).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_volumes_luns_get_builder()` + `baremetalsolution_projects_locations_volumes_luns_get_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_volumes_luns_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_luns_get(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsVolumesLunsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Lun>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        baremetalsolution_projects_locations_volumes_luns_get_builder(client, &args.name)?;
    baremetalsolution_projects_locations_volumes_luns_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}/luns
/// List storage volume luns for given storage volume.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_volumes_luns_list_execute()` to send, or `baremetalsolution_projects_locations_volumes_luns_list` for simplest API.

pub fn baremetalsolution_projects_locations_volumes_luns_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/volumes/{volumesId}/luns",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}/luns
/// List storage volume luns for given storage volume.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_volumes_luns_list_execute()` or `baremetalsolution_projects_locations_volumes_luns_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_volumes_luns_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_luns_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLunsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLunsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}/luns
/// List storage volume luns for given storage volume.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_volumes_luns_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_volumes_luns_list_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_volumes_luns_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_volumes_luns_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_volumes_luns_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLunsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_volumes_luns_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_volumes_luns_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsVolumesLunsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}/luns
/// List storage volume luns for given storage volume.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_volumes_luns_list_builder()` + `baremetalsolution_projects_locations_volumes_luns_list_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_volumes_luns_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_luns_list(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsVolumesLunsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLunsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_volumes_luns_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    baremetalsolution_projects_locations_volumes_luns_list_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}/snapshots
/// Takes a snapshot of a boot volume. Returns INVALID_ARGUMENT if called for a non-boot volume.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_volumes_snapshots_create_execute()` to send, or `baremetalsolution_projects_locations_volumes_snapshots_create` for simplest API.

pub fn baremetalsolution_projects_locations_volumes_snapshots_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/volumes/{volumesId}/snapshots",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}/snapshots
/// Takes a snapshot of a boot volume. Returns INVALID_ARGUMENT if called for a non-boot volume.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_volumes_snapshots_create_execute()` or `baremetalsolution_projects_locations_volumes_snapshots_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_volumes_snapshots_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_snapshots_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VolumeSnapshot>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VolumeSnapshot = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}/snapshots
/// Takes a snapshot of a boot volume. Returns INVALID_ARGUMENT if called for a non-boot volume.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_volumes_snapshots_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_volumes_snapshots_create_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_volumes_snapshots_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_volumes_snapshots_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_volumes_snapshots_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VolumeSnapshot>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_volumes_snapshots_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_volumes_snapshots_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsVolumesSnapshotsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}/snapshots
/// Takes a snapshot of a boot volume. Returns INVALID_ARGUMENT if called for a non-boot volume.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_volumes_snapshots_create_builder()` + `baremetalsolution_projects_locations_volumes_snapshots_create_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_volumes_snapshots_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_snapshots_create(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsVolumesSnapshotsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VolumeSnapshot>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_volumes_snapshots_create_builder(
        client,
        &args.parent,
    )?;
    baremetalsolution_projects_locations_volumes_snapshots_create_execute(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}/snapshots/{snapshotsId}
/// Deletes a volume snapshot. Returns INVALID_ARGUMENT if called for a non-boot volume.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_volumes_snapshots_delete_execute()` to send, or `baremetalsolution_projects_locations_volumes_snapshots_delete` for simplest API.

pub fn baremetalsolution_projects_locations_volumes_snapshots_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/volumes/{volumesId}/snapshots/{snapshotsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}/snapshots/{snapshotsId}
/// Deletes a volume snapshot. Returns INVALID_ARGUMENT if called for a non-boot volume.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_volumes_snapshots_delete_execute()` or `baremetalsolution_projects_locations_volumes_snapshots_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_volumes_snapshots_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_snapshots_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}/snapshots/{snapshotsId}
/// Deletes a volume snapshot. Returns INVALID_ARGUMENT if called for a non-boot volume.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_volumes_snapshots_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_volumes_snapshots_delete_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_volumes_snapshots_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_volumes_snapshots_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_volumes_snapshots_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_volumes_snapshots_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_volumes_snapshots_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsVolumesSnapshotsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}/snapshots/{snapshotsId}
/// Deletes a volume snapshot. Returns INVALID_ARGUMENT if called for a non-boot volume.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_volumes_snapshots_delete_builder()` + `baremetalsolution_projects_locations_volumes_snapshots_delete_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_volumes_snapshots_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_snapshots_delete(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsVolumesSnapshotsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        baremetalsolution_projects_locations_volumes_snapshots_delete_builder(client, &args.name)?;
    baremetalsolution_projects_locations_volumes_snapshots_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}/snapshots/{snapshotsId}
/// Returns the specified snapshot resource. Returns INVALID_ARGUMENT if called for a non-boot volume.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_volumes_snapshots_get_execute()` to send, or `baremetalsolution_projects_locations_volumes_snapshots_get` for simplest API.

pub fn baremetalsolution_projects_locations_volumes_snapshots_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/volumes/{volumesId}/snapshots/{snapshotsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}/snapshots/{snapshotsId}
/// Returns the specified snapshot resource. Returns INVALID_ARGUMENT if called for a non-boot volume.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_volumes_snapshots_get_execute()` or `baremetalsolution_projects_locations_volumes_snapshots_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_volumes_snapshots_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_snapshots_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VolumeSnapshot>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VolumeSnapshot = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}/snapshots/{snapshotsId}
/// Returns the specified snapshot resource. Returns INVALID_ARGUMENT if called for a non-boot volume.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_volumes_snapshots_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_volumes_snapshots_get_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_volumes_snapshots_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_volumes_snapshots_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_volumes_snapshots_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VolumeSnapshot>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_volumes_snapshots_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_volumes_snapshots_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsVolumesSnapshotsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}/snapshots/{snapshotsId}
/// Returns the specified snapshot resource. Returns INVALID_ARGUMENT if called for a non-boot volume.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_volumes_snapshots_get_builder()` + `baremetalsolution_projects_locations_volumes_snapshots_get_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_volumes_snapshots_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_snapshots_get(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsVolumesSnapshotsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VolumeSnapshot>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        baremetalsolution_projects_locations_volumes_snapshots_get_builder(client, &args.name)?;
    baremetalsolution_projects_locations_volumes_snapshots_get_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}/snapshots
/// Retrieves the list of snapshots for the specified volume. Returns a response with an empty list of snapshots if called for a non-boot volume.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_volumes_snapshots_list_execute()` to send, or `baremetalsolution_projects_locations_volumes_snapshots_list` for simplest API.

pub fn baremetalsolution_projects_locations_volumes_snapshots_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/volumes/{volumesId}/snapshots",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}/snapshots
/// Retrieves the list of snapshots for the specified volume. Returns a response with an empty list of snapshots if called for a non-boot volume.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_volumes_snapshots_list_execute()` or `baremetalsolution_projects_locations_volumes_snapshots_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_volumes_snapshots_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_snapshots_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListVolumeSnapshotsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListVolumeSnapshotsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}/snapshots
/// Retrieves the list of snapshots for the specified volume. Returns a response with an empty list of snapshots if called for a non-boot volume.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_volumes_snapshots_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_volumes_snapshots_list_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_volumes_snapshots_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_volumes_snapshots_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_volumes_snapshots_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListVolumeSnapshotsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_volumes_snapshots_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_volumes_snapshots_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsVolumesSnapshotsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}/snapshots
/// Retrieves the list of snapshots for the specified volume. Returns a response with an empty list of snapshots if called for a non-boot volume.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_volumes_snapshots_list_builder()` + `baremetalsolution_projects_locations_volumes_snapshots_list_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_volumes_snapshots_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_snapshots_list(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsVolumesSnapshotsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListVolumeSnapshotsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_volumes_snapshots_list_builder(
        client,
        &args.parent,
        &args.pageSize,
        &args.pageToken,
    )?;
    baremetalsolution_projects_locations_volumes_snapshots_list_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}/snapshots/{snapshotsId}:restoreVolumeSnapshot
/// Uses the specified snapshot to restore its parent volume. Returns INVALID_ARGUMENT if called for a non-boot volume.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_volumes_snapshots_restore_volume_snapshot_execute()` to send, or `baremetalsolution_projects_locations_volumes_snapshots_restore_volume_snapshot` for simplest API.

pub fn baremetalsolution_projects_locations_volumes_snapshots_restore_volume_snapshot_builder<R>(
    client: &SimpleHttpClient<R>,
    volumeSnapshot: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://baremetalsolution.googleapis.com/v2/projects/{}/locations/{locationsId}/volumes/{volumesId}/snapshots/{snapshotsId}:restoreVolumeSnapshot",
        volumeSnapshot,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}/snapshots/{snapshotsId}:restoreVolumeSnapshot
/// Uses the specified snapshot to restore its parent volume. Returns INVALID_ARGUMENT if called for a non-boot volume.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `baremetalsolution_projects_locations_volumes_snapshots_restore_volume_snapshot_execute()` or `baremetalsolution_projects_locations_volumes_snapshots_restore_volume_snapshot`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_volumes_snapshots_restore_volume_snapshot_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_snapshots_restore_volume_snapshot_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}/snapshots/{snapshotsId}:restoreVolumeSnapshot
/// Uses the specified snapshot to restore its parent volume. Returns INVALID_ARGUMENT if called for a non-boot volume.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `baremetalsolution_projects_locations_volumes_snapshots_restore_volume_snapshot_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `baremetalsolution_projects_locations_volumes_snapshots_restore_volume_snapshot_task()`.
/// For the simplest API, use `baremetalsolution_projects_locations_volumes_snapshots_restore_volume_snapshot()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `baremetalsolution_projects_locations_volumes_snapshots_restore_volume_snapshot_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn baremetalsolution_projects_locations_volumes_snapshots_restore_volume_snapshot_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = baremetalsolution_projects_locations_volumes_snapshots_restore_volume_snapshot_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`baremetalsolution_projects_locations_volumes_snapshots_restore_volume_snapshot`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BaremetalsolutionProjectsLocationsVolumesSnapshotsRestoreVolumeSnapshotArgs {
    /// Path parameter: volumeSnapshot
    pub volumeSnapshot: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/volumes/{volumesId}/snapshots/{snapshotsId}:restoreVolumeSnapshot
/// Uses the specified snapshot to restore its parent volume. Returns INVALID_ARGUMENT if called for a non-boot volume.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_volumes_snapshots_restore_volume_snapshot_builder()` + `baremetalsolution_projects_locations_volumes_snapshots_restore_volume_snapshot_execute()`.
/// For task-level control, use `baremetalsolution_projects_locations_volumes_snapshots_restore_volume_snapshot_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_snapshots_restore_volume_snapshot(
    client: &SimpleHttpClient,
    args: &BaremetalsolutionProjectsLocationsVolumesSnapshotsRestoreVolumeSnapshotArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        baremetalsolution_projects_locations_volumes_snapshots_restore_volume_snapshot_builder(
            client,
            &args.volumeSnapshot,
        )?;
    baremetalsolution_projects_locations_volumes_snapshots_restore_volume_snapshot_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for Location
// =============================================================================

/// ResourceIdentifier implementation for Location with BaremetalsolutionProjectsLocationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsGetArgs> for Location {
    fn generate_resource_id(&self, input: &BaremetalsolutionProjectsLocationsGetArgs) -> String {
        format!("gcp::baremetalsolution::Location/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::Location"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLocationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLocationsResponse with BaremetalsolutionProjectsLocationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsListArgs> for ListLocationsResponse {
    fn generate_resource_id(&self, input: &BaremetalsolutionProjectsLocationsListArgs) -> String {
        format!(
            "gcp::baremetalsolution::ListLocationsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::ListLocationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with BaremetalsolutionProjectsLocationsInstancesDetachLunArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsInstancesDetachLunArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsInstancesDetachLunArgs,
    ) -> String {
        format!("gcp::baremetalsolution::Operation/{}", input.instance)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with BaremetalsolutionProjectsLocationsInstancesDisableHyperthreadingArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsInstancesDisableHyperthreadingArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsInstancesDisableHyperthreadingArgs,
    ) -> String {
        format!("gcp::baremetalsolution::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with BaremetalsolutionProjectsLocationsInstancesDisableInteractiveSerialConsoleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        BaremetalsolutionProjectsLocationsInstancesDisableInteractiveSerialConsoleArgs,
    > for Operation
{
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsInstancesDisableInteractiveSerialConsoleArgs,
    ) -> String {
        format!("gcp::baremetalsolution::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with BaremetalsolutionProjectsLocationsInstancesEnableHyperthreadingArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsInstancesEnableHyperthreadingArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsInstancesEnableHyperthreadingArgs,
    ) -> String {
        format!("gcp::baremetalsolution::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with BaremetalsolutionProjectsLocationsInstancesEnableInteractiveSerialConsoleArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        BaremetalsolutionProjectsLocationsInstancesEnableInteractiveSerialConsoleArgs,
    > for Operation
{
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsInstancesEnableInteractiveSerialConsoleArgs,
    ) -> String {
        format!("gcp::baremetalsolution::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Instance
// =============================================================================

/// ResourceIdentifier implementation for Instance with BaremetalsolutionProjectsLocationsInstancesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsInstancesGetArgs> for Instance {
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsInstancesGetArgs,
    ) -> String {
        format!("gcp::baremetalsolution::Instance/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::Instance"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListInstancesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListInstancesResponse with BaremetalsolutionProjectsLocationsInstancesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsInstancesListArgs>
    for ListInstancesResponse
{
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsInstancesListArgs,
    ) -> String {
        format!(
            "gcp::baremetalsolution::ListInstancesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::ListInstancesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for LoadInstanceAuthInfoResponse
// =============================================================================

/// ResourceIdentifier implementation for LoadInstanceAuthInfoResponse with BaremetalsolutionProjectsLocationsInstancesLoadAuthInfoArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsInstancesLoadAuthInfoArgs>
    for LoadInstanceAuthInfoResponse
{
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsInstancesLoadAuthInfoArgs,
    ) -> String {
        format!(
            "gcp::baremetalsolution::LoadInstanceAuthInfoResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::LoadInstanceAuthInfoResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with BaremetalsolutionProjectsLocationsInstancesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsInstancesPatchArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsInstancesPatchArgs,
    ) -> String {
        format!("gcp::baremetalsolution::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with BaremetalsolutionProjectsLocationsInstancesReimageArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsInstancesReimageArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsInstancesReimageArgs,
    ) -> String {
        format!("gcp::baremetalsolution::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Instance
// =============================================================================

/// ResourceIdentifier implementation for Instance with BaremetalsolutionProjectsLocationsInstancesRenameArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsInstancesRenameArgs> for Instance {
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsInstancesRenameArgs,
    ) -> String {
        format!("gcp::baremetalsolution::Instance/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::Instance"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with BaremetalsolutionProjectsLocationsInstancesResetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsInstancesResetArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsInstancesResetArgs,
    ) -> String {
        format!("gcp::baremetalsolution::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with BaremetalsolutionProjectsLocationsInstancesStartArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsInstancesStartArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsInstancesStartArgs,
    ) -> String {
        format!("gcp::baremetalsolution::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with BaremetalsolutionProjectsLocationsInstancesStopArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsInstancesStopArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsInstancesStopArgs,
    ) -> String {
        format!("gcp::baremetalsolution::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Network
// =============================================================================

/// ResourceIdentifier implementation for Network with BaremetalsolutionProjectsLocationsNetworksGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsNetworksGetArgs> for Network {
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsNetworksGetArgs,
    ) -> String {
        format!("gcp::baremetalsolution::Network/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::Network"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListNetworksResponse
// =============================================================================

/// ResourceIdentifier implementation for ListNetworksResponse with BaremetalsolutionProjectsLocationsNetworksListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsNetworksListArgs>
    for ListNetworksResponse
{
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsNetworksListArgs,
    ) -> String {
        format!(
            "gcp::baremetalsolution::ListNetworksResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::ListNetworksResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListNetworkUsageResponse
// =============================================================================

/// ResourceIdentifier implementation for ListNetworkUsageResponse with BaremetalsolutionProjectsLocationsNetworksListNetworkUsageArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsNetworksListNetworkUsageArgs>
    for ListNetworkUsageResponse
{
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsNetworksListNetworkUsageArgs,
    ) -> String {
        format!(
            "gcp::baremetalsolution::ListNetworkUsageResponse/{}",
            input.location
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::ListNetworkUsageResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with BaremetalsolutionProjectsLocationsNetworksPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsNetworksPatchArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsNetworksPatchArgs,
    ) -> String {
        format!("gcp::baremetalsolution::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Network
// =============================================================================

/// ResourceIdentifier implementation for Network with BaremetalsolutionProjectsLocationsNetworksRenameArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsNetworksRenameArgs> for Network {
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsNetworksRenameArgs,
    ) -> String {
        format!("gcp::baremetalsolution::Network/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::Network"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with BaremetalsolutionProjectsLocationsNfsSharesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsNfsSharesCreateArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsNfsSharesCreateArgs,
    ) -> String {
        format!("gcp::baremetalsolution::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with BaremetalsolutionProjectsLocationsNfsSharesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsNfsSharesDeleteArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsNfsSharesDeleteArgs,
    ) -> String {
        format!("gcp::baremetalsolution::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NfsShare
// =============================================================================

/// ResourceIdentifier implementation for NfsShare with BaremetalsolutionProjectsLocationsNfsSharesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsNfsSharesGetArgs> for NfsShare {
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsNfsSharesGetArgs,
    ) -> String {
        format!("gcp::baremetalsolution::NfsShare/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::NfsShare"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListNfsSharesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListNfsSharesResponse with BaremetalsolutionProjectsLocationsNfsSharesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsNfsSharesListArgs>
    for ListNfsSharesResponse
{
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsNfsSharesListArgs,
    ) -> String {
        format!(
            "gcp::baremetalsolution::ListNfsSharesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::ListNfsSharesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with BaremetalsolutionProjectsLocationsNfsSharesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsNfsSharesPatchArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsNfsSharesPatchArgs,
    ) -> String {
        format!("gcp::baremetalsolution::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for NfsShare
// =============================================================================

/// ResourceIdentifier implementation for NfsShare with BaremetalsolutionProjectsLocationsNfsSharesRenameArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsNfsSharesRenameArgs> for NfsShare {
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsNfsSharesRenameArgs,
    ) -> String {
        format!("gcp::baremetalsolution::NfsShare/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::NfsShare"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with BaremetalsolutionProjectsLocationsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsOperationsGetArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsOperationsGetArgs,
    ) -> String {
        format!("gcp::baremetalsolution::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for OSImage
// =============================================================================

/// ResourceIdentifier implementation for OSImage with BaremetalsolutionProjectsLocationsOsImagesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsOsImagesGetArgs> for OSImage {
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsOsImagesGetArgs,
    ) -> String {
        format!("gcp::baremetalsolution::OSImage/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::OSImage"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListOSImagesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListOSImagesResponse with BaremetalsolutionProjectsLocationsOsImagesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsOsImagesListArgs>
    for ListOSImagesResponse
{
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsOsImagesListArgs,
    ) -> String {
        format!(
            "gcp::baremetalsolution::ListOSImagesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::ListOSImagesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ProvisioningConfig
// =============================================================================

/// ResourceIdentifier implementation for ProvisioningConfig with BaremetalsolutionProjectsLocationsProvisioningConfigsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsProvisioningConfigsCreateArgs>
    for ProvisioningConfig
{
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsProvisioningConfigsCreateArgs,
    ) -> String {
        format!(
            "gcp::baremetalsolution::ProvisioningConfig/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::ProvisioningConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ProvisioningConfig
// =============================================================================

/// ResourceIdentifier implementation for ProvisioningConfig with BaremetalsolutionProjectsLocationsProvisioningConfigsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsProvisioningConfigsGetArgs>
    for ProvisioningConfig
{
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsProvisioningConfigsGetArgs,
    ) -> String {
        format!("gcp::baremetalsolution::ProvisioningConfig/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::ProvisioningConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ProvisioningConfig
// =============================================================================

/// ResourceIdentifier implementation for ProvisioningConfig with BaremetalsolutionProjectsLocationsProvisioningConfigsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsProvisioningConfigsPatchArgs>
    for ProvisioningConfig
{
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsProvisioningConfigsPatchArgs,
    ) -> String {
        format!("gcp::baremetalsolution::ProvisioningConfig/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::ProvisioningConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SubmitProvisioningConfigResponse
// =============================================================================

/// ResourceIdentifier implementation for SubmitProvisioningConfigResponse with BaremetalsolutionProjectsLocationsProvisioningConfigsSubmitArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsProvisioningConfigsSubmitArgs>
    for SubmitProvisioningConfigResponse
{
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsProvisioningConfigsSubmitArgs,
    ) -> String {
        format!(
            "gcp::baremetalsolution::SubmitProvisioningConfigResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::SubmitProvisioningConfigResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListProvisioningQuotasResponse
// =============================================================================

/// ResourceIdentifier implementation for ListProvisioningQuotasResponse with BaremetalsolutionProjectsLocationsProvisioningQuotasListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsProvisioningQuotasListArgs>
    for ListProvisioningQuotasResponse
{
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsProvisioningQuotasListArgs,
    ) -> String {
        format!(
            "gcp::baremetalsolution::ListProvisioningQuotasResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::ListProvisioningQuotasResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SSHKey
// =============================================================================

/// ResourceIdentifier implementation for SSHKey with BaremetalsolutionProjectsLocationsSshKeysCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsSshKeysCreateArgs> for SSHKey {
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsSshKeysCreateArgs,
    ) -> String {
        format!("gcp::baremetalsolution::SSHKey/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::SSHKey"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with BaremetalsolutionProjectsLocationsSshKeysDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsSshKeysDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsSshKeysDeleteArgs,
    ) -> String {
        format!("gcp::baremetalsolution::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListSSHKeysResponse
// =============================================================================

/// ResourceIdentifier implementation for ListSSHKeysResponse with BaremetalsolutionProjectsLocationsSshKeysListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsSshKeysListArgs> for ListSSHKeysResponse {
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsSshKeysListArgs,
    ) -> String {
        format!(
            "gcp::baremetalsolution::ListSSHKeysResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::ListSSHKeysResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with BaremetalsolutionProjectsLocationsVolumesEvictArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsVolumesEvictArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsVolumesEvictArgs,
    ) -> String {
        format!("gcp::baremetalsolution::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Volume
// =============================================================================

/// ResourceIdentifier implementation for Volume with BaremetalsolutionProjectsLocationsVolumesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsVolumesGetArgs> for Volume {
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsVolumesGetArgs,
    ) -> String {
        format!("gcp::baremetalsolution::Volume/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::Volume"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListVolumesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListVolumesResponse with BaremetalsolutionProjectsLocationsVolumesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsVolumesListArgs> for ListVolumesResponse {
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsVolumesListArgs,
    ) -> String {
        format!(
            "gcp::baremetalsolution::ListVolumesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::ListVolumesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with BaremetalsolutionProjectsLocationsVolumesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsVolumesPatchArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsVolumesPatchArgs,
    ) -> String {
        format!("gcp::baremetalsolution::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Volume
// =============================================================================

/// ResourceIdentifier implementation for Volume with BaremetalsolutionProjectsLocationsVolumesRenameArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsVolumesRenameArgs> for Volume {
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsVolumesRenameArgs,
    ) -> String {
        format!("gcp::baremetalsolution::Volume/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::Volume"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with BaremetalsolutionProjectsLocationsVolumesResizeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsVolumesResizeArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsVolumesResizeArgs,
    ) -> String {
        format!("gcp::baremetalsolution::Operation/{}", input.volume)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with BaremetalsolutionProjectsLocationsVolumesLunsEvictArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsVolumesLunsEvictArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsVolumesLunsEvictArgs,
    ) -> String {
        format!("gcp::baremetalsolution::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Lun
// =============================================================================

/// ResourceIdentifier implementation for Lun with BaremetalsolutionProjectsLocationsVolumesLunsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsVolumesLunsGetArgs> for Lun {
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsVolumesLunsGetArgs,
    ) -> String {
        format!("gcp::baremetalsolution::Lun/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::Lun"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLunsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLunsResponse with BaremetalsolutionProjectsLocationsVolumesLunsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsVolumesLunsListArgs>
    for ListLunsResponse
{
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsVolumesLunsListArgs,
    ) -> String {
        format!("gcp::baremetalsolution::ListLunsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::ListLunsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for VolumeSnapshot
// =============================================================================

/// ResourceIdentifier implementation for VolumeSnapshot with BaremetalsolutionProjectsLocationsVolumesSnapshotsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsVolumesSnapshotsCreateArgs>
    for VolumeSnapshot
{
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsVolumesSnapshotsCreateArgs,
    ) -> String {
        format!("gcp::baremetalsolution::VolumeSnapshot/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::VolumeSnapshot"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with BaremetalsolutionProjectsLocationsVolumesSnapshotsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsVolumesSnapshotsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsVolumesSnapshotsDeleteArgs,
    ) -> String {
        format!("gcp::baremetalsolution::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for VolumeSnapshot
// =============================================================================

/// ResourceIdentifier implementation for VolumeSnapshot with BaremetalsolutionProjectsLocationsVolumesSnapshotsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsVolumesSnapshotsGetArgs>
    for VolumeSnapshot
{
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsVolumesSnapshotsGetArgs,
    ) -> String {
        format!("gcp::baremetalsolution::VolumeSnapshot/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::VolumeSnapshot"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListVolumeSnapshotsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListVolumeSnapshotsResponse with BaremetalsolutionProjectsLocationsVolumesSnapshotsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsVolumesSnapshotsListArgs>
    for ListVolumeSnapshotsResponse
{
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsVolumesSnapshotsListArgs,
    ) -> String {
        format!(
            "gcp::baremetalsolution::ListVolumeSnapshotsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::ListVolumeSnapshotsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with BaremetalsolutionProjectsLocationsVolumesSnapshotsRestoreVolumeSnapshotArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BaremetalsolutionProjectsLocationsVolumesSnapshotsRestoreVolumeSnapshotArgs>
    for Operation
{
    fn generate_resource_id(
        &self,
        input: &BaremetalsolutionProjectsLocationsVolumesSnapshotsRestoreVolumeSnapshotArgs,
    ) -> String {
        format!("gcp::baremetalsolution::Operation/{}", input.volumeSnapshot)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::baremetalsolution::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
