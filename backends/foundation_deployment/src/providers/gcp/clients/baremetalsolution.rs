//! Auto-generated API clients for Google Cloud Platform - baremetalsolution.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v2/{+name}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_get_execute()` to send, or `baremetalsolution_projects_locations_get` for simplest API.

pub fn baremetalsolution_projects_locations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_get_builder()` + `baremetalsolution_projects_locations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_get_builder(client, name)?;
    baremetalsolution_projects_locations_get_execute(builder)
}

/// GET v2/{+name}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_list_execute()` to send, or `baremetalsolution_projects_locations_list` for simplest API.

pub fn baremetalsolution_projects_locations_list_builder(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}/locations", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "extraLocationTypes",
            extraLocationTypes
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+name}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_list_builder()` + `baremetalsolution_projects_locations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_list(
    client: &SimpleHttpClient,
    name: &str,
    extraLocationTypes: Option<&str>,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_list_builder(
        client,
        name,
        extraLocationTypes,
        filter,
        pageSize,
        pageToken,
    )?;
    baremetalsolution_projects_locations_list_execute(builder)
}

/// GET v2/{+instance}:detachLun
/// Detach LUN from Instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_instances_detach_lun_execute()` to send, or `baremetalsolution_projects_locations_instances_detach_lun` for simplest API.

pub fn baremetalsolution_projects_locations_instances_detach_lun_builder(
    client: &SimpleHttpClient,
    instance: &str,
    body: &DetachLunRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+instance}:detachLun", instance,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+instance}:detachLun
/// Detach LUN from Instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_instances_detach_lun_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_instances_detach_lun_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_detach_lun_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+instance}:detachLun
/// Detach LUN from Instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_instances_detach_lun_builder()` + `baremetalsolution_projects_locations_instances_detach_lun_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_detach_lun(
    client: &SimpleHttpClient,
    instance: &str,
    body: &DetachLunRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        baremetalsolution_projects_locations_instances_detach_lun_builder(client, instance, body)?;
    baremetalsolution_projects_locations_instances_detach_lun_execute(builder)
}

/// GET v2/{+name}:disableHyperthreading
/// Perform disable hyperthreading operation on a single server.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_instances_disable_hyperthreading_execute()` to send, or `baremetalsolution_projects_locations_instances_disable_hyperthreading` for simplest API.

pub fn baremetalsolution_projects_locations_instances_disable_hyperthreading_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &DisableHyperthreadingRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+name}:disableHyperthreading",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:disableHyperthreading
/// Perform disable hyperthreading operation on a single server.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_instances_disable_hyperthreading_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_instances_disable_hyperthreading_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_disable_hyperthreading_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:disableHyperthreading
/// Perform disable hyperthreading operation on a single server.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_instances_disable_hyperthreading_builder()` + `baremetalsolution_projects_locations_instances_disable_hyperthreading_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_disable_hyperthreading(
    client: &SimpleHttpClient,
    name: &str,
    body: &DisableHyperthreadingRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_instances_disable_hyperthreading_builder(
        client, name, body,
    )?;
    baremetalsolution_projects_locations_instances_disable_hyperthreading_execute(builder)
}

/// GET v2/{+name}:disableInteractiveSerialConsole
/// Disable the interactive serial console feature on an instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_instances_disable_interactive_serial_console_execute()` to send, or `baremetalsolution_projects_locations_instances_disable_interactive_serial_console` for simplest API.

pub fn baremetalsolution_projects_locations_instances_disable_interactive_serial_console_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &DisableInteractiveSerialConsoleRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+name}:disableInteractiveSerialConsole",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:disableInteractiveSerialConsole
/// Disable the interactive serial console feature on an instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_instances_disable_interactive_serial_console_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_instances_disable_interactive_serial_console_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_disable_interactive_serial_console_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:disableInteractiveSerialConsole
/// Disable the interactive serial console feature on an instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_instances_disable_interactive_serial_console_builder()` + `baremetalsolution_projects_locations_instances_disable_interactive_serial_console_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_disable_interactive_serial_console(
    client: &SimpleHttpClient,
    name: &str,
    body: &DisableInteractiveSerialConsoleRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        baremetalsolution_projects_locations_instances_disable_interactive_serial_console_builder(
            client, name, body,
        )?;
    baremetalsolution_projects_locations_instances_disable_interactive_serial_console_execute(
        builder,
    )
}

/// GET v2/{+name}:enableHyperthreading
/// Perform enable hyperthreading operation on a single server.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_instances_enable_hyperthreading_execute()` to send, or `baremetalsolution_projects_locations_instances_enable_hyperthreading` for simplest API.

pub fn baremetalsolution_projects_locations_instances_enable_hyperthreading_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &EnableHyperthreadingRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+name}:enableHyperthreading",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:enableHyperthreading
/// Perform enable hyperthreading operation on a single server.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_instances_enable_hyperthreading_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_instances_enable_hyperthreading_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_enable_hyperthreading_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:enableHyperthreading
/// Perform enable hyperthreading operation on a single server.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_instances_enable_hyperthreading_builder()` + `baremetalsolution_projects_locations_instances_enable_hyperthreading_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_enable_hyperthreading(
    client: &SimpleHttpClient,
    name: &str,
    body: &EnableHyperthreadingRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_instances_enable_hyperthreading_builder(
        client, name, body,
    )?;
    baremetalsolution_projects_locations_instances_enable_hyperthreading_execute(builder)
}

/// GET v2/{+name}:enableInteractiveSerialConsole
/// Enable the interactive serial console feature on an instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_instances_enable_interactive_serial_console_execute()` to send, or `baremetalsolution_projects_locations_instances_enable_interactive_serial_console` for simplest API.

pub fn baremetalsolution_projects_locations_instances_enable_interactive_serial_console_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &EnableInteractiveSerialConsoleRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+name}:enableInteractiveSerialConsole",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:enableInteractiveSerialConsole
/// Enable the interactive serial console feature on an instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_instances_enable_interactive_serial_console_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_instances_enable_interactive_serial_console_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_enable_interactive_serial_console_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:enableInteractiveSerialConsole
/// Enable the interactive serial console feature on an instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_instances_enable_interactive_serial_console_builder()` + `baremetalsolution_projects_locations_instances_enable_interactive_serial_console_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_enable_interactive_serial_console(
    client: &SimpleHttpClient,
    name: &str,
    body: &EnableInteractiveSerialConsoleRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        baremetalsolution_projects_locations_instances_enable_interactive_serial_console_builder(
            client, name, body,
        )?;
    baremetalsolution_projects_locations_instances_enable_interactive_serial_console_execute(
        builder,
    )
}

/// GET v2/{+name}
/// Get details about a single server.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_instances_get_execute()` to send, or `baremetalsolution_projects_locations_instances_get` for simplest API.

pub fn baremetalsolution_projects_locations_instances_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Get details about a single server.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_instances_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_instances_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Instance>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Instance = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Get details about a single server.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_instances_get_builder()` + `baremetalsolution_projects_locations_instances_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Instance>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_instances_get_builder(client, name)?;
    baremetalsolution_projects_locations_instances_get_execute(builder)
}

/// GET v2/{+parent}/instances
/// List servers in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_instances_list_execute()` to send, or `baremetalsolution_projects_locations_instances_list` for simplest API.

pub fn baremetalsolution_projects_locations_instances_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/instances", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/instances
/// List servers in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_instances_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_instances_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListInstancesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListInstancesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/instances
/// List servers in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_instances_list_builder()` + `baremetalsolution_projects_locations_instances_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListInstancesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_instances_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    baremetalsolution_projects_locations_instances_list_execute(builder)
}

/// GET v2/{+name}:loadAuthInfo
/// Load auth info for a server.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_instances_load_auth_info_execute()` to send, or `baremetalsolution_projects_locations_instances_load_auth_info` for simplest API.

pub fn baremetalsolution_projects_locations_instances_load_auth_info_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:loadAuthInfo", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:loadAuthInfo
/// Load auth info for a server.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_instances_load_auth_info_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_instances_load_auth_info_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_load_auth_info_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LoadInstanceAuthInfoResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LoadInstanceAuthInfoResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:loadAuthInfo
/// Load auth info for a server.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_instances_load_auth_info_builder()` + `baremetalsolution_projects_locations_instances_load_auth_info_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_load_auth_info(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<LoadInstanceAuthInfoResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        baremetalsolution_projects_locations_instances_load_auth_info_builder(client, name)?;
    baremetalsolution_projects_locations_instances_load_auth_info_execute(builder)
}

/// GET v2/{+name}
/// Update details of a single server.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_instances_patch_execute()` to send, or `baremetalsolution_projects_locations_instances_patch` for simplest API.

pub fn baremetalsolution_projects_locations_instances_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Instance,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Update details of a single server.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_instances_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_instances_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Update details of a single server.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_instances_patch_builder()` + `baremetalsolution_projects_locations_instances_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Instance,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_instances_patch_builder(
        client, name, updateMask, body,
    )?;
    baremetalsolution_projects_locations_instances_patch_execute(builder)
}

/// GET v2/{+name}:reimage
/// Perform reimage operation on a single server.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_instances_reimage_execute()` to send, or `baremetalsolution_projects_locations_instances_reimage` for simplest API.

pub fn baremetalsolution_projects_locations_instances_reimage_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ReimageInstanceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:reimage", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:reimage
/// Perform reimage operation on a single server.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_instances_reimage_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_instances_reimage_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_reimage_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:reimage
/// Perform reimage operation on a single server.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_instances_reimage_builder()` + `baremetalsolution_projects_locations_instances_reimage_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_reimage(
    client: &SimpleHttpClient,
    name: &str,
    body: &ReimageInstanceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        baremetalsolution_projects_locations_instances_reimage_builder(client, name, body)?;
    baremetalsolution_projects_locations_instances_reimage_execute(builder)
}

/// GET v2/{+name}:rename
/// RenameInstance sets a new name for an instance. Use with caution, previous names become immediately invalidated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_instances_rename_execute()` to send, or `baremetalsolution_projects_locations_instances_rename` for simplest API.

pub fn baremetalsolution_projects_locations_instances_rename_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &RenameInstanceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:rename", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:rename
/// RenameInstance sets a new name for an instance. Use with caution, previous names become immediately invalidated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_instances_rename_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_instances_rename_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_rename_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Instance>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Instance = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:rename
/// RenameInstance sets a new name for an instance. Use with caution, previous names become immediately invalidated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_instances_rename_builder()` + `baremetalsolution_projects_locations_instances_rename_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_rename(
    client: &SimpleHttpClient,
    name: &str,
    body: &RenameInstanceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Instance>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        baremetalsolution_projects_locations_instances_rename_builder(client, name, body)?;
    baremetalsolution_projects_locations_instances_rename_execute(builder)
}

/// GET v2/{+name}:reset
/// Perform an ungraceful, hard reset on a server. Equivalent to shutting the power off and then turning it back on.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_instances_reset_execute()` to send, or `baremetalsolution_projects_locations_instances_reset` for simplest API.

pub fn baremetalsolution_projects_locations_instances_reset_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ResetInstanceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:reset", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:reset
/// Perform an ungraceful, hard reset on a server. Equivalent to shutting the power off and then turning it back on.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_instances_reset_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_instances_reset_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_reset_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:reset
/// Perform an ungraceful, hard reset on a server. Equivalent to shutting the power off and then turning it back on.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_instances_reset_builder()` + `baremetalsolution_projects_locations_instances_reset_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_reset(
    client: &SimpleHttpClient,
    name: &str,
    body: &ResetInstanceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_instances_reset_builder(client, name, body)?;
    baremetalsolution_projects_locations_instances_reset_execute(builder)
}

/// GET v2/{+name}:start
/// Starts a server that was shutdown.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_instances_start_execute()` to send, or `baremetalsolution_projects_locations_instances_start` for simplest API.

pub fn baremetalsolution_projects_locations_instances_start_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &StartInstanceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:start", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:start
/// Starts a server that was shutdown.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_instances_start_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_instances_start_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_start_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:start
/// Starts a server that was shutdown.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_instances_start_builder()` + `baremetalsolution_projects_locations_instances_start_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_start(
    client: &SimpleHttpClient,
    name: &str,
    body: &StartInstanceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_instances_start_builder(client, name, body)?;
    baremetalsolution_projects_locations_instances_start_execute(builder)
}

/// GET v2/{+name}:stop
/// Stop a running server.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_instances_stop_execute()` to send, or `baremetalsolution_projects_locations_instances_stop` for simplest API.

pub fn baremetalsolution_projects_locations_instances_stop_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &StopInstanceRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:stop", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:stop
/// Stop a running server.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_instances_stop_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_instances_stop_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_stop_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:stop
/// Stop a running server.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_instances_stop_builder()` + `baremetalsolution_projects_locations_instances_stop_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_instances_stop(
    client: &SimpleHttpClient,
    name: &str,
    body: &StopInstanceRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_instances_stop_builder(client, name, body)?;
    baremetalsolution_projects_locations_instances_stop_execute(builder)
}

/// GET v2/{+name}
/// Get details of a single network.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_networks_get_execute()` to send, or `baremetalsolution_projects_locations_networks_get` for simplest API.

pub fn baremetalsolution_projects_locations_networks_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Get details of a single network.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_networks_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_networks_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_networks_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Network>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Network = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Get details of a single network.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_networks_get_builder()` + `baremetalsolution_projects_locations_networks_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_networks_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Network>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_networks_get_builder(client, name)?;
    baremetalsolution_projects_locations_networks_get_execute(builder)
}

/// GET v2/{+parent}/networks
/// List network in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_networks_list_execute()` to send, or `baremetalsolution_projects_locations_networks_list` for simplest API.

pub fn baremetalsolution_projects_locations_networks_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/networks", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/networks
/// List network in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_networks_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_networks_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_networks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListNetworksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListNetworksResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/networks
/// List network in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_networks_list_builder()` + `baremetalsolution_projects_locations_networks_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_networks_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListNetworksResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_networks_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    baremetalsolution_projects_locations_networks_list_execute(builder)
}

/// GET v2/{+location}/networks:listNetworkUsage
/// List all Networks (and used IPs for each Network) in the vendor account associated with the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_networks_list_network_usage_execute()` to send, or `baremetalsolution_projects_locations_networks_list_network_usage` for simplest API.

pub fn baremetalsolution_projects_locations_networks_list_network_usage_builder(
    client: &SimpleHttpClient,
    location: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+location}/networks:listNetworkUsage",
        location,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+location}/networks:listNetworkUsage
/// List all Networks (and used IPs for each Network) in the vendor account associated with the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_networks_list_network_usage_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_networks_list_network_usage_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_networks_list_network_usage_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListNetworkUsageResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListNetworkUsageResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+location}/networks:listNetworkUsage
/// List all Networks (and used IPs for each Network) in the vendor account associated with the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_networks_list_network_usage_builder()` + `baremetalsolution_projects_locations_networks_list_network_usage_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_networks_list_network_usage(
    client: &SimpleHttpClient,
    location: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListNetworkUsageResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        baremetalsolution_projects_locations_networks_list_network_usage_builder(client, location)?;
    baremetalsolution_projects_locations_networks_list_network_usage_execute(builder)
}

/// GET v2/{+name}
/// Update details of a single network.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_networks_patch_execute()` to send, or `baremetalsolution_projects_locations_networks_patch` for simplest API.

pub fn baremetalsolution_projects_locations_networks_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Network,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Update details of a single network.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_networks_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_networks_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_networks_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Update details of a single network.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_networks_patch_builder()` + `baremetalsolution_projects_locations_networks_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_networks_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Network,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_networks_patch_builder(
        client, name, updateMask, body,
    )?;
    baremetalsolution_projects_locations_networks_patch_execute(builder)
}

/// GET v2/{+name}:rename
/// RenameNetwork sets a new name for a network. Use with caution, previous names become immediately invalidated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_networks_rename_execute()` to send, or `baremetalsolution_projects_locations_networks_rename` for simplest API.

pub fn baremetalsolution_projects_locations_networks_rename_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &RenameNetworkRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:rename", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:rename
/// RenameNetwork sets a new name for a network. Use with caution, previous names become immediately invalidated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_networks_rename_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_networks_rename_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_networks_rename_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Network>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Network = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:rename
/// RenameNetwork sets a new name for a network. Use with caution, previous names become immediately invalidated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_networks_rename_builder()` + `baremetalsolution_projects_locations_networks_rename_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_networks_rename(
    client: &SimpleHttpClient,
    name: &str,
    body: &RenameNetworkRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Network>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_networks_rename_builder(client, name, body)?;
    baremetalsolution_projects_locations_networks_rename_execute(builder)
}

/// GET v2/{+parent}/nfsShares
/// Create an NFS share.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_nfs_shares_create_execute()` to send, or `baremetalsolution_projects_locations_nfs_shares_create` for simplest API.

pub fn baremetalsolution_projects_locations_nfs_shares_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &NfsShare,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/nfsShares", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/nfsShares
/// Create an NFS share.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_nfs_shares_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_nfs_shares_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_nfs_shares_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/nfsShares
/// Create an NFS share.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_nfs_shares_create_builder()` + `baremetalsolution_projects_locations_nfs_shares_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_nfs_shares_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &NfsShare,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        baremetalsolution_projects_locations_nfs_shares_create_builder(client, parent, body)?;
    baremetalsolution_projects_locations_nfs_shares_create_execute(builder)
}

/// GET v2/{+name}
/// Delete an NFS share. The underlying volume is automatically deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_nfs_shares_delete_execute()` to send, or `baremetalsolution_projects_locations_nfs_shares_delete` for simplest API.

pub fn baremetalsolution_projects_locations_nfs_shares_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Delete an NFS share. The underlying volume is automatically deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_nfs_shares_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_nfs_shares_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_nfs_shares_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Delete an NFS share. The underlying volume is automatically deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_nfs_shares_delete_builder()` + `baremetalsolution_projects_locations_nfs_shares_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_nfs_shares_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_nfs_shares_delete_builder(client, name)?;
    baremetalsolution_projects_locations_nfs_shares_delete_execute(builder)
}

/// GET v2/{+name}
/// Get details of a single NFS share.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_nfs_shares_get_execute()` to send, or `baremetalsolution_projects_locations_nfs_shares_get` for simplest API.

pub fn baremetalsolution_projects_locations_nfs_shares_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Get details of a single NFS share.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_nfs_shares_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_nfs_shares_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_nfs_shares_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NfsShare>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NfsShare = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Get details of a single NFS share.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_nfs_shares_get_builder()` + `baremetalsolution_projects_locations_nfs_shares_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_nfs_shares_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NfsShare>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_nfs_shares_get_builder(client, name)?;
    baremetalsolution_projects_locations_nfs_shares_get_execute(builder)
}

/// GET v2/{+parent}/nfsShares
/// List NFS shares.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_nfs_shares_list_execute()` to send, or `baremetalsolution_projects_locations_nfs_shares_list` for simplest API.

pub fn baremetalsolution_projects_locations_nfs_shares_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/nfsShares", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/nfsShares
/// List NFS shares.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_nfs_shares_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_nfs_shares_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_nfs_shares_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListNfsSharesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListNfsSharesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/nfsShares
/// List NFS shares.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_nfs_shares_list_builder()` + `baremetalsolution_projects_locations_nfs_shares_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_nfs_shares_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListNfsSharesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_nfs_shares_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    baremetalsolution_projects_locations_nfs_shares_list_execute(builder)
}

/// GET v2/{+name}
/// Update details of a single NFS share.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_nfs_shares_patch_execute()` to send, or `baremetalsolution_projects_locations_nfs_shares_patch` for simplest API.

pub fn baremetalsolution_projects_locations_nfs_shares_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &NfsShare,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Update details of a single NFS share.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_nfs_shares_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_nfs_shares_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_nfs_shares_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Update details of a single NFS share.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_nfs_shares_patch_builder()` + `baremetalsolution_projects_locations_nfs_shares_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_nfs_shares_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &NfsShare,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_nfs_shares_patch_builder(
        client, name, updateMask, body,
    )?;
    baremetalsolution_projects_locations_nfs_shares_patch_execute(builder)
}

/// GET v2/{+name}:rename
/// RenameNfsShare sets a new name for an nfsshare. Use with caution, previous names become immediately invalidated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_nfs_shares_rename_execute()` to send, or `baremetalsolution_projects_locations_nfs_shares_rename` for simplest API.

pub fn baremetalsolution_projects_locations_nfs_shares_rename_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &RenameNfsShareRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:rename", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:rename
/// RenameNfsShare sets a new name for an nfsshare. Use with caution, previous names become immediately invalidated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_nfs_shares_rename_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_nfs_shares_rename_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_nfs_shares_rename_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NfsShare>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: NfsShare = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:rename
/// RenameNfsShare sets a new name for an nfsshare. Use with caution, previous names become immediately invalidated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_nfs_shares_rename_builder()` + `baremetalsolution_projects_locations_nfs_shares_rename_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_nfs_shares_rename(
    client: &SimpleHttpClient,
    name: &str,
    body: &RenameNfsShareRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<NfsShare>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        baremetalsolution_projects_locations_nfs_shares_rename_builder(client, name, body)?;
    baremetalsolution_projects_locations_nfs_shares_rename_execute(builder)
}

/// GET v2/{+name}
/// Get details about an operation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_operations_get_execute()` to send, or `baremetalsolution_projects_locations_operations_get` for simplest API.

pub fn baremetalsolution_projects_locations_operations_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Get details about an operation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Get details about an operation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_operations_get_builder()` + `baremetalsolution_projects_locations_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_operations_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_operations_get_builder(client, name)?;
    baremetalsolution_projects_locations_operations_get_execute(builder)
}

/// GET v2/{+name}
/// Get details of a single OS image.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_os_images_get_execute()` to send, or `baremetalsolution_projects_locations_os_images_get` for simplest API.

pub fn baremetalsolution_projects_locations_os_images_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Get details of a single OS image.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_os_images_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_os_images_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_os_images_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OSImage>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OSImage = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Get details of a single OS image.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_os_images_get_builder()` + `baremetalsolution_projects_locations_os_images_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_os_images_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OSImage>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_os_images_get_builder(client, name)?;
    baremetalsolution_projects_locations_os_images_get_execute(builder)
}

/// GET v2/{+parent}/osImages
/// Retrieves the list of OS images which are currently approved.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_os_images_list_execute()` to send, or `baremetalsolution_projects_locations_os_images_list` for simplest API.

pub fn baremetalsolution_projects_locations_os_images_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/osImages", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/osImages
/// Retrieves the list of OS images which are currently approved.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_os_images_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_os_images_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_os_images_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOSImagesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOSImagesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/osImages
/// Retrieves the list of OS images which are currently approved.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_os_images_list_builder()` + `baremetalsolution_projects_locations_os_images_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_os_images_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOSImagesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_os_images_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    baremetalsolution_projects_locations_os_images_list_execute(builder)
}

/// GET v2/{+parent}/provisioningConfigs
/// Create new ProvisioningConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_provisioning_configs_create_execute()` to send, or `baremetalsolution_projects_locations_provisioning_configs_create` for simplest API.

pub fn baremetalsolution_projects_locations_provisioning_configs_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    email: Option<&str>,
    body: &ProvisioningConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/provisioningConfigs",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "email",
            email
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/provisioningConfigs
/// Create new ProvisioningConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_provisioning_configs_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_provisioning_configs_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_provisioning_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProvisioningConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProvisioningConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/provisioningConfigs
/// Create new ProvisioningConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_provisioning_configs_create_builder()` + `baremetalsolution_projects_locations_provisioning_configs_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_provisioning_configs_create(
    client: &SimpleHttpClient,
    parent: &str,
    email: Option<&str>,
    body: &ProvisioningConfig,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProvisioningConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_provisioning_configs_create_builder(
        client, parent, email, body,
    )?;
    baremetalsolution_projects_locations_provisioning_configs_create_execute(builder)
}

/// GET v2/{+name}
/// Get ProvisioningConfig by name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_provisioning_configs_get_execute()` to send, or `baremetalsolution_projects_locations_provisioning_configs_get` for simplest API.

pub fn baremetalsolution_projects_locations_provisioning_configs_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Get ProvisioningConfig by name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_provisioning_configs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_provisioning_configs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_provisioning_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProvisioningConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProvisioningConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Get ProvisioningConfig by name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_provisioning_configs_get_builder()` + `baremetalsolution_projects_locations_provisioning_configs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_provisioning_configs_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProvisioningConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        baremetalsolution_projects_locations_provisioning_configs_get_builder(client, name)?;
    baremetalsolution_projects_locations_provisioning_configs_get_execute(builder)
}

/// GET v2/{+name}
/// Update existing ProvisioningConfig.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_provisioning_configs_patch_execute()` to send, or `baremetalsolution_projects_locations_provisioning_configs_patch` for simplest API.

pub fn baremetalsolution_projects_locations_provisioning_configs_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    email: Option<&str>,
    updateMask: Option<&str>,
    body: &ProvisioningConfig,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "email",
            email
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Update existing ProvisioningConfig.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_provisioning_configs_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_provisioning_configs_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_provisioning_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProvisioningConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ProvisioningConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Update existing ProvisioningConfig.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_provisioning_configs_patch_builder()` + `baremetalsolution_projects_locations_provisioning_configs_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_provisioning_configs_patch(
    client: &SimpleHttpClient,
    name: &str,
    email: Option<&str>,
    updateMask: Option<&str>,
    body: &ProvisioningConfig,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ProvisioningConfig>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_provisioning_configs_patch_builder(
        client, name, email, updateMask, body,
    )?;
    baremetalsolution_projects_locations_provisioning_configs_patch_execute(builder)
}

/// GET v2/{+parent}/provisioningConfigs:submit
/// Submit a provisioning configuration for a given project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_provisioning_configs_submit_execute()` to send, or `baremetalsolution_projects_locations_provisioning_configs_submit` for simplest API.

pub fn baremetalsolution_projects_locations_provisioning_configs_submit_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SubmitProvisioningConfigRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/provisioningConfigs:submit",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/provisioningConfigs:submit
/// Submit a provisioning configuration for a given project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_provisioning_configs_submit_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_provisioning_configs_submit_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_provisioning_configs_submit_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SubmitProvisioningConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SubmitProvisioningConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/provisioningConfigs:submit
/// Submit a provisioning configuration for a given project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_provisioning_configs_submit_builder()` + `baremetalsolution_projects_locations_provisioning_configs_submit_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_provisioning_configs_submit(
    client: &SimpleHttpClient,
    parent: &str,
    body: &SubmitProvisioningConfigRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SubmitProvisioningConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_provisioning_configs_submit_builder(
        client, parent, body,
    )?;
    baremetalsolution_projects_locations_provisioning_configs_submit_execute(builder)
}

/// GET v2/{+parent}/provisioningQuotas
/// List the budget details to provision resources on a given project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_provisioning_quotas_list_execute()` to send, or `baremetalsolution_projects_locations_provisioning_quotas_list` for simplest API.

pub fn baremetalsolution_projects_locations_provisioning_quotas_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/provisioningQuotas",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/provisioningQuotas
/// List the budget details to provision resources on a given project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_provisioning_quotas_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_provisioning_quotas_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_provisioning_quotas_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListProvisioningQuotasResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListProvisioningQuotasResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/provisioningQuotas
/// List the budget details to provision resources on a given project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_provisioning_quotas_list_builder()` + `baremetalsolution_projects_locations_provisioning_quotas_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_provisioning_quotas_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListProvisioningQuotasResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_provisioning_quotas_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    baremetalsolution_projects_locations_provisioning_quotas_list_execute(builder)
}

/// GET v2/{+parent}/sshKeys
/// Register a public SSH key in the specified project for use with the interactive serial console feature.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_ssh_keys_create_execute()` to send, or `baremetalsolution_projects_locations_ssh_keys_create` for simplest API.

pub fn baremetalsolution_projects_locations_ssh_keys_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    sshKeyId: Option<&str>,
    body: &SSHKey,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/sshKeys", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "sshKeyId",
            sshKeyId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/sshKeys
/// Register a public SSH key in the specified project for use with the interactive serial console feature.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_ssh_keys_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_ssh_keys_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_ssh_keys_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SSHKey>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SSHKey = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/sshKeys
/// Register a public SSH key in the specified project for use with the interactive serial console feature.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_ssh_keys_create_builder()` + `baremetalsolution_projects_locations_ssh_keys_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_ssh_keys_create(
    client: &SimpleHttpClient,
    parent: &str,
    sshKeyId: Option<&str>,
    body: &SSHKey,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SSHKey>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_ssh_keys_create_builder(
        client, parent, sshKeyId, body,
    )?;
    baremetalsolution_projects_locations_ssh_keys_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes a public SSH key registered in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_ssh_keys_delete_execute()` to send, or `baremetalsolution_projects_locations_ssh_keys_delete` for simplest API.

pub fn baremetalsolution_projects_locations_ssh_keys_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a public SSH key registered in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_ssh_keys_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_ssh_keys_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_ssh_keys_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a public SSH key registered in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_ssh_keys_delete_builder()` + `baremetalsolution_projects_locations_ssh_keys_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_ssh_keys_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_ssh_keys_delete_builder(client, name)?;
    baremetalsolution_projects_locations_ssh_keys_delete_execute(builder)
}

/// GET v2/{+parent}/sshKeys
/// Lists the public SSH keys registered for the specified project. These SSH keys are used only for the interactive serial console feature.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_ssh_keys_list_execute()` to send, or `baremetalsolution_projects_locations_ssh_keys_list` for simplest API.

pub fn baremetalsolution_projects_locations_ssh_keys_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/sshKeys", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/sshKeys
/// Lists the public SSH keys registered for the specified project. These SSH keys are used only for the interactive serial console feature.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_ssh_keys_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_ssh_keys_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_ssh_keys_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSSHKeysResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSSHKeysResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/sshKeys
/// Lists the public SSH keys registered for the specified project. These SSH keys are used only for the interactive serial console feature.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_ssh_keys_list_builder()` + `baremetalsolution_projects_locations_ssh_keys_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_ssh_keys_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSSHKeysResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_ssh_keys_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    baremetalsolution_projects_locations_ssh_keys_list_execute(builder)
}

/// GET v2/{+name}:evict
/// Skips volume's cooloff and deletes it now. Volume must be in cooloff state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_volumes_evict_execute()` to send, or `baremetalsolution_projects_locations_volumes_evict` for simplest API.

pub fn baremetalsolution_projects_locations_volumes_evict_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &EvictVolumeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:evict", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:evict
/// Skips volume's cooloff and deletes it now. Volume must be in cooloff state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_volumes_evict_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_volumes_evict_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_evict_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:evict
/// Skips volume's cooloff and deletes it now. Volume must be in cooloff state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_volumes_evict_builder()` + `baremetalsolution_projects_locations_volumes_evict_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_evict(
    client: &SimpleHttpClient,
    name: &str,
    body: &EvictVolumeRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_volumes_evict_builder(client, name, body)?;
    baremetalsolution_projects_locations_volumes_evict_execute(builder)
}

/// GET v2/{+name}
/// Get details of a single storage volume.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_volumes_get_execute()` to send, or `baremetalsolution_projects_locations_volumes_get` for simplest API.

pub fn baremetalsolution_projects_locations_volumes_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Get details of a single storage volume.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_volumes_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_volumes_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volume>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Volume = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Get details of a single storage volume.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_volumes_get_builder()` + `baremetalsolution_projects_locations_volumes_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volume>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_volumes_get_builder(client, name)?;
    baremetalsolution_projects_locations_volumes_get_execute(builder)
}

/// GET v2/{+parent}/volumes
/// List storage volumes in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_volumes_list_execute()` to send, or `baremetalsolution_projects_locations_volumes_list` for simplest API.

pub fn baremetalsolution_projects_locations_volumes_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/volumes", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/volumes
/// List storage volumes in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_volumes_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_volumes_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListVolumesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListVolumesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/volumes
/// List storage volumes in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_volumes_list_builder()` + `baremetalsolution_projects_locations_volumes_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListVolumesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_volumes_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    baremetalsolution_projects_locations_volumes_list_execute(builder)
}

/// GET v2/{+name}
/// Update details of a single storage volume.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_volumes_patch_execute()` to send, or `baremetalsolution_projects_locations_volumes_patch` for simplest API.

pub fn baremetalsolution_projects_locations_volumes_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Volume,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Update details of a single storage volume.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_volumes_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_volumes_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Update details of a single storage volume.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_volumes_patch_builder()` + `baremetalsolution_projects_locations_volumes_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Volume,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        baremetalsolution_projects_locations_volumes_patch_builder(client, name, updateMask, body)?;
    baremetalsolution_projects_locations_volumes_patch_execute(builder)
}

/// GET v2/{+name}:rename
/// RenameVolume sets a new name for a volume. Use with caution, previous names become immediately invalidated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_volumes_rename_execute()` to send, or `baremetalsolution_projects_locations_volumes_rename` for simplest API.

pub fn baremetalsolution_projects_locations_volumes_rename_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &RenameVolumeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:rename", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:rename
/// RenameVolume sets a new name for a volume. Use with caution, previous names become immediately invalidated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_volumes_rename_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_volumes_rename_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_rename_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volume>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Volume = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:rename
/// RenameVolume sets a new name for a volume. Use with caution, previous names become immediately invalidated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_volumes_rename_builder()` + `baremetalsolution_projects_locations_volumes_rename_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_rename(
    client: &SimpleHttpClient,
    name: &str,
    body: &RenameVolumeRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Volume>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_volumes_rename_builder(client, name, body)?;
    baremetalsolution_projects_locations_volumes_rename_execute(builder)
}

/// GET v2/{+volume}:resize
/// Emergency Volume resize.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_volumes_resize_execute()` to send, or `baremetalsolution_projects_locations_volumes_resize` for simplest API.

pub fn baremetalsolution_projects_locations_volumes_resize_builder(
    client: &SimpleHttpClient,
    volume: &str,
    body: &ResizeVolumeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+volume}:resize", volume,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+volume}:resize
/// Emergency Volume resize.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_volumes_resize_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_volumes_resize_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_resize_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+volume}:resize
/// Emergency Volume resize.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_volumes_resize_builder()` + `baremetalsolution_projects_locations_volumes_resize_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_resize(
    client: &SimpleHttpClient,
    volume: &str,
    body: &ResizeVolumeRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        baremetalsolution_projects_locations_volumes_resize_builder(client, volume, body)?;
    baremetalsolution_projects_locations_volumes_resize_execute(builder)
}

/// GET v2/{+name}:evict
/// Skips lun's cooloff and deletes it now. Lun must be in cooloff state.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_volumes_luns_evict_execute()` to send, or `baremetalsolution_projects_locations_volumes_luns_evict` for simplest API.

pub fn baremetalsolution_projects_locations_volumes_luns_evict_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &EvictLunRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:evict", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:evict
/// Skips lun's cooloff and deletes it now. Lun must be in cooloff state.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_volumes_luns_evict_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_volumes_luns_evict_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_luns_evict_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:evict
/// Skips lun's cooloff and deletes it now. Lun must be in cooloff state.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_volumes_luns_evict_builder()` + `baremetalsolution_projects_locations_volumes_luns_evict_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_luns_evict(
    client: &SimpleHttpClient,
    name: &str,
    body: &EvictLunRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        baremetalsolution_projects_locations_volumes_luns_evict_builder(client, name, body)?;
    baremetalsolution_projects_locations_volumes_luns_evict_execute(builder)
}

/// GET v2/{+name}
/// Get details of a single storage logical unit number(LUN).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_volumes_luns_get_execute()` to send, or `baremetalsolution_projects_locations_volumes_luns_get` for simplest API.

pub fn baremetalsolution_projects_locations_volumes_luns_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Get details of a single storage logical unit number(LUN).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_volumes_luns_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_volumes_luns_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_luns_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Lun>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Lun = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Get details of a single storage logical unit number(LUN).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_volumes_luns_get_builder()` + `baremetalsolution_projects_locations_volumes_luns_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_luns_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Lun>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_volumes_luns_get_builder(client, name)?;
    baremetalsolution_projects_locations_volumes_luns_get_execute(builder)
}

/// GET v2/{+parent}/luns
/// List storage volume luns for given storage volume.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_volumes_luns_list_execute()` to send, or `baremetalsolution_projects_locations_volumes_luns_list` for simplest API.

pub fn baremetalsolution_projects_locations_volumes_luns_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/luns", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/luns
/// List storage volume luns for given storage volume.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_volumes_luns_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_volumes_luns_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_luns_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLunsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLunsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/luns
/// List storage volume luns for given storage volume.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_volumes_luns_list_builder()` + `baremetalsolution_projects_locations_volumes_luns_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_luns_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLunsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_volumes_luns_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    baremetalsolution_projects_locations_volumes_luns_list_execute(builder)
}

/// GET v2/{+parent}/snapshots
/// Takes a snapshot of a boot volume. Returns INVALID_ARGUMENT if called for a non-boot volume.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_volumes_snapshots_create_execute()` to send, or `baremetalsolution_projects_locations_volumes_snapshots_create` for simplest API.

pub fn baremetalsolution_projects_locations_volumes_snapshots_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &VolumeSnapshot,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/snapshots", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/snapshots
/// Takes a snapshot of a boot volume. Returns INVALID_ARGUMENT if called for a non-boot volume.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_volumes_snapshots_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_volumes_snapshots_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_snapshots_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VolumeSnapshot>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VolumeSnapshot = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/snapshots
/// Takes a snapshot of a boot volume. Returns INVALID_ARGUMENT if called for a non-boot volume.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_volumes_snapshots_create_builder()` + `baremetalsolution_projects_locations_volumes_snapshots_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_snapshots_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &VolumeSnapshot,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VolumeSnapshot>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_volumes_snapshots_create_builder(
        client, parent, body,
    )?;
    baremetalsolution_projects_locations_volumes_snapshots_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes a volume snapshot. Returns INVALID_ARGUMENT if called for a non-boot volume.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_volumes_snapshots_delete_execute()` to send, or `baremetalsolution_projects_locations_volumes_snapshots_delete` for simplest API.

pub fn baremetalsolution_projects_locations_volumes_snapshots_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a volume snapshot. Returns INVALID_ARGUMENT if called for a non-boot volume.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_volumes_snapshots_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_volumes_snapshots_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_snapshots_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a volume snapshot. Returns INVALID_ARGUMENT if called for a non-boot volume.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_volumes_snapshots_delete_builder()` + `baremetalsolution_projects_locations_volumes_snapshots_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_snapshots_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        baremetalsolution_projects_locations_volumes_snapshots_delete_builder(client, name)?;
    baremetalsolution_projects_locations_volumes_snapshots_delete_execute(builder)
}

/// GET v2/{+name}
/// Returns the specified snapshot resource. Returns INVALID_ARGUMENT if called for a non-boot volume.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_volumes_snapshots_get_execute()` to send, or `baremetalsolution_projects_locations_volumes_snapshots_get` for simplest API.

pub fn baremetalsolution_projects_locations_volumes_snapshots_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Returns the specified snapshot resource. Returns INVALID_ARGUMENT if called for a non-boot volume.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_volumes_snapshots_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_volumes_snapshots_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_snapshots_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VolumeSnapshot>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: VolumeSnapshot = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Returns the specified snapshot resource. Returns INVALID_ARGUMENT if called for a non-boot volume.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_volumes_snapshots_get_builder()` + `baremetalsolution_projects_locations_volumes_snapshots_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_snapshots_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<VolumeSnapshot>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_volumes_snapshots_get_builder(client, name)?;
    baremetalsolution_projects_locations_volumes_snapshots_get_execute(builder)
}

/// GET v2/{+parent}/snapshots
/// Retrieves the list of snapshots for the specified volume. Returns a response with an empty list of snapshots if called for a non-boot volume.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_volumes_snapshots_list_execute()` to send, or `baremetalsolution_projects_locations_volumes_snapshots_list` for simplest API.

pub fn baremetalsolution_projects_locations_volumes_snapshots_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/snapshots", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/snapshots
/// Retrieves the list of snapshots for the specified volume. Returns a response with an empty list of snapshots if called for a non-boot volume.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_volumes_snapshots_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_volumes_snapshots_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_snapshots_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListVolumeSnapshotsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListVolumeSnapshotsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/snapshots
/// Retrieves the list of snapshots for the specified volume. Returns a response with an empty list of snapshots if called for a non-boot volume.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_volumes_snapshots_list_builder()` + `baremetalsolution_projects_locations_volumes_snapshots_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_snapshots_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListVolumeSnapshotsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = baremetalsolution_projects_locations_volumes_snapshots_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    baremetalsolution_projects_locations_volumes_snapshots_list_execute(builder)
}

/// GET v2/{+volumeSnapshot}:restoreVolumeSnapshot
/// Uses the specified snapshot to restore its parent volume. Returns INVALID_ARGUMENT if called for a non-boot volume.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `baremetalsolution_projects_locations_volumes_snapshots_restore_volume_snapshot_execute()` to send, or `baremetalsolution_projects_locations_volumes_snapshots_restore_volume_snapshot` for simplest API.

pub fn baremetalsolution_projects_locations_volumes_snapshots_restore_volume_snapshot_builder(
    client: &SimpleHttpClient,
    volumeSnapshot: &str,
    body: &RestoreVolumeSnapshotRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+volumeSnapshot}:restoreVolumeSnapshot",
        volumeSnapshot,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+volumeSnapshot}:restoreVolumeSnapshot
/// Uses the specified snapshot to restore its parent volume. Returns INVALID_ARGUMENT if called for a non-boot volume.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `baremetalsolution_projects_locations_volumes_snapshots_restore_volume_snapshot_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from baremetalsolution_projects_locations_volumes_snapshots_restore_volume_snapshot_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_snapshots_restore_volume_snapshot_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+volumeSnapshot}:restoreVolumeSnapshot
/// Uses the specified snapshot to restore its parent volume. Returns INVALID_ARGUMENT if called for a non-boot volume.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `baremetalsolution_projects_locations_volumes_snapshots_restore_volume_snapshot_builder()` + `baremetalsolution_projects_locations_volumes_snapshots_restore_volume_snapshot_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn baremetalsolution_projects_locations_volumes_snapshots_restore_volume_snapshot(
    client: &SimpleHttpClient,
    volumeSnapshot: &str,
    body: &RestoreVolumeSnapshotRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        baremetalsolution_projects_locations_volumes_snapshots_restore_volume_snapshot_builder(
            client,
            volumeSnapshot,
            body,
        )?;
    baremetalsolution_projects_locations_volumes_snapshots_restore_volume_snapshot_execute(builder)
}
