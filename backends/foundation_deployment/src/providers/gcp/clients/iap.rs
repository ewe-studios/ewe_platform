//! Auto-generated API clients for gcp/iap.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/iap`

#![cfg(feature = "gcp/iap")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/projects/{projectsId}/brands
/// Constructs a new OAuth brand for the project if one does not exist. The created brand is "internal only", meaning that OAuth clients created under it only accept requests from users who belong to the same Google Workspace organization as the project. The brand is created in an un-reviewed status. NOTE: The "internal only" status can be manually changed in the Google Cloud Console. Requires that a brand does not already exist for the project, and that the specified support email is owned by the caller.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iap_projects_brands_create_execute()` to send, or `iap_projects_brands_create` for simplest API.

pub fn iap_projects_brands_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Brand,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://iap.googleapis.com/v1/projects/{}/brands", parent,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/brands
/// Constructs a new OAuth brand for the project if one does not exist. The created brand is "internal only", meaning that OAuth clients created under it only accept requests from users who belong to the same Google Workspace organization as the project. The brand is created in an un-reviewed status. NOTE: The "internal only" status can be manually changed in the Google Cloud Console. Requires that a brand does not already exist for the project, and that the specified support email is owned by the caller.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `iap_projects_brands_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from iap_projects_brands_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_projects_brands_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Brand>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Brand = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/brands
/// Constructs a new OAuth brand for the project if one does not exist. The created brand is "internal only", meaning that OAuth clients created under it only accept requests from users who belong to the same Google Workspace organization as the project. The brand is created in an un-reviewed status. NOTE: The "internal only" status can be manually changed in the Google Cloud Console. Requires that a brand does not already exist for the project, and that the specified support email is owned by the caller.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iap_projects_brands_create_builder()` + `iap_projects_brands_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_projects_brands_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Brand,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Brand>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = iap_projects_brands_create_builder(client, parent, body)?;
    iap_projects_brands_create_execute(builder)
}

/// GET v1/projects/{projectsId}/brands/{brandsId}
/// Retrieves the OAuth brand of the project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iap_projects_brands_get_execute()` to send, or `iap_projects_brands_get` for simplest API.

pub fn iap_projects_brands_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://iap.googleapis.com/v1/projects/{}/brands/{}", name,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/brands/{brandsId}
/// Retrieves the OAuth brand of the project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `iap_projects_brands_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from iap_projects_brands_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_projects_brands_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Brand>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Brand = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/brands/{brandsId}
/// Retrieves the OAuth brand of the project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iap_projects_brands_get_builder()` + `iap_projects_brands_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_projects_brands_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Brand>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = iap_projects_brands_get_builder(client, name)?;
    iap_projects_brands_get_execute(builder)
}

/// GET v1/projects/{projectsId}/brands
/// Lists the existing brands for the project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iap_projects_brands_list_execute()` to send, or `iap_projects_brands_list` for simplest API.

pub fn iap_projects_brands_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://iap.googleapis.com/v1/projects/{}/brands", parent,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/brands
/// Lists the existing brands for the project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `iap_projects_brands_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from iap_projects_brands_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_projects_brands_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBrandsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBrandsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/brands
/// Lists the existing brands for the project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iap_projects_brands_list_builder()` + `iap_projects_brands_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_projects_brands_list(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBrandsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = iap_projects_brands_list_builder(client, parent)?;
    iap_projects_brands_list_execute(builder)
}

/// GET v1/projects/{projectsId}/brands/{brandsId}/identityAwareProxyClients
/// Creates an Identity Aware Proxy (IAP) OAuth client. The client is owned by IAP. Requires that the brand for the project exists and that it is set for internal-only use.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iap_projects_brands_identity_aware_proxy_clients_create_execute()` to send, or `iap_projects_brands_identity_aware_proxy_clients_create` for simplest API.

pub fn iap_projects_brands_identity_aware_proxy_clients_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &IdentityAwareProxyClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://iap.googleapis.com/v1/projects/{}/brands/{}/identityAwareProxyClients",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/brands/{brandsId}/identityAwareProxyClients
/// Creates an Identity Aware Proxy (IAP) OAuth client. The client is owned by IAP. Requires that the brand for the project exists and that it is set for internal-only use.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `iap_projects_brands_identity_aware_proxy_clients_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from iap_projects_brands_identity_aware_proxy_clients_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_projects_brands_identity_aware_proxy_clients_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IdentityAwareProxyClient>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IdentityAwareProxyClient = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/brands/{brandsId}/identityAwareProxyClients
/// Creates an Identity Aware Proxy (IAP) OAuth client. The client is owned by IAP. Requires that the brand for the project exists and that it is set for internal-only use.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iap_projects_brands_identity_aware_proxy_clients_create_builder()` + `iap_projects_brands_identity_aware_proxy_clients_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_projects_brands_identity_aware_proxy_clients_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &IdentityAwareProxyClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IdentityAwareProxyClient>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        iap_projects_brands_identity_aware_proxy_clients_create_builder(client, parent, body)?;
    iap_projects_brands_identity_aware_proxy_clients_create_execute(builder)
}

/// GET v1/projects/{projectsId}/brands/{brandsId}/identityAwareProxyClients/{identityAwareProxyClientsId}
/// Deletes an Identity Aware Proxy (IAP) OAuth client. Useful for removing obsolete clients, managing the number of clients in a given project, and cleaning up after tests. Requires that the client is owned by IAP.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iap_projects_brands_identity_aware_proxy_clients_delete_execute()` to send, or `iap_projects_brands_identity_aware_proxy_clients_delete` for simplest API.

pub fn iap_projects_brands_identity_aware_proxy_clients_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://iap.googleapis.com/v1/projects/{}/brands/{}/identityAwareProxyClients/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/brands/{brandsId}/identityAwareProxyClients/{identityAwareProxyClientsId}
/// Deletes an Identity Aware Proxy (IAP) OAuth client. Useful for removing obsolete clients, managing the number of clients in a given project, and cleaning up after tests. Requires that the client is owned by IAP.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `iap_projects_brands_identity_aware_proxy_clients_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from iap_projects_brands_identity_aware_proxy_clients_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_projects_brands_identity_aware_proxy_clients_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/brands/{brandsId}/identityAwareProxyClients/{identityAwareProxyClientsId}
/// Deletes an Identity Aware Proxy (IAP) OAuth client. Useful for removing obsolete clients, managing the number of clients in a given project, and cleaning up after tests. Requires that the client is owned by IAP.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iap_projects_brands_identity_aware_proxy_clients_delete_builder()` + `iap_projects_brands_identity_aware_proxy_clients_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_projects_brands_identity_aware_proxy_clients_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = iap_projects_brands_identity_aware_proxy_clients_delete_builder(client, name)?;
    iap_projects_brands_identity_aware_proxy_clients_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/brands/{brandsId}/identityAwareProxyClients/{identityAwareProxyClientsId}
/// Retrieves an Identity Aware Proxy (IAP) OAuth client. Requires that the client is owned by IAP.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iap_projects_brands_identity_aware_proxy_clients_get_execute()` to send, or `iap_projects_brands_identity_aware_proxy_clients_get` for simplest API.

pub fn iap_projects_brands_identity_aware_proxy_clients_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://iap.googleapis.com/v1/projects/{}/brands/{}/identityAwareProxyClients/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/brands/{brandsId}/identityAwareProxyClients/{identityAwareProxyClientsId}
/// Retrieves an Identity Aware Proxy (IAP) OAuth client. Requires that the client is owned by IAP.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `iap_projects_brands_identity_aware_proxy_clients_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from iap_projects_brands_identity_aware_proxy_clients_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_projects_brands_identity_aware_proxy_clients_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IdentityAwareProxyClient>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IdentityAwareProxyClient = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/brands/{brandsId}/identityAwareProxyClients/{identityAwareProxyClientsId}
/// Retrieves an Identity Aware Proxy (IAP) OAuth client. Requires that the client is owned by IAP.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iap_projects_brands_identity_aware_proxy_clients_get_builder()` + `iap_projects_brands_identity_aware_proxy_clients_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_projects_brands_identity_aware_proxy_clients_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IdentityAwareProxyClient>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = iap_projects_brands_identity_aware_proxy_clients_get_builder(client, name)?;
    iap_projects_brands_identity_aware_proxy_clients_get_execute(builder)
}

/// GET v1/projects/{projectsId}/brands/{brandsId}/identityAwareProxyClients
/// Lists the existing clients for the brand.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iap_projects_brands_identity_aware_proxy_clients_list_execute()` to send, or `iap_projects_brands_identity_aware_proxy_clients_list` for simplest API.

pub fn iap_projects_brands_identity_aware_proxy_clients_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://iap.googleapis.com/v1/projects/{}/brands/{}/identityAwareProxyClients",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/brands/{brandsId}/identityAwareProxyClients
/// Lists the existing clients for the brand.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `iap_projects_brands_identity_aware_proxy_clients_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from iap_projects_brands_identity_aware_proxy_clients_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_projects_brands_identity_aware_proxy_clients_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListIdentityAwareProxyClientsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListIdentityAwareProxyClientsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/brands/{brandsId}/identityAwareProxyClients
/// Lists the existing clients for the brand.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iap_projects_brands_identity_aware_proxy_clients_list_builder()` + `iap_projects_brands_identity_aware_proxy_clients_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_projects_brands_identity_aware_proxy_clients_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListIdentityAwareProxyClientsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = iap_projects_brands_identity_aware_proxy_clients_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    iap_projects_brands_identity_aware_proxy_clients_list_execute(builder)
}

/// GET v1/projects/{projectsId}/brands/{brandsId}/identityAwareProxyClients/{identityAwareProxyClientsId}:resetSecret
/// Resets an Identity Aware Proxy (IAP) OAuth client secret. Useful if the secret was compromised. Requires that the client is owned by IAP.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iap_projects_brands_identity_aware_proxy_clients_reset_secret_execute()` to send, or `iap_projects_brands_identity_aware_proxy_clients_reset_secret` for simplest API.

pub fn iap_projects_brands_identity_aware_proxy_clients_reset_secret_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &ResetIdentityAwareProxyClientSecretRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://iap.googleapis.com/v1/projects/{}/brands/{}/identityAwareProxyClients/{}:resetSecret",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/brands/{brandsId}/identityAwareProxyClients/{identityAwareProxyClientsId}:resetSecret
/// Resets an Identity Aware Proxy (IAP) OAuth client secret. Useful if the secret was compromised. Requires that the client is owned by IAP.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `iap_projects_brands_identity_aware_proxy_clients_reset_secret_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from iap_projects_brands_identity_aware_proxy_clients_reset_secret_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_projects_brands_identity_aware_proxy_clients_reset_secret_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IdentityAwareProxyClient>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IdentityAwareProxyClient = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/brands/{brandsId}/identityAwareProxyClients/{identityAwareProxyClientsId}:resetSecret
/// Resets an Identity Aware Proxy (IAP) OAuth client secret. Useful if the secret was compromised. Requires that the client is owned by IAP.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iap_projects_brands_identity_aware_proxy_clients_reset_secret_builder()` + `iap_projects_brands_identity_aware_proxy_clients_reset_secret_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_projects_brands_identity_aware_proxy_clients_reset_secret(
    client: &SimpleHttpClient,
    name: &str,
    body: &ResetIdentityAwareProxyClientSecretRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IdentityAwareProxyClient>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        iap_projects_brands_identity_aware_proxy_clients_reset_secret_builder(client, name, body)?;
    iap_projects_brands_identity_aware_proxy_clients_reset_secret_execute(builder)
}

/// GET v1/projects/{projectsId}/iap_tunnel/locations/{locationsId}/destGroups
/// Creates a new TunnelDestGroup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iap_projects_iap_tunnel_locations_dest_groups_create_execute()` to send, or `iap_projects_iap_tunnel_locations_dest_groups_create` for simplest API.

pub fn iap_projects_iap_tunnel_locations_dest_groups_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    tunnelDestGroupId: Option<&str>,
    body: &TunnelDestGroup,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://iap.googleapis.com/v1/projects/{}/iap_tunnel/locations/{}/destGroups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = tunnelDestGroupId {
        query_parts.push(format!("tunnelDestGroupId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/iap_tunnel/locations/{locationsId}/destGroups
/// Creates a new TunnelDestGroup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `iap_projects_iap_tunnel_locations_dest_groups_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from iap_projects_iap_tunnel_locations_dest_groups_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_projects_iap_tunnel_locations_dest_groups_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TunnelDestGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TunnelDestGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/iap_tunnel/locations/{locationsId}/destGroups
/// Creates a new TunnelDestGroup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iap_projects_iap_tunnel_locations_dest_groups_create_builder()` + `iap_projects_iap_tunnel_locations_dest_groups_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_projects_iap_tunnel_locations_dest_groups_create(
    client: &SimpleHttpClient,
    parent: &str,
    tunnelDestGroupId: Option<&str>,
    body: &TunnelDestGroup,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TunnelDestGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = iap_projects_iap_tunnel_locations_dest_groups_create_builder(
        client,
        parent,
        tunnelDestGroupId,
        body,
    )?;
    iap_projects_iap_tunnel_locations_dest_groups_create_execute(builder)
}

/// GET v1/projects/{projectsId}/iap_tunnel/locations/{locationsId}/destGroups/{destGroupsId}
/// Deletes a TunnelDestGroup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iap_projects_iap_tunnel_locations_dest_groups_delete_execute()` to send, or `iap_projects_iap_tunnel_locations_dest_groups_delete` for simplest API.

pub fn iap_projects_iap_tunnel_locations_dest_groups_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://iap.googleapis.com/v1/projects/{}/iap_tunnel/locations/{}/destGroups/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/iap_tunnel/locations/{locationsId}/destGroups/{destGroupsId}
/// Deletes a TunnelDestGroup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `iap_projects_iap_tunnel_locations_dest_groups_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from iap_projects_iap_tunnel_locations_dest_groups_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_projects_iap_tunnel_locations_dest_groups_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/iap_tunnel/locations/{locationsId}/destGroups/{destGroupsId}
/// Deletes a TunnelDestGroup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iap_projects_iap_tunnel_locations_dest_groups_delete_builder()` + `iap_projects_iap_tunnel_locations_dest_groups_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_projects_iap_tunnel_locations_dest_groups_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = iap_projects_iap_tunnel_locations_dest_groups_delete_builder(client, name)?;
    iap_projects_iap_tunnel_locations_dest_groups_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/iap_tunnel/locations/{locationsId}/destGroups/{destGroupsId}
/// Retrieves an existing TunnelDestGroup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iap_projects_iap_tunnel_locations_dest_groups_get_execute()` to send, or `iap_projects_iap_tunnel_locations_dest_groups_get` for simplest API.

pub fn iap_projects_iap_tunnel_locations_dest_groups_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://iap.googleapis.com/v1/projects/{}/iap_tunnel/locations/{}/destGroups/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/iap_tunnel/locations/{locationsId}/destGroups/{destGroupsId}
/// Retrieves an existing TunnelDestGroup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `iap_projects_iap_tunnel_locations_dest_groups_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from iap_projects_iap_tunnel_locations_dest_groups_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_projects_iap_tunnel_locations_dest_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TunnelDestGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TunnelDestGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/iap_tunnel/locations/{locationsId}/destGroups/{destGroupsId}
/// Retrieves an existing TunnelDestGroup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iap_projects_iap_tunnel_locations_dest_groups_get_builder()` + `iap_projects_iap_tunnel_locations_dest_groups_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_projects_iap_tunnel_locations_dest_groups_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TunnelDestGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = iap_projects_iap_tunnel_locations_dest_groups_get_builder(client, name)?;
    iap_projects_iap_tunnel_locations_dest_groups_get_execute(builder)
}

/// GET v1/projects/{projectsId}/iap_tunnel/locations/{locationsId}/destGroups
/// Lists the existing TunnelDestGroups. To group across all locations, use a - as the location ID. For example: /v1/`projects/123/iap_tunnel/locations/-/`destGroups``
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iap_projects_iap_tunnel_locations_dest_groups_list_execute()` to send, or `iap_projects_iap_tunnel_locations_dest_groups_list` for simplest API.

pub fn iap_projects_iap_tunnel_locations_dest_groups_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://iap.googleapis.com/v1/projects/{}/iap_tunnel/locations/{}/destGroups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/iap_tunnel/locations/{locationsId}/destGroups
/// Lists the existing TunnelDestGroups. To group across all locations, use a - as the location ID. For example: /v1/`projects/123/iap_tunnel/locations/-/`destGroups``
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `iap_projects_iap_tunnel_locations_dest_groups_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from iap_projects_iap_tunnel_locations_dest_groups_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_projects_iap_tunnel_locations_dest_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListTunnelDestGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTunnelDestGroupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/iap_tunnel/locations/{locationsId}/destGroups
/// Lists the existing TunnelDestGroups. To group across all locations, use a - as the location ID. For example: /v1/`projects/123/iap_tunnel/locations/-/`destGroups``
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iap_projects_iap_tunnel_locations_dest_groups_list_builder()` + `iap_projects_iap_tunnel_locations_dest_groups_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_projects_iap_tunnel_locations_dest_groups_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListTunnelDestGroupsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = iap_projects_iap_tunnel_locations_dest_groups_list_builder(
        client, parent, pageSize, pageToken,
    )?;
    iap_projects_iap_tunnel_locations_dest_groups_list_execute(builder)
}

/// GET v1/projects/{projectsId}/iap_tunnel/locations/{locationsId}/destGroups/{destGroupsId}
/// Updates a TunnelDestGroup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iap_projects_iap_tunnel_locations_dest_groups_patch_execute()` to send, or `iap_projects_iap_tunnel_locations_dest_groups_patch` for simplest API.

pub fn iap_projects_iap_tunnel_locations_dest_groups_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &TunnelDestGroup,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://iap.googleapis.com/v1/projects/{}/iap_tunnel/locations/{}/destGroups/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/iap_tunnel/locations/{locationsId}/destGroups/{destGroupsId}
/// Updates a TunnelDestGroup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `iap_projects_iap_tunnel_locations_dest_groups_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from iap_projects_iap_tunnel_locations_dest_groups_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_projects_iap_tunnel_locations_dest_groups_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TunnelDestGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TunnelDestGroup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/iap_tunnel/locations/{locationsId}/destGroups/{destGroupsId}
/// Updates a TunnelDestGroup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iap_projects_iap_tunnel_locations_dest_groups_patch_builder()` + `iap_projects_iap_tunnel_locations_dest_groups_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_projects_iap_tunnel_locations_dest_groups_patch(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &TunnelDestGroup,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TunnelDestGroup>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = iap_projects_iap_tunnel_locations_dest_groups_patch_builder(
        client, name, updateMask, body,
    )?;
    iap_projects_iap_tunnel_locations_dest_groups_patch_execute(builder)
}

/// GET v1/{v1Id}:getIamPolicy
/// Gets the access control policy for an Identity-Aware Proxy protected resource. More information about managing access via IAP can be found at: <https://cloud.google.`com/iap/docs/managing-access`#managing_access_via_the_api>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iap_get_iam_policy_execute()` to send, or `iap_get_iam_policy` for simplest API.

pub fn iap_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://iap.googleapis.com/v1/{}:getIamPolicy", resource,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{v1Id}:getIamPolicy
/// Gets the access control policy for an Identity-Aware Proxy protected resource. More information about managing access via IAP can be found at: <https://cloud.google.`com/iap/docs/managing-access`#managing_access_via_the_api>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `iap_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from iap_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{v1Id}:getIamPolicy
/// Gets the access control policy for an Identity-Aware Proxy protected resource. More information about managing access via IAP can be found at: <https://cloud.google.`com/iap/docs/managing-access`#managing_access_via_the_api>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iap_get_iam_policy_builder()` + `iap_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_get_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = iap_get_iam_policy_builder(client, resource, body)?;
    iap_get_iam_policy_execute(builder)
}

/// GET v1/{v1Id}:iapSettings
/// Gets the IAP settings on a particular IAP protected resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iap_get_iap_settings_execute()` to send, or `iap_get_iap_settings` for simplest API.

pub fn iap_get_iap_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://iap.googleapis.com/v1/{}:iapSettings", name,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/{v1Id}:iapSettings
/// Gets the IAP settings on a particular IAP protected resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `iap_get_iap_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from iap_get_iap_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_get_iap_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IapSettings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IapSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{v1Id}:iapSettings
/// Gets the IAP settings on a particular IAP protected resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iap_get_iap_settings_builder()` + `iap_get_iap_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_get_iap_settings(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IapSettings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = iap_get_iap_settings_builder(client, name)?;
    iap_get_iap_settings_execute(builder)
}

/// GET v1/{v1Id}:setIamPolicy
/// Sets the access control policy for an Identity-Aware Proxy protected resource. Replaces any existing policy. More information about managing access via IAP can be found at: <https://cloud.google.`com/iap/docs/managing-access`#managing_access_via_the_api>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iap_set_iam_policy_execute()` to send, or `iap_set_iam_policy` for simplest API.

pub fn iap_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://iap.googleapis.com/v1/{}:setIamPolicy", resource,);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{v1Id}:setIamPolicy
/// Sets the access control policy for an Identity-Aware Proxy protected resource. Replaces any existing policy. More information about managing access via IAP can be found at: <https://cloud.google.`com/iap/docs/managing-access`#managing_access_via_the_api>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `iap_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from iap_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{v1Id}:setIamPolicy
/// Sets the access control policy for an Identity-Aware Proxy protected resource. Replaces any existing policy. More information about managing access via IAP can be found at: <https://cloud.google.`com/iap/docs/managing-access`#managing_access_via_the_api>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iap_set_iam_policy_builder()` + `iap_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_set_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = iap_set_iam_policy_builder(client, resource, body)?;
    iap_set_iam_policy_execute(builder)
}

/// GET v1/{v1Id}:testIamPermissions
/// Returns permissions that a caller has on the Identity-Aware Proxy protected resource. More information about managing access via IAP can be found at: <https://cloud.google.`com/iap/docs/managing-access`#managing_access_via_the_api>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iap_test_iam_permissions_execute()` to send, or `iap_test_iam_permissions` for simplest API.

pub fn iap_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://iap.googleapis.com/v1/{}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{v1Id}:testIamPermissions
/// Returns permissions that a caller has on the Identity-Aware Proxy protected resource. More information about managing access via IAP can be found at: <https://cloud.google.`com/iap/docs/managing-access`#managing_access_via_the_api>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `iap_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from iap_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{v1Id}:testIamPermissions
/// Returns permissions that a caller has on the Identity-Aware Proxy protected resource. More information about managing access via IAP can be found at: <https://cloud.google.`com/iap/docs/managing-access`#managing_access_via_the_api>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iap_test_iam_permissions_builder()` + `iap_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_test_iam_permissions(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = iap_test_iam_permissions_builder(client, resource, body)?;
    iap_test_iam_permissions_execute(builder)
}

/// GET v1/{v1Id}:iapSettings
/// Updates the IAP settings on a particular IAP protected resource. It replaces all fields unless the update_mask is set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iap_update_iap_settings_execute()` to send, or `iap_update_iap_settings` for simplest API.

pub fn iap_update_iap_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &IapSettings,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://iap.googleapis.com/v1/{}:iapSettings", name,);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{v1Id}:iapSettings
/// Updates the IAP settings on a particular IAP protected resource. It replaces all fields unless the update_mask is set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `iap_update_iap_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from iap_update_iap_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_update_iap_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IapSettings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IapSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{v1Id}:iapSettings
/// Updates the IAP settings on a particular IAP protected resource. It replaces all fields unless the update_mask is set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iap_update_iap_settings_builder()` + `iap_update_iap_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_update_iap_settings(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &IapSettings,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IapSettings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = iap_update_iap_settings_builder(client, name, updateMask, body)?;
    iap_update_iap_settings_execute(builder)
}

/// GET v1/{v1Id}:validateAttributeExpression
/// Validates that a given CEL expression conforms to IAP restrictions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iap_validate_attribute_expression_execute()` to send, or `iap_validate_attribute_expression` for simplest API.

pub fn iap_validate_attribute_expression_builder(
    client: &SimpleHttpClient,
    name: &str,
    expression: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://iap.googleapis.com/v1/{}:validateAttributeExpression",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expression {
        query_parts.push(format!("expression={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/{v1Id}:validateAttributeExpression
/// Validates that a given CEL expression conforms to IAP restrictions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `iap_validate_attribute_expression_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from iap_validate_attribute_expression_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_validate_attribute_expression_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ValidateIapAttributeExpressionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ValidateIapAttributeExpressionResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{v1Id}:validateAttributeExpression
/// Validates that a given CEL expression conforms to IAP restrictions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iap_validate_attribute_expression_builder()` + `iap_validate_attribute_expression_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_validate_attribute_expression(
    client: &SimpleHttpClient,
    name: &str,
    expression: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ValidateIapAttributeExpressionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = iap_validate_attribute_expression_builder(client, name, expression)?;
    iap_validate_attribute_expression_execute(builder)
}
