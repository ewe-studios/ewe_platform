//! Auto-generated API clients for gcp/iap.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/projects/{projectsId}/brands
/// Constructs a new OAuth brand for the project if one does not exist. The created brand is "internal only", meaning that OAuth clients created under it only accept requests from users who belong to the same Google Workspace organization as the project. The brand is created in an un-reviewed status. NOTE: The "internal only" status can be manually changed in the Google Cloud Console. Requires that a brand does not already exist for the project, and that the specified support email is owned by the caller.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iap_projects_brands_create_execute()` to send, or `iap_projects_brands_create` for simplest API.

pub fn iap_projects_brands_create_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &Brand,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://iap.googleapis.com/v1/projects/{}/brands",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/brands
/// Constructs a new OAuth brand for the project if one does not exist. The created brand is "internal only", meaning that OAuth clients created under it only accept requests from users who belong to the same Google Workspace organization as the project. The brand is created in an un-reviewed status. NOTE: The "internal only" status can be manually changed in the Google Cloud Console. Requires that a brand does not already exist for the project, and that the specified support email is owned by the caller.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `iap_projects_brands_create_execute()` or `iap_projects_brands_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iap_projects_brands_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_projects_brands_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Brand>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Brand = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/brands
/// Constructs a new OAuth brand for the project if one does not exist. The created brand is "internal only", meaning that OAuth clients created under it only accept requests from users who belong to the same Google Workspace organization as the project. The brand is created in an un-reviewed status. NOTE: The "internal only" status can be manually changed in the Google Cloud Console. Requires that a brand does not already exist for the project, and that the specified support email is owned by the caller.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `iap_projects_brands_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `iap_projects_brands_create_task()`.
/// For the simplest API, use `iap_projects_brands_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iap_projects_brands_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn iap_projects_brands_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Brand>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = iap_projects_brands_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`iap_projects_brands_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IapProjectsBrandsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: Brand,
}

/// GET v1/projects/{projectsId}/brands
/// Constructs a new OAuth brand for the project if one does not exist. The created brand is "internal only", meaning that OAuth clients created under it only accept requests from users who belong to the same Google Workspace organization as the project. The brand is created in an un-reviewed status. NOTE: The "internal only" status can be manually changed in the Google Cloud Console. Requires that a brand does not already exist for the project, and that the specified support email is owned by the caller.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iap_projects_brands_create_builder()` + `iap_projects_brands_create_execute()`.
/// For task-level control, use `iap_projects_brands_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_projects_brands_create(
    client: &SimpleHttpClient,
    args: &IapProjectsBrandsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Brand>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = iap_projects_brands_create_builder(client, &args.parent, &args.body)?;
    iap_projects_brands_create_execute(builder)
}

/// GET v1/{v1Id}:getIamPolicy
/// Gets the access control policy for an Identity-Aware Proxy protected resource. More information about managing access via IAP can be found at: <https://cloud.google.`com/iap/docs/managing-access`#managing_access_via_the_api>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iap_get_iam_policy_execute()` to send, or `iap_get_iam_policy` for simplest API.

pub fn iap_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &String,
    body: &GetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://iap.googleapis.com/v1/{}:getIamPolicy",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{v1Id}:getIamPolicy
/// Gets the access control policy for an Identity-Aware Proxy protected resource. More information about managing access via IAP can be found at: <https://cloud.google.`com/iap/docs/managing-access`#managing_access_via_the_api>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `iap_get_iam_policy_execute()` or `iap_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iap_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}:getIamPolicy
/// Gets the access control policy for an Identity-Aware Proxy protected resource. More information about managing access via IAP can be found at: <https://cloud.google.`com/iap/docs/managing-access`#managing_access_via_the_api>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `iap_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `iap_get_iam_policy_task()`.
/// For the simplest API, use `iap_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iap_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn iap_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = iap_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`iap_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IapGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: GetIamPolicyRequest,
}

/// GET v1/{v1Id}:getIamPolicy
/// Gets the access control policy for an Identity-Aware Proxy protected resource. More information about managing access via IAP can be found at: <https://cloud.google.`com/iap/docs/managing-access`#managing_access_via_the_api>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iap_get_iam_policy_builder()` + `iap_get_iam_policy_execute()`.
/// For task-level control, use `iap_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_get_iam_policy(
    client: &SimpleHttpClient,
    args: &IapGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = iap_get_iam_policy_builder(client, &args.resource, &args.body)?;
    iap_get_iam_policy_execute(builder)
}

/// GET v1/{v1Id}:iapSettings
/// Gets the IAP settings on a particular IAP protected resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iap_get_iap_settings_execute()` to send, or `iap_get_iap_settings` for simplest API.

pub fn iap_get_iap_settings_builder(
    client: &SimpleHttpClient,
    name: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://iap.googleapis.com/v1/{}:iapSettings",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/{v1Id}:iapSettings
/// Gets the IAP settings on a particular IAP protected resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `iap_get_iap_settings_execute()` or `iap_get_iap_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iap_get_iap_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_get_iap_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<IapSettings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: IapSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}:iapSettings
/// Gets the IAP settings on a particular IAP protected resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `iap_get_iap_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `iap_get_iap_settings_task()`.
/// For the simplest API, use `iap_get_iap_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iap_get_iap_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn iap_get_iap_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IapSettings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = iap_get_iap_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`iap_get_iap_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IapGetIapSettingsArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/{v1Id}:iapSettings
/// Gets the IAP settings on a particular IAP protected resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iap_get_iap_settings_builder()` + `iap_get_iap_settings_execute()`.
/// For task-level control, use `iap_get_iap_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_get_iap_settings(
    client: &SimpleHttpClient,
    args: &IapGetIapSettingsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<IapSettings>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = iap_get_iap_settings_builder(client, &args.name)?;
    iap_get_iap_settings_execute(builder)
}

/// GET v1/{v1Id}:setIamPolicy
/// Sets the access control policy for an Identity-Aware Proxy protected resource. Replaces any existing policy. More information about managing access via IAP can be found at: <https://cloud.google.`com/iap/docs/managing-access`#managing_access_via_the_api>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iap_set_iam_policy_execute()` to send, or `iap_set_iam_policy` for simplest API.

pub fn iap_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &String,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://iap.googleapis.com/v1/{}:setIamPolicy",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{v1Id}:setIamPolicy
/// Sets the access control policy for an Identity-Aware Proxy protected resource. Replaces any existing policy. More information about managing access via IAP can be found at: <https://cloud.google.`com/iap/docs/managing-access`#managing_access_via_the_api>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `iap_set_iam_policy_execute()` or `iap_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iap_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}:setIamPolicy
/// Sets the access control policy for an Identity-Aware Proxy protected resource. Replaces any existing policy. More information about managing access via IAP can be found at: <https://cloud.google.`com/iap/docs/managing-access`#managing_access_via_the_api>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `iap_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `iap_set_iam_policy_task()`.
/// For the simplest API, use `iap_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iap_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn iap_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = iap_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`iap_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IapSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: SetIamPolicyRequest,
}

/// GET v1/{v1Id}:setIamPolicy
/// Sets the access control policy for an Identity-Aware Proxy protected resource. Replaces any existing policy. More information about managing access via IAP can be found at: <https://cloud.google.`com/iap/docs/managing-access`#managing_access_via_the_api>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iap_set_iam_policy_builder()` + `iap_set_iam_policy_execute()`.
/// For task-level control, use `iap_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_set_iam_policy(
    client: &SimpleHttpClient,
    args: &IapSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = iap_set_iam_policy_builder(client, &args.resource, &args.body)?;
    iap_set_iam_policy_execute(builder)
}

/// GET v1/{v1Id}:testIamPermissions
/// Returns permissions that a caller has on the Identity-Aware Proxy protected resource. More information about managing access via IAP can be found at: <https://cloud.google.`com/iap/docs/managing-access`#managing_access_via_the_api>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iap_test_iam_permissions_execute()` to send, or `iap_test_iam_permissions` for simplest API.

pub fn iap_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &String,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://iap.googleapis.com/v1/{}:testIamPermissions",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{v1Id}:testIamPermissions
/// Returns permissions that a caller has on the Identity-Aware Proxy protected resource. More information about managing access via IAP can be found at: <https://cloud.google.`com/iap/docs/managing-access`#managing_access_via_the_api>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `iap_test_iam_permissions_execute()` or `iap_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iap_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}:testIamPermissions
/// Returns permissions that a caller has on the Identity-Aware Proxy protected resource. More information about managing access via IAP can be found at: <https://cloud.google.`com/iap/docs/managing-access`#managing_access_via_the_api>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `iap_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `iap_test_iam_permissions_task()`.
/// For the simplest API, use `iap_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iap_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn iap_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = iap_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`iap_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IapTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Request body.
    pub body: TestIamPermissionsRequest,
}

/// GET v1/{v1Id}:testIamPermissions
/// Returns permissions that a caller has on the Identity-Aware Proxy protected resource. More information about managing access via IAP can be found at: <https://cloud.google.`com/iap/docs/managing-access`#managing_access_via_the_api>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iap_test_iam_permissions_builder()` + `iap_test_iam_permissions_execute()`.
/// For task-level control, use `iap_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &IapTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = iap_test_iam_permissions_builder(client, &args.resource, &args.body)?;
    iap_test_iam_permissions_execute(builder)
}

/// GET v1/{v1Id}:validateAttributeExpression
/// Validates that a given CEL expression conforms to IAP restrictions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `iap_validate_attribute_expression_execute()` to send, or `iap_validate_attribute_expression` for simplest API.

pub fn iap_validate_attribute_expression_builder(
    client: &SimpleHttpClient,
    name: &String,
    expression: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://iap.googleapis.com/v1/{}:validateAttributeExpression",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = expression.as_ref() {
        query_parts.push(format!("expression={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/{v1Id}:validateAttributeExpression
/// Validates that a given CEL expression conforms to IAP restrictions.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `iap_validate_attribute_expression_execute()` or `iap_validate_attribute_expression`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iap_validate_attribute_expression_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_validate_attribute_expression_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ValidateIapAttributeExpressionResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ValidateIapAttributeExpressionResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}:validateAttributeExpression
/// Validates that a given CEL expression conforms to IAP restrictions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `iap_validate_attribute_expression_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `iap_validate_attribute_expression_task()`.
/// For the simplest API, use `iap_validate_attribute_expression()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `iap_validate_attribute_expression_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn iap_validate_attribute_expression_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ValidateIapAttributeExpressionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = iap_validate_attribute_expression_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`iap_validate_attribute_expression`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct IapValidateAttributeExpressionArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: expression
    pub expression: Option<String>,
}

/// GET v1/{v1Id}:validateAttributeExpression
/// Validates that a given CEL expression conforms to IAP restrictions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `iap_validate_attribute_expression_builder()` + `iap_validate_attribute_expression_execute()`.
/// For task-level control, use `iap_validate_attribute_expression_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn iap_validate_attribute_expression(
    client: &SimpleHttpClient,
    args: &IapValidateAttributeExpressionArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ValidateIapAttributeExpressionResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = iap_validate_attribute_expression_builder(client, &args.name, &args.expression)?;
    iap_validate_attribute_expression_execute(builder)
}
