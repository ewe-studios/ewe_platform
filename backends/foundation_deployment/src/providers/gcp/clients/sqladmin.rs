//! Auto-generated API clients for gcp/sqladmin.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/sqladmin`

#![cfg(feature = "gcp/sqladmin")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/projects/{projectsId}/backups
/// Creates a backup for a Cloud SQL instance. This API can be used only to create on-demand backups.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql__backups__create_backup_execute()` to send, or `sql__backups__create_backup` for simplest API.

pub fn sql__backups__create_backup_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Backup,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/backups",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/backups
/// Creates a backup for a Cloud SQL instance. This API can be used only to create on-demand backups.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql__backups__create_backup_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql__backups__create_backup_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql__backups__create_backup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql__backups__create_backup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlBackupsCreateBackupArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: Backup,
}

/// GET v1/projects/{projectsId}/backups
/// Creates a backup for a Cloud SQL instance. This API can be used only to create on-demand backups.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql__backups__create_backup_builder()` + `sql__backups__create_backup_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql__backups__create_backup(
    client: &SimpleHttpClient,
    args: &SqlBackupsCreateBackupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql__backups__create_backup_builder(client, &args.parent, &args.body)?;
    sql__backups__create_backup_execute(builder)
}

/// GET v1/projects/{projectsId}/backups/{backupsId}
/// Deletes the backup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql__backups__delete_backup_execute()` to send, or `sql__backups__delete_backup` for simplest API.

pub fn sql__backups__delete_backup_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/backups/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/backups/{backupsId}
/// Deletes the backup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql__backups__delete_backup_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql__backups__delete_backup_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql__backups__delete_backup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql__backups__delete_backup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlBackupsDeleteBackupArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/backups/{backupsId}
/// Deletes the backup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql__backups__delete_backup_builder()` + `sql__backups__delete_backup_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql__backups__delete_backup(
    client: &SimpleHttpClient,
    args: &SqlBackupsDeleteBackupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql__backups__delete_backup_builder(client, &args.name)?;
    sql__backups__delete_backup_execute(builder)
}

/// GET v1/projects/{projectsId}/backups/{backupsId}
/// Retrieves a resource containing information about a backup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql__backups__get_backup_execute()` to send, or `sql__backups__get_backup` for simplest API.

pub fn sql__backups__get_backup_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/backups/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/backups/{backupsId}
/// Retrieves a resource containing information about a backup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql__backups__get_backup_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql__backups__get_backup_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql__backups__get_backup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Backup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Backup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql__backups__get_backup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlBackupsGetBackupArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/backups/{backupsId}
/// Retrieves a resource containing information about a backup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql__backups__get_backup_builder()` + `sql__backups__get_backup_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql__backups__get_backup(
    client: &SimpleHttpClient,
    args: &SqlBackupsGetBackupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Backup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql__backups__get_backup_builder(client, &args.name)?;
    sql__backups__get_backup_execute(builder)
}

/// GET v1/projects/{projectsId}/backups
/// Lists all backups associated with the project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql__backups__list_backups_execute()` to send, or `sql__backups__list_backups` for simplest API.

pub fn sql__backups__list_backups_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/backups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/backups
/// Lists all backups associated with the project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql__backups__list_backups_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql__backups__list_backups_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql__backups__list_backups_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBackupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBackupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql__backups__list_backups`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlBackupsListBackupsArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/backups
/// Lists all backups associated with the project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql__backups__list_backups_builder()` + `sql__backups__list_backups_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql__backups__list_backups(
    client: &SimpleHttpClient,
    args: &SqlBackupsListBackupsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBackupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sql__backups__list_backups_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
    )?;
    sql__backups__list_backups_execute(builder)
}

/// GET v1/projects/{projectsId}/backups/{backupsId}
/// Updates the retention period and description of the backup. You can use this API to update final backups only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql__backups__update_backup_execute()` to send, or `sql__backups__update_backup` for simplest API.

pub fn sql__backups__update_backup_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Backup,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/backups/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/backups/{backupsId}
/// Updates the retention period and description of the backup. You can use this API to update final backups only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql__backups__update_backup_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql__backups__update_backup_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql__backups__update_backup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql__backups__update_backup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlBackupsUpdateBackupArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: Backup,
}

/// GET v1/projects/{projectsId}/backups/{backupsId}
/// Updates the retention period and description of the backup. You can use this API to update final backups only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql__backups__update_backup_builder()` + `sql__backups__update_backup_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql__backups__update_backup(
    client: &SimpleHttpClient,
    args: &SqlBackupsUpdateBackupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql__backups__update_backup_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    sql__backups__update_backup_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/backupRuns/{id}
/// Deletes the backup taken by a backup run.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_backup_runs_delete_execute()` to send, or `sql_backup_runs_delete` for simplest API.

pub fn sql_backup_runs_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/backupRuns/{}",
        project, instance, id,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/backupRuns/{id}
/// Deletes the backup taken by a backup run.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_backup_runs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_backup_runs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_backup_runs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_backup_runs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlBackupRunsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Path parameter: id
    pub id: String,
}

/// GET v1/projects/{project}/instances/{instance}/backupRuns/{id}
/// Deletes the backup taken by a backup run.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_backup_runs_delete_builder()` + `sql_backup_runs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_backup_runs_delete(
    client: &SimpleHttpClient,
    args: &SqlBackupRunsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_backup_runs_delete_builder(client, &args.project, &args.instance, &args.id)?;
    sql_backup_runs_delete_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/backupRuns/{id}
/// Retrieves a resource containing information about a backup run.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_backup_runs_get_execute()` to send, or `sql_backup_runs_get` for simplest API.

pub fn sql_backup_runs_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    id: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/backupRuns/{}",
        project, instance, id,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/backupRuns/{id}
/// Retrieves a resource containing information about a backup run.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_backup_runs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_backup_runs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_backup_runs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackupRun>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BackupRun = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_backup_runs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlBackupRunsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Path parameter: id
    pub id: String,
}

/// GET v1/projects/{project}/instances/{instance}/backupRuns/{id}
/// Retrieves a resource containing information about a backup run.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_backup_runs_get_builder()` + `sql_backup_runs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_backup_runs_get(
    client: &SimpleHttpClient,
    args: &SqlBackupRunsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackupRun>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_backup_runs_get_builder(client, &args.project, &args.instance, &args.id)?;
    sql_backup_runs_get_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/backupRuns
/// Creates a new backup run on demand.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_backup_runs_insert_execute()` to send, or `sql_backup_runs_insert` for simplest API.

pub fn sql_backup_runs_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    body: &BackupRun,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/backupRuns",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{project}/instances/{instance}/backupRuns
/// Creates a new backup run on demand.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_backup_runs_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_backup_runs_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_backup_runs_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_backup_runs_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlBackupRunsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Request body.
    pub body: BackupRun,
}

/// GET v1/projects/{project}/instances/{instance}/backupRuns
/// Creates a new backup run on demand.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_backup_runs_insert_builder()` + `sql_backup_runs_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_backup_runs_insert(
    client: &SimpleHttpClient,
    args: &SqlBackupRunsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        sql_backup_runs_insert_builder(client, &args.project, &args.instance, &args.body)?;
    sql_backup_runs_insert_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/backupRuns
/// Lists all backup runs associated with the project or a given instance and configuration in the reverse chronological order of the backup initiation time.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_backup_runs_list_execute()` to send, or `sql_backup_runs_list` for simplest API.

pub fn sql_backup_runs_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/backupRuns",
        project, instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/backupRuns
/// Lists all backup runs associated with the project or a given instance and configuration in the reverse chronological order of the backup initiation time.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_backup_runs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_backup_runs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_backup_runs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackupRunsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BackupRunsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_backup_runs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlBackupRunsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{project}/instances/{instance}/backupRuns
/// Lists all backup runs associated with the project or a given instance and configuration in the reverse chronological order of the backup initiation time.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_backup_runs_list_builder()` + `sql_backup_runs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_backup_runs_list(
    client: &SimpleHttpClient,
    args: &SqlBackupRunsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackupRunsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sql_backup_runs_list_builder(
        client,
        &args.project,
        &args.instance,
        args.maxResults,
        args.pageToken.as_deref(),
    )?;
    sql_backup_runs_list_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}:generateEphemeralCert
/// Generates a short-lived X509 certificate containing the provided public key and signed by a private key specific to the target instance. Users may use the certificate to authenticate as themselves when connecting to the database.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_connect_generate_ephemeral_execute()` to send, or `sql_connect_generate_ephemeral` for simplest API.

pub fn sql_connect_generate_ephemeral_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    body: &GenerateEphemeralCertRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}:generateEphemeralCert",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{project}/instances/{instance}:generateEphemeralCert
/// Generates a short-lived X509 certificate containing the provided public key and signed by a private key specific to the target instance. Users may use the certificate to authenticate as themselves when connecting to the database.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_connect_generate_ephemeral_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_connect_generate_ephemeral_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_connect_generate_ephemeral_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateEphemeralCertResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GenerateEphemeralCertResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_connect_generate_ephemeral`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlConnectGenerateEphemeralArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Request body.
    pub body: GenerateEphemeralCertRequest,
}

/// GET v1/projects/{project}/instances/{instance}:generateEphemeralCert
/// Generates a short-lived X509 certificate containing the provided public key and signed by a private key specific to the target instance. Users may use the certificate to authenticate as themselves when connecting to the database.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_connect_generate_ephemeral_builder()` + `sql_connect_generate_ephemeral_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_connect_generate_ephemeral(
    client: &SimpleHttpClient,
    args: &SqlConnectGenerateEphemeralArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateEphemeralCertResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        sql_connect_generate_ephemeral_builder(client, &args.project, &args.instance, &args.body)?;
    sql_connect_generate_ephemeral_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/connectSettings
/// Retrieves connect settings about a Cloud SQL instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_connect_get_execute()` to send, or `sql_connect_get` for simplest API.

pub fn sql_connect_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    readTime: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/connectSettings",
        project, instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = readTime {
        query_parts.push(format!("readTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/connectSettings
/// Retrieves connect settings about a Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_connect_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_connect_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_connect_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConnectSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ConnectSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_connect_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlConnectGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: readTime
    pub readTime: Option<String>,
}

/// GET v1/projects/{project}/instances/{instance}/connectSettings
/// Retrieves connect settings about a Cloud SQL instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_connect_get_builder()` + `sql_connect_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_connect_get(
    client: &SimpleHttpClient,
    args: &SqlConnectGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConnectSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sql_connect_get_builder(
        client,
        &args.project,
        &args.instance,
        args.readTime.as_deref(),
    )?;
    sql_connect_get_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/databases/{database}
/// Deletes a database from a Cloud SQL instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_databases_delete_execute()` to send, or `sql_databases_delete` for simplest API.

pub fn sql_databases_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    database: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/databases/{}",
        project, instance, database,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/databases/{database}
/// Deletes a database from a Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_databases_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_databases_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_databases_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_databases_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlDatabasesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Path parameter: database
    pub database: String,
}

/// GET v1/projects/{project}/instances/{instance}/databases/{database}
/// Deletes a database from a Cloud SQL instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_databases_delete_builder()` + `sql_databases_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_databases_delete(
    client: &SimpleHttpClient,
    args: &SqlDatabasesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        sql_databases_delete_builder(client, &args.project, &args.instance, &args.database)?;
    sql_databases_delete_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/databases/{database}
/// Retrieves a resource containing information about a database inside a Cloud SQL instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_databases_get_execute()` to send, or `sql_databases_get` for simplest API.

pub fn sql_databases_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    database: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/databases/{}",
        project, instance, database,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/databases/{database}
/// Retrieves a resource containing information about a database inside a Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_databases_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_databases_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_databases_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Database>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Database = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_databases_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlDatabasesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Path parameter: database
    pub database: String,
}

/// GET v1/projects/{project}/instances/{instance}/databases/{database}
/// Retrieves a resource containing information about a database inside a Cloud SQL instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_databases_get_builder()` + `sql_databases_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_databases_get(
    client: &SimpleHttpClient,
    args: &SqlDatabasesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Database>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_databases_get_builder(client, &args.project, &args.instance, &args.database)?;
    sql_databases_get_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/databases
/// Inserts a resource containing information about a database inside a Cloud SQL instance. **Note:** You can't modify the default character set and collation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_databases_insert_execute()` to send, or `sql_databases_insert` for simplest API.

pub fn sql_databases_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    body: &Database,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/databases",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{project}/instances/{instance}/databases
/// Inserts a resource containing information about a database inside a Cloud SQL instance. **Note:** You can't modify the default character set and collation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_databases_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_databases_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_databases_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_databases_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlDatabasesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Request body.
    pub body: Database,
}

/// GET v1/projects/{project}/instances/{instance}/databases
/// Inserts a resource containing information about a database inside a Cloud SQL instance. **Note:** You can't modify the default character set and collation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_databases_insert_builder()` + `sql_databases_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_databases_insert(
    client: &SimpleHttpClient,
    args: &SqlDatabasesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_databases_insert_builder(client, &args.project, &args.instance, &args.body)?;
    sql_databases_insert_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/databases
/// Lists databases in the specified Cloud SQL instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_databases_list_execute()` to send, or `sql_databases_list` for simplest API.

pub fn sql_databases_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/databases",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/databases
/// Lists databases in the specified Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_databases_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_databases_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_databases_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DatabasesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DatabasesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_databases_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlDatabasesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// GET v1/projects/{project}/instances/{instance}/databases
/// Lists databases in the specified Cloud SQL instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_databases_list_builder()` + `sql_databases_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_databases_list(
    client: &SimpleHttpClient,
    args: &SqlDatabasesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DatabasesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sql_databases_list_builder(client, &args.project, &args.instance)?;
    sql_databases_list_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/databases/{database}
/// Partially updates a resource containing information about a database inside a Cloud SQL instance. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_databases_patch_execute()` to send, or `sql_databases_patch` for simplest API.

pub fn sql_databases_patch_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    database: &str,
    body: &Database,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/databases/{}",
        project, instance, database,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{project}/instances/{instance}/databases/{database}
/// Partially updates a resource containing information about a database inside a Cloud SQL instance. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_databases_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_databases_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_databases_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_databases_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlDatabasesPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Path parameter: database
    pub database: String,
    /// Request body.
    pub body: Database,
}

/// GET v1/projects/{project}/instances/{instance}/databases/{database}
/// Partially updates a resource containing information about a database inside a Cloud SQL instance. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_databases_patch_builder()` + `sql_databases_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_databases_patch(
    client: &SimpleHttpClient,
    args: &SqlDatabasesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_databases_patch_builder(
        client,
        &args.project,
        &args.instance,
        &args.database,
        &args.body,
    )?;
    sql_databases_patch_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/databases/{database}
/// Updates a resource containing information about a database inside a Cloud SQL instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_databases_update_execute()` to send, or `sql_databases_update` for simplest API.

pub fn sql_databases_update_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    database: &str,
    body: &Database,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/databases/{}",
        project, instance, database,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{project}/instances/{instance}/databases/{database}
/// Updates a resource containing information about a database inside a Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_databases_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_databases_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_databases_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_databases_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlDatabasesUpdateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Path parameter: database
    pub database: String,
    /// Request body.
    pub body: Database,
}

/// GET v1/projects/{project}/instances/{instance}/databases/{database}
/// Updates a resource containing information about a database inside a Cloud SQL instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_databases_update_builder()` + `sql_databases_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_databases_update(
    client: &SimpleHttpClient,
    args: &SqlDatabasesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_databases_update_builder(
        client,
        &args.project,
        &args.instance,
        &args.database,
        &args.body,
    )?;
    sql_databases_update_execute(builder)
}

/// GET v1/flags
/// Lists all available database flags for Cloud SQL instances.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_flags_list_execute()` to send, or `sql_flags_list` for simplest API.

pub fn sql_flags_list_builder(
    client: &SimpleHttpClient,
    databaseVersion: Option<&str>,
    flagScope: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://sqladmin.googleapis.com/v1/flags",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = databaseVersion {
        query_parts.push(format!("databaseVersion={}", val));
    }
    if let Some(val) = flagScope {
        query_parts.push(format!("flagScope={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/flags
/// Lists all available database flags for Cloud SQL instances.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_flags_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_flags_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_flags_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FlagsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FlagsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_flags_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlFlagsListArgs {
    /// Query parameter: databaseVersion
    pub databaseVersion: Option<String>,
    /// Query parameter: flagScope
    pub flagScope: Option<String>,
}

/// GET v1/flags
/// Lists all available database flags for Cloud SQL instances.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_flags_list_builder()` + `sql_flags_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_flags_list(
    client: &SimpleHttpClient,
    args: &SqlFlagsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FlagsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sql_flags_list_builder(
        client,
        args.databaseVersion.as_deref(),
        args.flagScope.as_deref(),
    )?;
    sql_flags_list_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/listEntraIdCertificates
/// Lists all versions of EntraID certificates for the specified instance. There can be up to three sets of certificates listed: the certificate that is currently in use, a future that has been added but not yet used to sign a certificate, and a certificate that has been rotated out.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances__list_entra_id_certificates_execute()` to send, or `sql_instances__list_entra_id_certificates` for simplest API.

pub fn sql_instances__list_entra_id_certificates_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/listEntraIdCertificates",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/listEntraIdCertificates
/// Lists all versions of EntraID certificates for the specified instance. There can be up to three sets of certificates listed: the certificate that is currently in use, a future that has been added but not yet used to sign a certificate, and a certificate that has been rotated out.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances__list_entra_id_certificates_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances__list_entra_id_certificates_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances__list_entra_id_certificates_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstancesListEntraIdCertificatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstancesListEntraIdCertificatesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances__list_entra_id_certificates`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesListEntraIdCertificatesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// GET v1/projects/{project}/instances/{instance}/listEntraIdCertificates
/// Lists all versions of EntraID certificates for the specified instance. There can be up to three sets of certificates listed: the certificate that is currently in use, a future that has been added but not yet used to sign a certificate, and a certificate that has been rotated out.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances__list_entra_id_certificates_builder()` + `sql_instances__list_entra_id_certificates_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances__list_entra_id_certificates(
    client: &SimpleHttpClient,
    args: &SqlInstancesListEntraIdCertificatesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstancesListEntraIdCertificatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        sql_instances__list_entra_id_certificates_builder(client, &args.project, &args.instance)?;
    sql_instances__list_entra_id_certificates_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/listServerCertificates
/// Lists all versions of server certificates and certificate authorities (CAs) for the specified instance. There can be up to three sets of certs listed: the certificate that is currently in use, a future that has been added but not yet used to sign a certificate, and a certificate that has been rotated out. For instances not using Certificate Authority Service (CAS) server CA, use ListServerCas instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances__list_server_certificates_execute()` to send, or `sql_instances__list_server_certificates` for simplest API.

pub fn sql_instances__list_server_certificates_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/listServerCertificates",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/listServerCertificates
/// Lists all versions of server certificates and certificate authorities (CAs) for the specified instance. There can be up to three sets of certs listed: the certificate that is currently in use, a future that has been added but not yet used to sign a certificate, and a certificate that has been rotated out. For instances not using Certificate Authority Service (CAS) server CA, use ListServerCas instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances__list_server_certificates_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances__list_server_certificates_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances__list_server_certificates_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstancesListServerCertificatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstancesListServerCertificatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances__list_server_certificates`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesListServerCertificatesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// GET v1/projects/{project}/instances/{instance}/listServerCertificates
/// Lists all versions of server certificates and certificate authorities (CAs) for the specified instance. There can be up to three sets of certs listed: the certificate that is currently in use, a future that has been added but not yet used to sign a certificate, and a certificate that has been rotated out. For instances not using Certificate Authority Service (CAS) server CA, use ListServerCas instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances__list_server_certificates_builder()` + `sql_instances__list_server_certificates_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances__list_server_certificates(
    client: &SimpleHttpClient,
    args: &SqlInstancesListServerCertificatesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstancesListServerCertificatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        sql_instances__list_server_certificates_builder(client, &args.project, &args.instance)?;
    sql_instances__list_server_certificates_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/rotateEntraIdCertificate
/// Rotates the server certificate version to one previously added with the `addEntraIdCertificate` method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances__rotate_entra_id_certificate_execute()` to send, or `sql_instances__rotate_entra_id_certificate` for simplest API.

pub fn sql_instances__rotate_entra_id_certificate_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    body: &InstancesRotateEntraIdCertificateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/rotateEntraIdCertificate",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{project}/instances/{instance}/rotateEntraIdCertificate
/// Rotates the server certificate version to one previously added with the `addEntraIdCertificate` method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances__rotate_entra_id_certificate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances__rotate_entra_id_certificate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances__rotate_entra_id_certificate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances__rotate_entra_id_certificate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesRotateEntraIdCertificateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Request body.
    pub body: InstancesRotateEntraIdCertificateRequest,
}

/// GET v1/projects/{project}/instances/{instance}/rotateEntraIdCertificate
/// Rotates the server certificate version to one previously added with the `addEntraIdCertificate` method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances__rotate_entra_id_certificate_builder()` + `sql_instances__rotate_entra_id_certificate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances__rotate_entra_id_certificate(
    client: &SimpleHttpClient,
    args: &SqlInstancesRotateEntraIdCertificateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances__rotate_entra_id_certificate_builder(
        client,
        &args.project,
        &args.instance,
        &args.body,
    )?;
    sql_instances__rotate_entra_id_certificate_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/rotateServerCertificate
/// Rotates the server certificate version to one previously added with the `addServerCertificate` method. For instances not using Certificate Authority Service (CAS) server CA, use RotateServerCa instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances__rotate_server_certificate_execute()` to send, or `sql_instances__rotate_server_certificate` for simplest API.

pub fn sql_instances__rotate_server_certificate_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    body: &InstancesRotateServerCertificateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/rotateServerCertificate",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{project}/instances/{instance}/rotateServerCertificate
/// Rotates the server certificate version to one previously added with the `addServerCertificate` method. For instances not using Certificate Authority Service (CAS) server CA, use RotateServerCa instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances__rotate_server_certificate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances__rotate_server_certificate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances__rotate_server_certificate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances__rotate_server_certificate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesRotateServerCertificateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Request body.
    pub body: InstancesRotateServerCertificateRequest,
}

/// GET v1/projects/{project}/instances/{instance}/rotateServerCertificate
/// Rotates the server certificate version to one previously added with the `addServerCertificate` method. For instances not using Certificate Authority Service (CAS) server CA, use RotateServerCa instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances__rotate_server_certificate_builder()` + `sql_instances__rotate_server_certificate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances__rotate_server_certificate(
    client: &SimpleHttpClient,
    args: &SqlInstancesRotateServerCertificateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances__rotate_server_certificate_builder(
        client,
        &args.project,
        &args.instance,
        &args.body,
    )?;
    sql_instances__rotate_server_certificate_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/acquireSsrsLease
/// Acquire a lease for the setup of SQL Server Reporting Services (SSRS).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_acquire_ssrs_lease_execute()` to send, or `sql_instances_acquire_ssrs_lease` for simplest API.

pub fn sql_instances_acquire_ssrs_lease_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    body: &InstancesAcquireSsrsLeaseRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/acquireSsrsLease",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{project}/instances/{instance}/acquireSsrsLease
/// Acquire a lease for the setup of SQL Server Reporting Services (SSRS).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances_acquire_ssrs_lease_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances_acquire_ssrs_lease_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_acquire_ssrs_lease_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SqlInstancesAcquireSsrsLeaseResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SqlInstancesAcquireSsrsLeaseResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_acquire_ssrs_lease`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesAcquireSsrsLeaseArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Request body.
    pub body: InstancesAcquireSsrsLeaseRequest,
}

/// GET v1/projects/{project}/instances/{instance}/acquireSsrsLease
/// Acquire a lease for the setup of SQL Server Reporting Services (SSRS).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_acquire_ssrs_lease_builder()` + `sql_instances_acquire_ssrs_lease_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_acquire_ssrs_lease(
    client: &SimpleHttpClient,
    args: &SqlInstancesAcquireSsrsLeaseArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SqlInstancesAcquireSsrsLeaseResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sql_instances_acquire_ssrs_lease_builder(
        client,
        &args.project,
        &args.instance,
        &args.body,
    )?;
    sql_instances_acquire_ssrs_lease_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/addEntraIdCertificate
/// Adds a new Entra ID certificate for the specified instance. If an Entra ID certificate was previously added but never used in a certificate rotation, this operation replaces that version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_add_entra_id_certificate_execute()` to send, or `sql_instances_add_entra_id_certificate` for simplest API.

pub fn sql_instances_add_entra_id_certificate_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/addEntraIdCertificate",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/addEntraIdCertificate
/// Adds a new Entra ID certificate for the specified instance. If an Entra ID certificate was previously added but never used in a certificate rotation, this operation replaces that version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances_add_entra_id_certificate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances_add_entra_id_certificate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_add_entra_id_certificate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_add_entra_id_certificate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesAddEntraIdCertificateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// GET v1/projects/{project}/instances/{instance}/addEntraIdCertificate
/// Adds a new Entra ID certificate for the specified instance. If an Entra ID certificate was previously added but never used in a certificate rotation, this operation replaces that version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_add_entra_id_certificate_builder()` + `sql_instances_add_entra_id_certificate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_add_entra_id_certificate(
    client: &SimpleHttpClient,
    args: &SqlInstancesAddEntraIdCertificateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        sql_instances_add_entra_id_certificate_builder(client, &args.project, &args.instance)?;
    sql_instances_add_entra_id_certificate_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/addServerCa
/// Adds a new trusted Certificate Authority (CA) version for the specified instance. Required to prepare for a certificate rotation. If a CA version was previously added but never used in a certificate rotation, this operation replaces that version. There cannot be more than one CA version waiting to be rotated in. For instances that have enabled Certificate Authority Service (CAS) based server CA, use AddServerCertificate to add a new server certificate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_add_server_ca_execute()` to send, or `sql_instances_add_server_ca` for simplest API.

pub fn sql_instances_add_server_ca_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/addServerCa",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/addServerCa
/// Adds a new trusted Certificate Authority (CA) version for the specified instance. Required to prepare for a certificate rotation. If a CA version was previously added but never used in a certificate rotation, this operation replaces that version. There cannot be more than one CA version waiting to be rotated in. For instances that have enabled Certificate Authority Service (CAS) based server CA, use AddServerCertificate to add a new server certificate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances_add_server_ca_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances_add_server_ca_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_add_server_ca_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_add_server_ca`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesAddServerCaArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// GET v1/projects/{project}/instances/{instance}/addServerCa
/// Adds a new trusted Certificate Authority (CA) version for the specified instance. Required to prepare for a certificate rotation. If a CA version was previously added but never used in a certificate rotation, this operation replaces that version. There cannot be more than one CA version waiting to be rotated in. For instances that have enabled Certificate Authority Service (CAS) based server CA, use AddServerCertificate to add a new server certificate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_add_server_ca_builder()` + `sql_instances_add_server_ca_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_add_server_ca(
    client: &SimpleHttpClient,
    args: &SqlInstancesAddServerCaArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_add_server_ca_builder(client, &args.project, &args.instance)?;
    sql_instances_add_server_ca_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/addServerCertificate
/// Add a new trusted server certificate version for the specified instance using Certificate Authority Service (CAS) server CA. Required to prepare for a certificate rotation. If a server certificate version was previously added but never used in a certificate rotation, this operation replaces that version. There cannot be more than one certificate version waiting to be rotated in. For instances not using CAS server CA, use AddServerCa instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_add_server_certificate_execute()` to send, or `sql_instances_add_server_certificate` for simplest API.

pub fn sql_instances_add_server_certificate_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/addServerCertificate",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/addServerCertificate
/// Add a new trusted server certificate version for the specified instance using Certificate Authority Service (CAS) server CA. Required to prepare for a certificate rotation. If a server certificate version was previously added but never used in a certificate rotation, this operation replaces that version. There cannot be more than one certificate version waiting to be rotated in. For instances not using CAS server CA, use AddServerCa instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances_add_server_certificate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances_add_server_certificate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_add_server_certificate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_add_server_certificate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesAddServerCertificateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// GET v1/projects/{project}/instances/{instance}/addServerCertificate
/// Add a new trusted server certificate version for the specified instance using Certificate Authority Service (CAS) server CA. Required to prepare for a certificate rotation. If a server certificate version was previously added but never used in a certificate rotation, this operation replaces that version. There cannot be more than one certificate version waiting to be rotated in. For instances not using CAS server CA, use AddServerCa instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_add_server_certificate_builder()` + `sql_instances_add_server_certificate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_add_server_certificate(
    client: &SimpleHttpClient,
    args: &SqlInstancesAddServerCertificateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        sql_instances_add_server_certificate_builder(client, &args.project, &args.instance)?;
    sql_instances_add_server_certificate_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/clone
/// Creates a Cloud SQL instance as a clone of the source instance. Using this operation might cause your instance to restart.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_clone_execute()` to send, or `sql_instances_clone` for simplest API.

pub fn sql_instances_clone_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    body: &InstancesCloneRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/clone",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{project}/instances/{instance}/clone
/// Creates a Cloud SQL instance as a clone of the source instance. Using this operation might cause your instance to restart.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances_clone_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances_clone_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_clone_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_clone`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesCloneArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Request body.
    pub body: InstancesCloneRequest,
}

/// GET v1/projects/{project}/instances/{instance}/clone
/// Creates a Cloud SQL instance as a clone of the source instance. Using this operation might cause your instance to restart.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_clone_builder()` + `sql_instances_clone_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_clone(
    client: &SimpleHttpClient,
    args: &SqlInstancesCloneArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_clone_builder(client, &args.project, &args.instance, &args.body)?;
    sql_instances_clone_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}
/// Deletes a Cloud SQL instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_delete_execute()` to send, or `sql_instances_delete` for simplest API.

pub fn sql_instances_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    enableFinalBackup: Option<bool>,
    finalBackupDescription: Option<&str>,
    finalBackupExpiryTime: Option<&str>,
    finalBackupTtlDays: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}",
        project, instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = enableFinalBackup {
        query_parts.push(format!("enableFinalBackup={}", val));
    }
    if let Some(val) = finalBackupDescription {
        query_parts.push(format!("finalBackupDescription={}", val));
    }
    if let Some(val) = finalBackupExpiryTime {
        query_parts.push(format!("finalBackupExpiryTime={}", val));
    }
    if let Some(val) = finalBackupTtlDays {
        query_parts.push(format!("finalBackupTtlDays={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}
/// Deletes a Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: enableFinalBackup
    pub enableFinalBackup: Option<bool>,
    /// Query parameter: finalBackupDescription
    pub finalBackupDescription: Option<String>,
    /// Query parameter: finalBackupExpiryTime
    pub finalBackupExpiryTime: Option<String>,
    /// Query parameter: finalBackupTtlDays
    pub finalBackupTtlDays: Option<String>,
}

/// GET v1/projects/{project}/instances/{instance}
/// Deletes a Cloud SQL instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_delete_builder()` + `sql_instances_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_delete(
    client: &SimpleHttpClient,
    args: &SqlInstancesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_delete_builder(
        client,
        &args.project,
        &args.instance,
        args.enableFinalBackup,
        args.finalBackupDescription.as_deref(),
        args.finalBackupExpiryTime.as_deref(),
        args.finalBackupTtlDays.as_deref(),
    )?;
    sql_instances_delete_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/demote
/// Demotes an existing standalone instance to be a Cloud SQL read replica for an external database server.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_demote_execute()` to send, or `sql_instances_demote` for simplest API.

pub fn sql_instances_demote_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    body: &InstancesDemoteRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/demote",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{project}/instances/{instance}/demote
/// Demotes an existing standalone instance to be a Cloud SQL read replica for an external database server.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances_demote_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances_demote_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_demote_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_demote`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesDemoteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Request body.
    pub body: InstancesDemoteRequest,
}

/// GET v1/projects/{project}/instances/{instance}/demote
/// Demotes an existing standalone instance to be a Cloud SQL read replica for an external database server.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_demote_builder()` + `sql_instances_demote_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_demote(
    client: &SimpleHttpClient,
    args: &SqlInstancesDemoteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_demote_builder(client, &args.project, &args.instance, &args.body)?;
    sql_instances_demote_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/demoteMaster
/// Demotes the stand-alone instance to be a Cloud SQL read replica for an external database server.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_demote_master_execute()` to send, or `sql_instances_demote_master` for simplest API.

pub fn sql_instances_demote_master_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    body: &InstancesDemoteMasterRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/demoteMaster",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{project}/instances/{instance}/demoteMaster
/// Demotes the stand-alone instance to be a Cloud SQL read replica for an external database server.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances_demote_master_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances_demote_master_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_demote_master_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_demote_master`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesDemoteMasterArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Request body.
    pub body: InstancesDemoteMasterRequest,
}

/// GET v1/projects/{project}/instances/{instance}/demoteMaster
/// Demotes the stand-alone instance to be a Cloud SQL read replica for an external database server.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_demote_master_builder()` + `sql_instances_demote_master_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_demote_master(
    client: &SimpleHttpClient,
    args: &SqlInstancesDemoteMasterArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        sql_instances_demote_master_builder(client, &args.project, &args.instance, &args.body)?;
    sql_instances_demote_master_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/executeSql
/// Execute SQL statements.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_execute_sql_execute()` to send, or `sql_instances_execute_sql` for simplest API.

pub fn sql_instances_execute_sql_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    body: &ExecuteSqlPayload,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/executeSql",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{project}/instances/{instance}/executeSql
/// Execute SQL statements.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances_execute_sql_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances_execute_sql_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_execute_sql_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SqlInstancesExecuteSqlResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SqlInstancesExecuteSqlResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_execute_sql`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesExecuteSqlArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Request body.
    pub body: ExecuteSqlPayload,
}

/// GET v1/projects/{project}/instances/{instance}/executeSql
/// Execute SQL statements.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_execute_sql_builder()` + `sql_instances_execute_sql_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_execute_sql(
    client: &SimpleHttpClient,
    args: &SqlInstancesExecuteSqlArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SqlInstancesExecuteSqlResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        sql_instances_execute_sql_builder(client, &args.project, &args.instance, &args.body)?;
    sql_instances_execute_sql_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/export
/// Exports data from a Cloud SQL instance to a Cloud Storage bucket as a SQL dump or CSV file.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_export_execute()` to send, or `sql_instances_export` for simplest API.

pub fn sql_instances_export_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    body: &InstancesExportRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/export",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{project}/instances/{instance}/export
/// Exports data from a Cloud SQL instance to a Cloud Storage bucket as a SQL dump or CSV file.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances_export_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances_export_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_export_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_export`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesExportArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Request body.
    pub body: InstancesExportRequest,
}

/// GET v1/projects/{project}/instances/{instance}/export
/// Exports data from a Cloud SQL instance to a Cloud Storage bucket as a SQL dump or CSV file.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_export_builder()` + `sql_instances_export_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_export(
    client: &SimpleHttpClient,
    args: &SqlInstancesExportArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_export_builder(client, &args.project, &args.instance, &args.body)?;
    sql_instances_export_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/failover
/// Initiates a manual failover of a high availability (HA) primary instance to a standby instance, which becomes the primary instance. Users are then rerouted to the new primary. For more information, see the [Overview of high availability](<https://cloud.google.`com/sql/docs/mysql/high-availability`>) page in the Cloud SQL documentation. If using Legacy HA (MySQL only), this causes the instance to failover to its failover replica instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_failover_execute()` to send, or `sql_instances_failover` for simplest API.

pub fn sql_instances_failover_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    body: &InstancesFailoverRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/failover",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{project}/instances/{instance}/failover
/// Initiates a manual failover of a high availability (HA) primary instance to a standby instance, which becomes the primary instance. Users are then rerouted to the new primary. For more information, see the [Overview of high availability](<https://cloud.google.`com/sql/docs/mysql/high-availability`>) page in the Cloud SQL documentation. If using Legacy HA (MySQL only), this causes the instance to failover to its failover replica instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances_failover_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances_failover_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_failover_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_failover`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesFailoverArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Request body.
    pub body: InstancesFailoverRequest,
}

/// GET v1/projects/{project}/instances/{instance}/failover
/// Initiates a manual failover of a high availability (HA) primary instance to a standby instance, which becomes the primary instance. Users are then rerouted to the new primary. For more information, see the [Overview of high availability](<https://cloud.google.`com/sql/docs/mysql/high-availability`>) page in the Cloud SQL documentation. If using Legacy HA (MySQL only), this causes the instance to failover to its failover replica instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_failover_builder()` + `sql_instances_failover_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_failover(
    client: &SimpleHttpClient,
    args: &SqlInstancesFailoverArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        sql_instances_failover_builder(client, &args.project, &args.instance, &args.body)?;
    sql_instances_failover_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}
/// Retrieves a resource containing information about a Cloud SQL instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_get_execute()` to send, or `sql_instances_get` for simplest API.

pub fn sql_instances_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}
/// Retrieves a resource containing information about a Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DatabaseInstance>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DatabaseInstance = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// GET v1/projects/{project}/instances/{instance}
/// Retrieves a resource containing information about a Cloud SQL instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_get_builder()` + `sql_instances_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_get(
    client: &SimpleHttpClient,
    args: &SqlInstancesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DatabaseInstance>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sql_instances_get_builder(client, &args.project, &args.instance)?;
    sql_instances_get_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/import
/// Imports data into a Cloud SQL instance from a SQL dump or CSV file in Cloud Storage.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_import_execute()` to send, or `sql_instances_import` for simplest API.

pub fn sql_instances_import_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    body: &InstancesImportRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/import",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{project}/instances/{instance}/import
/// Imports data into a Cloud SQL instance from a SQL dump or CSV file in Cloud Storage.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances_import_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances_import_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_import`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesImportArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Request body.
    pub body: InstancesImportRequest,
}

/// GET v1/projects/{project}/instances/{instance}/import
/// Imports data into a Cloud SQL instance from a SQL dump or CSV file in Cloud Storage.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_import_builder()` + `sql_instances_import_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_import(
    client: &SimpleHttpClient,
    args: &SqlInstancesImportArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_import_builder(client, &args.project, &args.instance, &args.body)?;
    sql_instances_import_execute(builder)
}

/// GET v1/projects/{project}/instances
/// Creates a new Cloud SQL instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_insert_execute()` to send, or `sql_instances_insert` for simplest API.

pub fn sql_instances_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    body: &DatabaseInstance,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances",
        project,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{project}/instances
/// Creates a new Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Request body.
    pub body: DatabaseInstance,
}

/// GET v1/projects/{project}/instances
/// Creates a new Cloud SQL instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_insert_builder()` + `sql_instances_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_insert(
    client: &SimpleHttpClient,
    args: &SqlInstancesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_insert_builder(client, &args.project, &args.body)?;
    sql_instances_insert_execute(builder)
}

/// GET v1/projects/{project}/instances
/// Lists instances under a given project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_list_execute()` to send, or `sql_instances_list` for simplest API.

pub fn sql_instances_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    filter: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances
/// Lists instances under a given project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstancesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstancesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{project}/instances
/// Lists instances under a given project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_list_builder()` + `sql_instances_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_list(
    client: &SimpleHttpClient,
    args: &SqlInstancesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstancesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sql_instances_list_builder(
        client,
        &args.project,
        args.filter.as_deref(),
        args.maxResults,
        args.pageToken.as_deref(),
    )?;
    sql_instances_list_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/listServerCas
/// Lists all of the trusted Certificate Authorities (CAs) for the specified instance. There can be up to three CAs listed: the CA that was used to sign the certificate that is currently in use, a CA that has been added but not yet used to sign a certificate, and a CA used to sign a certificate that has previously rotated out.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_list_server_cas_execute()` to send, or `sql_instances_list_server_cas` for simplest API.

pub fn sql_instances_list_server_cas_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/listServerCas",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/listServerCas
/// Lists all of the trusted Certificate Authorities (CAs) for the specified instance. There can be up to three CAs listed: the CA that was used to sign the certificate that is currently in use, a CA that has been added but not yet used to sign a certificate, and a CA used to sign a certificate that has previously rotated out.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances_list_server_cas_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances_list_server_cas_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_list_server_cas_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstancesListServerCasResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstancesListServerCasResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_list_server_cas`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesListServerCasArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// GET v1/projects/{project}/instances/{instance}/listServerCas
/// Lists all of the trusted Certificate Authorities (CAs) for the specified instance. There can be up to three CAs listed: the CA that was used to sign the certificate that is currently in use, a CA that has been added but not yet used to sign a certificate, and a CA used to sign a certificate that has previously rotated out.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_list_server_cas_builder()` + `sql_instances_list_server_cas_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_list_server_cas(
    client: &SimpleHttpClient,
    args: &SqlInstancesListServerCasArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstancesListServerCasResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sql_instances_list_server_cas_builder(client, &args.project, &args.instance)?;
    sql_instances_list_server_cas_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}
/// Partially updates settings of a Cloud SQL instance by merging the request with the current configuration. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_patch_execute()` to send, or `sql_instances_patch` for simplest API.

pub fn sql_instances_patch_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    body: &DatabaseInstance,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{project}/instances/{instance}
/// Partially updates settings of a Cloud SQL instance by merging the request with the current configuration. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Request body.
    pub body: DatabaseInstance,
}

/// GET v1/projects/{project}/instances/{instance}
/// Partially updates settings of a Cloud SQL instance by merging the request with the current configuration. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_patch_builder()` + `sql_instances_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_patch(
    client: &SimpleHttpClient,
    args: &SqlInstancesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_patch_builder(client, &args.project, &args.instance, &args.body)?;
    sql_instances_patch_execute(builder)
}

/// GET v1/projects/{projectsId}:pointInTimeRestore
/// Point in time restore for an instance managed by Google Cloud Backup and Disaster Recovery.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_point_in_time_restore_execute()` to send, or `sql_instances_point_in_time_restore` for simplest API.

pub fn sql_instances_point_in_time_restore_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &PointInTimeRestoreContext,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}:pointInTimeRestore",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}:pointInTimeRestore
/// Point in time restore for an instance managed by Google Cloud Backup and Disaster Recovery.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances_point_in_time_restore_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances_point_in_time_restore_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_point_in_time_restore_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_point_in_time_restore`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesPointInTimeRestoreArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: PointInTimeRestoreContext,
}

/// GET v1/projects/{projectsId}:pointInTimeRestore
/// Point in time restore for an instance managed by Google Cloud Backup and Disaster Recovery.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_point_in_time_restore_builder()` + `sql_instances_point_in_time_restore_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_point_in_time_restore(
    client: &SimpleHttpClient,
    args: &SqlInstancesPointInTimeRestoreArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_point_in_time_restore_builder(client, &args.parent, &args.body)?;
    sql_instances_point_in_time_restore_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/preCheckMajorVersionUpgrade
/// Execute MVU Pre-checks
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_pre_check_major_version_upgrade_execute()` to send, or `sql_instances_pre_check_major_version_upgrade` for simplest API.

pub fn sql_instances_pre_check_major_version_upgrade_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    body: &InstancesPreCheckMajorVersionUpgradeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/preCheckMajorVersionUpgrade",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{project}/instances/{instance}/preCheckMajorVersionUpgrade
/// Execute MVU Pre-checks
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances_pre_check_major_version_upgrade_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances_pre_check_major_version_upgrade_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_pre_check_major_version_upgrade_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_pre_check_major_version_upgrade`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesPreCheckMajorVersionUpgradeArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Request body.
    pub body: InstancesPreCheckMajorVersionUpgradeRequest,
}

/// GET v1/projects/{project}/instances/{instance}/preCheckMajorVersionUpgrade
/// Execute MVU Pre-checks
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_pre_check_major_version_upgrade_builder()` + `sql_instances_pre_check_major_version_upgrade_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_pre_check_major_version_upgrade(
    client: &SimpleHttpClient,
    args: &SqlInstancesPreCheckMajorVersionUpgradeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_pre_check_major_version_upgrade_builder(
        client,
        &args.project,
        &args.instance,
        &args.body,
    )?;
    sql_instances_pre_check_major_version_upgrade_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/promoteReplica
/// Promotes the read replica instance to be an independent Cloud SQL primary instance. Using this operation might cause your instance to restart.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_promote_replica_execute()` to send, or `sql_instances_promote_replica` for simplest API.

pub fn sql_instances_promote_replica_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    failover: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/promoteReplica",
        project, instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = failover {
        query_parts.push(format!("failover={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/promoteReplica
/// Promotes the read replica instance to be an independent Cloud SQL primary instance. Using this operation might cause your instance to restart.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances_promote_replica_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances_promote_replica_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_promote_replica_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_promote_replica`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesPromoteReplicaArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: failover
    pub failover: Option<bool>,
}

/// GET v1/projects/{project}/instances/{instance}/promoteReplica
/// Promotes the read replica instance to be an independent Cloud SQL primary instance. Using this operation might cause your instance to restart.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_promote_replica_builder()` + `sql_instances_promote_replica_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_promote_replica(
    client: &SimpleHttpClient,
    args: &SqlInstancesPromoteReplicaArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_promote_replica_builder(
        client,
        &args.project,
        &args.instance,
        args.failover,
    )?;
    sql_instances_promote_replica_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/reencrypt
/// Reencrypt CMEK instance with latest key version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_reencrypt_execute()` to send, or `sql_instances_reencrypt` for simplest API.

pub fn sql_instances_reencrypt_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    body: &InstancesReencryptRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/reencrypt",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{project}/instances/{instance}/reencrypt
/// Reencrypt CMEK instance with latest key version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances_reencrypt_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances_reencrypt_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_reencrypt_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_reencrypt`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesReencryptArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Request body.
    pub body: InstancesReencryptRequest,
}

/// GET v1/projects/{project}/instances/{instance}/reencrypt
/// Reencrypt CMEK instance with latest key version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_reencrypt_builder()` + `sql_instances_reencrypt_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_reencrypt(
    client: &SimpleHttpClient,
    args: &SqlInstancesReencryptArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        sql_instances_reencrypt_builder(client, &args.project, &args.instance, &args.body)?;
    sql_instances_reencrypt_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/releaseSsrsLease
/// Release a lease for the setup of SQL Server Reporting Services (SSRS).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_release_ssrs_lease_execute()` to send, or `sql_instances_release_ssrs_lease` for simplest API.

pub fn sql_instances_release_ssrs_lease_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/releaseSsrsLease",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/releaseSsrsLease
/// Release a lease for the setup of SQL Server Reporting Services (SSRS).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances_release_ssrs_lease_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances_release_ssrs_lease_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_release_ssrs_lease_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SqlInstancesReleaseSsrsLeaseResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SqlInstancesReleaseSsrsLeaseResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_release_ssrs_lease`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesReleaseSsrsLeaseArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// GET v1/projects/{project}/instances/{instance}/releaseSsrsLease
/// Release a lease for the setup of SQL Server Reporting Services (SSRS).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_release_ssrs_lease_builder()` + `sql_instances_release_ssrs_lease_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_release_ssrs_lease(
    client: &SimpleHttpClient,
    args: &SqlInstancesReleaseSsrsLeaseArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SqlInstancesReleaseSsrsLeaseResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sql_instances_release_ssrs_lease_builder(client, &args.project, &args.instance)?;
    sql_instances_release_ssrs_lease_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/resetSslConfig
/// Deletes all client certificates and generates a new server SSL certificate for the instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_reset_ssl_config_execute()` to send, or `sql_instances_reset_ssl_config` for simplest API.

pub fn sql_instances_reset_ssl_config_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    mode: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/resetSslConfig",
        project, instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = mode {
        query_parts.push(format!("mode={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/resetSslConfig
/// Deletes all client certificates and generates a new server SSL certificate for the instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances_reset_ssl_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances_reset_ssl_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_reset_ssl_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_reset_ssl_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesResetSslConfigArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: mode
    pub mode: Option<String>,
}

/// GET v1/projects/{project}/instances/{instance}/resetSslConfig
/// Deletes all client certificates and generates a new server SSL certificate for the instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_reset_ssl_config_builder()` + `sql_instances_reset_ssl_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_reset_ssl_config(
    client: &SimpleHttpClient,
    args: &SqlInstancesResetSslConfigArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_reset_ssl_config_builder(
        client,
        &args.project,
        &args.instance,
        args.mode.as_deref(),
    )?;
    sql_instances_reset_ssl_config_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/restart
/// Restarts a Cloud SQL instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_restart_execute()` to send, or `sql_instances_restart` for simplest API.

pub fn sql_instances_restart_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/restart",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/restart
/// Restarts a Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances_restart_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances_restart_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_restart_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_restart`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesRestartArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// GET v1/projects/{project}/instances/{instance}/restart
/// Restarts a Cloud SQL instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_restart_builder()` + `sql_instances_restart_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_restart(
    client: &SimpleHttpClient,
    args: &SqlInstancesRestartArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_restart_builder(client, &args.project, &args.instance)?;
    sql_instances_restart_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/restoreBackup
/// Restores a backup of a Cloud SQL instance. Using this operation might cause your instance to restart.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_restore_backup_execute()` to send, or `sql_instances_restore_backup` for simplest API.

pub fn sql_instances_restore_backup_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    body: &InstancesRestoreBackupRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/restoreBackup",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{project}/instances/{instance}/restoreBackup
/// Restores a backup of a Cloud SQL instance. Using this operation might cause your instance to restart.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances_restore_backup_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances_restore_backup_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_restore_backup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_restore_backup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesRestoreBackupArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Request body.
    pub body: InstancesRestoreBackupRequest,
}

/// GET v1/projects/{project}/instances/{instance}/restoreBackup
/// Restores a backup of a Cloud SQL instance. Using this operation might cause your instance to restart.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_restore_backup_builder()` + `sql_instances_restore_backup_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_restore_backup(
    client: &SimpleHttpClient,
    args: &SqlInstancesRestoreBackupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        sql_instances_restore_backup_builder(client, &args.project, &args.instance, &args.body)?;
    sql_instances_restore_backup_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/rotateServerCa
/// Rotates the server certificate to one signed by the Certificate Authority (CA) version previously added with the `addServerCA` method. For instances that have enabled Certificate Authority Service (CAS) based server CA, use RotateServerCertificate to rotate the server certificate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_rotate_server_ca_execute()` to send, or `sql_instances_rotate_server_ca` for simplest API.

pub fn sql_instances_rotate_server_ca_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    body: &InstancesRotateServerCaRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/rotateServerCa",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{project}/instances/{instance}/rotateServerCa
/// Rotates the server certificate to one signed by the Certificate Authority (CA) version previously added with the `addServerCA` method. For instances that have enabled Certificate Authority Service (CAS) based server CA, use RotateServerCertificate to rotate the server certificate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances_rotate_server_ca_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances_rotate_server_ca_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_rotate_server_ca_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_rotate_server_ca`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesRotateServerCaArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Request body.
    pub body: InstancesRotateServerCaRequest,
}

/// GET v1/projects/{project}/instances/{instance}/rotateServerCa
/// Rotates the server certificate to one signed by the Certificate Authority (CA) version previously added with the `addServerCA` method. For instances that have enabled Certificate Authority Service (CAS) based server CA, use RotateServerCertificate to rotate the server certificate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_rotate_server_ca_builder()` + `sql_instances_rotate_server_ca_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_rotate_server_ca(
    client: &SimpleHttpClient,
    args: &SqlInstancesRotateServerCaArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        sql_instances_rotate_server_ca_builder(client, &args.project, &args.instance, &args.body)?;
    sql_instances_rotate_server_ca_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/startReplica
/// Starts the replication in the read replica instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_start_replica_execute()` to send, or `sql_instances_start_replica` for simplest API.

pub fn sql_instances_start_replica_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/startReplica",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/startReplica
/// Starts the replication in the read replica instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances_start_replica_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances_start_replica_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_start_replica_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_start_replica`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesStartReplicaArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// GET v1/projects/{project}/instances/{instance}/startReplica
/// Starts the replication in the read replica instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_start_replica_builder()` + `sql_instances_start_replica_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_start_replica(
    client: &SimpleHttpClient,
    args: &SqlInstancesStartReplicaArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_start_replica_builder(client, &args.project, &args.instance)?;
    sql_instances_start_replica_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/stopReplica
/// Stops the replication in the read replica instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_stop_replica_execute()` to send, or `sql_instances_stop_replica` for simplest API.

pub fn sql_instances_stop_replica_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/stopReplica",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/stopReplica
/// Stops the replication in the read replica instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances_stop_replica_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances_stop_replica_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_stop_replica_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_stop_replica`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesStopReplicaArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// GET v1/projects/{project}/instances/{instance}/stopReplica
/// Stops the replication in the read replica instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_stop_replica_builder()` + `sql_instances_stop_replica_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_stop_replica(
    client: &SimpleHttpClient,
    args: &SqlInstancesStopReplicaArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_stop_replica_builder(client, &args.project, &args.instance)?;
    sql_instances_stop_replica_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/switchover
/// Switches over from the primary instance to the DR replica instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_switchover_execute()` to send, or `sql_instances_switchover` for simplest API.

pub fn sql_instances_switchover_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    dbTimeout: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/switchover",
        project, instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = dbTimeout {
        query_parts.push(format!("dbTimeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/switchover
/// Switches over from the primary instance to the DR replica instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances_switchover_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances_switchover_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_switchover_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_switchover`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesSwitchoverArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: dbTimeout
    pub dbTimeout: Option<String>,
}

/// GET v1/projects/{project}/instances/{instance}/switchover
/// Switches over from the primary instance to the DR replica instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_switchover_builder()` + `sql_instances_switchover_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_switchover(
    client: &SimpleHttpClient,
    args: &SqlInstancesSwitchoverArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_switchover_builder(
        client,
        &args.project,
        &args.instance,
        args.dbTimeout.as_deref(),
    )?;
    sql_instances_switchover_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/truncateLog
/// Truncate MySQL general and slow query log tables MySQL only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_truncate_log_execute()` to send, or `sql_instances_truncate_log` for simplest API.

pub fn sql_instances_truncate_log_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    body: &InstancesTruncateLogRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/truncateLog",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{project}/instances/{instance}/truncateLog
/// Truncate MySQL general and slow query log tables MySQL only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances_truncate_log_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances_truncate_log_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_truncate_log_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_truncate_log`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesTruncateLogArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Request body.
    pub body: InstancesTruncateLogRequest,
}

/// GET v1/projects/{project}/instances/{instance}/truncateLog
/// Truncate MySQL general and slow query log tables MySQL only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_truncate_log_builder()` + `sql_instances_truncate_log_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_truncate_log(
    client: &SimpleHttpClient,
    args: &SqlInstancesTruncateLogArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        sql_instances_truncate_log_builder(client, &args.project, &args.instance, &args.body)?;
    sql_instances_truncate_log_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}
/// Updates settings of a Cloud SQL instance. Using this operation might cause your instance to restart.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_update_execute()` to send, or `sql_instances_update` for simplest API.

pub fn sql_instances_update_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    body: &DatabaseInstance,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{project}/instances/{instance}
/// Updates settings of a Cloud SQL instance. Using this operation might cause your instance to restart.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_instances_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_instances_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesUpdateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Request body.
    pub body: DatabaseInstance,
}

/// GET v1/projects/{project}/instances/{instance}
/// Updates settings of a Cloud SQL instance. Using this operation might cause your instance to restart.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_update_builder()` + `sql_instances_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_update(
    client: &SimpleHttpClient,
    args: &SqlInstancesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_update_builder(client, &args.project, &args.instance, &args.body)?;
    sql_instances_update_execute(builder)
}

/// GET v1/projects/{project}/operations/{operation}/cancel
/// Cancels an instance operation that has been performed on an instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_operations_cancel_execute()` to send, or `sql_operations_cancel` for simplest API.

pub fn sql_operations_cancel_builder(
    client: &SimpleHttpClient,
    project: &str,
    operation: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/operations/{}/cancel",
        project, operation,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/operations/{operation}/cancel
/// Cancels an instance operation that has been performed on an instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_operations_cancel_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_operations_cancel_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlOperationsCancelArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: operation
    pub operation: String,
}

/// GET v1/projects/{project}/operations/{operation}/cancel
/// Cancels an instance operation that has been performed on an instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_operations_cancel_builder()` + `sql_operations_cancel_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_operations_cancel(
    client: &SimpleHttpClient,
    args: &SqlOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_operations_cancel_builder(client, &args.project, &args.operation)?;
    sql_operations_cancel_execute(builder)
}

/// GET v1/projects/{project}/operations/{operation}
/// Retrieves an instance operation that has been performed on an instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_operations_get_execute()` to send, or `sql_operations_get` for simplest API.

pub fn sql_operations_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    operation: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/operations/{}",
        project, operation,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/operations/{operation}
/// Retrieves an instance operation that has been performed on an instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_operations_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_operations_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlOperationsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: operation
    pub operation: String,
}

/// GET v1/projects/{project}/operations/{operation}
/// Retrieves an instance operation that has been performed on an instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_operations_get_builder()` + `sql_operations_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_operations_get(
    client: &SimpleHttpClient,
    args: &SqlOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_operations_get_builder(client, &args.project, &args.operation)?;
    sql_operations_get_execute(builder)
}

/// GET v1/projects/{project}/operations
/// Lists all instance operations that have been performed on the given Cloud SQL instance in the reverse chronological order of the start time.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_operations_list_execute()` to send, or `sql_operations_list` for simplest API.

pub fn sql_operations_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: Option<&str>,
    maxResults: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/operations",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = instance {
        query_parts.push(format!("instance={}", val));
    }
    if let Some(val) = maxResults {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/operations
/// Lists all instance operations that have been performed on the given Cloud SQL instance in the reverse chronological order of the start time.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_operations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_operations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OperationsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OperationsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlOperationsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: instance
    pub instance: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{project}/operations
/// Lists all instance operations that have been performed on the given Cloud SQL instance in the reverse chronological order of the start time.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_operations_list_builder()` + `sql_operations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_operations_list(
    client: &SimpleHttpClient,
    args: &SqlOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OperationsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sql_operations_list_builder(
        client,
        &args.project,
        args.instance.as_deref(),
        args.maxResults,
        args.pageToken.as_deref(),
    )?;
    sql_operations_list_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/getDiskShrinkConfig
/// Get Disk Shrink Config for a given instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_projects_instances_get_disk_shrink_config_execute()` to send, or `sql_projects_instances_get_disk_shrink_config` for simplest API.

pub fn sql_projects_instances_get_disk_shrink_config_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/getDiskShrinkConfig",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/getDiskShrinkConfig
/// Get Disk Shrink Config for a given instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_projects_instances_get_disk_shrink_config_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_projects_instances_get_disk_shrink_config_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_projects_instances_get_disk_shrink_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SqlInstancesGetDiskShrinkConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SqlInstancesGetDiskShrinkConfigResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_projects_instances_get_disk_shrink_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlProjectsInstancesGetDiskShrinkConfigArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// GET v1/projects/{project}/instances/{instance}/getDiskShrinkConfig
/// Get Disk Shrink Config for a given instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_projects_instances_get_disk_shrink_config_builder()` + `sql_projects_instances_get_disk_shrink_config_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_projects_instances_get_disk_shrink_config(
    client: &SimpleHttpClient,
    args: &SqlProjectsInstancesGetDiskShrinkConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SqlInstancesGetDiskShrinkConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sql_projects_instances_get_disk_shrink_config_builder(
        client,
        &args.project,
        &args.instance,
    )?;
    sql_projects_instances_get_disk_shrink_config_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/getLatestRecoveryTime
/// Get Latest Recovery Time for a given instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_projects_instances_get_latest_recovery_time_execute()` to send, or `sql_projects_instances_get_latest_recovery_time` for simplest API.

pub fn sql_projects_instances_get_latest_recovery_time_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    sourceInstanceDeletionTime: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/getLatestRecoveryTime",
        project, instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = sourceInstanceDeletionTime {
        query_parts.push(format!("sourceInstanceDeletionTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/getLatestRecoveryTime
/// Get Latest Recovery Time for a given instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_projects_instances_get_latest_recovery_time_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_projects_instances_get_latest_recovery_time_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_projects_instances_get_latest_recovery_time_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SqlInstancesGetLatestRecoveryTimeResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SqlInstancesGetLatestRecoveryTimeResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_projects_instances_get_latest_recovery_time`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlProjectsInstancesGetLatestRecoveryTimeArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: sourceInstanceDeletionTime
    pub sourceInstanceDeletionTime: Option<String>,
}

/// GET v1/projects/{project}/instances/{instance}/getLatestRecoveryTime
/// Get Latest Recovery Time for a given instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_projects_instances_get_latest_recovery_time_builder()` + `sql_projects_instances_get_latest_recovery_time_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_projects_instances_get_latest_recovery_time(
    client: &SimpleHttpClient,
    args: &SqlProjectsInstancesGetLatestRecoveryTimeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SqlInstancesGetLatestRecoveryTimeResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sql_projects_instances_get_latest_recovery_time_builder(
        client,
        &args.project,
        &args.instance,
        args.sourceInstanceDeletionTime.as_deref(),
    )?;
    sql_projects_instances_get_latest_recovery_time_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/performDiskShrink
/// Perform Disk Shrink on primary instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_projects_instances_perform_disk_shrink_execute()` to send, or `sql_projects_instances_perform_disk_shrink` for simplest API.

pub fn sql_projects_instances_perform_disk_shrink_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    body: &PerformDiskShrinkContext,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/performDiskShrink",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{project}/instances/{instance}/performDiskShrink
/// Perform Disk Shrink on primary instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_projects_instances_perform_disk_shrink_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_projects_instances_perform_disk_shrink_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_projects_instances_perform_disk_shrink_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_projects_instances_perform_disk_shrink`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlProjectsInstancesPerformDiskShrinkArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Request body.
    pub body: PerformDiskShrinkContext,
}

/// GET v1/projects/{project}/instances/{instance}/performDiskShrink
/// Perform Disk Shrink on primary instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_projects_instances_perform_disk_shrink_builder()` + `sql_projects_instances_perform_disk_shrink_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_projects_instances_perform_disk_shrink(
    client: &SimpleHttpClient,
    args: &SqlProjectsInstancesPerformDiskShrinkArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_projects_instances_perform_disk_shrink_builder(
        client,
        &args.project,
        &args.instance,
        &args.body,
    )?;
    sql_projects_instances_perform_disk_shrink_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/rescheduleMaintenance
/// Reschedules the maintenance on the given instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_projects_instances_reschedule_maintenance_execute()` to send, or `sql_projects_instances_reschedule_maintenance` for simplest API.

pub fn sql_projects_instances_reschedule_maintenance_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    body: &SqlInstancesRescheduleMaintenanceRequestBody,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/rescheduleMaintenance",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{project}/instances/{instance}/rescheduleMaintenance
/// Reschedules the maintenance on the given instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_projects_instances_reschedule_maintenance_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_projects_instances_reschedule_maintenance_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_projects_instances_reschedule_maintenance_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_projects_instances_reschedule_maintenance`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlProjectsInstancesRescheduleMaintenanceArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Request body.
    pub body: SqlInstancesRescheduleMaintenanceRequestBody,
}

/// GET v1/projects/{project}/instances/{instance}/rescheduleMaintenance
/// Reschedules the maintenance on the given instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_projects_instances_reschedule_maintenance_builder()` + `sql_projects_instances_reschedule_maintenance_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_projects_instances_reschedule_maintenance(
    client: &SimpleHttpClient,
    args: &SqlProjectsInstancesRescheduleMaintenanceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_projects_instances_reschedule_maintenance_builder(
        client,
        &args.project,
        &args.instance,
        &args.body,
    )?;
    sql_projects_instances_reschedule_maintenance_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/resetReplicaSize
/// Reset Replica Size to primary instance disk size.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_projects_instances_reset_replica_size_execute()` to send, or `sql_projects_instances_reset_replica_size` for simplest API.

pub fn sql_projects_instances_reset_replica_size_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    body: &SqlInstancesResetReplicaSizeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/resetReplicaSize",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{project}/instances/{instance}/resetReplicaSize
/// Reset Replica Size to primary instance disk size.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_projects_instances_reset_replica_size_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_projects_instances_reset_replica_size_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_projects_instances_reset_replica_size_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_projects_instances_reset_replica_size`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlProjectsInstancesResetReplicaSizeArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Request body.
    pub body: SqlInstancesResetReplicaSizeRequest,
}

/// GET v1/projects/{project}/instances/{instance}/resetReplicaSize
/// Reset Replica Size to primary instance disk size.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_projects_instances_reset_replica_size_builder()` + `sql_projects_instances_reset_replica_size_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_projects_instances_reset_replica_size(
    client: &SimpleHttpClient,
    args: &SqlProjectsInstancesResetReplicaSizeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_projects_instances_reset_replica_size_builder(
        client,
        &args.project,
        &args.instance,
        &args.body,
    )?;
    sql_projects_instances_reset_replica_size_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/startExternalSync
/// Start External primary instance migration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_projects_instances_start_external_sync_execute()` to send, or `sql_projects_instances_start_external_sync` for simplest API.

pub fn sql_projects_instances_start_external_sync_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    body: &SqlInstancesStartExternalSyncRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/startExternalSync",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{project}/instances/{instance}/startExternalSync
/// Start External primary instance migration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_projects_instances_start_external_sync_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_projects_instances_start_external_sync_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_projects_instances_start_external_sync_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_projects_instances_start_external_sync`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlProjectsInstancesStartExternalSyncArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Request body.
    pub body: SqlInstancesStartExternalSyncRequest,
}

/// GET v1/projects/{project}/instances/{instance}/startExternalSync
/// Start External primary instance migration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_projects_instances_start_external_sync_builder()` + `sql_projects_instances_start_external_sync_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_projects_instances_start_external_sync(
    client: &SimpleHttpClient,
    args: &SqlProjectsInstancesStartExternalSyncArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_projects_instances_start_external_sync_builder(
        client,
        &args.project,
        &args.instance,
        &args.body,
    )?;
    sql_projects_instances_start_external_sync_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/verifyExternalSyncSettings
/// Verify External primary instance external sync settings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_projects_instances_verify_external_sync_settings_execute()` to send, or `sql_projects_instances_verify_external_sync_settings` for simplest API.

pub fn sql_projects_instances_verify_external_sync_settings_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    body: &SqlInstancesVerifyExternalSyncSettingsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/verifyExternalSyncSettings",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{project}/instances/{instance}/verifyExternalSyncSettings
/// Verify External primary instance external sync settings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_projects_instances_verify_external_sync_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_projects_instances_verify_external_sync_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_projects_instances_verify_external_sync_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SqlInstancesVerifyExternalSyncSettingsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SqlInstancesVerifyExternalSyncSettingsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_projects_instances_verify_external_sync_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlProjectsInstancesVerifyExternalSyncSettingsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Request body.
    pub body: SqlInstancesVerifyExternalSyncSettingsRequest,
}

/// GET v1/projects/{project}/instances/{instance}/verifyExternalSyncSettings
/// Verify External primary instance external sync settings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_projects_instances_verify_external_sync_settings_builder()` + `sql_projects_instances_verify_external_sync_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_projects_instances_verify_external_sync_settings(
    client: &SimpleHttpClient,
    args: &SqlProjectsInstancesVerifyExternalSyncSettingsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SqlInstancesVerifyExternalSyncSettingsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sql_projects_instances_verify_external_sync_settings_builder(
        client,
        &args.project,
        &args.instance,
        &args.body,
    )?;
    sql_projects_instances_verify_external_sync_settings_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/createEphemeral
/// Generates a short-lived X509 certificate containing the provided public key and signed by a private key specific to the target instance. Users may use the certificate to authenticate as themselves when connecting to the database.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_ssl_certs_create_ephemeral_execute()` to send, or `sql_ssl_certs_create_ephemeral` for simplest API.

pub fn sql_ssl_certs_create_ephemeral_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    body: &SslCertsCreateEphemeralRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/createEphemeral",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{project}/instances/{instance}/createEphemeral
/// Generates a short-lived X509 certificate containing the provided public key and signed by a private key specific to the target instance. Users may use the certificate to authenticate as themselves when connecting to the database.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_ssl_certs_create_ephemeral_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_ssl_certs_create_ephemeral_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_ssl_certs_create_ephemeral_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslCert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslCert = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_ssl_certs_create_ephemeral`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlSslCertsCreateEphemeralArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Request body.
    pub body: SslCertsCreateEphemeralRequest,
}

/// GET v1/projects/{project}/instances/{instance}/createEphemeral
/// Generates a short-lived X509 certificate containing the provided public key and signed by a private key specific to the target instance. Users may use the certificate to authenticate as themselves when connecting to the database.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_ssl_certs_create_ephemeral_builder()` + `sql_ssl_certs_create_ephemeral_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_ssl_certs_create_ephemeral(
    client: &SimpleHttpClient,
    args: &SqlSslCertsCreateEphemeralArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslCert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        sql_ssl_certs_create_ephemeral_builder(client, &args.project, &args.instance, &args.body)?;
    sql_ssl_certs_create_ephemeral_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/sslCerts/{sha1Fingerprint}
/// Deletes the SSL certificate. For First Generation instances, the certificate remains valid until the instance is restarted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_ssl_certs_delete_execute()` to send, or `sql_ssl_certs_delete` for simplest API.

pub fn sql_ssl_certs_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    sha1Fingerprint: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/sslCerts/{}",
        project, instance, sha1Fingerprint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/sslCerts/{sha1Fingerprint}
/// Deletes the SSL certificate. For First Generation instances, the certificate remains valid until the instance is restarted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_ssl_certs_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_ssl_certs_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_ssl_certs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_ssl_certs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlSslCertsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Path parameter: sha1Fingerprint
    pub sha1Fingerprint: String,
}

/// GET v1/projects/{project}/instances/{instance}/sslCerts/{sha1Fingerprint}
/// Deletes the SSL certificate. For First Generation instances, the certificate remains valid until the instance is restarted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_ssl_certs_delete_builder()` + `sql_ssl_certs_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_ssl_certs_delete(
    client: &SimpleHttpClient,
    args: &SqlSslCertsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        sql_ssl_certs_delete_builder(client, &args.project, &args.instance, &args.sha1Fingerprint)?;
    sql_ssl_certs_delete_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/sslCerts/{sha1Fingerprint}
/// Retrieves a particular SSL certificate. Does not include the private key (required for usage). The private key must be saved from the response to initial creation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_ssl_certs_get_execute()` to send, or `sql_ssl_certs_get` for simplest API.

pub fn sql_ssl_certs_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    sha1Fingerprint: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/sslCerts/{}",
        project, instance, sha1Fingerprint,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/sslCerts/{sha1Fingerprint}
/// Retrieves a particular SSL certificate. Does not include the private key (required for usage). The private key must be saved from the response to initial creation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_ssl_certs_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_ssl_certs_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_ssl_certs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslCert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslCert = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_ssl_certs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlSslCertsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Path parameter: sha1Fingerprint
    pub sha1Fingerprint: String,
}

/// GET v1/projects/{project}/instances/{instance}/sslCerts/{sha1Fingerprint}
/// Retrieves a particular SSL certificate. Does not include the private key (required for usage). The private key must be saved from the response to initial creation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_ssl_certs_get_builder()` + `sql_ssl_certs_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_ssl_certs_get(
    client: &SimpleHttpClient,
    args: &SqlSslCertsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslCert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        sql_ssl_certs_get_builder(client, &args.project, &args.instance, &args.sha1Fingerprint)?;
    sql_ssl_certs_get_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/sslCerts
/// Creates an SSL certificate and returns it along with the private key and server certificate authority. The new certificate will not be usable until the instance is restarted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_ssl_certs_insert_execute()` to send, or `sql_ssl_certs_insert` for simplest API.

pub fn sql_ssl_certs_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    body: &SslCertsInsertRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/sslCerts",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{project}/instances/{instance}/sslCerts
/// Creates an SSL certificate and returns it along with the private key and server certificate authority. The new certificate will not be usable until the instance is restarted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_ssl_certs_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_ssl_certs_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_ssl_certs_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslCertsInsertResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslCertsInsertResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_ssl_certs_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlSslCertsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Request body.
    pub body: SslCertsInsertRequest,
}

/// GET v1/projects/{project}/instances/{instance}/sslCerts
/// Creates an SSL certificate and returns it along with the private key and server certificate authority. The new certificate will not be usable until the instance is restarted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_ssl_certs_insert_builder()` + `sql_ssl_certs_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_ssl_certs_insert(
    client: &SimpleHttpClient,
    args: &SqlSslCertsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslCertsInsertResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sql_ssl_certs_insert_builder(client, &args.project, &args.instance, &args.body)?;
    sql_ssl_certs_insert_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/sslCerts
/// Lists all of the current SSL certificates for the instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_ssl_certs_list_execute()` to send, or `sql_ssl_certs_list` for simplest API.

pub fn sql_ssl_certs_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/sslCerts",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/sslCerts
/// Lists all of the current SSL certificates for the instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_ssl_certs_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_ssl_certs_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_ssl_certs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslCertsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslCertsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_ssl_certs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlSslCertsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// GET v1/projects/{project}/instances/{instance}/sslCerts
/// Lists all of the current SSL certificates for the instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_ssl_certs_list_builder()` + `sql_ssl_certs_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_ssl_certs_list(
    client: &SimpleHttpClient,
    args: &SqlSslCertsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslCertsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sql_ssl_certs_list_builder(client, &args.project, &args.instance)?;
    sql_ssl_certs_list_execute(builder)
}

/// GET v1/projects/{project}/tiers
/// Lists all available machine types (tiers) for Cloud SQL, for example, db-custom-1-3840. For more information, see <https://cloud.google.`com/sql/pricing`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_tiers_list_execute()` to send, or `sql_tiers_list` for simplest API.

pub fn sql_tiers_list_builder(
    client: &SimpleHttpClient,
    project: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/tiers",
        project,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/tiers
/// Lists all available machine types (tiers) for Cloud SQL, for example, db-custom-1-3840. For more information, see <https://cloud.google.`com/sql/pricing`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_tiers_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_tiers_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_tiers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TiersListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TiersListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_tiers_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlTiersListArgs {
    /// Path parameter: project
    pub project: String,
}

/// GET v1/projects/{project}/tiers
/// Lists all available machine types (tiers) for Cloud SQL, for example, db-custom-1-3840. For more information, see <https://cloud.google.`com/sql/pricing`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_tiers_list_builder()` + `sql_tiers_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_tiers_list(
    client: &SimpleHttpClient,
    args: &SqlTiersListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TiersListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sql_tiers_list_builder(client, &args.project)?;
    sql_tiers_list_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/users
/// Deletes a user from a Cloud SQL instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_users_delete_execute()` to send, or `sql_users_delete` for simplest API.

pub fn sql_users_delete_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    host: Option<&str>,
    name: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/users",
        project, instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = host {
        query_parts.push(format!("host={}", val));
    }
    if let Some(val) = name {
        query_parts.push(format!("name={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/users
/// Deletes a user from a Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_users_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_users_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_users_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_users_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlUsersDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: host
    pub host: Option<String>,
    /// Query parameter: name
    pub name: Option<String>,
}

/// GET v1/projects/{project}/instances/{instance}/users
/// Deletes a user from a Cloud SQL instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_users_delete_builder()` + `sql_users_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_users_delete(
    client: &SimpleHttpClient,
    args: &SqlUsersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_users_delete_builder(
        client,
        &args.project,
        &args.instance,
        args.host.as_deref(),
        args.name.as_deref(),
    )?;
    sql_users_delete_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/users/{name}
/// Retrieves a resource containing information about a user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_users_get_execute()` to send, or `sql_users_get` for simplest API.

pub fn sql_users_get_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    name: &str,
    host: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/users/{}",
        project, instance, name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = host {
        query_parts.push(format!("host={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/users/{name}
/// Retrieves a resource containing information about a user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_users_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_users_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_users_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<User>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: User = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_users_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlUsersGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Path parameter: name
    pub name: String,
    /// Query parameter: host
    pub host: Option<String>,
}

/// GET v1/projects/{project}/instances/{instance}/users/{name}
/// Retrieves a resource containing information about a user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_users_get_builder()` + `sql_users_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_users_get(
    client: &SimpleHttpClient,
    args: &SqlUsersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<User>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_users_get_builder(
        client,
        &args.project,
        &args.instance,
        &args.name,
        args.host.as_deref(),
    )?;
    sql_users_get_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/users
/// Creates a new user in a Cloud SQL instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_users_insert_execute()` to send, or `sql_users_insert` for simplest API.

pub fn sql_users_insert_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    body: &User,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/users",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{project}/instances/{instance}/users
/// Creates a new user in a Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_users_insert_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_users_insert_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_users_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_users_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlUsersInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Request body.
    pub body: User,
}

/// GET v1/projects/{project}/instances/{instance}/users
/// Creates a new user in a Cloud SQL instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_users_insert_builder()` + `sql_users_insert_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_users_insert(
    client: &SimpleHttpClient,
    args: &SqlUsersInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_users_insert_builder(client, &args.project, &args.instance, &args.body)?;
    sql_users_insert_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/users
/// Lists users in the specified Cloud SQL instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_users_list_execute()` to send, or `sql_users_list` for simplest API.

pub fn sql_users_list_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/users",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/users
/// Lists users in the specified Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_users_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_users_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_users_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UsersListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UsersListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_users_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlUsersListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// GET v1/projects/{project}/instances/{instance}/users
/// Lists users in the specified Cloud SQL instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_users_list_builder()` + `sql_users_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_users_list(
    client: &SimpleHttpClient,
    args: &SqlUsersListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UsersListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sql_users_list_builder(client, &args.project, &args.instance)?;
    sql_users_list_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/users
/// Updates an existing user in a Cloud SQL instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_users_update_execute()` to send, or `sql_users_update` for simplest API.

pub fn sql_users_update_builder(
    client: &SimpleHttpClient,
    project: &str,
    instance: &str,
    databaseRoles: Option<&str>,
    host: Option<&str>,
    name: Option<&str>,
    revokeExistingRoles: Option<bool>,
    body: &User,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/users",
        project, instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = databaseRoles {
        query_parts.push(format!("databaseRoles={}", val));
    }
    if let Some(val) = host {
        query_parts.push(format!("host={}", val));
    }
    if let Some(val) = name {
        query_parts.push(format!("name={}", val));
    }
    if let Some(val) = revokeExistingRoles {
        query_parts.push(format!("revokeExistingRoles={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{project}/instances/{instance}/users
/// Updates an existing user in a Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sql_users_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sql_users_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_users_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_users_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlUsersUpdateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: databaseRoles
    pub databaseRoles: Option<String>,
    /// Query parameter: host
    pub host: Option<String>,
    /// Query parameter: name
    pub name: Option<String>,
    /// Query parameter: revokeExistingRoles
    pub revokeExistingRoles: Option<bool>,
    /// Request body.
    pub body: User,
}

/// GET v1/projects/{project}/instances/{instance}/users
/// Updates an existing user in a Cloud SQL instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_users_update_builder()` + `sql_users_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_users_update(
    client: &SimpleHttpClient,
    args: &SqlUsersUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_users_update_builder(
        client,
        &args.project,
        &args.instance,
        args.databaseRoles.as_deref(),
        args.host.as_deref(),
        args.name.as_deref(),
        args.revokeExistingRoles,
        &args.body,
    )?;
    sql_users_update_execute(builder)
}
