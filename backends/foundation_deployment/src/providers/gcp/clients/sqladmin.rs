//! Auto-generated API clients for gcp/sqladmin.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST v1/projects/{projectsId}/backups
/// Creates a backup for a Cloud SQL instance. This API can be used only to create on-demand backups.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_backups_create_backup_execute()` to send, or `sql_backups_create_backup` for simplest API.

pub fn sql_backups_create_backup_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/backups",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/backups
/// Creates a backup for a Cloud SQL instance. This API can be used only to create on-demand backups.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_backups_create_backup_execute()` or `sql_backups_create_backup`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_backups_create_backup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_backups_create_backup_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/backups
/// Creates a backup for a Cloud SQL instance. This API can be used only to create on-demand backups.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_backups_create_backup_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_backups_create_backup_task()`.
/// For the simplest API, use `sql_backups_create_backup()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_backups_create_backup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_backups_create_backup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_backups_create_backup_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_backups_create_backup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlBackupsCreateBackupArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}/backups
/// Creates a backup for a Cloud SQL instance. This API can be used only to create on-demand backups.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_backups_create_backup_builder()` + `sql_backups_create_backup_execute()`.
/// For task-level control, use `sql_backups_create_backup_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_backups_create_backup(
    client: &SimpleHttpClient,
    args: &SqlBackupsCreateBackupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_backups_create_backup_builder(client, &args.parent)?;
    sql_backups_create_backup_execute(builder)
}

/// DELETE v1/projects/{projectsId}/backups/{backupsId}
/// Deletes the backup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_backups_delete_backup_execute()` to send, or `sql_backups_delete_backup` for simplest API.

pub fn sql_backups_delete_backup_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/backups/{backupsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/backups/{backupsId}
/// Deletes the backup.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_backups_delete_backup_execute()` or `sql_backups_delete_backup`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_backups_delete_backup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_backups_delete_backup_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/backups/{backupsId}
/// Deletes the backup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_backups_delete_backup_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_backups_delete_backup_task()`.
/// For the simplest API, use `sql_backups_delete_backup()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_backups_delete_backup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_backups_delete_backup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_backups_delete_backup_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_backups_delete_backup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlBackupsDeleteBackupArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/backups/{backupsId}
/// Deletes the backup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_backups_delete_backup_builder()` + `sql_backups_delete_backup_execute()`.
/// For task-level control, use `sql_backups_delete_backup_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_backups_delete_backup(
    client: &SimpleHttpClient,
    args: &SqlBackupsDeleteBackupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_backups_delete_backup_builder(client, &args.name)?;
    sql_backups_delete_backup_execute(builder)
}

/// GET v1/projects/{projectsId}/backups/{backupsId}
/// Retrieves a resource containing information about a backup.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_backups_get_backup_execute()` to send, or `sql_backups_get_backup` for simplest API.

pub fn sql_backups_get_backup_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/backups/{backupsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/backups/{backupsId}
/// Retrieves a resource containing information about a backup.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_backups_get_backup_execute()` or `sql_backups_get_backup`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_backups_get_backup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_backups_get_backup_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Backup>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Backup = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/backups/{backupsId}
/// Retrieves a resource containing information about a backup.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_backups_get_backup_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_backups_get_backup_task()`.
/// For the simplest API, use `sql_backups_get_backup()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_backups_get_backup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_backups_get_backup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Backup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_backups_get_backup_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_backups_get_backup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlBackupsGetBackupArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/backups/{backupsId}
/// Retrieves a resource containing information about a backup.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_backups_get_backup_builder()` + `sql_backups_get_backup_execute()`.
/// For task-level control, use `sql_backups_get_backup_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_backups_get_backup(
    client: &SimpleHttpClient,
    args: &SqlBackupsGetBackupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Backup>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_backups_get_backup_builder(client, &args.name)?;
    sql_backups_get_backup_execute(builder)
}

/// GET v1/projects/{projectsId}/backups
/// Lists all backups associated with the project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_backups_list_backups_execute()` to send, or `sql_backups_list_backups` for simplest API.

pub fn sql_backups_list_backups_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/backups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/backups
/// Lists all backups associated with the project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_backups_list_backups_execute()` or `sql_backups_list_backups`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_backups_list_backups_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_backups_list_backups_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListBackupsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListBackupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/backups
/// Lists all backups associated with the project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_backups_list_backups_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_backups_list_backups_task()`.
/// For the simplest API, use `sql_backups_list_backups()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_backups_list_backups_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_backups_list_backups_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBackupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = sql_backups_list_backups_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_backups_list_backups`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlBackupsListBackupsArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/backups
/// Lists all backups associated with the project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_backups_list_backups_builder()` + `sql_backups_list_backups_execute()`.
/// For task-level control, use `sql_backups_list_backups_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_backups_list_backups(
    client: &SimpleHttpClient,
    args: &SqlBackupsListBackupsArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListBackupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sql_backups_list_backups_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    sql_backups_list_backups_execute(builder)
}

/// PATCH v1/projects/{projectsId}/backups/{backupsId}
/// Updates the retention period and description of the backup. You can use this API to update final backups only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_backups_update_backup_execute()` to send, or `sql_backups_update_backup` for simplest API.

pub fn sql_backups_update_backup_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/backups/{backupsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/backups/{backupsId}
/// Updates the retention period and description of the backup. You can use this API to update final backups only.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_backups_update_backup_execute()` or `sql_backups_update_backup`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_backups_update_backup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_backups_update_backup_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/backups/{backupsId}
/// Updates the retention period and description of the backup. You can use this API to update final backups only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_backups_update_backup_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_backups_update_backup_task()`.
/// For the simplest API, use `sql_backups_update_backup()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_backups_update_backup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_backups_update_backup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_backups_update_backup_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_backups_update_backup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlBackupsUpdateBackupArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/backups/{backupsId}
/// Updates the retention period and description of the backup. You can use this API to update final backups only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_backups_update_backup_builder()` + `sql_backups_update_backup_execute()`.
/// For task-level control, use `sql_backups_update_backup_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_backups_update_backup(
    client: &SimpleHttpClient,
    args: &SqlBackupsUpdateBackupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_backups_update_backup_builder(client, &args.name, &args.updateMask)?;
    sql_backups_update_backup_execute(builder)
}

/// DELETE v1/projects/{project}/instances/{instance}/backupRuns/{id}
/// Deletes the backup taken by a backup run.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_backup_runs_delete_execute()` to send, or `sql_backup_runs_delete` for simplest API.

pub fn sql_backup_runs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/backupRuns/{}",
        project, instance, id,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{project}/instances/{instance}/backupRuns/{id}
/// Deletes the backup taken by a backup run.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_backup_runs_delete_execute()` or `sql_backup_runs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_backup_runs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_backup_runs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{project}/instances/{instance}/backupRuns/{id}
/// Deletes the backup taken by a backup run.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_backup_runs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_backup_runs_delete_task()`.
/// For the simplest API, use `sql_backup_runs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_backup_runs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_backup_runs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_backup_runs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_backup_runs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlBackupRunsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Path parameter: id
    pub id: String,
}

/// DELETE v1/projects/{project}/instances/{instance}/backupRuns/{id}
/// Deletes the backup taken by a backup run.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_backup_runs_delete_builder()` + `sql_backup_runs_delete_execute()`.
/// For task-level control, use `sql_backup_runs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_backup_runs_delete(
    client: &SimpleHttpClient,
    args: &SqlBackupRunsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_backup_runs_delete_builder(client, &args.project, &args.instance, &args.id)?;
    sql_backup_runs_delete_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/backupRuns/{id}
/// Retrieves a resource containing information about a backup run.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_backup_runs_get_execute()` to send, or `sql_backup_runs_get` for simplest API.

pub fn sql_backup_runs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
    id: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/backupRuns/{}",
        project, instance, id,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/backupRuns/{id}
/// Retrieves a resource containing information about a backup run.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_backup_runs_get_execute()` or `sql_backup_runs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_backup_runs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_backup_runs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackupRun>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BackupRun = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{project}/instances/{instance}/backupRuns/{id}
/// Retrieves a resource containing information about a backup run.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_backup_runs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_backup_runs_get_task()`.
/// For the simplest API, use `sql_backup_runs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_backup_runs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_backup_runs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackupRun>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_backup_runs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_backup_runs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlBackupRunsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Path parameter: id
    pub id: String,
}

/// GET v1/projects/{project}/instances/{instance}/backupRuns/{id}
/// Retrieves a resource containing information about a backup run.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_backup_runs_get_builder()` + `sql_backup_runs_get_execute()`.
/// For task-level control, use `sql_backup_runs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_backup_runs_get(
    client: &SimpleHttpClient,
    args: &SqlBackupRunsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackupRun>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_backup_runs_get_builder(client, &args.project, &args.instance, &args.id)?;
    sql_backup_runs_get_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/backupRuns
/// Creates a new backup run on demand.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_backup_runs_insert_execute()` to send, or `sql_backup_runs_insert` for simplest API.

pub fn sql_backup_runs_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/backupRuns",
        project, instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/backupRuns
/// Creates a new backup run on demand.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_backup_runs_insert_execute()` or `sql_backup_runs_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_backup_runs_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_backup_runs_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/backupRuns
/// Creates a new backup run on demand.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_backup_runs_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_backup_runs_insert_task()`.
/// For the simplest API, use `sql_backup_runs_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_backup_runs_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_backup_runs_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_backup_runs_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_backup_runs_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlBackupRunsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// POST v1/projects/{project}/instances/{instance}/backupRuns
/// Creates a new backup run on demand.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_backup_runs_insert_builder()` + `sql_backup_runs_insert_execute()`.
/// For task-level control, use `sql_backup_runs_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_backup_runs_insert(
    client: &SimpleHttpClient,
    args: &SqlBackupRunsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_backup_runs_insert_builder(client, &args.project, &args.instance)?;
    sql_backup_runs_insert_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/backupRuns
/// Lists all backup runs associated with the project or a given instance and configuration in the reverse chronological order of the backup initiation time.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_backup_runs_list_execute()` to send, or `sql_backup_runs_list` for simplest API.

pub fn sql_backup_runs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
    maxResults: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/backupRuns",
        project, instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/backupRuns
/// Lists all backup runs associated with the project or a given instance and configuration in the reverse chronological order of the backup initiation time.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_backup_runs_list_execute()` or `sql_backup_runs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_backup_runs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_backup_runs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackupRunsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BackupRunsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{project}/instances/{instance}/backupRuns
/// Lists all backup runs associated with the project or a given instance and configuration in the reverse chronological order of the backup initiation time.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_backup_runs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_backup_runs_list_task()`.
/// For the simplest API, use `sql_backup_runs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_backup_runs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_backup_runs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackupRunsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = sql_backup_runs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_backup_runs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlBackupRunsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{project}/instances/{instance}/backupRuns
/// Lists all backup runs associated with the project or a given instance and configuration in the reverse chronological order of the backup initiation time.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_backup_runs_list_builder()` + `sql_backup_runs_list_execute()`.
/// For task-level control, use `sql_backup_runs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_backup_runs_list(
    client: &SimpleHttpClient,
    args: &SqlBackupRunsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BackupRunsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sql_backup_runs_list_builder(
        client,
        &args.project,
        &args.instance,
        &args.maxResults,
        &args.pageToken,
    )?;
    sql_backup_runs_list_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}:generateEphemeralCert
/// Generates a short-lived X509 certificate containing the provided public key and signed by a private key specific to the target instance. Users may use the certificate to authenticate as themselves when connecting to the database.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_connect_generate_ephemeral_execute()` to send, or `sql_connect_generate_ephemeral` for simplest API.

pub fn sql_connect_generate_ephemeral_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}:generateEphemeralCert",
        project, instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}:generateEphemeralCert
/// Generates a short-lived X509 certificate containing the provided public key and signed by a private key specific to the target instance. Users may use the certificate to authenticate as themselves when connecting to the database.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_connect_generate_ephemeral_execute()` or `sql_connect_generate_ephemeral`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_connect_generate_ephemeral_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_connect_generate_ephemeral_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GenerateEphemeralCertResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GenerateEphemeralCertResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}:generateEphemeralCert
/// Generates a short-lived X509 certificate containing the provided public key and signed by a private key specific to the target instance. Users may use the certificate to authenticate as themselves when connecting to the database.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_connect_generate_ephemeral_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_connect_generate_ephemeral_task()`.
/// For the simplest API, use `sql_connect_generate_ephemeral()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_connect_generate_ephemeral_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_connect_generate_ephemeral_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateEphemeralCertResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = sql_connect_generate_ephemeral_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_connect_generate_ephemeral`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlConnectGenerateEphemeralArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// POST v1/projects/{project}/instances/{instance}:generateEphemeralCert
/// Generates a short-lived X509 certificate containing the provided public key and signed by a private key specific to the target instance. Users may use the certificate to authenticate as themselves when connecting to the database.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_connect_generate_ephemeral_builder()` + `sql_connect_generate_ephemeral_execute()`.
/// For task-level control, use `sql_connect_generate_ephemeral_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_connect_generate_ephemeral(
    client: &SimpleHttpClient,
    args: &SqlConnectGenerateEphemeralArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GenerateEphemeralCertResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sql_connect_generate_ephemeral_builder(client, &args.project, &args.instance)?;
    sql_connect_generate_ephemeral_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/connectSettings
/// Retrieves connect settings about a Cloud SQL instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_connect_get_execute()` to send, or `sql_connect_get` for simplest API.

pub fn sql_connect_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
    readTime: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/connectSettings",
        project, instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = readTime.as_ref() {
        query_parts.push(format!("readTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/connectSettings
/// Retrieves connect settings about a Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_connect_get_execute()` or `sql_connect_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_connect_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_connect_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ConnectSettings>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ConnectSettings = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{project}/instances/{instance}/connectSettings
/// Retrieves connect settings about a Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_connect_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_connect_get_task()`.
/// For the simplest API, use `sql_connect_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_connect_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_connect_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConnectSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = sql_connect_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_connect_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlConnectGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: readTime
    pub readTime: Option<String>,
}

/// GET v1/projects/{project}/instances/{instance}/connectSettings
/// Retrieves connect settings about a Cloud SQL instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_connect_get_builder()` + `sql_connect_get_execute()`.
/// For task-level control, use `sql_connect_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_connect_get(
    client: &SimpleHttpClient,
    args: &SqlConnectGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ConnectSettings>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sql_connect_get_builder(client, &args.project, &args.instance, &args.readTime)?;
    sql_connect_get_execute(builder)
}

/// DELETE v1/projects/{project}/instances/{instance}/databases/{database}
/// Deletes a database from a Cloud SQL instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_databases_delete_execute()` to send, or `sql_databases_delete` for simplest API.

pub fn sql_databases_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
    database: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/databases/{}",
        project, instance, database,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{project}/instances/{instance}/databases/{database}
/// Deletes a database from a Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_databases_delete_execute()` or `sql_databases_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_databases_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_databases_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{project}/instances/{instance}/databases/{database}
/// Deletes a database from a Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_databases_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_databases_delete_task()`.
/// For the simplest API, use `sql_databases_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_databases_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_databases_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_databases_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_databases_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlDatabasesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Path parameter: database
    pub database: String,
}

/// DELETE v1/projects/{project}/instances/{instance}/databases/{database}
/// Deletes a database from a Cloud SQL instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_databases_delete_builder()` + `sql_databases_delete_execute()`.
/// For task-level control, use `sql_databases_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_databases_delete(
    client: &SimpleHttpClient,
    args: &SqlDatabasesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        sql_databases_delete_builder(client, &args.project, &args.instance, &args.database)?;
    sql_databases_delete_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/databases/{database}
/// Retrieves a resource containing information about a database inside a Cloud SQL instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_databases_get_execute()` to send, or `sql_databases_get` for simplest API.

pub fn sql_databases_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
    database: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/databases/{}",
        project, instance, database,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/databases/{database}
/// Retrieves a resource containing information about a database inside a Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_databases_get_execute()` or `sql_databases_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_databases_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_databases_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Database>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Database = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{project}/instances/{instance}/databases/{database}
/// Retrieves a resource containing information about a database inside a Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_databases_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_databases_get_task()`.
/// For the simplest API, use `sql_databases_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_databases_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_databases_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Database>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_databases_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_databases_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlDatabasesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Path parameter: database
    pub database: String,
}

/// GET v1/projects/{project}/instances/{instance}/databases/{database}
/// Retrieves a resource containing information about a database inside a Cloud SQL instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_databases_get_builder()` + `sql_databases_get_execute()`.
/// For task-level control, use `sql_databases_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_databases_get(
    client: &SimpleHttpClient,
    args: &SqlDatabasesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Database>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_databases_get_builder(client, &args.project, &args.instance, &args.database)?;
    sql_databases_get_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/databases
/// Inserts a resource containing information about a database inside a Cloud SQL instance. **Note:** You can''t modify the default character set and collation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_databases_insert_execute()` to send, or `sql_databases_insert` for simplest API.

pub fn sql_databases_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/databases",
        project, instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/databases
/// Inserts a resource containing information about a database inside a Cloud SQL instance. **Note:** You can''t modify the default character set and collation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_databases_insert_execute()` or `sql_databases_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_databases_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_databases_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/databases
/// Inserts a resource containing information about a database inside a Cloud SQL instance. **Note:** You can''t modify the default character set and collation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_databases_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_databases_insert_task()`.
/// For the simplest API, use `sql_databases_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_databases_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_databases_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_databases_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_databases_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlDatabasesInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// POST v1/projects/{project}/instances/{instance}/databases
/// Inserts a resource containing information about a database inside a Cloud SQL instance. **Note:** You can''t modify the default character set and collation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_databases_insert_builder()` + `sql_databases_insert_execute()`.
/// For task-level control, use `sql_databases_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_databases_insert(
    client: &SimpleHttpClient,
    args: &SqlDatabasesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_databases_insert_builder(client, &args.project, &args.instance)?;
    sql_databases_insert_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/databases
/// Lists databases in the specified Cloud SQL instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_databases_list_execute()` to send, or `sql_databases_list` for simplest API.

pub fn sql_databases_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/databases",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/databases
/// Lists databases in the specified Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_databases_list_execute()` or `sql_databases_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_databases_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_databases_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DatabasesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DatabasesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{project}/instances/{instance}/databases
/// Lists databases in the specified Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_databases_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_databases_list_task()`.
/// For the simplest API, use `sql_databases_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_databases_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_databases_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DatabasesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = sql_databases_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_databases_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlDatabasesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// GET v1/projects/{project}/instances/{instance}/databases
/// Lists databases in the specified Cloud SQL instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_databases_list_builder()` + `sql_databases_list_execute()`.
/// For task-level control, use `sql_databases_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_databases_list(
    client: &SimpleHttpClient,
    args: &SqlDatabasesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DatabasesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sql_databases_list_builder(client, &args.project, &args.instance)?;
    sql_databases_list_execute(builder)
}

/// PATCH v1/projects/{project}/instances/{instance}/databases/{database}
/// Partially updates a resource containing information about a database inside a Cloud SQL instance. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_databases_patch_execute()` to send, or `sql_databases_patch` for simplest API.

pub fn sql_databases_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
    database: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/databases/{}",
        project, instance, database,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{project}/instances/{instance}/databases/{database}
/// Partially updates a resource containing information about a database inside a Cloud SQL instance. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_databases_patch_execute()` or `sql_databases_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_databases_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_databases_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{project}/instances/{instance}/databases/{database}
/// Partially updates a resource containing information about a database inside a Cloud SQL instance. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_databases_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_databases_patch_task()`.
/// For the simplest API, use `sql_databases_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_databases_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_databases_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_databases_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_databases_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlDatabasesPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Path parameter: database
    pub database: String,
}

/// PATCH v1/projects/{project}/instances/{instance}/databases/{database}
/// Partially updates a resource containing information about a database inside a Cloud SQL instance. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_databases_patch_builder()` + `sql_databases_patch_execute()`.
/// For task-level control, use `sql_databases_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_databases_patch(
    client: &SimpleHttpClient,
    args: &SqlDatabasesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        sql_databases_patch_builder(client, &args.project, &args.instance, &args.database)?;
    sql_databases_patch_execute(builder)
}

/// PUT v1/projects/{project}/instances/{instance}/databases/{database}
/// Updates a resource containing information about a database inside a Cloud SQL instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_databases_update_execute()` to send, or `sql_databases_update` for simplest API.

pub fn sql_databases_update_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
    database: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/databases/{}",
        project, instance, database,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT v1/projects/{project}/instances/{instance}/databases/{database}
/// Updates a resource containing information about a database inside a Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_databases_update_execute()` or `sql_databases_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_databases_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_databases_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT v1/projects/{project}/instances/{instance}/databases/{database}
/// Updates a resource containing information about a database inside a Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_databases_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_databases_update_task()`.
/// For the simplest API, use `sql_databases_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_databases_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_databases_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_databases_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_databases_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlDatabasesUpdateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Path parameter: database
    pub database: String,
}

/// PUT v1/projects/{project}/instances/{instance}/databases/{database}
/// Updates a resource containing information about a database inside a Cloud SQL instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_databases_update_builder()` + `sql_databases_update_execute()`.
/// For task-level control, use `sql_databases_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_databases_update(
    client: &SimpleHttpClient,
    args: &SqlDatabasesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        sql_databases_update_builder(client, &args.project, &args.instance, &args.database)?;
    sql_databases_update_execute(builder)
}

/// GET v1/flags
/// Lists all available database flags for Cloud SQL instances.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_flags_list_execute()` to send, or `sql_flags_list` for simplest API.

pub fn sql_flags_list_builder<R>(
    client: &SimpleHttpClient<R>,
    databaseVersion: &Option<String>,
    flagScope: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://sqladmin.googleapis.com/v1/flags",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = databaseVersion.as_ref() {
        query_parts.push(format!("databaseVersion={}", val));
    }
    if let Some(val) = flagScope.as_ref() {
        query_parts.push(format!("flagScope={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/flags
/// Lists all available database flags for Cloud SQL instances.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_flags_list_execute()` or `sql_flags_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_flags_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_flags_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FlagsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: FlagsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/flags
/// Lists all available database flags for Cloud SQL instances.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_flags_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_flags_list_task()`.
/// For the simplest API, use `sql_flags_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_flags_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_flags_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FlagsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = sql_flags_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_flags_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlFlagsListArgs {
    /// Query parameter: databaseVersion
    pub databaseVersion: Option<String>,
    /// Query parameter: flagScope
    pub flagScope: Option<String>,
}

/// GET v1/flags
/// Lists all available database flags for Cloud SQL instances.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_flags_list_builder()` + `sql_flags_list_execute()`.
/// For task-level control, use `sql_flags_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_flags_list(
    client: &SimpleHttpClient,
    args: &SqlFlagsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<FlagsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sql_flags_list_builder(client, &args.databaseVersion, &args.flagScope)?;
    sql_flags_list_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/listEntraIdCertificates
/// Lists all versions of EntraID certificates for the specified instance. There can be up to three sets of certificates listed: the certificate that is currently in use, a future that has been added but not yet used to sign a certificate, and a certificate that has been rotated out.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_list_entra_id_certificates_execute()` to send, or `sql_instances_list_entra_id_certificates` for simplest API.

pub fn sql_instances_list_entra_id_certificates_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/listEntraIdCertificates",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/listEntraIdCertificates
/// Lists all versions of EntraID certificates for the specified instance. There can be up to three sets of certificates listed: the certificate that is currently in use, a future that has been added but not yet used to sign a certificate, and a certificate that has been rotated out.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_list_entra_id_certificates_execute()` or `sql_instances_list_entra_id_certificates`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_list_entra_id_certificates_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_list_entra_id_certificates_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstancesListEntraIdCertificatesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstancesListEntraIdCertificatesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{project}/instances/{instance}/listEntraIdCertificates
/// Lists all versions of EntraID certificates for the specified instance. There can be up to three sets of certificates listed: the certificate that is currently in use, a future that has been added but not yet used to sign a certificate, and a certificate that has been rotated out.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_list_entra_id_certificates_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_list_entra_id_certificates_task()`.
/// For the simplest API, use `sql_instances_list_entra_id_certificates()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_list_entra_id_certificates_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_list_entra_id_certificates_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstancesListEntraIdCertificatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = sql_instances_list_entra_id_certificates_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_list_entra_id_certificates`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesListEntraIdCertificatesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// GET v1/projects/{project}/instances/{instance}/listEntraIdCertificates
/// Lists all versions of EntraID certificates for the specified instance. There can be up to three sets of certificates listed: the certificate that is currently in use, a future that has been added but not yet used to sign a certificate, and a certificate that has been rotated out.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_list_entra_id_certificates_builder()` + `sql_instances_list_entra_id_certificates_execute()`.
/// For task-level control, use `sql_instances_list_entra_id_certificates_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_list_entra_id_certificates(
    client: &SimpleHttpClient,
    args: &SqlInstancesListEntraIdCertificatesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstancesListEntraIdCertificatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        sql_instances_list_entra_id_certificates_builder(client, &args.project, &args.instance)?;
    sql_instances_list_entra_id_certificates_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/listServerCertificates
/// Lists all versions of server certificates and certificate authorities (CAs) for the specified instance. There can be up to three sets of certs listed: the certificate that is currently in use, a future that has been added but not yet used to sign a certificate, and a certificate that has been rotated out. For instances not using Certificate Authority Service (CAS) server CA, use ListServerCas instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_list_server_certificates_execute()` to send, or `sql_instances_list_server_certificates` for simplest API.

pub fn sql_instances_list_server_certificates_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/listServerCertificates",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/listServerCertificates
/// Lists all versions of server certificates and certificate authorities (CAs) for the specified instance. There can be up to three sets of certs listed: the certificate that is currently in use, a future that has been added but not yet used to sign a certificate, and a certificate that has been rotated out. For instances not using Certificate Authority Service (CAS) server CA, use ListServerCas instead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_list_server_certificates_execute()` or `sql_instances_list_server_certificates`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_list_server_certificates_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_list_server_certificates_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstancesListServerCertificatesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstancesListServerCertificatesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{project}/instances/{instance}/listServerCertificates
/// Lists all versions of server certificates and certificate authorities (CAs) for the specified instance. There can be up to three sets of certs listed: the certificate that is currently in use, a future that has been added but not yet used to sign a certificate, and a certificate that has been rotated out. For instances not using Certificate Authority Service (CAS) server CA, use ListServerCas instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_list_server_certificates_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_list_server_certificates_task()`.
/// For the simplest API, use `sql_instances_list_server_certificates()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_list_server_certificates_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_list_server_certificates_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstancesListServerCertificatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = sql_instances_list_server_certificates_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_list_server_certificates`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesListServerCertificatesArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// GET v1/projects/{project}/instances/{instance}/listServerCertificates
/// Lists all versions of server certificates and certificate authorities (CAs) for the specified instance. There can be up to three sets of certs listed: the certificate that is currently in use, a future that has been added but not yet used to sign a certificate, and a certificate that has been rotated out. For instances not using Certificate Authority Service (CAS) server CA, use ListServerCas instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_list_server_certificates_builder()` + `sql_instances_list_server_certificates_execute()`.
/// For task-level control, use `sql_instances_list_server_certificates_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_list_server_certificates(
    client: &SimpleHttpClient,
    args: &SqlInstancesListServerCertificatesArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstancesListServerCertificatesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        sql_instances_list_server_certificates_builder(client, &args.project, &args.instance)?;
    sql_instances_list_server_certificates_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/rotateEntraIdCertificate
/// Rotates the server certificate version to one previously added with the `addEntraIdCertificate` method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_rotate_entra_id_certificate_execute()` to send, or `sql_instances_rotate_entra_id_certificate` for simplest API.

pub fn sql_instances_rotate_entra_id_certificate_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/rotateEntraIdCertificate",
        project, instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/rotateEntraIdCertificate
/// Rotates the server certificate version to one previously added with the `addEntraIdCertificate` method.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_rotate_entra_id_certificate_execute()` or `sql_instances_rotate_entra_id_certificate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_rotate_entra_id_certificate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_rotate_entra_id_certificate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/rotateEntraIdCertificate
/// Rotates the server certificate version to one previously added with the `addEntraIdCertificate` method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_rotate_entra_id_certificate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_rotate_entra_id_certificate_task()`.
/// For the simplest API, use `sql_instances_rotate_entra_id_certificate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_rotate_entra_id_certificate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_rotate_entra_id_certificate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_instances_rotate_entra_id_certificate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_rotate_entra_id_certificate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesRotateEntraIdCertificateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// POST v1/projects/{project}/instances/{instance}/rotateEntraIdCertificate
/// Rotates the server certificate version to one previously added with the `addEntraIdCertificate` method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_rotate_entra_id_certificate_builder()` + `sql_instances_rotate_entra_id_certificate_execute()`.
/// For task-level control, use `sql_instances_rotate_entra_id_certificate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_rotate_entra_id_certificate(
    client: &SimpleHttpClient,
    args: &SqlInstancesRotateEntraIdCertificateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        sql_instances_rotate_entra_id_certificate_builder(client, &args.project, &args.instance)?;
    sql_instances_rotate_entra_id_certificate_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/rotateServerCertificate
/// Rotates the server certificate version to one previously added with the `addServerCertificate` method. For instances not using Certificate Authority Service (CAS) server CA, use RotateServerCa instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_rotate_server_certificate_execute()` to send, or `sql_instances_rotate_server_certificate` for simplest API.

pub fn sql_instances_rotate_server_certificate_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/rotateServerCertificate",
        project, instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/rotateServerCertificate
/// Rotates the server certificate version to one previously added with the `addServerCertificate` method. For instances not using Certificate Authority Service (CAS) server CA, use RotateServerCa instead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_rotate_server_certificate_execute()` or `sql_instances_rotate_server_certificate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_rotate_server_certificate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_rotate_server_certificate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/rotateServerCertificate
/// Rotates the server certificate version to one previously added with the `addServerCertificate` method. For instances not using Certificate Authority Service (CAS) server CA, use RotateServerCa instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_rotate_server_certificate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_rotate_server_certificate_task()`.
/// For the simplest API, use `sql_instances_rotate_server_certificate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_rotate_server_certificate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_rotate_server_certificate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_instances_rotate_server_certificate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_rotate_server_certificate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesRotateServerCertificateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// POST v1/projects/{project}/instances/{instance}/rotateServerCertificate
/// Rotates the server certificate version to one previously added with the `addServerCertificate` method. For instances not using Certificate Authority Service (CAS) server CA, use RotateServerCa instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_rotate_server_certificate_builder()` + `sql_instances_rotate_server_certificate_execute()`.
/// For task-level control, use `sql_instances_rotate_server_certificate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_rotate_server_certificate(
    client: &SimpleHttpClient,
    args: &SqlInstancesRotateServerCertificateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        sql_instances_rotate_server_certificate_builder(client, &args.project, &args.instance)?;
    sql_instances_rotate_server_certificate_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/acquireSsrsLease
/// Acquire a lease for the setup of SQL Server Reporting Services (SSRS).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_acquire_ssrs_lease_execute()` to send, or `sql_instances_acquire_ssrs_lease` for simplest API.

pub fn sql_instances_acquire_ssrs_lease_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/acquireSsrsLease",
        project, instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/acquireSsrsLease
/// Acquire a lease for the setup of SQL Server Reporting Services (SSRS).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_acquire_ssrs_lease_execute()` or `sql_instances_acquire_ssrs_lease`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_acquire_ssrs_lease_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_acquire_ssrs_lease_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SqlInstancesAcquireSsrsLeaseResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SqlInstancesAcquireSsrsLeaseResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/acquireSsrsLease
/// Acquire a lease for the setup of SQL Server Reporting Services (SSRS).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_acquire_ssrs_lease_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_acquire_ssrs_lease_task()`.
/// For the simplest API, use `sql_instances_acquire_ssrs_lease()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_acquire_ssrs_lease_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_acquire_ssrs_lease_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SqlInstancesAcquireSsrsLeaseResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = sql_instances_acquire_ssrs_lease_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_acquire_ssrs_lease`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesAcquireSsrsLeaseArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// POST v1/projects/{project}/instances/{instance}/acquireSsrsLease
/// Acquire a lease for the setup of SQL Server Reporting Services (SSRS).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_acquire_ssrs_lease_builder()` + `sql_instances_acquire_ssrs_lease_execute()`.
/// For task-level control, use `sql_instances_acquire_ssrs_lease_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_acquire_ssrs_lease(
    client: &SimpleHttpClient,
    args: &SqlInstancesAcquireSsrsLeaseArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SqlInstancesAcquireSsrsLeaseResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sql_instances_acquire_ssrs_lease_builder(client, &args.project, &args.instance)?;
    sql_instances_acquire_ssrs_lease_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/addEntraIdCertificate
/// Adds a new Entra ID certificate for the specified instance. If an Entra ID certificate was previously added but never used in a certificate rotation, this operation replaces that version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_add_entra_id_certificate_execute()` to send, or `sql_instances_add_entra_id_certificate` for simplest API.

pub fn sql_instances_add_entra_id_certificate_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/addEntraIdCertificate",
        project, instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/addEntraIdCertificate
/// Adds a new Entra ID certificate for the specified instance. If an Entra ID certificate was previously added but never used in a certificate rotation, this operation replaces that version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_add_entra_id_certificate_execute()` or `sql_instances_add_entra_id_certificate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_add_entra_id_certificate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_add_entra_id_certificate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/addEntraIdCertificate
/// Adds a new Entra ID certificate for the specified instance. If an Entra ID certificate was previously added but never used in a certificate rotation, this operation replaces that version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_add_entra_id_certificate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_add_entra_id_certificate_task()`.
/// For the simplest API, use `sql_instances_add_entra_id_certificate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_add_entra_id_certificate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_add_entra_id_certificate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_instances_add_entra_id_certificate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_add_entra_id_certificate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesAddEntraIdCertificateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// POST v1/projects/{project}/instances/{instance}/addEntraIdCertificate
/// Adds a new Entra ID certificate for the specified instance. If an Entra ID certificate was previously added but never used in a certificate rotation, this operation replaces that version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_add_entra_id_certificate_builder()` + `sql_instances_add_entra_id_certificate_execute()`.
/// For task-level control, use `sql_instances_add_entra_id_certificate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_add_entra_id_certificate(
    client: &SimpleHttpClient,
    args: &SqlInstancesAddEntraIdCertificateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        sql_instances_add_entra_id_certificate_builder(client, &args.project, &args.instance)?;
    sql_instances_add_entra_id_certificate_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/addServerCa
/// Adds a new trusted Certificate Authority (CA) version for the specified instance. Required to prepare for a certificate rotation. If a CA version was previously added but never used in a certificate rotation, this operation replaces that version. There cannot be more than one CA version waiting to be rotated in. For instances that have enabled Certificate Authority Service (CAS) based server CA, use AddServerCertificate to add a new server certificate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_add_server_ca_execute()` to send, or `sql_instances_add_server_ca` for simplest API.

pub fn sql_instances_add_server_ca_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/addServerCa",
        project, instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/addServerCa
/// Adds a new trusted Certificate Authority (CA) version for the specified instance. Required to prepare for a certificate rotation. If a CA version was previously added but never used in a certificate rotation, this operation replaces that version. There cannot be more than one CA version waiting to be rotated in. For instances that have enabled Certificate Authority Service (CAS) based server CA, use AddServerCertificate to add a new server certificate.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_add_server_ca_execute()` or `sql_instances_add_server_ca`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_add_server_ca_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_add_server_ca_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/addServerCa
/// Adds a new trusted Certificate Authority (CA) version for the specified instance. Required to prepare for a certificate rotation. If a CA version was previously added but never used in a certificate rotation, this operation replaces that version. There cannot be more than one CA version waiting to be rotated in. For instances that have enabled Certificate Authority Service (CAS) based server CA, use AddServerCertificate to add a new server certificate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_add_server_ca_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_add_server_ca_task()`.
/// For the simplest API, use `sql_instances_add_server_ca()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_add_server_ca_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_add_server_ca_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_instances_add_server_ca_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_add_server_ca`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesAddServerCaArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// POST v1/projects/{project}/instances/{instance}/addServerCa
/// Adds a new trusted Certificate Authority (CA) version for the specified instance. Required to prepare for a certificate rotation. If a CA version was previously added but never used in a certificate rotation, this operation replaces that version. There cannot be more than one CA version waiting to be rotated in. For instances that have enabled Certificate Authority Service (CAS) based server CA, use AddServerCertificate to add a new server certificate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_add_server_ca_builder()` + `sql_instances_add_server_ca_execute()`.
/// For task-level control, use `sql_instances_add_server_ca_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_add_server_ca(
    client: &SimpleHttpClient,
    args: &SqlInstancesAddServerCaArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_add_server_ca_builder(client, &args.project, &args.instance)?;
    sql_instances_add_server_ca_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/addServerCertificate
/// Add a new trusted server certificate version for the specified instance using Certificate Authority Service (CAS) server CA. Required to prepare for a certificate rotation. If a server certificate version was previously added but never used in a certificate rotation, this operation replaces that version. There cannot be more than one certificate version waiting to be rotated in. For instances not using CAS server CA, use AddServerCa instead.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_add_server_certificate_execute()` to send, or `sql_instances_add_server_certificate` for simplest API.

pub fn sql_instances_add_server_certificate_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/addServerCertificate",
        project, instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/addServerCertificate
/// Add a new trusted server certificate version for the specified instance using Certificate Authority Service (CAS) server CA. Required to prepare for a certificate rotation. If a server certificate version was previously added but never used in a certificate rotation, this operation replaces that version. There cannot be more than one certificate version waiting to be rotated in. For instances not using CAS server CA, use AddServerCa instead.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_add_server_certificate_execute()` or `sql_instances_add_server_certificate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_add_server_certificate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_add_server_certificate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/addServerCertificate
/// Add a new trusted server certificate version for the specified instance using Certificate Authority Service (CAS) server CA. Required to prepare for a certificate rotation. If a server certificate version was previously added but never used in a certificate rotation, this operation replaces that version. There cannot be more than one certificate version waiting to be rotated in. For instances not using CAS server CA, use AddServerCa instead.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_add_server_certificate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_add_server_certificate_task()`.
/// For the simplest API, use `sql_instances_add_server_certificate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_add_server_certificate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_add_server_certificate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_instances_add_server_certificate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_add_server_certificate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesAddServerCertificateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// POST v1/projects/{project}/instances/{instance}/addServerCertificate
/// Add a new trusted server certificate version for the specified instance using Certificate Authority Service (CAS) server CA. Required to prepare for a certificate rotation. If a server certificate version was previously added but never used in a certificate rotation, this operation replaces that version. There cannot be more than one certificate version waiting to be rotated in. For instances not using CAS server CA, use AddServerCa instead.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_add_server_certificate_builder()` + `sql_instances_add_server_certificate_execute()`.
/// For task-level control, use `sql_instances_add_server_certificate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_add_server_certificate(
    client: &SimpleHttpClient,
    args: &SqlInstancesAddServerCertificateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        sql_instances_add_server_certificate_builder(client, &args.project, &args.instance)?;
    sql_instances_add_server_certificate_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/clone
/// Creates a Cloud SQL instance as a clone of the source instance. Using this operation might cause your instance to restart.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_clone_execute()` to send, or `sql_instances_clone` for simplest API.

pub fn sql_instances_clone_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/clone",
        project, instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/clone
/// Creates a Cloud SQL instance as a clone of the source instance. Using this operation might cause your instance to restart.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_clone_execute()` or `sql_instances_clone`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_clone_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_clone_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/clone
/// Creates a Cloud SQL instance as a clone of the source instance. Using this operation might cause your instance to restart.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_clone_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_clone_task()`.
/// For the simplest API, use `sql_instances_clone()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_clone_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_clone_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_instances_clone_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_clone`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesCloneArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// POST v1/projects/{project}/instances/{instance}/clone
/// Creates a Cloud SQL instance as a clone of the source instance. Using this operation might cause your instance to restart.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_clone_builder()` + `sql_instances_clone_execute()`.
/// For task-level control, use `sql_instances_clone_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_clone(
    client: &SimpleHttpClient,
    args: &SqlInstancesCloneArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_clone_builder(client, &args.project, &args.instance)?;
    sql_instances_clone_execute(builder)
}

/// DELETE v1/projects/{project}/instances/{instance}
/// Deletes a Cloud SQL instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_delete_execute()` to send, or `sql_instances_delete` for simplest API.

pub fn sql_instances_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
    enableFinalBackup: &Option<String>,
    finalBackupDescription: &Option<String>,
    finalBackupExpiryTime: &Option<String>,
    finalBackupTtlDays: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}",
        project, instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = enableFinalBackup.as_ref() {
        query_parts.push(format!("enableFinalBackup={}", val));
    }
    if let Some(val) = finalBackupDescription.as_ref() {
        query_parts.push(format!("finalBackupDescription={}", val));
    }
    if let Some(val) = finalBackupExpiryTime.as_ref() {
        query_parts.push(format!("finalBackupExpiryTime={}", val));
    }
    if let Some(val) = finalBackupTtlDays.as_ref() {
        query_parts.push(format!("finalBackupTtlDays={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{project}/instances/{instance}
/// Deletes a Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_delete_execute()` or `sql_instances_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{project}/instances/{instance}
/// Deletes a Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_delete_task()`.
/// For the simplest API, use `sql_instances_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_instances_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: enableFinalBackup
    pub enableFinalBackup: Option<String>,
    /// Query parameter: finalBackupDescription
    pub finalBackupDescription: Option<String>,
    /// Query parameter: finalBackupExpiryTime
    pub finalBackupExpiryTime: Option<String>,
    /// Query parameter: finalBackupTtlDays
    pub finalBackupTtlDays: Option<String>,
}

/// DELETE v1/projects/{project}/instances/{instance}
/// Deletes a Cloud SQL instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_delete_builder()` + `sql_instances_delete_execute()`.
/// For task-level control, use `sql_instances_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_delete(
    client: &SimpleHttpClient,
    args: &SqlInstancesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_delete_builder(
        client,
        &args.project,
        &args.instance,
        &args.enableFinalBackup,
        &args.finalBackupDescription,
        &args.finalBackupExpiryTime,
        &args.finalBackupTtlDays,
    )?;
    sql_instances_delete_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/demote
/// Demotes an existing standalone instance to be a Cloud SQL read replica for an external database server.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_demote_execute()` to send, or `sql_instances_demote` for simplest API.

pub fn sql_instances_demote_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/demote",
        project, instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/demote
/// Demotes an existing standalone instance to be a Cloud SQL read replica for an external database server.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_demote_execute()` or `sql_instances_demote`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_demote_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_demote_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/demote
/// Demotes an existing standalone instance to be a Cloud SQL read replica for an external database server.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_demote_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_demote_task()`.
/// For the simplest API, use `sql_instances_demote()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_demote_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_demote_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_instances_demote_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_demote`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesDemoteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// POST v1/projects/{project}/instances/{instance}/demote
/// Demotes an existing standalone instance to be a Cloud SQL read replica for an external database server.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_demote_builder()` + `sql_instances_demote_execute()`.
/// For task-level control, use `sql_instances_demote_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_demote(
    client: &SimpleHttpClient,
    args: &SqlInstancesDemoteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_demote_builder(client, &args.project, &args.instance)?;
    sql_instances_demote_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/demoteMaster
/// Demotes the stand-alone instance to be a Cloud SQL read replica for an external database server.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_demote_master_execute()` to send, or `sql_instances_demote_master` for simplest API.

pub fn sql_instances_demote_master_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/demoteMaster",
        project, instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/demoteMaster
/// Demotes the stand-alone instance to be a Cloud SQL read replica for an external database server.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_demote_master_execute()` or `sql_instances_demote_master`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_demote_master_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_demote_master_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/demoteMaster
/// Demotes the stand-alone instance to be a Cloud SQL read replica for an external database server.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_demote_master_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_demote_master_task()`.
/// For the simplest API, use `sql_instances_demote_master()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_demote_master_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_demote_master_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_instances_demote_master_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_demote_master`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesDemoteMasterArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// POST v1/projects/{project}/instances/{instance}/demoteMaster
/// Demotes the stand-alone instance to be a Cloud SQL read replica for an external database server.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_demote_master_builder()` + `sql_instances_demote_master_execute()`.
/// For task-level control, use `sql_instances_demote_master_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_demote_master(
    client: &SimpleHttpClient,
    args: &SqlInstancesDemoteMasterArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_demote_master_builder(client, &args.project, &args.instance)?;
    sql_instances_demote_master_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/executeSql
/// Execute SQL statements.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_execute_sql_execute()` to send, or `sql_instances_execute_sql` for simplest API.

pub fn sql_instances_execute_sql_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/executeSql",
        project, instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/executeSql
/// Execute SQL statements.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_execute_sql_execute()` or `sql_instances_execute_sql`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_execute_sql_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_execute_sql_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SqlInstancesExecuteSqlResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SqlInstancesExecuteSqlResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/executeSql
/// Execute SQL statements.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_execute_sql_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_execute_sql_task()`.
/// For the simplest API, use `sql_instances_execute_sql()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_execute_sql_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_execute_sql_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SqlInstancesExecuteSqlResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = sql_instances_execute_sql_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_execute_sql`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesExecuteSqlArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// POST v1/projects/{project}/instances/{instance}/executeSql
/// Execute SQL statements.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_execute_sql_builder()` + `sql_instances_execute_sql_execute()`.
/// For task-level control, use `sql_instances_execute_sql_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_execute_sql(
    client: &SimpleHttpClient,
    args: &SqlInstancesExecuteSqlArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SqlInstancesExecuteSqlResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sql_instances_execute_sql_builder(client, &args.project, &args.instance)?;
    sql_instances_execute_sql_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/export
/// Exports data from a Cloud SQL instance to a Cloud Storage bucket as a SQL dump or CSV file.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_export_execute()` to send, or `sql_instances_export` for simplest API.

pub fn sql_instances_export_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/export",
        project, instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/export
/// Exports data from a Cloud SQL instance to a Cloud Storage bucket as a SQL dump or CSV file.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_export_execute()` or `sql_instances_export`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_export_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/export
/// Exports data from a Cloud SQL instance to a Cloud Storage bucket as a SQL dump or CSV file.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_export_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_export_task()`.
/// For the simplest API, use `sql_instances_export()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_export_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_export_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_instances_export_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_export`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesExportArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// POST v1/projects/{project}/instances/{instance}/export
/// Exports data from a Cloud SQL instance to a Cloud Storage bucket as a SQL dump or CSV file.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_export_builder()` + `sql_instances_export_execute()`.
/// For task-level control, use `sql_instances_export_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_export(
    client: &SimpleHttpClient,
    args: &SqlInstancesExportArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_export_builder(client, &args.project, &args.instance)?;
    sql_instances_export_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/failover
/// Initiates a manual failover of a high availability (HA) primary instance to a standby instance, which becomes the primary instance. Users are then rerouted to the new primary. For more information, see the [Overview of high availability](<https://cloud.google.`com/sql/docs/mysql/high-availability`>) page in the Cloud SQL documentation. If using Legacy HA (MySQL only), this causes the instance to failover to its failover replica instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_failover_execute()` to send, or `sql_instances_failover` for simplest API.

pub fn sql_instances_failover_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/failover",
        project, instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/failover
/// Initiates a manual failover of a high availability (HA) primary instance to a standby instance, which becomes the primary instance. Users are then rerouted to the new primary. For more information, see the [Overview of high availability](<https://cloud.google.`com/sql/docs/mysql/high-availability`>) page in the Cloud SQL documentation. If using Legacy HA (MySQL only), this causes the instance to failover to its failover replica instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_failover_execute()` or `sql_instances_failover`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_failover_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_failover_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/failover
/// Initiates a manual failover of a high availability (HA) primary instance to a standby instance, which becomes the primary instance. Users are then rerouted to the new primary. For more information, see the [Overview of high availability](<https://cloud.google.`com/sql/docs/mysql/high-availability`>) page in the Cloud SQL documentation. If using Legacy HA (MySQL only), this causes the instance to failover to its failover replica instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_failover_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_failover_task()`.
/// For the simplest API, use `sql_instances_failover()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_failover_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_failover_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_instances_failover_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_failover`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesFailoverArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// POST v1/projects/{project}/instances/{instance}/failover
/// Initiates a manual failover of a high availability (HA) primary instance to a standby instance, which becomes the primary instance. Users are then rerouted to the new primary. For more information, see the [Overview of high availability](<https://cloud.google.`com/sql/docs/mysql/high-availability`>) page in the Cloud SQL documentation. If using Legacy HA (MySQL only), this causes the instance to failover to its failover replica instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_failover_builder()` + `sql_instances_failover_execute()`.
/// For task-level control, use `sql_instances_failover_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_failover(
    client: &SimpleHttpClient,
    args: &SqlInstancesFailoverArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_failover_builder(client, &args.project, &args.instance)?;
    sql_instances_failover_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}
/// Retrieves a resource containing information about a Cloud SQL instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_get_execute()` to send, or `sql_instances_get` for simplest API.

pub fn sql_instances_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}
/// Retrieves a resource containing information about a Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_get_execute()` or `sql_instances_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DatabaseInstance>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DatabaseInstance = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{project}/instances/{instance}
/// Retrieves a resource containing information about a Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_get_task()`.
/// For the simplest API, use `sql_instances_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DatabaseInstance>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = sql_instances_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// GET v1/projects/{project}/instances/{instance}
/// Retrieves a resource containing information about a Cloud SQL instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_get_builder()` + `sql_instances_get_execute()`.
/// For task-level control, use `sql_instances_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_get(
    client: &SimpleHttpClient,
    args: &SqlInstancesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DatabaseInstance>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sql_instances_get_builder(client, &args.project, &args.instance)?;
    sql_instances_get_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/import
/// Imports data into a Cloud SQL instance from a SQL dump or CSV file in Cloud Storage.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_import_execute()` to send, or `sql_instances_import` for simplest API.

pub fn sql_instances_import_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/import",
        project, instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/import
/// Imports data into a Cloud SQL instance from a SQL dump or CSV file in Cloud Storage.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_import_execute()` or `sql_instances_import`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_import_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/import
/// Imports data into a Cloud SQL instance from a SQL dump or CSV file in Cloud Storage.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_import_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_import_task()`.
/// For the simplest API, use `sql_instances_import()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_import_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_import_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_instances_import_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_import`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesImportArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// POST v1/projects/{project}/instances/{instance}/import
/// Imports data into a Cloud SQL instance from a SQL dump or CSV file in Cloud Storage.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_import_builder()` + `sql_instances_import_execute()`.
/// For task-level control, use `sql_instances_import_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_import(
    client: &SimpleHttpClient,
    args: &SqlInstancesImportArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_import_builder(client, &args.project, &args.instance)?;
    sql_instances_import_execute(builder)
}

/// POST v1/projects/{project}/instances
/// Creates a new Cloud SQL instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_insert_execute()` to send, or `sql_instances_insert` for simplest API.

pub fn sql_instances_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances",
        project,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances
/// Creates a new Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_insert_execute()` or `sql_instances_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances
/// Creates a new Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_insert_task()`.
/// For the simplest API, use `sql_instances_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_instances_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesInsertArgs {
    /// Path parameter: project
    pub project: String,
}

/// POST v1/projects/{project}/instances
/// Creates a new Cloud SQL instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_insert_builder()` + `sql_instances_insert_execute()`.
/// For task-level control, use `sql_instances_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_insert(
    client: &SimpleHttpClient,
    args: &SqlInstancesInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_insert_builder(client, &args.project)?;
    sql_instances_insert_execute(builder)
}

/// GET v1/projects/{project}/instances
/// Lists instances under a given project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_list_execute()` to send, or `sql_instances_list` for simplest API.

pub fn sql_instances_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    filter: &Option<String>,
    maxResults: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances
/// Lists instances under a given project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_list_execute()` or `sql_instances_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstancesListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstancesListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{project}/instances
/// Lists instances under a given project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_list_task()`.
/// For the simplest API, use `sql_instances_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstancesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = sql_instances_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{project}/instances
/// Lists instances under a given project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_list_builder()` + `sql_instances_list_execute()`.
/// For task-level control, use `sql_instances_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_list(
    client: &SimpleHttpClient,
    args: &SqlInstancesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InstancesListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sql_instances_list_builder(
        client,
        &args.project,
        &args.filter,
        &args.maxResults,
        &args.pageToken,
    )?;
    sql_instances_list_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/listServerCas
/// Lists all of the trusted Certificate Authorities (CAs) for the specified instance. There can be up to three CAs listed: the CA that was used to sign the certificate that is currently in use, a CA that has been added but not yet used to sign a certificate, and a CA used to sign a certificate that has previously rotated out.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_list_server_cas_execute()` to send, or `sql_instances_list_server_cas` for simplest API.

pub fn sql_instances_list_server_cas_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/listServerCas",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/listServerCas
/// Lists all of the trusted Certificate Authorities (CAs) for the specified instance. There can be up to three CAs listed: the CA that was used to sign the certificate that is currently in use, a CA that has been added but not yet used to sign a certificate, and a CA used to sign a certificate that has previously rotated out.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_list_server_cas_execute()` or `sql_instances_list_server_cas`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_list_server_cas_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_list_server_cas_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InstancesListServerCasResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InstancesListServerCasResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{project}/instances/{instance}/listServerCas
/// Lists all of the trusted Certificate Authorities (CAs) for the specified instance. There can be up to three CAs listed: the CA that was used to sign the certificate that is currently in use, a CA that has been added but not yet used to sign a certificate, and a CA used to sign a certificate that has previously rotated out.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_list_server_cas_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_list_server_cas_task()`.
/// For the simplest API, use `sql_instances_list_server_cas()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_list_server_cas_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_list_server_cas_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstancesListServerCasResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = sql_instances_list_server_cas_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_list_server_cas`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesListServerCasArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// GET v1/projects/{project}/instances/{instance}/listServerCas
/// Lists all of the trusted Certificate Authorities (CAs) for the specified instance. There can be up to three CAs listed: the CA that was used to sign the certificate that is currently in use, a CA that has been added but not yet used to sign a certificate, and a CA used to sign a certificate that has previously rotated out.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_list_server_cas_builder()` + `sql_instances_list_server_cas_execute()`.
/// For task-level control, use `sql_instances_list_server_cas_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_list_server_cas(
    client: &SimpleHttpClient,
    args: &SqlInstancesListServerCasArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<InstancesListServerCasResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sql_instances_list_server_cas_builder(client, &args.project, &args.instance)?;
    sql_instances_list_server_cas_execute(builder)
}

/// PATCH v1/projects/{project}/instances/{instance}
/// Partially updates settings of a Cloud SQL instance by merging the request with the current configuration. This method supports patch semantics.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_patch_execute()` to send, or `sql_instances_patch` for simplest API.

pub fn sql_instances_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}",
        project, instance,
    );

    // Build request
    let builder = client
        .patch(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{project}/instances/{instance}
/// Partially updates settings of a Cloud SQL instance by merging the request with the current configuration. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_patch_execute()` or `sql_instances_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{project}/instances/{instance}
/// Partially updates settings of a Cloud SQL instance by merging the request with the current configuration. This method supports patch semantics.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_patch_task()`.
/// For the simplest API, use `sql_instances_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_instances_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesPatchArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// PATCH v1/projects/{project}/instances/{instance}
/// Partially updates settings of a Cloud SQL instance by merging the request with the current configuration. This method supports patch semantics.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_patch_builder()` + `sql_instances_patch_execute()`.
/// For task-level control, use `sql_instances_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_patch(
    client: &SimpleHttpClient,
    args: &SqlInstancesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_patch_builder(client, &args.project, &args.instance)?;
    sql_instances_patch_execute(builder)
}

/// POST v1/projects/{projectsId}:pointInTimeRestore
/// Point in time restore for an instance managed by Google Cloud Backup and Disaster Recovery.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_point_in_time_restore_execute()` to send, or `sql_instances_point_in_time_restore` for simplest API.

pub fn sql_instances_point_in_time_restore_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}:pointInTimeRestore",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}:pointInTimeRestore
/// Point in time restore for an instance managed by Google Cloud Backup and Disaster Recovery.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_point_in_time_restore_execute()` or `sql_instances_point_in_time_restore`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_point_in_time_restore_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_point_in_time_restore_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}:pointInTimeRestore
/// Point in time restore for an instance managed by Google Cloud Backup and Disaster Recovery.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_point_in_time_restore_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_point_in_time_restore_task()`.
/// For the simplest API, use `sql_instances_point_in_time_restore()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_point_in_time_restore_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_point_in_time_restore_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_instances_point_in_time_restore_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_point_in_time_restore`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesPointInTimeRestoreArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1/projects/{projectsId}:pointInTimeRestore
/// Point in time restore for an instance managed by Google Cloud Backup and Disaster Recovery.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_point_in_time_restore_builder()` + `sql_instances_point_in_time_restore_execute()`.
/// For task-level control, use `sql_instances_point_in_time_restore_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_point_in_time_restore(
    client: &SimpleHttpClient,
    args: &SqlInstancesPointInTimeRestoreArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_point_in_time_restore_builder(client, &args.parent)?;
    sql_instances_point_in_time_restore_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/preCheckMajorVersionUpgrade
/// Execute MVU Pre-checks
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_pre_check_major_version_upgrade_execute()` to send, or `sql_instances_pre_check_major_version_upgrade` for simplest API.

pub fn sql_instances_pre_check_major_version_upgrade_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/preCheckMajorVersionUpgrade",
        project, instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/preCheckMajorVersionUpgrade
/// Execute MVU Pre-checks
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_pre_check_major_version_upgrade_execute()` or `sql_instances_pre_check_major_version_upgrade`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_pre_check_major_version_upgrade_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_pre_check_major_version_upgrade_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/preCheckMajorVersionUpgrade
/// Execute MVU Pre-checks
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_pre_check_major_version_upgrade_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_pre_check_major_version_upgrade_task()`.
/// For the simplest API, use `sql_instances_pre_check_major_version_upgrade()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_pre_check_major_version_upgrade_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_pre_check_major_version_upgrade_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_instances_pre_check_major_version_upgrade_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_pre_check_major_version_upgrade`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesPreCheckMajorVersionUpgradeArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// POST v1/projects/{project}/instances/{instance}/preCheckMajorVersionUpgrade
/// Execute MVU Pre-checks
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_pre_check_major_version_upgrade_builder()` + `sql_instances_pre_check_major_version_upgrade_execute()`.
/// For task-level control, use `sql_instances_pre_check_major_version_upgrade_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_pre_check_major_version_upgrade(
    client: &SimpleHttpClient,
    args: &SqlInstancesPreCheckMajorVersionUpgradeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_pre_check_major_version_upgrade_builder(
        client,
        &args.project,
        &args.instance,
    )?;
    sql_instances_pre_check_major_version_upgrade_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/promoteReplica
/// Promotes the read replica instance to be an independent Cloud SQL primary instance. Using this operation might cause your instance to restart.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_promote_replica_execute()` to send, or `sql_instances_promote_replica` for simplest API.

pub fn sql_instances_promote_replica_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
    failover: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/promoteReplica",
        project, instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = failover.as_ref() {
        query_parts.push(format!("failover={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/promoteReplica
/// Promotes the read replica instance to be an independent Cloud SQL primary instance. Using this operation might cause your instance to restart.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_promote_replica_execute()` or `sql_instances_promote_replica`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_promote_replica_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_promote_replica_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/promoteReplica
/// Promotes the read replica instance to be an independent Cloud SQL primary instance. Using this operation might cause your instance to restart.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_promote_replica_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_promote_replica_task()`.
/// For the simplest API, use `sql_instances_promote_replica()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_promote_replica_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_promote_replica_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_instances_promote_replica_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_promote_replica`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesPromoteReplicaArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: failover
    pub failover: Option<String>,
}

/// POST v1/projects/{project}/instances/{instance}/promoteReplica
/// Promotes the read replica instance to be an independent Cloud SQL primary instance. Using this operation might cause your instance to restart.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_promote_replica_builder()` + `sql_instances_promote_replica_execute()`.
/// For task-level control, use `sql_instances_promote_replica_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_promote_replica(
    client: &SimpleHttpClient,
    args: &SqlInstancesPromoteReplicaArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_promote_replica_builder(
        client,
        &args.project,
        &args.instance,
        &args.failover,
    )?;
    sql_instances_promote_replica_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/reencrypt
/// Reencrypt CMEK instance with latest key version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_reencrypt_execute()` to send, or `sql_instances_reencrypt` for simplest API.

pub fn sql_instances_reencrypt_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/reencrypt",
        project, instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/reencrypt
/// Reencrypt CMEK instance with latest key version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_reencrypt_execute()` or `sql_instances_reencrypt`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_reencrypt_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_reencrypt_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/reencrypt
/// Reencrypt CMEK instance with latest key version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_reencrypt_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_reencrypt_task()`.
/// For the simplest API, use `sql_instances_reencrypt()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_reencrypt_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_reencrypt_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_instances_reencrypt_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_reencrypt`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesReencryptArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// POST v1/projects/{project}/instances/{instance}/reencrypt
/// Reencrypt CMEK instance with latest key version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_reencrypt_builder()` + `sql_instances_reencrypt_execute()`.
/// For task-level control, use `sql_instances_reencrypt_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_reencrypt(
    client: &SimpleHttpClient,
    args: &SqlInstancesReencryptArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_reencrypt_builder(client, &args.project, &args.instance)?;
    sql_instances_reencrypt_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/releaseSsrsLease
/// Release a lease for the setup of SQL Server Reporting Services (SSRS).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_release_ssrs_lease_execute()` to send, or `sql_instances_release_ssrs_lease` for simplest API.

pub fn sql_instances_release_ssrs_lease_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/releaseSsrsLease",
        project, instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/releaseSsrsLease
/// Release a lease for the setup of SQL Server Reporting Services (SSRS).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_release_ssrs_lease_execute()` or `sql_instances_release_ssrs_lease`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_release_ssrs_lease_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_release_ssrs_lease_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SqlInstancesReleaseSsrsLeaseResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SqlInstancesReleaseSsrsLeaseResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/releaseSsrsLease
/// Release a lease for the setup of SQL Server Reporting Services (SSRS).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_release_ssrs_lease_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_release_ssrs_lease_task()`.
/// For the simplest API, use `sql_instances_release_ssrs_lease()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_release_ssrs_lease_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_release_ssrs_lease_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SqlInstancesReleaseSsrsLeaseResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = sql_instances_release_ssrs_lease_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_release_ssrs_lease`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesReleaseSsrsLeaseArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// POST v1/projects/{project}/instances/{instance}/releaseSsrsLease
/// Release a lease for the setup of SQL Server Reporting Services (SSRS).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_release_ssrs_lease_builder()` + `sql_instances_release_ssrs_lease_execute()`.
/// For task-level control, use `sql_instances_release_ssrs_lease_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_release_ssrs_lease(
    client: &SimpleHttpClient,
    args: &SqlInstancesReleaseSsrsLeaseArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SqlInstancesReleaseSsrsLeaseResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sql_instances_release_ssrs_lease_builder(client, &args.project, &args.instance)?;
    sql_instances_release_ssrs_lease_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/resetSslConfig
/// Deletes all client certificates and generates a new server SSL certificate for the instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_reset_ssl_config_execute()` to send, or `sql_instances_reset_ssl_config` for simplest API.

pub fn sql_instances_reset_ssl_config_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
    mode: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/resetSslConfig",
        project, instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = mode.as_ref() {
        query_parts.push(format!("mode={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/resetSslConfig
/// Deletes all client certificates and generates a new server SSL certificate for the instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_reset_ssl_config_execute()` or `sql_instances_reset_ssl_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_reset_ssl_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_reset_ssl_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/resetSslConfig
/// Deletes all client certificates and generates a new server SSL certificate for the instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_reset_ssl_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_reset_ssl_config_task()`.
/// For the simplest API, use `sql_instances_reset_ssl_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_reset_ssl_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_reset_ssl_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_instances_reset_ssl_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_reset_ssl_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesResetSslConfigArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: mode
    pub mode: Option<String>,
}

/// POST v1/projects/{project}/instances/{instance}/resetSslConfig
/// Deletes all client certificates and generates a new server SSL certificate for the instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_reset_ssl_config_builder()` + `sql_instances_reset_ssl_config_execute()`.
/// For task-level control, use `sql_instances_reset_ssl_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_reset_ssl_config(
    client: &SimpleHttpClient,
    args: &SqlInstancesResetSslConfigArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        sql_instances_reset_ssl_config_builder(client, &args.project, &args.instance, &args.mode)?;
    sql_instances_reset_ssl_config_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/restart
/// Restarts a Cloud SQL instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_restart_execute()` to send, or `sql_instances_restart` for simplest API.

pub fn sql_instances_restart_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/restart",
        project, instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/restart
/// Restarts a Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_restart_execute()` or `sql_instances_restart`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_restart_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_restart_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/restart
/// Restarts a Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_restart_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_restart_task()`.
/// For the simplest API, use `sql_instances_restart()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_restart_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_restart_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_instances_restart_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_restart`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesRestartArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// POST v1/projects/{project}/instances/{instance}/restart
/// Restarts a Cloud SQL instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_restart_builder()` + `sql_instances_restart_execute()`.
/// For task-level control, use `sql_instances_restart_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_restart(
    client: &SimpleHttpClient,
    args: &SqlInstancesRestartArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_restart_builder(client, &args.project, &args.instance)?;
    sql_instances_restart_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/restoreBackup
/// Restores a backup of a Cloud SQL instance. Using this operation might cause your instance to restart.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_restore_backup_execute()` to send, or `sql_instances_restore_backup` for simplest API.

pub fn sql_instances_restore_backup_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/restoreBackup",
        project, instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/restoreBackup
/// Restores a backup of a Cloud SQL instance. Using this operation might cause your instance to restart.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_restore_backup_execute()` or `sql_instances_restore_backup`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_restore_backup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_restore_backup_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/restoreBackup
/// Restores a backup of a Cloud SQL instance. Using this operation might cause your instance to restart.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_restore_backup_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_restore_backup_task()`.
/// For the simplest API, use `sql_instances_restore_backup()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_restore_backup_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_restore_backup_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_instances_restore_backup_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_restore_backup`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesRestoreBackupArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// POST v1/projects/{project}/instances/{instance}/restoreBackup
/// Restores a backup of a Cloud SQL instance. Using this operation might cause your instance to restart.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_restore_backup_builder()` + `sql_instances_restore_backup_execute()`.
/// For task-level control, use `sql_instances_restore_backup_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_restore_backup(
    client: &SimpleHttpClient,
    args: &SqlInstancesRestoreBackupArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_restore_backup_builder(client, &args.project, &args.instance)?;
    sql_instances_restore_backup_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/rotateServerCa
/// Rotates the server certificate to one signed by the Certificate Authority (CA) version previously added with the `addServerCA` method. For instances that have enabled Certificate Authority Service (CAS) based server CA, use RotateServerCertificate to rotate the server certificate.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_rotate_server_ca_execute()` to send, or `sql_instances_rotate_server_ca` for simplest API.

pub fn sql_instances_rotate_server_ca_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/rotateServerCa",
        project, instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/rotateServerCa
/// Rotates the server certificate to one signed by the Certificate Authority (CA) version previously added with the `addServerCA` method. For instances that have enabled Certificate Authority Service (CAS) based server CA, use RotateServerCertificate to rotate the server certificate.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_rotate_server_ca_execute()` or `sql_instances_rotate_server_ca`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_rotate_server_ca_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_rotate_server_ca_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/rotateServerCa
/// Rotates the server certificate to one signed by the Certificate Authority (CA) version previously added with the `addServerCA` method. For instances that have enabled Certificate Authority Service (CAS) based server CA, use RotateServerCertificate to rotate the server certificate.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_rotate_server_ca_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_rotate_server_ca_task()`.
/// For the simplest API, use `sql_instances_rotate_server_ca()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_rotate_server_ca_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_rotate_server_ca_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_instances_rotate_server_ca_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_rotate_server_ca`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesRotateServerCaArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// POST v1/projects/{project}/instances/{instance}/rotateServerCa
/// Rotates the server certificate to one signed by the Certificate Authority (CA) version previously added with the `addServerCA` method. For instances that have enabled Certificate Authority Service (CAS) based server CA, use RotateServerCertificate to rotate the server certificate.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_rotate_server_ca_builder()` + `sql_instances_rotate_server_ca_execute()`.
/// For task-level control, use `sql_instances_rotate_server_ca_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_rotate_server_ca(
    client: &SimpleHttpClient,
    args: &SqlInstancesRotateServerCaArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_rotate_server_ca_builder(client, &args.project, &args.instance)?;
    sql_instances_rotate_server_ca_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/startReplica
/// Starts the replication in the read replica instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_start_replica_execute()` to send, or `sql_instances_start_replica` for simplest API.

pub fn sql_instances_start_replica_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/startReplica",
        project, instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/startReplica
/// Starts the replication in the read replica instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_start_replica_execute()` or `sql_instances_start_replica`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_start_replica_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_start_replica_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/startReplica
/// Starts the replication in the read replica instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_start_replica_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_start_replica_task()`.
/// For the simplest API, use `sql_instances_start_replica()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_start_replica_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_start_replica_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_instances_start_replica_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_start_replica`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesStartReplicaArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// POST v1/projects/{project}/instances/{instance}/startReplica
/// Starts the replication in the read replica instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_start_replica_builder()` + `sql_instances_start_replica_execute()`.
/// For task-level control, use `sql_instances_start_replica_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_start_replica(
    client: &SimpleHttpClient,
    args: &SqlInstancesStartReplicaArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_start_replica_builder(client, &args.project, &args.instance)?;
    sql_instances_start_replica_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/stopReplica
/// Stops the replication in the read replica instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_stop_replica_execute()` to send, or `sql_instances_stop_replica` for simplest API.

pub fn sql_instances_stop_replica_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/stopReplica",
        project, instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/stopReplica
/// Stops the replication in the read replica instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_stop_replica_execute()` or `sql_instances_stop_replica`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_stop_replica_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_stop_replica_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/stopReplica
/// Stops the replication in the read replica instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_stop_replica_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_stop_replica_task()`.
/// For the simplest API, use `sql_instances_stop_replica()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_stop_replica_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_stop_replica_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_instances_stop_replica_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_stop_replica`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesStopReplicaArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// POST v1/projects/{project}/instances/{instance}/stopReplica
/// Stops the replication in the read replica instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_stop_replica_builder()` + `sql_instances_stop_replica_execute()`.
/// For task-level control, use `sql_instances_stop_replica_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_stop_replica(
    client: &SimpleHttpClient,
    args: &SqlInstancesStopReplicaArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_stop_replica_builder(client, &args.project, &args.instance)?;
    sql_instances_stop_replica_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/switchover
/// Switches over from the primary instance to the DR replica instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_switchover_execute()` to send, or `sql_instances_switchover` for simplest API.

pub fn sql_instances_switchover_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
    dbTimeout: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/switchover",
        project, instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = dbTimeout.as_ref() {
        query_parts.push(format!("dbTimeout={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/switchover
/// Switches over from the primary instance to the DR replica instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_switchover_execute()` or `sql_instances_switchover`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_switchover_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_switchover_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/switchover
/// Switches over from the primary instance to the DR replica instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_switchover_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_switchover_task()`.
/// For the simplest API, use `sql_instances_switchover()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_switchover_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_switchover_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_instances_switchover_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_switchover`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesSwitchoverArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: dbTimeout
    pub dbTimeout: Option<String>,
}

/// POST v1/projects/{project}/instances/{instance}/switchover
/// Switches over from the primary instance to the DR replica instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_switchover_builder()` + `sql_instances_switchover_execute()`.
/// For task-level control, use `sql_instances_switchover_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_switchover(
    client: &SimpleHttpClient,
    args: &SqlInstancesSwitchoverArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        sql_instances_switchover_builder(client, &args.project, &args.instance, &args.dbTimeout)?;
    sql_instances_switchover_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/truncateLog
/// Truncate MySQL general and slow query log tables MySQL only.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_truncate_log_execute()` to send, or `sql_instances_truncate_log` for simplest API.

pub fn sql_instances_truncate_log_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/truncateLog",
        project, instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/truncateLog
/// Truncate MySQL general and slow query log tables MySQL only.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_truncate_log_execute()` or `sql_instances_truncate_log`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_truncate_log_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_truncate_log_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/truncateLog
/// Truncate MySQL general and slow query log tables MySQL only.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_truncate_log_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_truncate_log_task()`.
/// For the simplest API, use `sql_instances_truncate_log()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_truncate_log_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_truncate_log_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_instances_truncate_log_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_truncate_log`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesTruncateLogArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// POST v1/projects/{project}/instances/{instance}/truncateLog
/// Truncate MySQL general and slow query log tables MySQL only.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_truncate_log_builder()` + `sql_instances_truncate_log_execute()`.
/// For task-level control, use `sql_instances_truncate_log_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_truncate_log(
    client: &SimpleHttpClient,
    args: &SqlInstancesTruncateLogArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_truncate_log_builder(client, &args.project, &args.instance)?;
    sql_instances_truncate_log_execute(builder)
}

/// PUT v1/projects/{project}/instances/{instance}
/// Updates settings of a Cloud SQL instance. Using this operation might cause your instance to restart.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_instances_update_execute()` to send, or `sql_instances_update` for simplest API.

pub fn sql_instances_update_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}",
        project, instance,
    );

    // Build request
    let builder = client
        .put(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT v1/projects/{project}/instances/{instance}
/// Updates settings of a Cloud SQL instance. Using this operation might cause your instance to restart.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_instances_update_execute()` or `sql_instances_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT v1/projects/{project}/instances/{instance}
/// Updates settings of a Cloud SQL instance. Using this operation might cause your instance to restart.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_instances_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_instances_update_task()`.
/// For the simplest API, use `sql_instances_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_instances_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_instances_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_instances_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_instances_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlInstancesUpdateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// PUT v1/projects/{project}/instances/{instance}
/// Updates settings of a Cloud SQL instance. Using this operation might cause your instance to restart.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_instances_update_builder()` + `sql_instances_update_execute()`.
/// For task-level control, use `sql_instances_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_instances_update(
    client: &SimpleHttpClient,
    args: &SqlInstancesUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_instances_update_builder(client, &args.project, &args.instance)?;
    sql_instances_update_execute(builder)
}

/// POST v1/projects/{project}/operations/{operation}/cancel
/// Cancels an instance operation that has been performed on an instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_operations_cancel_execute()` to send, or `sql_operations_cancel` for simplest API.

pub fn sql_operations_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    operation: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/operations/{}/cancel",
        project, operation,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/operations/{operation}/cancel
/// Cancels an instance operation that has been performed on an instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_operations_cancel_execute()` or `sql_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/operations/{operation}/cancel
/// Cancels an instance operation that has been performed on an instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_operations_cancel_task()`.
/// For the simplest API, use `sql_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlOperationsCancelArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: operation
    pub operation: String,
}

/// POST v1/projects/{project}/operations/{operation}/cancel
/// Cancels an instance operation that has been performed on an instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_operations_cancel_builder()` + `sql_operations_cancel_execute()`.
/// For task-level control, use `sql_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_operations_cancel(
    client: &SimpleHttpClient,
    args: &SqlOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_operations_cancel_builder(client, &args.project, &args.operation)?;
    sql_operations_cancel_execute(builder)
}

/// GET v1/projects/{project}/operations/{operation}
/// Retrieves an instance operation that has been performed on an instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_operations_get_execute()` to send, or `sql_operations_get` for simplest API.

pub fn sql_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    operation: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/operations/{}",
        project, operation,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/operations/{operation}
/// Retrieves an instance operation that has been performed on an instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_operations_get_execute()` or `sql_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{project}/operations/{operation}
/// Retrieves an instance operation that has been performed on an instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_operations_get_task()`.
/// For the simplest API, use `sql_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlOperationsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: operation
    pub operation: String,
}

/// GET v1/projects/{project}/operations/{operation}
/// Retrieves an instance operation that has been performed on an instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_operations_get_builder()` + `sql_operations_get_execute()`.
/// For task-level control, use `sql_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_operations_get(
    client: &SimpleHttpClient,
    args: &SqlOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_operations_get_builder(client, &args.project, &args.operation)?;
    sql_operations_get_execute(builder)
}

/// GET v1/projects/{project}/operations
/// Lists all instance operations that have been performed on the given Cloud SQL instance in the reverse chronological order of the start time.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_operations_list_execute()` to send, or `sql_operations_list` for simplest API.

pub fn sql_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &Option<String>,
    maxResults: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/operations",
        project,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = instance.as_ref() {
        query_parts.push(format!("instance={}", val));
    }
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/operations
/// Lists all instance operations that have been performed on the given Cloud SQL instance in the reverse chronological order of the start time.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_operations_list_execute()` or `sql_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OperationsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: OperationsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{project}/operations
/// Lists all instance operations that have been performed on the given Cloud SQL instance in the reverse chronological order of the start time.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_operations_list_task()`.
/// For the simplest API, use `sql_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OperationsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = sql_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlOperationsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Query parameter: instance
    pub instance: Option<String>,
    /// Query parameter: maxResults
    pub maxResults: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{project}/operations
/// Lists all instance operations that have been performed on the given Cloud SQL instance in the reverse chronological order of the start time.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_operations_list_builder()` + `sql_operations_list_execute()`.
/// For task-level control, use `sql_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_operations_list(
    client: &SimpleHttpClient,
    args: &SqlOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<OperationsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sql_operations_list_builder(
        client,
        &args.project,
        &args.instance,
        &args.maxResults,
        &args.pageToken,
    )?;
    sql_operations_list_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/getDiskShrinkConfig
/// Get Disk Shrink Config for a given instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_projects_instances_get_disk_shrink_config_execute()` to send, or `sql_projects_instances_get_disk_shrink_config` for simplest API.

pub fn sql_projects_instances_get_disk_shrink_config_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/getDiskShrinkConfig",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/getDiskShrinkConfig
/// Get Disk Shrink Config for a given instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_projects_instances_get_disk_shrink_config_execute()` or `sql_projects_instances_get_disk_shrink_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_projects_instances_get_disk_shrink_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_projects_instances_get_disk_shrink_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SqlInstancesGetDiskShrinkConfigResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SqlInstancesGetDiskShrinkConfigResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{project}/instances/{instance}/getDiskShrinkConfig
/// Get Disk Shrink Config for a given instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_projects_instances_get_disk_shrink_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_projects_instances_get_disk_shrink_config_task()`.
/// For the simplest API, use `sql_projects_instances_get_disk_shrink_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_projects_instances_get_disk_shrink_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_projects_instances_get_disk_shrink_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SqlInstancesGetDiskShrinkConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = sql_projects_instances_get_disk_shrink_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_projects_instances_get_disk_shrink_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlProjectsInstancesGetDiskShrinkConfigArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// GET v1/projects/{project}/instances/{instance}/getDiskShrinkConfig
/// Get Disk Shrink Config for a given instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_projects_instances_get_disk_shrink_config_builder()` + `sql_projects_instances_get_disk_shrink_config_execute()`.
/// For task-level control, use `sql_projects_instances_get_disk_shrink_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_projects_instances_get_disk_shrink_config(
    client: &SimpleHttpClient,
    args: &SqlProjectsInstancesGetDiskShrinkConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SqlInstancesGetDiskShrinkConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sql_projects_instances_get_disk_shrink_config_builder(
        client,
        &args.project,
        &args.instance,
    )?;
    sql_projects_instances_get_disk_shrink_config_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/getLatestRecoveryTime
/// Get Latest Recovery Time for a given instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_projects_instances_get_latest_recovery_time_execute()` to send, or `sql_projects_instances_get_latest_recovery_time` for simplest API.

pub fn sql_projects_instances_get_latest_recovery_time_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
    sourceInstanceDeletionTime: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/getLatestRecoveryTime",
        project, instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = sourceInstanceDeletionTime.as_ref() {
        query_parts.push(format!("sourceInstanceDeletionTime={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/getLatestRecoveryTime
/// Get Latest Recovery Time for a given instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_projects_instances_get_latest_recovery_time_execute()` or `sql_projects_instances_get_latest_recovery_time`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_projects_instances_get_latest_recovery_time_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_projects_instances_get_latest_recovery_time_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SqlInstancesGetLatestRecoveryTimeResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SqlInstancesGetLatestRecoveryTimeResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{project}/instances/{instance}/getLatestRecoveryTime
/// Get Latest Recovery Time for a given instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_projects_instances_get_latest_recovery_time_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_projects_instances_get_latest_recovery_time_task()`.
/// For the simplest API, use `sql_projects_instances_get_latest_recovery_time()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_projects_instances_get_latest_recovery_time_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_projects_instances_get_latest_recovery_time_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SqlInstancesGetLatestRecoveryTimeResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = sql_projects_instances_get_latest_recovery_time_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_projects_instances_get_latest_recovery_time`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlProjectsInstancesGetLatestRecoveryTimeArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: sourceInstanceDeletionTime
    pub sourceInstanceDeletionTime: Option<String>,
}

/// GET v1/projects/{project}/instances/{instance}/getLatestRecoveryTime
/// Get Latest Recovery Time for a given instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_projects_instances_get_latest_recovery_time_builder()` + `sql_projects_instances_get_latest_recovery_time_execute()`.
/// For task-level control, use `sql_projects_instances_get_latest_recovery_time_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_projects_instances_get_latest_recovery_time(
    client: &SimpleHttpClient,
    args: &SqlProjectsInstancesGetLatestRecoveryTimeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SqlInstancesGetLatestRecoveryTimeResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sql_projects_instances_get_latest_recovery_time_builder(
        client,
        &args.project,
        &args.instance,
        &args.sourceInstanceDeletionTime,
    )?;
    sql_projects_instances_get_latest_recovery_time_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/performDiskShrink
/// Perform Disk Shrink on primary instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_projects_instances_perform_disk_shrink_execute()` to send, or `sql_projects_instances_perform_disk_shrink` for simplest API.

pub fn sql_projects_instances_perform_disk_shrink_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/performDiskShrink",
        project, instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/performDiskShrink
/// Perform Disk Shrink on primary instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_projects_instances_perform_disk_shrink_execute()` or `sql_projects_instances_perform_disk_shrink`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_projects_instances_perform_disk_shrink_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_projects_instances_perform_disk_shrink_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/performDiskShrink
/// Perform Disk Shrink on primary instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_projects_instances_perform_disk_shrink_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_projects_instances_perform_disk_shrink_task()`.
/// For the simplest API, use `sql_projects_instances_perform_disk_shrink()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_projects_instances_perform_disk_shrink_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_projects_instances_perform_disk_shrink_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_projects_instances_perform_disk_shrink_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_projects_instances_perform_disk_shrink`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlProjectsInstancesPerformDiskShrinkArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// POST v1/projects/{project}/instances/{instance}/performDiskShrink
/// Perform Disk Shrink on primary instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_projects_instances_perform_disk_shrink_builder()` + `sql_projects_instances_perform_disk_shrink_execute()`.
/// For task-level control, use `sql_projects_instances_perform_disk_shrink_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_projects_instances_perform_disk_shrink(
    client: &SimpleHttpClient,
    args: &SqlProjectsInstancesPerformDiskShrinkArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        sql_projects_instances_perform_disk_shrink_builder(client, &args.project, &args.instance)?;
    sql_projects_instances_perform_disk_shrink_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/rescheduleMaintenance
/// Reschedules the maintenance on the given instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_projects_instances_reschedule_maintenance_execute()` to send, or `sql_projects_instances_reschedule_maintenance` for simplest API.

pub fn sql_projects_instances_reschedule_maintenance_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/rescheduleMaintenance",
        project, instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/rescheduleMaintenance
/// Reschedules the maintenance on the given instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_projects_instances_reschedule_maintenance_execute()` or `sql_projects_instances_reschedule_maintenance`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_projects_instances_reschedule_maintenance_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_projects_instances_reschedule_maintenance_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/rescheduleMaintenance
/// Reschedules the maintenance on the given instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_projects_instances_reschedule_maintenance_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_projects_instances_reschedule_maintenance_task()`.
/// For the simplest API, use `sql_projects_instances_reschedule_maintenance()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_projects_instances_reschedule_maintenance_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_projects_instances_reschedule_maintenance_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_projects_instances_reschedule_maintenance_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_projects_instances_reschedule_maintenance`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlProjectsInstancesRescheduleMaintenanceArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// POST v1/projects/{project}/instances/{instance}/rescheduleMaintenance
/// Reschedules the maintenance on the given instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_projects_instances_reschedule_maintenance_builder()` + `sql_projects_instances_reschedule_maintenance_execute()`.
/// For task-level control, use `sql_projects_instances_reschedule_maintenance_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_projects_instances_reschedule_maintenance(
    client: &SimpleHttpClient,
    args: &SqlProjectsInstancesRescheduleMaintenanceArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_projects_instances_reschedule_maintenance_builder(
        client,
        &args.project,
        &args.instance,
    )?;
    sql_projects_instances_reschedule_maintenance_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/resetReplicaSize
/// Reset Replica Size to primary instance disk size.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_projects_instances_reset_replica_size_execute()` to send, or `sql_projects_instances_reset_replica_size` for simplest API.

pub fn sql_projects_instances_reset_replica_size_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/resetReplicaSize",
        project, instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/resetReplicaSize
/// Reset Replica Size to primary instance disk size.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_projects_instances_reset_replica_size_execute()` or `sql_projects_instances_reset_replica_size`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_projects_instances_reset_replica_size_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_projects_instances_reset_replica_size_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/resetReplicaSize
/// Reset Replica Size to primary instance disk size.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_projects_instances_reset_replica_size_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_projects_instances_reset_replica_size_task()`.
/// For the simplest API, use `sql_projects_instances_reset_replica_size()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_projects_instances_reset_replica_size_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_projects_instances_reset_replica_size_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_projects_instances_reset_replica_size_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_projects_instances_reset_replica_size`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlProjectsInstancesResetReplicaSizeArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// POST v1/projects/{project}/instances/{instance}/resetReplicaSize
/// Reset Replica Size to primary instance disk size.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_projects_instances_reset_replica_size_builder()` + `sql_projects_instances_reset_replica_size_execute()`.
/// For task-level control, use `sql_projects_instances_reset_replica_size_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_projects_instances_reset_replica_size(
    client: &SimpleHttpClient,
    args: &SqlProjectsInstancesResetReplicaSizeArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        sql_projects_instances_reset_replica_size_builder(client, &args.project, &args.instance)?;
    sql_projects_instances_reset_replica_size_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/startExternalSync
/// Start External primary instance migration.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_projects_instances_start_external_sync_execute()` to send, or `sql_projects_instances_start_external_sync` for simplest API.

pub fn sql_projects_instances_start_external_sync_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/startExternalSync",
        project, instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/startExternalSync
/// Start External primary instance migration.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_projects_instances_start_external_sync_execute()` or `sql_projects_instances_start_external_sync`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_projects_instances_start_external_sync_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_projects_instances_start_external_sync_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/startExternalSync
/// Start External primary instance migration.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_projects_instances_start_external_sync_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_projects_instances_start_external_sync_task()`.
/// For the simplest API, use `sql_projects_instances_start_external_sync()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_projects_instances_start_external_sync_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_projects_instances_start_external_sync_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_projects_instances_start_external_sync_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_projects_instances_start_external_sync`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlProjectsInstancesStartExternalSyncArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// POST v1/projects/{project}/instances/{instance}/startExternalSync
/// Start External primary instance migration.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_projects_instances_start_external_sync_builder()` + `sql_projects_instances_start_external_sync_execute()`.
/// For task-level control, use `sql_projects_instances_start_external_sync_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_projects_instances_start_external_sync(
    client: &SimpleHttpClient,
    args: &SqlProjectsInstancesStartExternalSyncArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        sql_projects_instances_start_external_sync_builder(client, &args.project, &args.instance)?;
    sql_projects_instances_start_external_sync_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/verifyExternalSyncSettings
/// Verify External primary instance external sync settings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_projects_instances_verify_external_sync_settings_execute()` to send, or `sql_projects_instances_verify_external_sync_settings` for simplest API.

pub fn sql_projects_instances_verify_external_sync_settings_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/verifyExternalSyncSettings",
        project, instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/verifyExternalSyncSettings
/// Verify External primary instance external sync settings.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_projects_instances_verify_external_sync_settings_execute()` or `sql_projects_instances_verify_external_sync_settings`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_projects_instances_verify_external_sync_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_projects_instances_verify_external_sync_settings_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SqlInstancesVerifyExternalSyncSettingsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SqlInstancesVerifyExternalSyncSettingsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/verifyExternalSyncSettings
/// Verify External primary instance external sync settings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_projects_instances_verify_external_sync_settings_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_projects_instances_verify_external_sync_settings_task()`.
/// For the simplest API, use `sql_projects_instances_verify_external_sync_settings()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_projects_instances_verify_external_sync_settings_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_projects_instances_verify_external_sync_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SqlInstancesVerifyExternalSyncSettingsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = sql_projects_instances_verify_external_sync_settings_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_projects_instances_verify_external_sync_settings`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlProjectsInstancesVerifyExternalSyncSettingsArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// POST v1/projects/{project}/instances/{instance}/verifyExternalSyncSettings
/// Verify External primary instance external sync settings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_projects_instances_verify_external_sync_settings_builder()` + `sql_projects_instances_verify_external_sync_settings_execute()`.
/// For task-level control, use `sql_projects_instances_verify_external_sync_settings_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_projects_instances_verify_external_sync_settings(
    client: &SimpleHttpClient,
    args: &SqlProjectsInstancesVerifyExternalSyncSettingsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SqlInstancesVerifyExternalSyncSettingsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sql_projects_instances_verify_external_sync_settings_builder(
        client,
        &args.project,
        &args.instance,
    )?;
    sql_projects_instances_verify_external_sync_settings_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/createEphemeral
/// Generates a short-lived X509 certificate containing the provided public key and signed by a private key specific to the target instance. Users may use the certificate to authenticate as themselves when connecting to the database.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_ssl_certs_create_ephemeral_execute()` to send, or `sql_ssl_certs_create_ephemeral` for simplest API.

pub fn sql_ssl_certs_create_ephemeral_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/createEphemeral",
        project, instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/createEphemeral
/// Generates a short-lived X509 certificate containing the provided public key and signed by a private key specific to the target instance. Users may use the certificate to authenticate as themselves when connecting to the database.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_ssl_certs_create_ephemeral_execute()` or `sql_ssl_certs_create_ephemeral`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_ssl_certs_create_ephemeral_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_ssl_certs_create_ephemeral_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SslCert>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslCert = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/createEphemeral
/// Generates a short-lived X509 certificate containing the provided public key and signed by a private key specific to the target instance. Users may use the certificate to authenticate as themselves when connecting to the database.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_ssl_certs_create_ephemeral_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_ssl_certs_create_ephemeral_task()`.
/// For the simplest API, use `sql_ssl_certs_create_ephemeral()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_ssl_certs_create_ephemeral_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_ssl_certs_create_ephemeral_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslCert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_ssl_certs_create_ephemeral_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_ssl_certs_create_ephemeral`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlSslCertsCreateEphemeralArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// POST v1/projects/{project}/instances/{instance}/createEphemeral
/// Generates a short-lived X509 certificate containing the provided public key and signed by a private key specific to the target instance. Users may use the certificate to authenticate as themselves when connecting to the database.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_ssl_certs_create_ephemeral_builder()` + `sql_ssl_certs_create_ephemeral_execute()`.
/// For task-level control, use `sql_ssl_certs_create_ephemeral_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_ssl_certs_create_ephemeral(
    client: &SimpleHttpClient,
    args: &SqlSslCertsCreateEphemeralArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslCert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_ssl_certs_create_ephemeral_builder(client, &args.project, &args.instance)?;
    sql_ssl_certs_create_ephemeral_execute(builder)
}

/// DELETE v1/projects/{project}/instances/{instance}/sslCerts/{sha1Fingerprint}
/// Deletes the SSL certificate. For First Generation instances, the certificate remains valid until the instance is restarted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_ssl_certs_delete_execute()` to send, or `sql_ssl_certs_delete` for simplest API.

pub fn sql_ssl_certs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
    sha1Fingerprint: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/sslCerts/{}",
        project, instance, sha1Fingerprint,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{project}/instances/{instance}/sslCerts/{sha1Fingerprint}
/// Deletes the SSL certificate. For First Generation instances, the certificate remains valid until the instance is restarted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_ssl_certs_delete_execute()` or `sql_ssl_certs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_ssl_certs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_ssl_certs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{project}/instances/{instance}/sslCerts/{sha1Fingerprint}
/// Deletes the SSL certificate. For First Generation instances, the certificate remains valid until the instance is restarted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_ssl_certs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_ssl_certs_delete_task()`.
/// For the simplest API, use `sql_ssl_certs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_ssl_certs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_ssl_certs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_ssl_certs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_ssl_certs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlSslCertsDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Path parameter: sha1Fingerprint
    pub sha1Fingerprint: String,
}

/// DELETE v1/projects/{project}/instances/{instance}/sslCerts/{sha1Fingerprint}
/// Deletes the SSL certificate. For First Generation instances, the certificate remains valid until the instance is restarted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_ssl_certs_delete_builder()` + `sql_ssl_certs_delete_execute()`.
/// For task-level control, use `sql_ssl_certs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_ssl_certs_delete(
    client: &SimpleHttpClient,
    args: &SqlSslCertsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        sql_ssl_certs_delete_builder(client, &args.project, &args.instance, &args.sha1Fingerprint)?;
    sql_ssl_certs_delete_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/sslCerts/{sha1Fingerprint}
/// Retrieves a particular SSL certificate. Does not include the private key (required for usage). The private key must be saved from the response to initial creation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_ssl_certs_get_execute()` to send, or `sql_ssl_certs_get` for simplest API.

pub fn sql_ssl_certs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
    sha1Fingerprint: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/sslCerts/{}",
        project, instance, sha1Fingerprint,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/sslCerts/{sha1Fingerprint}
/// Retrieves a particular SSL certificate. Does not include the private key (required for usage). The private key must be saved from the response to initial creation.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_ssl_certs_get_execute()` or `sql_ssl_certs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_ssl_certs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_ssl_certs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SslCert>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslCert = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{project}/instances/{instance}/sslCerts/{sha1Fingerprint}
/// Retrieves a particular SSL certificate. Does not include the private key (required for usage). The private key must be saved from the response to initial creation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_ssl_certs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_ssl_certs_get_task()`.
/// For the simplest API, use `sql_ssl_certs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_ssl_certs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_ssl_certs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslCert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_ssl_certs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_ssl_certs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlSslCertsGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Path parameter: sha1Fingerprint
    pub sha1Fingerprint: String,
}

/// GET v1/projects/{project}/instances/{instance}/sslCerts/{sha1Fingerprint}
/// Retrieves a particular SSL certificate. Does not include the private key (required for usage). The private key must be saved from the response to initial creation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_ssl_certs_get_builder()` + `sql_ssl_certs_get_execute()`.
/// For task-level control, use `sql_ssl_certs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_ssl_certs_get(
    client: &SimpleHttpClient,
    args: &SqlSslCertsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslCert>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        sql_ssl_certs_get_builder(client, &args.project, &args.instance, &args.sha1Fingerprint)?;
    sql_ssl_certs_get_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/sslCerts
/// Creates an SSL certificate and returns it along with the private key and server certificate authority. The new certificate will not be usable until the instance is restarted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_ssl_certs_insert_execute()` to send, or `sql_ssl_certs_insert` for simplest API.

pub fn sql_ssl_certs_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/sslCerts",
        project, instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/sslCerts
/// Creates an SSL certificate and returns it along with the private key and server certificate authority. The new certificate will not be usable until the instance is restarted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_ssl_certs_insert_execute()` or `sql_ssl_certs_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_ssl_certs_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_ssl_certs_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SslCertsInsertResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslCertsInsertResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/sslCerts
/// Creates an SSL certificate and returns it along with the private key and server certificate authority. The new certificate will not be usable until the instance is restarted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_ssl_certs_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_ssl_certs_insert_task()`.
/// For the simplest API, use `sql_ssl_certs_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_ssl_certs_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_ssl_certs_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslCertsInsertResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = sql_ssl_certs_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_ssl_certs_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlSslCertsInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// POST v1/projects/{project}/instances/{instance}/sslCerts
/// Creates an SSL certificate and returns it along with the private key and server certificate authority. The new certificate will not be usable until the instance is restarted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_ssl_certs_insert_builder()` + `sql_ssl_certs_insert_execute()`.
/// For task-level control, use `sql_ssl_certs_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_ssl_certs_insert(
    client: &SimpleHttpClient,
    args: &SqlSslCertsInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslCertsInsertResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sql_ssl_certs_insert_builder(client, &args.project, &args.instance)?;
    sql_ssl_certs_insert_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/sslCerts
/// Lists all of the current SSL certificates for the instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_ssl_certs_list_execute()` to send, or `sql_ssl_certs_list` for simplest API.

pub fn sql_ssl_certs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/sslCerts",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/sslCerts
/// Lists all of the current SSL certificates for the instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_ssl_certs_list_execute()` or `sql_ssl_certs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_ssl_certs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_ssl_certs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SslCertsListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SslCertsListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{project}/instances/{instance}/sslCerts
/// Lists all of the current SSL certificates for the instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_ssl_certs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_ssl_certs_list_task()`.
/// For the simplest API, use `sql_ssl_certs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_ssl_certs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_ssl_certs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslCertsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = sql_ssl_certs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_ssl_certs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlSslCertsListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// GET v1/projects/{project}/instances/{instance}/sslCerts
/// Lists all of the current SSL certificates for the instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_ssl_certs_list_builder()` + `sql_ssl_certs_list_execute()`.
/// For task-level control, use `sql_ssl_certs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_ssl_certs_list(
    client: &SimpleHttpClient,
    args: &SqlSslCertsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SslCertsListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sql_ssl_certs_list_builder(client, &args.project, &args.instance)?;
    sql_ssl_certs_list_execute(builder)
}

/// GET v1/projects/{project}/tiers
/// Lists all available machine types (tiers) for Cloud SQL, for example, db-custom-1-3840. For more information, see <https://cloud.google.`com/sql/pricing`.>
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_tiers_list_execute()` to send, or `sql_tiers_list` for simplest API.

pub fn sql_tiers_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/tiers",
        project,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/tiers
/// Lists all available machine types (tiers) for Cloud SQL, for example, db-custom-1-3840. For more information, see <https://cloud.google.`com/sql/pricing`.>
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_tiers_list_execute()` or `sql_tiers_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_tiers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_tiers_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TiersListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TiersListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{project}/tiers
/// Lists all available machine types (tiers) for Cloud SQL, for example, db-custom-1-3840. For more information, see <https://cloud.google.`com/sql/pricing`.>
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_tiers_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_tiers_list_task()`.
/// For the simplest API, use `sql_tiers_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_tiers_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_tiers_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TiersListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = sql_tiers_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_tiers_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlTiersListArgs {
    /// Path parameter: project
    pub project: String,
}

/// GET v1/projects/{project}/tiers
/// Lists all available machine types (tiers) for Cloud SQL, for example, db-custom-1-3840. For more information, see <https://cloud.google.`com/sql/pricing`.>
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_tiers_list_builder()` + `sql_tiers_list_execute()`.
/// For task-level control, use `sql_tiers_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_tiers_list(
    client: &SimpleHttpClient,
    args: &SqlTiersListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<TiersListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sql_tiers_list_builder(client, &args.project)?;
    sql_tiers_list_execute(builder)
}

/// DELETE v1/projects/{project}/instances/{instance}/users
/// Deletes a user from a Cloud SQL instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_users_delete_execute()` to send, or `sql_users_delete` for simplest API.

pub fn sql_users_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
    host: &Option<String>,
    name: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/users",
        project, instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = host.as_ref() {
        query_parts.push(format!("host={}", val));
    }
    if let Some(val) = name.as_ref() {
        query_parts.push(format!("name={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{project}/instances/{instance}/users
/// Deletes a user from a Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_users_delete_execute()` or `sql_users_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_users_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_users_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{project}/instances/{instance}/users
/// Deletes a user from a Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_users_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_users_delete_task()`.
/// For the simplest API, use `sql_users_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_users_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_users_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_users_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_users_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlUsersDeleteArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: host
    pub host: Option<String>,
    /// Query parameter: name
    pub name: Option<String>,
}

/// DELETE v1/projects/{project}/instances/{instance}/users
/// Deletes a user from a Cloud SQL instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_users_delete_builder()` + `sql_users_delete_execute()`.
/// For task-level control, use `sql_users_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_users_delete(
    client: &SimpleHttpClient,
    args: &SqlUsersDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_users_delete_builder(
        client,
        &args.project,
        &args.instance,
        &args.host,
        &args.name,
    )?;
    sql_users_delete_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/users/{name}
/// Retrieves a resource containing information about a user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_users_get_execute()` to send, or `sql_users_get` for simplest API.

pub fn sql_users_get_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
    name: &String,
    host: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/users/{}",
        project, instance, name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = host.as_ref() {
        query_parts.push(format!("host={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/users/{name}
/// Retrieves a resource containing information about a user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_users_get_execute()` or `sql_users_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_users_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_users_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<User>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: User = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{project}/instances/{instance}/users/{name}
/// Retrieves a resource containing information about a user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_users_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_users_get_task()`.
/// For the simplest API, use `sql_users_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_users_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_users_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<User>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_users_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_users_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlUsersGetArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Path parameter: name
    pub name: String,
    /// Query parameter: host
    pub host: Option<String>,
}

/// GET v1/projects/{project}/instances/{instance}/users/{name}
/// Retrieves a resource containing information about a user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_users_get_builder()` + `sql_users_get_execute()`.
/// For task-level control, use `sql_users_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_users_get(
    client: &SimpleHttpClient,
    args: &SqlUsersGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<User>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_users_get_builder(
        client,
        &args.project,
        &args.instance,
        &args.name,
        &args.host,
    )?;
    sql_users_get_execute(builder)
}

/// POST v1/projects/{project}/instances/{instance}/users
/// Creates a new user in a Cloud SQL instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_users_insert_execute()` to send, or `sql_users_insert` for simplest API.

pub fn sql_users_insert_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/users",
        project, instance,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{project}/instances/{instance}/users
/// Creates a new user in a Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_users_insert_execute()` or `sql_users_insert`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_users_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_users_insert_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{project}/instances/{instance}/users
/// Creates a new user in a Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_users_insert_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_users_insert_task()`.
/// For the simplest API, use `sql_users_insert()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_users_insert_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_users_insert_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_users_insert_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_users_insert`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlUsersInsertArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// POST v1/projects/{project}/instances/{instance}/users
/// Creates a new user in a Cloud SQL instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_users_insert_builder()` + `sql_users_insert_execute()`.
/// For task-level control, use `sql_users_insert_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_users_insert(
    client: &SimpleHttpClient,
    args: &SqlUsersInsertArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_users_insert_builder(client, &args.project, &args.instance)?;
    sql_users_insert_execute(builder)
}

/// GET v1/projects/{project}/instances/{instance}/users
/// Lists users in the specified Cloud SQL instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_users_list_execute()` to send, or `sql_users_list` for simplest API.

pub fn sql_users_list_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/users",
        project, instance,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{project}/instances/{instance}/users
/// Lists users in the specified Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_users_list_execute()` or `sql_users_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_users_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_users_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UsersListResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UsersListResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{project}/instances/{instance}/users
/// Lists users in the specified Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_users_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_users_list_task()`.
/// For the simplest API, use `sql_users_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_users_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_users_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UsersListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = sql_users_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_users_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlUsersListArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
}

/// GET v1/projects/{project}/instances/{instance}/users
/// Lists users in the specified Cloud SQL instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_users_list_builder()` + `sql_users_list_execute()`.
/// For task-level control, use `sql_users_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_users_list(
    client: &SimpleHttpClient,
    args: &SqlUsersListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UsersListResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sql_users_list_builder(client, &args.project, &args.instance)?;
    sql_users_list_execute(builder)
}

/// PUT v1/projects/{project}/instances/{instance}/users
/// Updates an existing user in a Cloud SQL instance.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sql_users_update_execute()` to send, or `sql_users_update` for simplest API.

pub fn sql_users_update_builder<R>(
    client: &SimpleHttpClient<R>,
    project: &String,
    instance: &String,
    databaseRoles: &Option<String>,
    host: &Option<String>,
    name: &Option<String>,
    revokeExistingRoles: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://sqladmin.googleapis.com/v1/projects/{}/instances/{}/users",
        project, instance,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = databaseRoles.as_ref() {
        query_parts.push(format!("databaseRoles={}", val));
    }
    if let Some(val) = host.as_ref() {
        query_parts.push(format!("host={}", val));
    }
    if let Some(val) = name.as_ref() {
        query_parts.push(format!("name={}", val));
    }
    if let Some(val) = revokeExistingRoles.as_ref() {
        query_parts.push(format!("revokeExistingRoles={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .put(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PUT v1/projects/{project}/instances/{instance}/users
/// Updates an existing user in a Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `sql_users_update_execute()` or `sql_users_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_users_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_users_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PUT v1/projects/{project}/instances/{instance}/users
/// Updates an existing user in a Cloud SQL instance.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `sql_users_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `sql_users_update_task()`.
/// For the simplest API, use `sql_users_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `sql_users_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn sql_users_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = sql_users_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`sql_users_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct SqlUsersUpdateArgs {
    /// Path parameter: project
    pub project: String,
    /// Path parameter: instance
    pub instance: String,
    /// Query parameter: databaseRoles
    pub databaseRoles: Option<String>,
    /// Query parameter: host
    pub host: Option<String>,
    /// Query parameter: name
    pub name: Option<String>,
    /// Query parameter: revokeExistingRoles
    pub revokeExistingRoles: Option<String>,
}

/// PUT v1/projects/{project}/instances/{instance}/users
/// Updates an existing user in a Cloud SQL instance.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sql_users_update_builder()` + `sql_users_update_execute()`.
/// For task-level control, use `sql_users_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sql_users_update(
    client: &SimpleHttpClient,
    args: &SqlUsersUpdateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sql_users_update_builder(
        client,
        &args.project,
        &args.instance,
        &args.databaseRoles,
        &args.host,
        &args.name,
        &args.revokeExistingRoles,
    )?;
    sql_users_update_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlBackupsCreateBackupArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlBackupsCreateBackupArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlBackupsCreateBackupArgs) -> String {
        format!("gcp::sqladmin::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlBackupsDeleteBackupArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlBackupsDeleteBackupArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlBackupsDeleteBackupArgs) -> String {
        format!("gcp::sqladmin::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Backup
// =============================================================================

/// ResourceIdentifier implementation for Backup with SqlBackupsGetBackupArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlBackupsGetBackupArgs> for Backup {
    fn generate_resource_id(&self, input: &SqlBackupsGetBackupArgs) -> String {
        format!("gcp::sqladmin::Backup/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Backup"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListBackupsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListBackupsResponse with SqlBackupsListBackupsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlBackupsListBackupsArgs> for ListBackupsResponse {
    fn generate_resource_id(&self, input: &SqlBackupsListBackupsArgs) -> String {
        format!("gcp::sqladmin::ListBackupsResponse/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::ListBackupsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlBackupsUpdateBackupArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlBackupsUpdateBackupArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlBackupsUpdateBackupArgs) -> String {
        format!("gcp::sqladmin::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlBackupRunsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlBackupRunsDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlBackupRunsDeleteArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}/{}",
            input.project, input.instance, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BackupRun
// =============================================================================

/// ResourceIdentifier implementation for BackupRun with SqlBackupRunsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlBackupRunsGetArgs> for BackupRun {
    fn generate_resource_id(&self, input: &SqlBackupRunsGetArgs) -> String {
        format!(
            "gcp::sqladmin::BackupRun/{}/{}/{}",
            input.project, input.instance, input.id
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::BackupRun"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlBackupRunsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlBackupRunsInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlBackupRunsInsertArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for BackupRunsListResponse
// =============================================================================

/// ResourceIdentifier implementation for BackupRunsListResponse with SqlBackupRunsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlBackupRunsListArgs> for BackupRunsListResponse {
    fn generate_resource_id(&self, input: &SqlBackupRunsListArgs) -> String {
        format!(
            "gcp::sqladmin::BackupRunsListResponse/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::BackupRunsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GenerateEphemeralCertResponse
// =============================================================================

/// ResourceIdentifier implementation for GenerateEphemeralCertResponse with SqlConnectGenerateEphemeralArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlConnectGenerateEphemeralArgs> for GenerateEphemeralCertResponse {
    fn generate_resource_id(&self, input: &SqlConnectGenerateEphemeralArgs) -> String {
        format!(
            "gcp::sqladmin::GenerateEphemeralCertResponse/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::GenerateEphemeralCertResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ConnectSettings
// =============================================================================

/// ResourceIdentifier implementation for ConnectSettings with SqlConnectGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlConnectGetArgs> for ConnectSettings {
    fn generate_resource_id(&self, input: &SqlConnectGetArgs) -> String {
        format!(
            "gcp::sqladmin::ConnectSettings/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::ConnectSettings"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlDatabasesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlDatabasesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlDatabasesDeleteArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}/{}",
            input.project, input.instance, input.database
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Database
// =============================================================================

/// ResourceIdentifier implementation for Database with SqlDatabasesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlDatabasesGetArgs> for Database {
    fn generate_resource_id(&self, input: &SqlDatabasesGetArgs) -> String {
        format!(
            "gcp::sqladmin::Database/{}/{}/{}",
            input.project, input.instance, input.database
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Database"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlDatabasesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlDatabasesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlDatabasesInsertArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DatabasesListResponse
// =============================================================================

/// ResourceIdentifier implementation for DatabasesListResponse with SqlDatabasesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlDatabasesListArgs> for DatabasesListResponse {
    fn generate_resource_id(&self, input: &SqlDatabasesListArgs) -> String {
        format!(
            "gcp::sqladmin::DatabasesListResponse/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::DatabasesListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlDatabasesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlDatabasesPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlDatabasesPatchArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}/{}",
            input.project, input.instance, input.database
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlDatabasesUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlDatabasesUpdateArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlDatabasesUpdateArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}/{}",
            input.project, input.instance, input.database
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for FlagsListResponse
// =============================================================================

/// ResourceIdentifier implementation for FlagsListResponse with SqlFlagsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlFlagsListArgs> for FlagsListResponse {
    fn generate_resource_id(&self, input: &SqlFlagsListArgs) -> String {
        "gcp::sqladmin::FlagsListResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::FlagsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstancesListEntraIdCertificatesResponse
// =============================================================================

/// ResourceIdentifier implementation for InstancesListEntraIdCertificatesResponse with SqlInstancesListEntraIdCertificatesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesListEntraIdCertificatesArgs>
    for InstancesListEntraIdCertificatesResponse
{
    fn generate_resource_id(&self, input: &SqlInstancesListEntraIdCertificatesArgs) -> String {
        format!(
            "gcp::sqladmin::InstancesListEntraIdCertificatesResponse/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::InstancesListEntraIdCertificatesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstancesListServerCertificatesResponse
// =============================================================================

/// ResourceIdentifier implementation for InstancesListServerCertificatesResponse with SqlInstancesListServerCertificatesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesListServerCertificatesArgs>
    for InstancesListServerCertificatesResponse
{
    fn generate_resource_id(&self, input: &SqlInstancesListServerCertificatesArgs) -> String {
        format!(
            "gcp::sqladmin::InstancesListServerCertificatesResponse/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::InstancesListServerCertificatesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlInstancesRotateEntraIdCertificateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesRotateEntraIdCertificateArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlInstancesRotateEntraIdCertificateArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlInstancesRotateServerCertificateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesRotateServerCertificateArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlInstancesRotateServerCertificateArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SqlInstancesAcquireSsrsLeaseResponse
// =============================================================================

/// ResourceIdentifier implementation for SqlInstancesAcquireSsrsLeaseResponse with SqlInstancesAcquireSsrsLeaseArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesAcquireSsrsLeaseArgs> for SqlInstancesAcquireSsrsLeaseResponse {
    fn generate_resource_id(&self, input: &SqlInstancesAcquireSsrsLeaseArgs) -> String {
        format!(
            "gcp::sqladmin::SqlInstancesAcquireSsrsLeaseResponse/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::SqlInstancesAcquireSsrsLeaseResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlInstancesAddEntraIdCertificateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesAddEntraIdCertificateArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlInstancesAddEntraIdCertificateArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlInstancesAddServerCaArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesAddServerCaArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlInstancesAddServerCaArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlInstancesAddServerCertificateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesAddServerCertificateArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlInstancesAddServerCertificateArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlInstancesCloneArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesCloneArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlInstancesCloneArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlInstancesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlInstancesDeleteArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlInstancesDemoteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesDemoteArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlInstancesDemoteArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlInstancesDemoteMasterArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesDemoteMasterArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlInstancesDemoteMasterArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SqlInstancesExecuteSqlResponse
// =============================================================================

/// ResourceIdentifier implementation for SqlInstancesExecuteSqlResponse with SqlInstancesExecuteSqlArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesExecuteSqlArgs> for SqlInstancesExecuteSqlResponse {
    fn generate_resource_id(&self, input: &SqlInstancesExecuteSqlArgs) -> String {
        format!(
            "gcp::sqladmin::SqlInstancesExecuteSqlResponse/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::SqlInstancesExecuteSqlResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlInstancesExportArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesExportArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlInstancesExportArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlInstancesFailoverArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesFailoverArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlInstancesFailoverArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DatabaseInstance
// =============================================================================

/// ResourceIdentifier implementation for DatabaseInstance with SqlInstancesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesGetArgs> for DatabaseInstance {
    fn generate_resource_id(&self, input: &SqlInstancesGetArgs) -> String {
        format!(
            "gcp::sqladmin::DatabaseInstance/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::DatabaseInstance"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlInstancesImportArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesImportArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlInstancesImportArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlInstancesInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlInstancesInsertArgs) -> String {
        format!("gcp::sqladmin::Operation/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstancesListResponse
// =============================================================================

/// ResourceIdentifier implementation for InstancesListResponse with SqlInstancesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesListArgs> for InstancesListResponse {
    fn generate_resource_id(&self, input: &SqlInstancesListArgs) -> String {
        format!("gcp::sqladmin::InstancesListResponse/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::InstancesListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InstancesListServerCasResponse
// =============================================================================

/// ResourceIdentifier implementation for InstancesListServerCasResponse with SqlInstancesListServerCasArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesListServerCasArgs> for InstancesListServerCasResponse {
    fn generate_resource_id(&self, input: &SqlInstancesListServerCasArgs) -> String {
        format!(
            "gcp::sqladmin::InstancesListServerCasResponse/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::InstancesListServerCasResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlInstancesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesPatchArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlInstancesPatchArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlInstancesPointInTimeRestoreArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesPointInTimeRestoreArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlInstancesPointInTimeRestoreArgs) -> String {
        format!("gcp::sqladmin::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlInstancesPreCheckMajorVersionUpgradeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesPreCheckMajorVersionUpgradeArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlInstancesPreCheckMajorVersionUpgradeArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlInstancesPromoteReplicaArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesPromoteReplicaArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlInstancesPromoteReplicaArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlInstancesReencryptArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesReencryptArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlInstancesReencryptArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SqlInstancesReleaseSsrsLeaseResponse
// =============================================================================

/// ResourceIdentifier implementation for SqlInstancesReleaseSsrsLeaseResponse with SqlInstancesReleaseSsrsLeaseArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesReleaseSsrsLeaseArgs> for SqlInstancesReleaseSsrsLeaseResponse {
    fn generate_resource_id(&self, input: &SqlInstancesReleaseSsrsLeaseArgs) -> String {
        format!(
            "gcp::sqladmin::SqlInstancesReleaseSsrsLeaseResponse/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::SqlInstancesReleaseSsrsLeaseResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlInstancesResetSslConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesResetSslConfigArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlInstancesResetSslConfigArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlInstancesRestartArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesRestartArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlInstancesRestartArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlInstancesRestoreBackupArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesRestoreBackupArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlInstancesRestoreBackupArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlInstancesRotateServerCaArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesRotateServerCaArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlInstancesRotateServerCaArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlInstancesStartReplicaArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesStartReplicaArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlInstancesStartReplicaArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlInstancesStopReplicaArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesStopReplicaArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlInstancesStopReplicaArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlInstancesSwitchoverArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesSwitchoverArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlInstancesSwitchoverArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlInstancesTruncateLogArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesTruncateLogArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlInstancesTruncateLogArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlInstancesUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlInstancesUpdateArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlInstancesUpdateArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with SqlOperationsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlOperationsCancelArgs> for Empty {
    fn generate_resource_id(&self, input: &SqlOperationsCancelArgs) -> String {
        format!("gcp::sqladmin::Empty/{}/{}", input.project, input.operation)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlOperationsGetArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlOperationsGetArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.operation
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for OperationsListResponse
// =============================================================================

/// ResourceIdentifier implementation for OperationsListResponse with SqlOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlOperationsListArgs> for OperationsListResponse {
    fn generate_resource_id(&self, input: &SqlOperationsListArgs) -> String {
        format!("gcp::sqladmin::OperationsListResponse/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::OperationsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SqlInstancesGetDiskShrinkConfigResponse
// =============================================================================

/// ResourceIdentifier implementation for SqlInstancesGetDiskShrinkConfigResponse with SqlProjectsInstancesGetDiskShrinkConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlProjectsInstancesGetDiskShrinkConfigArgs>
    for SqlInstancesGetDiskShrinkConfigResponse
{
    fn generate_resource_id(&self, input: &SqlProjectsInstancesGetDiskShrinkConfigArgs) -> String {
        format!(
            "gcp::sqladmin::SqlInstancesGetDiskShrinkConfigResponse/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::SqlInstancesGetDiskShrinkConfigResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SqlInstancesGetLatestRecoveryTimeResponse
// =============================================================================

/// ResourceIdentifier implementation for SqlInstancesGetLatestRecoveryTimeResponse with SqlProjectsInstancesGetLatestRecoveryTimeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlProjectsInstancesGetLatestRecoveryTimeArgs>
    for SqlInstancesGetLatestRecoveryTimeResponse
{
    fn generate_resource_id(
        &self,
        input: &SqlProjectsInstancesGetLatestRecoveryTimeArgs,
    ) -> String {
        format!(
            "gcp::sqladmin::SqlInstancesGetLatestRecoveryTimeResponse/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::SqlInstancesGetLatestRecoveryTimeResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlProjectsInstancesPerformDiskShrinkArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlProjectsInstancesPerformDiskShrinkArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlProjectsInstancesPerformDiskShrinkArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlProjectsInstancesRescheduleMaintenanceArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlProjectsInstancesRescheduleMaintenanceArgs> for Operation {
    fn generate_resource_id(
        &self,
        input: &SqlProjectsInstancesRescheduleMaintenanceArgs,
    ) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlProjectsInstancesResetReplicaSizeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlProjectsInstancesResetReplicaSizeArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlProjectsInstancesResetReplicaSizeArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlProjectsInstancesStartExternalSyncArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlProjectsInstancesStartExternalSyncArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlProjectsInstancesStartExternalSyncArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SqlInstancesVerifyExternalSyncSettingsResponse
// =============================================================================

/// ResourceIdentifier implementation for SqlInstancesVerifyExternalSyncSettingsResponse with SqlProjectsInstancesVerifyExternalSyncSettingsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlProjectsInstancesVerifyExternalSyncSettingsArgs>
    for SqlInstancesVerifyExternalSyncSettingsResponse
{
    fn generate_resource_id(
        &self,
        input: &SqlProjectsInstancesVerifyExternalSyncSettingsArgs,
    ) -> String {
        format!(
            "gcp::sqladmin::SqlInstancesVerifyExternalSyncSettingsResponse/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::SqlInstancesVerifyExternalSyncSettingsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SslCert
// =============================================================================

/// ResourceIdentifier implementation for SslCert with SqlSslCertsCreateEphemeralArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlSslCertsCreateEphemeralArgs> for SslCert {
    fn generate_resource_id(&self, input: &SqlSslCertsCreateEphemeralArgs) -> String {
        format!(
            "gcp::sqladmin::SslCert/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::SslCert"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlSslCertsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlSslCertsDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlSslCertsDeleteArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}/{}",
            input.project, input.instance, input.sha1Fingerprint
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SslCert
// =============================================================================

/// ResourceIdentifier implementation for SslCert with SqlSslCertsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlSslCertsGetArgs> for SslCert {
    fn generate_resource_id(&self, input: &SqlSslCertsGetArgs) -> String {
        format!(
            "gcp::sqladmin::SslCert/{}/{}/{}",
            input.project, input.instance, input.sha1Fingerprint
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::SslCert"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SslCertsInsertResponse
// =============================================================================

/// ResourceIdentifier implementation for SslCertsInsertResponse with SqlSslCertsInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlSslCertsInsertArgs> for SslCertsInsertResponse {
    fn generate_resource_id(&self, input: &SqlSslCertsInsertArgs) -> String {
        format!(
            "gcp::sqladmin::SslCertsInsertResponse/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::SslCertsInsertResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for SslCertsListResponse
// =============================================================================

/// ResourceIdentifier implementation for SslCertsListResponse with SqlSslCertsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlSslCertsListArgs> for SslCertsListResponse {
    fn generate_resource_id(&self, input: &SqlSslCertsListArgs) -> String {
        format!(
            "gcp::sqladmin::SslCertsListResponse/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::SslCertsListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TiersListResponse
// =============================================================================

/// ResourceIdentifier implementation for TiersListResponse with SqlTiersListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlTiersListArgs> for TiersListResponse {
    fn generate_resource_id(&self, input: &SqlTiersListArgs) -> String {
        format!("gcp::sqladmin::TiersListResponse/{}", input.project)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::TiersListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlUsersDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlUsersDeleteArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlUsersDeleteArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for User
// =============================================================================

/// ResourceIdentifier implementation for User with SqlUsersGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlUsersGetArgs> for User {
    fn generate_resource_id(&self, input: &SqlUsersGetArgs) -> String {
        format!(
            "gcp::sqladmin::User/{}/{}/{}",
            input.project, input.instance, input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::User"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlUsersInsertArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlUsersInsertArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlUsersInsertArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for UsersListResponse
// =============================================================================

/// ResourceIdentifier implementation for UsersListResponse with SqlUsersListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlUsersListArgs> for UsersListResponse {
    fn generate_resource_id(&self, input: &SqlUsersListArgs) -> String {
        format!(
            "gcp::sqladmin::UsersListResponse/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::UsersListResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with SqlUsersUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<SqlUsersUpdateArgs> for Operation {
    fn generate_resource_id(&self, input: &SqlUsersUpdateArgs) -> String {
        format!(
            "gcp::sqladmin::Operation/{}/{}",
            input.project, input.instance
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::sqladmin::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
