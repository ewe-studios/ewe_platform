//! Auto-generated API clients for gcp/mybusinessaccountmanagement.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp/mybusinessaccountmanagement`

#![cfg(feature = "gcp/mybusinessaccountmanagement")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/accounts
/// Creates an account with the specified name and type under the given parent. - Personal accounts and Organizations cannot be created. - User Groups cannot be created with a Personal account as primary owner. - Location Groups cannot be created with a primary owner of a Personal account if the Personal account is in an Organization. - Location Groups cannot own Location Groups.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessaccountmanagement_accounts_create_execute()` to send, or `mybusinessaccountmanagement_accounts_create` for simplest API.

pub fn mybusinessaccountmanagement_accounts_create_builder(
    client: &SimpleHttpClient,
    body: &Account,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://mybusinessaccountmanagement.googleapis.com/v1/accounts",);

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/accounts
/// Creates an account with the specified name and type under the given parent. - Personal accounts and Organizations cannot be created. - User Groups cannot be created with a Personal account as primary owner. - Location Groups cannot be created with a primary owner of a Personal account if the Personal account is in an Organization. - Location Groups cannot own Location Groups.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessaccountmanagement_accounts_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessaccountmanagement_accounts_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessaccountmanagement_accounts_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessaccountmanagement_accounts_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessaccountmanagementAccountsCreateArgs {
    /// Request body.
    pub body: Account,
}

/// GET v1/accounts
/// Creates an account with the specified name and type under the given parent. - Personal accounts and Organizations cannot be created. - User Groups cannot be created with a Personal account as primary owner. - Location Groups cannot be created with a primary owner of a Personal account if the Personal account is in an Organization. - Location Groups cannot own Location Groups.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessaccountmanagement_accounts_create_builder()` + `mybusinessaccountmanagement_accounts_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessaccountmanagement_accounts_create(
    client: &SimpleHttpClient,
    args: &MybusinessaccountmanagementAccountsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = mybusinessaccountmanagement_accounts_create_builder(client, &args.body)?;
    mybusinessaccountmanagement_accounts_create_execute(builder)
}

/// GET v1/accounts/{accountsId}
/// Gets the specified account. Returns NOT_FOUND if the account does not exist or if the caller does not have access rights to it.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessaccountmanagement_accounts_get_execute()` to send, or `mybusinessaccountmanagement_accounts_get` for simplest API.

pub fn mybusinessaccountmanagement_accounts_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://mybusinessaccountmanagement.googleapis.com/v1/accounts/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/accounts/{accountsId}
/// Gets the specified account. Returns NOT_FOUND if the account does not exist or if the caller does not have access rights to it.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessaccountmanagement_accounts_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessaccountmanagement_accounts_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessaccountmanagement_accounts_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessaccountmanagement_accounts_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessaccountmanagementAccountsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/accounts/{accountsId}
/// Gets the specified account. Returns NOT_FOUND if the account does not exist or if the caller does not have access rights to it.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessaccountmanagement_accounts_get_builder()` + `mybusinessaccountmanagement_accounts_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessaccountmanagement_accounts_get(
    client: &SimpleHttpClient,
    args: &MybusinessaccountmanagementAccountsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = mybusinessaccountmanagement_accounts_get_builder(client, &args.name)?;
    mybusinessaccountmanagement_accounts_get_execute(builder)
}

/// GET v1/accounts
/// Lists all of the accounts for the authenticated user. This includes all accounts that the user owns, as well as any accounts for which the user has management rights.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessaccountmanagement_accounts_list_execute()` to send, or `mybusinessaccountmanagement_accounts_list` for simplest API.

pub fn mybusinessaccountmanagement_accounts_list_builder(
    client: &SimpleHttpClient,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    parentAccount: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://mybusinessaccountmanagement.googleapis.com/v1/accounts",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = parentAccount {
        query_parts.push(format!("parentAccount={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/accounts
/// Lists all of the accounts for the authenticated user. This includes all accounts that the user owns, as well as any accounts for which the user has management rights.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessaccountmanagement_accounts_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessaccountmanagement_accounts_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessaccountmanagement_accounts_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAccountsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAccountsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessaccountmanagement_accounts_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessaccountmanagementAccountsListArgs {
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: parentAccount
    pub parentAccount: Option<String>,
}

/// GET v1/accounts
/// Lists all of the accounts for the authenticated user. This includes all accounts that the user owns, as well as any accounts for which the user has management rights.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessaccountmanagement_accounts_list_builder()` + `mybusinessaccountmanagement_accounts_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessaccountmanagement_accounts_list(
    client: &SimpleHttpClient,
    args: &MybusinessaccountmanagementAccountsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAccountsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = mybusinessaccountmanagement_accounts_list_builder(
        client,
        args.filter.as_deref(),
        args.pageSize,
        args.pageToken.as_deref(),
        args.parentAccount.as_deref(),
    )?;
    mybusinessaccountmanagement_accounts_list_execute(builder)
}

/// GET v1/accounts/{accountsId}
/// Updates the specified business account. Personal accounts cannot be updated using this method.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessaccountmanagement_accounts_patch_execute()` to send, or `mybusinessaccountmanagement_accounts_patch` for simplest API.

pub fn mybusinessaccountmanagement_accounts_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    validateOnly: Option<bool>,
    body: &Account,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://mybusinessaccountmanagement.googleapis.com/v1/accounts/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }
    if let Some(val) = validateOnly {
        query_parts.push(format!("validateOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/accounts/{accountsId}
/// Updates the specified business account. Personal accounts cannot be updated using this method.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessaccountmanagement_accounts_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessaccountmanagement_accounts_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessaccountmanagement_accounts_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Account = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessaccountmanagement_accounts_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessaccountmanagementAccountsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Query parameter: validateOnly
    pub validateOnly: Option<bool>,
    /// Request body.
    pub body: Account,
}

/// GET v1/accounts/{accountsId}
/// Updates the specified business account. Personal accounts cannot be updated using this method.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessaccountmanagement_accounts_patch_builder()` + `mybusinessaccountmanagement_accounts_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessaccountmanagement_accounts_patch(
    client: &SimpleHttpClient,
    args: &MybusinessaccountmanagementAccountsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Account>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = mybusinessaccountmanagement_accounts_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        args.validateOnly,
        &args.body,
    )?;
    mybusinessaccountmanagement_accounts_patch_execute(builder)
}

/// GET v1/accounts/{accountsId}/admins
/// Invites the specified user to become an administrator for the specified account. The invitee must accept the invitation in order to be granted access to the account. See AcceptInvitation to programmatically accept an invitation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessaccountmanagement_accounts_admins_create_execute()` to send, or `mybusinessaccountmanagement_accounts_admins_create` for simplest API.

pub fn mybusinessaccountmanagement_accounts_admins_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Admin,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://mybusinessaccountmanagement.googleapis.com/v1/accounts/{}/admins",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/accounts/{accountsId}/admins
/// Invites the specified user to become an administrator for the specified account. The invitee must accept the invitation in order to be granted access to the account. See AcceptInvitation to programmatically accept an invitation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessaccountmanagement_accounts_admins_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessaccountmanagement_accounts_admins_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessaccountmanagement_accounts_admins_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Admin>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Admin = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessaccountmanagement_accounts_admins_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessaccountmanagementAccountsAdminsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: Admin,
}

/// GET v1/accounts/{accountsId}/admins
/// Invites the specified user to become an administrator for the specified account. The invitee must accept the invitation in order to be granted access to the account. See AcceptInvitation to programmatically accept an invitation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessaccountmanagement_accounts_admins_create_builder()` + `mybusinessaccountmanagement_accounts_admins_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessaccountmanagement_accounts_admins_create(
    client: &SimpleHttpClient,
    args: &MybusinessaccountmanagementAccountsAdminsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Admin>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = mybusinessaccountmanagement_accounts_admins_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    mybusinessaccountmanagement_accounts_admins_create_execute(builder)
}

/// GET v1/accounts/{accountsId}/admins/{adminsId}
/// Removes the specified admin from the specified account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessaccountmanagement_accounts_admins_delete_execute()` to send, or `mybusinessaccountmanagement_accounts_admins_delete` for simplest API.

pub fn mybusinessaccountmanagement_accounts_admins_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://mybusinessaccountmanagement.googleapis.com/v1/accounts/{}/admins/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/accounts/{accountsId}/admins/{adminsId}
/// Removes the specified admin from the specified account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessaccountmanagement_accounts_admins_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessaccountmanagement_accounts_admins_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessaccountmanagement_accounts_admins_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessaccountmanagement_accounts_admins_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessaccountmanagementAccountsAdminsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/accounts/{accountsId}/admins/{adminsId}
/// Removes the specified admin from the specified account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessaccountmanagement_accounts_admins_delete_builder()` + `mybusinessaccountmanagement_accounts_admins_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessaccountmanagement_accounts_admins_delete(
    client: &SimpleHttpClient,
    args: &MybusinessaccountmanagementAccountsAdminsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = mybusinessaccountmanagement_accounts_admins_delete_builder(client, &args.name)?;
    mybusinessaccountmanagement_accounts_admins_delete_execute(builder)
}

/// GET v1/accounts/{accountsId}/admins
/// Lists the admins for the specified account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessaccountmanagement_accounts_admins_list_execute()` to send, or `mybusinessaccountmanagement_accounts_admins_list` for simplest API.

pub fn mybusinessaccountmanagement_accounts_admins_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://mybusinessaccountmanagement.googleapis.com/v1/accounts/{}/admins",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/accounts/{accountsId}/admins
/// Lists the admins for the specified account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessaccountmanagement_accounts_admins_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessaccountmanagement_accounts_admins_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessaccountmanagement_accounts_admins_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAccountAdminsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAccountAdminsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessaccountmanagement_accounts_admins_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessaccountmanagementAccountsAdminsListArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// GET v1/accounts/{accountsId}/admins
/// Lists the admins for the specified account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessaccountmanagement_accounts_admins_list_builder()` + `mybusinessaccountmanagement_accounts_admins_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessaccountmanagement_accounts_admins_list(
    client: &SimpleHttpClient,
    args: &MybusinessaccountmanagementAccountsAdminsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListAccountAdminsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = mybusinessaccountmanagement_accounts_admins_list_builder(client, &args.parent)?;
    mybusinessaccountmanagement_accounts_admins_list_execute(builder)
}

/// GET v1/accounts/{accountsId}/admins/{adminsId}
/// Updates the Admin for the specified Account Admin.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessaccountmanagement_accounts_admins_patch_execute()` to send, or `mybusinessaccountmanagement_accounts_admins_patch` for simplest API.

pub fn mybusinessaccountmanagement_accounts_admins_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Admin,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://mybusinessaccountmanagement.googleapis.com/v1/accounts/{}/admins/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/accounts/{accountsId}/admins/{adminsId}
/// Updates the Admin for the specified Account Admin.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessaccountmanagement_accounts_admins_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessaccountmanagement_accounts_admins_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessaccountmanagement_accounts_admins_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Admin>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Admin = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessaccountmanagement_accounts_admins_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessaccountmanagementAccountsAdminsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: Admin,
}

/// GET v1/accounts/{accountsId}/admins/{adminsId}
/// Updates the Admin for the specified Account Admin.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessaccountmanagement_accounts_admins_patch_builder()` + `mybusinessaccountmanagement_accounts_admins_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessaccountmanagement_accounts_admins_patch(
    client: &SimpleHttpClient,
    args: &MybusinessaccountmanagementAccountsAdminsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Admin>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = mybusinessaccountmanagement_accounts_admins_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    mybusinessaccountmanagement_accounts_admins_patch_execute(builder)
}

/// GET v1/accounts/{accountsId}/invitations/{invitationsId}:accept
/// Accepts the specified invitation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessaccountmanagement_accounts_invitations_accept_execute()` to send, or `mybusinessaccountmanagement_accounts_invitations_accept` for simplest API.

pub fn mybusinessaccountmanagement_accounts_invitations_accept_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &AcceptInvitationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://mybusinessaccountmanagement.googleapis.com/v1/accounts/{}/invitations/{}:accept",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/accounts/{accountsId}/invitations/{invitationsId}:accept
/// Accepts the specified invitation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessaccountmanagement_accounts_invitations_accept_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessaccountmanagement_accounts_invitations_accept_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessaccountmanagement_accounts_invitations_accept_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessaccountmanagement_accounts_invitations_accept`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessaccountmanagementAccountsInvitationsAcceptArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: AcceptInvitationRequest,
}

/// GET v1/accounts/{accountsId}/invitations/{invitationsId}:accept
/// Accepts the specified invitation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessaccountmanagement_accounts_invitations_accept_builder()` + `mybusinessaccountmanagement_accounts_invitations_accept_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessaccountmanagement_accounts_invitations_accept(
    client: &SimpleHttpClient,
    args: &MybusinessaccountmanagementAccountsInvitationsAcceptArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = mybusinessaccountmanagement_accounts_invitations_accept_builder(
        client, &args.name, &args.body,
    )?;
    mybusinessaccountmanagement_accounts_invitations_accept_execute(builder)
}

/// GET v1/accounts/{accountsId}/invitations/{invitationsId}:decline
/// Declines the specified invitation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessaccountmanagement_accounts_invitations_decline_execute()` to send, or `mybusinessaccountmanagement_accounts_invitations_decline` for simplest API.

pub fn mybusinessaccountmanagement_accounts_invitations_decline_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &DeclineInvitationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://mybusinessaccountmanagement.googleapis.com/v1/accounts/{}/invitations/{}:decline",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/accounts/{accountsId}/invitations/{invitationsId}:decline
/// Declines the specified invitation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessaccountmanagement_accounts_invitations_decline_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessaccountmanagement_accounts_invitations_decline_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessaccountmanagement_accounts_invitations_decline_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessaccountmanagement_accounts_invitations_decline`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessaccountmanagementAccountsInvitationsDeclineArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: DeclineInvitationRequest,
}

/// GET v1/accounts/{accountsId}/invitations/{invitationsId}:decline
/// Declines the specified invitation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessaccountmanagement_accounts_invitations_decline_builder()` + `mybusinessaccountmanagement_accounts_invitations_decline_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessaccountmanagement_accounts_invitations_decline(
    client: &SimpleHttpClient,
    args: &MybusinessaccountmanagementAccountsInvitationsDeclineArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = mybusinessaccountmanagement_accounts_invitations_decline_builder(
        client, &args.name, &args.body,
    )?;
    mybusinessaccountmanagement_accounts_invitations_decline_execute(builder)
}

/// GET v1/accounts/{accountsId}/invitations
/// Lists pending invitations for the specified account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessaccountmanagement_accounts_invitations_list_execute()` to send, or `mybusinessaccountmanagement_accounts_invitations_list` for simplest API.

pub fn mybusinessaccountmanagement_accounts_invitations_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://mybusinessaccountmanagement.googleapis.com/v1/accounts/{}/invitations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter {
        query_parts.push(format!("filter={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/accounts/{accountsId}/invitations
/// Lists pending invitations for the specified account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessaccountmanagement_accounts_invitations_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessaccountmanagement_accounts_invitations_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessaccountmanagement_accounts_invitations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListInvitationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListInvitationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessaccountmanagement_accounts_invitations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessaccountmanagementAccountsInvitationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
}

/// GET v1/accounts/{accountsId}/invitations
/// Lists pending invitations for the specified account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessaccountmanagement_accounts_invitations_list_builder()` + `mybusinessaccountmanagement_accounts_invitations_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessaccountmanagement_accounts_invitations_list(
    client: &SimpleHttpClient,
    args: &MybusinessaccountmanagementAccountsInvitationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListInvitationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = mybusinessaccountmanagement_accounts_invitations_list_builder(
        client,
        &args.parent,
        args.filter.as_deref(),
    )?;
    mybusinessaccountmanagement_accounts_invitations_list_execute(builder)
}

/// GET v1/locations/{locationsId}:transfer
/// Moves a location from an account that the user owns to another account that the same user administers. The user must be an owner of the account the location is currently associated with and must also be at least a manager of the destination account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessaccountmanagement_locations_transfer_execute()` to send, or `mybusinessaccountmanagement_locations_transfer` for simplest API.

pub fn mybusinessaccountmanagement_locations_transfer_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &TransferLocationRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://mybusinessaccountmanagement.googleapis.com/v1/locations/{}:transfer",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/locations/{locationsId}:transfer
/// Moves a location from an account that the user owns to another account that the same user administers. The user must be an owner of the account the location is currently associated with and must also be at least a manager of the destination account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessaccountmanagement_locations_transfer_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessaccountmanagement_locations_transfer_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessaccountmanagement_locations_transfer_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessaccountmanagement_locations_transfer`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessaccountmanagementLocationsTransferArgs {
    /// Path parameter: name
    pub name: String,
    /// Request body.
    pub body: TransferLocationRequest,
}

/// GET v1/locations/{locationsId}:transfer
/// Moves a location from an account that the user owns to another account that the same user administers. The user must be an owner of the account the location is currently associated with and must also be at least a manager of the destination account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessaccountmanagement_locations_transfer_builder()` + `mybusinessaccountmanagement_locations_transfer_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessaccountmanagement_locations_transfer(
    client: &SimpleHttpClient,
    args: &MybusinessaccountmanagementLocationsTransferArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        mybusinessaccountmanagement_locations_transfer_builder(client, &args.name, &args.body)?;
    mybusinessaccountmanagement_locations_transfer_execute(builder)
}

/// GET v1/locations/{locationsId}/admins
/// Invites the specified user to become an administrator for the specified location. The invitee must accept the invitation in order to be granted access to the location. See AcceptInvitation to programmatically accept an invitation.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessaccountmanagement_locations_admins_create_execute()` to send, or `mybusinessaccountmanagement_locations_admins_create` for simplest API.

pub fn mybusinessaccountmanagement_locations_admins_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &Admin,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://mybusinessaccountmanagement.googleapis.com/v1/locations/{}/admins",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/locations/{locationsId}/admins
/// Invites the specified user to become an administrator for the specified location. The invitee must accept the invitation in order to be granted access to the location. See AcceptInvitation to programmatically accept an invitation.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessaccountmanagement_locations_admins_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessaccountmanagement_locations_admins_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessaccountmanagement_locations_admins_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Admin>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Admin = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessaccountmanagement_locations_admins_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessaccountmanagementLocationsAdminsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: Admin,
}

/// GET v1/locations/{locationsId}/admins
/// Invites the specified user to become an administrator for the specified location. The invitee must accept the invitation in order to be granted access to the location. See AcceptInvitation to programmatically accept an invitation.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessaccountmanagement_locations_admins_create_builder()` + `mybusinessaccountmanagement_locations_admins_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessaccountmanagement_locations_admins_create(
    client: &SimpleHttpClient,
    args: &MybusinessaccountmanagementLocationsAdminsCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Admin>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = mybusinessaccountmanagement_locations_admins_create_builder(
        client,
        &args.parent,
        &args.body,
    )?;
    mybusinessaccountmanagement_locations_admins_create_execute(builder)
}

/// GET v1/locations/{locationsId}/admins/{adminsId}
/// Removes the specified admin as a manager of the specified location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessaccountmanagement_locations_admins_delete_execute()` to send, or `mybusinessaccountmanagement_locations_admins_delete` for simplest API.

pub fn mybusinessaccountmanagement_locations_admins_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://mybusinessaccountmanagement.googleapis.com/v1/locations/{}/admins/{}",
        name,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/locations/{locationsId}/admins/{adminsId}
/// Removes the specified admin as a manager of the specified location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessaccountmanagement_locations_admins_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessaccountmanagement_locations_admins_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessaccountmanagement_locations_admins_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessaccountmanagement_locations_admins_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessaccountmanagementLocationsAdminsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/locations/{locationsId}/admins/{adminsId}
/// Removes the specified admin as a manager of the specified location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessaccountmanagement_locations_admins_delete_builder()` + `mybusinessaccountmanagement_locations_admins_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessaccountmanagement_locations_admins_delete(
    client: &SimpleHttpClient,
    args: &MybusinessaccountmanagementLocationsAdminsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = mybusinessaccountmanagement_locations_admins_delete_builder(client, &args.name)?;
    mybusinessaccountmanagement_locations_admins_delete_execute(builder)
}

/// GET v1/locations/{locationsId}/admins
/// Lists all of the admins for the specified location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessaccountmanagement_locations_admins_list_execute()` to send, or `mybusinessaccountmanagement_locations_admins_list` for simplest API.

pub fn mybusinessaccountmanagement_locations_admins_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://mybusinessaccountmanagement.googleapis.com/v1/locations/{}/admins",
        parent,
    );

    // Build request
    let builder = client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/locations/{locationsId}/admins
/// Lists all of the admins for the specified location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessaccountmanagement_locations_admins_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessaccountmanagement_locations_admins_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessaccountmanagement_locations_admins_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListLocationAdminsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationAdminsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessaccountmanagement_locations_admins_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessaccountmanagementLocationsAdminsListArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// GET v1/locations/{locationsId}/admins
/// Lists all of the admins for the specified location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessaccountmanagement_locations_admins_list_builder()` + `mybusinessaccountmanagement_locations_admins_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessaccountmanagement_locations_admins_list(
    client: &SimpleHttpClient,
    args: &MybusinessaccountmanagementLocationsAdminsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListLocationAdminsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = mybusinessaccountmanagement_locations_admins_list_builder(client, &args.parent)?;
    mybusinessaccountmanagement_locations_admins_list_execute(builder)
}

/// GET v1/locations/{locationsId}/admins/{adminsId}
/// Updates the Admin for the specified location. Only the AdminRole of the Admin can be updated.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `mybusinessaccountmanagement_locations_admins_patch_execute()` to send, or `mybusinessaccountmanagement_locations_admins_patch` for simplest API.

pub fn mybusinessaccountmanagement_locations_admins_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    updateMask: Option<&str>,
    body: &Admin,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://mybusinessaccountmanagement.googleapis.com/v1/locations/{}/admins/{}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = updateMask {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        url
    } else {
        format!("{}?{}", url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/locations/{locationsId}/admins/{adminsId}
/// Updates the Admin for the specified location. Only the AdminRole of the Admin can be updated.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `mybusinessaccountmanagement_locations_admins_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from mybusinessaccountmanagement_locations_admins_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessaccountmanagement_locations_admins_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Admin>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Admin = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`mybusinessaccountmanagement_locations_admins_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessaccountmanagementLocationsAdminsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
    /// Request body.
    pub body: Admin,
}

/// GET v1/locations/{locationsId}/admins/{adminsId}
/// Updates the Admin for the specified location. Only the AdminRole of the Admin can be updated.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `mybusinessaccountmanagement_locations_admins_patch_builder()` + `mybusinessaccountmanagement_locations_admins_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn mybusinessaccountmanagement_locations_admins_patch(
    client: &SimpleHttpClient,
    args: &MybusinessaccountmanagementLocationsAdminsPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Admin>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = mybusinessaccountmanagement_locations_admins_patch_builder(
        client,
        &args.name,
        args.updateMask.as_deref(),
        &args.body,
    )?;
    mybusinessaccountmanagement_locations_admins_patch_execute(builder)
}
