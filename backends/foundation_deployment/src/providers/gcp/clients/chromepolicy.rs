//! Auto-generated API clients for gcp/chromepolicy.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/{+customer}/policies:resolve
/// Gets the resolved policy values for a list of policies that match a search query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromepolicy_customers_policies_resolve_execute()` to send, or `chromepolicy_customers_policies_resolve` for simplest API.

pub fn chromepolicy_customers_policies_resolve_builder(
    client: &SimpleHttpClient,
    customer: &str,
    body: &GoogleChromePolicyVersionsV1ResolveRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+customer}/policies:resolve",
        customer,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+customer}/policies:resolve
/// Gets the resolved policy values for a list of policies that match a search query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromepolicy_customers_policies_resolve_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromepolicy_customers_policies_resolve_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_resolve_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromePolicyVersionsV1ResolveResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromePolicyVersionsV1ResolveResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+customer}/policies:resolve
/// Gets the resolved policy values for a list of policies that match a search query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromepolicy_customers_policies_resolve_builder()` + `chromepolicy_customers_policies_resolve_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_resolve(
    client: &SimpleHttpClient,
    customer: &str,
    body: &GoogleChromePolicyVersionsV1ResolveRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromePolicyVersionsV1ResolveResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromepolicy_customers_policies_resolve_builder(client, customer, body)?;
    chromepolicy_customers_policies_resolve_execute(builder)
}

/// GET v1/{+customer}/policies/groups:batchDelete
/// Delete multiple policy values that are applied to a specific group. All targets must have the same target format. That is to say that they must point to the same target resource and must have the same keys specified in `additionalTargetKeyNames`, though the values for those keys may be different. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromepolicy_customers_policies_groups_batch_delete_execute()` to send, or `chromepolicy_customers_policies_groups_batch_delete` for simplest API.

pub fn chromepolicy_customers_policies_groups_batch_delete_builder(
    client: &SimpleHttpClient,
    customer: &str,
    body: &GoogleChromePolicyVersionsV1BatchDeleteGroupPoliciesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+customer}/policies/groups:batchDelete",
        customer,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+customer}/policies/groups:batchDelete
/// Delete multiple policy values that are applied to a specific group. All targets must have the same target format. That is to say that they must point to the same target resource and must have the same keys specified in `additionalTargetKeyNames`, though the values for those keys may be different. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromepolicy_customers_policies_groups_batch_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromepolicy_customers_policies_groups_batch_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_groups_batch_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+customer}/policies/groups:batchDelete
/// Delete multiple policy values that are applied to a specific group. All targets must have the same target format. That is to say that they must point to the same target resource and must have the same keys specified in `additionalTargetKeyNames`, though the values for those keys may be different. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromepolicy_customers_policies_groups_batch_delete_builder()` + `chromepolicy_customers_policies_groups_batch_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_groups_batch_delete(
    client: &SimpleHttpClient,
    customer: &str,
    body: &GoogleChromePolicyVersionsV1BatchDeleteGroupPoliciesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        chromepolicy_customers_policies_groups_batch_delete_builder(client, customer, body)?;
    chromepolicy_customers_policies_groups_batch_delete_execute(builder)
}

/// GET v1/{+customer}/policies/groups:batchModify
/// Modify multiple policy values that are applied to a specific group. All targets must have the same target format. That is to say that they must point to the same target resource and must have the same keys specified in `additionalTargetKeyNames`, though the values for those keys may be different. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromepolicy_customers_policies_groups_batch_modify_execute()` to send, or `chromepolicy_customers_policies_groups_batch_modify` for simplest API.

pub fn chromepolicy_customers_policies_groups_batch_modify_builder(
    client: &SimpleHttpClient,
    customer: &str,
    body: &GoogleChromePolicyVersionsV1BatchModifyGroupPoliciesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+customer}/policies/groups:batchModify",
        customer,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+customer}/policies/groups:batchModify
/// Modify multiple policy values that are applied to a specific group. All targets must have the same target format. That is to say that they must point to the same target resource and must have the same keys specified in `additionalTargetKeyNames`, though the values for those keys may be different. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromepolicy_customers_policies_groups_batch_modify_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromepolicy_customers_policies_groups_batch_modify_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_groups_batch_modify_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+customer}/policies/groups:batchModify
/// Modify multiple policy values that are applied to a specific group. All targets must have the same target format. That is to say that they must point to the same target resource and must have the same keys specified in `additionalTargetKeyNames`, though the values for those keys may be different. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromepolicy_customers_policies_groups_batch_modify_builder()` + `chromepolicy_customers_policies_groups_batch_modify_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_groups_batch_modify(
    client: &SimpleHttpClient,
    customer: &str,
    body: &GoogleChromePolicyVersionsV1BatchModifyGroupPoliciesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        chromepolicy_customers_policies_groups_batch_modify_builder(client, customer, body)?;
    chromepolicy_customers_policies_groups_batch_modify_execute(builder)
}

/// GET v1/{+customer}/policies/groups:listGroupPriorityOrdering
/// Retrieve a group priority ordering for an app. The target app must be supplied in `additionalTargetKeyNames` in the PolicyTargetKey. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromepolicy_customers_policies_groups_list_group_priority_ordering_execute()` to send, or `chromepolicy_customers_policies_groups_list_group_priority_ordering` for simplest API.

pub fn chromepolicy_customers_policies_groups_list_group_priority_ordering_builder(
    client: &SimpleHttpClient,
    customer: &str,
    body: &GoogleChromePolicyVersionsV1ListGroupPriorityOrderingRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+customer}/policies/groups:listGroupPriorityOrdering",
        customer,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+customer}/policies/groups:listGroupPriorityOrdering
/// Retrieve a group priority ordering for an app. The target app must be supplied in `additionalTargetKeyNames` in the PolicyTargetKey. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromepolicy_customers_policies_groups_list_group_priority_ordering_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromepolicy_customers_policies_groups_list_group_priority_ordering_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_groups_list_group_priority_ordering_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromePolicyVersionsV1ListGroupPriorityOrderingResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromePolicyVersionsV1ListGroupPriorityOrderingResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+customer}/policies/groups:listGroupPriorityOrdering
/// Retrieve a group priority ordering for an app. The target app must be supplied in `additionalTargetKeyNames` in the PolicyTargetKey. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromepolicy_customers_policies_groups_list_group_priority_ordering_builder()` + `chromepolicy_customers_policies_groups_list_group_priority_ordering_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_groups_list_group_priority_ordering(
    client: &SimpleHttpClient,
    customer: &str,
    body: &GoogleChromePolicyVersionsV1ListGroupPriorityOrderingRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromePolicyVersionsV1ListGroupPriorityOrderingResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromepolicy_customers_policies_groups_list_group_priority_ordering_builder(
        client, customer, body,
    )?;
    chromepolicy_customers_policies_groups_list_group_priority_ordering_execute(builder)
}

/// GET v1/{+customer}/policies/groups:updateGroupPriorityOrdering
/// Update a group priority ordering for an app. The target app must be supplied in `additionalTargetKeyNames` in the PolicyTargetKey. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromepolicy_customers_policies_groups_update_group_priority_ordering_execute()` to send, or `chromepolicy_customers_policies_groups_update_group_priority_ordering` for simplest API.

pub fn chromepolicy_customers_policies_groups_update_group_priority_ordering_builder(
    client: &SimpleHttpClient,
    customer: &str,
    body: &GoogleChromePolicyVersionsV1UpdateGroupPriorityOrderingRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+customer}/policies/groups:updateGroupPriorityOrdering",
        customer,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+customer}/policies/groups:updateGroupPriorityOrdering
/// Update a group priority ordering for an app. The target app must be supplied in `additionalTargetKeyNames` in the PolicyTargetKey. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromepolicy_customers_policies_groups_update_group_priority_ordering_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromepolicy_customers_policies_groups_update_group_priority_ordering_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_groups_update_group_priority_ordering_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+customer}/policies/groups:updateGroupPriorityOrdering
/// Update a group priority ordering for an app. The target app must be supplied in `additionalTargetKeyNames` in the PolicyTargetKey. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromepolicy_customers_policies_groups_update_group_priority_ordering_builder()` + `chromepolicy_customers_policies_groups_update_group_priority_ordering_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_groups_update_group_priority_ordering(
    client: &SimpleHttpClient,
    customer: &str,
    body: &GoogleChromePolicyVersionsV1UpdateGroupPriorityOrderingRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = chromepolicy_customers_policies_groups_update_group_priority_ordering_builder(
        client, customer, body,
    )?;
    chromepolicy_customers_policies_groups_update_group_priority_ordering_execute(builder)
}

/// GET v1/{+customer}/policies/networks:defineCertificate
/// Creates a certificate at a specified OU for a customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromepolicy_customers_policies_networks_define_certificate_execute()` to send, or `chromepolicy_customers_policies_networks_define_certificate` for simplest API.

pub fn chromepolicy_customers_policies_networks_define_certificate_builder(
    client: &SimpleHttpClient,
    customer: &str,
    body: &GoogleChromePolicyVersionsV1DefineCertificateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+customer}/policies/networks:defineCertificate",
        customer,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+customer}/policies/networks:defineCertificate
/// Creates a certificate at a specified OU for a customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromepolicy_customers_policies_networks_define_certificate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromepolicy_customers_policies_networks_define_certificate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_networks_define_certificate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromePolicyVersionsV1DefineCertificateResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromePolicyVersionsV1DefineCertificateResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+customer}/policies/networks:defineCertificate
/// Creates a certificate at a specified OU for a customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromepolicy_customers_policies_networks_define_certificate_builder()` + `chromepolicy_customers_policies_networks_define_certificate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_networks_define_certificate(
    client: &SimpleHttpClient,
    customer: &str,
    body: &GoogleChromePolicyVersionsV1DefineCertificateRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromePolicyVersionsV1DefineCertificateResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromepolicy_customers_policies_networks_define_certificate_builder(
        client, customer, body,
    )?;
    chromepolicy_customers_policies_networks_define_certificate_execute(builder)
}

/// GET v1/{+customer}/policies/networks:defineNetwork
/// Define a new network.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromepolicy_customers_policies_networks_define_network_execute()` to send, or `chromepolicy_customers_policies_networks_define_network` for simplest API.

pub fn chromepolicy_customers_policies_networks_define_network_builder(
    client: &SimpleHttpClient,
    customer: &str,
    body: &GoogleChromePolicyVersionsV1DefineNetworkRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+customer}/policies/networks:defineNetwork",
        customer,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+customer}/policies/networks:defineNetwork
/// Define a new network.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromepolicy_customers_policies_networks_define_network_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromepolicy_customers_policies_networks_define_network_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_networks_define_network_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromePolicyVersionsV1DefineNetworkResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromePolicyVersionsV1DefineNetworkResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+customer}/policies/networks:defineNetwork
/// Define a new network.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromepolicy_customers_policies_networks_define_network_builder()` + `chromepolicy_customers_policies_networks_define_network_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_networks_define_network(
    client: &SimpleHttpClient,
    customer: &str,
    body: &GoogleChromePolicyVersionsV1DefineNetworkRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromePolicyVersionsV1DefineNetworkResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        chromepolicy_customers_policies_networks_define_network_builder(client, customer, body)?;
    chromepolicy_customers_policies_networks_define_network_execute(builder)
}

/// GET v1/{+customer}/policies/networks:removeCertificate
/// Remove an existing certificate by guid.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromepolicy_customers_policies_networks_remove_certificate_execute()` to send, or `chromepolicy_customers_policies_networks_remove_certificate` for simplest API.

pub fn chromepolicy_customers_policies_networks_remove_certificate_builder(
    client: &SimpleHttpClient,
    customer: &str,
    body: &GoogleChromePolicyVersionsV1RemoveCertificateRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+customer}/policies/networks:removeCertificate",
        customer,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+customer}/policies/networks:removeCertificate
/// Remove an existing certificate by guid.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromepolicy_customers_policies_networks_remove_certificate_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromepolicy_customers_policies_networks_remove_certificate_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_networks_remove_certificate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromePolicyVersionsV1RemoveCertificateResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromePolicyVersionsV1RemoveCertificateResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+customer}/policies/networks:removeCertificate
/// Remove an existing certificate by guid.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromepolicy_customers_policies_networks_remove_certificate_builder()` + `chromepolicy_customers_policies_networks_remove_certificate_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_networks_remove_certificate(
    client: &SimpleHttpClient,
    customer: &str,
    body: &GoogleChromePolicyVersionsV1RemoveCertificateRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromePolicyVersionsV1RemoveCertificateResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromepolicy_customers_policies_networks_remove_certificate_builder(
        client, customer, body,
    )?;
    chromepolicy_customers_policies_networks_remove_certificate_execute(builder)
}

/// GET v1/{+customer}/policies/networks:removeNetwork
/// Remove an existing network by guid.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromepolicy_customers_policies_networks_remove_network_execute()` to send, or `chromepolicy_customers_policies_networks_remove_network` for simplest API.

pub fn chromepolicy_customers_policies_networks_remove_network_builder(
    client: &SimpleHttpClient,
    customer: &str,
    body: &GoogleChromePolicyVersionsV1RemoveNetworkRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+customer}/policies/networks:removeNetwork",
        customer,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+customer}/policies/networks:removeNetwork
/// Remove an existing network by guid.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromepolicy_customers_policies_networks_remove_network_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromepolicy_customers_policies_networks_remove_network_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_networks_remove_network_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromePolicyVersionsV1RemoveNetworkResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromePolicyVersionsV1RemoveNetworkResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+customer}/policies/networks:removeNetwork
/// Remove an existing network by guid.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromepolicy_customers_policies_networks_remove_network_builder()` + `chromepolicy_customers_policies_networks_remove_network_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_networks_remove_network(
    client: &SimpleHttpClient,
    customer: &str,
    body: &GoogleChromePolicyVersionsV1RemoveNetworkRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromePolicyVersionsV1RemoveNetworkResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        chromepolicy_customers_policies_networks_remove_network_builder(client, customer, body)?;
    chromepolicy_customers_policies_networks_remove_network_execute(builder)
}

/// GET v1/{+customer}/policies/orgunits:batchInherit
/// Modify multiple policy values that are applied to a specific org unit so that they now inherit the value from a parent (if applicable). All targets must have the same target format. That is to say that they must point to the same target resource and must have the same keys specified in `additionalTargetKeyNames`, though the values for those keys may be different. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromepolicy_customers_policies_orgunits_batch_inherit_execute()` to send, or `chromepolicy_customers_policies_orgunits_batch_inherit` for simplest API.

pub fn chromepolicy_customers_policies_orgunits_batch_inherit_builder(
    client: &SimpleHttpClient,
    customer: &str,
    body: &GoogleChromePolicyVersionsV1BatchInheritOrgUnitPoliciesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+customer}/policies/orgunits:batchInherit",
        customer,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+customer}/policies/orgunits:batchInherit
/// Modify multiple policy values that are applied to a specific org unit so that they now inherit the value from a parent (if applicable). All targets must have the same target format. That is to say that they must point to the same target resource and must have the same keys specified in `additionalTargetKeyNames`, though the values for those keys may be different. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromepolicy_customers_policies_orgunits_batch_inherit_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromepolicy_customers_policies_orgunits_batch_inherit_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_orgunits_batch_inherit_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+customer}/policies/orgunits:batchInherit
/// Modify multiple policy values that are applied to a specific org unit so that they now inherit the value from a parent (if applicable). All targets must have the same target format. That is to say that they must point to the same target resource and must have the same keys specified in `additionalTargetKeyNames`, though the values for those keys may be different. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromepolicy_customers_policies_orgunits_batch_inherit_builder()` + `chromepolicy_customers_policies_orgunits_batch_inherit_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_orgunits_batch_inherit(
    client: &SimpleHttpClient,
    customer: &str,
    body: &GoogleChromePolicyVersionsV1BatchInheritOrgUnitPoliciesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        chromepolicy_customers_policies_orgunits_batch_inherit_builder(client, customer, body)?;
    chromepolicy_customers_policies_orgunits_batch_inherit_execute(builder)
}

/// GET v1/{+customer}/policies/orgunits:batchModify
/// Modify multiple policy values that are applied to a specific org unit. All targets must have the same target format. That is to say that they must point to the same target resource and must have the same keys specified in `additionalTargetKeyNames`, though the values for those keys may be different. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromepolicy_customers_policies_orgunits_batch_modify_execute()` to send, or `chromepolicy_customers_policies_orgunits_batch_modify` for simplest API.

pub fn chromepolicy_customers_policies_orgunits_batch_modify_builder(
    client: &SimpleHttpClient,
    customer: &str,
    body: &GoogleChromePolicyVersionsV1BatchModifyOrgUnitPoliciesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+customer}/policies/orgunits:batchModify",
        customer,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+customer}/policies/orgunits:batchModify
/// Modify multiple policy values that are applied to a specific org unit. All targets must have the same target format. That is to say that they must point to the same target resource and must have the same keys specified in `additionalTargetKeyNames`, though the values for those keys may be different. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromepolicy_customers_policies_orgunits_batch_modify_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromepolicy_customers_policies_orgunits_batch_modify_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_orgunits_batch_modify_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+customer}/policies/orgunits:batchModify
/// Modify multiple policy values that are applied to a specific org unit. All targets must have the same target format. That is to say that they must point to the same target resource and must have the same keys specified in `additionalTargetKeyNames`, though the values for those keys may be different. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromepolicy_customers_policies_orgunits_batch_modify_builder()` + `chromepolicy_customers_policies_orgunits_batch_modify_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_orgunits_batch_modify(
    client: &SimpleHttpClient,
    customer: &str,
    body: &GoogleChromePolicyVersionsV1BatchModifyOrgUnitPoliciesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        chromepolicy_customers_policies_orgunits_batch_modify_builder(client, customer, body)?;
    chromepolicy_customers_policies_orgunits_batch_modify_execute(builder)
}

/// GET v1/{+name}
/// Get a specific policy schema for a customer by its resource name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromepolicy_customers_policy_schemas_get_execute()` to send, or `chromepolicy_customers_policy_schemas_get` for simplest API.

pub fn chromepolicy_customers_policy_schemas_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get a specific policy schema for a customer by its resource name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromepolicy_customers_policy_schemas_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromepolicy_customers_policy_schemas_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policy_schemas_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromePolicyVersionsV1PolicySchema>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromePolicyVersionsV1PolicySchema = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+name}
/// Get a specific policy schema for a customer by its resource name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromepolicy_customers_policy_schemas_get_builder()` + `chromepolicy_customers_policy_schemas_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policy_schemas_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromePolicyVersionsV1PolicySchema>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromepolicy_customers_policy_schemas_get_builder(client, name)?;
    chromepolicy_customers_policy_schemas_get_execute(builder)
}

/// GET v1/{+parent}/policySchemas
/// Gets a list of policy schemas that match a specified filter value for a given customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromepolicy_customers_policy_schemas_list_execute()` to send, or `chromepolicy_customers_policy_schemas_list` for simplest API.

pub fn chromepolicy_customers_policy_schemas_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/{+parent}/policySchemas", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/policySchemas
/// Gets a list of policy schemas that match a specified filter value for a given customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromepolicy_customers_policy_schemas_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromepolicy_customers_policy_schemas_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policy_schemas_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromePolicyVersionsV1ListPolicySchemasResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromePolicyVersionsV1ListPolicySchemasResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/policySchemas
/// Gets a list of policy schemas that match a specified filter value for a given customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromepolicy_customers_policy_schemas_list_builder()` + `chromepolicy_customers_policy_schemas_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policy_schemas_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromePolicyVersionsV1ListPolicySchemasResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromepolicy_customers_policy_schemas_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    chromepolicy_customers_policy_schemas_list_execute(builder)
}

/// GET v1/{+customer}/policies/files:uploadPolicyFile
/// Creates an enterprise file from the content provided by user. Returns a public download url for end user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromepolicy_media_upload_execute()` to send, or `chromepolicy_media_upload` for simplest API.

pub fn chromepolicy_media_upload_builder(
    client: &SimpleHttpClient,
    customer: &str,
    body: &GoogleChromePolicyVersionsV1UploadPolicyFileRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+customer}/policies/files:uploadPolicyFile",
        customer,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/{+customer}/policies/files:uploadPolicyFile
/// Creates an enterprise file from the content provided by user. Returns a public download url for end user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromepolicy_media_upload_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromepolicy_media_upload_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_media_upload_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromePolicyVersionsV1UploadPolicyFileResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromePolicyVersionsV1UploadPolicyFileResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+customer}/policies/files:uploadPolicyFile
/// Creates an enterprise file from the content provided by user. Returns a public download url for end user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromepolicy_media_upload_builder()` + `chromepolicy_media_upload_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_media_upload(
    client: &SimpleHttpClient,
    customer: &str,
    body: &GoogleChromePolicyVersionsV1UploadPolicyFileRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromePolicyVersionsV1UploadPolicyFileResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromepolicy_media_upload_builder(client, customer, body)?;
    chromepolicy_media_upload_execute(builder)
}
