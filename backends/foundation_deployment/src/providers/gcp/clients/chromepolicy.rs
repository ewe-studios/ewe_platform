//! Auto-generated API clients for gcp/chromepolicy.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST v1/customers/{customersId}/policies:resolve
/// Gets the resolved policy values for a list of policies that match a search query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromepolicy_customers_policies_resolve_execute()` to send, or `chromepolicy_customers_policies_resolve` for simplest API.

pub fn chromepolicy_customers_policies_resolve_builder<R>(
    client: &SimpleHttpClient<R>,
    customer: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://chromepolicy.googleapis.com/v1/customers/{}/policies:resolve",
        customer,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/customers/{customersId}/policies:resolve
/// Gets the resolved policy values for a list of policies that match a search query.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromepolicy_customers_policies_resolve_execute()` or `chromepolicy_customers_policies_resolve`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromepolicy_customers_policies_resolve_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_resolve_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleChromePolicyVersionsV1ResolveResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromePolicyVersionsV1ResolveResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/customers/{customersId}/policies:resolve
/// Gets the resolved policy values for a list of policies that match a search query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromepolicy_customers_policies_resolve_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromepolicy_customers_policies_resolve_task()`.
/// For the simplest API, use `chromepolicy_customers_policies_resolve()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromepolicy_customers_policies_resolve_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromepolicy_customers_policies_resolve_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromePolicyVersionsV1ResolveResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = chromepolicy_customers_policies_resolve_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromepolicy_customers_policies_resolve`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromepolicyCustomersPoliciesResolveArgs {
    /// Path parameter: customer
    pub customer: String,
}

/// POST v1/customers/{customersId}/policies:resolve
/// Gets the resolved policy values for a list of policies that match a search query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromepolicy_customers_policies_resolve_builder()` + `chromepolicy_customers_policies_resolve_execute()`.
/// For task-level control, use `chromepolicy_customers_policies_resolve_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_resolve(
    client: &SimpleHttpClient,
    args: &ChromepolicyCustomersPoliciesResolveArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromePolicyVersionsV1ResolveResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromepolicy_customers_policies_resolve_builder(client, &args.customer)?;
    chromepolicy_customers_policies_resolve_execute(builder)
}

/// POST v1/customers/{customersId}/policies/groups:batchDelete
/// Delete multiple policy values that are applied to a specific group. All targets must have the same target format. That is to say that they must point to the same target resource and must have the same keys specified in `additionalTargetKeyNames`, though the values for those keys may be different. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromepolicy_customers_policies_groups_batch_delete_execute()` to send, or `chromepolicy_customers_policies_groups_batch_delete` for simplest API.

pub fn chromepolicy_customers_policies_groups_batch_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    customer: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://chromepolicy.googleapis.com/v1/customers/{}/policies/groups:batchDelete",
        customer,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/customers/{customersId}/policies/groups:batchDelete
/// Delete multiple policy values that are applied to a specific group. All targets must have the same target format. That is to say that they must point to the same target resource and must have the same keys specified in `additionalTargetKeyNames`, though the values for those keys may be different. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromepolicy_customers_policies_groups_batch_delete_execute()` or `chromepolicy_customers_policies_groups_batch_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromepolicy_customers_policies_groups_batch_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_groups_batch_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/customers/{customersId}/policies/groups:batchDelete
/// Delete multiple policy values that are applied to a specific group. All targets must have the same target format. That is to say that they must point to the same target resource and must have the same keys specified in `additionalTargetKeyNames`, though the values for those keys may be different. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromepolicy_customers_policies_groups_batch_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromepolicy_customers_policies_groups_batch_delete_task()`.
/// For the simplest API, use `chromepolicy_customers_policies_groups_batch_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromepolicy_customers_policies_groups_batch_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromepolicy_customers_policies_groups_batch_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = chromepolicy_customers_policies_groups_batch_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromepolicy_customers_policies_groups_batch_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromepolicyCustomersPoliciesGroupsBatchDeleteArgs {
    /// Path parameter: customer
    pub customer: String,
}

/// POST v1/customers/{customersId}/policies/groups:batchDelete
/// Delete multiple policy values that are applied to a specific group. All targets must have the same target format. That is to say that they must point to the same target resource and must have the same keys specified in `additionalTargetKeyNames`, though the values for those keys may be different. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromepolicy_customers_policies_groups_batch_delete_builder()` + `chromepolicy_customers_policies_groups_batch_delete_execute()`.
/// For task-level control, use `chromepolicy_customers_policies_groups_batch_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_groups_batch_delete(
    client: &SimpleHttpClient,
    args: &ChromepolicyCustomersPoliciesGroupsBatchDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        chromepolicy_customers_policies_groups_batch_delete_builder(client, &args.customer)?;
    chromepolicy_customers_policies_groups_batch_delete_execute(builder)
}

/// POST v1/customers/{customersId}/policies/groups:batchModify
/// Modify multiple policy values that are applied to a specific group. All targets must have the same target format. That is to say that they must point to the same target resource and must have the same keys specified in `additionalTargetKeyNames`, though the values for those keys may be different. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromepolicy_customers_policies_groups_batch_modify_execute()` to send, or `chromepolicy_customers_policies_groups_batch_modify` for simplest API.

pub fn chromepolicy_customers_policies_groups_batch_modify_builder<R>(
    client: &SimpleHttpClient<R>,
    customer: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://chromepolicy.googleapis.com/v1/customers/{}/policies/groups:batchModify",
        customer,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/customers/{customersId}/policies/groups:batchModify
/// Modify multiple policy values that are applied to a specific group. All targets must have the same target format. That is to say that they must point to the same target resource and must have the same keys specified in `additionalTargetKeyNames`, though the values for those keys may be different. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromepolicy_customers_policies_groups_batch_modify_execute()` or `chromepolicy_customers_policies_groups_batch_modify`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromepolicy_customers_policies_groups_batch_modify_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_groups_batch_modify_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/customers/{customersId}/policies/groups:batchModify
/// Modify multiple policy values that are applied to a specific group. All targets must have the same target format. That is to say that they must point to the same target resource and must have the same keys specified in `additionalTargetKeyNames`, though the values for those keys may be different. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromepolicy_customers_policies_groups_batch_modify_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromepolicy_customers_policies_groups_batch_modify_task()`.
/// For the simplest API, use `chromepolicy_customers_policies_groups_batch_modify()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromepolicy_customers_policies_groups_batch_modify_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromepolicy_customers_policies_groups_batch_modify_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = chromepolicy_customers_policies_groups_batch_modify_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromepolicy_customers_policies_groups_batch_modify`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromepolicyCustomersPoliciesGroupsBatchModifyArgs {
    /// Path parameter: customer
    pub customer: String,
}

/// POST v1/customers/{customersId}/policies/groups:batchModify
/// Modify multiple policy values that are applied to a specific group. All targets must have the same target format. That is to say that they must point to the same target resource and must have the same keys specified in `additionalTargetKeyNames`, though the values for those keys may be different. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromepolicy_customers_policies_groups_batch_modify_builder()` + `chromepolicy_customers_policies_groups_batch_modify_execute()`.
/// For task-level control, use `chromepolicy_customers_policies_groups_batch_modify_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_groups_batch_modify(
    client: &SimpleHttpClient,
    args: &ChromepolicyCustomersPoliciesGroupsBatchModifyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        chromepolicy_customers_policies_groups_batch_modify_builder(client, &args.customer)?;
    chromepolicy_customers_policies_groups_batch_modify_execute(builder)
}

/// POST v1/customers/{customersId}/policies/groups:listGroupPriorityOrdering
/// Retrieve a group priority ordering for an app. The target app must be supplied in `additionalTargetKeyNames` in the PolicyTargetKey. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromepolicy_customers_policies_groups_list_group_priority_ordering_execute()` to send, or `chromepolicy_customers_policies_groups_list_group_priority_ordering` for simplest API.

pub fn chromepolicy_customers_policies_groups_list_group_priority_ordering_builder<R>(
    client: &SimpleHttpClient<R>,
    customer: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://chromepolicy.googleapis.com/v1/customers/{}/policies/groups:listGroupPriorityOrdering",
        customer,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/customers/{customersId}/policies/groups:listGroupPriorityOrdering
/// Retrieve a group priority ordering for an app. The target app must be supplied in `additionalTargetKeyNames` in the PolicyTargetKey. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromepolicy_customers_policies_groups_list_group_priority_ordering_execute()` or `chromepolicy_customers_policies_groups_list_group_priority_ordering`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromepolicy_customers_policies_groups_list_group_priority_ordering_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_groups_list_group_priority_ordering_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromePolicyVersionsV1ListGroupPriorityOrderingResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromePolicyVersionsV1ListGroupPriorityOrderingResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/customers/{customersId}/policies/groups:listGroupPriorityOrdering
/// Retrieve a group priority ordering for an app. The target app must be supplied in `additionalTargetKeyNames` in the PolicyTargetKey. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromepolicy_customers_policies_groups_list_group_priority_ordering_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromepolicy_customers_policies_groups_list_group_priority_ordering_task()`.
/// For the simplest API, use `chromepolicy_customers_policies_groups_list_group_priority_ordering()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromepolicy_customers_policies_groups_list_group_priority_ordering_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromepolicy_customers_policies_groups_list_group_priority_ordering_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromePolicyVersionsV1ListGroupPriorityOrderingResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = chromepolicy_customers_policies_groups_list_group_priority_ordering_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromepolicy_customers_policies_groups_list_group_priority_ordering`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromepolicyCustomersPoliciesGroupsListGroupPriorityOrderingArgs {
    /// Path parameter: customer
    pub customer: String,
}

/// POST v1/customers/{customersId}/policies/groups:listGroupPriorityOrdering
/// Retrieve a group priority ordering for an app. The target app must be supplied in `additionalTargetKeyNames` in the PolicyTargetKey. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromepolicy_customers_policies_groups_list_group_priority_ordering_builder()` + `chromepolicy_customers_policies_groups_list_group_priority_ordering_execute()`.
/// For task-level control, use `chromepolicy_customers_policies_groups_list_group_priority_ordering_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_groups_list_group_priority_ordering(
    client: &SimpleHttpClient,
    args: &ChromepolicyCustomersPoliciesGroupsListGroupPriorityOrderingArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromePolicyVersionsV1ListGroupPriorityOrderingResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromepolicy_customers_policies_groups_list_group_priority_ordering_builder(
        client,
        &args.customer,
    )?;
    chromepolicy_customers_policies_groups_list_group_priority_ordering_execute(builder)
}

/// POST v1/customers/{customersId}/policies/groups:updateGroupPriorityOrdering
/// Update a group priority ordering for an app. The target app must be supplied in `additionalTargetKeyNames` in the PolicyTargetKey. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromepolicy_customers_policies_groups_update_group_priority_ordering_execute()` to send, or `chromepolicy_customers_policies_groups_update_group_priority_ordering` for simplest API.

pub fn chromepolicy_customers_policies_groups_update_group_priority_ordering_builder<R>(
    client: &SimpleHttpClient<R>,
    customer: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://chromepolicy.googleapis.com/v1/customers/{}/policies/groups:updateGroupPriorityOrdering",
        customer,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/customers/{customersId}/policies/groups:updateGroupPriorityOrdering
/// Update a group priority ordering for an app. The target app must be supplied in `additionalTargetKeyNames` in the PolicyTargetKey. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromepolicy_customers_policies_groups_update_group_priority_ordering_execute()` or `chromepolicy_customers_policies_groups_update_group_priority_ordering`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromepolicy_customers_policies_groups_update_group_priority_ordering_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_groups_update_group_priority_ordering_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/customers/{customersId}/policies/groups:updateGroupPriorityOrdering
/// Update a group priority ordering for an app. The target app must be supplied in `additionalTargetKeyNames` in the PolicyTargetKey. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromepolicy_customers_policies_groups_update_group_priority_ordering_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromepolicy_customers_policies_groups_update_group_priority_ordering_task()`.
/// For the simplest API, use `chromepolicy_customers_policies_groups_update_group_priority_ordering()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromepolicy_customers_policies_groups_update_group_priority_ordering_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromepolicy_customers_policies_groups_update_group_priority_ordering_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = chromepolicy_customers_policies_groups_update_group_priority_ordering_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromepolicy_customers_policies_groups_update_group_priority_ordering`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromepolicyCustomersPoliciesGroupsUpdateGroupPriorityOrderingArgs {
    /// Path parameter: customer
    pub customer: String,
}

/// POST v1/customers/{customersId}/policies/groups:updateGroupPriorityOrdering
/// Update a group priority ordering for an app. The target app must be supplied in `additionalTargetKeyNames` in the PolicyTargetKey. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromepolicy_customers_policies_groups_update_group_priority_ordering_builder()` + `chromepolicy_customers_policies_groups_update_group_priority_ordering_execute()`.
/// For task-level control, use `chromepolicy_customers_policies_groups_update_group_priority_ordering_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_groups_update_group_priority_ordering(
    client: &SimpleHttpClient,
    args: &ChromepolicyCustomersPoliciesGroupsUpdateGroupPriorityOrderingArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = chromepolicy_customers_policies_groups_update_group_priority_ordering_builder(
        client,
        &args.customer,
    )?;
    chromepolicy_customers_policies_groups_update_group_priority_ordering_execute(builder)
}

/// POST v1/customers/{customersId}/policies/networks:defineCertificate
/// Creates a certificate at a specified OU for a customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromepolicy_customers_policies_networks_define_certificate_execute()` to send, or `chromepolicy_customers_policies_networks_define_certificate` for simplest API.

pub fn chromepolicy_customers_policies_networks_define_certificate_builder<R>(
    client: &SimpleHttpClient<R>,
    customer: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://chromepolicy.googleapis.com/v1/customers/{}/policies/networks:defineCertificate",
        customer,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/customers/{customersId}/policies/networks:defineCertificate
/// Creates a certificate at a specified OU for a customer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromepolicy_customers_policies_networks_define_certificate_execute()` or `chromepolicy_customers_policies_networks_define_certificate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromepolicy_customers_policies_networks_define_certificate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_networks_define_certificate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromePolicyVersionsV1DefineCertificateResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromePolicyVersionsV1DefineCertificateResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/customers/{customersId}/policies/networks:defineCertificate
/// Creates a certificate at a specified OU for a customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromepolicy_customers_policies_networks_define_certificate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromepolicy_customers_policies_networks_define_certificate_task()`.
/// For the simplest API, use `chromepolicy_customers_policies_networks_define_certificate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromepolicy_customers_policies_networks_define_certificate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromepolicy_customers_policies_networks_define_certificate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromePolicyVersionsV1DefineCertificateResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = chromepolicy_customers_policies_networks_define_certificate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromepolicy_customers_policies_networks_define_certificate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromepolicyCustomersPoliciesNetworksDefineCertificateArgs {
    /// Path parameter: customer
    pub customer: String,
}

/// POST v1/customers/{customersId}/policies/networks:defineCertificate
/// Creates a certificate at a specified OU for a customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromepolicy_customers_policies_networks_define_certificate_builder()` + `chromepolicy_customers_policies_networks_define_certificate_execute()`.
/// For task-level control, use `chromepolicy_customers_policies_networks_define_certificate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_networks_define_certificate(
    client: &SimpleHttpClient,
    args: &ChromepolicyCustomersPoliciesNetworksDefineCertificateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromePolicyVersionsV1DefineCertificateResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromepolicy_customers_policies_networks_define_certificate_builder(
        client,
        &args.customer,
    )?;
    chromepolicy_customers_policies_networks_define_certificate_execute(builder)
}

/// POST v1/customers/{customersId}/policies/networks:defineNetwork
/// Define a new network.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromepolicy_customers_policies_networks_define_network_execute()` to send, or `chromepolicy_customers_policies_networks_define_network` for simplest API.

pub fn chromepolicy_customers_policies_networks_define_network_builder<R>(
    client: &SimpleHttpClient<R>,
    customer: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://chromepolicy.googleapis.com/v1/customers/{}/policies/networks:defineNetwork",
        customer,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/customers/{customersId}/policies/networks:defineNetwork
/// Define a new network.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromepolicy_customers_policies_networks_define_network_execute()` or `chromepolicy_customers_policies_networks_define_network`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromepolicy_customers_policies_networks_define_network_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_networks_define_network_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromePolicyVersionsV1DefineNetworkResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromePolicyVersionsV1DefineNetworkResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/customers/{customersId}/policies/networks:defineNetwork
/// Define a new network.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromepolicy_customers_policies_networks_define_network_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromepolicy_customers_policies_networks_define_network_task()`.
/// For the simplest API, use `chromepolicy_customers_policies_networks_define_network()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromepolicy_customers_policies_networks_define_network_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromepolicy_customers_policies_networks_define_network_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromePolicyVersionsV1DefineNetworkResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = chromepolicy_customers_policies_networks_define_network_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromepolicy_customers_policies_networks_define_network`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromepolicyCustomersPoliciesNetworksDefineNetworkArgs {
    /// Path parameter: customer
    pub customer: String,
}

/// POST v1/customers/{customersId}/policies/networks:defineNetwork
/// Define a new network.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromepolicy_customers_policies_networks_define_network_builder()` + `chromepolicy_customers_policies_networks_define_network_execute()`.
/// For task-level control, use `chromepolicy_customers_policies_networks_define_network_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_networks_define_network(
    client: &SimpleHttpClient,
    args: &ChromepolicyCustomersPoliciesNetworksDefineNetworkArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromePolicyVersionsV1DefineNetworkResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        chromepolicy_customers_policies_networks_define_network_builder(client, &args.customer)?;
    chromepolicy_customers_policies_networks_define_network_execute(builder)
}

/// POST v1/customers/{customersId}/policies/networks:removeCertificate
/// Remove an existing certificate by guid.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromepolicy_customers_policies_networks_remove_certificate_execute()` to send, or `chromepolicy_customers_policies_networks_remove_certificate` for simplest API.

pub fn chromepolicy_customers_policies_networks_remove_certificate_builder<R>(
    client: &SimpleHttpClient<R>,
    customer: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://chromepolicy.googleapis.com/v1/customers/{}/policies/networks:removeCertificate",
        customer,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/customers/{customersId}/policies/networks:removeCertificate
/// Remove an existing certificate by guid.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromepolicy_customers_policies_networks_remove_certificate_execute()` or `chromepolicy_customers_policies_networks_remove_certificate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromepolicy_customers_policies_networks_remove_certificate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_networks_remove_certificate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromePolicyVersionsV1RemoveCertificateResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromePolicyVersionsV1RemoveCertificateResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/customers/{customersId}/policies/networks:removeCertificate
/// Remove an existing certificate by guid.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromepolicy_customers_policies_networks_remove_certificate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromepolicy_customers_policies_networks_remove_certificate_task()`.
/// For the simplest API, use `chromepolicy_customers_policies_networks_remove_certificate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromepolicy_customers_policies_networks_remove_certificate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromepolicy_customers_policies_networks_remove_certificate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromePolicyVersionsV1RemoveCertificateResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = chromepolicy_customers_policies_networks_remove_certificate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromepolicy_customers_policies_networks_remove_certificate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromepolicyCustomersPoliciesNetworksRemoveCertificateArgs {
    /// Path parameter: customer
    pub customer: String,
}

/// POST v1/customers/{customersId}/policies/networks:removeCertificate
/// Remove an existing certificate by guid.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromepolicy_customers_policies_networks_remove_certificate_builder()` + `chromepolicy_customers_policies_networks_remove_certificate_execute()`.
/// For task-level control, use `chromepolicy_customers_policies_networks_remove_certificate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_networks_remove_certificate(
    client: &SimpleHttpClient,
    args: &ChromepolicyCustomersPoliciesNetworksRemoveCertificateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromePolicyVersionsV1RemoveCertificateResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromepolicy_customers_policies_networks_remove_certificate_builder(
        client,
        &args.customer,
    )?;
    chromepolicy_customers_policies_networks_remove_certificate_execute(builder)
}

/// POST v1/customers/{customersId}/policies/networks:removeNetwork
/// Remove an existing network by guid.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromepolicy_customers_policies_networks_remove_network_execute()` to send, or `chromepolicy_customers_policies_networks_remove_network` for simplest API.

pub fn chromepolicy_customers_policies_networks_remove_network_builder<R>(
    client: &SimpleHttpClient<R>,
    customer: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://chromepolicy.googleapis.com/v1/customers/{}/policies/networks:removeNetwork",
        customer,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/customers/{customersId}/policies/networks:removeNetwork
/// Remove an existing network by guid.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromepolicy_customers_policies_networks_remove_network_execute()` or `chromepolicy_customers_policies_networks_remove_network`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromepolicy_customers_policies_networks_remove_network_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_networks_remove_network_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromePolicyVersionsV1RemoveNetworkResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromePolicyVersionsV1RemoveNetworkResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/customers/{customersId}/policies/networks:removeNetwork
/// Remove an existing network by guid.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromepolicy_customers_policies_networks_remove_network_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromepolicy_customers_policies_networks_remove_network_task()`.
/// For the simplest API, use `chromepolicy_customers_policies_networks_remove_network()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromepolicy_customers_policies_networks_remove_network_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromepolicy_customers_policies_networks_remove_network_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromePolicyVersionsV1RemoveNetworkResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = chromepolicy_customers_policies_networks_remove_network_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromepolicy_customers_policies_networks_remove_network`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromepolicyCustomersPoliciesNetworksRemoveNetworkArgs {
    /// Path parameter: customer
    pub customer: String,
}

/// POST v1/customers/{customersId}/policies/networks:removeNetwork
/// Remove an existing network by guid.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromepolicy_customers_policies_networks_remove_network_builder()` + `chromepolicy_customers_policies_networks_remove_network_execute()`.
/// For task-level control, use `chromepolicy_customers_policies_networks_remove_network_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_networks_remove_network(
    client: &SimpleHttpClient,
    args: &ChromepolicyCustomersPoliciesNetworksRemoveNetworkArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromePolicyVersionsV1RemoveNetworkResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        chromepolicy_customers_policies_networks_remove_network_builder(client, &args.customer)?;
    chromepolicy_customers_policies_networks_remove_network_execute(builder)
}

/// POST v1/customers/{customersId}/policies/orgunits:batchInherit
/// Modify multiple policy values that are applied to a specific org unit so that they now inherit the value from a parent (if applicable). All targets must have the same target format. That is to say that they must point to the same target resource and must have the same keys specified in `additionalTargetKeyNames`, though the values for those keys may be different. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromepolicy_customers_policies_orgunits_batch_inherit_execute()` to send, or `chromepolicy_customers_policies_orgunits_batch_inherit` for simplest API.

pub fn chromepolicy_customers_policies_orgunits_batch_inherit_builder<R>(
    client: &SimpleHttpClient<R>,
    customer: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://chromepolicy.googleapis.com/v1/customers/{}/policies/orgunits:batchInherit",
        customer,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/customers/{customersId}/policies/orgunits:batchInherit
/// Modify multiple policy values that are applied to a specific org unit so that they now inherit the value from a parent (if applicable). All targets must have the same target format. That is to say that they must point to the same target resource and must have the same keys specified in `additionalTargetKeyNames`, though the values for those keys may be different. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromepolicy_customers_policies_orgunits_batch_inherit_execute()` or `chromepolicy_customers_policies_orgunits_batch_inherit`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromepolicy_customers_policies_orgunits_batch_inherit_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_orgunits_batch_inherit_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/customers/{customersId}/policies/orgunits:batchInherit
/// Modify multiple policy values that are applied to a specific org unit so that they now inherit the value from a parent (if applicable). All targets must have the same target format. That is to say that they must point to the same target resource and must have the same keys specified in `additionalTargetKeyNames`, though the values for those keys may be different. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromepolicy_customers_policies_orgunits_batch_inherit_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromepolicy_customers_policies_orgunits_batch_inherit_task()`.
/// For the simplest API, use `chromepolicy_customers_policies_orgunits_batch_inherit()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromepolicy_customers_policies_orgunits_batch_inherit_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromepolicy_customers_policies_orgunits_batch_inherit_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = chromepolicy_customers_policies_orgunits_batch_inherit_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromepolicy_customers_policies_orgunits_batch_inherit`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromepolicyCustomersPoliciesOrgunitsBatchInheritArgs {
    /// Path parameter: customer
    pub customer: String,
}

/// POST v1/customers/{customersId}/policies/orgunits:batchInherit
/// Modify multiple policy values that are applied to a specific org unit so that they now inherit the value from a parent (if applicable). All targets must have the same target format. That is to say that they must point to the same target resource and must have the same keys specified in `additionalTargetKeyNames`, though the values for those keys may be different. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromepolicy_customers_policies_orgunits_batch_inherit_builder()` + `chromepolicy_customers_policies_orgunits_batch_inherit_execute()`.
/// For task-level control, use `chromepolicy_customers_policies_orgunits_batch_inherit_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_orgunits_batch_inherit(
    client: &SimpleHttpClient,
    args: &ChromepolicyCustomersPoliciesOrgunitsBatchInheritArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        chromepolicy_customers_policies_orgunits_batch_inherit_builder(client, &args.customer)?;
    chromepolicy_customers_policies_orgunits_batch_inherit_execute(builder)
}

/// POST v1/customers/{customersId}/policies/orgunits:batchModify
/// Modify multiple policy values that are applied to a specific org unit. All targets must have the same target format. That is to say that they must point to the same target resource and must have the same keys specified in `additionalTargetKeyNames`, though the values for those keys may be different. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromepolicy_customers_policies_orgunits_batch_modify_execute()` to send, or `chromepolicy_customers_policies_orgunits_batch_modify` for simplest API.

pub fn chromepolicy_customers_policies_orgunits_batch_modify_builder<R>(
    client: &SimpleHttpClient<R>,
    customer: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://chromepolicy.googleapis.com/v1/customers/{}/policies/orgunits:batchModify",
        customer,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/customers/{customersId}/policies/orgunits:batchModify
/// Modify multiple policy values that are applied to a specific org unit. All targets must have the same target format. That is to say that they must point to the same target resource and must have the same keys specified in `additionalTargetKeyNames`, though the values for those keys may be different. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromepolicy_customers_policies_orgunits_batch_modify_execute()` or `chromepolicy_customers_policies_orgunits_batch_modify`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromepolicy_customers_policies_orgunits_batch_modify_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_orgunits_batch_modify_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/customers/{customersId}/policies/orgunits:batchModify
/// Modify multiple policy values that are applied to a specific org unit. All targets must have the same target format. That is to say that they must point to the same target resource and must have the same keys specified in `additionalTargetKeyNames`, though the values for those keys may be different. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromepolicy_customers_policies_orgunits_batch_modify_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromepolicy_customers_policies_orgunits_batch_modify_task()`.
/// For the simplest API, use `chromepolicy_customers_policies_orgunits_batch_modify()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromepolicy_customers_policies_orgunits_batch_modify_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromepolicy_customers_policies_orgunits_batch_modify_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = chromepolicy_customers_policies_orgunits_batch_modify_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromepolicy_customers_policies_orgunits_batch_modify`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromepolicyCustomersPoliciesOrgunitsBatchModifyArgs {
    /// Path parameter: customer
    pub customer: String,
}

/// POST v1/customers/{customersId}/policies/orgunits:batchModify
/// Modify multiple policy values that are applied to a specific org unit. All targets must have the same target format. That is to say that they must point to the same target resource and must have the same keys specified in `additionalTargetKeyNames`, though the values for those keys may be different. On failure the request will return the error details as part of the google.rpc.Status.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromepolicy_customers_policies_orgunits_batch_modify_builder()` + `chromepolicy_customers_policies_orgunits_batch_modify_execute()`.
/// For task-level control, use `chromepolicy_customers_policies_orgunits_batch_modify_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policies_orgunits_batch_modify(
    client: &SimpleHttpClient,
    args: &ChromepolicyCustomersPoliciesOrgunitsBatchModifyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        chromepolicy_customers_policies_orgunits_batch_modify_builder(client, &args.customer)?;
    chromepolicy_customers_policies_orgunits_batch_modify_execute(builder)
}

/// GET v1/customers/{customersId}/policySchemas/{policySchemasId}
/// Get a specific policy schema for a customer by its resource name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromepolicy_customers_policy_schemas_get_execute()` to send, or `chromepolicy_customers_policy_schemas_get` for simplest API.

pub fn chromepolicy_customers_policy_schemas_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://chromepolicy.googleapis.com/v1/customers/{}/policySchemas/{policySchemasId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customers/{customersId}/policySchemas/{policySchemasId}
/// Get a specific policy schema for a customer by its resource name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromepolicy_customers_policy_schemas_get_execute()` or `chromepolicy_customers_policy_schemas_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromepolicy_customers_policy_schemas_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policy_schemas_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleChromePolicyVersionsV1PolicySchema>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromePolicyVersionsV1PolicySchema = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customers/{customersId}/policySchemas/{policySchemasId}
/// Get a specific policy schema for a customer by its resource name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromepolicy_customers_policy_schemas_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromepolicy_customers_policy_schemas_get_task()`.
/// For the simplest API, use `chromepolicy_customers_policy_schemas_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromepolicy_customers_policy_schemas_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromepolicy_customers_policy_schemas_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromePolicyVersionsV1PolicySchema>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = chromepolicy_customers_policy_schemas_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromepolicy_customers_policy_schemas_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromepolicyCustomersPolicySchemasGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/customers/{customersId}/policySchemas/{policySchemasId}
/// Get a specific policy schema for a customer by its resource name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromepolicy_customers_policy_schemas_get_builder()` + `chromepolicy_customers_policy_schemas_get_execute()`.
/// For task-level control, use `chromepolicy_customers_policy_schemas_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policy_schemas_get(
    client: &SimpleHttpClient,
    args: &ChromepolicyCustomersPolicySchemasGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromePolicyVersionsV1PolicySchema>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromepolicy_customers_policy_schemas_get_builder(client, &args.name)?;
    chromepolicy_customers_policy_schemas_get_execute(builder)
}

/// GET v1/customers/{customersId}/policySchemas
/// Gets a list of policy schemas that match a specified filter value for a given customer.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromepolicy_customers_policy_schemas_list_execute()` to send, or `chromepolicy_customers_policy_schemas_list` for simplest API.

pub fn chromepolicy_customers_policy_schemas_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://chromepolicy.googleapis.com/v1/customers/{}/policySchemas",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/customers/{customersId}/policySchemas
/// Gets a list of policy schemas that match a specified filter value for a given customer.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromepolicy_customers_policy_schemas_list_execute()` or `chromepolicy_customers_policy_schemas_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromepolicy_customers_policy_schemas_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policy_schemas_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromePolicyVersionsV1ListPolicySchemasResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromePolicyVersionsV1ListPolicySchemasResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/customers/{customersId}/policySchemas
/// Gets a list of policy schemas that match a specified filter value for a given customer.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromepolicy_customers_policy_schemas_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromepolicy_customers_policy_schemas_list_task()`.
/// For the simplest API, use `chromepolicy_customers_policy_schemas_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromepolicy_customers_policy_schemas_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromepolicy_customers_policy_schemas_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromePolicyVersionsV1ListPolicySchemasResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = chromepolicy_customers_policy_schemas_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromepolicy_customers_policy_schemas_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromepolicyCustomersPolicySchemasListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/customers/{customersId}/policySchemas
/// Gets a list of policy schemas that match a specified filter value for a given customer.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromepolicy_customers_policy_schemas_list_builder()` + `chromepolicy_customers_policy_schemas_list_execute()`.
/// For task-level control, use `chromepolicy_customers_policy_schemas_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_customers_policy_schemas_list(
    client: &SimpleHttpClient,
    args: &ChromepolicyCustomersPolicySchemasListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleChromePolicyVersionsV1ListPolicySchemasResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromepolicy_customers_policy_schemas_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    chromepolicy_customers_policy_schemas_list_execute(builder)
}

/// POST v1/customers/{customersId}/policies/files:uploadPolicyFile
/// Creates an enterprise file from the content provided by user. Returns a public download url for end user.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromepolicy_media_upload_execute()` to send, or `chromepolicy_media_upload` for simplest API.

pub fn chromepolicy_media_upload_builder<R>(
    client: &SimpleHttpClient<R>,
    customer: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://chromepolicy.googleapis.com/v1/customers/{}/policies/files:uploadPolicyFile",
        customer,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/customers/{customersId}/policies/files:uploadPolicyFile
/// Creates an enterprise file from the content provided by user. Returns a public download url for end user.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromepolicy_media_upload_execute()` or `chromepolicy_media_upload`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromepolicy_media_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_media_upload_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChromePolicyVersionsV1UploadPolicyFileResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChromePolicyVersionsV1UploadPolicyFileResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/customers/{customersId}/policies/files:uploadPolicyFile
/// Creates an enterprise file from the content provided by user. Returns a public download url for end user.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromepolicy_media_upload_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromepolicy_media_upload_task()`.
/// For the simplest API, use `chromepolicy_media_upload()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromepolicy_media_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromepolicy_media_upload_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromePolicyVersionsV1UploadPolicyFileResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = chromepolicy_media_upload_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`chromepolicy_media_upload`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChromepolicyMediaUploadArgs {
    /// Path parameter: customer
    pub customer: String,
}

/// POST v1/customers/{customersId}/policies/files:uploadPolicyFile
/// Creates an enterprise file from the content provided by user. Returns a public download url for end user.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromepolicy_media_upload_builder()` + `chromepolicy_media_upload_execute()`.
/// For task-level control, use `chromepolicy_media_upload_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromepolicy_media_upload(
    client: &SimpleHttpClient,
    args: &ChromepolicyMediaUploadArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChromePolicyVersionsV1UploadPolicyFileResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = chromepolicy_media_upload_builder(client, &args.customer)?;
    chromepolicy_media_upload_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for GoogleChromePolicyVersionsV1ResolveResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleChromePolicyVersionsV1ResolveResponse with ChromepolicyCustomersPoliciesResolveArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ChromepolicyCustomersPoliciesResolveArgs>
    for GoogleChromePolicyVersionsV1ResolveResponse
{
    fn generate_resource_id(&self, input: &ChromepolicyCustomersPoliciesResolveArgs) -> String {
        format!(
            "gcp::chromepolicy::GoogleChromePolicyVersionsV1ResolveResponse/{}",
            input.customer
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::chromepolicy::GoogleChromePolicyVersionsV1ResolveResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with ChromepolicyCustomersPoliciesGroupsBatchDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ChromepolicyCustomersPoliciesGroupsBatchDeleteArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &ChromepolicyCustomersPoliciesGroupsBatchDeleteArgs,
    ) -> String {
        format!("gcp::chromepolicy::GoogleProtobufEmpty/{}", input.customer)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::chromepolicy::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with ChromepolicyCustomersPoliciesGroupsBatchModifyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ChromepolicyCustomersPoliciesGroupsBatchModifyArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &ChromepolicyCustomersPoliciesGroupsBatchModifyArgs,
    ) -> String {
        format!("gcp::chromepolicy::GoogleProtobufEmpty/{}", input.customer)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::chromepolicy::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleChromePolicyVersionsV1ListGroupPriorityOrderingResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleChromePolicyVersionsV1ListGroupPriorityOrderingResponse with ChromepolicyCustomersPoliciesGroupsListGroupPriorityOrderingArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ChromepolicyCustomersPoliciesGroupsListGroupPriorityOrderingArgs>
    for GoogleChromePolicyVersionsV1ListGroupPriorityOrderingResponse
{
    fn generate_resource_id(
        &self,
        input: &ChromepolicyCustomersPoliciesGroupsListGroupPriorityOrderingArgs,
    ) -> String {
        format!(
            "gcp::chromepolicy::GoogleChromePolicyVersionsV1ListGroupPriorityOrderingResponse/{}",
            input.customer
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::chromepolicy::GoogleChromePolicyVersionsV1ListGroupPriorityOrderingResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with ChromepolicyCustomersPoliciesGroupsUpdateGroupPriorityOrderingArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ChromepolicyCustomersPoliciesGroupsUpdateGroupPriorityOrderingArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &ChromepolicyCustomersPoliciesGroupsUpdateGroupPriorityOrderingArgs,
    ) -> String {
        format!("gcp::chromepolicy::GoogleProtobufEmpty/{}", input.customer)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::chromepolicy::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleChromePolicyVersionsV1DefineCertificateResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleChromePolicyVersionsV1DefineCertificateResponse with ChromepolicyCustomersPoliciesNetworksDefineCertificateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ChromepolicyCustomersPoliciesNetworksDefineCertificateArgs>
    for GoogleChromePolicyVersionsV1DefineCertificateResponse
{
    fn generate_resource_id(
        &self,
        input: &ChromepolicyCustomersPoliciesNetworksDefineCertificateArgs,
    ) -> String {
        format!(
            "gcp::chromepolicy::GoogleChromePolicyVersionsV1DefineCertificateResponse/{}",
            input.customer
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::chromepolicy::GoogleChromePolicyVersionsV1DefineCertificateResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleChromePolicyVersionsV1DefineNetworkResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleChromePolicyVersionsV1DefineNetworkResponse with ChromepolicyCustomersPoliciesNetworksDefineNetworkArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ChromepolicyCustomersPoliciesNetworksDefineNetworkArgs>
    for GoogleChromePolicyVersionsV1DefineNetworkResponse
{
    fn generate_resource_id(
        &self,
        input: &ChromepolicyCustomersPoliciesNetworksDefineNetworkArgs,
    ) -> String {
        format!(
            "gcp::chromepolicy::GoogleChromePolicyVersionsV1DefineNetworkResponse/{}",
            input.customer
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::chromepolicy::GoogleChromePolicyVersionsV1DefineNetworkResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleChromePolicyVersionsV1RemoveCertificateResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleChromePolicyVersionsV1RemoveCertificateResponse with ChromepolicyCustomersPoliciesNetworksRemoveCertificateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ChromepolicyCustomersPoliciesNetworksRemoveCertificateArgs>
    for GoogleChromePolicyVersionsV1RemoveCertificateResponse
{
    fn generate_resource_id(
        &self,
        input: &ChromepolicyCustomersPoliciesNetworksRemoveCertificateArgs,
    ) -> String {
        format!(
            "gcp::chromepolicy::GoogleChromePolicyVersionsV1RemoveCertificateResponse/{}",
            input.customer
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::chromepolicy::GoogleChromePolicyVersionsV1RemoveCertificateResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleChromePolicyVersionsV1RemoveNetworkResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleChromePolicyVersionsV1RemoveNetworkResponse with ChromepolicyCustomersPoliciesNetworksRemoveNetworkArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ChromepolicyCustomersPoliciesNetworksRemoveNetworkArgs>
    for GoogleChromePolicyVersionsV1RemoveNetworkResponse
{
    fn generate_resource_id(
        &self,
        input: &ChromepolicyCustomersPoliciesNetworksRemoveNetworkArgs,
    ) -> String {
        format!(
            "gcp::chromepolicy::GoogleChromePolicyVersionsV1RemoveNetworkResponse/{}",
            input.customer
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::chromepolicy::GoogleChromePolicyVersionsV1RemoveNetworkResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with ChromepolicyCustomersPoliciesOrgunitsBatchInheritArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ChromepolicyCustomersPoliciesOrgunitsBatchInheritArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &ChromepolicyCustomersPoliciesOrgunitsBatchInheritArgs,
    ) -> String {
        format!("gcp::chromepolicy::GoogleProtobufEmpty/{}", input.customer)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::chromepolicy::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleProtobufEmpty
// =============================================================================

/// ResourceIdentifier implementation for GoogleProtobufEmpty with ChromepolicyCustomersPoliciesOrgunitsBatchModifyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ChromepolicyCustomersPoliciesOrgunitsBatchModifyArgs>
    for GoogleProtobufEmpty
{
    fn generate_resource_id(
        &self,
        input: &ChromepolicyCustomersPoliciesOrgunitsBatchModifyArgs,
    ) -> String {
        format!("gcp::chromepolicy::GoogleProtobufEmpty/{}", input.customer)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::chromepolicy::GoogleProtobufEmpty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleChromePolicyVersionsV1PolicySchema
// =============================================================================

/// ResourceIdentifier implementation for GoogleChromePolicyVersionsV1PolicySchema with ChromepolicyCustomersPolicySchemasGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ChromepolicyCustomersPolicySchemasGetArgs>
    for GoogleChromePolicyVersionsV1PolicySchema
{
    fn generate_resource_id(&self, input: &ChromepolicyCustomersPolicySchemasGetArgs) -> String {
        format!(
            "gcp::chromepolicy::GoogleChromePolicyVersionsV1PolicySchema/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::chromepolicy::GoogleChromePolicyVersionsV1PolicySchema"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleChromePolicyVersionsV1ListPolicySchemasResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleChromePolicyVersionsV1ListPolicySchemasResponse with ChromepolicyCustomersPolicySchemasListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ChromepolicyCustomersPolicySchemasListArgs>
    for GoogleChromePolicyVersionsV1ListPolicySchemasResponse
{
    fn generate_resource_id(&self, input: &ChromepolicyCustomersPolicySchemasListArgs) -> String {
        format!(
            "gcp::chromepolicy::GoogleChromePolicyVersionsV1ListPolicySchemasResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::chromepolicy::GoogleChromePolicyVersionsV1ListPolicySchemasResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleChromePolicyVersionsV1UploadPolicyFileResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleChromePolicyVersionsV1UploadPolicyFileResponse with ChromepolicyMediaUploadArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ChromepolicyMediaUploadArgs>
    for GoogleChromePolicyVersionsV1UploadPolicyFileResponse
{
    fn generate_resource_id(&self, input: &ChromepolicyMediaUploadArgs) -> String {
        format!(
            "gcp::chromepolicy::GoogleChromePolicyVersionsV1UploadPolicyFileResponse/{}",
            input.customer
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::chromepolicy::GoogleChromePolicyVersionsV1UploadPolicyFileResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
