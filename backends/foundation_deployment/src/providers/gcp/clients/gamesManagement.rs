//! Auto-generated API clients for gcp/gamesManagement.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET games/v1management/achievements/{achievementId}/reset
/// Resets the achievement with the given ID for the currently authenticated player. This method is only accessible to whitelisted tester accounts for your application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_management_achievements_reset_execute()` to send, or `games_management_achievements_reset` for simplest API.

pub fn games_management_achievements_reset_builder(
    client: &SimpleHttpClient,
    achievementId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://gamesmanagement.googleapis.com/games/v1management/achievements/{}/reset",
        achievementId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1management/achievements/{achievementId}/reset
/// Resets the achievement with the given ID for the currently authenticated player. This method is only accessible to whitelisted tester accounts for your application.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_management_achievements_reset_execute()` or `games_management_achievements_reset`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_management_achievements_reset_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_management_achievements_reset_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AchievementResetResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AchievementResetResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1management/achievements/{achievementId}/reset
/// Resets the achievement with the given ID for the currently authenticated player. This method is only accessible to whitelisted tester accounts for your application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_management_achievements_reset_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_management_achievements_reset_task()`.
/// For the simplest API, use `games_management_achievements_reset()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_management_achievements_reset_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_management_achievements_reset_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AchievementResetResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = games_management_achievements_reset_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_management_achievements_reset`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesManagementAchievementsResetArgs {
    /// Path parameter: achievementId
    pub achievementId: String,
}

/// GET games/v1management/achievements/{achievementId}/reset
/// Resets the achievement with the given ID for the currently authenticated player. This method is only accessible to whitelisted tester accounts for your application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_management_achievements_reset_builder()` + `games_management_achievements_reset_execute()`.
/// For task-level control, use `games_management_achievements_reset_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_management_achievements_reset(
    client: &SimpleHttpClient,
    args: &GamesManagementAchievementsResetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AchievementResetResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_management_achievements_reset_builder(client, &args.achievementId)?;
    games_management_achievements_reset_execute(builder)
}

/// GET games/v1management/achievements/reset
/// Resets all achievements for the currently authenticated player for your application. This method is only accessible to whitelisted tester accounts for your application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_management_achievements_reset_all_execute()` to send, or `games_management_achievements_reset_all` for simplest API.

pub fn games_management_achievements_reset_all_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://gamesmanagement.googleapis.com/games/v1management/achievements/reset",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1management/achievements/reset
/// Resets all achievements for the currently authenticated player for your application. This method is only accessible to whitelisted tester accounts for your application.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_management_achievements_reset_all_execute()` or `games_management_achievements_reset_all`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_management_achievements_reset_all_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_management_achievements_reset_all_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AchievementResetAllResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AchievementResetAllResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1management/achievements/reset
/// Resets all achievements for the currently authenticated player for your application. This method is only accessible to whitelisted tester accounts for your application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_management_achievements_reset_all_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_management_achievements_reset_all_task()`.
/// For the simplest API, use `games_management_achievements_reset_all()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_management_achievements_reset_all_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_management_achievements_reset_all_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AchievementResetAllResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = games_management_achievements_reset_all_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1management/achievements/reset
/// Resets all achievements for the currently authenticated player for your application. This method is only accessible to whitelisted tester accounts for your application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_management_achievements_reset_all_builder()` + `games_management_achievements_reset_all_execute()`.
/// For task-level control, use `games_management_achievements_reset_all_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_management_achievements_reset_all(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<AchievementResetAllResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = games_management_achievements_reset_all_builder(client)?;
    games_management_achievements_reset_all_execute(builder)
}

/// GET games/v1management/achievements/resetAllForAllPlayers
/// Resets all draft achievements for all players. This method is only available to user accounts for your developer console.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_management_achievements_reset_all_for_all_players_execute()` to send, or `games_management_achievements_reset_all_for_all_players` for simplest API.

pub fn games_management_achievements_reset_all_for_all_players_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://gamesmanagement.googleapis.com/games/v1management/achievements/resetAllForAllPlayers",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1management/achievements/resetAllForAllPlayers
/// Resets all draft achievements for all players. This method is only available to user accounts for your developer console.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_management_achievements_reset_all_for_all_players_execute()` or `games_management_achievements_reset_all_for_all_players`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_management_achievements_reset_all_for_all_players_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_management_achievements_reset_all_for_all_players_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1management/achievements/resetAllForAllPlayers
/// Resets all draft achievements for all players. This method is only available to user accounts for your developer console.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_management_achievements_reset_all_for_all_players_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_management_achievements_reset_all_for_all_players_task()`.
/// For the simplest API, use `games_management_achievements_reset_all_for_all_players()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_management_achievements_reset_all_for_all_players_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_management_achievements_reset_all_for_all_players_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = games_management_achievements_reset_all_for_all_players_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1management/achievements/resetAllForAllPlayers
/// Resets all draft achievements for all players. This method is only available to user accounts for your developer console.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_management_achievements_reset_all_for_all_players_builder()` + `games_management_achievements_reset_all_for_all_players_execute()`.
/// For task-level control, use `games_management_achievements_reset_all_for_all_players_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_management_achievements_reset_all_for_all_players(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = games_management_achievements_reset_all_for_all_players_builder(client)?;
    games_management_achievements_reset_all_for_all_players_execute(builder)
}

/// GET games/v1management/achievements/{achievementId}/resetForAllPlayers
/// Resets the achievement with the given ID for all players. This method is only available to user accounts for your developer console. Only draft achievements can be reset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_management_achievements_reset_for_all_players_execute()` to send, or `games_management_achievements_reset_for_all_players` for simplest API.

pub fn games_management_achievements_reset_for_all_players_builder(
    client: &SimpleHttpClient,
    achievementId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://gamesmanagement.googleapis.com/games/v1management/achievements/{}/resetForAllPlayers",
        achievementId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1management/achievements/{achievementId}/resetForAllPlayers
/// Resets the achievement with the given ID for all players. This method is only available to user accounts for your developer console. Only draft achievements can be reset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_management_achievements_reset_for_all_players_execute()` or `games_management_achievements_reset_for_all_players`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_management_achievements_reset_for_all_players_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_management_achievements_reset_for_all_players_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1management/achievements/{achievementId}/resetForAllPlayers
/// Resets the achievement with the given ID for all players. This method is only available to user accounts for your developer console. Only draft achievements can be reset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_management_achievements_reset_for_all_players_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_management_achievements_reset_for_all_players_task()`.
/// For the simplest API, use `games_management_achievements_reset_for_all_players()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_management_achievements_reset_for_all_players_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_management_achievements_reset_for_all_players_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = games_management_achievements_reset_for_all_players_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_management_achievements_reset_for_all_players`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesManagementAchievementsResetForAllPlayersArgs {
    /// Path parameter: achievementId
    pub achievementId: String,
}

/// GET games/v1management/achievements/{achievementId}/resetForAllPlayers
/// Resets the achievement with the given ID for all players. This method is only available to user accounts for your developer console. Only draft achievements can be reset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_management_achievements_reset_for_all_players_builder()` + `games_management_achievements_reset_for_all_players_execute()`.
/// For task-level control, use `games_management_achievements_reset_for_all_players_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_management_achievements_reset_for_all_players(
    client: &SimpleHttpClient,
    args: &GamesManagementAchievementsResetForAllPlayersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        games_management_achievements_reset_for_all_players_builder(client, &args.achievementId)?;
    games_management_achievements_reset_for_all_players_execute(builder)
}

/// GET games/v1management/achievements/resetMultipleForAllPlayers
/// Resets achievements with the given IDs for all players. This method is only available to user accounts for your developer console. Only draft achievements may be reset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_management_achievements_reset_multiple_for_all_players_execute()` to send, or `games_management_achievements_reset_multiple_for_all_players` for simplest API.

pub fn games_management_achievements_reset_multiple_for_all_players_builder(
    client: &SimpleHttpClient,
    body: &AchievementResetMultipleForAllRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://gamesmanagement.googleapis.com/games/v1management/achievements/resetMultipleForAllPlayers",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1management/achievements/resetMultipleForAllPlayers
/// Resets achievements with the given IDs for all players. This method is only available to user accounts for your developer console. Only draft achievements may be reset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_management_achievements_reset_multiple_for_all_players_execute()` or `games_management_achievements_reset_multiple_for_all_players`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_management_achievements_reset_multiple_for_all_players_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_management_achievements_reset_multiple_for_all_players_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1management/achievements/resetMultipleForAllPlayers
/// Resets achievements with the given IDs for all players. This method is only available to user accounts for your developer console. Only draft achievements may be reset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_management_achievements_reset_multiple_for_all_players_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_management_achievements_reset_multiple_for_all_players_task()`.
/// For the simplest API, use `games_management_achievements_reset_multiple_for_all_players()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_management_achievements_reset_multiple_for_all_players_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_management_achievements_reset_multiple_for_all_players_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = games_management_achievements_reset_multiple_for_all_players_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_management_achievements_reset_multiple_for_all_players`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesManagementAchievementsResetMultipleForAllPlayersArgs {
    /// Request body.
    pub body: AchievementResetMultipleForAllRequest,
}

/// GET games/v1management/achievements/resetMultipleForAllPlayers
/// Resets achievements with the given IDs for all players. This method is only available to user accounts for your developer console. Only draft achievements may be reset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_management_achievements_reset_multiple_for_all_players_builder()` + `games_management_achievements_reset_multiple_for_all_players_execute()`.
/// For task-level control, use `games_management_achievements_reset_multiple_for_all_players_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_management_achievements_reset_multiple_for_all_players(
    client: &SimpleHttpClient,
    args: &GamesManagementAchievementsResetMultipleForAllPlayersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        games_management_achievements_reset_multiple_for_all_players_builder(client, &args.body)?;
    games_management_achievements_reset_multiple_for_all_players_execute(builder)
}

/// GET games/v1management/applications/{applicationId}/players/hidden
/// Get the list of players hidden from the given application. This method is only available to user accounts for your developer console.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_management_applications_list_hidden_execute()` to send, or `games_management_applications_list_hidden` for simplest API.

pub fn games_management_applications_list_hidden_builder(
    client: &SimpleHttpClient,
    applicationId: &String,
    maxResults: &Option<i32>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://gamesmanagement.googleapis.com/games/v1management/applications/{}/players/hidden",
        applicationId,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = maxResults.as_ref() {
        query_parts.push(format!("maxResults={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1management/applications/{applicationId}/players/hidden
/// Get the list of players hidden from the given application. This method is only available to user accounts for your developer console.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_management_applications_list_hidden_execute()` or `games_management_applications_list_hidden`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_management_applications_list_hidden_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_management_applications_list_hidden_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HiddenPlayerList>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: HiddenPlayerList = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1management/applications/{applicationId}/players/hidden
/// Get the list of players hidden from the given application. This method is only available to user accounts for your developer console.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_management_applications_list_hidden_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_management_applications_list_hidden_task()`.
/// For the simplest API, use `games_management_applications_list_hidden()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_management_applications_list_hidden_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_management_applications_list_hidden_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HiddenPlayerList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = games_management_applications_list_hidden_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_management_applications_list_hidden`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesManagementApplicationsListHiddenArgs {
    /// Path parameter: applicationId
    pub applicationId: String,
    /// Query parameter: maxResults
    pub maxResults: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET games/v1management/applications/{applicationId}/players/hidden
/// Get the list of players hidden from the given application. This method is only available to user accounts for your developer console.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_management_applications_list_hidden_builder()` + `games_management_applications_list_hidden_execute()`.
/// For task-level control, use `games_management_applications_list_hidden_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_management_applications_list_hidden(
    client: &SimpleHttpClient,
    args: &GamesManagementApplicationsListHiddenArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<HiddenPlayerList>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_management_applications_list_hidden_builder(
        client,
        &args.applicationId,
        &args.maxResults,
        &args.pageToken,
    )?;
    games_management_applications_list_hidden_execute(builder)
}

/// GET games/v1management/events/{eventId}/reset
/// Resets all player progress on the event with the given ID for the currently authenticated player. This method is only accessible to whitelisted tester accounts for your application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_management_events_reset_execute()` to send, or `games_management_events_reset` for simplest API.

pub fn games_management_events_reset_builder(
    client: &SimpleHttpClient,
    eventId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://gamesmanagement.googleapis.com/games/v1management/events/{}/reset",
        eventId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1management/events/{eventId}/reset
/// Resets all player progress on the event with the given ID for the currently authenticated player. This method is only accessible to whitelisted tester accounts for your application.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_management_events_reset_execute()` or `games_management_events_reset`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_management_events_reset_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_management_events_reset_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1management/events/{eventId}/reset
/// Resets all player progress on the event with the given ID for the currently authenticated player. This method is only accessible to whitelisted tester accounts for your application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_management_events_reset_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_management_events_reset_task()`.
/// For the simplest API, use `games_management_events_reset()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_management_events_reset_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_management_events_reset_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = games_management_events_reset_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_management_events_reset`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesManagementEventsResetArgs {
    /// Path parameter: eventId
    pub eventId: String,
}

/// GET games/v1management/events/{eventId}/reset
/// Resets all player progress on the event with the given ID for the currently authenticated player. This method is only accessible to whitelisted tester accounts for your application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_management_events_reset_builder()` + `games_management_events_reset_execute()`.
/// For task-level control, use `games_management_events_reset_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_management_events_reset(
    client: &SimpleHttpClient,
    args: &GamesManagementEventsResetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = games_management_events_reset_builder(client, &args.eventId)?;
    games_management_events_reset_execute(builder)
}

/// GET games/v1management/events/reset
/// Resets all player progress on all events for the currently authenticated player. This method is only accessible to whitelisted tester accounts for your application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_management_events_reset_all_execute()` to send, or `games_management_events_reset_all` for simplest API.

pub fn games_management_events_reset_all_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://gamesmanagement.googleapis.com/games/v1management/events/reset",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1management/events/reset
/// Resets all player progress on all events for the currently authenticated player. This method is only accessible to whitelisted tester accounts for your application.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_management_events_reset_all_execute()` or `games_management_events_reset_all`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_management_events_reset_all_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_management_events_reset_all_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1management/events/reset
/// Resets all player progress on all events for the currently authenticated player. This method is only accessible to whitelisted tester accounts for your application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_management_events_reset_all_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_management_events_reset_all_task()`.
/// For the simplest API, use `games_management_events_reset_all()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_management_events_reset_all_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_management_events_reset_all_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = games_management_events_reset_all_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1management/events/reset
/// Resets all player progress on all events for the currently authenticated player. This method is only accessible to whitelisted tester accounts for your application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_management_events_reset_all_builder()` + `games_management_events_reset_all_execute()`.
/// For task-level control, use `games_management_events_reset_all_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_management_events_reset_all(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = games_management_events_reset_all_builder(client)?;
    games_management_events_reset_all_execute(builder)
}

/// GET games/v1management/events/resetAllForAllPlayers
/// Resets all draft events for all players. This method is only available to user accounts for your developer console.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_management_events_reset_all_for_all_players_execute()` to send, or `games_management_events_reset_all_for_all_players` for simplest API.

pub fn games_management_events_reset_all_for_all_players_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://gamesmanagement.googleapis.com/games/v1management/events/resetAllForAllPlayers",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1management/events/resetAllForAllPlayers
/// Resets all draft events for all players. This method is only available to user accounts for your developer console.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_management_events_reset_all_for_all_players_execute()` or `games_management_events_reset_all_for_all_players`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_management_events_reset_all_for_all_players_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_management_events_reset_all_for_all_players_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1management/events/resetAllForAllPlayers
/// Resets all draft events for all players. This method is only available to user accounts for your developer console.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_management_events_reset_all_for_all_players_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_management_events_reset_all_for_all_players_task()`.
/// For the simplest API, use `games_management_events_reset_all_for_all_players()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_management_events_reset_all_for_all_players_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_management_events_reset_all_for_all_players_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = games_management_events_reset_all_for_all_players_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1management/events/resetAllForAllPlayers
/// Resets all draft events for all players. This method is only available to user accounts for your developer console.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_management_events_reset_all_for_all_players_builder()` + `games_management_events_reset_all_for_all_players_execute()`.
/// For task-level control, use `games_management_events_reset_all_for_all_players_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_management_events_reset_all_for_all_players(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = games_management_events_reset_all_for_all_players_builder(client)?;
    games_management_events_reset_all_for_all_players_execute(builder)
}

/// GET games/v1management/events/{eventId}/resetForAllPlayers
/// Resets the event with the given ID for all players. This method is only available to user accounts for your developer console. Only draft events can be reset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_management_events_reset_for_all_players_execute()` to send, or `games_management_events_reset_for_all_players` for simplest API.

pub fn games_management_events_reset_for_all_players_builder(
    client: &SimpleHttpClient,
    eventId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://gamesmanagement.googleapis.com/games/v1management/events/{}/resetForAllPlayers",
        eventId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1management/events/{eventId}/resetForAllPlayers
/// Resets the event with the given ID for all players. This method is only available to user accounts for your developer console. Only draft events can be reset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_management_events_reset_for_all_players_execute()` or `games_management_events_reset_for_all_players`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_management_events_reset_for_all_players_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_management_events_reset_for_all_players_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1management/events/{eventId}/resetForAllPlayers
/// Resets the event with the given ID for all players. This method is only available to user accounts for your developer console. Only draft events can be reset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_management_events_reset_for_all_players_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_management_events_reset_for_all_players_task()`.
/// For the simplest API, use `games_management_events_reset_for_all_players()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_management_events_reset_for_all_players_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_management_events_reset_for_all_players_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = games_management_events_reset_for_all_players_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_management_events_reset_for_all_players`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesManagementEventsResetForAllPlayersArgs {
    /// Path parameter: eventId
    pub eventId: String,
}

/// GET games/v1management/events/{eventId}/resetForAllPlayers
/// Resets the event with the given ID for all players. This method is only available to user accounts for your developer console. Only draft events can be reset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_management_events_reset_for_all_players_builder()` + `games_management_events_reset_for_all_players_execute()`.
/// For task-level control, use `games_management_events_reset_for_all_players_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_management_events_reset_for_all_players(
    client: &SimpleHttpClient,
    args: &GamesManagementEventsResetForAllPlayersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = games_management_events_reset_for_all_players_builder(client, &args.eventId)?;
    games_management_events_reset_for_all_players_execute(builder)
}

/// GET games/v1management/events/resetMultipleForAllPlayers
/// Resets events with the given IDs for all players. This method is only available to user accounts for your developer console. Only draft events may be reset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_management_events_reset_multiple_for_all_players_execute()` to send, or `games_management_events_reset_multiple_for_all_players` for simplest API.

pub fn games_management_events_reset_multiple_for_all_players_builder(
    client: &SimpleHttpClient,
    body: &EventsResetMultipleForAllRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://gamesmanagement.googleapis.com/games/v1management/events/resetMultipleForAllPlayers",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1management/events/resetMultipleForAllPlayers
/// Resets events with the given IDs for all players. This method is only available to user accounts for your developer console. Only draft events may be reset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_management_events_reset_multiple_for_all_players_execute()` or `games_management_events_reset_multiple_for_all_players`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_management_events_reset_multiple_for_all_players_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_management_events_reset_multiple_for_all_players_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1management/events/resetMultipleForAllPlayers
/// Resets events with the given IDs for all players. This method is only available to user accounts for your developer console. Only draft events may be reset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_management_events_reset_multiple_for_all_players_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_management_events_reset_multiple_for_all_players_task()`.
/// For the simplest API, use `games_management_events_reset_multiple_for_all_players()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_management_events_reset_multiple_for_all_players_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_management_events_reset_multiple_for_all_players_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = games_management_events_reset_multiple_for_all_players_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_management_events_reset_multiple_for_all_players`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesManagementEventsResetMultipleForAllPlayersArgs {
    /// Request body.
    pub body: EventsResetMultipleForAllRequest,
}

/// GET games/v1management/events/resetMultipleForAllPlayers
/// Resets events with the given IDs for all players. This method is only available to user accounts for your developer console. Only draft events may be reset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_management_events_reset_multiple_for_all_players_builder()` + `games_management_events_reset_multiple_for_all_players_execute()`.
/// For task-level control, use `games_management_events_reset_multiple_for_all_players_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_management_events_reset_multiple_for_all_players(
    client: &SimpleHttpClient,
    args: &GamesManagementEventsResetMultipleForAllPlayersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        games_management_events_reset_multiple_for_all_players_builder(client, &args.body)?;
    games_management_events_reset_multiple_for_all_players_execute(builder)
}

/// GET games/v1management/applications/{applicationId}/players/hidden/{playerId}
/// Hide the given player's leaderboard scores from the given application. This method is only available to user accounts for your developer console.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_management_players_hide_execute()` to send, or `games_management_players_hide` for simplest API.

pub fn games_management_players_hide_builder(
    client: &SimpleHttpClient,
    applicationId: &String,
    playerId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://gamesmanagement.googleapis.com/games/v1management/applications/{}/players/hidden/{}",
        applicationId,
        playerId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1management/applications/{applicationId}/players/hidden/{playerId}
/// Hide the given player's leaderboard scores from the given application. This method is only available to user accounts for your developer console.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_management_players_hide_execute()` or `games_management_players_hide`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_management_players_hide_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_management_players_hide_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1management/applications/{applicationId}/players/hidden/{playerId}
/// Hide the given player's leaderboard scores from the given application. This method is only available to user accounts for your developer console.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_management_players_hide_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_management_players_hide_task()`.
/// For the simplest API, use `games_management_players_hide()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_management_players_hide_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_management_players_hide_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = games_management_players_hide_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_management_players_hide`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesManagementPlayersHideArgs {
    /// Path parameter: applicationId
    pub applicationId: String,
    /// Path parameter: playerId
    pub playerId: String,
}

/// GET games/v1management/applications/{applicationId}/players/hidden/{playerId}
/// Hide the given player's leaderboard scores from the given application. This method is only available to user accounts for your developer console.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_management_players_hide_builder()` + `games_management_players_hide_execute()`.
/// For task-level control, use `games_management_players_hide_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_management_players_hide(
    client: &SimpleHttpClient,
    args: &GamesManagementPlayersHideArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        games_management_players_hide_builder(client, &args.applicationId, &args.playerId)?;
    games_management_players_hide_execute(builder)
}

/// GET games/v1management/leaderboards/{leaderboardId}/scores/reset
/// Resets scores for the leaderboard with the given ID for the currently authenticated player. This method is only accessible to whitelisted tester accounts for your application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_management_scores_reset_execute()` to send, or `games_management_scores_reset` for simplest API.

pub fn games_management_scores_reset_builder(
    client: &SimpleHttpClient,
    leaderboardId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://gamesmanagement.googleapis.com/games/v1management/leaderboards/{}/scores/reset",
        leaderboardId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1management/leaderboards/{leaderboardId}/scores/reset
/// Resets scores for the leaderboard with the given ID for the currently authenticated player. This method is only accessible to whitelisted tester accounts for your application.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_management_scores_reset_execute()` or `games_management_scores_reset`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_management_scores_reset_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_management_scores_reset_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PlayerScoreResetResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlayerScoreResetResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1management/leaderboards/{leaderboardId}/scores/reset
/// Resets scores for the leaderboard with the given ID for the currently authenticated player. This method is only accessible to whitelisted tester accounts for your application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_management_scores_reset_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_management_scores_reset_task()`.
/// For the simplest API, use `games_management_scores_reset()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_management_scores_reset_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_management_scores_reset_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlayerScoreResetResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = games_management_scores_reset_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_management_scores_reset`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesManagementScoresResetArgs {
    /// Path parameter: leaderboardId
    pub leaderboardId: String,
}

/// GET games/v1management/leaderboards/{leaderboardId}/scores/reset
/// Resets scores for the leaderboard with the given ID for the currently authenticated player. This method is only accessible to whitelisted tester accounts for your application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_management_scores_reset_builder()` + `games_management_scores_reset_execute()`.
/// For task-level control, use `games_management_scores_reset_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_management_scores_reset(
    client: &SimpleHttpClient,
    args: &GamesManagementScoresResetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PlayerScoreResetResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = games_management_scores_reset_builder(client, &args.leaderboardId)?;
    games_management_scores_reset_execute(builder)
}

/// GET games/v1management/scores/reset
/// Resets all scores for all leaderboards for the currently authenticated players. This method is only accessible to whitelisted tester accounts for your application.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_management_scores_reset_all_execute()` to send, or `games_management_scores_reset_all` for simplest API.

pub fn games_management_scores_reset_all_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://gamesmanagement.googleapis.com/games/v1management/scores/reset",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1management/scores/reset
/// Resets all scores for all leaderboards for the currently authenticated players. This method is only accessible to whitelisted tester accounts for your application.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_management_scores_reset_all_execute()` or `games_management_scores_reset_all`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_management_scores_reset_all_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_management_scores_reset_all_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PlayerScoreResetAllResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PlayerScoreResetAllResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1management/scores/reset
/// Resets all scores for all leaderboards for the currently authenticated players. This method is only accessible to whitelisted tester accounts for your application.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_management_scores_reset_all_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_management_scores_reset_all_task()`.
/// For the simplest API, use `games_management_scores_reset_all()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_management_scores_reset_all_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_management_scores_reset_all_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PlayerScoreResetAllResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = games_management_scores_reset_all_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1management/scores/reset
/// Resets all scores for all leaderboards for the currently authenticated players. This method is only accessible to whitelisted tester accounts for your application.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_management_scores_reset_all_builder()` + `games_management_scores_reset_all_execute()`.
/// For task-level control, use `games_management_scores_reset_all_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_management_scores_reset_all(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<PlayerScoreResetAllResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = games_management_scores_reset_all_builder(client)?;
    games_management_scores_reset_all_execute(builder)
}

/// GET games/v1management/scores/resetAllForAllPlayers
/// Resets scores for all draft leaderboards for all players. This method is only available to user accounts for your developer console.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_management_scores_reset_all_for_all_players_execute()` to send, or `games_management_scores_reset_all_for_all_players` for simplest API.

pub fn games_management_scores_reset_all_for_all_players_builder(
    client: &SimpleHttpClient,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://gamesmanagement.googleapis.com/games/v1management/scores/resetAllForAllPlayers",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1management/scores/resetAllForAllPlayers
/// Resets scores for all draft leaderboards for all players. This method is only available to user accounts for your developer console.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_management_scores_reset_all_for_all_players_execute()` or `games_management_scores_reset_all_for_all_players`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_management_scores_reset_all_for_all_players_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_management_scores_reset_all_for_all_players_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1management/scores/resetAllForAllPlayers
/// Resets scores for all draft leaderboards for all players. This method is only available to user accounts for your developer console.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_management_scores_reset_all_for_all_players_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_management_scores_reset_all_for_all_players_task()`.
/// For the simplest API, use `games_management_scores_reset_all_for_all_players()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_management_scores_reset_all_for_all_players_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_management_scores_reset_all_for_all_players_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = games_management_scores_reset_all_for_all_players_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1management/scores/resetAllForAllPlayers
/// Resets scores for all draft leaderboards for all players. This method is only available to user accounts for your developer console.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_management_scores_reset_all_for_all_players_builder()` + `games_management_scores_reset_all_for_all_players_execute()`.
/// For task-level control, use `games_management_scores_reset_all_for_all_players_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_management_scores_reset_all_for_all_players(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = games_management_scores_reset_all_for_all_players_builder(client)?;
    games_management_scores_reset_all_for_all_players_execute(builder)
}

/// GET games/v1management/leaderboards/{leaderboardId}/scores/resetForAllPlayers
/// Resets scores for the leaderboard with the given ID for all players. This method is only available to user accounts for your developer console. Only draft leaderboards can be reset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_management_scores_reset_for_all_players_execute()` to send, or `games_management_scores_reset_for_all_players` for simplest API.

pub fn games_management_scores_reset_for_all_players_builder(
    client: &SimpleHttpClient,
    leaderboardId: &String,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://gamesmanagement.googleapis.com/games/v1management/leaderboards/{}/scores/resetForAllPlayers",
        leaderboardId,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET games/v1management/leaderboards/{leaderboardId}/scores/resetForAllPlayers
/// Resets scores for the leaderboard with the given ID for all players. This method is only available to user accounts for your developer console. Only draft leaderboards can be reset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_management_scores_reset_for_all_players_execute()` or `games_management_scores_reset_for_all_players`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_management_scores_reset_for_all_players_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_management_scores_reset_for_all_players_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1management/leaderboards/{leaderboardId}/scores/resetForAllPlayers
/// Resets scores for the leaderboard with the given ID for all players. This method is only available to user accounts for your developer console. Only draft leaderboards can be reset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_management_scores_reset_for_all_players_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_management_scores_reset_for_all_players_task()`.
/// For the simplest API, use `games_management_scores_reset_for_all_players()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_management_scores_reset_for_all_players_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_management_scores_reset_for_all_players_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = games_management_scores_reset_for_all_players_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_management_scores_reset_for_all_players`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesManagementScoresResetForAllPlayersArgs {
    /// Path parameter: leaderboardId
    pub leaderboardId: String,
}

/// GET games/v1management/leaderboards/{leaderboardId}/scores/resetForAllPlayers
/// Resets scores for the leaderboard with the given ID for all players. This method is only available to user accounts for your developer console. Only draft leaderboards can be reset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_management_scores_reset_for_all_players_builder()` + `games_management_scores_reset_for_all_players_execute()`.
/// For task-level control, use `games_management_scores_reset_for_all_players_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_management_scores_reset_for_all_players(
    client: &SimpleHttpClient,
    args: &GamesManagementScoresResetForAllPlayersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        games_management_scores_reset_for_all_players_builder(client, &args.leaderboardId)?;
    games_management_scores_reset_for_all_players_execute(builder)
}

/// GET games/v1management/scores/resetMultipleForAllPlayers
/// Resets scores for the leaderboards with the given IDs for all players. This method is only available to user accounts for your developer console. Only draft leaderboards may be reset.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `games_management_scores_reset_multiple_for_all_players_execute()` to send, or `games_management_scores_reset_multiple_for_all_players` for simplest API.

pub fn games_management_scores_reset_multiple_for_all_players_builder(
    client: &SimpleHttpClient,
    body: &ScoresResetMultipleForAllRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!(
        "https://gamesmanagement.googleapis.com/games/v1management/scores/resetMultipleForAllPlayers",
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET games/v1management/scores/resetMultipleForAllPlayers
/// Resets scores for the leaderboards with the given IDs for all players. This method is only available to user accounts for your developer console. Only draft leaderboards may be reset.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `games_management_scores_reset_multiple_for_all_players_execute()` or `games_management_scores_reset_multiple_for_all_players`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_management_scores_reset_multiple_for_all_players_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_management_scores_reset_multiple_for_all_players_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET games/v1management/scores/resetMultipleForAllPlayers
/// Resets scores for the leaderboards with the given IDs for all players. This method is only available to user accounts for your developer console. Only draft leaderboards may be reset.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `games_management_scores_reset_multiple_for_all_players_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `games_management_scores_reset_multiple_for_all_players_task()`.
/// For the simplest API, use `games_management_scores_reset_multiple_for_all_players()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `games_management_scores_reset_multiple_for_all_players_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn games_management_scores_reset_multiple_for_all_players_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = games_management_scores_reset_multiple_for_all_players_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`games_management_scores_reset_multiple_for_all_players`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GamesManagementScoresResetMultipleForAllPlayersArgs {
    /// Request body.
    pub body: ScoresResetMultipleForAllRequest,
}

/// GET games/v1management/scores/resetMultipleForAllPlayers
/// Resets scores for the leaderboards with the given IDs for all players. This method is only available to user accounts for your developer console. Only draft leaderboards may be reset.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `games_management_scores_reset_multiple_for_all_players_builder()` + `games_management_scores_reset_multiple_for_all_players_execute()`.
/// For task-level control, use `games_management_scores_reset_multiple_for_all_players_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn games_management_scores_reset_multiple_for_all_players(
    client: &SimpleHttpClient,
    args: &GamesManagementScoresResetMultipleForAllPlayersArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<()>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        games_management_scores_reset_multiple_for_all_players_builder(client, &args.body)?;
    games_management_scores_reset_multiple_for_all_players_execute(builder)
}
