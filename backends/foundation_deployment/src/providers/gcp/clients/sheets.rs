//! Auto-generated API clients for Google Cloud Platform - sheets.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v4/spreadsheets/{spreadsheetId}:batchUpdate
/// Applies one or more updates to the spreadsheet. Each request is validated before being applied. If any request is not valid then the entire request will fail and nothing will be applied. Some requests have replies to give you some information about how they are applied. The replies will mirror the requests. For example, if you applied 4 updates and the 3rd one had a reply, then the response will have 2 empty replies, the actual reply, and another empty reply, in that order. Due to the collaborative nature of spreadsheets, it is not guaranteed that the spreadsheet will reflect exactly your changes after this completes, however it is guaranteed that the updates in the request will be applied together atomically. Your changes may be altered with respect to collaborator changes. If there are no collaborators, the spreadsheet should reflect your changes.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sheets_spreadsheets_batch_update_execute()` to send, or `sheets_spreadsheets_batch_update` for simplest API.

pub fn sheets_spreadsheets_batch_update_builder(
    client: &SimpleHttpClient,
    spreadsheetId: &str,
    body: &BatchUpdateSpreadsheetRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv4/spreadsheets/{}:batchUpdate",
        spreadsheetId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v4/spreadsheets/{spreadsheetId}:batchUpdate
/// Applies one or more updates to the spreadsheet. Each request is validated before being applied. If any request is not valid then the entire request will fail and nothing will be applied. Some requests have replies to give you some information about how they are applied. The replies will mirror the requests. For example, if you applied 4 updates and the 3rd one had a reply, then the response will have 2 empty replies, the actual reply, and another empty reply, in that order. Due to the collaborative nature of spreadsheets, it is not guaranteed that the spreadsheet will reflect exactly your changes after this completes, however it is guaranteed that the updates in the request will be applied together atomically. Your changes may be altered with respect to collaborator changes. If there are no collaborators, the spreadsheet should reflect your changes.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sheets_spreadsheets_batch_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sheets_spreadsheets_batch_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sheets_spreadsheets_batch_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateSpreadsheetResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchUpdateSpreadsheetResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/spreadsheets/{spreadsheetId}:batchUpdate
/// Applies one or more updates to the spreadsheet. Each request is validated before being applied. If any request is not valid then the entire request will fail and nothing will be applied. Some requests have replies to give you some information about how they are applied. The replies will mirror the requests. For example, if you applied 4 updates and the 3rd one had a reply, then the response will have 2 empty replies, the actual reply, and another empty reply, in that order. Due to the collaborative nature of spreadsheets, it is not guaranteed that the spreadsheet will reflect exactly your changes after this completes, however it is guaranteed that the updates in the request will be applied together atomically. Your changes may be altered with respect to collaborator changes. If there are no collaborators, the spreadsheet should reflect your changes.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sheets_spreadsheets_batch_update_builder()` + `sheets_spreadsheets_batch_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sheets_spreadsheets_batch_update(
    client: &SimpleHttpClient,
    spreadsheetId: &str,
    body: &BatchUpdateSpreadsheetRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateSpreadsheetResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sheets_spreadsheets_batch_update_builder(client, spreadsheetId, body)?;
    sheets_spreadsheets_batch_update_execute(builder)
}

/// GET v4/spreadsheets
/// Creates a spreadsheet, returning the newly created spreadsheet.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sheets_spreadsheets_create_execute()` to send, or `sheets_spreadsheets_create` for simplest API.

pub fn sheets_spreadsheets_create_builder(
    client: &SimpleHttpClient,
    body: &Spreadsheet,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv4/spreadsheets",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v4/spreadsheets
/// Creates a spreadsheet, returning the newly created spreadsheet.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sheets_spreadsheets_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sheets_spreadsheets_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sheets_spreadsheets_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Spreadsheet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Spreadsheet = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/spreadsheets
/// Creates a spreadsheet, returning the newly created spreadsheet.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sheets_spreadsheets_create_builder()` + `sheets_spreadsheets_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sheets_spreadsheets_create(
    client: &SimpleHttpClient,
    body: &Spreadsheet,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Spreadsheet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sheets_spreadsheets_create_builder(client, body)?;
    sheets_spreadsheets_create_execute(builder)
}

/// GET v4/spreadsheets/{spreadsheetId}
/// Returns the spreadsheet at the given ID. The caller must specify the spreadsheet ID. By default, data within grids is not returned. You can include grid data in one of 2 ways: * Specify a [field mask](<https://developers.google.`com/workspace/sheets/api/guides/field-masks`>) listing your desired fields using the fields URL parameter in HTTP * Set the `includeGridData` URL parameter to `true`. If a field mask is set, the `includeGridData` parameter is ignored For large spreadsheets, as a best practice, retrieve only the specific spreadsheet fields that you want. To retrieve only subsets of spreadsheet data, use the ranges URL parameter. Ranges are specified using [A1 notation](<https://developers.google.`com/workspace/sheets/api/guides/concepts`#cell>). You can define a single cell (for example, A1) or multiple cells (for example, A1:D5). You can also get cells from other sheets within the same spreadsheet (for example, Sheet2!A1:C4) or retrieve multiple ranges at once (for example, ?ranges=A1:D5&ranges=Sheet2!A1:C4). Limiting the range returns only the portions of the spreadsheet that intersect the requested ranges.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sheets_spreadsheets_get_execute()` to send, or `sheets_spreadsheets_get` for simplest API.

pub fn sheets_spreadsheets_get_builder(
    client: &SimpleHttpClient,
    spreadsheetId: &str,
    excludeTablesInBandedRanges: Option<bool>,
    includeGridData: Option<bool>,
    ranges: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv4/spreadsheets/{}", spreadsheetId,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "excludeTablesInBandedRanges",
            excludeTablesInBandedRanges
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "includeGridData",
            includeGridData
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "ranges",
            ranges
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v4/spreadsheets/{spreadsheetId}
/// Returns the spreadsheet at the given ID. The caller must specify the spreadsheet ID. By default, data within grids is not returned. You can include grid data in one of 2 ways: * Specify a [field mask](<https://developers.google.`com/workspace/sheets/api/guides/field-masks`>) listing your desired fields using the fields URL parameter in HTTP * Set the `includeGridData` URL parameter to `true`. If a field mask is set, the `includeGridData` parameter is ignored For large spreadsheets, as a best practice, retrieve only the specific spreadsheet fields that you want. To retrieve only subsets of spreadsheet data, use the ranges URL parameter. Ranges are specified using [A1 notation](<https://developers.google.`com/workspace/sheets/api/guides/concepts`#cell>). You can define a single cell (for example, A1) or multiple cells (for example, A1:D5). You can also get cells from other sheets within the same spreadsheet (for example, Sheet2!A1:C4) or retrieve multiple ranges at once (for example, ?ranges=A1:D5&ranges=Sheet2!A1:C4). Limiting the range returns only the portions of the spreadsheet that intersect the requested ranges.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sheets_spreadsheets_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sheets_spreadsheets_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sheets_spreadsheets_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Spreadsheet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Spreadsheet = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/spreadsheets/{spreadsheetId}
/// Returns the spreadsheet at the given ID. The caller must specify the spreadsheet ID. By default, data within grids is not returned. You can include grid data in one of 2 ways: * Specify a [field mask](<https://developers.google.`com/workspace/sheets/api/guides/field-masks`>) listing your desired fields using the fields URL parameter in HTTP * Set the `includeGridData` URL parameter to `true`. If a field mask is set, the `includeGridData` parameter is ignored For large spreadsheets, as a best practice, retrieve only the specific spreadsheet fields that you want. To retrieve only subsets of spreadsheet data, use the ranges URL parameter. Ranges are specified using [A1 notation](<https://developers.google.`com/workspace/sheets/api/guides/concepts`#cell>). You can define a single cell (for example, A1) or multiple cells (for example, A1:D5). You can also get cells from other sheets within the same spreadsheet (for example, Sheet2!A1:C4) or retrieve multiple ranges at once (for example, ?ranges=A1:D5&ranges=Sheet2!A1:C4). Limiting the range returns only the portions of the spreadsheet that intersect the requested ranges.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sheets_spreadsheets_get_builder()` + `sheets_spreadsheets_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sheets_spreadsheets_get(
    client: &SimpleHttpClient,
    spreadsheetId: &str,
    excludeTablesInBandedRanges: Option<bool>,
    includeGridData: Option<bool>,
    ranges: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Spreadsheet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sheets_spreadsheets_get_builder(
        client,
        spreadsheetId,
        excludeTablesInBandedRanges,
        includeGridData,
        ranges,
    )?;
    sheets_spreadsheets_get_execute(builder)
}

/// GET v4/spreadsheets/{spreadsheetId}:getByDataFilter
/// Returns the spreadsheet at the given ID. The caller must specify the spreadsheet ID. For more information, see [Read, write, and search metadata](<https://developers.google.`com/workspace/sheets/api/guides/metadata`>). This method differs from GetSpreadsheet in that it allows selecting which subsets of spreadsheet data to return by specifying a `dataFilters` parameter. Multiple DataFilters can be specified. Specifying one or more data filters returns the portions of the spreadsheet that intersect ranges matched by any of the filters. By default, data within grids is not returned. You can include grid data in one of two ways: * Specify a [field mask](<https://developers.google.`com/workspace/sheets/api/guides/field-masks`>) listing your desired fields using the fields URL parameter in HTTP. * Set the `includeGridData` parameter to `true`. If a field mask is set, the `includeGridData` parameter is ignored. For large spreadsheets, as a best practice, retrieve only the specific spreadsheet fields that you want.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sheets_spreadsheets_get_by_data_filter_execute()` to send, or `sheets_spreadsheets_get_by_data_filter` for simplest API.

pub fn sheets_spreadsheets_get_by_data_filter_builder(
    client: &SimpleHttpClient,
    spreadsheetId: &str,
    body: &GetSpreadsheetByDataFilterRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv4/spreadsheets/{}:getByDataFilter",
        spreadsheetId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v4/spreadsheets/{spreadsheetId}:getByDataFilter
/// Returns the spreadsheet at the given ID. The caller must specify the spreadsheet ID. For more information, see [Read, write, and search metadata](<https://developers.google.`com/workspace/sheets/api/guides/metadata`>). This method differs from GetSpreadsheet in that it allows selecting which subsets of spreadsheet data to return by specifying a `dataFilters` parameter. Multiple DataFilters can be specified. Specifying one or more data filters returns the portions of the spreadsheet that intersect ranges matched by any of the filters. By default, data within grids is not returned. You can include grid data in one of two ways: * Specify a [field mask](<https://developers.google.`com/workspace/sheets/api/guides/field-masks`>) listing your desired fields using the fields URL parameter in HTTP. * Set the `includeGridData` parameter to `true`. If a field mask is set, the `includeGridData` parameter is ignored. For large spreadsheets, as a best practice, retrieve only the specific spreadsheet fields that you want.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sheets_spreadsheets_get_by_data_filter_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sheets_spreadsheets_get_by_data_filter_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sheets_spreadsheets_get_by_data_filter_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Spreadsheet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Spreadsheet = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/spreadsheets/{spreadsheetId}:getByDataFilter
/// Returns the spreadsheet at the given ID. The caller must specify the spreadsheet ID. For more information, see [Read, write, and search metadata](<https://developers.google.`com/workspace/sheets/api/guides/metadata`>). This method differs from GetSpreadsheet in that it allows selecting which subsets of spreadsheet data to return by specifying a `dataFilters` parameter. Multiple DataFilters can be specified. Specifying one or more data filters returns the portions of the spreadsheet that intersect ranges matched by any of the filters. By default, data within grids is not returned. You can include grid data in one of two ways: * Specify a [field mask](<https://developers.google.`com/workspace/sheets/api/guides/field-masks`>) listing your desired fields using the fields URL parameter in HTTP. * Set the `includeGridData` parameter to `true`. If a field mask is set, the `includeGridData` parameter is ignored. For large spreadsheets, as a best practice, retrieve only the specific spreadsheet fields that you want.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sheets_spreadsheets_get_by_data_filter_builder()` + `sheets_spreadsheets_get_by_data_filter_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sheets_spreadsheets_get_by_data_filter(
    client: &SimpleHttpClient,
    spreadsheetId: &str,
    body: &GetSpreadsheetByDataFilterRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Spreadsheet>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sheets_spreadsheets_get_by_data_filter_builder(client, spreadsheetId, body)?;
    sheets_spreadsheets_get_by_data_filter_execute(builder)
}

/// GET v4/spreadsheets/{spreadsheetId}/developerMetadata/{metadataId}
/// Returns the developer metadata with the specified ID. The caller must specify the spreadsheet ID and the developer metadata's unique `metadataId`. For more information, see [Read, write, and search metadata](<https://developers.google.`com/workspace/sheets/api/guides/metadata`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sheets_spreadsheets_developer_metadata_get_execute()` to send, or `sheets_spreadsheets_developer_metadata_get` for simplest API.

pub fn sheets_spreadsheets_developer_metadata_get_builder(
    client: &SimpleHttpClient,
    metadataId: &str,
    spreadsheetId: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv4/spreadsheets/{}/developerMetadata/{}",
        metadataId, spreadsheetId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/spreadsheets/{spreadsheetId}/developerMetadata/{metadataId}
/// Returns the developer metadata with the specified ID. The caller must specify the spreadsheet ID and the developer metadata's unique `metadataId`. For more information, see [Read, write, and search metadata](<https://developers.google.`com/workspace/sheets/api/guides/metadata`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sheets_spreadsheets_developer_metadata_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sheets_spreadsheets_developer_metadata_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sheets_spreadsheets_developer_metadata_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeveloperMetadata>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DeveloperMetadata = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/spreadsheets/{spreadsheetId}/developerMetadata/{metadataId}
/// Returns the developer metadata with the specified ID. The caller must specify the spreadsheet ID and the developer metadata's unique `metadataId`. For more information, see [Read, write, and search metadata](<https://developers.google.`com/workspace/sheets/api/guides/metadata`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sheets_spreadsheets_developer_metadata_get_builder()` + `sheets_spreadsheets_developer_metadata_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sheets_spreadsheets_developer_metadata_get(
    client: &SimpleHttpClient,
    metadataId: &str,
    spreadsheetId: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DeveloperMetadata>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        sheets_spreadsheets_developer_metadata_get_builder(client, metadataId, spreadsheetId)?;
    sheets_spreadsheets_developer_metadata_get_execute(builder)
}

/// GET v4/spreadsheets/{spreadsheetId}/developerMetadata:search
/// Returns all developer metadata matching the specified DataFilter. For more information, see [Read, write, and search metadata](<https://developers.google.`com/workspace/sheets/api/guides/metadata`>). If the provided DataFilter represents a DeveloperMetadataLookup object, this will return all DeveloperMetadata entries selected by it. If the DataFilter represents a location in a spreadsheet, this will return all developer metadata associated with locations intersecting that region.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sheets_spreadsheets_developer_metadata_search_execute()` to send, or `sheets_spreadsheets_developer_metadata_search` for simplest API.

pub fn sheets_spreadsheets_developer_metadata_search_builder(
    client: &SimpleHttpClient,
    spreadsheetId: &str,
    body: &SearchDeveloperMetadataRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv4/spreadsheets/{}/developerMetadata:search",
        spreadsheetId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v4/spreadsheets/{spreadsheetId}/developerMetadata:search
/// Returns all developer metadata matching the specified DataFilter. For more information, see [Read, write, and search metadata](<https://developers.google.`com/workspace/sheets/api/guides/metadata`>). If the provided DataFilter represents a DeveloperMetadataLookup object, this will return all DeveloperMetadata entries selected by it. If the DataFilter represents a location in a spreadsheet, this will return all developer metadata associated with locations intersecting that region.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sheets_spreadsheets_developer_metadata_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sheets_spreadsheets_developer_metadata_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sheets_spreadsheets_developer_metadata_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchDeveloperMetadataResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SearchDeveloperMetadataResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/spreadsheets/{spreadsheetId}/developerMetadata:search
/// Returns all developer metadata matching the specified DataFilter. For more information, see [Read, write, and search metadata](<https://developers.google.`com/workspace/sheets/api/guides/metadata`>). If the provided DataFilter represents a DeveloperMetadataLookup object, this will return all DeveloperMetadata entries selected by it. If the DataFilter represents a location in a spreadsheet, this will return all developer metadata associated with locations intersecting that region.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sheets_spreadsheets_developer_metadata_search_builder()` + `sheets_spreadsheets_developer_metadata_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sheets_spreadsheets_developer_metadata_search(
    client: &SimpleHttpClient,
    spreadsheetId: &str,
    body: &SearchDeveloperMetadataRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<SearchDeveloperMetadataResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        sheets_spreadsheets_developer_metadata_search_builder(client, spreadsheetId, body)?;
    sheets_spreadsheets_developer_metadata_search_execute(builder)
}

/// GET v4/spreadsheets/{spreadsheetId}/sheets/{sheetId}:copyTo
/// Copies a single sheet from a spreadsheet to another spreadsheet. Returns the properties of the newly created sheet.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sheets_spreadsheets_sheets_copy_to_execute()` to send, or `sheets_spreadsheets_sheets_copy_to` for simplest API.

pub fn sheets_spreadsheets_sheets_copy_to_builder(
    client: &SimpleHttpClient,
    sheetId: &str,
    spreadsheetId: &str,
    body: &CopySheetToAnotherSpreadsheetRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv4/spreadsheets/{}/sheets/{}:copyTo",
        sheetId, spreadsheetId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v4/spreadsheets/{spreadsheetId}/sheets/{sheetId}:copyTo
/// Copies a single sheet from a spreadsheet to another spreadsheet. Returns the properties of the newly created sheet.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sheets_spreadsheets_sheets_copy_to_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sheets_spreadsheets_sheets_copy_to_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sheets_spreadsheets_sheets_copy_to_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SheetProperties>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SheetProperties = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/spreadsheets/{spreadsheetId}/sheets/{sheetId}:copyTo
/// Copies a single sheet from a spreadsheet to another spreadsheet. Returns the properties of the newly created sheet.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sheets_spreadsheets_sheets_copy_to_builder()` + `sheets_spreadsheets_sheets_copy_to_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sheets_spreadsheets_sheets_copy_to(
    client: &SimpleHttpClient,
    sheetId: &str,
    spreadsheetId: &str,
    body: &CopySheetToAnotherSpreadsheetRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SheetProperties>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sheets_spreadsheets_sheets_copy_to_builder(client, sheetId, spreadsheetId, body)?;
    sheets_spreadsheets_sheets_copy_to_execute(builder)
}

/// GET v4/spreadsheets/{spreadsheetId}/values/{range}:append
/// Appends values to a spreadsheet. The input range is used to search for existing data and find a "table" within that range. Values will be appended to the next row of the table, starting with the first column of the table. See the [guide](<https://developers.google.`com/workspace/sheets/api/guides/values`#appending_values>) and [sample code](<https://developers.google.`com/workspace/sheets/api/samples/writing`#append_values>) for specific details of how tables are detected and data is appended. The caller must specify the spreadsheet ID, range, and a `valueInputOption`. The `valueInputOption` only controls how the input data will be added to the sheet (column-wise or row-wise), it does not influence what cell the data starts being written to.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sheets_spreadsheets_values_append_execute()` to send, or `sheets_spreadsheets_values_append` for simplest API.

pub fn sheets_spreadsheets_values_append_builder(
    client: &SimpleHttpClient,
    range: &str,
    spreadsheetId: &str,
    includeValuesInResponse: Option<bool>,
    insertDataOption: Option<&str>,
    responseDateTimeRenderOption: Option<&str>,
    responseValueRenderOption: Option<&str>,
    valueInputOption: Option<&str>,
    body: &ValueRange,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv4/spreadsheets/{}/values/{}:append",
        range, spreadsheetId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "includeValuesInResponse",
            includeValuesInResponse
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "insertDataOption",
            insertDataOption
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "responseDateTimeRenderOption",
            responseDateTimeRenderOption
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "responseValueRenderOption",
            responseValueRenderOption
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "valueInputOption",
            valueInputOption
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v4/spreadsheets/{spreadsheetId}/values/{range}:append
/// Appends values to a spreadsheet. The input range is used to search for existing data and find a "table" within that range. Values will be appended to the next row of the table, starting with the first column of the table. See the [guide](<https://developers.google.`com/workspace/sheets/api/guides/values`#appending_values>) and [sample code](<https://developers.google.`com/workspace/sheets/api/samples/writing`#append_values>) for specific details of how tables are detected and data is appended. The caller must specify the spreadsheet ID, range, and a `valueInputOption`. The `valueInputOption` only controls how the input data will be added to the sheet (column-wise or row-wise), it does not influence what cell the data starts being written to.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sheets_spreadsheets_values_append_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sheets_spreadsheets_values_append_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sheets_spreadsheets_values_append_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppendValuesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AppendValuesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/spreadsheets/{spreadsheetId}/values/{range}:append
/// Appends values to a spreadsheet. The input range is used to search for existing data and find a "table" within that range. Values will be appended to the next row of the table, starting with the first column of the table. See the [guide](<https://developers.google.`com/workspace/sheets/api/guides/values`#appending_values>) and [sample code](<https://developers.google.`com/workspace/sheets/api/samples/writing`#append_values>) for specific details of how tables are detected and data is appended. The caller must specify the spreadsheet ID, range, and a `valueInputOption`. The `valueInputOption` only controls how the input data will be added to the sheet (column-wise or row-wise), it does not influence what cell the data starts being written to.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sheets_spreadsheets_values_append_builder()` + `sheets_spreadsheets_values_append_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sheets_spreadsheets_values_append(
    client: &SimpleHttpClient,
    range: &str,
    spreadsheetId: &str,
    includeValuesInResponse: Option<bool>,
    insertDataOption: Option<&str>,
    responseDateTimeRenderOption: Option<&str>,
    responseValueRenderOption: Option<&str>,
    valueInputOption: Option<&str>,
    body: &ValueRange,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AppendValuesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sheets_spreadsheets_values_append_builder(
        client,
        range,
        spreadsheetId,
        includeValuesInResponse,
        insertDataOption,
        responseDateTimeRenderOption,
        responseValueRenderOption,
        valueInputOption,
        body,
    )?;
    sheets_spreadsheets_values_append_execute(builder)
}

/// GET v4/spreadsheets/{spreadsheetId}/values:batchClear
/// Clears one or more ranges of values from a spreadsheet. The caller must specify the spreadsheet ID and one or more ranges. Only values are cleared -- all other properties of the cell (such as formatting and data validation) are kept.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sheets_spreadsheets_values_batch_clear_execute()` to send, or `sheets_spreadsheets_values_batch_clear` for simplest API.

pub fn sheets_spreadsheets_values_batch_clear_builder(
    client: &SimpleHttpClient,
    spreadsheetId: &str,
    body: &BatchClearValuesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv4/spreadsheets/{}/values:batchClear",
        spreadsheetId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v4/spreadsheets/{spreadsheetId}/values:batchClear
/// Clears one or more ranges of values from a spreadsheet. The caller must specify the spreadsheet ID and one or more ranges. Only values are cleared -- all other properties of the cell (such as formatting and data validation) are kept.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sheets_spreadsheets_values_batch_clear_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sheets_spreadsheets_values_batch_clear_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sheets_spreadsheets_values_batch_clear_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BatchClearValuesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchClearValuesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/spreadsheets/{spreadsheetId}/values:batchClear
/// Clears one or more ranges of values from a spreadsheet. The caller must specify the spreadsheet ID and one or more ranges. Only values are cleared -- all other properties of the cell (such as formatting and data validation) are kept.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sheets_spreadsheets_values_batch_clear_builder()` + `sheets_spreadsheets_values_batch_clear_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sheets_spreadsheets_values_batch_clear(
    client: &SimpleHttpClient,
    spreadsheetId: &str,
    body: &BatchClearValuesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BatchClearValuesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sheets_spreadsheets_values_batch_clear_builder(client, spreadsheetId, body)?;
    sheets_spreadsheets_values_batch_clear_execute(builder)
}

/// GET v4/spreadsheets/{spreadsheetId}/values:batchClearByDataFilter
/// Clears one or more ranges of values from a spreadsheet. For more information, see [Read, write, and search metadata](<https://developers.google.`com/workspace/sheets/api/guides/metadata`>). The caller must specify the spreadsheet ID and one or more DataFilters. Ranges matching any of the specified data filters will be cleared. Only values are cleared -- all other properties of the cell (such as formatting, data validation, etc.) are kept.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sheets_spreadsheets_values_batch_clear_by_data_filter_execute()` to send, or `sheets_spreadsheets_values_batch_clear_by_data_filter` for simplest API.

pub fn sheets_spreadsheets_values_batch_clear_by_data_filter_builder(
    client: &SimpleHttpClient,
    spreadsheetId: &str,
    body: &BatchClearValuesByDataFilterRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv4/spreadsheets/{}/values:batchClearByDataFilter",
        spreadsheetId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v4/spreadsheets/{spreadsheetId}/values:batchClearByDataFilter
/// Clears one or more ranges of values from a spreadsheet. For more information, see [Read, write, and search metadata](<https://developers.google.`com/workspace/sheets/api/guides/metadata`>). The caller must specify the spreadsheet ID and one or more DataFilters. Ranges matching any of the specified data filters will be cleared. Only values are cleared -- all other properties of the cell (such as formatting, data validation, etc.) are kept.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sheets_spreadsheets_values_batch_clear_by_data_filter_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sheets_spreadsheets_values_batch_clear_by_data_filter_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sheets_spreadsheets_values_batch_clear_by_data_filter_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchClearValuesByDataFilterResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchClearValuesByDataFilterResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/spreadsheets/{spreadsheetId}/values:batchClearByDataFilter
/// Clears one or more ranges of values from a spreadsheet. For more information, see [Read, write, and search metadata](<https://developers.google.`com/workspace/sheets/api/guides/metadata`>). The caller must specify the spreadsheet ID and one or more DataFilters. Ranges matching any of the specified data filters will be cleared. Only values are cleared -- all other properties of the cell (such as formatting, data validation, etc.) are kept.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sheets_spreadsheets_values_batch_clear_by_data_filter_builder()` + `sheets_spreadsheets_values_batch_clear_by_data_filter_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sheets_spreadsheets_values_batch_clear_by_data_filter(
    client: &SimpleHttpClient,
    spreadsheetId: &str,
    body: &BatchClearValuesByDataFilterRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchClearValuesByDataFilterResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        sheets_spreadsheets_values_batch_clear_by_data_filter_builder(client, spreadsheetId, body)?;
    sheets_spreadsheets_values_batch_clear_by_data_filter_execute(builder)
}

/// GET v4/spreadsheets/{spreadsheetId}/values:batchGet
/// Returns one or more ranges of values from a spreadsheet. The caller must specify the spreadsheet ID and one or more ranges.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sheets_spreadsheets_values_batch_get_execute()` to send, or `sheets_spreadsheets_values_batch_get` for simplest API.

pub fn sheets_spreadsheets_values_batch_get_builder(
    client: &SimpleHttpClient,
    spreadsheetId: &str,
    dateTimeRenderOption: Option<&str>,
    majorDimension: Option<&str>,
    ranges: Option<&str>,
    valueRenderOption: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv4/spreadsheets/{}/values:batchGet",
        spreadsheetId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "dateTimeRenderOption",
            dateTimeRenderOption
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "majorDimension",
            majorDimension
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "ranges",
            ranges
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "valueRenderOption",
            valueRenderOption
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v4/spreadsheets/{spreadsheetId}/values:batchGet
/// Returns one or more ranges of values from a spreadsheet. The caller must specify the spreadsheet ID and one or more ranges.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sheets_spreadsheets_values_batch_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sheets_spreadsheets_values_batch_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sheets_spreadsheets_values_batch_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BatchGetValuesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchGetValuesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/spreadsheets/{spreadsheetId}/values:batchGet
/// Returns one or more ranges of values from a spreadsheet. The caller must specify the spreadsheet ID and one or more ranges.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sheets_spreadsheets_values_batch_get_builder()` + `sheets_spreadsheets_values_batch_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sheets_spreadsheets_values_batch_get(
    client: &SimpleHttpClient,
    spreadsheetId: &str,
    dateTimeRenderOption: Option<&str>,
    majorDimension: Option<&str>,
    ranges: Option<&str>,
    valueRenderOption: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BatchGetValuesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sheets_spreadsheets_values_batch_get_builder(
        client,
        spreadsheetId,
        dateTimeRenderOption,
        majorDimension,
        ranges,
        valueRenderOption,
    )?;
    sheets_spreadsheets_values_batch_get_execute(builder)
}

/// GET v4/spreadsheets/{spreadsheetId}/values:batchGetByDataFilter
/// Returns one or more ranges of values that match the specified data filters. For more information, see [Read, write, and search metadata](<https://developers.google.`com/workspace/sheets/api/guides/metadata`>). The caller must specify the spreadsheet ID and one or more DataFilters. Ranges that match any of the data filters in the request will be returned.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sheets_spreadsheets_values_batch_get_by_data_filter_execute()` to send, or `sheets_spreadsheets_values_batch_get_by_data_filter` for simplest API.

pub fn sheets_spreadsheets_values_batch_get_by_data_filter_builder(
    client: &SimpleHttpClient,
    spreadsheetId: &str,
    body: &BatchGetValuesByDataFilterRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv4/spreadsheets/{}/values:batchGetByDataFilter",
        spreadsheetId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v4/spreadsheets/{spreadsheetId}/values:batchGetByDataFilter
/// Returns one or more ranges of values that match the specified data filters. For more information, see [Read, write, and search metadata](<https://developers.google.`com/workspace/sheets/api/guides/metadata`>). The caller must specify the spreadsheet ID and one or more DataFilters. Ranges that match any of the data filters in the request will be returned.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sheets_spreadsheets_values_batch_get_by_data_filter_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sheets_spreadsheets_values_batch_get_by_data_filter_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sheets_spreadsheets_values_batch_get_by_data_filter_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchGetValuesByDataFilterResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchGetValuesByDataFilterResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/spreadsheets/{spreadsheetId}/values:batchGetByDataFilter
/// Returns one or more ranges of values that match the specified data filters. For more information, see [Read, write, and search metadata](<https://developers.google.`com/workspace/sheets/api/guides/metadata`>). The caller must specify the spreadsheet ID and one or more DataFilters. Ranges that match any of the data filters in the request will be returned.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sheets_spreadsheets_values_batch_get_by_data_filter_builder()` + `sheets_spreadsheets_values_batch_get_by_data_filter_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sheets_spreadsheets_values_batch_get_by_data_filter(
    client: &SimpleHttpClient,
    spreadsheetId: &str,
    body: &BatchGetValuesByDataFilterRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchGetValuesByDataFilterResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        sheets_spreadsheets_values_batch_get_by_data_filter_builder(client, spreadsheetId, body)?;
    sheets_spreadsheets_values_batch_get_by_data_filter_execute(builder)
}

/// GET v4/spreadsheets/{spreadsheetId}/values:batchUpdate
/// Sets values in one or more ranges of a spreadsheet. The caller must specify the spreadsheet ID, a `valueInputOption`, and one or more ValueRanges.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sheets_spreadsheets_values_batch_update_execute()` to send, or `sheets_spreadsheets_values_batch_update` for simplest API.

pub fn sheets_spreadsheets_values_batch_update_builder(
    client: &SimpleHttpClient,
    spreadsheetId: &str,
    body: &BatchUpdateValuesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv4/spreadsheets/{}/values:batchUpdate",
        spreadsheetId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v4/spreadsheets/{spreadsheetId}/values:batchUpdate
/// Sets values in one or more ranges of a spreadsheet. The caller must specify the spreadsheet ID, a `valueInputOption`, and one or more ValueRanges.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sheets_spreadsheets_values_batch_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sheets_spreadsheets_values_batch_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sheets_spreadsheets_values_batch_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BatchUpdateValuesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchUpdateValuesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/spreadsheets/{spreadsheetId}/values:batchUpdate
/// Sets values in one or more ranges of a spreadsheet. The caller must specify the spreadsheet ID, a `valueInputOption`, and one or more ValueRanges.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sheets_spreadsheets_values_batch_update_builder()` + `sheets_spreadsheets_values_batch_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sheets_spreadsheets_values_batch_update(
    client: &SimpleHttpClient,
    spreadsheetId: &str,
    body: &BatchUpdateValuesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<BatchUpdateValuesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sheets_spreadsheets_values_batch_update_builder(client, spreadsheetId, body)?;
    sheets_spreadsheets_values_batch_update_execute(builder)
}

/// GET v4/spreadsheets/{spreadsheetId}/values:batchUpdateByDataFilter
/// Sets values in one or more ranges of a spreadsheet. For more information, see [Read, write, and search metadata](<https://developers.google.`com/workspace/sheets/api/guides/metadata`>). The caller must specify the spreadsheet ID, a `valueInputOption`, and one or more DataFilterValueRanges.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sheets_spreadsheets_values_batch_update_by_data_filter_execute()` to send, or `sheets_spreadsheets_values_batch_update_by_data_filter` for simplest API.

pub fn sheets_spreadsheets_values_batch_update_by_data_filter_builder(
    client: &SimpleHttpClient,
    spreadsheetId: &str,
    body: &BatchUpdateValuesByDataFilterRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv4/spreadsheets/{}/values:batchUpdateByDataFilter",
        spreadsheetId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v4/spreadsheets/{spreadsheetId}/values:batchUpdateByDataFilter
/// Sets values in one or more ranges of a spreadsheet. For more information, see [Read, write, and search metadata](<https://developers.google.`com/workspace/sheets/api/guides/metadata`>). The caller must specify the spreadsheet ID, a `valueInputOption`, and one or more DataFilterValueRanges.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sheets_spreadsheets_values_batch_update_by_data_filter_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sheets_spreadsheets_values_batch_update_by_data_filter_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sheets_spreadsheets_values_batch_update_by_data_filter_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateValuesByDataFilterResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: BatchUpdateValuesByDataFilterResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/spreadsheets/{spreadsheetId}/values:batchUpdateByDataFilter
/// Sets values in one or more ranges of a spreadsheet. For more information, see [Read, write, and search metadata](<https://developers.google.`com/workspace/sheets/api/guides/metadata`>). The caller must specify the spreadsheet ID, a `valueInputOption`, and one or more DataFilterValueRanges.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sheets_spreadsheets_values_batch_update_by_data_filter_builder()` + `sheets_spreadsheets_values_batch_update_by_data_filter_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sheets_spreadsheets_values_batch_update_by_data_filter(
    client: &SimpleHttpClient,
    spreadsheetId: &str,
    body: &BatchUpdateValuesByDataFilterRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<BatchUpdateValuesByDataFilterResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = sheets_spreadsheets_values_batch_update_by_data_filter_builder(
        client,
        spreadsheetId,
        body,
    )?;
    sheets_spreadsheets_values_batch_update_by_data_filter_execute(builder)
}

/// GET v4/spreadsheets/{spreadsheetId}/values/{range}:clear
/// Clears values from a spreadsheet. The caller must specify the spreadsheet ID and range. Only values are cleared -- all other properties of the cell (such as formatting, data validation, etc..) are kept.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sheets_spreadsheets_values_clear_execute()` to send, or `sheets_spreadsheets_values_clear` for simplest API.

pub fn sheets_spreadsheets_values_clear_builder(
    client: &SimpleHttpClient,
    range: &str,
    spreadsheetId: &str,
    body: &ClearValuesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv4/spreadsheets/{}/values/{}:clear",
        range, spreadsheetId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v4/spreadsheets/{spreadsheetId}/values/{range}:clear
/// Clears values from a spreadsheet. The caller must specify the spreadsheet ID and range. Only values are cleared -- all other properties of the cell (such as formatting, data validation, etc..) are kept.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sheets_spreadsheets_values_clear_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sheets_spreadsheets_values_clear_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sheets_spreadsheets_values_clear_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClearValuesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ClearValuesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/spreadsheets/{spreadsheetId}/values/{range}:clear
/// Clears values from a spreadsheet. The caller must specify the spreadsheet ID and range. Only values are cleared -- all other properties of the cell (such as formatting, data validation, etc..) are kept.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sheets_spreadsheets_values_clear_builder()` + `sheets_spreadsheets_values_clear_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sheets_spreadsheets_values_clear(
    client: &SimpleHttpClient,
    range: &str,
    spreadsheetId: &str,
    body: &ClearValuesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ClearValuesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sheets_spreadsheets_values_clear_builder(client, range, spreadsheetId, body)?;
    sheets_spreadsheets_values_clear_execute(builder)
}

/// GET v4/spreadsheets/{spreadsheetId}/values/{range}
/// Returns a range of values from a spreadsheet. The caller must specify the spreadsheet ID and a range.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sheets_spreadsheets_values_get_execute()` to send, or `sheets_spreadsheets_values_get` for simplest API.

pub fn sheets_spreadsheets_values_get_builder(
    client: &SimpleHttpClient,
    range: &str,
    spreadsheetId: &str,
    dateTimeRenderOption: Option<&str>,
    majorDimension: Option<&str>,
    valueRenderOption: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv4/spreadsheets/{}/values/{}",
        range, spreadsheetId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "dateTimeRenderOption",
            dateTimeRenderOption
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "majorDimension",
            majorDimension
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "valueRenderOption",
            valueRenderOption
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v4/spreadsheets/{spreadsheetId}/values/{range}
/// Returns a range of values from a spreadsheet. The caller must specify the spreadsheet ID and a range.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sheets_spreadsheets_values_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sheets_spreadsheets_values_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sheets_spreadsheets_values_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ValueRange>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ValueRange = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/spreadsheets/{spreadsheetId}/values/{range}
/// Returns a range of values from a spreadsheet. The caller must specify the spreadsheet ID and a range.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sheets_spreadsheets_values_get_builder()` + `sheets_spreadsheets_values_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sheets_spreadsheets_values_get(
    client: &SimpleHttpClient,
    range: &str,
    spreadsheetId: &str,
    dateTimeRenderOption: Option<&str>,
    majorDimension: Option<&str>,
    valueRenderOption: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ValueRange>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = sheets_spreadsheets_values_get_builder(
        client,
        range,
        spreadsheetId,
        dateTimeRenderOption,
        majorDimension,
        valueRenderOption,
    )?;
    sheets_spreadsheets_values_get_execute(builder)
}

/// GET v4/spreadsheets/{spreadsheetId}/values/{range}
/// Sets values in a range of a spreadsheet. The caller must specify the spreadsheet ID, range, and a `valueInputOption`.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `sheets_spreadsheets_values_update_execute()` to send, or `sheets_spreadsheets_values_update` for simplest API.

pub fn sheets_spreadsheets_values_update_builder(
    client: &SimpleHttpClient,
    range: &str,
    spreadsheetId: &str,
    includeValuesInResponse: Option<bool>,
    responseDateTimeRenderOption: Option<&str>,
    responseValueRenderOption: Option<&str>,
    valueInputOption: Option<&str>,
    body: &ValueRange,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv4/spreadsheets/{}/values/{}",
        range, spreadsheetId,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "includeValuesInResponse",
            includeValuesInResponse
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "responseDateTimeRenderOption",
            responseDateTimeRenderOption
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "responseValueRenderOption",
            responseValueRenderOption
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "valueInputOption",
            valueInputOption
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v4/spreadsheets/{spreadsheetId}/values/{range}
/// Sets values in a range of a spreadsheet. The caller must specify the spreadsheet ID, range, and a `valueInputOption`.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `sheets_spreadsheets_values_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from sheets_spreadsheets_values_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sheets_spreadsheets_values_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UpdateValuesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: UpdateValuesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v4/spreadsheets/{spreadsheetId}/values/{range}
/// Sets values in a range of a spreadsheet. The caller must specify the spreadsheet ID, range, and a `valueInputOption`.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `sheets_spreadsheets_values_update_builder()` + `sheets_spreadsheets_values_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn sheets_spreadsheets_values_update(
    client: &SimpleHttpClient,
    range: &str,
    spreadsheetId: &str,
    includeValuesInResponse: Option<bool>,
    responseDateTimeRenderOption: Option<&str>,
    responseValueRenderOption: Option<&str>,
    valueInputOption: Option<&str>,
    body: &ValueRange,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<UpdateValuesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = sheets_spreadsheets_values_update_builder(
        client,
        range,
        spreadsheetId,
        includeValuesInResponse,
        responseDateTimeRenderOption,
        responseValueRenderOption,
        valueInputOption,
        body,
    )?;
    sheets_spreadsheets_values_update_execute(builder)
}
