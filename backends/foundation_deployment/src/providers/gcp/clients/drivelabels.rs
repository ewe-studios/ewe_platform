//! Auto-generated API clients for Google Cloud Platform - drivelabels.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v2/labels
/// Creates a label. For more information, see [Create and publish a label](<https://developers.google.`com/workspace/drive/labels/guides/create-label`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drivelabels_labels_create_execute()` to send, or `drivelabels_labels_create` for simplest API.

pub fn drivelabels_labels_create_builder(
    client: &SimpleHttpClient,
    languageCode: Option<&str>,
    useAdminAccess: Option<bool>,
    body: &GoogleAppsDriveLabelsV2Label,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/labels",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "languageCode",
            languageCode
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "useAdminAccess",
            useAdminAccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/labels
/// Creates a label. For more information, see [Create and publish a label](<https://developers.google.`com/workspace/drive/labels/guides/create-label`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drivelabels_labels_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drivelabels_labels_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2Label>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2Label = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/labels
/// Creates a label. For more information, see [Create and publish a label](<https://developers.google.`com/workspace/drive/labels/guides/create-label`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drivelabels_labels_create_builder()` + `drivelabels_labels_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_create(
    client: &SimpleHttpClient,
    languageCode: Option<&str>,
    useAdminAccess: Option<bool>,
    body: &GoogleAppsDriveLabelsV2Label,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2Label>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = drivelabels_labels_create_builder(client, languageCode, useAdminAccess, body)?;
    drivelabels_labels_create_execute(builder)
}

/// GET v2/{+name}
/// Permanently deletes a label and related metadata on Drive items. For more information, see [Disable, enable, and delete a label](<https://developers.google.`com/workspace/drive/labels/guides/disable-delete-label`>). Once deleted, the label and related Drive item metadata will be deleted. Only draft labels and disabled labels may be deleted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drivelabels_labels_delete_execute()` to send, or `drivelabels_labels_delete` for simplest API.

pub fn drivelabels_labels_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    useAdminAccess: Option<bool>,
    writeControl_requiredRevisionId: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "useAdminAccess",
            useAdminAccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "writeControl_requiredRevisionId",
            writeControl_requiredRevisionId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+name}
/// Permanently deletes a label and related metadata on Drive items. For more information, see [Disable, enable, and delete a label](<https://developers.google.`com/workspace/drive/labels/guides/disable-delete-label`>). Once deleted, the label and related Drive item metadata will be deleted. Only draft labels and disabled labels may be deleted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drivelabels_labels_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drivelabels_labels_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Permanently deletes a label and related metadata on Drive items. For more information, see [Disable, enable, and delete a label](<https://developers.google.`com/workspace/drive/labels/guides/disable-delete-label`>). Once deleted, the label and related Drive item metadata will be deleted. Only draft labels and disabled labels may be deleted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drivelabels_labels_delete_builder()` + `drivelabels_labels_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_delete(
    client: &SimpleHttpClient,
    name: &str,
    useAdminAccess: Option<bool>,
    writeControl_requiredRevisionId: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = drivelabels_labels_delete_builder(
        client,
        name,
        useAdminAccess,
        writeControl_requiredRevisionId,
    )?;
    drivelabels_labels_delete_execute(builder)
}

/// GET v2/{+name}:delta
/// Updates a single label by applying a set of update requests resulting in a new draft revision. For more information, see [Update a label](<https://developers.google.`com/workspace/drive/labels/guides/update-label`>). The batch update is all-or-nothing: If any of the update requests are invalid, no changes are applied. The resulting draft revision must be published before the changes may be used with Drive items.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drivelabels_labels_delta_execute()` to send, or `drivelabels_labels_delta` for simplest API.

pub fn drivelabels_labels_delta_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleAppsDriveLabelsV2DeltaUpdateLabelRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:delta", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:delta
/// Updates a single label by applying a set of update requests resulting in a new draft revision. For more information, see [Update a label](<https://developers.google.`com/workspace/drive/labels/guides/update-label`>). The batch update is all-or-nothing: If any of the update requests are invalid, no changes are applied. The resulting draft revision must be published before the changes may be used with Drive items.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drivelabels_labels_delta_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drivelabels_labels_delta_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_delta_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2DeltaUpdateLabelResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2DeltaUpdateLabelResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:delta
/// Updates a single label by applying a set of update requests resulting in a new draft revision. For more information, see [Update a label](<https://developers.google.`com/workspace/drive/labels/guides/update-label`>). The batch update is all-or-nothing: If any of the update requests are invalid, no changes are applied. The resulting draft revision must be published before the changes may be used with Drive items.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drivelabels_labels_delta_builder()` + `drivelabels_labels_delta_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_delta(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleAppsDriveLabelsV2DeltaUpdateLabelRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2DeltaUpdateLabelResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = drivelabels_labels_delta_builder(client, name, body)?;
    drivelabels_labels_delta_execute(builder)
}

/// GET v2/{+name}:disable
/// Disable a published label. For more information, see [Disable, enable, and delete a label](<https://developers.google.`com/workspace/drive/labels/guides/disable-delete-label`>). Disabling a label will result in a new disabled published revision based on the current published revision. If there's a draft revision, a new disabled draft revision will be created based on the latest draft revision. Older draft revisions will be deleted. Once disabled, a label may be deleted with DeleteLabel.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drivelabels_labels_disable_execute()` to send, or `drivelabels_labels_disable` for simplest API.

pub fn drivelabels_labels_disable_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleAppsDriveLabelsV2DisableLabelRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:disable", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:disable
/// Disable a published label. For more information, see [Disable, enable, and delete a label](<https://developers.google.`com/workspace/drive/labels/guides/disable-delete-label`>). Disabling a label will result in a new disabled published revision based on the current published revision. If there's a draft revision, a new disabled draft revision will be created based on the latest draft revision. Older draft revisions will be deleted. Once disabled, a label may be deleted with DeleteLabel.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drivelabels_labels_disable_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drivelabels_labels_disable_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_disable_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2Label>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2Label = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:disable
/// Disable a published label. For more information, see [Disable, enable, and delete a label](<https://developers.google.`com/workspace/drive/labels/guides/disable-delete-label`>). Disabling a label will result in a new disabled published revision based on the current published revision. If there's a draft revision, a new disabled draft revision will be created based on the latest draft revision. Older draft revisions will be deleted. Once disabled, a label may be deleted with DeleteLabel.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drivelabels_labels_disable_builder()` + `drivelabels_labels_disable_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_disable(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleAppsDriveLabelsV2DisableLabelRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2Label>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = drivelabels_labels_disable_builder(client, name, body)?;
    drivelabels_labels_disable_execute(builder)
}

/// GET v2/{+name}:enable
/// Enable a disabled label and restore it to its published state. For more information, see [Disable, enable, and delete a label](<https://developers.google.`com/workspace/drive/labels/guides/disable-delete-label`>). This will result in a new published revision based on the current disabled published revision. If there's an existing disabled draft revision, a new revision will be created based on that draft and will be enabled.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drivelabels_labels_enable_execute()` to send, or `drivelabels_labels_enable` for simplest API.

pub fn drivelabels_labels_enable_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleAppsDriveLabelsV2EnableLabelRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:enable", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:enable
/// Enable a disabled label and restore it to its published state. For more information, see [Disable, enable, and delete a label](<https://developers.google.`com/workspace/drive/labels/guides/disable-delete-label`>). This will result in a new published revision based on the current disabled published revision. If there's an existing disabled draft revision, a new revision will be created based on that draft and will be enabled.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drivelabels_labels_enable_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drivelabels_labels_enable_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_enable_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2Label>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2Label = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:enable
/// Enable a disabled label and restore it to its published state. For more information, see [Disable, enable, and delete a label](<https://developers.google.`com/workspace/drive/labels/guides/disable-delete-label`>). This will result in a new published revision based on the current disabled published revision. If there's an existing disabled draft revision, a new revision will be created based on that draft and will be enabled.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drivelabels_labels_enable_builder()` + `drivelabels_labels_enable_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_enable(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleAppsDriveLabelsV2EnableLabelRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2Label>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = drivelabels_labels_enable_builder(client, name, body)?;
    drivelabels_labels_enable_execute(builder)
}

/// GET v2/{+name}
/// Get a label by its resource name. For more information, see [Search for labels](<https://developers.google.`com/workspace/drive/labels/guides/search-label`>). Resource name may be any of: * `labels/{id}` - See `labels/{id}`@latest * `labels/{id}`@latest - Gets the latest revision of the label. * `labels/{id}`@published - Gets the current published revision of the label. * `labels/{id}`@{revision_id} - Gets the label at the specified revision ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drivelabels_labels_get_execute()` to send, or `drivelabels_labels_get` for simplest API.

pub fn drivelabels_labels_get_builder(
    client: &SimpleHttpClient,
    name: &str,
    languageCode: Option<&str>,
    useAdminAccess: Option<bool>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "languageCode",
            languageCode
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "useAdminAccess",
            useAdminAccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "view",
            view.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+name}
/// Get a label by its resource name. For more information, see [Search for labels](<https://developers.google.`com/workspace/drive/labels/guides/search-label`>). Resource name may be any of: * `labels/{id}` - See `labels/{id}`@latest * `labels/{id}`@latest - Gets the latest revision of the label. * `labels/{id}`@published - Gets the current published revision of the label. * `labels/{id}`@{revision_id} - Gets the label at the specified revision ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drivelabels_labels_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drivelabels_labels_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2Label>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2Label = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Get a label by its resource name. For more information, see [Search for labels](<https://developers.google.`com/workspace/drive/labels/guides/search-label`>). Resource name may be any of: * `labels/{id}` - See `labels/{id}`@latest * `labels/{id}`@latest - Gets the latest revision of the label. * `labels/{id}`@published - Gets the current published revision of the label. * `labels/{id}`@{revision_id} - Gets the label at the specified revision ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drivelabels_labels_get_builder()` + `drivelabels_labels_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_get(
    client: &SimpleHttpClient,
    name: &str,
    languageCode: Option<&str>,
    useAdminAccess: Option<bool>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2Label>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = drivelabels_labels_get_builder(client, name, languageCode, useAdminAccess, view)?;
    drivelabels_labels_get_execute(builder)
}

/// GET v2/labels
/// List labels. For more information, see [Search for labels](<https://developers.google.`com/workspace/drive/labels/guides/search-label`>).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drivelabels_labels_list_execute()` to send, or `drivelabels_labels_list` for simplest API.

pub fn drivelabels_labels_list_builder(
    client: &SimpleHttpClient,
    customer: Option<&str>,
    languageCode: Option<&str>,
    minimumRole: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    publishedOnly: Option<bool>,
    useAdminAccess: Option<bool>,
    view: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/labels",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "customer",
            customer
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "languageCode",
            languageCode
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "minimumRole",
            minimumRole
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "publishedOnly",
            publishedOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "useAdminAccess",
            useAdminAccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "view",
            view.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/labels
/// List labels. For more information, see [Search for labels](<https://developers.google.`com/workspace/drive/labels/guides/search-label`>).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drivelabels_labels_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drivelabels_labels_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2ListLabelsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2ListLabelsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/labels
/// List labels. For more information, see [Search for labels](<https://developers.google.`com/workspace/drive/labels/guides/search-label`>).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drivelabels_labels_list_builder()` + `drivelabels_labels_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_list(
    client: &SimpleHttpClient,
    customer: Option<&str>,
    languageCode: Option<&str>,
    minimumRole: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    publishedOnly: Option<bool>,
    useAdminAccess: Option<bool>,
    view: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2ListLabelsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = drivelabels_labels_list_builder(
        client,
        customer,
        languageCode,
        minimumRole,
        pageSize,
        pageToken,
        publishedOnly,
        useAdminAccess,
        view,
    )?;
    drivelabels_labels_list_execute(builder)
}

/// GET v2/{+name}:publish
/// Publish all draft changes to the label. Once published, the label may not return to its draft state. For more information, see [Create and publish a label](<https://developers.google.`com/workspace/drive/labels/guides/create-label`>). Publishing a label will result in a new published revision. All previous draft revisions will be deleted. Previous published revisions will be kept but are subject to automated deletion as needed. For more information, see [Label lifecycle](<https://developers.google.`com/workspace/drive/labels/guides/label-lifecycle`>). Once published, some changes are no longer permitted. Generally, any change that would invalidate or cause new restrictions on existing metadata related to the label will be rejected. For example, the following changes to a label will be rejected after the label is published: * The label cannot be directly deleted. It must be disabled first, then deleted. * Field.FieldType cannot be changed. * Changes to field validation options cannot reject something that was previously accepted. * Reducing the maximum entries.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drivelabels_labels_publish_execute()` to send, or `drivelabels_labels_publish` for simplest API.

pub fn drivelabels_labels_publish_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleAppsDriveLabelsV2PublishLabelRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}:publish", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:publish
/// Publish all draft changes to the label. Once published, the label may not return to its draft state. For more information, see [Create and publish a label](<https://developers.google.`com/workspace/drive/labels/guides/create-label`>). Publishing a label will result in a new published revision. All previous draft revisions will be deleted. Previous published revisions will be kept but are subject to automated deletion as needed. For more information, see [Label lifecycle](<https://developers.google.`com/workspace/drive/labels/guides/label-lifecycle`>). Once published, some changes are no longer permitted. Generally, any change that would invalidate or cause new restrictions on existing metadata related to the label will be rejected. For example, the following changes to a label will be rejected after the label is published: * The label cannot be directly deleted. It must be disabled first, then deleted. * Field.FieldType cannot be changed. * Changes to field validation options cannot reject something that was previously accepted. * Reducing the maximum entries.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drivelabels_labels_publish_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drivelabels_labels_publish_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_publish_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2Label>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2Label = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:publish
/// Publish all draft changes to the label. Once published, the label may not return to its draft state. For more information, see [Create and publish a label](<https://developers.google.`com/workspace/drive/labels/guides/create-label`>). Publishing a label will result in a new published revision. All previous draft revisions will be deleted. Previous published revisions will be kept but are subject to automated deletion as needed. For more information, see [Label lifecycle](<https://developers.google.`com/workspace/drive/labels/guides/label-lifecycle`>). Once published, some changes are no longer permitted. Generally, any change that would invalidate or cause new restrictions on existing metadata related to the label will be rejected. For example, the following changes to a label will be rejected after the label is published: * The label cannot be directly deleted. It must be disabled first, then deleted. * Field.FieldType cannot be changed. * Changes to field validation options cannot reject something that was previously accepted. * Reducing the maximum entries.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drivelabels_labels_publish_builder()` + `drivelabels_labels_publish_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_publish(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleAppsDriveLabelsV2PublishLabelRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2Label>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = drivelabels_labels_publish_builder(client, name, body)?;
    drivelabels_labels_publish_execute(builder)
}

/// GET v2/{+name}:updateLabelCopyMode
/// Updates a label's CopyMode. Changes to this policy aren't revisioned, don't require publishing, and take effect immediately.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drivelabels_labels_update_label_copy_mode_execute()` to send, or `drivelabels_labels_update_label_copy_mode` for simplest API.

pub fn drivelabels_labels_update_label_copy_mode_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleAppsDriveLabelsV2UpdateLabelCopyModeRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+name}:updateLabelCopyMode",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:updateLabelCopyMode
/// Updates a label's CopyMode. Changes to this policy aren't revisioned, don't require publishing, and take effect immediately.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drivelabels_labels_update_label_copy_mode_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drivelabels_labels_update_label_copy_mode_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_update_label_copy_mode_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2Label>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2Label = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:updateLabelCopyMode
/// Updates a label's CopyMode. Changes to this policy aren't revisioned, don't require publishing, and take effect immediately.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drivelabels_labels_update_label_copy_mode_builder()` + `drivelabels_labels_update_label_copy_mode_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_update_label_copy_mode(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleAppsDriveLabelsV2UpdateLabelCopyModeRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2Label>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = drivelabels_labels_update_label_copy_mode_builder(client, name, body)?;
    drivelabels_labels_update_label_copy_mode_execute(builder)
}

/// GET v2/{+name}:updateLabelEnabledAppSettings
/// Updates a label's EnabledAppSettings. Enabling a label in a Google Workspace app allows it to be used in that app. This change isn't revisioned, doesn't require publishing, and takes effect immediately.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drivelabels_labels_update_label_enabled_app_settings_execute()` to send, or `drivelabels_labels_update_label_enabled_app_settings` for simplest API.

pub fn drivelabels_labels_update_label_enabled_app_settings_builder(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleAppsDriveLabelsV2UpdateLabelEnabledAppSettingsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+name}:updateLabelEnabledAppSettings",
        name,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}:updateLabelEnabledAppSettings
/// Updates a label's EnabledAppSettings. Enabling a label in a Google Workspace app allows it to be used in that app. This change isn't revisioned, doesn't require publishing, and takes effect immediately.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drivelabels_labels_update_label_enabled_app_settings_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drivelabels_labels_update_label_enabled_app_settings_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_update_label_enabled_app_settings_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2Label>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2Label = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}:updateLabelEnabledAppSettings
/// Updates a label's EnabledAppSettings. Enabling a label in a Google Workspace app allows it to be used in that app. This change isn't revisioned, doesn't require publishing, and takes effect immediately.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drivelabels_labels_update_label_enabled_app_settings_builder()` + `drivelabels_labels_update_label_enabled_app_settings_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_update_label_enabled_app_settings(
    client: &SimpleHttpClient,
    name: &str,
    body: &GoogleAppsDriveLabelsV2UpdateLabelEnabledAppSettingsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2Label>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = drivelabels_labels_update_label_enabled_app_settings_builder(client, name, body)?;
    drivelabels_labels_update_label_enabled_app_settings_execute(builder)
}

/// GET v2/{+parent}/permissions
/// Updates a label's permissions. If a permission for the indicated principal doesn't exist, a label permission is created, otherwise the existing permission is updated. Permissions affect the label resource as a whole, aren't revisioned, and don't require publishing.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drivelabels_labels_update_permissions_execute()` to send, or `drivelabels_labels_update_permissions` for simplest API.

pub fn drivelabels_labels_update_permissions_builder(
    client: &SimpleHttpClient,
    parent: &str,
    useAdminAccess: Option<bool>,
    body: &GoogleAppsDriveLabelsV2LabelPermission,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/permissions", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "useAdminAccess",
            useAdminAccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/permissions
/// Updates a label's permissions. If a permission for the indicated principal doesn't exist, a label permission is created, otherwise the existing permission is updated. Permissions affect the label resource as a whole, aren't revisioned, and don't require publishing.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drivelabels_labels_update_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drivelabels_labels_update_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_update_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2LabelPermission>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2LabelPermission = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/permissions
/// Updates a label's permissions. If a permission for the indicated principal doesn't exist, a label permission is created, otherwise the existing permission is updated. Permissions affect the label resource as a whole, aren't revisioned, and don't require publishing.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drivelabels_labels_update_permissions_builder()` + `drivelabels_labels_update_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_update_permissions(
    client: &SimpleHttpClient,
    parent: &str,
    useAdminAccess: Option<bool>,
    body: &GoogleAppsDriveLabelsV2LabelPermission,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2LabelPermission>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        drivelabels_labels_update_permissions_builder(client, parent, useAdminAccess, body)?;
    drivelabels_labels_update_permissions_execute(builder)
}

/// GET v2/{+parent}/locks
/// Lists the label locks on a label.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drivelabels_labels_locks_list_execute()` to send, or `drivelabels_labels_locks_list` for simplest API.

pub fn drivelabels_labels_locks_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/locks", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/locks
/// Lists the label locks on a label.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drivelabels_labels_locks_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drivelabels_labels_locks_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_locks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2ListLabelLocksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2ListLabelLocksResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/locks
/// Lists the label locks on a label.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drivelabels_labels_locks_list_builder()` + `drivelabels_labels_locks_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_locks_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2ListLabelLocksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = drivelabels_labels_locks_list_builder(client, parent, pageSize, pageToken)?;
    drivelabels_labels_locks_list_execute(builder)
}

/// GET v2/{+parent}/permissions:batchDelete
/// Deletes label permissions. Permissions affect the label resource as a whole, aren't revisioned, and don't require publishing.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drivelabels_labels_permissions_batch_delete_execute()` to send, or `drivelabels_labels_permissions_batch_delete` for simplest API.

pub fn drivelabels_labels_permissions_batch_delete_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleAppsDriveLabelsV2BatchDeleteLabelPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/permissions:batchDelete",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/permissions:batchDelete
/// Deletes label permissions. Permissions affect the label resource as a whole, aren't revisioned, and don't require publishing.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drivelabels_labels_permissions_batch_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drivelabels_labels_permissions_batch_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_permissions_batch_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/permissions:batchDelete
/// Deletes label permissions. Permissions affect the label resource as a whole, aren't revisioned, and don't require publishing.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drivelabels_labels_permissions_batch_delete_builder()` + `drivelabels_labels_permissions_batch_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_permissions_batch_delete(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleAppsDriveLabelsV2BatchDeleteLabelPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = drivelabels_labels_permissions_batch_delete_builder(client, parent, body)?;
    drivelabels_labels_permissions_batch_delete_execute(builder)
}

/// GET v2/{+parent}/permissions:batchUpdate
/// Updates label permissions. If a permission for the indicated principal doesn't exist, a label permission is created, otherwise the existing permission is updated. Permissions affect the label resource as a whole, aren't revisioned, and don't require publishing.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drivelabels_labels_permissions_batch_update_execute()` to send, or `drivelabels_labels_permissions_batch_update` for simplest API.

pub fn drivelabels_labels_permissions_batch_update_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleAppsDriveLabelsV2BatchUpdateLabelPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/permissions:batchUpdate",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/permissions:batchUpdate
/// Updates label permissions. If a permission for the indicated principal doesn't exist, a label permission is created, otherwise the existing permission is updated. Permissions affect the label resource as a whole, aren't revisioned, and don't require publishing.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drivelabels_labels_permissions_batch_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drivelabels_labels_permissions_batch_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_permissions_batch_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAppsDriveLabelsV2BatchUpdateLabelPermissionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2BatchUpdateLabelPermissionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/permissions:batchUpdate
/// Updates label permissions. If a permission for the indicated principal doesn't exist, a label permission is created, otherwise the existing permission is updated. Permissions affect the label resource as a whole, aren't revisioned, and don't require publishing.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drivelabels_labels_permissions_batch_update_builder()` + `drivelabels_labels_permissions_batch_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_permissions_batch_update(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleAppsDriveLabelsV2BatchUpdateLabelPermissionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAppsDriveLabelsV2BatchUpdateLabelPermissionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = drivelabels_labels_permissions_batch_update_builder(client, parent, body)?;
    drivelabels_labels_permissions_batch_update_execute(builder)
}

/// GET v2/{+parent}/permissions
/// Updates a label's permissions. If a permission for the indicated principal doesn't exist, a label permission is created, otherwise the existing permission is updated. Permissions affect the label resource as a whole, aren't revisioned, and don't require publishing.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drivelabels_labels_permissions_create_execute()` to send, or `drivelabels_labels_permissions_create` for simplest API.

pub fn drivelabels_labels_permissions_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    useAdminAccess: Option<bool>,
    body: &GoogleAppsDriveLabelsV2LabelPermission,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/permissions", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "useAdminAccess",
            useAdminAccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/permissions
/// Updates a label's permissions. If a permission for the indicated principal doesn't exist, a label permission is created, otherwise the existing permission is updated. Permissions affect the label resource as a whole, aren't revisioned, and don't require publishing.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drivelabels_labels_permissions_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drivelabels_labels_permissions_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_permissions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2LabelPermission>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2LabelPermission = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/permissions
/// Updates a label's permissions. If a permission for the indicated principal doesn't exist, a label permission is created, otherwise the existing permission is updated. Permissions affect the label resource as a whole, aren't revisioned, and don't require publishing.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drivelabels_labels_permissions_create_builder()` + `drivelabels_labels_permissions_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_permissions_create(
    client: &SimpleHttpClient,
    parent: &str,
    useAdminAccess: Option<bool>,
    body: &GoogleAppsDriveLabelsV2LabelPermission,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2LabelPermission>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        drivelabels_labels_permissions_create_builder(client, parent, useAdminAccess, body)?;
    drivelabels_labels_permissions_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes a label's permission. Permissions affect the label resource as a whole, aren't revisioned, and don't require publishing.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drivelabels_labels_permissions_delete_execute()` to send, or `drivelabels_labels_permissions_delete` for simplest API.

pub fn drivelabels_labels_permissions_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    useAdminAccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "useAdminAccess",
            useAdminAccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+name}
/// Deletes a label's permission. Permissions affect the label resource as a whole, aren't revisioned, and don't require publishing.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drivelabels_labels_permissions_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drivelabels_labels_permissions_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_permissions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a label's permission. Permissions affect the label resource as a whole, aren't revisioned, and don't require publishing.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drivelabels_labels_permissions_delete_builder()` + `drivelabels_labels_permissions_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_permissions_delete(
    client: &SimpleHttpClient,
    name: &str,
    useAdminAccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = drivelabels_labels_permissions_delete_builder(client, name, useAdminAccess)?;
    drivelabels_labels_permissions_delete_execute(builder)
}

/// GET v2/{+parent}/permissions
/// Lists a label's permissions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drivelabels_labels_permissions_list_execute()` to send, or `drivelabels_labels_permissions_list` for simplest API.

pub fn drivelabels_labels_permissions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    useAdminAccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/permissions", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "useAdminAccess",
            useAdminAccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/permissions
/// Lists a label's permissions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drivelabels_labels_permissions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drivelabels_labels_permissions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_permissions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2ListLabelPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2ListLabelPermissionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/permissions
/// Lists a label's permissions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drivelabels_labels_permissions_list_builder()` + `drivelabels_labels_permissions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_permissions_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    useAdminAccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2ListLabelPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = drivelabels_labels_permissions_list_builder(
        client,
        parent,
        pageSize,
        pageToken,
        useAdminAccess,
    )?;
    drivelabels_labels_permissions_list_execute(builder)
}

/// GET v2/{+parent}/permissions
/// Updates a label's permissions. If a permission for the indicated principal doesn't exist, a label permission is created, otherwise the existing permission is updated. Permissions affect the label resource as a whole, aren't revisioned, and don't require publishing.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drivelabels_labels_revisions_update_permissions_execute()` to send, or `drivelabels_labels_revisions_update_permissions` for simplest API.

pub fn drivelabels_labels_revisions_update_permissions_builder(
    client: &SimpleHttpClient,
    parent: &str,
    useAdminAccess: Option<bool>,
    body: &GoogleAppsDriveLabelsV2LabelPermission,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/permissions", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "useAdminAccess",
            useAdminAccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/permissions
/// Updates a label's permissions. If a permission for the indicated principal doesn't exist, a label permission is created, otherwise the existing permission is updated. Permissions affect the label resource as a whole, aren't revisioned, and don't require publishing.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drivelabels_labels_revisions_update_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drivelabels_labels_revisions_update_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_revisions_update_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2LabelPermission>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2LabelPermission = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/permissions
/// Updates a label's permissions. If a permission for the indicated principal doesn't exist, a label permission is created, otherwise the existing permission is updated. Permissions affect the label resource as a whole, aren't revisioned, and don't require publishing.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drivelabels_labels_revisions_update_permissions_builder()` + `drivelabels_labels_revisions_update_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_revisions_update_permissions(
    client: &SimpleHttpClient,
    parent: &str,
    useAdminAccess: Option<bool>,
    body: &GoogleAppsDriveLabelsV2LabelPermission,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2LabelPermission>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = drivelabels_labels_revisions_update_permissions_builder(
        client,
        parent,
        useAdminAccess,
        body,
    )?;
    drivelabels_labels_revisions_update_permissions_execute(builder)
}

/// GET v2/{+parent}/locks
/// Lists the label locks on a label.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drivelabels_labels_revisions_locks_list_execute()` to send, or `drivelabels_labels_revisions_locks_list` for simplest API.

pub fn drivelabels_labels_revisions_locks_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/locks", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/locks
/// Lists the label locks on a label.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drivelabels_labels_revisions_locks_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drivelabels_labels_revisions_locks_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_revisions_locks_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2ListLabelLocksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2ListLabelLocksResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/locks
/// Lists the label locks on a label.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drivelabels_labels_revisions_locks_list_builder()` + `drivelabels_labels_revisions_locks_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_revisions_locks_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2ListLabelLocksResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        drivelabels_labels_revisions_locks_list_builder(client, parent, pageSize, pageToken)?;
    drivelabels_labels_revisions_locks_list_execute(builder)
}

/// GET v2/{+parent}/permissions:batchDelete
/// Deletes label permissions. Permissions affect the label resource as a whole, aren't revisioned, and don't require publishing.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drivelabels_labels_revisions_permissions_batch_delete_execute()` to send, or `drivelabels_labels_revisions_permissions_batch_delete` for simplest API.

pub fn drivelabels_labels_revisions_permissions_batch_delete_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleAppsDriveLabelsV2BatchDeleteLabelPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/permissions:batchDelete",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/permissions:batchDelete
/// Deletes label permissions. Permissions affect the label resource as a whole, aren't revisioned, and don't require publishing.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drivelabels_labels_revisions_permissions_batch_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drivelabels_labels_revisions_permissions_batch_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_revisions_permissions_batch_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/permissions:batchDelete
/// Deletes label permissions. Permissions affect the label resource as a whole, aren't revisioned, and don't require publishing.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drivelabels_labels_revisions_permissions_batch_delete_builder()` + `drivelabels_labels_revisions_permissions_batch_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_revisions_permissions_batch_delete(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleAppsDriveLabelsV2BatchDeleteLabelPermissionsRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        drivelabels_labels_revisions_permissions_batch_delete_builder(client, parent, body)?;
    drivelabels_labels_revisions_permissions_batch_delete_execute(builder)
}

/// GET v2/{+parent}/permissions:batchUpdate
/// Updates label permissions. If a permission for the indicated principal doesn't exist, a label permission is created, otherwise the existing permission is updated. Permissions affect the label resource as a whole, aren't revisioned, and don't require publishing.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drivelabels_labels_revisions_permissions_batch_update_execute()` to send, or `drivelabels_labels_revisions_permissions_batch_update` for simplest API.

pub fn drivelabels_labels_revisions_permissions_batch_update_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleAppsDriveLabelsV2BatchUpdateLabelPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+parent}/permissions:batchUpdate",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/permissions:batchUpdate
/// Updates label permissions. If a permission for the indicated principal doesn't exist, a label permission is created, otherwise the existing permission is updated. Permissions affect the label resource as a whole, aren't revisioned, and don't require publishing.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drivelabels_labels_revisions_permissions_batch_update_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drivelabels_labels_revisions_permissions_batch_update_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_revisions_permissions_batch_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAppsDriveLabelsV2BatchUpdateLabelPermissionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2BatchUpdateLabelPermissionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/permissions:batchUpdate
/// Updates label permissions. If a permission for the indicated principal doesn't exist, a label permission is created, otherwise the existing permission is updated. Permissions affect the label resource as a whole, aren't revisioned, and don't require publishing.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drivelabels_labels_revisions_permissions_batch_update_builder()` + `drivelabels_labels_revisions_permissions_batch_update_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_revisions_permissions_batch_update(
    client: &SimpleHttpClient,
    parent: &str,
    body: &GoogleAppsDriveLabelsV2BatchUpdateLabelPermissionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<
                ApiResponse<GoogleAppsDriveLabelsV2BatchUpdateLabelPermissionsResponse>,
                ApiError,
            >,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        drivelabels_labels_revisions_permissions_batch_update_builder(client, parent, body)?;
    drivelabels_labels_revisions_permissions_batch_update_execute(builder)
}

/// GET v2/{+parent}/permissions
/// Updates a label's permissions. If a permission for the indicated principal doesn't exist, a label permission is created, otherwise the existing permission is updated. Permissions affect the label resource as a whole, aren't revisioned, and don't require publishing.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drivelabels_labels_revisions_permissions_create_execute()` to send, or `drivelabels_labels_revisions_permissions_create` for simplest API.

pub fn drivelabels_labels_revisions_permissions_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    useAdminAccess: Option<bool>,
    body: &GoogleAppsDriveLabelsV2LabelPermission,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/permissions", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "useAdminAccess",
            useAdminAccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/permissions
/// Updates a label's permissions. If a permission for the indicated principal doesn't exist, a label permission is created, otherwise the existing permission is updated. Permissions affect the label resource as a whole, aren't revisioned, and don't require publishing.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drivelabels_labels_revisions_permissions_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drivelabels_labels_revisions_permissions_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_revisions_permissions_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2LabelPermission>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2LabelPermission = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/permissions
/// Updates a label's permissions. If a permission for the indicated principal doesn't exist, a label permission is created, otherwise the existing permission is updated. Permissions affect the label resource as a whole, aren't revisioned, and don't require publishing.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drivelabels_labels_revisions_permissions_create_builder()` + `drivelabels_labels_revisions_permissions_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_revisions_permissions_create(
    client: &SimpleHttpClient,
    parent: &str,
    useAdminAccess: Option<bool>,
    body: &GoogleAppsDriveLabelsV2LabelPermission,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2LabelPermission>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = drivelabels_labels_revisions_permissions_create_builder(
        client,
        parent,
        useAdminAccess,
        body,
    )?;
    drivelabels_labels_revisions_permissions_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes a label's permission. Permissions affect the label resource as a whole, aren't revisioned, and don't require publishing.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drivelabels_labels_revisions_permissions_delete_execute()` to send, or `drivelabels_labels_revisions_permissions_delete` for simplest API.

pub fn drivelabels_labels_revisions_permissions_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
    useAdminAccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "useAdminAccess",
            useAdminAccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+name}
/// Deletes a label's permission. Permissions affect the label resource as a whole, aren't revisioned, and don't require publishing.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drivelabels_labels_revisions_permissions_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drivelabels_labels_revisions_permissions_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_revisions_permissions_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleProtobufEmpty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes a label's permission. Permissions affect the label resource as a whole, aren't revisioned, and don't require publishing.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drivelabels_labels_revisions_permissions_delete_builder()` + `drivelabels_labels_revisions_permissions_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_revisions_permissions_delete(
    client: &SimpleHttpClient,
    name: &str,
    useAdminAccess: Option<bool>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<GoogleProtobufEmpty>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        drivelabels_labels_revisions_permissions_delete_builder(client, name, useAdminAccess)?;
    drivelabels_labels_revisions_permissions_delete_execute(builder)
}

/// GET v2/{+parent}/permissions
/// Lists a label's permissions.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drivelabels_labels_revisions_permissions_list_execute()` to send, or `drivelabels_labels_revisions_permissions_list` for simplest API.

pub fn drivelabels_labels_revisions_permissions_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    useAdminAccess: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/permissions", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "useAdminAccess",
            useAdminAccess
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/permissions
/// Lists a label's permissions.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drivelabels_labels_revisions_permissions_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drivelabels_labels_revisions_permissions_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_revisions_permissions_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2ListLabelPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2ListLabelPermissionsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/permissions
/// Lists a label's permissions.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drivelabels_labels_revisions_permissions_list_builder()` + `drivelabels_labels_revisions_permissions_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_labels_revisions_permissions_list(
    client: &SimpleHttpClient,
    parent: &str,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
    useAdminAccess: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2ListLabelPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = drivelabels_labels_revisions_permissions_list_builder(
        client,
        parent,
        pageSize,
        pageToken,
        useAdminAccess,
    )?;
    drivelabels_labels_revisions_permissions_list_execute(builder)
}

/// GET v2/limits/label
/// Get the constraints on the structure of a label; such as, the maximum number of fields allowed and maximum length of the label title.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drivelabels_limits_get_label_execute()` to send, or `drivelabels_limits_get_label` for simplest API.

pub fn drivelabels_limits_get_label_builder(
    client: &SimpleHttpClient,
    name: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/limits/label",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "name",
            name.as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/limits/label
/// Get the constraints on the structure of a label; such as, the maximum number of fields allowed and maximum length of the label title.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drivelabels_limits_get_label_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drivelabels_limits_get_label_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_limits_get_label_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2LabelLimits>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2LabelLimits = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/limits/label
/// Get the constraints on the structure of a label; such as, the maximum number of fields allowed and maximum length of the label title.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drivelabels_limits_get_label_builder()` + `drivelabels_limits_get_label_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_limits_get_label(
    client: &SimpleHttpClient,
    name: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2LabelLimits>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = drivelabels_limits_get_label_builder(client, name)?;
    drivelabels_limits_get_label_execute(builder)
}

/// GET v2/{+name}
/// Gets the user capabilities.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `drivelabels_users_get_capabilities_execute()` to send, or `drivelabels_users_get_capabilities` for simplest API.

pub fn drivelabels_users_get_capabilities_builder(
    client: &SimpleHttpClient,
    name: &str,
    customer: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "customer",
            customer
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+name}
/// Gets the user capabilities.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `drivelabels_users_get_capabilities_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from drivelabels_users_get_capabilities_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_users_get_capabilities_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2UserCapabilities>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleAppsDriveLabelsV2UserCapabilities =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets the user capabilities.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `drivelabels_users_get_capabilities_builder()` + `drivelabels_users_get_capabilities_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn drivelabels_users_get_capabilities(
    client: &SimpleHttpClient,
    name: &str,
    customer: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleAppsDriveLabelsV2UserCapabilities>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = drivelabels_users_get_capabilities_builder(client, name, customer)?;
    drivelabels_users_get_capabilities_execute(builder)
}
