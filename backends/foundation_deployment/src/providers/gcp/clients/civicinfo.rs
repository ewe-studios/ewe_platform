//! Auto-generated API clients for gcp/civicinfo.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET civicinfo/v2/divisionsByAddress
/// Lookup OCDIDs and names for divisions related to an address.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `civicinfo_divisions_query_division_by_address_execute()` to send, or `civicinfo_divisions_query_division_by_address` for simplest API.

pub fn civicinfo_divisions_query_division_by_address_builder<R>(
    client: &SimpleHttpClient<R>,
    address: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://civicinfo.googleapis.com/civicinfo/v2/divisionsByAddress",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = address.as_ref() {
        query_parts.push(format!("address={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET civicinfo/v2/divisionsByAddress
/// Lookup OCDIDs and names for divisions related to an address.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `civicinfo_divisions_query_division_by_address_execute()` or `civicinfo_divisions_query_division_by_address`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `civicinfo_divisions_query_division_by_address_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn civicinfo_divisions_query_division_by_address_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CivicinfoApiprotosV2DivisionByAddressResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CivicinfoApiprotosV2DivisionByAddressResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET civicinfo/v2/divisionsByAddress
/// Lookup OCDIDs and names for divisions related to an address.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `civicinfo_divisions_query_division_by_address_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `civicinfo_divisions_query_division_by_address_task()`.
/// For the simplest API, use `civicinfo_divisions_query_division_by_address()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `civicinfo_divisions_query_division_by_address_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn civicinfo_divisions_query_division_by_address_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CivicinfoApiprotosV2DivisionByAddressResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = civicinfo_divisions_query_division_by_address_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`civicinfo_divisions_query_division_by_address`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CivicinfoDivisionsQueryDivisionByAddressArgs {
    /// Query parameter: address
    pub address: Option<String>,
}

/// GET civicinfo/v2/divisionsByAddress
/// Lookup OCDIDs and names for divisions related to an address.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `civicinfo_divisions_query_division_by_address_builder()` + `civicinfo_divisions_query_division_by_address_execute()`.
/// For task-level control, use `civicinfo_divisions_query_division_by_address_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn civicinfo_divisions_query_division_by_address(
    client: &SimpleHttpClient,
    args: &CivicinfoDivisionsQueryDivisionByAddressArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CivicinfoApiprotosV2DivisionByAddressResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = civicinfo_divisions_query_division_by_address_builder(client, &args.address)?;
    civicinfo_divisions_query_division_by_address_execute(builder)
}

/// GET civicinfo/v2/divisions
/// Searches for political divisions by their natural name or OCD ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `civicinfo_divisions_search_execute()` to send, or `civicinfo_divisions_search` for simplest API.

pub fn civicinfo_divisions_search_builder<R>(
    client: &SimpleHttpClient<R>,
    query: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://civicinfo.googleapis.com/civicinfo/v2/divisions",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = query.as_ref() {
        query_parts.push(format!("query={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET civicinfo/v2/divisions
/// Searches for political divisions by their natural name or OCD ID.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `civicinfo_divisions_search_execute()` or `civicinfo_divisions_search`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `civicinfo_divisions_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn civicinfo_divisions_search_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CivicinfoApiprotosV2DivisionSearchResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CivicinfoApiprotosV2DivisionSearchResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET civicinfo/v2/divisions
/// Searches for political divisions by their natural name or OCD ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `civicinfo_divisions_search_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `civicinfo_divisions_search_task()`.
/// For the simplest API, use `civicinfo_divisions_search()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `civicinfo_divisions_search_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn civicinfo_divisions_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CivicinfoApiprotosV2DivisionSearchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = civicinfo_divisions_search_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`civicinfo_divisions_search`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CivicinfoDivisionsSearchArgs {
    /// Query parameter: query
    pub query: Option<String>,
}

/// GET civicinfo/v2/divisions
/// Searches for political divisions by their natural name or OCD ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `civicinfo_divisions_search_builder()` + `civicinfo_divisions_search_execute()`.
/// For task-level control, use `civicinfo_divisions_search_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn civicinfo_divisions_search(
    client: &SimpleHttpClient,
    args: &CivicinfoDivisionsSearchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CivicinfoApiprotosV2DivisionSearchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = civicinfo_divisions_search_builder(client, &args.query)?;
    civicinfo_divisions_search_execute(builder)
}

/// GET civicinfo/v2/elections
/// List of available elections to query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `civicinfo_elections_election_query_execute()` to send, or `civicinfo_elections_election_query` for simplest API.

pub fn civicinfo_elections_election_query_builder<R>(
    client: &SimpleHttpClient<R>,
    productionDataOnly: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://civicinfo.googleapis.com/civicinfo/v2/elections",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = productionDataOnly.as_ref() {
        query_parts.push(format!("productionDataOnly={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET civicinfo/v2/elections
/// List of available elections to query.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `civicinfo_elections_election_query_execute()` or `civicinfo_elections_election_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `civicinfo_elections_election_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn civicinfo_elections_election_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CivicinfoApiprotosV2ElectionsQueryResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CivicinfoApiprotosV2ElectionsQueryResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET civicinfo/v2/elections
/// List of available elections to query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `civicinfo_elections_election_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `civicinfo_elections_election_query_task()`.
/// For the simplest API, use `civicinfo_elections_election_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `civicinfo_elections_election_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn civicinfo_elections_election_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CivicinfoApiprotosV2ElectionsQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = civicinfo_elections_election_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`civicinfo_elections_election_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CivicinfoElectionsElectionQueryArgs {
    /// Query parameter: productionDataOnly
    pub productionDataOnly: Option<String>,
}

/// GET civicinfo/v2/elections
/// List of available elections to query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `civicinfo_elections_election_query_builder()` + `civicinfo_elections_election_query_execute()`.
/// For task-level control, use `civicinfo_elections_election_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn civicinfo_elections_election_query(
    client: &SimpleHttpClient,
    args: &CivicinfoElectionsElectionQueryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CivicinfoApiprotosV2ElectionsQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = civicinfo_elections_election_query_builder(client, &args.productionDataOnly)?;
    civicinfo_elections_election_query_execute(builder)
}

/// GET civicinfo/v2/voterinfo
/// Looks up information relevant to a voter based on the voter''s registered address.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `civicinfo_elections_voter_info_query_execute()` to send, or `civicinfo_elections_voter_info_query` for simplest API.

pub fn civicinfo_elections_voter_info_query_builder<R>(
    client: &SimpleHttpClient<R>,
    address: &Option<String>,
    electionId: &Option<String>,
    officialOnly: &Option<String>,
    productionDataOnly: &Option<String>,
    returnAllAvailableData: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://civicinfo.googleapis.com/civicinfo/v2/voterinfo",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = address.as_ref() {
        query_parts.push(format!("address={}", val));
    }
    if let Some(val) = electionId.as_ref() {
        query_parts.push(format!("electionId={}", val));
    }
    if let Some(val) = officialOnly.as_ref() {
        query_parts.push(format!("officialOnly={}", val));
    }
    if let Some(val) = productionDataOnly.as_ref() {
        query_parts.push(format!("productionDataOnly={}", val));
    }
    if let Some(val) = returnAllAvailableData.as_ref() {
        query_parts.push(format!("returnAllAvailableData={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET civicinfo/v2/voterinfo
/// Looks up information relevant to a voter based on the voter''s registered address.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `civicinfo_elections_voter_info_query_execute()` or `civicinfo_elections_voter_info_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `civicinfo_elections_voter_info_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn civicinfo_elections_voter_info_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CivicinfoApiprotosV2VoterInfoResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CivicinfoApiprotosV2VoterInfoResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET civicinfo/v2/voterinfo
/// Looks up information relevant to a voter based on the voter''s registered address.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `civicinfo_elections_voter_info_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `civicinfo_elections_voter_info_query_task()`.
/// For the simplest API, use `civicinfo_elections_voter_info_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `civicinfo_elections_voter_info_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn civicinfo_elections_voter_info_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CivicinfoApiprotosV2VoterInfoResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = civicinfo_elections_voter_info_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`civicinfo_elections_voter_info_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CivicinfoElectionsVoterInfoQueryArgs {
    /// Query parameter: address
    pub address: Option<String>,
    /// Query parameter: electionId
    pub electionId: Option<String>,
    /// Query parameter: officialOnly
    pub officialOnly: Option<String>,
    /// Query parameter: productionDataOnly
    pub productionDataOnly: Option<String>,
    /// Query parameter: returnAllAvailableData
    pub returnAllAvailableData: Option<String>,
}

/// GET civicinfo/v2/voterinfo
/// Looks up information relevant to a voter based on the voter''s registered address.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `civicinfo_elections_voter_info_query_builder()` + `civicinfo_elections_voter_info_query_execute()`.
/// For task-level control, use `civicinfo_elections_voter_info_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn civicinfo_elections_voter_info_query(
    client: &SimpleHttpClient,
    args: &CivicinfoElectionsVoterInfoQueryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CivicinfoApiprotosV2VoterInfoResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = civicinfo_elections_voter_info_query_builder(
        client,
        &args.address,
        &args.electionId,
        &args.officialOnly,
        &args.productionDataOnly,
        &args.returnAllAvailableData,
    )?;
    civicinfo_elections_voter_info_query_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for CivicinfoApiprotosV2DivisionByAddressResponse
// =============================================================================

/// ResourceIdentifier implementation for CivicinfoApiprotosV2DivisionByAddressResponse with CivicinfoDivisionsQueryDivisionByAddressArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CivicinfoDivisionsQueryDivisionByAddressArgs>
    for CivicinfoApiprotosV2DivisionByAddressResponse
{
    fn generate_resource_id(&self, input: &CivicinfoDivisionsQueryDivisionByAddressArgs) -> String {
        "gcp::civicinfo::CivicinfoApiprotosV2DivisionByAddressResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::civicinfo::CivicinfoApiprotosV2DivisionByAddressResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CivicinfoApiprotosV2DivisionSearchResponse
// =============================================================================

/// ResourceIdentifier implementation for CivicinfoApiprotosV2DivisionSearchResponse with CivicinfoDivisionsSearchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CivicinfoDivisionsSearchArgs>
    for CivicinfoApiprotosV2DivisionSearchResponse
{
    fn generate_resource_id(&self, input: &CivicinfoDivisionsSearchArgs) -> String {
        "gcp::civicinfo::CivicinfoApiprotosV2DivisionSearchResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::civicinfo::CivicinfoApiprotosV2DivisionSearchResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CivicinfoApiprotosV2ElectionsQueryResponse
// =============================================================================

/// ResourceIdentifier implementation for CivicinfoApiprotosV2ElectionsQueryResponse with CivicinfoElectionsElectionQueryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CivicinfoElectionsElectionQueryArgs>
    for CivicinfoApiprotosV2ElectionsQueryResponse
{
    fn generate_resource_id(&self, input: &CivicinfoElectionsElectionQueryArgs) -> String {
        "gcp::civicinfo::CivicinfoApiprotosV2ElectionsQueryResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::civicinfo::CivicinfoApiprotosV2ElectionsQueryResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for CivicinfoApiprotosV2VoterInfoResponse
// =============================================================================

/// ResourceIdentifier implementation for CivicinfoApiprotosV2VoterInfoResponse with CivicinfoElectionsVoterInfoQueryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<CivicinfoElectionsVoterInfoQueryArgs>
    for CivicinfoApiprotosV2VoterInfoResponse
{
    fn generate_resource_id(&self, input: &CivicinfoElectionsVoterInfoQueryArgs) -> String {
        "gcp::civicinfo::CivicinfoApiprotosV2VoterInfoResponse".to_string()
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::civicinfo::CivicinfoApiprotosV2VoterInfoResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
