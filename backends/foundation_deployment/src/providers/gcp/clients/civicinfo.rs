//! Auto-generated API clients for gcp/civicinfo.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET civicinfo/v2/divisionsByAddress
/// Lookup OCDIDs and names for divisions related to an address.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `civicinfo_divisions_query_division_by_address_execute()` to send, or `civicinfo_divisions_query_division_by_address` for simplest API.

pub fn civicinfo_divisions_query_division_by_address_builder(
    client: &SimpleHttpClient,
    address: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comcivicinfo/v2/divisionsByAddress",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "address",
            address
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET civicinfo/v2/divisionsByAddress
/// Lookup OCDIDs and names for divisions related to an address.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `civicinfo_divisions_query_division_by_address_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from civicinfo_divisions_query_division_by_address_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn civicinfo_divisions_query_division_by_address_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CivicinfoApiprotosV2DivisionByAddressResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CivicinfoApiprotosV2DivisionByAddressResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET civicinfo/v2/divisionsByAddress
/// Lookup OCDIDs and names for divisions related to an address.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `civicinfo_divisions_query_division_by_address_builder()` + `civicinfo_divisions_query_division_by_address_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn civicinfo_divisions_query_division_by_address(
    client: &SimpleHttpClient,
    address: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CivicinfoApiprotosV2DivisionByAddressResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = civicinfo_divisions_query_division_by_address_builder(client, address)?;
    civicinfo_divisions_query_division_by_address_execute(builder)
}

/// GET civicinfo/v2/divisions
/// Searches for political divisions by their natural name or OCD ID.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `civicinfo_divisions_search_execute()` to send, or `civicinfo_divisions_search` for simplest API.

pub fn civicinfo_divisions_search_builder(
    client: &SimpleHttpClient,
    query: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comcivicinfo/v2/divisions",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "query",
            query
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET civicinfo/v2/divisions
/// Searches for political divisions by their natural name or OCD ID.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `civicinfo_divisions_search_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from civicinfo_divisions_search_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn civicinfo_divisions_search_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CivicinfoApiprotosV2DivisionSearchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CivicinfoApiprotosV2DivisionSearchResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET civicinfo/v2/divisions
/// Searches for political divisions by their natural name or OCD ID.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `civicinfo_divisions_search_builder()` + `civicinfo_divisions_search_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn civicinfo_divisions_search(
    client: &SimpleHttpClient,
    query: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CivicinfoApiprotosV2DivisionSearchResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = civicinfo_divisions_search_builder(client, query)?;
    civicinfo_divisions_search_execute(builder)
}

/// GET civicinfo/v2/elections
/// List of available elections to query.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `civicinfo_elections_election_query_execute()` to send, or `civicinfo_elections_election_query` for simplest API.

pub fn civicinfo_elections_election_query_builder(
    client: &SimpleHttpClient,
    productionDataOnly: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comcivicinfo/v2/elections",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "productionDataOnly",
            productionDataOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET civicinfo/v2/elections
/// List of available elections to query.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `civicinfo_elections_election_query_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from civicinfo_elections_election_query_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn civicinfo_elections_election_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CivicinfoApiprotosV2ElectionsQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CivicinfoApiprotosV2ElectionsQueryResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET civicinfo/v2/elections
/// List of available elections to query.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `civicinfo_elections_election_query_builder()` + `civicinfo_elections_election_query_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn civicinfo_elections_election_query(
    client: &SimpleHttpClient,
    productionDataOnly: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CivicinfoApiprotosV2ElectionsQueryResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = civicinfo_elections_election_query_builder(client, productionDataOnly)?;
    civicinfo_elections_election_query_execute(builder)
}

/// GET civicinfo/v2/voterinfo
/// Looks up information relevant to a voter based on the voter's registered address.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `civicinfo_elections_voter_info_query_execute()` to send, or `civicinfo_elections_voter_info_query` for simplest API.

pub fn civicinfo_elections_voter_info_query_builder(
    client: &SimpleHttpClient,
    address: Option<&str>,
    electionId: Option<&str>,
    officialOnly: Option<bool>,
    productionDataOnly: Option<bool>,
    returnAllAvailableData: Option<bool>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comcivicinfo/v2/voterinfo",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "address",
            address
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "electionId",
            electionId
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "officialOnly",
            officialOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "productionDataOnly",
            productionDataOnly
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "returnAllAvailableData",
            returnAllAvailableData
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET civicinfo/v2/voterinfo
/// Looks up information relevant to a voter based on the voter's registered address.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `civicinfo_elections_voter_info_query_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from civicinfo_elections_voter_info_query_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn civicinfo_elections_voter_info_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CivicinfoApiprotosV2VoterInfoResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CivicinfoApiprotosV2VoterInfoResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET civicinfo/v2/voterinfo
/// Looks up information relevant to a voter based on the voter's registered address.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `civicinfo_elections_voter_info_query_builder()` + `civicinfo_elections_voter_info_query_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn civicinfo_elections_voter_info_query(
    client: &SimpleHttpClient,
    address: Option<&str>,
    electionId: Option<&str>,
    officialOnly: Option<bool>,
    productionDataOnly: Option<bool>,
    returnAllAvailableData: Option<bool>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CivicinfoApiprotosV2VoterInfoResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = civicinfo_elections_voter_info_query_builder(
        client,
        address,
        electionId,
        officialOnly,
        productionDataOnly,
        returnAllAvailableData,
    )?;
    civicinfo_elections_voter_info_query_execute(builder)
}
