//! Auto-generated API clients for gcp/checks.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1alpha/accounts/{accountsId}/apps/{appsId}
/// Gets an app.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `checks_accounts_apps_get_execute()` to send, or `checks_accounts_apps_get` for simplest API.

pub fn checks_accounts_apps_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://checks.googleapis.com/v1alpha/accounts/{}/apps/{appsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/accounts/{accountsId}/apps/{appsId}
/// Gets an app.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `checks_accounts_apps_get_execute()` or `checks_accounts_apps_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `checks_accounts_apps_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn checks_accounts_apps_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleChecksAccountV1alphaApp>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChecksAccountV1alphaApp = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/accounts/{accountsId}/apps/{appsId}
/// Gets an app.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `checks_accounts_apps_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `checks_accounts_apps_get_task()`.
/// For the simplest API, use `checks_accounts_apps_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `checks_accounts_apps_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn checks_accounts_apps_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChecksAccountV1alphaApp>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = checks_accounts_apps_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`checks_accounts_apps_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChecksAccountsAppsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha/accounts/{accountsId}/apps/{appsId}
/// Gets an app.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `checks_accounts_apps_get_builder()` + `checks_accounts_apps_get_execute()`.
/// For task-level control, use `checks_accounts_apps_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn checks_accounts_apps_get(
    client: &SimpleHttpClient,
    args: &ChecksAccountsAppsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChecksAccountV1alphaApp>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = checks_accounts_apps_get_builder(client, &args.name)?;
    checks_accounts_apps_get_execute(builder)
}

/// GET v1alpha/accounts/{accountsId}/apps
/// Lists the apps under the given account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `checks_accounts_apps_list_execute()` to send, or `checks_accounts_apps_list` for simplest API.

pub fn checks_accounts_apps_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://checks.googleapis.com/v1alpha/accounts/{}/apps",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/accounts/{accountsId}/apps
/// Lists the apps under the given account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `checks_accounts_apps_list_execute()` or `checks_accounts_apps_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `checks_accounts_apps_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn checks_accounts_apps_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleChecksAccountV1alphaListAppsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChecksAccountV1alphaListAppsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/accounts/{accountsId}/apps
/// Lists the apps under the given account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `checks_accounts_apps_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `checks_accounts_apps_list_task()`.
/// For the simplest API, use `checks_accounts_apps_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `checks_accounts_apps_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn checks_accounts_apps_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChecksAccountV1alphaListAppsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = checks_accounts_apps_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`checks_accounts_apps_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChecksAccountsAppsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1alpha/accounts/{accountsId}/apps
/// Lists the apps under the given account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `checks_accounts_apps_list_builder()` + `checks_accounts_apps_list_execute()`.
/// For task-level control, use `checks_accounts_apps_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn checks_accounts_apps_list(
    client: &SimpleHttpClient,
    args: &ChecksAccountsAppsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChecksAccountV1alphaListAppsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        checks_accounts_apps_list_builder(client, &args.parent, &args.pageSize, &args.pageToken)?;
    checks_accounts_apps_list_execute(builder)
}

/// POST v1alpha/accounts/{accountsId}/apps/{appsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `checks_accounts_apps_operations_cancel_execute()` to send, or `checks_accounts_apps_operations_cancel` for simplest API.

pub fn checks_accounts_apps_operations_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://checks.googleapis.com/v1alpha/accounts/{}/apps/{appsId}/operations/{operationsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha/accounts/{accountsId}/apps/{appsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `checks_accounts_apps_operations_cancel_execute()` or `checks_accounts_apps_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `checks_accounts_apps_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn checks_accounts_apps_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha/accounts/{accountsId}/apps/{appsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `checks_accounts_apps_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `checks_accounts_apps_operations_cancel_task()`.
/// For the simplest API, use `checks_accounts_apps_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `checks_accounts_apps_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn checks_accounts_apps_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = checks_accounts_apps_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`checks_accounts_apps_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChecksAccountsAppsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1alpha/accounts/{accountsId}/apps/{appsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `checks_accounts_apps_operations_cancel_builder()` + `checks_accounts_apps_operations_cancel_execute()`.
/// For task-level control, use `checks_accounts_apps_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn checks_accounts_apps_operations_cancel(
    client: &SimpleHttpClient,
    args: &ChecksAccountsAppsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = checks_accounts_apps_operations_cancel_builder(client, &args.name)?;
    checks_accounts_apps_operations_cancel_execute(builder)
}

/// DELETE v1alpha/accounts/{accountsId}/apps/{appsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `checks_accounts_apps_operations_delete_execute()` to send, or `checks_accounts_apps_operations_delete` for simplest API.

pub fn checks_accounts_apps_operations_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://checks.googleapis.com/v1alpha/accounts/{}/apps/{appsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1alpha/accounts/{accountsId}/apps/{appsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `checks_accounts_apps_operations_delete_execute()` or `checks_accounts_apps_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `checks_accounts_apps_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn checks_accounts_apps_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1alpha/accounts/{accountsId}/apps/{appsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `checks_accounts_apps_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `checks_accounts_apps_operations_delete_task()`.
/// For the simplest API, use `checks_accounts_apps_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `checks_accounts_apps_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn checks_accounts_apps_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = checks_accounts_apps_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`checks_accounts_apps_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChecksAccountsAppsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1alpha/accounts/{accountsId}/apps/{appsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `checks_accounts_apps_operations_delete_builder()` + `checks_accounts_apps_operations_delete_execute()`.
/// For task-level control, use `checks_accounts_apps_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn checks_accounts_apps_operations_delete(
    client: &SimpleHttpClient,
    args: &ChecksAccountsAppsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = checks_accounts_apps_operations_delete_builder(client, &args.name)?;
    checks_accounts_apps_operations_delete_execute(builder)
}

/// GET v1alpha/accounts/{accountsId}/apps/{appsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `checks_accounts_apps_operations_get_execute()` to send, or `checks_accounts_apps_operations_get` for simplest API.

pub fn checks_accounts_apps_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://checks.googleapis.com/v1alpha/accounts/{}/apps/{appsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/accounts/{accountsId}/apps/{appsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `checks_accounts_apps_operations_get_execute()` or `checks_accounts_apps_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `checks_accounts_apps_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn checks_accounts_apps_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/accounts/{accountsId}/apps/{appsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `checks_accounts_apps_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `checks_accounts_apps_operations_get_task()`.
/// For the simplest API, use `checks_accounts_apps_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `checks_accounts_apps_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn checks_accounts_apps_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = checks_accounts_apps_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`checks_accounts_apps_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChecksAccountsAppsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha/accounts/{accountsId}/apps/{appsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `checks_accounts_apps_operations_get_builder()` + `checks_accounts_apps_operations_get_execute()`.
/// For task-level control, use `checks_accounts_apps_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn checks_accounts_apps_operations_get(
    client: &SimpleHttpClient,
    args: &ChecksAccountsAppsOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = checks_accounts_apps_operations_get_builder(client, &args.name)?;
    checks_accounts_apps_operations_get_execute(builder)
}

/// GET v1alpha/accounts/{accountsId}/apps/{appsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `checks_accounts_apps_operations_list_execute()` to send, or `checks_accounts_apps_operations_list` for simplest API.

pub fn checks_accounts_apps_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    returnPartialSuccess: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://checks.googleapis.com/v1alpha/accounts/{}/apps/{appsId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/accounts/{accountsId}/apps/{appsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `checks_accounts_apps_operations_list_execute()` or `checks_accounts_apps_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `checks_accounts_apps_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn checks_accounts_apps_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListOperationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/accounts/{accountsId}/apps/{appsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `checks_accounts_apps_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `checks_accounts_apps_operations_list_task()`.
/// For the simplest API, use `checks_accounts_apps_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `checks_accounts_apps_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn checks_accounts_apps_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = checks_accounts_apps_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`checks_accounts_apps_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChecksAccountsAppsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<String>,
}

/// GET v1alpha/accounts/{accountsId}/apps/{appsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `checks_accounts_apps_operations_list_builder()` + `checks_accounts_apps_operations_list_execute()`.
/// For task-level control, use `checks_accounts_apps_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn checks_accounts_apps_operations_list(
    client: &SimpleHttpClient,
    args: &ChecksAccountsAppsOperationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListOperationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = checks_accounts_apps_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    checks_accounts_apps_operations_list_execute(builder)
}

/// POST v1alpha/accounts/{accountsId}/apps/{appsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `checks_accounts_apps_operations_wait_execute()` to send, or `checks_accounts_apps_operations_wait` for simplest API.

pub fn checks_accounts_apps_operations_wait_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://checks.googleapis.com/v1alpha/accounts/{}/apps/{appsId}/operations/{operationsId}:wait",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha/accounts/{accountsId}/apps/{appsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `checks_accounts_apps_operations_wait_execute()` or `checks_accounts_apps_operations_wait`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `checks_accounts_apps_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn checks_accounts_apps_operations_wait_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha/accounts/{accountsId}/apps/{appsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `checks_accounts_apps_operations_wait_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `checks_accounts_apps_operations_wait_task()`.
/// For the simplest API, use `checks_accounts_apps_operations_wait()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `checks_accounts_apps_operations_wait_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn checks_accounts_apps_operations_wait_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = checks_accounts_apps_operations_wait_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`checks_accounts_apps_operations_wait`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChecksAccountsAppsOperationsWaitArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1alpha/accounts/{accountsId}/apps/{appsId}/operations/{operationsId}:wait
/// Waits until the specified long-running operation is done or reaches at most a specified timeout, returning the latest state. If the operation is already done, the latest state is immediately returned. If the timeout specified is greater than the default HTTP/RPC timeout, the HTTP/RPC timeout is used. If the server does not support this method, it returns google.rpc.Code.UNIMPLEMENTED. Note that this method is on a best-effort basis. It may return the latest state before the specified timeout (including immediately), meaning even an immediate response is no guarantee that the operation is done.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `checks_accounts_apps_operations_wait_builder()` + `checks_accounts_apps_operations_wait_execute()`.
/// For task-level control, use `checks_accounts_apps_operations_wait_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn checks_accounts_apps_operations_wait(
    client: &SimpleHttpClient,
    args: &ChecksAccountsAppsOperationsWaitArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = checks_accounts_apps_operations_wait_builder(client, &args.name)?;
    checks_accounts_apps_operations_wait_execute(builder)
}

/// GET v1alpha/accounts/{accountsId}/apps/{appsId}/reports/{reportsId}
/// Gets a report. By default, only the name and results_uri fields are returned. You can include other fields by listing them in the fields URL query parameter. For example, ?fields=name,checks will return the name and checks fields.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `checks_accounts_apps_reports_get_execute()` to send, or `checks_accounts_apps_reports_get` for simplest API.

pub fn checks_accounts_apps_reports_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    checksFilter: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://checks.googleapis.com/v1alpha/accounts/{}/apps/{appsId}/reports/{reportsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = checksFilter.as_ref() {
        query_parts.push(format!("checksFilter={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/accounts/{accountsId}/apps/{appsId}/reports/{reportsId}
/// Gets a report. By default, only the name and results_uri fields are returned. You can include other fields by listing them in the fields URL query parameter. For example, ?fields=name,checks will return the name and checks fields.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `checks_accounts_apps_reports_get_execute()` or `checks_accounts_apps_reports_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `checks_accounts_apps_reports_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn checks_accounts_apps_reports_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleChecksReportV1alphaReport>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChecksReportV1alphaReport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/accounts/{accountsId}/apps/{appsId}/reports/{reportsId}
/// Gets a report. By default, only the name and results_uri fields are returned. You can include other fields by listing them in the fields URL query parameter. For example, ?fields=name,checks will return the name and checks fields.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `checks_accounts_apps_reports_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `checks_accounts_apps_reports_get_task()`.
/// For the simplest API, use `checks_accounts_apps_reports_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `checks_accounts_apps_reports_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn checks_accounts_apps_reports_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChecksReportV1alphaReport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = checks_accounts_apps_reports_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`checks_accounts_apps_reports_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChecksAccountsAppsReportsGetArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: checksFilter
    pub checksFilter: Option<String>,
}

/// GET v1alpha/accounts/{accountsId}/apps/{appsId}/reports/{reportsId}
/// Gets a report. By default, only the name and results_uri fields are returned. You can include other fields by listing them in the fields URL query parameter. For example, ?fields=name,checks will return the name and checks fields.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `checks_accounts_apps_reports_get_builder()` + `checks_accounts_apps_reports_get_execute()`.
/// For task-level control, use `checks_accounts_apps_reports_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn checks_accounts_apps_reports_get(
    client: &SimpleHttpClient,
    args: &ChecksAccountsAppsReportsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChecksReportV1alphaReport>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = checks_accounts_apps_reports_get_builder(client, &args.name, &args.checksFilter)?;
    checks_accounts_apps_reports_get_execute(builder)
}

/// GET v1alpha/accounts/{accountsId}/apps/{appsId}/reports
/// Lists reports for the specified app. By default, only the name and results_uri fields are returned. You can include other fields by listing them in the fields URL query parameter. For example, ?fields=reports(name,checks) will return the name and checks fields.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `checks_accounts_apps_reports_list_execute()` to send, or `checks_accounts_apps_reports_list` for simplest API.

pub fn checks_accounts_apps_reports_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    checksFilter: &Option<String>,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://checks.googleapis.com/v1alpha/accounts/{}/apps/{appsId}/reports",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = checksFilter.as_ref() {
        query_parts.push(format!("checksFilter={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/accounts/{accountsId}/apps/{appsId}/reports
/// Lists reports for the specified app. By default, only the name and results_uri fields are returned. You can include other fields by listing them in the fields URL query parameter. For example, ?fields=reports(name,checks) will return the name and checks fields.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `checks_accounts_apps_reports_list_execute()` or `checks_accounts_apps_reports_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `checks_accounts_apps_reports_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn checks_accounts_apps_reports_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleChecksReportV1alphaListReportsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChecksReportV1alphaListReportsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/accounts/{accountsId}/apps/{appsId}/reports
/// Lists reports for the specified app. By default, only the name and results_uri fields are returned. You can include other fields by listing them in the fields URL query parameter. For example, ?fields=reports(name,checks) will return the name and checks fields.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `checks_accounts_apps_reports_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `checks_accounts_apps_reports_list_task()`.
/// For the simplest API, use `checks_accounts_apps_reports_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `checks_accounts_apps_reports_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn checks_accounts_apps_reports_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChecksReportV1alphaListReportsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = checks_accounts_apps_reports_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`checks_accounts_apps_reports_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChecksAccountsAppsReportsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: checksFilter
    pub checksFilter: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1alpha/accounts/{accountsId}/apps/{appsId}/reports
/// Lists reports for the specified app. By default, only the name and results_uri fields are returned. You can include other fields by listing them in the fields URL query parameter. For example, ?fields=reports(name,checks) will return the name and checks fields.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `checks_accounts_apps_reports_list_builder()` + `checks_accounts_apps_reports_list_execute()`.
/// For task-level control, use `checks_accounts_apps_reports_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn checks_accounts_apps_reports_list(
    client: &SimpleHttpClient,
    args: &ChecksAccountsAppsReportsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChecksReportV1alphaListReportsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = checks_accounts_apps_reports_list_builder(
        client,
        &args.parent,
        &args.checksFilter,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    checks_accounts_apps_reports_list_execute(builder)
}

/// GET v1alpha/accounts/{accountsId}/repos/{reposId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `checks_accounts_repos_operations_get_execute()` to send, or `checks_accounts_repos_operations_get` for simplest API.

pub fn checks_accounts_repos_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://checks.googleapis.com/v1alpha/accounts/{}/repos/{reposId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/accounts/{accountsId}/repos/{reposId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `checks_accounts_repos_operations_get_execute()` or `checks_accounts_repos_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `checks_accounts_repos_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn checks_accounts_repos_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/accounts/{accountsId}/repos/{reposId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `checks_accounts_repos_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `checks_accounts_repos_operations_get_task()`.
/// For the simplest API, use `checks_accounts_repos_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `checks_accounts_repos_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn checks_accounts_repos_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = checks_accounts_repos_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`checks_accounts_repos_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChecksAccountsReposOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha/accounts/{accountsId}/repos/{reposId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `checks_accounts_repos_operations_get_builder()` + `checks_accounts_repos_operations_get_execute()`.
/// For task-level control, use `checks_accounts_repos_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn checks_accounts_repos_operations_get(
    client: &SimpleHttpClient,
    args: &ChecksAccountsReposOperationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = checks_accounts_repos_operations_get_builder(client, &args.name)?;
    checks_accounts_repos_operations_get_execute(builder)
}

/// POST v1alpha/accounts/{accountsId}/repos/{reposId}/scans:generate
/// Uploads the results of local Code Compliance analysis and generates a scan of privacy issues. Returns a google.longrunning.Operation containing analysis and findings.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `checks_accounts_repos_scans_generate_execute()` to send, or `checks_accounts_repos_scans_generate` for simplest API.

pub fn checks_accounts_repos_scans_generate_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://checks.googleapis.com/v1alpha/accounts/{}/repos/{reposId}/scans:generate",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha/accounts/{accountsId}/repos/{reposId}/scans:generate
/// Uploads the results of local Code Compliance analysis and generates a scan of privacy issues. Returns a google.longrunning.Operation containing analysis and findings.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `checks_accounts_repos_scans_generate_execute()` or `checks_accounts_repos_scans_generate`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `checks_accounts_repos_scans_generate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn checks_accounts_repos_scans_generate_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha/accounts/{accountsId}/repos/{reposId}/scans:generate
/// Uploads the results of local Code Compliance analysis and generates a scan of privacy issues. Returns a google.longrunning.Operation containing analysis and findings.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `checks_accounts_repos_scans_generate_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `checks_accounts_repos_scans_generate_task()`.
/// For the simplest API, use `checks_accounts_repos_scans_generate()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `checks_accounts_repos_scans_generate_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn checks_accounts_repos_scans_generate_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = checks_accounts_repos_scans_generate_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`checks_accounts_repos_scans_generate`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChecksAccountsReposScansGenerateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1alpha/accounts/{accountsId}/repos/{reposId}/scans:generate
/// Uploads the results of local Code Compliance analysis and generates a scan of privacy issues. Returns a google.longrunning.Operation containing analysis and findings.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `checks_accounts_repos_scans_generate_builder()` + `checks_accounts_repos_scans_generate_execute()`.
/// For task-level control, use `checks_accounts_repos_scans_generate_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn checks_accounts_repos_scans_generate(
    client: &SimpleHttpClient,
    args: &ChecksAccountsReposScansGenerateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = checks_accounts_repos_scans_generate_builder(client, &args.parent)?;
    checks_accounts_repos_scans_generate_execute(builder)
}

/// GET v1alpha/accounts/{accountsId}/repos/{reposId}/scans/{scansId}
/// Gets a repo scan. By default, only the name and results_uri fields are returned. You can include other fields by listing them in the fields URL query parameter. For example, ?fields=name,sources will return the name and sources fields.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `checks_accounts_repos_scans_get_execute()` to send, or `checks_accounts_repos_scans_get` for simplest API.

pub fn checks_accounts_repos_scans_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://checks.googleapis.com/v1alpha/accounts/{}/repos/{reposId}/scans/{scansId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/accounts/{accountsId}/repos/{reposId}/scans/{scansId}
/// Gets a repo scan. By default, only the name and results_uri fields are returned. You can include other fields by listing them in the fields URL query parameter. For example, ?fields=name,sources will return the name and sources fields.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `checks_accounts_repos_scans_get_execute()` or `checks_accounts_repos_scans_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `checks_accounts_repos_scans_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn checks_accounts_repos_scans_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleChecksRepoScanV1alphaRepoScan>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChecksRepoScanV1alphaRepoScan = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/accounts/{accountsId}/repos/{reposId}/scans/{scansId}
/// Gets a repo scan. By default, only the name and results_uri fields are returned. You can include other fields by listing them in the fields URL query parameter. For example, ?fields=name,sources will return the name and sources fields.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `checks_accounts_repos_scans_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `checks_accounts_repos_scans_get_task()`.
/// For the simplest API, use `checks_accounts_repos_scans_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `checks_accounts_repos_scans_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn checks_accounts_repos_scans_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChecksRepoScanV1alphaRepoScan>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = checks_accounts_repos_scans_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`checks_accounts_repos_scans_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChecksAccountsReposScansGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1alpha/accounts/{accountsId}/repos/{reposId}/scans/{scansId}
/// Gets a repo scan. By default, only the name and results_uri fields are returned. You can include other fields by listing them in the fields URL query parameter. For example, ?fields=name,sources will return the name and sources fields.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `checks_accounts_repos_scans_get_builder()` + `checks_accounts_repos_scans_get_execute()`.
/// For task-level control, use `checks_accounts_repos_scans_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn checks_accounts_repos_scans_get(
    client: &SimpleHttpClient,
    args: &ChecksAccountsReposScansGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChecksRepoScanV1alphaRepoScan>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = checks_accounts_repos_scans_get_builder(client, &args.name)?;
    checks_accounts_repos_scans_get_execute(builder)
}

/// GET v1alpha/accounts/{accountsId}/repos/{reposId}/scans
/// Lists repo scans for the specified repo.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `checks_accounts_repos_scans_list_execute()` to send, or `checks_accounts_repos_scans_list` for simplest API.

pub fn checks_accounts_repos_scans_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://checks.googleapis.com/v1alpha/accounts/{}/repos/{reposId}/scans",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/accounts/{accountsId}/repos/{reposId}/scans
/// Lists repo scans for the specified repo.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `checks_accounts_repos_scans_list_execute()` or `checks_accounts_repos_scans_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `checks_accounts_repos_scans_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn checks_accounts_repos_scans_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleChecksRepoScanV1alphaListRepoScansResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChecksRepoScanV1alphaListRepoScansResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/accounts/{accountsId}/repos/{reposId}/scans
/// Lists repo scans for the specified repo.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `checks_accounts_repos_scans_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `checks_accounts_repos_scans_list_task()`.
/// For the simplest API, use `checks_accounts_repos_scans_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `checks_accounts_repos_scans_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn checks_accounts_repos_scans_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChecksRepoScanV1alphaListRepoScansResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = checks_accounts_repos_scans_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`checks_accounts_repos_scans_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChecksAccountsReposScansListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1alpha/accounts/{accountsId}/repos/{reposId}/scans
/// Lists repo scans for the specified repo.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `checks_accounts_repos_scans_list_builder()` + `checks_accounts_repos_scans_list_execute()`.
/// For task-level control, use `checks_accounts_repos_scans_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn checks_accounts_repos_scans_list(
    client: &SimpleHttpClient,
    args: &ChecksAccountsReposScansListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChecksRepoScanV1alphaListRepoScansResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = checks_accounts_repos_scans_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    checks_accounts_repos_scans_list_execute(builder)
}

/// POST v1alpha/aisafety:classifyContent
/// Analyze a piece of content with the provided set of policies.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `checks_aisafety_classify_content_execute()` to send, or `checks_aisafety_classify_content` for simplest API.

pub fn checks_aisafety_classify_content_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://checks.googleapis.com/v1alpha/aisafety:classifyContent",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha/aisafety:classifyContent
/// Analyze a piece of content with the provided set of policies.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `checks_aisafety_classify_content_execute()` or `checks_aisafety_classify_content`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `checks_aisafety_classify_content_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn checks_aisafety_classify_content_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChecksAisafetyV1alphaClassifyContentResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChecksAisafetyV1alphaClassifyContentResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha/aisafety:classifyContent
/// Analyze a piece of content with the provided set of policies.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `checks_aisafety_classify_content_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `checks_aisafety_classify_content_task()`.
/// For the simplest API, use `checks_aisafety_classify_content()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `checks_aisafety_classify_content_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn checks_aisafety_classify_content_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChecksAisafetyV1alphaClassifyContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = checks_aisafety_classify_content_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1alpha/aisafety:classifyContent
/// Analyze a piece of content with the provided set of policies.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `checks_aisafety_classify_content_builder()` + `checks_aisafety_classify_content_execute()`.
/// For task-level control, use `checks_aisafety_classify_content_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn checks_aisafety_classify_content(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChecksAisafetyV1alphaClassifyContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = checks_aisafety_classify_content_builder(client)?;
    checks_aisafety_classify_content_execute(builder)
}

/// POST v1alpha/accounts/{accountsId}/apps/{appsId}/reports:analyzeUpload
/// Analyzes the uploaded app bundle and returns a google.longrunning.Operation containing the generated Report. ## Example (upload only) Send a regular POST request with the header X-Goog-Upload-Protocol: raw.  POST <https://checks.googleapis.`com/upload/v1alpha/{parent`=accounts/*/apps/*}/reports:`analyzeUpload`> HTTP/1.1 X-Goog-Upload-Protocol: raw Content-Length: Content-Type: `application/octet-stream`  ## Example (upload with metadata) Send a multipart POST request where the first body part contains the metadata JSON and the second body part contains the binary upload. Include the header X-Goog-Upload-Protocol: multipart.  POST <https://checks.googleapis.`com/upload/v1alpha/{parent`=accounts/*/apps/*}/reports:`analyzeUpload`> HTTP/1.1 X-Goog-Upload-Protocol: multipart Content-Length: ? Content-Type: `multipart/related`; boundary=BOUNDARY --BOUNDARY Content-Type: `application/json` {"code_reference_id":"db5bcc20f94055fb5bc08cbb9b0e7a5530308786"} --BOUNDARY --BOUNDARY--  *Note:* Metadata-only requests are not supported.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `checks_media_upload_execute()` to send, or `checks_media_upload` for simplest API.

pub fn checks_media_upload_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://checks.googleapis.com/v1alpha/accounts/{}/apps/{appsId}/reports:analyzeUpload",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1alpha/accounts/{accountsId}/apps/{appsId}/reports:analyzeUpload
/// Analyzes the uploaded app bundle and returns a google.longrunning.Operation containing the generated Report. ## Example (upload only) Send a regular POST request with the header X-Goog-Upload-Protocol: raw.  POST <https://checks.googleapis.`com/upload/v1alpha/{parent`=accounts/*/apps/*}/reports:`analyzeUpload`> HTTP/1.1 X-Goog-Upload-Protocol: raw Content-Length: Content-Type: `application/octet-stream`  ## Example (upload with metadata) Send a multipart POST request where the first body part contains the metadata JSON and the second body part contains the binary upload. Include the header X-Goog-Upload-Protocol: multipart.  POST <https://checks.googleapis.`com/upload/v1alpha/{parent`=accounts/*/apps/*}/reports:`analyzeUpload`> HTTP/1.1 X-Goog-Upload-Protocol: multipart Content-Length: ? Content-Type: `multipart/related`; boundary=BOUNDARY --BOUNDARY Content-Type: `application/json` {"code_reference_id":"db5bcc20f94055fb5bc08cbb9b0e7a5530308786"} --BOUNDARY --BOUNDARY--  *Note:* Metadata-only requests are not supported.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `checks_media_upload_execute()` or `checks_media_upload`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `checks_media_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn checks_media_upload_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Operation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1alpha/accounts/{accountsId}/apps/{appsId}/reports:analyzeUpload
/// Analyzes the uploaded app bundle and returns a google.longrunning.Operation containing the generated Report. ## Example (upload only) Send a regular POST request with the header X-Goog-Upload-Protocol: raw.  POST <https://checks.googleapis.`com/upload/v1alpha/{parent`=accounts/*/apps/*}/reports:`analyzeUpload`> HTTP/1.1 X-Goog-Upload-Protocol: raw Content-Length: Content-Type: `application/octet-stream`  ## Example (upload with metadata) Send a multipart POST request where the first body part contains the metadata JSON and the second body part contains the binary upload. Include the header X-Goog-Upload-Protocol: multipart.  POST <https://checks.googleapis.`com/upload/v1alpha/{parent`=accounts/*/apps/*}/reports:`analyzeUpload`> HTTP/1.1 X-Goog-Upload-Protocol: multipart Content-Length: ? Content-Type: `multipart/related`; boundary=BOUNDARY --BOUNDARY Content-Type: `application/json` {"code_reference_id":"db5bcc20f94055fb5bc08cbb9b0e7a5530308786"} --BOUNDARY --BOUNDARY--  *Note:* Metadata-only requests are not supported.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `checks_media_upload_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `checks_media_upload_task()`.
/// For the simplest API, use `checks_media_upload()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `checks_media_upload_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn checks_media_upload_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = checks_media_upload_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`checks_media_upload`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChecksMediaUploadArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v1alpha/accounts/{accountsId}/apps/{appsId}/reports:analyzeUpload
/// Analyzes the uploaded app bundle and returns a google.longrunning.Operation containing the generated Report. ## Example (upload only) Send a regular POST request with the header X-Goog-Upload-Protocol: raw.  POST <https://checks.googleapis.`com/upload/v1alpha/{parent`=accounts/*/apps/*}/reports:`analyzeUpload`> HTTP/1.1 X-Goog-Upload-Protocol: raw Content-Length: Content-Type: `application/octet-stream`  ## Example (upload with metadata) Send a multipart POST request where the first body part contains the metadata JSON and the second body part contains the binary upload. Include the header X-Goog-Upload-Protocol: multipart.  POST <https://checks.googleapis.`com/upload/v1alpha/{parent`=accounts/*/apps/*}/reports:`analyzeUpload`> HTTP/1.1 X-Goog-Upload-Protocol: multipart Content-Length: ? Content-Type: `multipart/related`; boundary=BOUNDARY --BOUNDARY Content-Type: `application/json` {"code_reference_id":"db5bcc20f94055fb5bc08cbb9b0e7a5530308786"} --BOUNDARY --BOUNDARY--  *Note:* Metadata-only requests are not supported.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `checks_media_upload_builder()` + `checks_media_upload_execute()`.
/// For task-level control, use `checks_media_upload_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn checks_media_upload(
    client: &SimpleHttpClient,
    args: &ChecksMediaUploadArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Operation>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = checks_media_upload_builder(client, &args.parent)?;
    checks_media_upload_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for GoogleChecksAccountV1alphaApp
// =============================================================================

/// ResourceIdentifier implementation for GoogleChecksAccountV1alphaApp with ChecksAccountsAppsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ChecksAccountsAppsGetArgs> for GoogleChecksAccountV1alphaApp {
    fn generate_resource_id(&self, input: &ChecksAccountsAppsGetArgs) -> String {
        format!("gcp::checks::GoogleChecksAccountV1alphaApp/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::checks::GoogleChecksAccountV1alphaApp"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleChecksAccountV1alphaListAppsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleChecksAccountV1alphaListAppsResponse with ChecksAccountsAppsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ChecksAccountsAppsListArgs> for GoogleChecksAccountV1alphaListAppsResponse {
    fn generate_resource_id(&self, input: &ChecksAccountsAppsListArgs) -> String {
        format!(
            "gcp::checks::GoogleChecksAccountV1alphaListAppsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::checks::GoogleChecksAccountV1alphaListAppsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with ChecksAccountsAppsOperationsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ChecksAccountsAppsOperationsCancelArgs> for Empty {
    fn generate_resource_id(&self, input: &ChecksAccountsAppsOperationsCancelArgs) -> String {
        format!("gcp::checks::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::checks::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with ChecksAccountsAppsOperationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ChecksAccountsAppsOperationsDeleteArgs> for Empty {
    fn generate_resource_id(&self, input: &ChecksAccountsAppsOperationsDeleteArgs) -> String {
        format!("gcp::checks::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::checks::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ChecksAccountsAppsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ChecksAccountsAppsOperationsGetArgs> for Operation {
    fn generate_resource_id(&self, input: &ChecksAccountsAppsOperationsGetArgs) -> String {
        format!("gcp::checks::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::checks::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListOperationsResponse with ChecksAccountsAppsOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ChecksAccountsAppsOperationsListArgs> for ListOperationsResponse {
    fn generate_resource_id(&self, input: &ChecksAccountsAppsOperationsListArgs) -> String {
        format!("gcp::checks::ListOperationsResponse/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::checks::ListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ChecksAccountsAppsOperationsWaitArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ChecksAccountsAppsOperationsWaitArgs> for Operation {
    fn generate_resource_id(&self, input: &ChecksAccountsAppsOperationsWaitArgs) -> String {
        format!("gcp::checks::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::checks::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleChecksReportV1alphaReport
// =============================================================================

/// ResourceIdentifier implementation for GoogleChecksReportV1alphaReport with ChecksAccountsAppsReportsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ChecksAccountsAppsReportsGetArgs> for GoogleChecksReportV1alphaReport {
    fn generate_resource_id(&self, input: &ChecksAccountsAppsReportsGetArgs) -> String {
        format!(
            "gcp::checks::GoogleChecksReportV1alphaReport/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::checks::GoogleChecksReportV1alphaReport"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleChecksReportV1alphaListReportsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleChecksReportV1alphaListReportsResponse with ChecksAccountsAppsReportsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ChecksAccountsAppsReportsListArgs>
    for GoogleChecksReportV1alphaListReportsResponse
{
    fn generate_resource_id(&self, input: &ChecksAccountsAppsReportsListArgs) -> String {
        format!(
            "gcp::checks::GoogleChecksReportV1alphaListReportsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::checks::GoogleChecksReportV1alphaListReportsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ChecksAccountsReposOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ChecksAccountsReposOperationsGetArgs> for Operation {
    fn generate_resource_id(&self, input: &ChecksAccountsReposOperationsGetArgs) -> String {
        format!("gcp::checks::Operation/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::checks::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ChecksAccountsReposScansGenerateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ChecksAccountsReposScansGenerateArgs> for Operation {
    fn generate_resource_id(&self, input: &ChecksAccountsReposScansGenerateArgs) -> String {
        format!("gcp::checks::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::checks::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleChecksRepoScanV1alphaRepoScan
// =============================================================================

/// ResourceIdentifier implementation for GoogleChecksRepoScanV1alphaRepoScan with ChecksAccountsReposScansGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ChecksAccountsReposScansGetArgs> for GoogleChecksRepoScanV1alphaRepoScan {
    fn generate_resource_id(&self, input: &ChecksAccountsReposScansGetArgs) -> String {
        format!(
            "gcp::checks::GoogleChecksRepoScanV1alphaRepoScan/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::checks::GoogleChecksRepoScanV1alphaRepoScan"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleChecksRepoScanV1alphaListRepoScansResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleChecksRepoScanV1alphaListRepoScansResponse with ChecksAccountsReposScansListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ChecksAccountsReposScansListArgs>
    for GoogleChecksRepoScanV1alphaListRepoScansResponse
{
    fn generate_resource_id(&self, input: &ChecksAccountsReposScansListArgs) -> String {
        format!(
            "gcp::checks::GoogleChecksRepoScanV1alphaListRepoScansResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::checks::GoogleChecksRepoScanV1alphaListRepoScansResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Operation
// =============================================================================

/// ResourceIdentifier implementation for Operation with ChecksMediaUploadArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<ChecksMediaUploadArgs> for Operation {
    fn generate_resource_id(&self, input: &ChecksMediaUploadArgs) -> String {
        format!("gcp::checks::Operation/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::checks::Operation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
