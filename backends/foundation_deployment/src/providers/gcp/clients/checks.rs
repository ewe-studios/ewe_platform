//! Auto-generated API clients for gcp/checks.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1alpha/accounts/{accountsId}/apps
/// Lists the apps under the given account.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `checks_accounts_apps_list_execute()` to send, or `checks_accounts_apps_list` for simplest API.

pub fn checks_accounts_apps_list_builder(
    client: &SimpleHttpClient,
    parent: String,
    pageSize: Option<i32>,
    pageToken: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://checks.googleapis.com/v1alpha/accounts/{}/apps",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1alpha/accounts/{accountsId}/apps
/// Lists the apps under the given account.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `checks_accounts_apps_list_execute()` or `checks_accounts_apps_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `checks_accounts_apps_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn checks_accounts_apps_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleChecksAccountV1alphaListAppsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChecksAccountV1alphaListAppsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/accounts/{accountsId}/apps
/// Lists the apps under the given account.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `checks_accounts_apps_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `checks_accounts_apps_list_task()`.
/// For the simplest API, use `checks_accounts_apps_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `checks_accounts_apps_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn checks_accounts_apps_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChecksAccountV1alphaListAppsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = checks_accounts_apps_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`checks_accounts_apps_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChecksAccountsAppsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<i32>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1alpha/accounts/{accountsId}/apps
/// Lists the apps under the given account.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `checks_accounts_apps_list_builder()` + `checks_accounts_apps_list_execute()`.
/// For task-level control, use `checks_accounts_apps_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn checks_accounts_apps_list(
    client: &SimpleHttpClient,
    args: &ChecksAccountsAppsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChecksAccountV1alphaListAppsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = checks_accounts_apps_list_builder(
        client,
        args.parent.clone(),
        args.pageSize.clone(),
        args.pageToken.clone(),
    )?;
    checks_accounts_apps_list_execute(builder)
}

/// GET v1alpha/aisafety:classifyContent
/// Analyze a piece of content with the provided set of policies.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `checks_aisafety_classify_content_execute()` to send, or `checks_aisafety_classify_content` for simplest API.

pub fn checks_aisafety_classify_content_builder(
    client: &SimpleHttpClient,
    body: &GoogleChecksAisafetyV1alphaClassifyContentRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://checks.googleapis.com/v1alpha/aisafety:classifyContent",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1alpha/aisafety:classifyContent
/// Analyze a piece of content with the provided set of policies.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `checks_aisafety_classify_content_execute()` or `checks_aisafety_classify_content`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `checks_aisafety_classify_content_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn checks_aisafety_classify_content_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleChecksAisafetyV1alphaClassifyContentResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleChecksAisafetyV1alphaClassifyContentResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1alpha/aisafety:classifyContent
/// Analyze a piece of content with the provided set of policies.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `checks_aisafety_classify_content_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `checks_aisafety_classify_content_task()`.
/// For the simplest API, use `checks_aisafety_classify_content()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `checks_aisafety_classify_content_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn checks_aisafety_classify_content_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChecksAisafetyV1alphaClassifyContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = checks_aisafety_classify_content_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`checks_aisafety_classify_content`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ChecksAisafetyClassifyContentArgs {
    /// Request body.
    pub body: GoogleChecksAisafetyV1alphaClassifyContentRequest,
}

/// GET v1alpha/aisafety:classifyContent
/// Analyze a piece of content with the provided set of policies.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `checks_aisafety_classify_content_builder()` + `checks_aisafety_classify_content_execute()`.
/// For task-level control, use `checks_aisafety_classify_content_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn checks_aisafety_classify_content(
    client: &SimpleHttpClient,
    args: &ChecksAisafetyClassifyContentArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleChecksAisafetyV1alphaClassifyContentResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = checks_aisafety_classify_content_builder(client, &args.body)?;
    checks_aisafety_classify_content_execute(builder)
}
