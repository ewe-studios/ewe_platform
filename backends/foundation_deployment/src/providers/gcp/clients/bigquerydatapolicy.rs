//! Auto-generated API clients for gcp/bigquerydatapolicy.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST v2/projects/{projectsId}/locations/{locationsId}/dataPolicies/{dataPoliciesId}:addGrantees
/// Adds new grantees to a data policy. The new grantees will be added to the existing grantees. If the request contains a duplicate grantee, the grantee will be ignored. If the request contains a grantee that already exists, the grantee will be ignored.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatapolicy_projects_locations_data_policies_add_grantees_execute()` to send, or `bigquerydatapolicy_projects_locations_data_policies_add_grantees` for simplest API.

pub fn bigquerydatapolicy_projects_locations_data_policies_add_grantees_builder<R>(
    client: &SimpleHttpClient<R>,
    dataPolicy: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigquerydatapolicy.googleapis.com/v2/projects/{}/locations/{locationsId}/dataPolicies/{dataPoliciesId}:addGrantees",
        dataPolicy,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dataPolicies/{dataPoliciesId}:addGrantees
/// Adds new grantees to a data policy. The new grantees will be added to the existing grantees. If the request contains a duplicate grantee, the grantee will be ignored. If the request contains a grantee that already exists, the grantee will be ignored.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquerydatapolicy_projects_locations_data_policies_add_grantees_execute()` or `bigquerydatapolicy_projects_locations_data_policies_add_grantees`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquerydatapolicy_projects_locations_data_policies_add_grantees_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_add_grantees_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DataPolicy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DataPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dataPolicies/{dataPoliciesId}:addGrantees
/// Adds new grantees to a data policy. The new grantees will be added to the existing grantees. If the request contains a duplicate grantee, the grantee will be ignored. If the request contains a grantee that already exists, the grantee will be ignored.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquerydatapolicy_projects_locations_data_policies_add_grantees_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquerydatapolicy_projects_locations_data_policies_add_grantees_task()`.
/// For the simplest API, use `bigquerydatapolicy_projects_locations_data_policies_add_grantees()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquerydatapolicy_projects_locations_data_policies_add_grantees_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquerydatapolicy_projects_locations_data_policies_add_grantees_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigquerydatapolicy_projects_locations_data_policies_add_grantees_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquerydatapolicy_projects_locations_data_policies_add_grantees`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigquerydatapolicyProjectsLocationsDataPoliciesAddGranteesArgs {
    /// Path parameter: dataPolicy
    pub dataPolicy: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dataPolicies/{dataPoliciesId}:addGrantees
/// Adds new grantees to a data policy. The new grantees will be added to the existing grantees. If the request contains a duplicate grantee, the grantee will be ignored. If the request contains a grantee that already exists, the grantee will be ignored.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatapolicy_projects_locations_data_policies_add_grantees_builder()` + `bigquerydatapolicy_projects_locations_data_policies_add_grantees_execute()`.
/// For task-level control, use `bigquerydatapolicy_projects_locations_data_policies_add_grantees_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_add_grantees(
    client: &SimpleHttpClient,
    args: &BigquerydatapolicyProjectsLocationsDataPoliciesAddGranteesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigquerydatapolicy_projects_locations_data_policies_add_grantees_builder(
        client,
        &args.dataPolicy,
    )?;
    bigquerydatapolicy_projects_locations_data_policies_add_grantees_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dataPolicies
/// Creates a new data policy under a project with the given data_policy_id (used as the display name), and data policy type.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatapolicy_projects_locations_data_policies_create_execute()` to send, or `bigquerydatapolicy_projects_locations_data_policies_create` for simplest API.

pub fn bigquerydatapolicy_projects_locations_data_policies_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigquerydatapolicy.googleapis.com/v2/projects/{}/locations/{locationsId}/dataPolicies",
        parent,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dataPolicies
/// Creates a new data policy under a project with the given data_policy_id (used as the display name), and data policy type.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquerydatapolicy_projects_locations_data_policies_create_execute()` or `bigquerydatapolicy_projects_locations_data_policies_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquerydatapolicy_projects_locations_data_policies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DataPolicy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DataPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dataPolicies
/// Creates a new data policy under a project with the given data_policy_id (used as the display name), and data policy type.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquerydatapolicy_projects_locations_data_policies_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquerydatapolicy_projects_locations_data_policies_create_task()`.
/// For the simplest API, use `bigquerydatapolicy_projects_locations_data_policies_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquerydatapolicy_projects_locations_data_policies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquerydatapolicy_projects_locations_data_policies_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigquerydatapolicy_projects_locations_data_policies_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquerydatapolicy_projects_locations_data_policies_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigquerydatapolicyProjectsLocationsDataPoliciesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dataPolicies
/// Creates a new data policy under a project with the given data_policy_id (used as the display name), and data policy type.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatapolicy_projects_locations_data_policies_create_builder()` + `bigquerydatapolicy_projects_locations_data_policies_create_execute()`.
/// For task-level control, use `bigquerydatapolicy_projects_locations_data_policies_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_create(
    client: &SimpleHttpClient,
    args: &BigquerydatapolicyProjectsLocationsDataPoliciesCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        bigquerydatapolicy_projects_locations_data_policies_create_builder(client, &args.parent)?;
    bigquerydatapolicy_projects_locations_data_policies_create_execute(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/dataPolicies/{dataPoliciesId}
/// Deletes the data policy specified by its resource name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatapolicy_projects_locations_data_policies_delete_execute()` to send, or `bigquerydatapolicy_projects_locations_data_policies_delete` for simplest API.

pub fn bigquerydatapolicy_projects_locations_data_policies_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigquerydatapolicy.googleapis.com/v2/projects/{}/locations/{locationsId}/dataPolicies/{dataPoliciesId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/dataPolicies/{dataPoliciesId}
/// Deletes the data policy specified by its resource name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquerydatapolicy_projects_locations_data_policies_delete_execute()` or `bigquerydatapolicy_projects_locations_data_policies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquerydatapolicy_projects_locations_data_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/dataPolicies/{dataPoliciesId}
/// Deletes the data policy specified by its resource name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquerydatapolicy_projects_locations_data_policies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquerydatapolicy_projects_locations_data_policies_delete_task()`.
/// For the simplest API, use `bigquerydatapolicy_projects_locations_data_policies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquerydatapolicy_projects_locations_data_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquerydatapolicy_projects_locations_data_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigquerydatapolicy_projects_locations_data_policies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquerydatapolicy_projects_locations_data_policies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigquerydatapolicyProjectsLocationsDataPoliciesDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/dataPolicies/{dataPoliciesId}
/// Deletes the data policy specified by its resource name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatapolicy_projects_locations_data_policies_delete_builder()` + `bigquerydatapolicy_projects_locations_data_policies_delete_execute()`.
/// For task-level control, use `bigquerydatapolicy_projects_locations_data_policies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_delete(
    client: &SimpleHttpClient,
    args: &BigquerydatapolicyProjectsLocationsDataPoliciesDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        bigquerydatapolicy_projects_locations_data_policies_delete_builder(client, &args.name)?;
    bigquerydatapolicy_projects_locations_data_policies_delete_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/dataPolicies/{dataPoliciesId}
/// Gets the data policy specified by its resource name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatapolicy_projects_locations_data_policies_get_execute()` to send, or `bigquerydatapolicy_projects_locations_data_policies_get` for simplest API.

pub fn bigquerydatapolicy_projects_locations_data_policies_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigquerydatapolicy.googleapis.com/v2/projects/{}/locations/{locationsId}/dataPolicies/{dataPoliciesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/dataPolicies/{dataPoliciesId}
/// Gets the data policy specified by its resource name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquerydatapolicy_projects_locations_data_policies_get_execute()` or `bigquerydatapolicy_projects_locations_data_policies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquerydatapolicy_projects_locations_data_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DataPolicy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DataPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/dataPolicies/{dataPoliciesId}
/// Gets the data policy specified by its resource name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquerydatapolicy_projects_locations_data_policies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquerydatapolicy_projects_locations_data_policies_get_task()`.
/// For the simplest API, use `bigquerydatapolicy_projects_locations_data_policies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquerydatapolicy_projects_locations_data_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquerydatapolicy_projects_locations_data_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigquerydatapolicy_projects_locations_data_policies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquerydatapolicy_projects_locations_data_policies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigquerydatapolicyProjectsLocationsDataPoliciesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/dataPolicies/{dataPoliciesId}
/// Gets the data policy specified by its resource name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatapolicy_projects_locations_data_policies_get_builder()` + `bigquerydatapolicy_projects_locations_data_policies_get_execute()`.
/// For task-level control, use `bigquerydatapolicy_projects_locations_data_policies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_get(
    client: &SimpleHttpClient,
    args: &BigquerydatapolicyProjectsLocationsDataPoliciesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        bigquerydatapolicy_projects_locations_data_policies_get_builder(client, &args.name)?;
    bigquerydatapolicy_projects_locations_data_policies_get_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dataPolicies/{dataPoliciesId}:getIamPolicy
/// Gets the IAM policy for the specified data policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatapolicy_projects_locations_data_policies_get_iam_policy_execute()` to send, or `bigquerydatapolicy_projects_locations_data_policies_get_iam_policy` for simplest API.

pub fn bigquerydatapolicy_projects_locations_data_policies_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigquerydatapolicy.googleapis.com/v2/projects/{}/locations/{locationsId}/dataPolicies/{dataPoliciesId}:getIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dataPolicies/{dataPoliciesId}:getIamPolicy
/// Gets the IAM policy for the specified data policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquerydatapolicy_projects_locations_data_policies_get_iam_policy_execute()` or `bigquerydatapolicy_projects_locations_data_policies_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquerydatapolicy_projects_locations_data_policies_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dataPolicies/{dataPoliciesId}:getIamPolicy
/// Gets the IAM policy for the specified data policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquerydatapolicy_projects_locations_data_policies_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquerydatapolicy_projects_locations_data_policies_get_iam_policy_task()`.
/// For the simplest API, use `bigquerydatapolicy_projects_locations_data_policies_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquerydatapolicy_projects_locations_data_policies_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquerydatapolicy_projects_locations_data_policies_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigquerydatapolicy_projects_locations_data_policies_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquerydatapolicy_projects_locations_data_policies_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigquerydatapolicyProjectsLocationsDataPoliciesGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dataPolicies/{dataPoliciesId}:getIamPolicy
/// Gets the IAM policy for the specified data policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatapolicy_projects_locations_data_policies_get_iam_policy_builder()` + `bigquerydatapolicy_projects_locations_data_policies_get_iam_policy_execute()`.
/// For task-level control, use `bigquerydatapolicy_projects_locations_data_policies_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_get_iam_policy(
    client: &SimpleHttpClient,
    args: &BigquerydatapolicyProjectsLocationsDataPoliciesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigquerydatapolicy_projects_locations_data_policies_get_iam_policy_builder(
        client,
        &args.resource,
    )?;
    bigquerydatapolicy_projects_locations_data_policies_get_iam_policy_execute(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/dataPolicies
/// List all of the data policies in the specified parent project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatapolicy_projects_locations_data_policies_list_execute()` to send, or `bigquerydatapolicy_projects_locations_data_policies_list` for simplest API.

pub fn bigquerydatapolicy_projects_locations_data_policies_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigquerydatapolicy.googleapis.com/v2/projects/{}/locations/{locationsId}/dataPolicies",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/dataPolicies
/// List all of the data policies in the specified parent project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquerydatapolicy_projects_locations_data_policies_list_execute()` or `bigquerydatapolicy_projects_locations_data_policies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquerydatapolicy_projects_locations_data_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDataPoliciesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDataPoliciesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/dataPolicies
/// List all of the data policies in the specified parent project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquerydatapolicy_projects_locations_data_policies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquerydatapolicy_projects_locations_data_policies_list_task()`.
/// For the simplest API, use `bigquerydatapolicy_projects_locations_data_policies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquerydatapolicy_projects_locations_data_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquerydatapolicy_projects_locations_data_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDataPoliciesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = bigquerydatapolicy_projects_locations_data_policies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquerydatapolicy_projects_locations_data_policies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigquerydatapolicyProjectsLocationsDataPoliciesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v2/projects/{projectsId}/locations/{locationsId}/dataPolicies
/// List all of the data policies in the specified parent project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatapolicy_projects_locations_data_policies_list_builder()` + `bigquerydatapolicy_projects_locations_data_policies_list_execute()`.
/// For task-level control, use `bigquerydatapolicy_projects_locations_data_policies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_list(
    client: &SimpleHttpClient,
    args: &BigquerydatapolicyProjectsLocationsDataPoliciesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDataPoliciesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = bigquerydatapolicy_projects_locations_data_policies_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    bigquerydatapolicy_projects_locations_data_policies_list_execute(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/dataPolicies/{dataPoliciesId}
/// Updates the metadata for an existing data policy. The target data policy can be specified by the resource name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatapolicy_projects_locations_data_policies_patch_execute()` to send, or `bigquerydatapolicy_projects_locations_data_policies_patch` for simplest API.

pub fn bigquerydatapolicy_projects_locations_data_policies_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    allowMissing: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigquerydatapolicy.googleapis.com/v2/projects/{}/locations/{locationsId}/dataPolicies/{dataPoliciesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = allowMissing.as_ref() {
        query_parts.push(format!("allowMissing={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/dataPolicies/{dataPoliciesId}
/// Updates the metadata for an existing data policy. The target data policy can be specified by the resource name.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquerydatapolicy_projects_locations_data_policies_patch_execute()` or `bigquerydatapolicy_projects_locations_data_policies_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquerydatapolicy_projects_locations_data_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DataPolicy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DataPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/dataPolicies/{dataPoliciesId}
/// Updates the metadata for an existing data policy. The target data policy can be specified by the resource name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquerydatapolicy_projects_locations_data_policies_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquerydatapolicy_projects_locations_data_policies_patch_task()`.
/// For the simplest API, use `bigquerydatapolicy_projects_locations_data_policies_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquerydatapolicy_projects_locations_data_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquerydatapolicy_projects_locations_data_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigquerydatapolicy_projects_locations_data_policies_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquerydatapolicy_projects_locations_data_policies_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigquerydatapolicyProjectsLocationsDataPoliciesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: allowMissing
    pub allowMissing: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v2/projects/{projectsId}/locations/{locationsId}/dataPolicies/{dataPoliciesId}
/// Updates the metadata for an existing data policy. The target data policy can be specified by the resource name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatapolicy_projects_locations_data_policies_patch_builder()` + `bigquerydatapolicy_projects_locations_data_policies_patch_execute()`.
/// For task-level control, use `bigquerydatapolicy_projects_locations_data_policies_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_patch(
    client: &SimpleHttpClient,
    args: &BigquerydatapolicyProjectsLocationsDataPoliciesPatchArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigquerydatapolicy_projects_locations_data_policies_patch_builder(
        client,
        &args.name,
        &args.allowMissing,
        &args.updateMask,
    )?;
    bigquerydatapolicy_projects_locations_data_policies_patch_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dataPolicies/{dataPoliciesId}:removeGrantees
/// Removes grantees from a data policy. The grantees will be removed from the existing grantees. If the request contains a grantee that does not exist, the grantee will be ignored.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatapolicy_projects_locations_data_policies_remove_grantees_execute()` to send, or `bigquerydatapolicy_projects_locations_data_policies_remove_grantees` for simplest API.

pub fn bigquerydatapolicy_projects_locations_data_policies_remove_grantees_builder<R>(
    client: &SimpleHttpClient<R>,
    dataPolicy: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigquerydatapolicy.googleapis.com/v2/projects/{}/locations/{locationsId}/dataPolicies/{dataPoliciesId}:removeGrantees",
        dataPolicy,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dataPolicies/{dataPoliciesId}:removeGrantees
/// Removes grantees from a data policy. The grantees will be removed from the existing grantees. If the request contains a grantee that does not exist, the grantee will be ignored.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquerydatapolicy_projects_locations_data_policies_remove_grantees_execute()` or `bigquerydatapolicy_projects_locations_data_policies_remove_grantees`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquerydatapolicy_projects_locations_data_policies_remove_grantees_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_remove_grantees_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DataPolicy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DataPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dataPolicies/{dataPoliciesId}:removeGrantees
/// Removes grantees from a data policy. The grantees will be removed from the existing grantees. If the request contains a grantee that does not exist, the grantee will be ignored.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquerydatapolicy_projects_locations_data_policies_remove_grantees_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquerydatapolicy_projects_locations_data_policies_remove_grantees_task()`.
/// For the simplest API, use `bigquerydatapolicy_projects_locations_data_policies_remove_grantees()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquerydatapolicy_projects_locations_data_policies_remove_grantees_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquerydatapolicy_projects_locations_data_policies_remove_grantees_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigquerydatapolicy_projects_locations_data_policies_remove_grantees_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquerydatapolicy_projects_locations_data_policies_remove_grantees`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigquerydatapolicyProjectsLocationsDataPoliciesRemoveGranteesArgs {
    /// Path parameter: dataPolicy
    pub dataPolicy: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dataPolicies/{dataPoliciesId}:removeGrantees
/// Removes grantees from a data policy. The grantees will be removed from the existing grantees. If the request contains a grantee that does not exist, the grantee will be ignored.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatapolicy_projects_locations_data_policies_remove_grantees_builder()` + `bigquerydatapolicy_projects_locations_data_policies_remove_grantees_execute()`.
/// For task-level control, use `bigquerydatapolicy_projects_locations_data_policies_remove_grantees_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_remove_grantees(
    client: &SimpleHttpClient,
    args: &BigquerydatapolicyProjectsLocationsDataPoliciesRemoveGranteesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigquerydatapolicy_projects_locations_data_policies_remove_grantees_builder(
        client,
        &args.dataPolicy,
    )?;
    bigquerydatapolicy_projects_locations_data_policies_remove_grantees_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dataPolicies/{dataPoliciesId}:setIamPolicy
/// Sets the IAM policy for the specified data policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatapolicy_projects_locations_data_policies_set_iam_policy_execute()` to send, or `bigquerydatapolicy_projects_locations_data_policies_set_iam_policy` for simplest API.

pub fn bigquerydatapolicy_projects_locations_data_policies_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigquerydatapolicy.googleapis.com/v2/projects/{}/locations/{locationsId}/dataPolicies/{dataPoliciesId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dataPolicies/{dataPoliciesId}:setIamPolicy
/// Sets the IAM policy for the specified data policy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquerydatapolicy_projects_locations_data_policies_set_iam_policy_execute()` or `bigquerydatapolicy_projects_locations_data_policies_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquerydatapolicy_projects_locations_data_policies_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dataPolicies/{dataPoliciesId}:setIamPolicy
/// Sets the IAM policy for the specified data policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquerydatapolicy_projects_locations_data_policies_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquerydatapolicy_projects_locations_data_policies_set_iam_policy_task()`.
/// For the simplest API, use `bigquerydatapolicy_projects_locations_data_policies_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquerydatapolicy_projects_locations_data_policies_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquerydatapolicy_projects_locations_data_policies_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = bigquerydatapolicy_projects_locations_data_policies_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquerydatapolicy_projects_locations_data_policies_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigquerydatapolicyProjectsLocationsDataPoliciesSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dataPolicies/{dataPoliciesId}:setIamPolicy
/// Sets the IAM policy for the specified data policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatapolicy_projects_locations_data_policies_set_iam_policy_builder()` + `bigquerydatapolicy_projects_locations_data_policies_set_iam_policy_execute()`.
/// For task-level control, use `bigquerydatapolicy_projects_locations_data_policies_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_set_iam_policy(
    client: &SimpleHttpClient,
    args: &BigquerydatapolicyProjectsLocationsDataPoliciesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigquerydatapolicy_projects_locations_data_policies_set_iam_policy_builder(
        client,
        &args.resource,
    )?;
    bigquerydatapolicy_projects_locations_data_policies_set_iam_policy_execute(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dataPolicies/{dataPoliciesId}:testIamPermissions
/// Returns the caller''s permission on the specified data policy resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatapolicy_projects_locations_data_policies_test_iam_permissions_execute()` to send, or `bigquerydatapolicy_projects_locations_data_policies_test_iam_permissions` for simplest API.

pub fn bigquerydatapolicy_projects_locations_data_policies_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://bigquerydatapolicy.googleapis.com/v2/projects/{}/locations/{locationsId}/dataPolicies/{dataPoliciesId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dataPolicies/{dataPoliciesId}:testIamPermissions
/// Returns the caller''s permission on the specified data policy resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `bigquerydatapolicy_projects_locations_data_policies_test_iam_permissions_execute()` or `bigquerydatapolicy_projects_locations_data_policies_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquerydatapolicy_projects_locations_data_policies_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dataPolicies/{dataPoliciesId}:testIamPermissions
/// Returns the caller''s permission on the specified data policy resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `bigquerydatapolicy_projects_locations_data_policies_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `bigquerydatapolicy_projects_locations_data_policies_test_iam_permissions_task()`.
/// For the simplest API, use `bigquerydatapolicy_projects_locations_data_policies_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `bigquerydatapolicy_projects_locations_data_policies_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn bigquerydatapolicy_projects_locations_data_policies_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        bigquerydatapolicy_projects_locations_data_policies_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`bigquerydatapolicy_projects_locations_data_policies_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigquerydatapolicyProjectsLocationsDataPoliciesTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v2/projects/{projectsId}/locations/{locationsId}/dataPolicies/{dataPoliciesId}:testIamPermissions
/// Returns the caller''s permission on the specified data policy resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatapolicy_projects_locations_data_policies_test_iam_permissions_builder()` + `bigquerydatapolicy_projects_locations_data_policies_test_iam_permissions_execute()`.
/// For task-level control, use `bigquerydatapolicy_projects_locations_data_policies_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &BigquerydatapolicyProjectsLocationsDataPoliciesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = bigquerydatapolicy_projects_locations_data_policies_test_iam_permissions_builder(
        client,
        &args.resource,
    )?;
    bigquerydatapolicy_projects_locations_data_policies_test_iam_permissions_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for DataPolicy
// =============================================================================

/// ResourceIdentifier implementation for DataPolicy with BigquerydatapolicyProjectsLocationsDataPoliciesAddGranteesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigquerydatapolicyProjectsLocationsDataPoliciesAddGranteesArgs>
    for DataPolicy
{
    fn generate_resource_id(
        &self,
        input: &BigquerydatapolicyProjectsLocationsDataPoliciesAddGranteesArgs,
    ) -> String {
        format!("gcp::bigquerydatapolicy::DataPolicy/{}", input.dataPolicy)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigquerydatapolicy::DataPolicy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DataPolicy
// =============================================================================

/// ResourceIdentifier implementation for DataPolicy with BigquerydatapolicyProjectsLocationsDataPoliciesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigquerydatapolicyProjectsLocationsDataPoliciesCreateArgs> for DataPolicy {
    fn generate_resource_id(
        &self,
        input: &BigquerydatapolicyProjectsLocationsDataPoliciesCreateArgs,
    ) -> String {
        format!("gcp::bigquerydatapolicy::DataPolicy/{}", input.parent)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigquerydatapolicy::DataPolicy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with BigquerydatapolicyProjectsLocationsDataPoliciesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigquerydatapolicyProjectsLocationsDataPoliciesDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &BigquerydatapolicyProjectsLocationsDataPoliciesDeleteArgs,
    ) -> String {
        format!("gcp::bigquerydatapolicy::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigquerydatapolicy::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DataPolicy
// =============================================================================

/// ResourceIdentifier implementation for DataPolicy with BigquerydatapolicyProjectsLocationsDataPoliciesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigquerydatapolicyProjectsLocationsDataPoliciesGetArgs> for DataPolicy {
    fn generate_resource_id(
        &self,
        input: &BigquerydatapolicyProjectsLocationsDataPoliciesGetArgs,
    ) -> String {
        format!("gcp::bigquerydatapolicy::DataPolicy/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigquerydatapolicy::DataPolicy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with BigquerydatapolicyProjectsLocationsDataPoliciesGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigquerydatapolicyProjectsLocationsDataPoliciesGetIamPolicyArgs>
    for Policy
{
    fn generate_resource_id(
        &self,
        input: &BigquerydatapolicyProjectsLocationsDataPoliciesGetIamPolicyArgs,
    ) -> String {
        format!("gcp::bigquerydatapolicy::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigquerydatapolicy::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListDataPoliciesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListDataPoliciesResponse with BigquerydatapolicyProjectsLocationsDataPoliciesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigquerydatapolicyProjectsLocationsDataPoliciesListArgs>
    for ListDataPoliciesResponse
{
    fn generate_resource_id(
        &self,
        input: &BigquerydatapolicyProjectsLocationsDataPoliciesListArgs,
    ) -> String {
        format!(
            "gcp::bigquerydatapolicy::ListDataPoliciesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigquerydatapolicy::ListDataPoliciesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DataPolicy
// =============================================================================

/// ResourceIdentifier implementation for DataPolicy with BigquerydatapolicyProjectsLocationsDataPoliciesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigquerydatapolicyProjectsLocationsDataPoliciesPatchArgs> for DataPolicy {
    fn generate_resource_id(
        &self,
        input: &BigquerydatapolicyProjectsLocationsDataPoliciesPatchArgs,
    ) -> String {
        format!("gcp::bigquerydatapolicy::DataPolicy/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigquerydatapolicy::DataPolicy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for DataPolicy
// =============================================================================

/// ResourceIdentifier implementation for DataPolicy with BigquerydatapolicyProjectsLocationsDataPoliciesRemoveGranteesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigquerydatapolicyProjectsLocationsDataPoliciesRemoveGranteesArgs>
    for DataPolicy
{
    fn generate_resource_id(
        &self,
        input: &BigquerydatapolicyProjectsLocationsDataPoliciesRemoveGranteesArgs,
    ) -> String {
        format!("gcp::bigquerydatapolicy::DataPolicy/{}", input.dataPolicy)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigquerydatapolicy::DataPolicy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with BigquerydatapolicyProjectsLocationsDataPoliciesSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigquerydatapolicyProjectsLocationsDataPoliciesSetIamPolicyArgs>
    for Policy
{
    fn generate_resource_id(
        &self,
        input: &BigquerydatapolicyProjectsLocationsDataPoliciesSetIamPolicyArgs,
    ) -> String {
        format!("gcp::bigquerydatapolicy::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigquerydatapolicy::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with BigquerydatapolicyProjectsLocationsDataPoliciesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<BigquerydatapolicyProjectsLocationsDataPoliciesTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &BigquerydatapolicyProjectsLocationsDataPoliciesTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::bigquerydatapolicy::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::bigquerydatapolicy::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
