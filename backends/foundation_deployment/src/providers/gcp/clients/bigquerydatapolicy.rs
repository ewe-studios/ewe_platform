//! Auto-generated API clients for Google Cloud Platform - bigquerydatapolicy.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v2/{+dataPolicy}:addGrantees
/// Adds new grantees to a data policy. The new grantees will be added to the existing grantees. If the request contains a duplicate grantee, the grantee will be ignored. If the request contains a grantee that already exists, the grantee will be ignored.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatapolicy_projects_locations_data_policies_add_grantees_execute()` to send, or `bigquerydatapolicy_projects_locations_data_policies_add_grantees` for simplest API.

pub fn bigquerydatapolicy_projects_locations_data_policies_add_grantees_builder(
    client: &SimpleHttpClient,
    dataPolicy: &str,
    body: &AddGranteesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+dataPolicy}:addGrantees",
        dataPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+dataPolicy}:addGrantees
/// Adds new grantees to a data policy. The new grantees will be added to the existing grantees. If the request contains a duplicate grantee, the grantee will be ignored. If the request contains a grantee that already exists, the grantee will be ignored.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatapolicy_projects_locations_data_policies_add_grantees_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatapolicy_projects_locations_data_policies_add_grantees_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_add_grantees_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DataPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+dataPolicy}:addGrantees
/// Adds new grantees to a data policy. The new grantees will be added to the existing grantees. If the request contains a duplicate grantee, the grantee will be ignored. If the request contains a grantee that already exists, the grantee will be ignored.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatapolicy_projects_locations_data_policies_add_grantees_builder()` + `bigquerydatapolicy_projects_locations_data_policies_add_grantees_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_add_grantees(
    client: &SimpleHttpClient,
    dataPolicy: &str,
    body: &AddGranteesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigquerydatapolicy_projects_locations_data_policies_add_grantees_builder(
        client, dataPolicy, body,
    )?;
    bigquerydatapolicy_projects_locations_data_policies_add_grantees_execute(builder)
}

/// GET v2/{+parent}/dataPolicies
/// Creates a new data policy under a project with the given data_policy_id (used as the display name), and data policy type.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatapolicy_projects_locations_data_policies_create_execute()` to send, or `bigquerydatapolicy_projects_locations_data_policies_create` for simplest API.

pub fn bigquerydatapolicy_projects_locations_data_policies_create_builder(
    client: &SimpleHttpClient,
    parent: &str,
    body: &CreateDataPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/dataPolicies", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+parent}/dataPolicies
/// Creates a new data policy under a project with the given data_policy_id (used as the display name), and data policy type.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatapolicy_projects_locations_data_policies_create_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatapolicy_projects_locations_data_policies_create_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DataPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/dataPolicies
/// Creates a new data policy under a project with the given data_policy_id (used as the display name), and data policy type.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatapolicy_projects_locations_data_policies_create_builder()` + `bigquerydatapolicy_projects_locations_data_policies_create_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_create(
    client: &SimpleHttpClient,
    parent: &str,
    body: &CreateDataPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        bigquerydatapolicy_projects_locations_data_policies_create_builder(client, parent, body)?;
    bigquerydatapolicy_projects_locations_data_policies_create_execute(builder)
}

/// GET v2/{+name}
/// Deletes the data policy specified by its resource name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatapolicy_projects_locations_data_policies_delete_execute()` to send, or `bigquerydatapolicy_projects_locations_data_policies_delete` for simplest API.

pub fn bigquerydatapolicy_projects_locations_data_policies_delete_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes the data policy specified by its resource name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatapolicy_projects_locations_data_policies_delete_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatapolicy_projects_locations_data_policies_delete_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Deletes the data policy specified by its resource name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatapolicy_projects_locations_data_policies_delete_builder()` + `bigquerydatapolicy_projects_locations_data_policies_delete_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_delete(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigquerydatapolicy_projects_locations_data_policies_delete_builder(client, name)?;
    bigquerydatapolicy_projects_locations_data_policies_delete_execute(builder)
}

/// GET v2/{+name}
/// Gets the data policy specified by its resource name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatapolicy_projects_locations_data_policies_get_execute()` to send, or `bigquerydatapolicy_projects_locations_data_policies_get` for simplest API.

pub fn bigquerydatapolicy_projects_locations_data_policies_get_builder(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets the data policy specified by its resource name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatapolicy_projects_locations_data_policies_get_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatapolicy_projects_locations_data_policies_get_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DataPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Gets the data policy specified by its resource name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatapolicy_projects_locations_data_policies_get_builder()` + `bigquerydatapolicy_projects_locations_data_policies_get_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_get(
    client: &SimpleHttpClient,
    name: &str,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigquerydatapolicy_projects_locations_data_policies_get_builder(client, name)?;
    bigquerydatapolicy_projects_locations_data_policies_get_execute(builder)
}

/// GET v2/{+resource}:getIamPolicy
/// Gets the IAM policy for the specified data policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatapolicy_projects_locations_data_policies_get_iam_policy_execute()` to send, or `bigquerydatapolicy_projects_locations_data_policies_get_iam_policy` for simplest API.

pub fn bigquerydatapolicy_projects_locations_data_policies_get_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+resource}:getIamPolicy",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+resource}:getIamPolicy
/// Gets the IAM policy for the specified data policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatapolicy_projects_locations_data_policies_get_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatapolicy_projects_locations_data_policies_get_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+resource}:getIamPolicy
/// Gets the IAM policy for the specified data policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatapolicy_projects_locations_data_policies_get_iam_policy_builder()` + `bigquerydatapolicy_projects_locations_data_policies_get_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_get_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &GetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigquerydatapolicy_projects_locations_data_policies_get_iam_policy_builder(
        client, resource, body,
    )?;
    bigquerydatapolicy_projects_locations_data_policies_get_iam_policy_execute(builder)
}

/// GET v2/{+parent}/dataPolicies
/// List all of the data policies in the specified parent project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatapolicy_projects_locations_data_policies_list_execute()` to send, or `bigquerydatapolicy_projects_locations_data_policies_list` for simplest API.

pub fn bigquerydatapolicy_projects_locations_data_policies_list_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+parent}/dataPolicies", parent,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v2/{+parent}/dataPolicies
/// List all of the data policies in the specified parent project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatapolicy_projects_locations_data_policies_list_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatapolicy_projects_locations_data_policies_list_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDataPoliciesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDataPoliciesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+parent}/dataPolicies
/// List all of the data policies in the specified parent project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatapolicy_projects_locations_data_policies_list_builder()` + `bigquerydatapolicy_projects_locations_data_policies_list_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_list(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDataPoliciesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = bigquerydatapolicy_projects_locations_data_policies_list_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    bigquerydatapolicy_projects_locations_data_policies_list_execute(builder)
}

/// GET v2/{+name}
/// Updates the metadata for an existing data policy. The target data policy can be specified by the resource name.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatapolicy_projects_locations_data_policies_patch_execute()` to send, or `bigquerydatapolicy_projects_locations_data_policies_patch` for simplest API.

pub fn bigquerydatapolicy_projects_locations_data_policies_patch_builder(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    updateMask: Option<&str>,
    body: &DataPolicy,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv2/{+name}", name,);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "allowMissing",
            allowMissing
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "updateMask",
            updateMask
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+name}
/// Updates the metadata for an existing data policy. The target data policy can be specified by the resource name.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatapolicy_projects_locations_data_policies_patch_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatapolicy_projects_locations_data_policies_patch_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DataPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+name}
/// Updates the metadata for an existing data policy. The target data policy can be specified by the resource name.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatapolicy_projects_locations_data_policies_patch_builder()` + `bigquerydatapolicy_projects_locations_data_policies_patch_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_patch(
    client: &SimpleHttpClient,
    name: &str,
    allowMissing: Option<bool>,
    updateMask: Option<&str>,
    body: &DataPolicy,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigquerydatapolicy_projects_locations_data_policies_patch_builder(
        client,
        name,
        allowMissing,
        updateMask,
        body,
    )?;
    bigquerydatapolicy_projects_locations_data_policies_patch_execute(builder)
}

/// GET v2/{+dataPolicy}:removeGrantees
/// Removes grantees from a data policy. The grantees will be removed from the existing grantees. If the request contains a grantee that does not exist, the grantee will be ignored.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatapolicy_projects_locations_data_policies_remove_grantees_execute()` to send, or `bigquerydatapolicy_projects_locations_data_policies_remove_grantees` for simplest API.

pub fn bigquerydatapolicy_projects_locations_data_policies_remove_grantees_builder(
    client: &SimpleHttpClient,
    dataPolicy: &str,
    body: &RemoveGranteesRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+dataPolicy}:removeGrantees",
        dataPolicy,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+dataPolicy}:removeGrantees
/// Removes grantees from a data policy. The grantees will be removed from the existing grantees. If the request contains a grantee that does not exist, the grantee will be ignored.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatapolicy_projects_locations_data_policies_remove_grantees_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatapolicy_projects_locations_data_policies_remove_grantees_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_remove_grantees_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DataPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+dataPolicy}:removeGrantees
/// Removes grantees from a data policy. The grantees will be removed from the existing grantees. If the request contains a grantee that does not exist, the grantee will be ignored.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatapolicy_projects_locations_data_policies_remove_grantees_builder()` + `bigquerydatapolicy_projects_locations_data_policies_remove_grantees_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_remove_grantees(
    client: &SimpleHttpClient,
    dataPolicy: &str,
    body: &RemoveGranteesRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<DataPolicy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigquerydatapolicy_projects_locations_data_policies_remove_grantees_builder(
        client, dataPolicy, body,
    )?;
    bigquerydatapolicy_projects_locations_data_policies_remove_grantees_execute(builder)
}

/// GET v2/{+resource}:setIamPolicy
/// Sets the IAM policy for the specified data policy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatapolicy_projects_locations_data_policies_set_iam_policy_execute()` to send, or `bigquerydatapolicy_projects_locations_data_policies_set_iam_policy` for simplest API.

pub fn bigquerydatapolicy_projects_locations_data_policies_set_iam_policy_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+resource}:setIamPolicy",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+resource}:setIamPolicy
/// Sets the IAM policy for the specified data policy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatapolicy_projects_locations_data_policies_set_iam_policy_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatapolicy_projects_locations_data_policies_set_iam_policy_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+resource}:setIamPolicy
/// Sets the IAM policy for the specified data policy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatapolicy_projects_locations_data_policies_set_iam_policy_builder()` + `bigquerydatapolicy_projects_locations_data_policies_set_iam_policy_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_set_iam_policy(
    client: &SimpleHttpClient,
    resource: &str,
    body: &SetIamPolicyRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = bigquerydatapolicy_projects_locations_data_policies_set_iam_policy_builder(
        client, resource, body,
    )?;
    bigquerydatapolicy_projects_locations_data_policies_set_iam_policy_execute(builder)
}

/// GET v2/{+resource}:testIamPermissions
/// Returns the caller's permission on the specified data policy resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `bigquerydatapolicy_projects_locations_data_policies_test_iam_permissions_execute()` to send, or `bigquerydatapolicy_projects_locations_data_policies_test_iam_permissions` for simplest API.

pub fn bigquerydatapolicy_projects_locations_data_policies_test_iam_permissions_builder(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv2/{+resource}:testIamPermissions",
        resource,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v2/{+resource}:testIamPermissions
/// Returns the caller's permission on the specified data policy resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `bigquerydatapolicy_projects_locations_data_policies_test_iam_permissions_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from bigquerydatapolicy_projects_locations_data_policies_test_iam_permissions_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v2/{+resource}:testIamPermissions
/// Returns the caller's permission on the specified data policy resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `bigquerydatapolicy_projects_locations_data_policies_test_iam_permissions_builder()` + `bigquerydatapolicy_projects_locations_data_policies_test_iam_permissions_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn bigquerydatapolicy_projects_locations_data_policies_test_iam_permissions(
    client: &SimpleHttpClient,
    resource: &str,
    body: &TestIamPermissionsRequest,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = bigquerydatapolicy_projects_locations_data_policies_test_iam_permissions_builder(
        client, resource, body,
    )?;
    bigquerydatapolicy_projects_locations_data_policies_test_iam_permissions_execute(builder)
}
