//! Auto-generated API clients for Google Cloud Platform - chromeuxreport.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/records:queryHistoryRecord
/// Queries the Chrome User Experience Report for a timeseries history record for a given site. Returns a history record that contains one or more metric timeseries corresponding to performance data about the requested site.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromeuxreport_records_query_history_record_execute()` to send, or `chromeuxreport_records_query_history_record` for simplest API.

pub fn chromeuxreport_records_query_history_record_builder(
    client: &SimpleHttpClient,
    body: &QueryHistoryRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/records:queryHistoryRecord",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/records:queryHistoryRecord
/// Queries the Chrome User Experience Report for a timeseries history record for a given site. Returns a history record that contains one or more metric timeseries corresponding to performance data about the requested site.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromeuxreport_records_query_history_record_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromeuxreport_records_query_history_record_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromeuxreport_records_query_history_record_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<QueryHistoryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: QueryHistoryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/records:queryHistoryRecord
/// Queries the Chrome User Experience Report for a timeseries history record for a given site. Returns a history record that contains one or more metric timeseries corresponding to performance data about the requested site.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromeuxreport_records_query_history_record_builder()` + `chromeuxreport_records_query_history_record_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromeuxreport_records_query_history_record(
    client: &SimpleHttpClient,
    body: &QueryHistoryRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<QueryHistoryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = chromeuxreport_records_query_history_record_builder(client, body)?;
    chromeuxreport_records_query_history_record_execute(builder)
}

/// GET v1/records:queryRecord
/// Queries the Chrome User Experience for a single record for a given site. Returns a record that contains one or more metrics corresponding to performance data about the requested site.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromeuxreport_records_query_record_execute()` to send, or `chromeuxreport_records_query_record` for simplest API.

pub fn chromeuxreport_records_query_record_builder(
    client: &SimpleHttpClient,
    body: &QueryRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!("https://api.example.comv1/records:queryRecord",);

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
}

/// GET v1/records:queryRecord
/// Queries the Chrome User Experience for a single record for a given site. Returns a record that contains one or more metrics corresponding to performance data about the requested site.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `chromeuxreport_records_query_record_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from chromeuxreport_records_query_record_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromeuxreport_records_query_record_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<QueryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: QueryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/records:queryRecord
/// Queries the Chrome User Experience for a single record for a given site. Returns a record that contains one or more metrics corresponding to performance data about the requested site.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromeuxreport_records_query_record_builder()` + `chromeuxreport_records_query_record_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromeuxreport_records_query_record(
    client: &SimpleHttpClient,
    body: &QueryRequest,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<QueryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = chromeuxreport_records_query_record_builder(client, body)?;
    chromeuxreport_records_query_record_execute(builder)
}
