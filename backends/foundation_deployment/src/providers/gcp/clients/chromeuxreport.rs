//! Auto-generated API clients for gcp/chromeuxreport.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST v1/records:queryHistoryRecord
/// Queries the Chrome User Experience Report for a timeseries history record for a given site. Returns a history record that contains one or more metric timeseries corresponding to performance data about the requested site.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromeuxreport_records_query_history_record_execute()` to send, or `chromeuxreport_records_query_history_record` for simplest API.

pub fn chromeuxreport_records_query_history_record_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url =
        format!("https://chromeuxreport.googleapis.com/v1/records:queryHistoryRecord",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/records:queryHistoryRecord
/// Queries the Chrome User Experience Report for a timeseries history record for a given site. Returns a history record that contains one or more metric timeseries corresponding to performance data about the requested site.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromeuxreport_records_query_history_record_execute()` or `chromeuxreport_records_query_history_record`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromeuxreport_records_query_history_record_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromeuxreport_records_query_history_record_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<QueryHistoryResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: QueryHistoryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/records:queryHistoryRecord
/// Queries the Chrome User Experience Report for a timeseries history record for a given site. Returns a history record that contains one or more metric timeseries corresponding to performance data about the requested site.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromeuxreport_records_query_history_record_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromeuxreport_records_query_history_record_task()`.
/// For the simplest API, use `chromeuxreport_records_query_history_record()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromeuxreport_records_query_history_record_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromeuxreport_records_query_history_record_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<QueryHistoryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = chromeuxreport_records_query_history_record_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1/records:queryHistoryRecord
/// Queries the Chrome User Experience Report for a timeseries history record for a given site. Returns a history record that contains one or more metric timeseries corresponding to performance data about the requested site.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromeuxreport_records_query_history_record_builder()` + `chromeuxreport_records_query_history_record_execute()`.
/// For task-level control, use `chromeuxreport_records_query_history_record_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromeuxreport_records_query_history_record(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<QueryHistoryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = chromeuxreport_records_query_history_record_builder(client)?;
    chromeuxreport_records_query_history_record_execute(builder)
}

/// POST v1/records:queryRecord
/// Queries the Chrome User Experience for a single record for a given site. Returns a record that contains one or more metrics corresponding to performance data about the requested site.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `chromeuxreport_records_query_record_execute()` to send, or `chromeuxreport_records_query_record` for simplest API.

pub fn chromeuxreport_records_query_record_builder<R>(
    client: &SimpleHttpClient<R>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!("https://chromeuxreport.googleapis.com/v1/records:queryRecord",);

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/records:queryRecord
/// Queries the Chrome User Experience for a single record for a given site. Returns a record that contains one or more metrics corresponding to performance data about the requested site.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `chromeuxreport_records_query_record_execute()` or `chromeuxreport_records_query_record`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromeuxreport_records_query_record_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromeuxreport_records_query_record_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<QueryResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: QueryResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/records:queryRecord
/// Queries the Chrome User Experience for a single record for a given site. Returns a record that contains one or more metrics corresponding to performance data about the requested site.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `chromeuxreport_records_query_record_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `chromeuxreport_records_query_record_task()`.
/// For the simplest API, use `chromeuxreport_records_query_record()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `chromeuxreport_records_query_record_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn chromeuxreport_records_query_record_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<QueryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = chromeuxreport_records_query_record_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// POST v1/records:queryRecord
/// Queries the Chrome User Experience for a single record for a given site. Returns a record that contains one or more metrics corresponding to performance data about the requested site.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `chromeuxreport_records_query_record_builder()` + `chromeuxreport_records_query_record_execute()`.
/// For task-level control, use `chromeuxreport_records_query_record_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn chromeuxreport_records_query_record(
    client: &SimpleHttpClient,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<QueryResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = chromeuxreport_records_query_record_builder(client)?;
    chromeuxreport_records_query_record_execute(builder)
}
