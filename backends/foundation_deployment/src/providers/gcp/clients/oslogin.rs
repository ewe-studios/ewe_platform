//! Auto-generated API clients for gcp/oslogin.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/users/{usersId}/loginProfile
/// Retrieves the profile information used for logging in to a virtual machine on Google Compute Engine.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `oslogin_users_get_login_profile_execute()` to send, or `oslogin_users_get_login_profile` for simplest API.

pub fn oslogin_users_get_login_profile_builder(
    client: &SimpleHttpClient,
    name: String,
    projectId: Option<String>,
    systemId: Option<String>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://oslogin.googleapis.com/v1/users/{}/loginProfile",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = projectId {
        query_parts.push(format!("projectId={}", val));
    }
    if let Some(val) = systemId {
        query_parts.push(format!("systemId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/users/{usersId}/loginProfile
/// Retrieves the profile information used for logging in to a virtual machine on Google Compute Engine.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `oslogin_users_get_login_profile_execute()` or `oslogin_users_get_login_profile`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `oslogin_users_get_login_profile_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn oslogin_users_get_login_profile_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<LoginProfile>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: LoginProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/users/{usersId}/loginProfile
/// Retrieves the profile information used for logging in to a virtual machine on Google Compute Engine.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `oslogin_users_get_login_profile_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `oslogin_users_get_login_profile_task()`.
/// For the simplest API, use `oslogin_users_get_login_profile()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `oslogin_users_get_login_profile_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn oslogin_users_get_login_profile_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LoginProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = oslogin_users_get_login_profile_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`oslogin_users_get_login_profile`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OsloginUsersGetLoginProfileArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: projectId
    pub projectId: Option<String>,
    /// Query parameter: systemId
    pub systemId: Option<String>,
}

/// GET v1/users/{usersId}/loginProfile
/// Retrieves the profile information used for logging in to a virtual machine on Google Compute Engine.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `oslogin_users_get_login_profile_builder()` + `oslogin_users_get_login_profile_execute()`.
/// For task-level control, use `oslogin_users_get_login_profile_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn oslogin_users_get_login_profile(
    client: &SimpleHttpClient,
    args: &OsloginUsersGetLoginProfileArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<LoginProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = oslogin_users_get_login_profile_builder(
        client,
        args.name.clone(),
        args.projectId.clone(),
        args.systemId.clone(),
    )?;
    oslogin_users_get_login_profile_execute(builder)
}

/// GET v1/users/{usersId}:importSshPublicKey
/// Adds an SSH public key and returns the profile information. Default POSIX account information is set when no username and UID exist as part of the login profile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `oslogin_users_import_ssh_public_key_execute()` to send, or `oslogin_users_import_ssh_public_key` for simplest API.

pub fn oslogin_users_import_ssh_public_key_builder(
    client: &SimpleHttpClient,
    parent: String,
    projectId: Option<String>,
    regions: Option<String>,
    body: &SshPublicKey,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://oslogin.googleapis.com/v1/users/{}:importSshPublicKey",);

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = projectId {
        query_parts.push(format!("projectId={}", val));
    }
    if let Some(val) = regions {
        query_parts.push(format!("regions={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/users/{usersId}:importSshPublicKey
/// Adds an SSH public key and returns the profile information. Default POSIX account information is set when no username and UID exist as part of the login profile.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `oslogin_users_import_ssh_public_key_execute()` or `oslogin_users_import_ssh_public_key`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `oslogin_users_import_ssh_public_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn oslogin_users_import_ssh_public_key_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ImportSshPublicKeyResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ImportSshPublicKeyResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/users/{usersId}:importSshPublicKey
/// Adds an SSH public key and returns the profile information. Default POSIX account information is set when no username and UID exist as part of the login profile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `oslogin_users_import_ssh_public_key_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `oslogin_users_import_ssh_public_key_task()`.
/// For the simplest API, use `oslogin_users_import_ssh_public_key()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `oslogin_users_import_ssh_public_key_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn oslogin_users_import_ssh_public_key_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ImportSshPublicKeyResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = oslogin_users_import_ssh_public_key_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`oslogin_users_import_ssh_public_key`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OsloginUsersImportSshPublicKeyArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: projectId
    pub projectId: Option<String>,
    /// Query parameter: regions
    pub regions: Option<String>,
    /// Request body.
    pub body: SshPublicKey,
}

/// GET v1/users/{usersId}:importSshPublicKey
/// Adds an SSH public key and returns the profile information. Default POSIX account information is set when no username and UID exist as part of the login profile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `oslogin_users_import_ssh_public_key_builder()` + `oslogin_users_import_ssh_public_key_execute()`.
/// For task-level control, use `oslogin_users_import_ssh_public_key_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn oslogin_users_import_ssh_public_key(
    client: &SimpleHttpClient,
    args: &OsloginUsersImportSshPublicKeyArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ImportSshPublicKeyResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = oslogin_users_import_ssh_public_key_builder(
        client,
        args.parent.clone(),
        args.projectId.clone(),
        args.regions.clone(),
        &args.body,
    )?;
    oslogin_users_import_ssh_public_key_execute(builder)
}

/// GET v1/users/{usersId}/sshPublicKeys
/// Create an SSH public key
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `oslogin_users_ssh_public_keys_create_execute()` to send, or `oslogin_users_ssh_public_keys_create` for simplest API.

pub fn oslogin_users_ssh_public_keys_create_builder(
    client: &SimpleHttpClient,
    parent: String,
    body: &SshPublicKey,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://oslogin.googleapis.com/v1/users/{}/sshPublicKeys",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/users/{usersId}/sshPublicKeys
/// Create an SSH public key
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `oslogin_users_ssh_public_keys_create_execute()` or `oslogin_users_ssh_public_keys_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `oslogin_users_ssh_public_keys_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn oslogin_users_ssh_public_keys_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SshPublicKey>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: SshPublicKey = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/users/{usersId}/sshPublicKeys
/// Create an SSH public key
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `oslogin_users_ssh_public_keys_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `oslogin_users_ssh_public_keys_create_task()`.
/// For the simplest API, use `oslogin_users_ssh_public_keys_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `oslogin_users_ssh_public_keys_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn oslogin_users_ssh_public_keys_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SshPublicKey>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = oslogin_users_ssh_public_keys_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`oslogin_users_ssh_public_keys_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OsloginUsersSshPublicKeysCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: SshPublicKey,
}

/// GET v1/users/{usersId}/sshPublicKeys
/// Create an SSH public key
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `oslogin_users_ssh_public_keys_create_builder()` + `oslogin_users_ssh_public_keys_create_execute()`.
/// For task-level control, use `oslogin_users_ssh_public_keys_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn oslogin_users_ssh_public_keys_create(
    client: &SimpleHttpClient,
    args: &OsloginUsersSshPublicKeysCreateArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<SshPublicKey>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        oslogin_users_ssh_public_keys_create_builder(client, args.parent.clone(), &args.body)?;
    oslogin_users_ssh_public_keys_create_execute(builder)
}
