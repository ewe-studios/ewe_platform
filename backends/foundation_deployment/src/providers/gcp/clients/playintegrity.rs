//! Auto-generated API clients for gcp/playintegrity.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/{v1Id}/deviceRecall:write
/// Writes recall bits for the device where Play Integrity API token is obtained. The endpoint is available to select Play partners in an early access program (EAP).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playintegrity_device_recall_write_execute()` to send, or `playintegrity_device_recall_write` for simplest API.

pub fn playintegrity_device_recall_write_builder(
    client: &SimpleHttpClient,
    packageName: &String,
    body: &WriteDeviceRecallRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://playintegrity.googleapis.com/v1/{}/deviceRecall:write",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{v1Id}/deviceRecall:write
/// Writes recall bits for the device where Play Integrity API token is obtained. The endpoint is available to select Play partners in an early access program (EAP).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `playintegrity_device_recall_write_execute()` or `playintegrity_device_recall_write`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playintegrity_device_recall_write_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playintegrity_device_recall_write_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WriteDeviceRecallResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: WriteDeviceRecallResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}/deviceRecall:write
/// Writes recall bits for the device where Play Integrity API token is obtained. The endpoint is available to select Play partners in an early access program (EAP).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `playintegrity_device_recall_write_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `playintegrity_device_recall_write_task()`.
/// For the simplest API, use `playintegrity_device_recall_write()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playintegrity_device_recall_write_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn playintegrity_device_recall_write_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WriteDeviceRecallResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = playintegrity_device_recall_write_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`playintegrity_device_recall_write`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PlayintegrityDeviceRecallWriteArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Request body.
    pub body: WriteDeviceRecallRequest,
}

/// GET v1/{v1Id}/deviceRecall:write
/// Writes recall bits for the device where Play Integrity API token is obtained. The endpoint is available to select Play partners in an early access program (EAP).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playintegrity_device_recall_write_builder()` + `playintegrity_device_recall_write_execute()`.
/// For task-level control, use `playintegrity_device_recall_write_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playintegrity_device_recall_write(
    client: &SimpleHttpClient,
    args: &PlayintegrityDeviceRecallWriteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<WriteDeviceRecallResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = playintegrity_device_recall_write_builder(client, &args.packageName, &args.body)?;
    playintegrity_device_recall_write_execute(builder)
}

/// GET v1/{v1Id}:decodeIntegrityToken
/// Decodes the integrity token and returns the token payload.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playintegrity_decode_integrity_token_execute()` to send, or `playintegrity_decode_integrity_token` for simplest API.

pub fn playintegrity_decode_integrity_token_builder(
    client: &SimpleHttpClient,
    packageName: &String,
    body: &DecodeIntegrityTokenRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://playintegrity.googleapis.com/v1/{}:decodeIntegrityToken",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{v1Id}:decodeIntegrityToken
/// Decodes the integrity token and returns the token payload.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `playintegrity_decode_integrity_token_execute()` or `playintegrity_decode_integrity_token`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playintegrity_decode_integrity_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playintegrity_decode_integrity_token_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DecodeIntegrityTokenResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DecodeIntegrityTokenResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}:decodeIntegrityToken
/// Decodes the integrity token and returns the token payload.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `playintegrity_decode_integrity_token_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `playintegrity_decode_integrity_token_task()`.
/// For the simplest API, use `playintegrity_decode_integrity_token()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playintegrity_decode_integrity_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn playintegrity_decode_integrity_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DecodeIntegrityTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = playintegrity_decode_integrity_token_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`playintegrity_decode_integrity_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PlayintegrityDecodeIntegrityTokenArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Request body.
    pub body: DecodeIntegrityTokenRequest,
}

/// GET v1/{v1Id}:decodeIntegrityToken
/// Decodes the integrity token and returns the token payload.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playintegrity_decode_integrity_token_builder()` + `playintegrity_decode_integrity_token_execute()`.
/// For task-level control, use `playintegrity_decode_integrity_token_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playintegrity_decode_integrity_token(
    client: &SimpleHttpClient,
    args: &PlayintegrityDecodeIntegrityTokenArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DecodeIntegrityTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        playintegrity_decode_integrity_token_builder(client, &args.packageName, &args.body)?;
    playintegrity_decode_integrity_token_execute(builder)
}

/// GET v1/{v1Id}:decodePcIntegrityToken
/// Decodes the PC integrity token and returns the PC token payload.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `playintegrity_decode_pc_integrity_token_execute()` to send, or `playintegrity_decode_pc_integrity_token` for simplest API.

pub fn playintegrity_decode_pc_integrity_token_builder(
    client: &SimpleHttpClient,
    packageName: &String,
    body: &DecodePcIntegrityTokenRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url =
        format!("https://playintegrity.googleapis.com/v1/{}:decodePcIntegrityToken",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{v1Id}:decodePcIntegrityToken
/// Decodes the PC integrity token and returns the PC token payload.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `playintegrity_decode_pc_integrity_token_execute()` or `playintegrity_decode_pc_integrity_token`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playintegrity_decode_pc_integrity_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playintegrity_decode_pc_integrity_token_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DecodePcIntegrityTokenResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: DecodePcIntegrityTokenResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/{v1Id}:decodePcIntegrityToken
/// Decodes the PC integrity token and returns the PC token payload.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `playintegrity_decode_pc_integrity_token_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `playintegrity_decode_pc_integrity_token_task()`.
/// For the simplest API, use `playintegrity_decode_pc_integrity_token()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `playintegrity_decode_pc_integrity_token_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn playintegrity_decode_pc_integrity_token_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DecodePcIntegrityTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = playintegrity_decode_pc_integrity_token_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`playintegrity_decode_pc_integrity_token`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PlayintegrityDecodePcIntegrityTokenArgs {
    /// Path parameter: packageName
    pub packageName: String,
    /// Request body.
    pub body: DecodePcIntegrityTokenRequest,
}

/// GET v1/{v1Id}:decodePcIntegrityToken
/// Decodes the PC integrity token and returns the PC token payload.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `playintegrity_decode_pc_integrity_token_builder()` + `playintegrity_decode_pc_integrity_token_execute()`.
/// For task-level control, use `playintegrity_decode_pc_integrity_token_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn playintegrity_decode_pc_integrity_token(
    client: &SimpleHttpClient,
    args: &PlayintegrityDecodePcIntegrityTokenArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<DecodePcIntegrityTokenResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        playintegrity_decode_pc_integrity_token_builder(client, &args.packageName, &args.body)?;
    playintegrity_decode_pc_integrity_token_execute(builder)
}
