//! Auto-generated API clients for gcp/fcm.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]


use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/projects/{projectsId}/messages:send
/// Send a message to specified target (a registration token, topic or condition).
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `fcm_projects_messages_send_execute()` to send, or `fcm_projects_messages_send` for simplest API.

pub fn fcm_projects_messages_send_builder(
    client: &SimpleHttpClient,
    parent: &String,
    body: &SendMessageRequest,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let endpoint_url = format!("https://fcm.googleapis.com/v1/projects/{}/messages:send",);

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    builder
        .body_json(body)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/projects/{projectsId}/messages:send
/// Send a message to specified target (a registration token, topic or condition).
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `fcm_projects_messages_send_execute()` or `fcm_projects_messages_send`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `fcm_projects_messages_send_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn fcm_projects_messages_send_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Message>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Message = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/messages:send
/// Send a message to specified target (a registration token, topic or condition).
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `fcm_projects_messages_send_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `fcm_projects_messages_send_task()`.
/// For the simplest API, use `fcm_projects_messages_send()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `fcm_projects_messages_send_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn fcm_projects_messages_send_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Message>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = fcm_projects_messages_send_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`fcm_projects_messages_send`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct FcmProjectsMessagesSendArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Request body.
    pub body: SendMessageRequest,
}

/// GET v1/projects/{projectsId}/messages:send
/// Send a message to specified target (a registration token, topic or condition).
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `fcm_projects_messages_send_builder()` + `fcm_projects_messages_send_execute()`.
/// For task-level control, use `fcm_projects_messages_send_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn fcm_projects_messages_send(
    client: &SimpleHttpClient,
    args: &FcmProjectsMessagesSendArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Message>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = fcm_projects_messages_send_builder(client, &args.parent, &args.body)?;
    fcm_projects_messages_send_execute(builder)
}
