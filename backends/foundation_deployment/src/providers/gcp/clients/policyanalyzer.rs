//! Auto-generated API clients for Google Cloud Platform - policyanalyzer.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`


#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{execute, StreamIterator, StreamIteratorExt};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, RequestIntro, SimpleHttpClient, SystemDnsResolver,
};

/// GET v1/{+parent}/activities:query
/// Queries policy activities on Google Cloud resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `policyanalyzer_folders_locations_activity_types_activities_query_execute()` to send, or `policyanalyzer_folders_locations_activity_types_activities_query` for simplest API.

pub fn policyanalyzer_folders_locations_activity_types_activities_query_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/activities:query",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/activities:query
/// Queries policy activities on Google Cloud resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `policyanalyzer_folders_locations_activity_types_activities_query_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from policyanalyzer_folders_locations_activity_types_activities_query_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn policyanalyzer_folders_locations_activity_types_activities_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudPolicyanalyzerV1QueryActivityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudPolicyanalyzerV1QueryActivityResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/activities:query
/// Queries policy activities on Google Cloud resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `policyanalyzer_folders_locations_activity_types_activities_query_builder()` + `policyanalyzer_folders_locations_activity_types_activities_query_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn policyanalyzer_folders_locations_activity_types_activities_query(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudPolicyanalyzerV1QueryActivityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = policyanalyzer_folders_locations_activity_types_activities_query_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    policyanalyzer_folders_locations_activity_types_activities_query_execute(builder)
}

/// GET v1/{+parent}/activities:query
/// Queries policy activities on Google Cloud resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `policyanalyzer_organizations_locations_activity_types_activities_query_execute()` to send, or `policyanalyzer_organizations_locations_activity_types_activities_query` for simplest API.

pub fn policyanalyzer_organizations_locations_activity_types_activities_query_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/activities:query",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/activities:query
/// Queries policy activities on Google Cloud resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `policyanalyzer_organizations_locations_activity_types_activities_query_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from policyanalyzer_organizations_locations_activity_types_activities_query_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn policyanalyzer_organizations_locations_activity_types_activities_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudPolicyanalyzerV1QueryActivityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudPolicyanalyzerV1QueryActivityResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/activities:query
/// Queries policy activities on Google Cloud resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `policyanalyzer_organizations_locations_activity_types_activities_query_builder()` + `policyanalyzer_organizations_locations_activity_types_activities_query_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn policyanalyzer_organizations_locations_activity_types_activities_query(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudPolicyanalyzerV1QueryActivityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = policyanalyzer_organizations_locations_activity_types_activities_query_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    policyanalyzer_organizations_locations_activity_types_activities_query_execute(builder)
}

/// GET v1/{+parent}/activities:query
/// Queries policy activities on Google Cloud resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `policyanalyzer_projects_locations_activity_types_activities_query_execute()` to send, or `policyanalyzer_projects_locations_activity_types_activities_query` for simplest API.

pub fn policyanalyzer_projects_locations_activity_types_activities_query_builder(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<ClientRequestBuilder<SystemDnsResolver>, ApiError> {
    // Build URL
    let url = format!(
        "https://api.example.comv1/{+parent}/activities:query",
        parent,
    );

    // Build request
    client
        .get(&url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
        .query(
            "filter",
            filter
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageSize",
            pageSize
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
        .query(
            "pageToken",
            pageToken
                .as_ref()
                .map(|s| s.to_string())
                .unwrap_or_default()
                .as_str(),
        )
}

/// GET v1/{+parent}/activities:query
/// Queries policy activities on Google Cloud resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request.
/// For customization, use `policyanalyzer_projects_locations_activity_types_activities_query_builder()` then pass to this function.
///
/// # Arguments
///
/// * `builder` - ClientRequestBuilder from policyanalyzer_projects_locations_activity_types_activities_query_builder()
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn policyanalyzer_projects_locations_activity_types_activities_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudPolicyanalyzerV1QueryActivityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    // Get SendRequestTask from builder
    let task = builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success { stream, status } => {
                let headers = status.headers().clone();

                if !status.is_success() {
                    return Err(ApiError::HttpStatus {
                        code: status.as_u16(),
                        headers,
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudPolicyanalyzerV1QueryActivityResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status.as_u16(),
                    headers,
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending);

    // Convert TaskIterator to StreamIterator
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// GET v1/{+parent}/activities:query
/// Queries policy activities on Google Cloud resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `policyanalyzer_projects_locations_activity_types_activities_query_builder()` + `policyanalyzer_projects_locations_activity_types_activities_query_execute()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn policyanalyzer_projects_locations_activity_types_activities_query(
    client: &SimpleHttpClient,
    parent: &str,
    filter: Option<&str>,
    pageSize: Option<i32>,
    pageToken: Option<&str>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudPolicyanalyzerV1QueryActivityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = policyanalyzer_projects_locations_activity_types_activities_query_builder(
        client, parent, filter, pageSize, pageToken,
    )?;
    policyanalyzer_projects_locations_activity_types_activities_query_execute(builder)
}
