//! Auto-generated API clients for gcp/policyanalyzer.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// GET v1/folders/{foldersId}/locations/{locationsId}/activityTypes/{activityTypesId}/activities:query
/// Queries policy activities on Google Cloud resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `policyanalyzer_folders_locations_activity_types_activities_query_execute()` to send, or `policyanalyzer_folders_locations_activity_types_activities_query` for simplest API.

pub fn policyanalyzer_folders_locations_activity_types_activities_query_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://policyanalyzer.googleapis.com/v1/folders/{}/locations/{locationsId}/activityTypes/{activityTypesId}/activities:query",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/activityTypes/{activityTypesId}/activities:query
/// Queries policy activities on Google Cloud resources.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `policyanalyzer_folders_locations_activity_types_activities_query_execute()` or `policyanalyzer_folders_locations_activity_types_activities_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `policyanalyzer_folders_locations_activity_types_activities_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn policyanalyzer_folders_locations_activity_types_activities_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudPolicyanalyzerV1QueryActivityResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudPolicyanalyzerV1QueryActivityResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/activityTypes/{activityTypesId}/activities:query
/// Queries policy activities on Google Cloud resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `policyanalyzer_folders_locations_activity_types_activities_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `policyanalyzer_folders_locations_activity_types_activities_query_task()`.
/// For the simplest API, use `policyanalyzer_folders_locations_activity_types_activities_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `policyanalyzer_folders_locations_activity_types_activities_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn policyanalyzer_folders_locations_activity_types_activities_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudPolicyanalyzerV1QueryActivityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = policyanalyzer_folders_locations_activity_types_activities_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`policyanalyzer_folders_locations_activity_types_activities_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PolicyanalyzerFoldersLocationsActivityTypesActivitiesQueryArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/folders/{foldersId}/locations/{locationsId}/activityTypes/{activityTypesId}/activities:query
/// Queries policy activities on Google Cloud resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `policyanalyzer_folders_locations_activity_types_activities_query_builder()` + `policyanalyzer_folders_locations_activity_types_activities_query_execute()`.
/// For task-level control, use `policyanalyzer_folders_locations_activity_types_activities_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn policyanalyzer_folders_locations_activity_types_activities_query(
    client: &SimpleHttpClient,
    args: &PolicyanalyzerFoldersLocationsActivityTypesActivitiesQueryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudPolicyanalyzerV1QueryActivityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = policyanalyzer_folders_locations_activity_types_activities_query_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    policyanalyzer_folders_locations_activity_types_activities_query_execute(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/activityTypes/{activityTypesId}/activities:query
/// Queries policy activities on Google Cloud resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `policyanalyzer_organizations_locations_activity_types_activities_query_execute()` to send, or `policyanalyzer_organizations_locations_activity_types_activities_query` for simplest API.

pub fn policyanalyzer_organizations_locations_activity_types_activities_query_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://policyanalyzer.googleapis.com/v1/organizations/{}/locations/{locationsId}/activityTypes/{activityTypesId}/activities:query",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/activityTypes/{activityTypesId}/activities:query
/// Queries policy activities on Google Cloud resources.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `policyanalyzer_organizations_locations_activity_types_activities_query_execute()` or `policyanalyzer_organizations_locations_activity_types_activities_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `policyanalyzer_organizations_locations_activity_types_activities_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn policyanalyzer_organizations_locations_activity_types_activities_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudPolicyanalyzerV1QueryActivityResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudPolicyanalyzerV1QueryActivityResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/activityTypes/{activityTypesId}/activities:query
/// Queries policy activities on Google Cloud resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `policyanalyzer_organizations_locations_activity_types_activities_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `policyanalyzer_organizations_locations_activity_types_activities_query_task()`.
/// For the simplest API, use `policyanalyzer_organizations_locations_activity_types_activities_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `policyanalyzer_organizations_locations_activity_types_activities_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn policyanalyzer_organizations_locations_activity_types_activities_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudPolicyanalyzerV1QueryActivityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        policyanalyzer_organizations_locations_activity_types_activities_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`policyanalyzer_organizations_locations_activity_types_activities_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PolicyanalyzerOrganizationsLocationsActivityTypesActivitiesQueryArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/organizations/{organizationsId}/locations/{locationsId}/activityTypes/{activityTypesId}/activities:query
/// Queries policy activities on Google Cloud resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `policyanalyzer_organizations_locations_activity_types_activities_query_builder()` + `policyanalyzer_organizations_locations_activity_types_activities_query_execute()`.
/// For task-level control, use `policyanalyzer_organizations_locations_activity_types_activities_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn policyanalyzer_organizations_locations_activity_types_activities_query(
    client: &SimpleHttpClient,
    args: &PolicyanalyzerOrganizationsLocationsActivityTypesActivitiesQueryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudPolicyanalyzerV1QueryActivityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = policyanalyzer_organizations_locations_activity_types_activities_query_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    policyanalyzer_organizations_locations_activity_types_activities_query_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/activityTypes/{activityTypesId}/activities:query
/// Queries policy activities on Google Cloud resources.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `policyanalyzer_projects_locations_activity_types_activities_query_execute()` to send, or `policyanalyzer_projects_locations_activity_types_activities_query` for simplest API.

pub fn policyanalyzer_projects_locations_activity_types_activities_query_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<Option<String>>,
    pageSize: &Option<Option<String>>,
    pageToken: &Option<Option<String>>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://policyanalyzer.googleapis.com/v1/projects/{}/locations/{locationsId}/activityTypes/{activityTypesId}/activities:query",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/activityTypes/{activityTypesId}/activities:query
/// Queries policy activities on Google Cloud resources.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `policyanalyzer_projects_locations_activity_types_activities_query_execute()` or `policyanalyzer_projects_locations_activity_types_activities_query`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `policyanalyzer_projects_locations_activity_types_activities_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn policyanalyzer_projects_locations_activity_types_activities_query_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleCloudPolicyanalyzerV1QueryActivityResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleCloudPolicyanalyzerV1QueryActivityResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/activityTypes/{activityTypesId}/activities:query
/// Queries policy activities on Google Cloud resources.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `policyanalyzer_projects_locations_activity_types_activities_query_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `policyanalyzer_projects_locations_activity_types_activities_query_task()`.
/// For the simplest API, use `policyanalyzer_projects_locations_activity_types_activities_query()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `policyanalyzer_projects_locations_activity_types_activities_query_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn policyanalyzer_projects_locations_activity_types_activities_query_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudPolicyanalyzerV1QueryActivityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = policyanalyzer_projects_locations_activity_types_activities_query_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`policyanalyzer_projects_locations_activity_types_activities_query`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct PolicyanalyzerProjectsLocationsActivityTypesActivitiesQueryArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<Option<String>>,
    /// Query parameter: pageSize
    pub pageSize: Option<Option<String>>,
    /// Query parameter: pageToken
    pub pageToken: Option<Option<String>>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/activityTypes/{activityTypesId}/activities:query
/// Queries policy activities on Google Cloud resources.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `policyanalyzer_projects_locations_activity_types_activities_query_builder()` + `policyanalyzer_projects_locations_activity_types_activities_query_execute()`.
/// For task-level control, use `policyanalyzer_projects_locations_activity_types_activities_query_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn policyanalyzer_projects_locations_activity_types_activities_query(
    client: &SimpleHttpClient,
    args: &PolicyanalyzerProjectsLocationsActivityTypesActivitiesQueryArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleCloudPolicyanalyzerV1QueryActivityResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = policyanalyzer_projects_locations_activity_types_activities_query_builder(
        client,
        &args.parent,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    policyanalyzer_projects_locations_activity_types_activities_query_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudPolicyanalyzerV1QueryActivityResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudPolicyanalyzerV1QueryActivityResponse with PolicyanalyzerFoldersLocationsActivityTypesActivitiesQueryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PolicyanalyzerFoldersLocationsActivityTypesActivitiesQueryArgs>
    for GoogleCloudPolicyanalyzerV1QueryActivityResponse
{
    fn generate_resource_id(
        &self,
        input: &PolicyanalyzerFoldersLocationsActivityTypesActivitiesQueryArgs,
    ) -> String {
        format!(
            "gcp::policyanalyzer::GoogleCloudPolicyanalyzerV1QueryActivityResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::policyanalyzer::GoogleCloudPolicyanalyzerV1QueryActivityResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudPolicyanalyzerV1QueryActivityResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudPolicyanalyzerV1QueryActivityResponse with PolicyanalyzerOrganizationsLocationsActivityTypesActivitiesQueryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PolicyanalyzerOrganizationsLocationsActivityTypesActivitiesQueryArgs>
    for GoogleCloudPolicyanalyzerV1QueryActivityResponse
{
    fn generate_resource_id(
        &self,
        input: &PolicyanalyzerOrganizationsLocationsActivityTypesActivitiesQueryArgs,
    ) -> String {
        format!(
            "gcp::policyanalyzer::GoogleCloudPolicyanalyzerV1QueryActivityResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::policyanalyzer::GoogleCloudPolicyanalyzerV1QueryActivityResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleCloudPolicyanalyzerV1QueryActivityResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleCloudPolicyanalyzerV1QueryActivityResponse with PolicyanalyzerProjectsLocationsActivityTypesActivitiesQueryArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<PolicyanalyzerProjectsLocationsActivityTypesActivitiesQueryArgs>
    for GoogleCloudPolicyanalyzerV1QueryActivityResponse
{
    fn generate_resource_id(
        &self,
        input: &PolicyanalyzerProjectsLocationsActivityTypesActivitiesQueryArgs,
    ) -> String {
        format!(
            "gcp::policyanalyzer::GoogleCloudPolicyanalyzerV1QueryActivityResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::policyanalyzer::GoogleCloudPolicyanalyzerV1QueryActivityResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
