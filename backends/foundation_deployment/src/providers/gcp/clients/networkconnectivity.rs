//! Auto-generated API clients for gcp/networkconnectivity.
//!
//! Generated by `cargo run --bin ewe_platform gen_provider_clients`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp`

#![cfg(feature = "gcp")]

pub mod types;

use crate::providers::gcp::clients::types::*;
use crate::providers::gcp::resources::*;
use foundation_core::valtron::{
    execute, BoxedSendExecutionAction, StreamIterator, StreamIteratorExt, TaskIterator,
    TaskIteratorExt,
};
use foundation_core::wire::simple_http::client::{
    body_reader, ClientRequestBuilder, DnsResolver, RequestIntro, SimpleHttpClient,
    SystemDnsResolver,
};
use foundation_db::state::resource_identifier::ResourceIdentifier;
use foundation_macros::JsonHash;
use serde::Serialize;

/// POST v1/projects/{projectsId}/locations/{locationsId}:checkConsumerConfig
/// CheckConsumerConfig validates the consumer network and project for potential PSC connection creation. This method performs several checks, including: - Validating the existence and permissions of the service class. - Ensuring the consumer network exists and is accessible. - Verifying XPN relationships if applicable. - Checking for compatible IP versions between the consumer network and the requested version. This method performs a dynamic IAM check for the networkconnectivity.`serviceClasses`.use permission on the service class resource in the Prepare phase.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_check_consumer_config_execute()` to send, or `networkconnectivity_projects_locations_check_consumer_config` for simplest API.

pub fn networkconnectivity_projects_locations_check_consumer_config_builder<R>(
    client: &SimpleHttpClient<R>,
    location: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}:checkConsumerConfig",
        location,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}:checkConsumerConfig
/// CheckConsumerConfig validates the consumer network and project for potential PSC connection creation. This method performs several checks, including: - Validating the existence and permissions of the service class. - Ensuring the consumer network exists and is accessible. - Verifying XPN relationships if applicable. - Checking for compatible IP versions between the consumer network and the requested version. This method performs a dynamic IAM check for the networkconnectivity.`serviceClasses`.use permission on the service class resource in the Prepare phase.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_check_consumer_config_execute()` or `networkconnectivity_projects_locations_check_consumer_config`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_check_consumer_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_check_consumer_config_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CheckConsumerConfigResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: CheckConsumerConfigResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}:checkConsumerConfig
/// CheckConsumerConfig validates the consumer network and project for potential PSC connection creation. This method performs several checks, including: - Validating the existence and permissions of the service class. - Ensuring the consumer network exists and is accessible. - Verifying XPN relationships if applicable. - Checking for compatible IP versions between the consumer network and the requested version. This method performs a dynamic IAM check for the networkconnectivity.`serviceClasses`.use permission on the service class resource in the Prepare phase.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_check_consumer_config_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_check_consumer_config_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_check_consumer_config()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_check_consumer_config_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_check_consumer_config_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CheckConsumerConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_check_consumer_config_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_check_consumer_config`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsCheckConsumerConfigArgs {
    /// Path parameter: location
    pub location: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}:checkConsumerConfig
/// CheckConsumerConfig validates the consumer network and project for potential PSC connection creation. This method performs several checks, including: - Validating the existence and permissions of the service class. - Ensuring the consumer network exists and is accessible. - Verifying XPN relationships if applicable. - Checking for compatible IP versions between the consumer network and the requested version. This method performs a dynamic IAM check for the networkconnectivity.`serviceClasses`.use permission on the service class resource in the Prepare phase.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_check_consumer_config_builder()` + `networkconnectivity_projects_locations_check_consumer_config_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_check_consumer_config_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_check_consumer_config(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsCheckConsumerConfigArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<CheckConsumerConfigResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_check_consumer_config_builder(
        client,
        &args.location,
    )?;
    networkconnectivity_projects_locations_check_consumer_config_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_get_execute()` to send, or `networkconnectivity_projects_locations_get` for simplest API.

pub fn networkconnectivity_projects_locations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_get_execute()` or `networkconnectivity_projects_locations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Location>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Location = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_get_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}
/// Gets information about a location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_get_builder()` + `networkconnectivity_projects_locations_get_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_get(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Location>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_get_builder(client, &args.name)?;
    networkconnectivity_projects_locations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_list_execute()` to send, or `networkconnectivity_projects_locations_list` for simplest API.

pub fn networkconnectivity_projects_locations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    extraLocationTypes: &Option<String>,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = extraLocationTypes.as_ref() {
        query_parts.push(format!("extraLocationTypes={}", val));
    }
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_list_execute()` or `networkconnectivity_projects_locations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLocationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListLocationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_list_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: extraLocationTypes
    pub extraLocationTypes: Option<String>,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations
/// Lists information about the supported locations for this service. This method lists locations based on the resource scope provided in the [ListLocationsRequest.name] field: * **Global locations**: If name is empty, the method lists the public locations available to all projects. * **Project-specific locations**: If name follows the format `projects/{project}`, the method lists locations visible to that specific project. This includes public, private, or other project-specific locations enabled for the project. For `gRPC` and client library implementations, the resource name is passed as the name field. For direct service calls, the resource name is incorporated into the request path based on the specific service implementation and version.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_list_builder()` + `networkconnectivity_projects_locations_list_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_list(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListLocationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_list_builder(
        client,
        &args.name,
        &args.extraLocationTypes,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
    )?;
    networkconnectivity_projects_locations_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/automatedDnsRecords
/// Creates a new AutomatedDnsRecord in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_automated_dns_records_create_execute()` to send, or `networkconnectivity_projects_locations_automated_dns_records_create` for simplest API.

pub fn networkconnectivity_projects_locations_automated_dns_records_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    automatedDnsRecordId: &Option<String>,
    insertMode: &Option<String>,
    requestId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/automatedDnsRecords",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = automatedDnsRecordId.as_ref() {
        query_parts.push(format!("automatedDnsRecordId={}", val));
    }
    if let Some(val) = insertMode.as_ref() {
        query_parts.push(format!("insertMode={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/automatedDnsRecords
/// Creates a new AutomatedDnsRecord in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_automated_dns_records_create_execute()` or `networkconnectivity_projects_locations_automated_dns_records_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_automated_dns_records_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_automated_dns_records_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/automatedDnsRecords
/// Creates a new AutomatedDnsRecord in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_automated_dns_records_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_automated_dns_records_create_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_automated_dns_records_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_automated_dns_records_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_automated_dns_records_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_automated_dns_records_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_automated_dns_records_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsAutomatedDnsRecordsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: automatedDnsRecordId
    pub automatedDnsRecordId: Option<String>,
    /// Query parameter: insertMode
    pub insertMode: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/automatedDnsRecords
/// Creates a new AutomatedDnsRecord in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_automated_dns_records_create_builder()` + `networkconnectivity_projects_locations_automated_dns_records_create_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_automated_dns_records_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_automated_dns_records_create(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsAutomatedDnsRecordsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_automated_dns_records_create_builder(
        client,
        &args.parent,
        &args.automatedDnsRecordId,
        &args.insertMode,
        &args.requestId,
    )?;
    networkconnectivity_projects_locations_automated_dns_records_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/automatedDnsRecords/{automatedDnsRecordsId}
/// Deletes a single AutomatedDnsRecord.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_automated_dns_records_delete_execute()` to send, or `networkconnectivity_projects_locations_automated_dns_records_delete` for simplest API.

pub fn networkconnectivity_projects_locations_automated_dns_records_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    deleteMode: &Option<String>,
    etag: &Option<String>,
    requestId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/automatedDnsRecords/{automatedDnsRecordsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = deleteMode.as_ref() {
        query_parts.push(format!("deleteMode={}", val));
    }
    if let Some(val) = etag.as_ref() {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/automatedDnsRecords/{automatedDnsRecordsId}
/// Deletes a single AutomatedDnsRecord.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_automated_dns_records_delete_execute()` or `networkconnectivity_projects_locations_automated_dns_records_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_automated_dns_records_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_automated_dns_records_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/automatedDnsRecords/{automatedDnsRecordsId}
/// Deletes a single AutomatedDnsRecord.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_automated_dns_records_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_automated_dns_records_delete_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_automated_dns_records_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_automated_dns_records_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_automated_dns_records_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_automated_dns_records_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_automated_dns_records_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsAutomatedDnsRecordsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: deleteMode
    pub deleteMode: Option<String>,
    /// Query parameter: etag
    pub etag: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/automatedDnsRecords/{automatedDnsRecordsId}
/// Deletes a single AutomatedDnsRecord.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_automated_dns_records_delete_builder()` + `networkconnectivity_projects_locations_automated_dns_records_delete_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_automated_dns_records_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_automated_dns_records_delete(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsAutomatedDnsRecordsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_automated_dns_records_delete_builder(
        client,
        &args.name,
        &args.deleteMode,
        &args.etag,
        &args.requestId,
    )?;
    networkconnectivity_projects_locations_automated_dns_records_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/automatedDnsRecords/{automatedDnsRecordsId}
/// Gets details of a single AutomatedDnsRecord.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_automated_dns_records_get_execute()` to send, or `networkconnectivity_projects_locations_automated_dns_records_get` for simplest API.

pub fn networkconnectivity_projects_locations_automated_dns_records_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/automatedDnsRecords/{automatedDnsRecordsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/automatedDnsRecords/{automatedDnsRecordsId}
/// Gets details of a single AutomatedDnsRecord.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_automated_dns_records_get_execute()` or `networkconnectivity_projects_locations_automated_dns_records_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_automated_dns_records_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_automated_dns_records_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AutomatedDnsRecord>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: AutomatedDnsRecord = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/automatedDnsRecords/{automatedDnsRecordsId}
/// Gets details of a single AutomatedDnsRecord.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_automated_dns_records_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_automated_dns_records_get_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_automated_dns_records_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_automated_dns_records_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_automated_dns_records_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutomatedDnsRecord>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_automated_dns_records_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_automated_dns_records_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsAutomatedDnsRecordsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/automatedDnsRecords/{automatedDnsRecordsId}
/// Gets details of a single AutomatedDnsRecord.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_automated_dns_records_get_builder()` + `networkconnectivity_projects_locations_automated_dns_records_get_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_automated_dns_records_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_automated_dns_records_get(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsAutomatedDnsRecordsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<AutomatedDnsRecord>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_automated_dns_records_get_builder(
        client, &args.name,
    )?;
    networkconnectivity_projects_locations_automated_dns_records_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/automatedDnsRecords
/// Lists AutomatedDnsRecords in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_automated_dns_records_list_execute()` to send, or `networkconnectivity_projects_locations_automated_dns_records_list` for simplest API.

pub fn networkconnectivity_projects_locations_automated_dns_records_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/automatedDnsRecords",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/automatedDnsRecords
/// Lists AutomatedDnsRecords in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_automated_dns_records_list_execute()` or `networkconnectivity_projects_locations_automated_dns_records_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_automated_dns_records_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_automated_dns_records_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAutomatedDnsRecordsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListAutomatedDnsRecordsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/automatedDnsRecords
/// Lists AutomatedDnsRecords in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_automated_dns_records_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_automated_dns_records_list_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_automated_dns_records_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_automated_dns_records_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_automated_dns_records_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAutomatedDnsRecordsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_automated_dns_records_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_automated_dns_records_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsAutomatedDnsRecordsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/automatedDnsRecords
/// Lists AutomatedDnsRecords in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_automated_dns_records_list_builder()` + `networkconnectivity_projects_locations_automated_dns_records_list_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_automated_dns_records_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_automated_dns_records_list(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsAutomatedDnsRecordsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListAutomatedDnsRecordsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_automated_dns_records_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    networkconnectivity_projects_locations_automated_dns_records_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/global/hubs/{hubsId}:acceptSpoke
/// Accepts a proposal to attach a Network Connectivity Center spoke to a hub.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_global_hubs_accept_spoke_execute()` to send, or `networkconnectivity_projects_locations_global_hubs_accept_spoke` for simplest API.

pub fn networkconnectivity_projects_locations_global_hubs_accept_spoke_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/global/hubs/{hubsId}:acceptSpoke",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/global/hubs/{hubsId}:acceptSpoke
/// Accepts a proposal to attach a Network Connectivity Center spoke to a hub.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_global_hubs_accept_spoke_execute()` or `networkconnectivity_projects_locations_global_hubs_accept_spoke`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_accept_spoke_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_accept_spoke_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/global/hubs/{hubsId}:acceptSpoke
/// Accepts a proposal to attach a Network Connectivity Center spoke to a hub.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_global_hubs_accept_spoke_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_accept_spoke_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_global_hubs_accept_spoke()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_accept_spoke_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_global_hubs_accept_spoke_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_global_hubs_accept_spoke_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_global_hubs_accept_spoke`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsGlobalHubsAcceptSpokeArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/global/hubs/{hubsId}:acceptSpoke
/// Accepts a proposal to attach a Network Connectivity Center spoke to a hub.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_global_hubs_accept_spoke_builder()` + `networkconnectivity_projects_locations_global_hubs_accept_spoke_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_accept_spoke_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_accept_spoke(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsGlobalHubsAcceptSpokeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_global_hubs_accept_spoke_builder(
        client, &args.name,
    )?;
    networkconnectivity_projects_locations_global_hubs_accept_spoke_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/global/hubs/{hubsId}:acceptSpokeUpdate
/// Accepts a proposal to update a Network Connectivity Center spoke in a hub.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_global_hubs_accept_spoke_update_execute()` to send, or `networkconnectivity_projects_locations_global_hubs_accept_spoke_update` for simplest API.

pub fn networkconnectivity_projects_locations_global_hubs_accept_spoke_update_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/global/hubs/{hubsId}:acceptSpokeUpdate",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/global/hubs/{hubsId}:acceptSpokeUpdate
/// Accepts a proposal to update a Network Connectivity Center spoke in a hub.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_global_hubs_accept_spoke_update_execute()` or `networkconnectivity_projects_locations_global_hubs_accept_spoke_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_accept_spoke_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_accept_spoke_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/global/hubs/{hubsId}:acceptSpokeUpdate
/// Accepts a proposal to update a Network Connectivity Center spoke in a hub.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_global_hubs_accept_spoke_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_accept_spoke_update_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_global_hubs_accept_spoke_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_accept_spoke_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_global_hubs_accept_spoke_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        networkconnectivity_projects_locations_global_hubs_accept_spoke_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_global_hubs_accept_spoke_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsGlobalHubsAcceptSpokeUpdateArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/global/hubs/{hubsId}:acceptSpokeUpdate
/// Accepts a proposal to update a Network Connectivity Center spoke in a hub.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_global_hubs_accept_spoke_update_builder()` + `networkconnectivity_projects_locations_global_hubs_accept_spoke_update_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_accept_spoke_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_accept_spoke_update(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsGlobalHubsAcceptSpokeUpdateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_global_hubs_accept_spoke_update_builder(
        client, &args.name,
    )?;
    networkconnectivity_projects_locations_global_hubs_accept_spoke_update_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/global/hubs
/// Creates a new Network Connectivity Center hub in the specified project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_global_hubs_create_execute()` to send, or `networkconnectivity_projects_locations_global_hubs_create` for simplest API.

pub fn networkconnectivity_projects_locations_global_hubs_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    hubId: &Option<String>,
    requestId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/global/hubs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = hubId.as_ref() {
        query_parts.push(format!("hubId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/global/hubs
/// Creates a new Network Connectivity Center hub in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_global_hubs_create_execute()` or `networkconnectivity_projects_locations_global_hubs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/global/hubs
/// Creates a new Network Connectivity Center hub in the specified project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_global_hubs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_create_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_global_hubs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_global_hubs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_global_hubs_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_global_hubs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsGlobalHubsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: hubId
    pub hubId: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/global/hubs
/// Creates a new Network Connectivity Center hub in the specified project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_global_hubs_create_builder()` + `networkconnectivity_projects_locations_global_hubs_create_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_create(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsGlobalHubsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_global_hubs_create_builder(
        client,
        &args.parent,
        &args.hubId,
        &args.requestId,
    )?;
    networkconnectivity_projects_locations_global_hubs_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/global/hubs/{hubsId}
/// Deletes a Network Connectivity Center hub.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_global_hubs_delete_execute()` to send, or `networkconnectivity_projects_locations_global_hubs_delete` for simplest API.

pub fn networkconnectivity_projects_locations_global_hubs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/global/hubs/{hubsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/global/hubs/{hubsId}
/// Deletes a Network Connectivity Center hub.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_global_hubs_delete_execute()` or `networkconnectivity_projects_locations_global_hubs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/global/hubs/{hubsId}
/// Deletes a Network Connectivity Center hub.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_global_hubs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_delete_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_global_hubs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_global_hubs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_global_hubs_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_global_hubs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsGlobalHubsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// DELETE v1/projects/{projectsId}/locations/global/hubs/{hubsId}
/// Deletes a Network Connectivity Center hub.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_global_hubs_delete_builder()` + `networkconnectivity_projects_locations_global_hubs_delete_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_delete(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsGlobalHubsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_global_hubs_delete_builder(
        client,
        &args.name,
        &args.requestId,
    )?;
    networkconnectivity_projects_locations_global_hubs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}
/// Gets details about a Network Connectivity Center hub.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_global_hubs_get_execute()` to send, or `networkconnectivity_projects_locations_global_hubs_get` for simplest API.

pub fn networkconnectivity_projects_locations_global_hubs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/global/hubs/{hubsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}
/// Gets details about a Network Connectivity Center hub.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_global_hubs_get_execute()` or `networkconnectivity_projects_locations_global_hubs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Hub>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Hub = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}
/// Gets details about a Network Connectivity Center hub.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_global_hubs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_get_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_global_hubs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_global_hubs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Hub>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_global_hubs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_global_hubs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsGlobalHubsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}
/// Gets details about a Network Connectivity Center hub.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_global_hubs_get_builder()` + `networkconnectivity_projects_locations_global_hubs_get_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_get(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsGlobalHubsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Hub>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        networkconnectivity_projects_locations_global_hubs_get_builder(client, &args.name)?;
    networkconnectivity_projects_locations_global_hubs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_global_hubs_get_iam_policy_execute()` to send, or `networkconnectivity_projects_locations_global_hubs_get_iam_policy` for simplest API.

pub fn networkconnectivity_projects_locations_global_hubs_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/global/hubs/{hubsId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_global_hubs_get_iam_policy_execute()` or `networkconnectivity_projects_locations_global_hubs_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_global_hubs_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_get_iam_policy_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_global_hubs_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_global_hubs_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_global_hubs_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_global_hubs_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsGlobalHubsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_global_hubs_get_iam_policy_builder()` + `networkconnectivity_projects_locations_global_hubs_get_iam_policy_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_get_iam_policy(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsGlobalHubsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_global_hubs_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    networkconnectivity_projects_locations_global_hubs_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/hubs
/// Lists the Network Connectivity Center hubs associated with a given project.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_global_hubs_list_execute()` to send, or `networkconnectivity_projects_locations_global_hubs_list` for simplest API.

pub fn networkconnectivity_projects_locations_global_hubs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/global/hubs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/global/hubs
/// Lists the Network Connectivity Center hubs associated with a given project.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_global_hubs_list_execute()` or `networkconnectivity_projects_locations_global_hubs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListHubsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListHubsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/hubs
/// Lists the Network Connectivity Center hubs associated with a given project.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_global_hubs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_list_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_global_hubs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_global_hubs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListHubsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_global_hubs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_global_hubs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsGlobalHubsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/global/hubs
/// Lists the Network Connectivity Center hubs associated with a given project.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_global_hubs_list_builder()` + `networkconnectivity_projects_locations_global_hubs_list_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_list(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsGlobalHubsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListHubsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_global_hubs_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    networkconnectivity_projects_locations_global_hubs_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}:listSpokes
/// Lists the Network Connectivity Center spokes associated with a specified hub and location. The list includes both spokes that are attached to the hub and spokes that have been proposed but not yet accepted.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_global_hubs_list_spokes_execute()` to send, or `networkconnectivity_projects_locations_global_hubs_list_spokes` for simplest API.

pub fn networkconnectivity_projects_locations_global_hubs_list_spokes_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    spokeLocations: &Option<String>,
    view: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/global/hubs/{hubsId}:listSpokes",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = spokeLocations.as_ref() {
        query_parts.push(format!("spokeLocations={}", val));
    }
    if let Some(val) = view.as_ref() {
        query_parts.push(format!("view={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}:listSpokes
/// Lists the Network Connectivity Center spokes associated with a specified hub and location. The list includes both spokes that are attached to the hub and spokes that have been proposed but not yet accepted.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_global_hubs_list_spokes_execute()` or `networkconnectivity_projects_locations_global_hubs_list_spokes`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_list_spokes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_list_spokes_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListHubSpokesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListHubSpokesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}:listSpokes
/// Lists the Network Connectivity Center spokes associated with a specified hub and location. The list includes both spokes that are attached to the hub and spokes that have been proposed but not yet accepted.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_global_hubs_list_spokes_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_list_spokes_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_global_hubs_list_spokes()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_list_spokes_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_global_hubs_list_spokes_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListHubSpokesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_global_hubs_list_spokes_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_global_hubs_list_spokes`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsGlobalHubsListSpokesArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: spokeLocations
    pub spokeLocations: Option<String>,
    /// Query parameter: view
    pub view: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}:listSpokes
/// Lists the Network Connectivity Center spokes associated with a specified hub and location. The list includes both spokes that are attached to the hub and spokes that have been proposed but not yet accepted.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_global_hubs_list_spokes_builder()` + `networkconnectivity_projects_locations_global_hubs_list_spokes_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_list_spokes_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_list_spokes(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsGlobalHubsListSpokesArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListHubSpokesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_global_hubs_list_spokes_builder(
        client,
        &args.name,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
        &args.spokeLocations,
        &args.view,
    )?;
    networkconnectivity_projects_locations_global_hubs_list_spokes_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/global/hubs/{hubsId}
/// Updates the description `and/or` labels of a Network Connectivity Center hub.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_global_hubs_patch_execute()` to send, or `networkconnectivity_projects_locations_global_hubs_patch` for simplest API.

pub fn networkconnectivity_projects_locations_global_hubs_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/global/hubs/{hubsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/global/hubs/{hubsId}
/// Updates the description `and/or` labels of a Network Connectivity Center hub.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_global_hubs_patch_execute()` or `networkconnectivity_projects_locations_global_hubs_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/global/hubs/{hubsId}
/// Updates the description `and/or` labels of a Network Connectivity Center hub.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_global_hubs_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_patch_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_global_hubs_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_global_hubs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_global_hubs_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_global_hubs_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsGlobalHubsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/locations/global/hubs/{hubsId}
/// Updates the description `and/or` labels of a Network Connectivity Center hub.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_global_hubs_patch_builder()` + `networkconnectivity_projects_locations_global_hubs_patch_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_patch(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsGlobalHubsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_global_hubs_patch_builder(
        client,
        &args.name,
        &args.requestId,
        &args.updateMask,
    )?;
    networkconnectivity_projects_locations_global_hubs_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}:queryStatus
/// Query the Private Service Connect propagation status of a Network Connectivity Center hub.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_global_hubs_query_status_execute()` to send, or `networkconnectivity_projects_locations_global_hubs_query_status` for simplest API.

pub fn networkconnectivity_projects_locations_global_hubs_query_status_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<String>,
    groupBy: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/global/hubs/{hubsId}:queryStatus",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = groupBy.as_ref() {
        query_parts.push(format!("groupBy={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}:queryStatus
/// Query the Private Service Connect propagation status of a Network Connectivity Center hub.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_global_hubs_query_status_execute()` or `networkconnectivity_projects_locations_global_hubs_query_status`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_query_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_query_status_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<QueryHubStatusResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: QueryHubStatusResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}:queryStatus
/// Query the Private Service Connect propagation status of a Network Connectivity Center hub.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_global_hubs_query_status_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_query_status_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_global_hubs_query_status()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_query_status_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_global_hubs_query_status_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<QueryHubStatusResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_global_hubs_query_status_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_global_hubs_query_status`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsGlobalHubsQueryStatusArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: groupBy
    pub groupBy: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}:queryStatus
/// Query the Private Service Connect propagation status of a Network Connectivity Center hub.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_global_hubs_query_status_builder()` + `networkconnectivity_projects_locations_global_hubs_query_status_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_query_status_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_query_status(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsGlobalHubsQueryStatusArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<QueryHubStatusResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_global_hubs_query_status_builder(
        client,
        &args.name,
        &args.filter,
        &args.groupBy,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    networkconnectivity_projects_locations_global_hubs_query_status_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/global/hubs/{hubsId}:rejectSpoke
/// Rejects a Network Connectivity Center spoke from being attached to a hub. If the spoke was previously in the `ACTIVE` state, it transitions to the `INACTIVE` state and is no longer able to connect to other spokes that are attached to the hub.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_global_hubs_reject_spoke_execute()` to send, or `networkconnectivity_projects_locations_global_hubs_reject_spoke` for simplest API.

pub fn networkconnectivity_projects_locations_global_hubs_reject_spoke_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/global/hubs/{hubsId}:rejectSpoke",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/global/hubs/{hubsId}:rejectSpoke
/// Rejects a Network Connectivity Center spoke from being attached to a hub. If the spoke was previously in the `ACTIVE` state, it transitions to the `INACTIVE` state and is no longer able to connect to other spokes that are attached to the hub.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_global_hubs_reject_spoke_execute()` or `networkconnectivity_projects_locations_global_hubs_reject_spoke`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_reject_spoke_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_reject_spoke_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/global/hubs/{hubsId}:rejectSpoke
/// Rejects a Network Connectivity Center spoke from being attached to a hub. If the spoke was previously in the `ACTIVE` state, it transitions to the `INACTIVE` state and is no longer able to connect to other spokes that are attached to the hub.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_global_hubs_reject_spoke_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_reject_spoke_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_global_hubs_reject_spoke()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_reject_spoke_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_global_hubs_reject_spoke_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_global_hubs_reject_spoke_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_global_hubs_reject_spoke`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsGlobalHubsRejectSpokeArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/global/hubs/{hubsId}:rejectSpoke
/// Rejects a Network Connectivity Center spoke from being attached to a hub. If the spoke was previously in the `ACTIVE` state, it transitions to the `INACTIVE` state and is no longer able to connect to other spokes that are attached to the hub.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_global_hubs_reject_spoke_builder()` + `networkconnectivity_projects_locations_global_hubs_reject_spoke_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_reject_spoke_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_reject_spoke(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsGlobalHubsRejectSpokeArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_global_hubs_reject_spoke_builder(
        client, &args.name,
    )?;
    networkconnectivity_projects_locations_global_hubs_reject_spoke_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/global/hubs/{hubsId}:rejectSpokeUpdate
/// Rejects a proposal to update a Network Connectivity Center spoke in a hub.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_global_hubs_reject_spoke_update_execute()` to send, or `networkconnectivity_projects_locations_global_hubs_reject_spoke_update` for simplest API.

pub fn networkconnectivity_projects_locations_global_hubs_reject_spoke_update_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/global/hubs/{hubsId}:rejectSpokeUpdate",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/global/hubs/{hubsId}:rejectSpokeUpdate
/// Rejects a proposal to update a Network Connectivity Center spoke in a hub.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_global_hubs_reject_spoke_update_execute()` or `networkconnectivity_projects_locations_global_hubs_reject_spoke_update`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_reject_spoke_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_reject_spoke_update_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/global/hubs/{hubsId}:rejectSpokeUpdate
/// Rejects a proposal to update a Network Connectivity Center spoke in a hub.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_global_hubs_reject_spoke_update_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_reject_spoke_update_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_global_hubs_reject_spoke_update()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_reject_spoke_update_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_global_hubs_reject_spoke_update_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        networkconnectivity_projects_locations_global_hubs_reject_spoke_update_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_global_hubs_reject_spoke_update`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsGlobalHubsRejectSpokeUpdateArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/global/hubs/{hubsId}:rejectSpokeUpdate
/// Rejects a proposal to update a Network Connectivity Center spoke in a hub.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_global_hubs_reject_spoke_update_builder()` + `networkconnectivity_projects_locations_global_hubs_reject_spoke_update_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_reject_spoke_update_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_reject_spoke_update(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsGlobalHubsRejectSpokeUpdateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_global_hubs_reject_spoke_update_builder(
        client, &args.name,
    )?;
    networkconnectivity_projects_locations_global_hubs_reject_spoke_update_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/global/hubs/{hubsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_global_hubs_set_iam_policy_execute()` to send, or `networkconnectivity_projects_locations_global_hubs_set_iam_policy` for simplest API.

pub fn networkconnectivity_projects_locations_global_hubs_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/global/hubs/{hubsId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/global/hubs/{hubsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_global_hubs_set_iam_policy_execute()` or `networkconnectivity_projects_locations_global_hubs_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/global/hubs/{hubsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_global_hubs_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_set_iam_policy_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_global_hubs_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_global_hubs_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_global_hubs_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_global_hubs_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsGlobalHubsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/global/hubs/{hubsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_global_hubs_set_iam_policy_builder()` + `networkconnectivity_projects_locations_global_hubs_set_iam_policy_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_set_iam_policy(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsGlobalHubsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_global_hubs_set_iam_policy_builder(
        client,
        &args.resource,
    )?;
    networkconnectivity_projects_locations_global_hubs_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/global/hubs/{hubsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_global_hubs_test_iam_permissions_execute()` to send, or `networkconnectivity_projects_locations_global_hubs_test_iam_permissions` for simplest API.

pub fn networkconnectivity_projects_locations_global_hubs_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/global/hubs/{hubsId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/global/hubs/{hubsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_global_hubs_test_iam_permissions_execute()` or `networkconnectivity_projects_locations_global_hubs_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/global/hubs/{hubsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_global_hubs_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_test_iam_permissions_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_global_hubs_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_global_hubs_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        networkconnectivity_projects_locations_global_hubs_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_global_hubs_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsGlobalHubsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/global/hubs/{hubsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_global_hubs_test_iam_permissions_builder()` + `networkconnectivity_projects_locations_global_hubs_test_iam_permissions_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsGlobalHubsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_global_hubs_test_iam_permissions_builder(
        client,
        &args.resource,
    )?;
    networkconnectivity_projects_locations_global_hubs_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}/groups/{groupsId}
/// Gets details about a Network Connectivity Center group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_global_hubs_groups_get_execute()` to send, or `networkconnectivity_projects_locations_global_hubs_groups_get` for simplest API.

pub fn networkconnectivity_projects_locations_global_hubs_groups_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/global/hubs/{hubsId}/groups/{groupsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}/groups/{groupsId}
/// Gets details about a Network Connectivity Center group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_global_hubs_groups_get_execute()` or `networkconnectivity_projects_locations_global_hubs_groups_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_groups_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Group>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Group = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}/groups/{groupsId}
/// Gets details about a Network Connectivity Center group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_global_hubs_groups_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_groups_get_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_global_hubs_groups_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_groups_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_global_hubs_groups_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Group>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_global_hubs_groups_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_global_hubs_groups_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsGlobalHubsGroupsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}/groups/{groupsId}
/// Gets details about a Network Connectivity Center group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_global_hubs_groups_get_builder()` + `networkconnectivity_projects_locations_global_hubs_groups_get_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_groups_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_groups_get(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsGlobalHubsGroupsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Group>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        networkconnectivity_projects_locations_global_hubs_groups_get_builder(client, &args.name)?;
    networkconnectivity_projects_locations_global_hubs_groups_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}/groups/{groupsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_global_hubs_groups_get_iam_policy_execute()` to send, or `networkconnectivity_projects_locations_global_hubs_groups_get_iam_policy` for simplest API.

pub fn networkconnectivity_projects_locations_global_hubs_groups_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/global/hubs/{hubsId}/groups/{groupsId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}/groups/{groupsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_global_hubs_groups_get_iam_policy_execute()` or `networkconnectivity_projects_locations_global_hubs_groups_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_groups_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_groups_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}/groups/{groupsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_global_hubs_groups_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_groups_get_iam_policy_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_global_hubs_groups_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_groups_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_global_hubs_groups_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        networkconnectivity_projects_locations_global_hubs_groups_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_global_hubs_groups_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsGlobalHubsGroupsGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}/groups/{groupsId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_global_hubs_groups_get_iam_policy_builder()` + `networkconnectivity_projects_locations_global_hubs_groups_get_iam_policy_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_groups_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_groups_get_iam_policy(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsGlobalHubsGroupsGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_global_hubs_groups_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    networkconnectivity_projects_locations_global_hubs_groups_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}/groups
/// Lists groups in a given hub.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_global_hubs_groups_list_execute()` to send, or `networkconnectivity_projects_locations_global_hubs_groups_list` for simplest API.

pub fn networkconnectivity_projects_locations_global_hubs_groups_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/global/hubs/{hubsId}/groups",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}/groups
/// Lists groups in a given hub.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_global_hubs_groups_list_execute()` or `networkconnectivity_projects_locations_global_hubs_groups_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_groups_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListGroupsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListGroupsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}/groups
/// Lists groups in a given hub.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_global_hubs_groups_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_groups_list_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_global_hubs_groups_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_groups_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_global_hubs_groups_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListGroupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_global_hubs_groups_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_global_hubs_groups_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsGlobalHubsGroupsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}/groups
/// Lists groups in a given hub.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_global_hubs_groups_list_builder()` + `networkconnectivity_projects_locations_global_hubs_groups_list_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_groups_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_groups_list(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsGlobalHubsGroupsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListGroupsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_global_hubs_groups_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    networkconnectivity_projects_locations_global_hubs_groups_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/global/hubs/{hubsId}/groups/{groupsId}
/// Updates the parameters of a Network Connectivity Center group.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_global_hubs_groups_patch_execute()` to send, or `networkconnectivity_projects_locations_global_hubs_groups_patch` for simplest API.

pub fn networkconnectivity_projects_locations_global_hubs_groups_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/global/hubs/{hubsId}/groups/{groupsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/global/hubs/{hubsId}/groups/{groupsId}
/// Updates the parameters of a Network Connectivity Center group.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_global_hubs_groups_patch_execute()` or `networkconnectivity_projects_locations_global_hubs_groups_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_groups_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/global/hubs/{hubsId}/groups/{groupsId}
/// Updates the parameters of a Network Connectivity Center group.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_global_hubs_groups_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_groups_patch_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_global_hubs_groups_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_groups_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_global_hubs_groups_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_global_hubs_groups_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_global_hubs_groups_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsGlobalHubsGroupsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/locations/global/hubs/{hubsId}/groups/{groupsId}
/// Updates the parameters of a Network Connectivity Center group.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_global_hubs_groups_patch_builder()` + `networkconnectivity_projects_locations_global_hubs_groups_patch_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_groups_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_groups_patch(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsGlobalHubsGroupsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_global_hubs_groups_patch_builder(
        client,
        &args.name,
        &args.requestId,
        &args.updateMask,
    )?;
    networkconnectivity_projects_locations_global_hubs_groups_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/global/hubs/{hubsId}/groups/{groupsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_global_hubs_groups_set_iam_policy_execute()` to send, or `networkconnectivity_projects_locations_global_hubs_groups_set_iam_policy` for simplest API.

pub fn networkconnectivity_projects_locations_global_hubs_groups_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/global/hubs/{hubsId}/groups/{groupsId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/global/hubs/{hubsId}/groups/{groupsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_global_hubs_groups_set_iam_policy_execute()` or `networkconnectivity_projects_locations_global_hubs_groups_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_groups_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_groups_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/global/hubs/{hubsId}/groups/{groupsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_global_hubs_groups_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_groups_set_iam_policy_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_global_hubs_groups_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_groups_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_global_hubs_groups_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        networkconnectivity_projects_locations_global_hubs_groups_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_global_hubs_groups_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsGlobalHubsGroupsSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/global/hubs/{hubsId}/groups/{groupsId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_global_hubs_groups_set_iam_policy_builder()` + `networkconnectivity_projects_locations_global_hubs_groups_set_iam_policy_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_groups_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_groups_set_iam_policy(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsGlobalHubsGroupsSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_global_hubs_groups_set_iam_policy_builder(
        client,
        &args.resource,
    )?;
    networkconnectivity_projects_locations_global_hubs_groups_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/global/hubs/{hubsId}/groups/{groupsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_global_hubs_groups_test_iam_permissions_execute()` to send, or `networkconnectivity_projects_locations_global_hubs_groups_test_iam_permissions` for simplest API.

pub fn networkconnectivity_projects_locations_global_hubs_groups_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/global/hubs/{hubsId}/groups/{groupsId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/global/hubs/{hubsId}/groups/{groupsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_global_hubs_groups_test_iam_permissions_execute()` or `networkconnectivity_projects_locations_global_hubs_groups_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_groups_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_groups_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/global/hubs/{hubsId}/groups/{groupsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_global_hubs_groups_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_groups_test_iam_permissions_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_global_hubs_groups_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_groups_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_global_hubs_groups_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_global_hubs_groups_test_iam_permissions_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_global_hubs_groups_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsGlobalHubsGroupsTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/global/hubs/{hubsId}/groups/{groupsId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_global_hubs_groups_test_iam_permissions_builder()` + `networkconnectivity_projects_locations_global_hubs_groups_test_iam_permissions_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_groups_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_groups_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsGlobalHubsGroupsTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        networkconnectivity_projects_locations_global_hubs_groups_test_iam_permissions_builder(
            client,
            &args.resource,
        )?;
    networkconnectivity_projects_locations_global_hubs_groups_test_iam_permissions_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}/routeTables/{routeTablesId}
/// Gets details about a Network Connectivity Center route table.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_global_hubs_route_tables_get_execute()` to send, or `networkconnectivity_projects_locations_global_hubs_route_tables_get` for simplest API.

pub fn networkconnectivity_projects_locations_global_hubs_route_tables_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/global/hubs/{hubsId}/routeTables/{routeTablesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}/routeTables/{routeTablesId}
/// Gets details about a Network Connectivity Center route table.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_global_hubs_route_tables_get_execute()` or `networkconnectivity_projects_locations_global_hubs_route_tables_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_route_tables_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_route_tables_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RouteTable>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RouteTable = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}/routeTables/{routeTablesId}
/// Gets details about a Network Connectivity Center route table.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_global_hubs_route_tables_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_route_tables_get_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_global_hubs_route_tables_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_route_tables_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_global_hubs_route_tables_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RouteTable>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_global_hubs_route_tables_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_global_hubs_route_tables_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsGlobalHubsRouteTablesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}/routeTables/{routeTablesId}
/// Gets details about a Network Connectivity Center route table.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_global_hubs_route_tables_get_builder()` + `networkconnectivity_projects_locations_global_hubs_route_tables_get_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_route_tables_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_route_tables_get(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsGlobalHubsRouteTablesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RouteTable>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_global_hubs_route_tables_get_builder(
        client, &args.name,
    )?;
    networkconnectivity_projects_locations_global_hubs_route_tables_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}/routeTables
/// Lists route tables in a given hub.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_global_hubs_route_tables_list_execute()` to send, or `networkconnectivity_projects_locations_global_hubs_route_tables_list` for simplest API.

pub fn networkconnectivity_projects_locations_global_hubs_route_tables_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/global/hubs/{hubsId}/routeTables",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}/routeTables
/// Lists route tables in a given hub.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_global_hubs_route_tables_list_execute()` or `networkconnectivity_projects_locations_global_hubs_route_tables_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_route_tables_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_route_tables_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListRouteTablesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListRouteTablesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}/routeTables
/// Lists route tables in a given hub.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_global_hubs_route_tables_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_route_tables_list_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_global_hubs_route_tables_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_route_tables_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_global_hubs_route_tables_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRouteTablesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_global_hubs_route_tables_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_global_hubs_route_tables_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsGlobalHubsRouteTablesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}/routeTables
/// Lists route tables in a given hub.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_global_hubs_route_tables_list_builder()` + `networkconnectivity_projects_locations_global_hubs_route_tables_list_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_route_tables_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_route_tables_list(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsGlobalHubsRouteTablesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRouteTablesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_global_hubs_route_tables_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    networkconnectivity_projects_locations_global_hubs_route_tables_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}/routeTables/{routeTablesId}/routes/{routesId}
/// Gets details about the specified route.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_global_hubs_route_tables_routes_get_execute()` to send, or `networkconnectivity_projects_locations_global_hubs_route_tables_routes_get` for simplest API.

pub fn networkconnectivity_projects_locations_global_hubs_route_tables_routes_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/global/hubs/{hubsId}/routeTables/{routeTablesId}/routes/{routesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}/routeTables/{routeTablesId}/routes/{routesId}
/// Gets details about the specified route.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_global_hubs_route_tables_routes_get_execute()` or `networkconnectivity_projects_locations_global_hubs_route_tables_routes_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_route_tables_routes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_route_tables_routes_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Route>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Route = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}/routeTables/{routeTablesId}/routes/{routesId}
/// Gets details about the specified route.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_global_hubs_route_tables_routes_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_route_tables_routes_get_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_global_hubs_route_tables_routes_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_route_tables_routes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_global_hubs_route_tables_routes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Route>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        networkconnectivity_projects_locations_global_hubs_route_tables_routes_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_global_hubs_route_tables_routes_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsGlobalHubsRouteTablesRoutesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}/routeTables/{routeTablesId}/routes/{routesId}
/// Gets details about the specified route.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_global_hubs_route_tables_routes_get_builder()` + `networkconnectivity_projects_locations_global_hubs_route_tables_routes_get_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_route_tables_routes_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_route_tables_routes_get(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsGlobalHubsRouteTablesRoutesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Route>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        networkconnectivity_projects_locations_global_hubs_route_tables_routes_get_builder(
            client, &args.name,
        )?;
    networkconnectivity_projects_locations_global_hubs_route_tables_routes_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}/routeTables/{routeTablesId}/routes
/// Lists routes in a given route table.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_global_hubs_route_tables_routes_list_execute()` to send, or `networkconnectivity_projects_locations_global_hubs_route_tables_routes_list` for simplest API.

pub fn networkconnectivity_projects_locations_global_hubs_route_tables_routes_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/global/hubs/{hubsId}/routeTables/{routeTablesId}/routes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}/routeTables/{routeTablesId}/routes
/// Lists routes in a given route table.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_global_hubs_route_tables_routes_list_execute()` or `networkconnectivity_projects_locations_global_hubs_route_tables_routes_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_route_tables_routes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_route_tables_routes_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListRoutesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListRoutesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}/routeTables/{routeTablesId}/routes
/// Lists routes in a given route table.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_global_hubs_route_tables_routes_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_route_tables_routes_list_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_global_hubs_route_tables_routes_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_hubs_route_tables_routes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_global_hubs_route_tables_routes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRoutesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        networkconnectivity_projects_locations_global_hubs_route_tables_routes_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_global_hubs_route_tables_routes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsGlobalHubsRouteTablesRoutesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/global/hubs/{hubsId}/routeTables/{routeTablesId}/routes
/// Lists routes in a given route table.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_global_hubs_route_tables_routes_list_builder()` + `networkconnectivity_projects_locations_global_hubs_route_tables_routes_list_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_global_hubs_route_tables_routes_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_hubs_route_tables_routes_list(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsGlobalHubsRouteTablesRoutesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListRoutesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        networkconnectivity_projects_locations_global_hubs_route_tables_routes_list_builder(
            client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )?;
    networkconnectivity_projects_locations_global_hubs_route_tables_routes_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/global/policyBasedRoutes
/// Creates a new policy-based route in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_global_policy_based_routes_create_execute()` to send, or `networkconnectivity_projects_locations_global_policy_based_routes_create` for simplest API.

pub fn networkconnectivity_projects_locations_global_policy_based_routes_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    policyBasedRouteId: &Option<String>,
    requestId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/global/policyBasedRoutes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = policyBasedRouteId.as_ref() {
        query_parts.push(format!("policyBasedRouteId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/global/policyBasedRoutes
/// Creates a new policy-based route in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_global_policy_based_routes_create_execute()` or `networkconnectivity_projects_locations_global_policy_based_routes_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_policy_based_routes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_policy_based_routes_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/global/policyBasedRoutes
/// Creates a new policy-based route in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_global_policy_based_routes_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_global_policy_based_routes_create_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_global_policy_based_routes_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_policy_based_routes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_global_policy_based_routes_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        networkconnectivity_projects_locations_global_policy_based_routes_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_global_policy_based_routes_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: policyBasedRouteId
    pub policyBasedRouteId: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/global/policyBasedRoutes
/// Creates a new policy-based route in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_global_policy_based_routes_create_builder()` + `networkconnectivity_projects_locations_global_policy_based_routes_create_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_global_policy_based_routes_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_policy_based_routes_create(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_global_policy_based_routes_create_builder(
        client,
        &args.parent,
        &args.policyBasedRouteId,
        &args.requestId,
    )?;
    networkconnectivity_projects_locations_global_policy_based_routes_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/global/policyBasedRoutes/{policyBasedRoutesId}
/// Deletes a single policy-based route.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_global_policy_based_routes_delete_execute()` to send, or `networkconnectivity_projects_locations_global_policy_based_routes_delete` for simplest API.

pub fn networkconnectivity_projects_locations_global_policy_based_routes_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/global/policyBasedRoutes/{policyBasedRoutesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/global/policyBasedRoutes/{policyBasedRoutesId}
/// Deletes a single policy-based route.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_global_policy_based_routes_delete_execute()` or `networkconnectivity_projects_locations_global_policy_based_routes_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_policy_based_routes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_policy_based_routes_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/global/policyBasedRoutes/{policyBasedRoutesId}
/// Deletes a single policy-based route.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_global_policy_based_routes_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_global_policy_based_routes_delete_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_global_policy_based_routes_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_policy_based_routes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_global_policy_based_routes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        networkconnectivity_projects_locations_global_policy_based_routes_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_global_policy_based_routes_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// DELETE v1/projects/{projectsId}/locations/global/policyBasedRoutes/{policyBasedRoutesId}
/// Deletes a single policy-based route.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_global_policy_based_routes_delete_builder()` + `networkconnectivity_projects_locations_global_policy_based_routes_delete_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_global_policy_based_routes_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_policy_based_routes_delete(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_global_policy_based_routes_delete_builder(
        client,
        &args.name,
        &args.requestId,
    )?;
    networkconnectivity_projects_locations_global_policy_based_routes_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/policyBasedRoutes/{policyBasedRoutesId}
/// Gets details of a single policy-based route.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_global_policy_based_routes_get_execute()` to send, or `networkconnectivity_projects_locations_global_policy_based_routes_get` for simplest API.

pub fn networkconnectivity_projects_locations_global_policy_based_routes_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/global/policyBasedRoutes/{policyBasedRoutesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/global/policyBasedRoutes/{policyBasedRoutesId}
/// Gets details of a single policy-based route.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_global_policy_based_routes_get_execute()` or `networkconnectivity_projects_locations_global_policy_based_routes_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_policy_based_routes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_policy_based_routes_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PolicyBasedRoute>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: PolicyBasedRoute = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/policyBasedRoutes/{policyBasedRoutesId}
/// Gets details of a single policy-based route.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_global_policy_based_routes_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_global_policy_based_routes_get_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_global_policy_based_routes_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_policy_based_routes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_global_policy_based_routes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PolicyBasedRoute>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_global_policy_based_routes_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_global_policy_based_routes_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/global/policyBasedRoutes/{policyBasedRoutesId}
/// Gets details of a single policy-based route.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_global_policy_based_routes_get_builder()` + `networkconnectivity_projects_locations_global_policy_based_routes_get_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_global_policy_based_routes_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_policy_based_routes_get(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<PolicyBasedRoute>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_global_policy_based_routes_get_builder(
        client, &args.name,
    )?;
    networkconnectivity_projects_locations_global_policy_based_routes_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/global/policyBasedRoutes/{policyBasedRoutesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_global_policy_based_routes_get_iam_policy_execute()` to send, or `networkconnectivity_projects_locations_global_policy_based_routes_get_iam_policy` for simplest API.

pub fn networkconnectivity_projects_locations_global_policy_based_routes_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/global/policyBasedRoutes/{policyBasedRoutesId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/global/policyBasedRoutes/{policyBasedRoutesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_global_policy_based_routes_get_iam_policy_execute()` or `networkconnectivity_projects_locations_global_policy_based_routes_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_policy_based_routes_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_policy_based_routes_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/policyBasedRoutes/{policyBasedRoutesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_global_policy_based_routes_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_global_policy_based_routes_get_iam_policy_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_global_policy_based_routes_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_policy_based_routes_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_global_policy_based_routes_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        networkconnectivity_projects_locations_global_policy_based_routes_get_iam_policy_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_global_policy_based_routes_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/global/policyBasedRoutes/{policyBasedRoutesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_global_policy_based_routes_get_iam_policy_builder()` + `networkconnectivity_projects_locations_global_policy_based_routes_get_iam_policy_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_global_policy_based_routes_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_policy_based_routes_get_iam_policy(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        networkconnectivity_projects_locations_global_policy_based_routes_get_iam_policy_builder(
            client,
            &args.resource,
            &args.options_requestedPolicyVersion,
        )?;
    networkconnectivity_projects_locations_global_policy_based_routes_get_iam_policy_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/global/policyBasedRoutes
/// Lists policy-based routes in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_global_policy_based_routes_list_execute()` to send, or `networkconnectivity_projects_locations_global_policy_based_routes_list` for simplest API.

pub fn networkconnectivity_projects_locations_global_policy_based_routes_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/global/policyBasedRoutes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/global/policyBasedRoutes
/// Lists policy-based routes in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_global_policy_based_routes_list_execute()` or `networkconnectivity_projects_locations_global_policy_based_routes_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_policy_based_routes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_policy_based_routes_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListPolicyBasedRoutesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListPolicyBasedRoutesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/global/policyBasedRoutes
/// Lists policy-based routes in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_global_policy_based_routes_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_global_policy_based_routes_list_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_global_policy_based_routes_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_policy_based_routes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_global_policy_based_routes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListPolicyBasedRoutesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        networkconnectivity_projects_locations_global_policy_based_routes_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_global_policy_based_routes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/global/policyBasedRoutes
/// Lists policy-based routes in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_global_policy_based_routes_list_builder()` + `networkconnectivity_projects_locations_global_policy_based_routes_list_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_global_policy_based_routes_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_policy_based_routes_list(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListPolicyBasedRoutesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_global_policy_based_routes_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    networkconnectivity_projects_locations_global_policy_based_routes_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/global/policyBasedRoutes/{policyBasedRoutesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_global_policy_based_routes_set_iam_policy_execute()` to send, or `networkconnectivity_projects_locations_global_policy_based_routes_set_iam_policy` for simplest API.

pub fn networkconnectivity_projects_locations_global_policy_based_routes_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/global/policyBasedRoutes/{policyBasedRoutesId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/global/policyBasedRoutes/{policyBasedRoutesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_global_policy_based_routes_set_iam_policy_execute()` or `networkconnectivity_projects_locations_global_policy_based_routes_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_policy_based_routes_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_policy_based_routes_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/global/policyBasedRoutes/{policyBasedRoutesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_global_policy_based_routes_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_global_policy_based_routes_set_iam_policy_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_global_policy_based_routes_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_policy_based_routes_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_global_policy_based_routes_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task =
        networkconnectivity_projects_locations_global_policy_based_routes_set_iam_policy_task(
            builder,
        )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_global_policy_based_routes_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/global/policyBasedRoutes/{policyBasedRoutesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_global_policy_based_routes_set_iam_policy_builder()` + `networkconnectivity_projects_locations_global_policy_based_routes_set_iam_policy_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_global_policy_based_routes_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_policy_based_routes_set_iam_policy(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        networkconnectivity_projects_locations_global_policy_based_routes_set_iam_policy_builder(
            client,
            &args.resource,
        )?;
    networkconnectivity_projects_locations_global_policy_based_routes_set_iam_policy_execute(
        builder,
    )
}

/// POST v1/projects/{projectsId}/locations/global/policyBasedRoutes/{policyBasedRoutesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_global_policy_based_routes_test_iam_permissions_execute()` to send, or `networkconnectivity_projects_locations_global_policy_based_routes_test_iam_permissions` for simplest API.

pub fn networkconnectivity_projects_locations_global_policy_based_routes_test_iam_permissions_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/global/policyBasedRoutes/{policyBasedRoutesId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/global/policyBasedRoutes/{policyBasedRoutesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_global_policy_based_routes_test_iam_permissions_execute()` or `networkconnectivity_projects_locations_global_policy_based_routes_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_policy_based_routes_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_policy_based_routes_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/global/policyBasedRoutes/{policyBasedRoutesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_global_policy_based_routes_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_global_policy_based_routes_test_iam_permissions_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_global_policy_based_routes_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_global_policy_based_routes_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_global_policy_based_routes_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_global_policy_based_routes_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_global_policy_based_routes_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/global/policyBasedRoutes/{policyBasedRoutesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_global_policy_based_routes_test_iam_permissions_builder()` + `networkconnectivity_projects_locations_global_policy_based_routes_test_iam_permissions_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_global_policy_based_routes_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_global_policy_based_routes_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_global_policy_based_routes_test_iam_permissions_builder(client, &args.resource)?;
    networkconnectivity_projects_locations_global_policy_based_routes_test_iam_permissions_execute(
        builder,
    )
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/internalRanges
/// Creates a new internal range in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_internal_ranges_create_execute()` to send, or `networkconnectivity_projects_locations_internal_ranges_create` for simplest API.

pub fn networkconnectivity_projects_locations_internal_ranges_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    internalRangeId: &Option<String>,
    requestId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/internalRanges",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = internalRangeId.as_ref() {
        query_parts.push(format!("internalRangeId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/internalRanges
/// Creates a new internal range in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_internal_ranges_create_execute()` or `networkconnectivity_projects_locations_internal_ranges_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_internal_ranges_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_internal_ranges_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/internalRanges
/// Creates a new internal range in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_internal_ranges_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_internal_ranges_create_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_internal_ranges_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_internal_ranges_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_internal_ranges_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_internal_ranges_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_internal_ranges_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsInternalRangesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: internalRangeId
    pub internalRangeId: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/internalRanges
/// Creates a new internal range in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_internal_ranges_create_builder()` + `networkconnectivity_projects_locations_internal_ranges_create_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_internal_ranges_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_internal_ranges_create(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsInternalRangesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_internal_ranges_create_builder(
        client,
        &args.parent,
        &args.internalRangeId,
        &args.requestId,
    )?;
    networkconnectivity_projects_locations_internal_ranges_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/internalRanges/{internalRangesId}
/// Deletes a single internal range.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_internal_ranges_delete_execute()` to send, or `networkconnectivity_projects_locations_internal_ranges_delete` for simplest API.

pub fn networkconnectivity_projects_locations_internal_ranges_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/internalRanges/{internalRangesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/internalRanges/{internalRangesId}
/// Deletes a single internal range.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_internal_ranges_delete_execute()` or `networkconnectivity_projects_locations_internal_ranges_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_internal_ranges_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_internal_ranges_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/internalRanges/{internalRangesId}
/// Deletes a single internal range.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_internal_ranges_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_internal_ranges_delete_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_internal_ranges_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_internal_ranges_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_internal_ranges_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_internal_ranges_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_internal_ranges_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsInternalRangesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/internalRanges/{internalRangesId}
/// Deletes a single internal range.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_internal_ranges_delete_builder()` + `networkconnectivity_projects_locations_internal_ranges_delete_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_internal_ranges_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_internal_ranges_delete(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsInternalRangesDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_internal_ranges_delete_builder(
        client,
        &args.name,
        &args.requestId,
    )?;
    networkconnectivity_projects_locations_internal_ranges_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/internalRanges/{internalRangesId}
/// Gets details of a single internal range.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_internal_ranges_get_execute()` to send, or `networkconnectivity_projects_locations_internal_ranges_get` for simplest API.

pub fn networkconnectivity_projects_locations_internal_ranges_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/internalRanges/{internalRangesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/internalRanges/{internalRangesId}
/// Gets details of a single internal range.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_internal_ranges_get_execute()` or `networkconnectivity_projects_locations_internal_ranges_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_internal_ranges_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_internal_ranges_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InternalRange>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: InternalRange = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/internalRanges/{internalRangesId}
/// Gets details of a single internal range.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_internal_ranges_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_internal_ranges_get_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_internal_ranges_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_internal_ranges_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_internal_ranges_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InternalRange>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_internal_ranges_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_internal_ranges_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsInternalRangesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/internalRanges/{internalRangesId}
/// Gets details of a single internal range.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_internal_ranges_get_builder()` + `networkconnectivity_projects_locations_internal_ranges_get_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_internal_ranges_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_internal_ranges_get(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsInternalRangesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<InternalRange>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        networkconnectivity_projects_locations_internal_ranges_get_builder(client, &args.name)?;
    networkconnectivity_projects_locations_internal_ranges_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/internalRanges/{internalRangesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_internal_ranges_get_iam_policy_execute()` to send, or `networkconnectivity_projects_locations_internal_ranges_get_iam_policy` for simplest API.

pub fn networkconnectivity_projects_locations_internal_ranges_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/internalRanges/{internalRangesId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/internalRanges/{internalRangesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_internal_ranges_get_iam_policy_execute()` or `networkconnectivity_projects_locations_internal_ranges_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_internal_ranges_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_internal_ranges_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/internalRanges/{internalRangesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_internal_ranges_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_internal_ranges_get_iam_policy_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_internal_ranges_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_internal_ranges_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_internal_ranges_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_internal_ranges_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_internal_ranges_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsInternalRangesGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/internalRanges/{internalRangesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_internal_ranges_get_iam_policy_builder()` + `networkconnectivity_projects_locations_internal_ranges_get_iam_policy_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_internal_ranges_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_internal_ranges_get_iam_policy(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsInternalRangesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_internal_ranges_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    networkconnectivity_projects_locations_internal_ranges_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/internalRanges
/// Lists internal ranges in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_internal_ranges_list_execute()` to send, or `networkconnectivity_projects_locations_internal_ranges_list` for simplest API.

pub fn networkconnectivity_projects_locations_internal_ranges_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/internalRanges",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/internalRanges
/// Lists internal ranges in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_internal_ranges_list_execute()` or `networkconnectivity_projects_locations_internal_ranges_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_internal_ranges_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_internal_ranges_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListInternalRangesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListInternalRangesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/internalRanges
/// Lists internal ranges in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_internal_ranges_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_internal_ranges_list_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_internal_ranges_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_internal_ranges_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_internal_ranges_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListInternalRangesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_internal_ranges_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_internal_ranges_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsInternalRangesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/internalRanges
/// Lists internal ranges in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_internal_ranges_list_builder()` + `networkconnectivity_projects_locations_internal_ranges_list_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_internal_ranges_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_internal_ranges_list(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsInternalRangesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListInternalRangesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_internal_ranges_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    networkconnectivity_projects_locations_internal_ranges_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/internalRanges/{internalRangesId}
/// Updates the parameters of a single internal range.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_internal_ranges_patch_execute()` to send, or `networkconnectivity_projects_locations_internal_ranges_patch` for simplest API.

pub fn networkconnectivity_projects_locations_internal_ranges_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/internalRanges/{internalRangesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/internalRanges/{internalRangesId}
/// Updates the parameters of a single internal range.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_internal_ranges_patch_execute()` or `networkconnectivity_projects_locations_internal_ranges_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_internal_ranges_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_internal_ranges_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/internalRanges/{internalRangesId}
/// Updates the parameters of a single internal range.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_internal_ranges_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_internal_ranges_patch_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_internal_ranges_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_internal_ranges_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_internal_ranges_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_internal_ranges_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_internal_ranges_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsInternalRangesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/internalRanges/{internalRangesId}
/// Updates the parameters of a single internal range.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_internal_ranges_patch_builder()` + `networkconnectivity_projects_locations_internal_ranges_patch_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_internal_ranges_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_internal_ranges_patch(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsInternalRangesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_internal_ranges_patch_builder(
        client,
        &args.name,
        &args.requestId,
        &args.updateMask,
    )?;
    networkconnectivity_projects_locations_internal_ranges_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/internalRanges/{internalRangesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_internal_ranges_set_iam_policy_execute()` to send, or `networkconnectivity_projects_locations_internal_ranges_set_iam_policy` for simplest API.

pub fn networkconnectivity_projects_locations_internal_ranges_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/internalRanges/{internalRangesId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/internalRanges/{internalRangesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_internal_ranges_set_iam_policy_execute()` or `networkconnectivity_projects_locations_internal_ranges_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_internal_ranges_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_internal_ranges_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/internalRanges/{internalRangesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_internal_ranges_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_internal_ranges_set_iam_policy_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_internal_ranges_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_internal_ranges_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_internal_ranges_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_internal_ranges_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_internal_ranges_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsInternalRangesSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/internalRanges/{internalRangesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_internal_ranges_set_iam_policy_builder()` + `networkconnectivity_projects_locations_internal_ranges_set_iam_policy_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_internal_ranges_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_internal_ranges_set_iam_policy(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsInternalRangesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_internal_ranges_set_iam_policy_builder(
        client,
        &args.resource,
    )?;
    networkconnectivity_projects_locations_internal_ranges_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/internalRanges/{internalRangesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_internal_ranges_test_iam_permissions_execute()` to send, or `networkconnectivity_projects_locations_internal_ranges_test_iam_permissions` for simplest API.

pub fn networkconnectivity_projects_locations_internal_ranges_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/internalRanges/{internalRangesId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/internalRanges/{internalRangesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_internal_ranges_test_iam_permissions_execute()` or `networkconnectivity_projects_locations_internal_ranges_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_internal_ranges_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_internal_ranges_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/internalRanges/{internalRangesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_internal_ranges_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_internal_ranges_test_iam_permissions_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_internal_ranges_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_internal_ranges_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_internal_ranges_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        networkconnectivity_projects_locations_internal_ranges_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_internal_ranges_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsInternalRangesTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/internalRanges/{internalRangesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_internal_ranges_test_iam_permissions_builder()` + `networkconnectivity_projects_locations_internal_ranges_test_iam_permissions_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_internal_ranges_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_internal_ranges_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsInternalRangesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        networkconnectivity_projects_locations_internal_ranges_test_iam_permissions_builder(
            client,
            &args.resource,
        )?;
    networkconnectivity_projects_locations_internal_ranges_test_iam_permissions_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs
/// Creates a MulticloudDataTransferConfig resource in a specified project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_create_execute()` to send, or `networkconnectivity_projects_locations_multicloud_data_transfer_configs_create` for simplest API.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    multicloudDataTransferConfigId: &Option<String>,
    requestId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/multicloudDataTransferConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = multicloudDataTransferConfigId.as_ref() {
        query_parts.push(format!("multicloudDataTransferConfigId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs
/// Creates a MulticloudDataTransferConfig resource in a specified project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_create_execute()` or `networkconnectivity_projects_locations_multicloud_data_transfer_configs_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_multicloud_data_transfer_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs
/// Creates a MulticloudDataTransferConfig resource in a specified project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_create_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_multicloud_data_transfer_configs_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_multicloud_data_transfer_configs_create_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_multicloud_data_transfer_configs_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: multicloudDataTransferConfigId
    pub multicloudDataTransferConfigId: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs
/// Creates a MulticloudDataTransferConfig resource in a specified project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_create_builder()` + `networkconnectivity_projects_locations_multicloud_data_transfer_configs_create_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_create(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        networkconnectivity_projects_locations_multicloud_data_transfer_configs_create_builder(
            client,
            &args.parent,
            &args.multicloudDataTransferConfigId,
            &args.requestId,
        )?;
    networkconnectivity_projects_locations_multicloud_data_transfer_configs_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}
/// Deletes a MulticloudDataTransferConfig resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_delete_execute()` to send, or `networkconnectivity_projects_locations_multicloud_data_transfer_configs_delete` for simplest API.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    etag: &Option<String>,
    requestId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = etag.as_ref() {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}
/// Deletes a MulticloudDataTransferConfig resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_delete_execute()` or `networkconnectivity_projects_locations_multicloud_data_transfer_configs_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_multicloud_data_transfer_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}
/// Deletes a MulticloudDataTransferConfig resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_delete_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_multicloud_data_transfer_configs_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_multicloud_data_transfer_configs_delete_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_multicloud_data_transfer_configs_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: etag
    pub etag: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}
/// Deletes a MulticloudDataTransferConfig resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_delete_builder()` + `networkconnectivity_projects_locations_multicloud_data_transfer_configs_delete_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_delete(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        networkconnectivity_projects_locations_multicloud_data_transfer_configs_delete_builder(
            client,
            &args.name,
            &args.etag,
            &args.requestId,
        )?;
    networkconnectivity_projects_locations_multicloud_data_transfer_configs_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}
/// Gets the details of a MulticloudDataTransferConfig resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_get_execute()` to send, or `networkconnectivity_projects_locations_multicloud_data_transfer_configs_get` for simplest API.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}
/// Gets the details of a MulticloudDataTransferConfig resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_get_execute()` or `networkconnectivity_projects_locations_multicloud_data_transfer_configs_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_multicloud_data_transfer_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MulticloudDataTransferConfig>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MulticloudDataTransferConfig = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}
/// Gets the details of a MulticloudDataTransferConfig resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_get_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_multicloud_data_transfer_configs_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<MulticloudDataTransferConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        networkconnectivity_projects_locations_multicloud_data_transfer_configs_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_multicloud_data_transfer_configs_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}
/// Gets the details of a MulticloudDataTransferConfig resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_get_builder()` + `networkconnectivity_projects_locations_multicloud_data_transfer_configs_get_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_get(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<MulticloudDataTransferConfig>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        networkconnectivity_projects_locations_multicloud_data_transfer_configs_get_builder(
            client, &args.name,
        )?;
    networkconnectivity_projects_locations_multicloud_data_transfer_configs_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs
/// Lists the MulticloudDataTransferConfig resources in a specified project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_list_execute()` to send, or `networkconnectivity_projects_locations_multicloud_data_transfer_configs_list` for simplest API.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    returnPartialSuccess: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/multicloudDataTransferConfigs",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs
/// Lists the MulticloudDataTransferConfig resources in a specified project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_list_execute()` or `networkconnectivity_projects_locations_multicloud_data_transfer_configs_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_multicloud_data_transfer_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListMulticloudDataTransferConfigsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListMulticloudDataTransferConfigsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs
/// Lists the MulticloudDataTransferConfig resources in a specified project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_list_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_multicloud_data_transfer_configs_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListMulticloudDataTransferConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        networkconnectivity_projects_locations_multicloud_data_transfer_configs_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_multicloud_data_transfer_configs_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs
/// Lists the MulticloudDataTransferConfig resources in a specified project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_list_builder()` + `networkconnectivity_projects_locations_multicloud_data_transfer_configs_list_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_list(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListMulticloudDataTransferConfigsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        networkconnectivity_projects_locations_multicloud_data_transfer_configs_list_builder(
            client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
            &args.returnPartialSuccess,
        )?;
    networkconnectivity_projects_locations_multicloud_data_transfer_configs_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}
/// Updates a MulticloudDataTransferConfig resource in a specified project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_patch_execute()` to send, or `networkconnectivity_projects_locations_multicloud_data_transfer_configs_patch` for simplest API.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}
/// Updates a MulticloudDataTransferConfig resource in a specified project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_patch_execute()` or `networkconnectivity_projects_locations_multicloud_data_transfer_configs_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_multicloud_data_transfer_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}
/// Updates a MulticloudDataTransferConfig resource in a specified project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_patch_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_multicloud_data_transfer_configs_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_multicloud_data_transfer_configs_patch_task(
        builder,
    )?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_multicloud_data_transfer_configs_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}
/// Updates a MulticloudDataTransferConfig resource in a specified project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_patch_builder()` + `networkconnectivity_projects_locations_multicloud_data_transfer_configs_patch_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_patch(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        networkconnectivity_projects_locations_multicloud_data_transfer_configs_patch_builder(
            client,
            &args.name,
            &args.requestId,
            &args.updateMask,
        )?;
    networkconnectivity_projects_locations_multicloud_data_transfer_configs_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}/destinations
/// Creates a Destination resource in a specified project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_create_execute()` to send, or `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_create` for simplest API.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_create_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    destinationId: &Option<String>,
    requestId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}/destinations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = destinationId.as_ref() {
        query_parts.push(format!("destinationId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}/destinations
/// Creates a Destination resource in a specified project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_create_execute()` or `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}/destinations
/// Creates a Destination resource in a specified project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_create_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsDestinationsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: destinationId
    pub destinationId: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}/destinations
/// Creates a Destination resource in a specified project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_create_builder()` + `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_create_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_create(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsDestinationsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_create_builder(client, &args.parent, &args.destinationId, &args.requestId)?;
    networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}/destinations/{destinationsId}
/// Deletes a Destination resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_delete_execute()` to send, or `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_delete` for simplest API.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_delete_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
    etag: &Option<String>,
    requestId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}/destinations/{destinationsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = etag.as_ref() {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}/destinations/{destinationsId}
/// Deletes a Destination resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_delete_execute()` or `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}/destinations/{destinationsId}
/// Deletes a Destination resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_delete_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsDestinationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: etag
    pub etag: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}/destinations/{destinationsId}
/// Deletes a Destination resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_delete_builder()` + `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_delete_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_delete(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsDestinationsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_delete_builder(client, &args.name, &args.etag, &args.requestId)?;
    networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}/destinations/{destinationsId}
/// Gets the details of a Destination resource.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_get_execute()` to send, or `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_get` for simplest API.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_get_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}/destinations/{destinationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}/destinations/{destinationsId}
/// Gets the details of a Destination resource.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_get_execute()` or `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Destination>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Destination = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}/destinations/{destinationsId}
/// Gets the details of a Destination resource.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_get_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Destination>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsDestinationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}/destinations/{destinationsId}
/// Gets the details of a Destination resource.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_get_builder()` + `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_get_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_get(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsDestinationsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Destination>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_get_builder(client, &args.name)?;
    networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_get_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}/destinations
/// Lists the Destination resources in a specified project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_list_execute()` to send, or `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_list` for simplest API.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_list_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    returnPartialSuccess: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}/destinations",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}/destinations
/// Lists the Destination resources in a specified project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_list_execute()` or `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDestinationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListDestinationsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}/destinations
/// Lists the Destination resources in a specified project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_list_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDestinationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsDestinationsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}/destinations
/// Lists the Destination resources in a specified project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_list_builder()` + `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_list_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_list(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsDestinationsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListDestinationsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_list_builder(client, &args.parent, &args.filter, &args.orderBy, &args.pageSize, &args.pageToken, &args.returnPartialSuccess)?;
    networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}/destinations/{destinationsId}
/// Updates a Destination resource in a specified project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_patch_execute()` to send, or `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_patch` for simplest API.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_patch_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}/destinations/{destinationsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}/destinations/{destinationsId}
/// Updates a Destination resource in a specified project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_patch_execute()` or `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}/destinations/{destinationsId}
/// Updates a Destination resource in a specified project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_patch_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsDestinationsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferConfigs/{multicloudDataTransferConfigsId}/destinations/{destinationsId}
/// Updates a Destination resource in a specified project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_patch_builder()` + `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_patch_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_patch(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsDestinationsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_patch_builder(client, &args.name, &args.requestId, &args.updateMask)?;
    networkconnectivity_projects_locations_multicloud_data_transfer_configs_destinations_patch_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferSupportedServices/{multicloudDataTransferSupportedServicesId}
/// Gets the details of a service that is supported for Data Transfer Essentials.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_get_execute()` to send, or `networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_get` for simplest API.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_get_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/multicloudDataTransferSupportedServices/{multicloudDataTransferSupportedServicesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferSupportedServices/{multicloudDataTransferSupportedServicesId}
/// Gets the details of a service that is supported for Data Transfer Essentials.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_get_execute()` or `networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<MulticloudDataTransferSupportedService>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: MulticloudDataTransferSupportedService = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferSupportedServices/{multicloudDataTransferSupportedServicesId}
/// Gets the details of a service that is supported for Data Transfer Essentials.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_get_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<MulticloudDataTransferSupportedService>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsMulticloudDataTransferSupportedServicesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferSupportedServices/{multicloudDataTransferSupportedServicesId}
/// Gets the details of a service that is supported for Data Transfer Essentials.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_get_builder()` + `networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_get_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_get(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsMulticloudDataTransferSupportedServicesGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<MulticloudDataTransferSupportedService>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_get_builder(client, &args.name)?;
    networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_get_execute(
        builder,
    )
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferSupportedServices
/// Lists the services in the project for a region that are supported for Data Transfer Essentials.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_list_execute()` to send, or `networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_list` for simplest API.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_list_builder<
    R,
>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/multicloudDataTransferSupportedServices",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferSupportedServices
/// Lists the services in the project for a region that are supported for Data Transfer Essentials.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_list_execute()` or `networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListMulticloudDataTransferSupportedServicesResponse>,
                ApiError,
            >,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListMulticloudDataTransferSupportedServicesResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferSupportedServices
/// Lists the services in the project for a region that are supported for Data Transfer Essentials.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_list_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListMulticloudDataTransferSupportedServicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsMulticloudDataTransferSupportedServicesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/multicloudDataTransferSupportedServices
/// Lists the services in the project for a region that are supported for Data Transfer Essentials.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_list_builder()` + `networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_list_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_list(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsMulticloudDataTransferSupportedServicesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListMulticloudDataTransferSupportedServicesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_list_builder(client, &args.parent, &args.pageSize, &args.pageToken)?;
    networkconnectivity_projects_locations_multicloud_data_transfer_supported_services_list_execute(
        builder,
    )
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_operations_cancel_execute()` to send, or `networkconnectivity_projects_locations_operations_cancel` for simplest API.

pub fn networkconnectivity_projects_locations_operations_cancel_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}:cancel",
        name,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_operations_cancel_execute()` or `networkconnectivity_projects_locations_operations_cancel`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_operations_cancel_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_operations_cancel_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_operations_cancel_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_operations_cancel()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_operations_cancel_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_operations_cancel_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_operations_cancel_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_operations_cancel`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsOperationsCancelArgs {
    /// Path parameter: name
    pub name: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}:cancel
/// Starts asynchronous cancellation on a long-running operation. The server makes a best effort to cancel the operation, but success is not guaranteed. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED. Clients can use Operations.GetOperation or other methods to check whether the cancellation succeeded or whether the operation completed despite cancellation. On successful cancellation, the operation is not deleted; instead, it becomes an operation with an Operation.error value with a google.rpc.Status.code of 1, corresponding to Code.CANCELLED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_operations_cancel_builder()` + `networkconnectivity_projects_locations_operations_cancel_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_operations_cancel_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_operations_cancel(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsOperationsCancelArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        networkconnectivity_projects_locations_operations_cancel_builder(client, &args.name)?;
    networkconnectivity_projects_locations_operations_cancel_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_operations_delete_execute()` to send, or `networkconnectivity_projects_locations_operations_delete` for simplest API.

pub fn networkconnectivity_projects_locations_operations_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .delete(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_operations_delete_execute()` or `networkconnectivity_projects_locations_operations_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_operations_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Empty = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_operations_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_operations_delete_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_operations_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_operations_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_operations_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_operations_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_operations_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsOperationsDeleteArgs {
    /// Path parameter: name
    pub name: String,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Deletes a long-running operation. This method indicates that the client is no longer interested in the operation result. It does not cancel the operation. If the server doesn''t support this method, it returns google.rpc.Code.UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_operations_delete_builder()` + `networkconnectivity_projects_locations_operations_delete_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_operations_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_operations_delete(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsOperationsDeleteArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Empty>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        networkconnectivity_projects_locations_operations_delete_builder(client, &args.name)?;
    networkconnectivity_projects_locations_operations_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_operations_get_execute()` to send, or `networkconnectivity_projects_locations_operations_get` for simplest API.

pub fn networkconnectivity_projects_locations_operations_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/operations/{operationsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_operations_get_execute()` or `networkconnectivity_projects_locations_operations_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_operations_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_operations_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_operations_get_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_operations_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_operations_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_operations_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_operations_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_operations_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsOperationsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations/{operationsId}
/// Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_operations_get_builder()` + `networkconnectivity_projects_locations_operations_get_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_operations_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_operations_get(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsOperationsGetArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        networkconnectivity_projects_locations_operations_get_builder(client, &args.name)?;
    networkconnectivity_projects_locations_operations_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_operations_list_execute()` to send, or `networkconnectivity_projects_locations_operations_list` for simplest API.

pub fn networkconnectivity_projects_locations_operations_list_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    filter: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
    returnPartialSuccess: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/operations",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }
    if let Some(val) = returnPartialSuccess.as_ref() {
        query_parts.push(format!("returnPartialSuccess={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_operations_list_execute()` or `networkconnectivity_projects_locations_operations_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_operations_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningListOperationsResponse =
                    serde_json::from_str(&body)
                        .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_operations_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_operations_list_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_operations_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_operations_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_operations_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_operations_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_operations_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsOperationsListArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
    /// Query parameter: returnPartialSuccess
    pub returnPartialSuccess: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/operations
/// Lists operations that match the specified filter in the request. If the server doesn''t support this method, it returns UNIMPLEMENTED.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_operations_list_builder()` + `networkconnectivity_projects_locations_operations_list_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_operations_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_operations_list(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsOperationsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningListOperationsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_operations_list_builder(
        client,
        &args.name,
        &args.filter,
        &args.pageSize,
        &args.pageToken,
        &args.returnPartialSuccess,
    )?;
    networkconnectivity_projects_locations_operations_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/regionalEndpoints
/// Creates a new RegionalEndpoint in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_regional_endpoints_create_execute()` to send, or `networkconnectivity_projects_locations_regional_endpoints_create` for simplest API.

pub fn networkconnectivity_projects_locations_regional_endpoints_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    regionalEndpointId: &Option<String>,
    requestId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/regionalEndpoints",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = regionalEndpointId.as_ref() {
        query_parts.push(format!("regionalEndpointId={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/regionalEndpoints
/// Creates a new RegionalEndpoint in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_regional_endpoints_create_execute()` or `networkconnectivity_projects_locations_regional_endpoints_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_regional_endpoints_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_regional_endpoints_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/regionalEndpoints
/// Creates a new RegionalEndpoint in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_regional_endpoints_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_regional_endpoints_create_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_regional_endpoints_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_regional_endpoints_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_regional_endpoints_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_regional_endpoints_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_regional_endpoints_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsRegionalEndpointsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: regionalEndpointId
    pub regionalEndpointId: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/regionalEndpoints
/// Creates a new RegionalEndpoint in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_regional_endpoints_create_builder()` + `networkconnectivity_projects_locations_regional_endpoints_create_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_regional_endpoints_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_regional_endpoints_create(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsRegionalEndpointsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_regional_endpoints_create_builder(
        client,
        &args.parent,
        &args.regionalEndpointId,
        &args.requestId,
    )?;
    networkconnectivity_projects_locations_regional_endpoints_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/regionalEndpoints/{regionalEndpointsId}
/// Deletes a single RegionalEndpoint.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_regional_endpoints_delete_execute()` to send, or `networkconnectivity_projects_locations_regional_endpoints_delete` for simplest API.

pub fn networkconnectivity_projects_locations_regional_endpoints_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/regionalEndpoints/{regionalEndpointsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/regionalEndpoints/{regionalEndpointsId}
/// Deletes a single RegionalEndpoint.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_regional_endpoints_delete_execute()` or `networkconnectivity_projects_locations_regional_endpoints_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_regional_endpoints_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_regional_endpoints_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/regionalEndpoints/{regionalEndpointsId}
/// Deletes a single RegionalEndpoint.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_regional_endpoints_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_regional_endpoints_delete_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_regional_endpoints_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_regional_endpoints_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_regional_endpoints_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_regional_endpoints_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_regional_endpoints_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsRegionalEndpointsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/regionalEndpoints/{regionalEndpointsId}
/// Deletes a single RegionalEndpoint.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_regional_endpoints_delete_builder()` + `networkconnectivity_projects_locations_regional_endpoints_delete_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_regional_endpoints_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_regional_endpoints_delete(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsRegionalEndpointsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_regional_endpoints_delete_builder(
        client,
        &args.name,
        &args.requestId,
    )?;
    networkconnectivity_projects_locations_regional_endpoints_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/regionalEndpoints/{regionalEndpointsId}
/// Gets details of a single RegionalEndpoint.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_regional_endpoints_get_execute()` to send, or `networkconnectivity_projects_locations_regional_endpoints_get` for simplest API.

pub fn networkconnectivity_projects_locations_regional_endpoints_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/regionalEndpoints/{regionalEndpointsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/regionalEndpoints/{regionalEndpointsId}
/// Gets details of a single RegionalEndpoint.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_regional_endpoints_get_execute()` or `networkconnectivity_projects_locations_regional_endpoints_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_regional_endpoints_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_regional_endpoints_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RegionalEndpoint>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RegionalEndpoint = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/regionalEndpoints/{regionalEndpointsId}
/// Gets details of a single RegionalEndpoint.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_regional_endpoints_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_regional_endpoints_get_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_regional_endpoints_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_regional_endpoints_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_regional_endpoints_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RegionalEndpoint>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_regional_endpoints_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_regional_endpoints_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsRegionalEndpointsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/regionalEndpoints/{regionalEndpointsId}
/// Gets details of a single RegionalEndpoint.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_regional_endpoints_get_builder()` + `networkconnectivity_projects_locations_regional_endpoints_get_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_regional_endpoints_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_regional_endpoints_get(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsRegionalEndpointsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RegionalEndpoint>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        networkconnectivity_projects_locations_regional_endpoints_get_builder(client, &args.name)?;
    networkconnectivity_projects_locations_regional_endpoints_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/regionalEndpoints
/// Lists RegionalEndpoints in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_regional_endpoints_list_execute()` to send, or `networkconnectivity_projects_locations_regional_endpoints_list` for simplest API.

pub fn networkconnectivity_projects_locations_regional_endpoints_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/regionalEndpoints",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/regionalEndpoints
/// Lists RegionalEndpoints in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_regional_endpoints_list_execute()` or `networkconnectivity_projects_locations_regional_endpoints_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_regional_endpoints_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_regional_endpoints_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListRegionalEndpointsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListRegionalEndpointsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/regionalEndpoints
/// Lists RegionalEndpoints in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_regional_endpoints_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_regional_endpoints_list_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_regional_endpoints_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_regional_endpoints_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_regional_endpoints_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListRegionalEndpointsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_regional_endpoints_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_regional_endpoints_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsRegionalEndpointsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/regionalEndpoints
/// Lists RegionalEndpoints in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_regional_endpoints_list_builder()` + `networkconnectivity_projects_locations_regional_endpoints_list_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_regional_endpoints_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_regional_endpoints_list(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsRegionalEndpointsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListRegionalEndpointsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_regional_endpoints_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    networkconnectivity_projects_locations_regional_endpoints_list_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/remoteTransportProfiles/{remoteTransportProfilesId}
/// Gets details of a single RemoteTransportProfile.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_remote_transport_profiles_get_execute()` to send, or `networkconnectivity_projects_locations_remote_transport_profiles_get` for simplest API.

pub fn networkconnectivity_projects_locations_remote_transport_profiles_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/remoteTransportProfiles/{remoteTransportProfilesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/remoteTransportProfiles/{remoteTransportProfilesId}
/// Gets details of a single RemoteTransportProfile.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_remote_transport_profiles_get_execute()` or `networkconnectivity_projects_locations_remote_transport_profiles_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_remote_transport_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_remote_transport_profiles_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RemoteTransportProfile>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: RemoteTransportProfile = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/remoteTransportProfiles/{remoteTransportProfilesId}
/// Gets details of a single RemoteTransportProfile.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_remote_transport_profiles_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_remote_transport_profiles_get_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_remote_transport_profiles_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_remote_transport_profiles_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_remote_transport_profiles_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RemoteTransportProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_remote_transport_profiles_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_remote_transport_profiles_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsRemoteTransportProfilesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/remoteTransportProfiles/{remoteTransportProfilesId}
/// Gets details of a single RemoteTransportProfile.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_remote_transport_profiles_get_builder()` + `networkconnectivity_projects_locations_remote_transport_profiles_get_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_remote_transport_profiles_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_remote_transport_profiles_get(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsRemoteTransportProfilesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<RemoteTransportProfile>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_remote_transport_profiles_get_builder(
        client, &args.name,
    )?;
    networkconnectivity_projects_locations_remote_transport_profiles_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/remoteTransportProfiles
/// Lists RemoteTransportProfiles in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_remote_transport_profiles_list_execute()` to send, or `networkconnectivity_projects_locations_remote_transport_profiles_list` for simplest API.

pub fn networkconnectivity_projects_locations_remote_transport_profiles_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/remoteTransportProfiles",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/remoteTransportProfiles
/// Lists RemoteTransportProfiles in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_remote_transport_profiles_list_execute()` or `networkconnectivity_projects_locations_remote_transport_profiles_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_remote_transport_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_remote_transport_profiles_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListRemoteTransportProfilesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListRemoteTransportProfilesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/remoteTransportProfiles
/// Lists RemoteTransportProfiles in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_remote_transport_profiles_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_remote_transport_profiles_list_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_remote_transport_profiles_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_remote_transport_profiles_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_remote_transport_profiles_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListRemoteTransportProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_remote_transport_profiles_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_remote_transport_profiles_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsRemoteTransportProfilesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/remoteTransportProfiles
/// Lists RemoteTransportProfiles in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_remote_transport_profiles_list_builder()` + `networkconnectivity_projects_locations_remote_transport_profiles_list_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_remote_transport_profiles_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_remote_transport_profiles_list(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsRemoteTransportProfilesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListRemoteTransportProfilesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_remote_transport_profiles_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    networkconnectivity_projects_locations_remote_transport_profiles_list_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/serviceClasses/{serviceClassesId}
/// Deletes a single ServiceClass.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_service_classes_delete_execute()` to send, or `networkconnectivity_projects_locations_service_classes_delete` for simplest API.

pub fn networkconnectivity_projects_locations_service_classes_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    etag: &Option<String>,
    requestId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/serviceClasses/{serviceClassesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = etag.as_ref() {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/serviceClasses/{serviceClassesId}
/// Deletes a single ServiceClass.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_service_classes_delete_execute()` or `networkconnectivity_projects_locations_service_classes_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_classes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_classes_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/serviceClasses/{serviceClassesId}
/// Deletes a single ServiceClass.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_service_classes_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_service_classes_delete_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_service_classes_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_classes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_service_classes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_service_classes_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_service_classes_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsServiceClassesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: etag
    pub etag: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/serviceClasses/{serviceClassesId}
/// Deletes a single ServiceClass.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_service_classes_delete_builder()` + `networkconnectivity_projects_locations_service_classes_delete_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_service_classes_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_classes_delete(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsServiceClassesDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_service_classes_delete_builder(
        client,
        &args.name,
        &args.etag,
        &args.requestId,
    )?;
    networkconnectivity_projects_locations_service_classes_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serviceClasses/{serviceClassesId}
/// Gets details of a single ServiceClass.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_service_classes_get_execute()` to send, or `networkconnectivity_projects_locations_service_classes_get` for simplest API.

pub fn networkconnectivity_projects_locations_service_classes_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/serviceClasses/{serviceClassesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serviceClasses/{serviceClassesId}
/// Gets details of a single ServiceClass.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_service_classes_get_execute()` or `networkconnectivity_projects_locations_service_classes_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_classes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_classes_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ServiceClass>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ServiceClass = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serviceClasses/{serviceClassesId}
/// Gets details of a single ServiceClass.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_service_classes_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_service_classes_get_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_service_classes_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_classes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_service_classes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_service_classes_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_service_classes_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsServiceClassesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serviceClasses/{serviceClassesId}
/// Gets details of a single ServiceClass.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_service_classes_get_builder()` + `networkconnectivity_projects_locations_service_classes_get_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_service_classes_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_classes_get(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsServiceClassesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceClass>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder =
        networkconnectivity_projects_locations_service_classes_get_builder(client, &args.name)?;
    networkconnectivity_projects_locations_service_classes_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serviceClasses
/// Lists ServiceClasses in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_service_classes_list_execute()` to send, or `networkconnectivity_projects_locations_service_classes_list` for simplest API.

pub fn networkconnectivity_projects_locations_service_classes_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/serviceClasses",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serviceClasses
/// Lists ServiceClasses in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_service_classes_list_execute()` or `networkconnectivity_projects_locations_service_classes_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_classes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_classes_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListServiceClassesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListServiceClassesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serviceClasses
/// Lists ServiceClasses in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_service_classes_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_service_classes_list_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_service_classes_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_classes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_service_classes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListServiceClassesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_service_classes_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_service_classes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsServiceClassesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serviceClasses
/// Lists ServiceClasses in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_service_classes_list_builder()` + `networkconnectivity_projects_locations_service_classes_list_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_service_classes_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_classes_list(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsServiceClassesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListServiceClassesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_service_classes_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    networkconnectivity_projects_locations_service_classes_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/serviceClasses/{serviceClassesId}
/// Updates the parameters of a single ServiceClass.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_service_classes_patch_execute()` to send, or `networkconnectivity_projects_locations_service_classes_patch` for simplest API.

pub fn networkconnectivity_projects_locations_service_classes_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/serviceClasses/{serviceClassesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/serviceClasses/{serviceClassesId}
/// Updates the parameters of a single ServiceClass.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_service_classes_patch_execute()` or `networkconnectivity_projects_locations_service_classes_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_classes_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_classes_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/serviceClasses/{serviceClassesId}
/// Updates the parameters of a single ServiceClass.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_service_classes_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_service_classes_patch_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_service_classes_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_classes_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_service_classes_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_service_classes_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_service_classes_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsServiceClassesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/serviceClasses/{serviceClassesId}
/// Updates the parameters of a single ServiceClass.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_service_classes_patch_builder()` + `networkconnectivity_projects_locations_service_classes_patch_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_service_classes_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_classes_patch(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsServiceClassesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_service_classes_patch_builder(
        client,
        &args.name,
        &args.requestId,
        &args.updateMask,
    )?;
    networkconnectivity_projects_locations_service_classes_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionMaps
/// Creates a new ServiceConnectionMap in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_service_connection_maps_create_execute()` to send, or `networkconnectivity_projects_locations_service_connection_maps_create` for simplest API.

pub fn networkconnectivity_projects_locations_service_connection_maps_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    requestId: &Option<String>,
    serviceConnectionMapId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/serviceConnectionMaps",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = serviceConnectionMapId.as_ref() {
        query_parts.push(format!("serviceConnectionMapId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionMaps
/// Creates a new ServiceConnectionMap in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_service_connection_maps_create_execute()` or `networkconnectivity_projects_locations_service_connection_maps_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_connection_maps_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_connection_maps_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionMaps
/// Creates a new ServiceConnectionMap in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_service_connection_maps_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_service_connection_maps_create_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_service_connection_maps_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_connection_maps_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_service_connection_maps_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_service_connection_maps_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_service_connection_maps_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsServiceConnectionMapsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: serviceConnectionMapId
    pub serviceConnectionMapId: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionMaps
/// Creates a new ServiceConnectionMap in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_service_connection_maps_create_builder()` + `networkconnectivity_projects_locations_service_connection_maps_create_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_service_connection_maps_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_connection_maps_create(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsServiceConnectionMapsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_service_connection_maps_create_builder(
        client,
        &args.parent,
        &args.requestId,
        &args.serviceConnectionMapId,
    )?;
    networkconnectivity_projects_locations_service_connection_maps_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionMaps/{serviceConnectionMapsId}
/// Deletes a single ServiceConnectionMap.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_service_connection_maps_delete_execute()` to send, or `networkconnectivity_projects_locations_service_connection_maps_delete` for simplest API.

pub fn networkconnectivity_projects_locations_service_connection_maps_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    etag: &Option<String>,
    requestId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/serviceConnectionMaps/{serviceConnectionMapsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = etag.as_ref() {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionMaps/{serviceConnectionMapsId}
/// Deletes a single ServiceConnectionMap.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_service_connection_maps_delete_execute()` or `networkconnectivity_projects_locations_service_connection_maps_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_connection_maps_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_connection_maps_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionMaps/{serviceConnectionMapsId}
/// Deletes a single ServiceConnectionMap.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_service_connection_maps_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_service_connection_maps_delete_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_service_connection_maps_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_connection_maps_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_service_connection_maps_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_service_connection_maps_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_service_connection_maps_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsServiceConnectionMapsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: etag
    pub etag: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionMaps/{serviceConnectionMapsId}
/// Deletes a single ServiceConnectionMap.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_service_connection_maps_delete_builder()` + `networkconnectivity_projects_locations_service_connection_maps_delete_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_service_connection_maps_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_connection_maps_delete(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsServiceConnectionMapsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_service_connection_maps_delete_builder(
        client,
        &args.name,
        &args.etag,
        &args.requestId,
    )?;
    networkconnectivity_projects_locations_service_connection_maps_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionMaps/{serviceConnectionMapsId}
/// Gets details of a single ServiceConnectionMap.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_service_connection_maps_get_execute()` to send, or `networkconnectivity_projects_locations_service_connection_maps_get` for simplest API.

pub fn networkconnectivity_projects_locations_service_connection_maps_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/serviceConnectionMaps/{serviceConnectionMapsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionMaps/{serviceConnectionMapsId}
/// Gets details of a single ServiceConnectionMap.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_service_connection_maps_get_execute()` or `networkconnectivity_projects_locations_service_connection_maps_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_connection_maps_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_connection_maps_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ServiceConnectionMap>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ServiceConnectionMap = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionMaps/{serviceConnectionMapsId}
/// Gets details of a single ServiceConnectionMap.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_service_connection_maps_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_service_connection_maps_get_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_service_connection_maps_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_connection_maps_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_service_connection_maps_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceConnectionMap>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_service_connection_maps_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_service_connection_maps_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsServiceConnectionMapsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionMaps/{serviceConnectionMapsId}
/// Gets details of a single ServiceConnectionMap.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_service_connection_maps_get_builder()` + `networkconnectivity_projects_locations_service_connection_maps_get_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_service_connection_maps_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_connection_maps_get(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsServiceConnectionMapsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceConnectionMap>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_service_connection_maps_get_builder(
        client, &args.name,
    )?;
    networkconnectivity_projects_locations_service_connection_maps_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionMaps
/// Lists ServiceConnectionMaps in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_service_connection_maps_list_execute()` to send, or `networkconnectivity_projects_locations_service_connection_maps_list` for simplest API.

pub fn networkconnectivity_projects_locations_service_connection_maps_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/serviceConnectionMaps",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionMaps
/// Lists ServiceConnectionMaps in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_service_connection_maps_list_execute()` or `networkconnectivity_projects_locations_service_connection_maps_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_connection_maps_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_connection_maps_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListServiceConnectionMapsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListServiceConnectionMapsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionMaps
/// Lists ServiceConnectionMaps in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_service_connection_maps_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_service_connection_maps_list_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_service_connection_maps_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_connection_maps_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_service_connection_maps_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListServiceConnectionMapsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_service_connection_maps_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_service_connection_maps_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsServiceConnectionMapsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionMaps
/// Lists ServiceConnectionMaps in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_service_connection_maps_list_builder()` + `networkconnectivity_projects_locations_service_connection_maps_list_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_service_connection_maps_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_connection_maps_list(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsServiceConnectionMapsListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListServiceConnectionMapsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_service_connection_maps_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    networkconnectivity_projects_locations_service_connection_maps_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionMaps/{serviceConnectionMapsId}
/// Updates the parameters of a single ServiceConnectionMap.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_service_connection_maps_patch_execute()` to send, or `networkconnectivity_projects_locations_service_connection_maps_patch` for simplest API.

pub fn networkconnectivity_projects_locations_service_connection_maps_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/serviceConnectionMaps/{serviceConnectionMapsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionMaps/{serviceConnectionMapsId}
/// Updates the parameters of a single ServiceConnectionMap.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_service_connection_maps_patch_execute()` or `networkconnectivity_projects_locations_service_connection_maps_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_connection_maps_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_connection_maps_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionMaps/{serviceConnectionMapsId}
/// Updates the parameters of a single ServiceConnectionMap.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_service_connection_maps_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_service_connection_maps_patch_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_service_connection_maps_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_connection_maps_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_service_connection_maps_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_service_connection_maps_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_service_connection_maps_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsServiceConnectionMapsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionMaps/{serviceConnectionMapsId}
/// Updates the parameters of a single ServiceConnectionMap.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_service_connection_maps_patch_builder()` + `networkconnectivity_projects_locations_service_connection_maps_patch_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_service_connection_maps_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_connection_maps_patch(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsServiceConnectionMapsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_service_connection_maps_patch_builder(
        client,
        &args.name,
        &args.requestId,
        &args.updateMask,
    )?;
    networkconnectivity_projects_locations_service_connection_maps_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionPolicies
/// Creates a new ServiceConnectionPolicy in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_service_connection_policies_create_execute()` to send, or `networkconnectivity_projects_locations_service_connection_policies_create` for simplest API.

pub fn networkconnectivity_projects_locations_service_connection_policies_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    autoSubnetworkConfig_allocRangeSpace: &Option<String>,
    autoSubnetworkConfig_ipStack: &Option<String>,
    autoSubnetworkConfig_prefixLength: &Option<String>,
    requestId: &Option<String>,
    serviceConnectionPolicyId: &Option<String>,
    subnetworkMode: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/serviceConnectionPolicies",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = autoSubnetworkConfig_allocRangeSpace.as_ref() {
        query_parts.push(format!("autoSubnetworkConfig.allocRangeSpace={}", val));
    }
    if let Some(val) = autoSubnetworkConfig_ipStack.as_ref() {
        query_parts.push(format!("autoSubnetworkConfig.ipStack={}", val));
    }
    if let Some(val) = autoSubnetworkConfig_prefixLength.as_ref() {
        query_parts.push(format!("autoSubnetworkConfig.prefixLength={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = serviceConnectionPolicyId.as_ref() {
        query_parts.push(format!("serviceConnectionPolicyId={}", val));
    }
    if let Some(val) = subnetworkMode.as_ref() {
        query_parts.push(format!("subnetworkMode={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionPolicies
/// Creates a new ServiceConnectionPolicy in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_service_connection_policies_create_execute()` or `networkconnectivity_projects_locations_service_connection_policies_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_connection_policies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_connection_policies_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionPolicies
/// Creates a new ServiceConnectionPolicy in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_service_connection_policies_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_service_connection_policies_create_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_service_connection_policies_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_connection_policies_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_service_connection_policies_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        networkconnectivity_projects_locations_service_connection_policies_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_service_connection_policies_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsServiceConnectionPoliciesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: autoSubnetworkConfig_allocRangeSpace
    pub autoSubnetworkConfig_allocRangeSpace: Option<String>,
    /// Query parameter: autoSubnetworkConfig_ipStack
    pub autoSubnetworkConfig_ipStack: Option<String>,
    /// Query parameter: autoSubnetworkConfig_prefixLength
    pub autoSubnetworkConfig_prefixLength: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: serviceConnectionPolicyId
    pub serviceConnectionPolicyId: Option<String>,
    /// Query parameter: subnetworkMode
    pub subnetworkMode: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionPolicies
/// Creates a new ServiceConnectionPolicy in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_service_connection_policies_create_builder()` + `networkconnectivity_projects_locations_service_connection_policies_create_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_service_connection_policies_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_connection_policies_create(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsServiceConnectionPoliciesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        networkconnectivity_projects_locations_service_connection_policies_create_builder(
            client,
            &args.parent,
            &args.autoSubnetworkConfig_allocRangeSpace,
            &args.autoSubnetworkConfig_ipStack,
            &args.autoSubnetworkConfig_prefixLength,
            &args.requestId,
            &args.serviceConnectionPolicyId,
            &args.subnetworkMode,
        )?;
    networkconnectivity_projects_locations_service_connection_policies_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionPolicies/{serviceConnectionPoliciesId}
/// Deletes a single ServiceConnectionPolicy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_service_connection_policies_delete_execute()` to send, or `networkconnectivity_projects_locations_service_connection_policies_delete` for simplest API.

pub fn networkconnectivity_projects_locations_service_connection_policies_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    etag: &Option<String>,
    requestId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/serviceConnectionPolicies/{serviceConnectionPoliciesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = etag.as_ref() {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionPolicies/{serviceConnectionPoliciesId}
/// Deletes a single ServiceConnectionPolicy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_service_connection_policies_delete_execute()` or `networkconnectivity_projects_locations_service_connection_policies_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_connection_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_connection_policies_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionPolicies/{serviceConnectionPoliciesId}
/// Deletes a single ServiceConnectionPolicy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_service_connection_policies_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_service_connection_policies_delete_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_service_connection_policies_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_connection_policies_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_service_connection_policies_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        networkconnectivity_projects_locations_service_connection_policies_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_service_connection_policies_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsServiceConnectionPoliciesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: etag
    pub etag: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionPolicies/{serviceConnectionPoliciesId}
/// Deletes a single ServiceConnectionPolicy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_service_connection_policies_delete_builder()` + `networkconnectivity_projects_locations_service_connection_policies_delete_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_service_connection_policies_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_connection_policies_delete(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsServiceConnectionPoliciesDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder =
        networkconnectivity_projects_locations_service_connection_policies_delete_builder(
            client,
            &args.name,
            &args.etag,
            &args.requestId,
        )?;
    networkconnectivity_projects_locations_service_connection_policies_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionPolicies/{serviceConnectionPoliciesId}
/// Gets details of a single ServiceConnectionPolicy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_service_connection_policies_get_execute()` to send, or `networkconnectivity_projects_locations_service_connection_policies_get` for simplest API.

pub fn networkconnectivity_projects_locations_service_connection_policies_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/serviceConnectionPolicies/{serviceConnectionPoliciesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionPolicies/{serviceConnectionPoliciesId}
/// Gets details of a single ServiceConnectionPolicy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_service_connection_policies_get_execute()` or `networkconnectivity_projects_locations_service_connection_policies_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_connection_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_connection_policies_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ServiceConnectionPolicy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ServiceConnectionPolicy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionPolicies/{serviceConnectionPoliciesId}
/// Gets details of a single ServiceConnectionPolicy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_service_connection_policies_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_service_connection_policies_get_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_service_connection_policies_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_connection_policies_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_service_connection_policies_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceConnectionPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task =
        networkconnectivity_projects_locations_service_connection_policies_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_service_connection_policies_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsServiceConnectionPoliciesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionPolicies/{serviceConnectionPoliciesId}
/// Gets details of a single ServiceConnectionPolicy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_service_connection_policies_get_builder()` + `networkconnectivity_projects_locations_service_connection_policies_get_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_service_connection_policies_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_connection_policies_get(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsServiceConnectionPoliciesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceConnectionPolicy>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_service_connection_policies_get_builder(
        client, &args.name,
    )?;
    networkconnectivity_projects_locations_service_connection_policies_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionPolicies
/// Lists ServiceConnectionPolicies in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_service_connection_policies_list_execute()` to send, or `networkconnectivity_projects_locations_service_connection_policies_list` for simplest API.

pub fn networkconnectivity_projects_locations_service_connection_policies_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/serviceConnectionPolicies",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionPolicies
/// Lists ServiceConnectionPolicies in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_service_connection_policies_list_execute()` or `networkconnectivity_projects_locations_service_connection_policies_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_connection_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_connection_policies_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListServiceConnectionPoliciesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListServiceConnectionPoliciesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionPolicies
/// Lists ServiceConnectionPolicies in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_service_connection_policies_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_service_connection_policies_list_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_service_connection_policies_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_connection_policies_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_service_connection_policies_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListServiceConnectionPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        networkconnectivity_projects_locations_service_connection_policies_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_service_connection_policies_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsServiceConnectionPoliciesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionPolicies
/// Lists ServiceConnectionPolicies in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_service_connection_policies_list_builder()` + `networkconnectivity_projects_locations_service_connection_policies_list_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_service_connection_policies_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_connection_policies_list(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsServiceConnectionPoliciesListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListServiceConnectionPoliciesResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_service_connection_policies_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    networkconnectivity_projects_locations_service_connection_policies_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionPolicies/{serviceConnectionPoliciesId}
/// Updates the parameters of a single ServiceConnectionPolicy.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_service_connection_policies_patch_execute()` to send, or `networkconnectivity_projects_locations_service_connection_policies_patch` for simplest API.

pub fn networkconnectivity_projects_locations_service_connection_policies_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/serviceConnectionPolicies/{serviceConnectionPoliciesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionPolicies/{serviceConnectionPoliciesId}
/// Updates the parameters of a single ServiceConnectionPolicy.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_service_connection_policies_patch_execute()` or `networkconnectivity_projects_locations_service_connection_policies_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_connection_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_connection_policies_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionPolicies/{serviceConnectionPoliciesId}
/// Updates the parameters of a single ServiceConnectionPolicy.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_service_connection_policies_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_service_connection_policies_patch_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_service_connection_policies_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_connection_policies_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_service_connection_policies_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        networkconnectivity_projects_locations_service_connection_policies_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_service_connection_policies_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsServiceConnectionPoliciesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionPolicies/{serviceConnectionPoliciesId}
/// Updates the parameters of a single ServiceConnectionPolicy.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_service_connection_policies_patch_builder()` + `networkconnectivity_projects_locations_service_connection_policies_patch_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_service_connection_policies_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_connection_policies_patch(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsServiceConnectionPoliciesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_service_connection_policies_patch_builder(
        client,
        &args.name,
        &args.requestId,
        &args.updateMask,
    )?;
    networkconnectivity_projects_locations_service_connection_policies_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionTokens
/// Creates a new ServiceConnectionToken in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_service_connection_tokens_create_execute()` to send, or `networkconnectivity_projects_locations_service_connection_tokens_create` for simplest API.

pub fn networkconnectivity_projects_locations_service_connection_tokens_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    requestId: &Option<String>,
    serviceConnectionTokenId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/serviceConnectionTokens",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = serviceConnectionTokenId.as_ref() {
        query_parts.push(format!("serviceConnectionTokenId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionTokens
/// Creates a new ServiceConnectionToken in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_service_connection_tokens_create_execute()` or `networkconnectivity_projects_locations_service_connection_tokens_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_connection_tokens_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_connection_tokens_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionTokens
/// Creates a new ServiceConnectionToken in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_service_connection_tokens_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_service_connection_tokens_create_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_service_connection_tokens_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_connection_tokens_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_service_connection_tokens_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        networkconnectivity_projects_locations_service_connection_tokens_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_service_connection_tokens_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsServiceConnectionTokensCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: serviceConnectionTokenId
    pub serviceConnectionTokenId: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionTokens
/// Creates a new ServiceConnectionToken in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_service_connection_tokens_create_builder()` + `networkconnectivity_projects_locations_service_connection_tokens_create_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_service_connection_tokens_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_connection_tokens_create(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsServiceConnectionTokensCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_service_connection_tokens_create_builder(
        client,
        &args.parent,
        &args.requestId,
        &args.serviceConnectionTokenId,
    )?;
    networkconnectivity_projects_locations_service_connection_tokens_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionTokens/{serviceConnectionTokensId}
/// Deletes a single ServiceConnectionToken.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_service_connection_tokens_delete_execute()` to send, or `networkconnectivity_projects_locations_service_connection_tokens_delete` for simplest API.

pub fn networkconnectivity_projects_locations_service_connection_tokens_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    etag: &Option<String>,
    requestId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/serviceConnectionTokens/{serviceConnectionTokensId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = etag.as_ref() {
        query_parts.push(format!("etag={}", val));
    }
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionTokens/{serviceConnectionTokensId}
/// Deletes a single ServiceConnectionToken.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_service_connection_tokens_delete_execute()` or `networkconnectivity_projects_locations_service_connection_tokens_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_connection_tokens_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_connection_tokens_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionTokens/{serviceConnectionTokensId}
/// Deletes a single ServiceConnectionToken.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_service_connection_tokens_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_service_connection_tokens_delete_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_service_connection_tokens_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_connection_tokens_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_service_connection_tokens_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task =
        networkconnectivity_projects_locations_service_connection_tokens_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_service_connection_tokens_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsServiceConnectionTokensDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: etag
    pub etag: Option<String>,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionTokens/{serviceConnectionTokensId}
/// Deletes a single ServiceConnectionToken.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_service_connection_tokens_delete_builder()` + `networkconnectivity_projects_locations_service_connection_tokens_delete_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_service_connection_tokens_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_connection_tokens_delete(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsServiceConnectionTokensDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_service_connection_tokens_delete_builder(
        client,
        &args.name,
        &args.etag,
        &args.requestId,
    )?;
    networkconnectivity_projects_locations_service_connection_tokens_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionTokens/{serviceConnectionTokensId}
/// Gets details of a single ServiceConnectionToken.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_service_connection_tokens_get_execute()` to send, or `networkconnectivity_projects_locations_service_connection_tokens_get` for simplest API.

pub fn networkconnectivity_projects_locations_service_connection_tokens_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/serviceConnectionTokens/{serviceConnectionTokensId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionTokens/{serviceConnectionTokensId}
/// Gets details of a single ServiceConnectionToken.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_service_connection_tokens_get_execute()` or `networkconnectivity_projects_locations_service_connection_tokens_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_connection_tokens_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_connection_tokens_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ServiceConnectionToken>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ServiceConnectionToken = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionTokens/{serviceConnectionTokensId}
/// Gets details of a single ServiceConnectionToken.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_service_connection_tokens_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_service_connection_tokens_get_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_service_connection_tokens_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_connection_tokens_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_service_connection_tokens_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceConnectionToken>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_service_connection_tokens_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_service_connection_tokens_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsServiceConnectionTokensGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionTokens/{serviceConnectionTokensId}
/// Gets details of a single ServiceConnectionToken.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_service_connection_tokens_get_builder()` + `networkconnectivity_projects_locations_service_connection_tokens_get_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_service_connection_tokens_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_connection_tokens_get(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsServiceConnectionTokensGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ServiceConnectionToken>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_service_connection_tokens_get_builder(
        client, &args.name,
    )?;
    networkconnectivity_projects_locations_service_connection_tokens_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionTokens
/// Lists ServiceConnectionTokens in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_service_connection_tokens_list_execute()` to send, or `networkconnectivity_projects_locations_service_connection_tokens_list` for simplest API.

pub fn networkconnectivity_projects_locations_service_connection_tokens_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/serviceConnectionTokens",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionTokens
/// Lists ServiceConnectionTokens in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_service_connection_tokens_list_execute()` or `networkconnectivity_projects_locations_service_connection_tokens_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_connection_tokens_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_connection_tokens_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListServiceConnectionTokensResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListServiceConnectionTokensResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionTokens
/// Lists ServiceConnectionTokens in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_service_connection_tokens_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_service_connection_tokens_list_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_service_connection_tokens_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_service_connection_tokens_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_service_connection_tokens_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListServiceConnectionTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_service_connection_tokens_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_service_connection_tokens_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsServiceConnectionTokensListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/serviceConnectionTokens
/// Lists ServiceConnectionTokens in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_service_connection_tokens_list_builder()` + `networkconnectivity_projects_locations_service_connection_tokens_list_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_service_connection_tokens_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_service_connection_tokens_list(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsServiceConnectionTokensListArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<ListServiceConnectionTokensResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_service_connection_tokens_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    networkconnectivity_projects_locations_service_connection_tokens_list_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/spokes
/// Creates a Network Connectivity Center spoke.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_spokes_create_execute()` to send, or `networkconnectivity_projects_locations_spokes_create` for simplest API.

pub fn networkconnectivity_projects_locations_spokes_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    requestId: &Option<String>,
    spokeId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/spokes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = spokeId.as_ref() {
        query_parts.push(format!("spokeId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/spokes
/// Creates a Network Connectivity Center spoke.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_spokes_create_execute()` or `networkconnectivity_projects_locations_spokes_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_spokes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_spokes_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/spokes
/// Creates a Network Connectivity Center spoke.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_spokes_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_spokes_create_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_spokes_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_spokes_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_spokes_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_spokes_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_spokes_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsSpokesCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: spokeId
    pub spokeId: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/spokes
/// Creates a Network Connectivity Center spoke.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_spokes_create_builder()` + `networkconnectivity_projects_locations_spokes_create_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_spokes_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_spokes_create(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsSpokesCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_spokes_create_builder(
        client,
        &args.parent,
        &args.requestId,
        &args.spokeId,
    )?;
    networkconnectivity_projects_locations_spokes_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/spokes/{spokesId}
/// Deletes a Network Connectivity Center spoke.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_spokes_delete_execute()` to send, or `networkconnectivity_projects_locations_spokes_delete` for simplest API.

pub fn networkconnectivity_projects_locations_spokes_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/spokes/{spokesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/spokes/{spokesId}
/// Deletes a Network Connectivity Center spoke.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_spokes_delete_execute()` or `networkconnectivity_projects_locations_spokes_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_spokes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_spokes_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/spokes/{spokesId}
/// Deletes a Network Connectivity Center spoke.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_spokes_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_spokes_delete_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_spokes_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_spokes_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_spokes_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_spokes_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_spokes_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsSpokesDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/spokes/{spokesId}
/// Deletes a Network Connectivity Center spoke.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_spokes_delete_builder()` + `networkconnectivity_projects_locations_spokes_delete_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_spokes_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_spokes_delete(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsSpokesDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_spokes_delete_builder(
        client,
        &args.name,
        &args.requestId,
    )?;
    networkconnectivity_projects_locations_spokes_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/spokes/{spokesId}
/// Gets details about a Network Connectivity Center spoke.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_spokes_get_execute()` to send, or `networkconnectivity_projects_locations_spokes_get` for simplest API.

pub fn networkconnectivity_projects_locations_spokes_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/spokes/{spokesId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/spokes/{spokesId}
/// Gets details about a Network Connectivity Center spoke.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_spokes_get_execute()` or `networkconnectivity_projects_locations_spokes_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_spokes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_spokes_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Spoke>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Spoke = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/spokes/{spokesId}
/// Gets details about a Network Connectivity Center spoke.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_spokes_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_spokes_get_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_spokes_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_spokes_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_spokes_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Spoke>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_spokes_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_spokes_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsSpokesGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/spokes/{spokesId}
/// Gets details about a Network Connectivity Center spoke.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_spokes_get_builder()` + `networkconnectivity_projects_locations_spokes_get_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_spokes_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_spokes_get(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsSpokesGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Spoke>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_spokes_get_builder(client, &args.name)?;
    networkconnectivity_projects_locations_spokes_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/spokes/{spokesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_spokes_get_iam_policy_execute()` to send, or `networkconnectivity_projects_locations_spokes_get_iam_policy` for simplest API.

pub fn networkconnectivity_projects_locations_spokes_get_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
    options_requestedPolicyVersion: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/spokes/{spokesId}:getIamPolicy",
        resource,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = options_requestedPolicyVersion.as_ref() {
        query_parts.push(format!("options.requestedPolicyVersion={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/spokes/{spokesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_spokes_get_iam_policy_execute()` or `networkconnectivity_projects_locations_spokes_get_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_spokes_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_spokes_get_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/spokes/{spokesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_spokes_get_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_spokes_get_iam_policy_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_spokes_get_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_spokes_get_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_spokes_get_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_spokes_get_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_spokes_get_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsSpokesGetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
    /// Query parameter: options_requestedPolicyVersion
    pub options_requestedPolicyVersion: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/spokes/{spokesId}:getIamPolicy
/// Gets the access control policy for a resource. Returns an empty policy if the resource exists and does not have a policy set.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_spokes_get_iam_policy_builder()` + `networkconnectivity_projects_locations_spokes_get_iam_policy_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_spokes_get_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_spokes_get_iam_policy(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsSpokesGetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_spokes_get_iam_policy_builder(
        client,
        &args.resource,
        &args.options_requestedPolicyVersion,
    )?;
    networkconnectivity_projects_locations_spokes_get_iam_policy_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/spokes
/// Lists the Network Connectivity Center spokes in a specified project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_spokes_list_execute()` to send, or `networkconnectivity_projects_locations_spokes_list` for simplest API.

pub fn networkconnectivity_projects_locations_spokes_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/spokes",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/spokes
/// Lists the Network Connectivity Center spokes in a specified project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_spokes_list_execute()` or `networkconnectivity_projects_locations_spokes_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_spokes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_spokes_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListSpokesResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListSpokesResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/spokes
/// Lists the Network Connectivity Center spokes in a specified project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_spokes_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_spokes_list_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_spokes_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_spokes_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_spokes_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSpokesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_spokes_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_spokes_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsSpokesListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/spokes
/// Lists the Network Connectivity Center spokes in a specified project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_spokes_list_builder()` + `networkconnectivity_projects_locations_spokes_list_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_spokes_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_spokes_list(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsSpokesListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListSpokesResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_spokes_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    networkconnectivity_projects_locations_spokes_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/spokes/{spokesId}
/// Updates the parameters of a Network Connectivity Center spoke.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_spokes_patch_execute()` to send, or `networkconnectivity_projects_locations_spokes_patch` for simplest API.

pub fn networkconnectivity_projects_locations_spokes_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/spokes/{spokesId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/spokes/{spokesId}
/// Updates the parameters of a Network Connectivity Center spoke.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_spokes_patch_execute()` or `networkconnectivity_projects_locations_spokes_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_spokes_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_spokes_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/spokes/{spokesId}
/// Updates the parameters of a Network Connectivity Center spoke.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_spokes_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_spokes_patch_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_spokes_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_spokes_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_spokes_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_spokes_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_spokes_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsSpokesPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/spokes/{spokesId}
/// Updates the parameters of a Network Connectivity Center spoke.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_spokes_patch_builder()` + `networkconnectivity_projects_locations_spokes_patch_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_spokes_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_spokes_patch(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsSpokesPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_spokes_patch_builder(
        client,
        &args.name,
        &args.requestId,
        &args.updateMask,
    )?;
    networkconnectivity_projects_locations_spokes_patch_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/spokes/{spokesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_spokes_set_iam_policy_execute()` to send, or `networkconnectivity_projects_locations_spokes_set_iam_policy` for simplest API.

pub fn networkconnectivity_projects_locations_spokes_set_iam_policy_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/spokes/{spokesId}:setIamPolicy",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/spokes/{spokesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_spokes_set_iam_policy_execute()` or `networkconnectivity_projects_locations_spokes_set_iam_policy`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_spokes_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_spokes_set_iam_policy_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Policy = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/spokes/{spokesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_spokes_set_iam_policy_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_spokes_set_iam_policy_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_spokes_set_iam_policy()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_spokes_set_iam_policy_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_spokes_set_iam_policy_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_spokes_set_iam_policy_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_spokes_set_iam_policy`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsSpokesSetIamPolicyArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/spokes/{spokesId}:setIamPolicy
/// Sets the access control policy on the specified resource. Replaces any existing policy. Can return NOT_FOUND, INVALID_ARGUMENT, and PERMISSION_DENIED errors.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_spokes_set_iam_policy_builder()` + `networkconnectivity_projects_locations_spokes_set_iam_policy_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_spokes_set_iam_policy_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_spokes_set_iam_policy(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsSpokesSetIamPolicyArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Policy>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_spokes_set_iam_policy_builder(
        client,
        &args.resource,
    )?;
    networkconnectivity_projects_locations_spokes_set_iam_policy_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/spokes/{spokesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_spokes_test_iam_permissions_execute()` to send, or `networkconnectivity_projects_locations_spokes_test_iam_permissions` for simplest API.

pub fn networkconnectivity_projects_locations_spokes_test_iam_permissions_builder<R>(
    client: &SimpleHttpClient<R>,
    resource: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/spokes/{spokesId}:testIamPermissions",
        resource,
    );

    // Build request
    let builder = client
        .post(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/spokes/{spokesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_spokes_test_iam_permissions_execute()` or `networkconnectivity_projects_locations_spokes_test_iam_permissions`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_spokes_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_spokes_test_iam_permissions_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/spokes/{spokesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_spokes_test_iam_permissions_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_spokes_test_iam_permissions_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_spokes_test_iam_permissions()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_spokes_test_iam_permissions_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_spokes_test_iam_permissions_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_spokes_test_iam_permissions_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_spokes_test_iam_permissions`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsSpokesTestIamPermissionsArgs {
    /// Path parameter: resource
    pub resource: String,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/spokes/{spokesId}:testIamPermissions
/// Returns permissions that a caller has on the specified resource. If the resource does not exist, this will return an empty set of permissions, not a NOT_FOUND error. Note: This operation is designed to be used for building permission-aware UIs and command-line tools, not for authorization checking. This operation may "fail open" without warning.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_spokes_test_iam_permissions_builder()` + `networkconnectivity_projects_locations_spokes_test_iam_permissions_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_spokes_test_iam_permissions_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_spokes_test_iam_permissions(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsSpokesTestIamPermissionsArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<TestIamPermissionsResponse>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_spokes_test_iam_permissions_builder(
        client,
        &args.resource,
    )?;
    networkconnectivity_projects_locations_spokes_test_iam_permissions_execute(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/transports
/// Creates a new Transport in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_transports_create_execute()` to send, or `networkconnectivity_projects_locations_transports_create` for simplest API.

pub fn networkconnectivity_projects_locations_transports_create_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    requestId: &Option<String>,
    transportId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/transports",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = transportId.as_ref() {
        query_parts.push(format!("transportId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .post(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/transports
/// Creates a new Transport in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_transports_create_execute()` or `networkconnectivity_projects_locations_transports_create`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_transports_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_transports_create_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/transports
/// Creates a new Transport in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_transports_create_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_transports_create_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_transports_create()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_transports_create_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_transports_create_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_transports_create_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_transports_create`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsTransportsCreateArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: transportId
    pub transportId: Option<String>,
}

/// POST v1/projects/{projectsId}/locations/{locationsId}/transports
/// Creates a new Transport in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_transports_create_builder()` + `networkconnectivity_projects_locations_transports_create_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_transports_create_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_transports_create(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsTransportsCreateArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_transports_create_builder(
        client,
        &args.parent,
        &args.requestId,
        &args.transportId,
    )?;
    networkconnectivity_projects_locations_transports_create_execute(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/transports/{transportsId}
/// Deletes a single Transport.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_transports_delete_execute()` to send, or `networkconnectivity_projects_locations_transports_delete` for simplest API.

pub fn networkconnectivity_projects_locations_transports_delete_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/transports/{transportsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .delete(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/transports/{transportsId}
/// Deletes a single Transport.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_transports_delete_execute()` or `networkconnectivity_projects_locations_transports_delete`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_transports_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_transports_delete_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/transports/{transportsId}
/// Deletes a single Transport.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_transports_delete_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_transports_delete_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_transports_delete()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_transports_delete_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_transports_delete_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_transports_delete_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_transports_delete`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsTransportsDeleteArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
}

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/transports/{transportsId}
/// Deletes a single Transport.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_transports_delete_builder()` + `networkconnectivity_projects_locations_transports_delete_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_transports_delete_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_transports_delete(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsTransportsDeleteArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_transports_delete_builder(
        client,
        &args.name,
        &args.requestId,
    )?;
    networkconnectivity_projects_locations_transports_delete_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/transports/{transportsId}
/// Gets details of a single Transport.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_transports_get_execute()` to send, or `networkconnectivity_projects_locations_transports_get` for simplest API.

pub fn networkconnectivity_projects_locations_transports_get_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/transports/{transportsId}",
        name,
    );

    // Build request
    let builder = client
        .get(&endpoint_url)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/transports/{transportsId}
/// Gets details of a single Transport.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_transports_get_execute()` or `networkconnectivity_projects_locations_transports_get`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_transports_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_transports_get_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Transport>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: Transport = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/transports/{transportsId}
/// Gets details of a single Transport.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_transports_get_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_transports_get_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_transports_get()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_transports_get_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_transports_get_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Transport>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_transports_get_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_transports_get`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsTransportsGetArgs {
    /// Path parameter: name
    pub name: String,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/transports/{transportsId}
/// Gets details of a single Transport.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_transports_get_builder()` + `networkconnectivity_projects_locations_transports_get_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_transports_get_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_transports_get(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsTransportsGetArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<Transport>, ApiError>, P = ApiPending> + Send + 'static,
    ApiError,
> {
    let builder =
        networkconnectivity_projects_locations_transports_get_builder(client, &args.name)?;
    networkconnectivity_projects_locations_transports_get_execute(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/transports
/// Lists Transports in a given project and location.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_transports_list_execute()` to send, or `networkconnectivity_projects_locations_transports_list` for simplest API.

pub fn networkconnectivity_projects_locations_transports_list_builder<R>(
    client: &SimpleHttpClient<R>,
    parent: &String,
    filter: &Option<String>,
    orderBy: &Option<String>,
    pageSize: &Option<String>,
    pageToken: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/transports",
        parent,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = filter.as_ref() {
        query_parts.push(format!("filter={}", val));
    }
    if let Some(val) = orderBy.as_ref() {
        query_parts.push(format!("orderBy={}", val));
    }
    if let Some(val) = pageSize.as_ref() {
        query_parts.push(format!("pageSize={}", val));
    }
    if let Some(val) = pageToken.as_ref() {
        query_parts.push(format!("pageToken={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .get(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/transports
/// Lists Transports in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_transports_list_execute()` or `networkconnectivity_projects_locations_transports_list`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_transports_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_transports_list_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListTransportsResponse>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: ListTransportsResponse = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/transports
/// Lists Transports in a given project and location.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_transports_list_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_transports_list_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_transports_list()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_transports_list_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_transports_list_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTransportsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_transports_list_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_transports_list`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsTransportsListArgs {
    /// Path parameter: parent
    pub parent: String,
    /// Query parameter: filter
    pub filter: Option<String>,
    /// Query parameter: orderBy
    pub orderBy: Option<String>,
    /// Query parameter: pageSize
    pub pageSize: Option<String>,
    /// Query parameter: pageToken
    pub pageToken: Option<String>,
}

/// GET v1/projects/{projectsId}/locations/{locationsId}/transports
/// Lists Transports in a given project and location.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_transports_list_builder()` + `networkconnectivity_projects_locations_transports_list_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_transports_list_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_transports_list(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsTransportsListArgs,
) -> Result<
    impl StreamIterator<D = Result<ApiResponse<ListTransportsResponse>, ApiError>, P = ApiPending>
        + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_transports_list_builder(
        client,
        &args.parent,
        &args.filter,
        &args.orderBy,
        &args.pageSize,
        &args.pageToken,
    )?;
    networkconnectivity_projects_locations_transports_list_execute(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/transports/{transportsId}
/// Updates the parameters of a single Transport.
///
/// Returns `ClientRequestBuilder` for customization.
/// Use `networkconnectivity_projects_locations_transports_patch_execute()` to send, or `networkconnectivity_projects_locations_transports_patch` for simplest API.

pub fn networkconnectivity_projects_locations_transports_patch_builder<R>(
    client: &SimpleHttpClient<R>,
    name: &String,
    requestId: &Option<String>,
    updateMask: &Option<String>,
) -> Result<ClientRequestBuilder<R>, ApiError>
where
    R: DnsResolver + Clone,
{
    // Build URL
    let endpoint_url = format!(
        "https://networkconnectivity.googleapis.com/v1/projects/{}/locations/{locationsId}/transports/{transportsId}",
        name,
    );

    // Build request
    let mut query_parts = Vec::new();
    if let Some(val) = requestId.as_ref() {
        query_parts.push(format!("requestId={}", val));
    }
    if let Some(val) = updateMask.as_ref() {
        query_parts.push(format!("updateMask={}", val));
    }

    let url_with_query = if query_parts.is_empty() {
        endpoint_url
    } else {
        format!("{}?{}", endpoint_url, query_parts.join("&"))
    };

    let builder = client
        .patch(&url_with_query)
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?;

    Ok(builder)
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/transports/{transportsId}
/// Updates the parameters of a single Transport.
///
/// Takes a `ClientRequestBuilder`, builds the request, applies valtron combinators,
/// and returns a `TaskIterator` for customization before execution.
///
/// Use this function when you need to:
/// - Wrap the task with custom valtron combinators
/// - Compose multiple tasks before execution
/// - Intercept task execution for logging or testing
///
/// For direct execution, use `networkconnectivity_projects_locations_transports_patch_execute()` or `networkconnectivity_projects_locations_transports_patch`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_transports_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_transports_patch_task(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            Pending = ApiPending,
            Spawner = BoxedSendExecutionAction,
        > + Send
        + 'static,
    ApiError,
> {
    Ok(builder
        .build_send_request()
        .map_err(|e| ApiError::RequestBuildFailed(e.to_string()))?
        .map_ready(|intro| match intro {
            RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status_code: usize = intro.0.into();

                if status_code < 200 || status_code >= 300 {
                    // Capture body for error parsing
                    let body = body_reader::collect_string(stream);
                    // Try to parse as structured API error
                    if let Ok(error_body) = serde_json::from_str::<ApiErrorBody>(&body) {
                        return Err(ApiError::ApiError(error_body.error));
                    }
                    // Fall back to raw HTTP status error
                    return Err(ApiError::HttpStatus {
                        code: status_code as u16,
                        headers: headers.clone(),
                        body: Some(body),
                    });
                }

                let body = body_reader::collect_string(stream);
                let parsed: GoogleLongrunningOperation = serde_json::from_str(&body)
                    .map_err(|e| ApiError::ParseFailed(e.to_string()))?;

                Ok(ApiResponse {
                    status: status_code as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            RequestIntro::Failed(e) => Err(ApiError::RequestSendFailed(e.to_string())),
        })
        .map_pending(|_| ApiPending::Sending))
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/transports/{transportsId}
/// Updates the parameters of a single Transport.
///
/// Takes a `ClientRequestBuilder`, builds and executes the request,
/// and returns the parsed response via a `StreamIterator`.
///
/// For full customization, use `networkconnectivity_projects_locations_transports_patch_builder()` to create the builder,
/// modify it, then call this function with your customized builder.
/// For task-level control, use `networkconnectivity_projects_locations_transports_patch_task()`.
/// For the simplest API, use `networkconnectivity_projects_locations_transports_patch()`.
///
/// # Arguments
///
/// * `builder` - A `ClientRequestBuilder`, typically from `networkconnectivity_projects_locations_transports_patch_builder()`
///
/// # Errors
///
/// Returns an error if the request cannot be built.
/// HTTP errors during execution are returned via the StreamIterator.

pub fn networkconnectivity_projects_locations_transports_patch_execute(
    builder: ClientRequestBuilder<SystemDnsResolver>,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let task = networkconnectivity_projects_locations_transports_patch_task(builder)?;
    execute(task, None).map_err(|e| ApiError::RequestBuildFailed(e.to_string()))
}

/// Arguments for [`networkconnectivity_projects_locations_transports_patch`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct NetworkconnectivityProjectsLocationsTransportsPatchArgs {
    /// Path parameter: name
    pub name: String,
    /// Query parameter: requestId
    pub requestId: Option<String>,
    /// Query parameter: updateMask
    pub updateMask: Option<String>,
}

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/transports/{transportsId}
/// Updates the parameters of a single Transport.
///
/// Simplest API - builds and executes the request in one call.
/// For customization, use `networkconnectivity_projects_locations_transports_patch_builder()` + `networkconnectivity_projects_locations_transports_patch_execute()`.
/// For task-level control, use `networkconnectivity_projects_locations_transports_patch_task()`.
///
/// # Errors
///
/// Returns an error if the request cannot be built.

pub fn networkconnectivity_projects_locations_transports_patch(
    client: &SimpleHttpClient,
    args: &NetworkconnectivityProjectsLocationsTransportsPatchArgs,
) -> Result<
    impl StreamIterator<
            D = Result<ApiResponse<GoogleLongrunningOperation>, ApiError>,
            P = ApiPending,
        > + Send
        + 'static,
    ApiError,
> {
    let builder = networkconnectivity_projects_locations_transports_patch_builder(
        client,
        &args.name,
        &args.requestId,
        &args.updateMask,
    )?;
    networkconnectivity_projects_locations_transports_patch_execute(builder)
}

// =============================================================================
// ResourceIdentifier implementation for CheckConsumerConfigResponse
// =============================================================================

/// ResourceIdentifier implementation for CheckConsumerConfigResponse with NetworkconnectivityProjectsLocationsCheckConsumerConfigArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsCheckConsumerConfigArgs>
    for CheckConsumerConfigResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsCheckConsumerConfigArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::CheckConsumerConfigResponse/{}",
            input.location
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::CheckConsumerConfigResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Location
// =============================================================================

/// ResourceIdentifier implementation for Location with NetworkconnectivityProjectsLocationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsGetArgs> for Location {
    fn generate_resource_id(&self, input: &NetworkconnectivityProjectsLocationsGetArgs) -> String {
        format!("gcp::networkconnectivity::Location/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::Location"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListLocationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListLocationsResponse with NetworkconnectivityProjectsLocationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsListArgs> for ListLocationsResponse {
    fn generate_resource_id(&self, input: &NetworkconnectivityProjectsLocationsListArgs) -> String {
        format!(
            "gcp::networkconnectivity::ListLocationsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::ListLocationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsAutomatedDnsRecordsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsAutomatedDnsRecordsCreateArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsAutomatedDnsRecordsCreateArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsAutomatedDnsRecordsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsAutomatedDnsRecordsDeleteArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsAutomatedDnsRecordsDeleteArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for AutomatedDnsRecord
// =============================================================================

/// ResourceIdentifier implementation for AutomatedDnsRecord with NetworkconnectivityProjectsLocationsAutomatedDnsRecordsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsAutomatedDnsRecordsGetArgs>
    for AutomatedDnsRecord
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsAutomatedDnsRecordsGetArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::AutomatedDnsRecord/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::AutomatedDnsRecord"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListAutomatedDnsRecordsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListAutomatedDnsRecordsResponse with NetworkconnectivityProjectsLocationsAutomatedDnsRecordsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsAutomatedDnsRecordsListArgs>
    for ListAutomatedDnsRecordsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsAutomatedDnsRecordsListArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::ListAutomatedDnsRecordsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::ListAutomatedDnsRecordsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsGlobalHubsAcceptSpokeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsGlobalHubsAcceptSpokeArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsGlobalHubsAcceptSpokeArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsGlobalHubsAcceptSpokeUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsGlobalHubsAcceptSpokeUpdateArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsGlobalHubsAcceptSpokeUpdateArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsGlobalHubsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsGlobalHubsCreateArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsGlobalHubsCreateArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsGlobalHubsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsGlobalHubsDeleteArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsGlobalHubsDeleteArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Hub
// =============================================================================

/// ResourceIdentifier implementation for Hub with NetworkconnectivityProjectsLocationsGlobalHubsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsGlobalHubsGetArgs> for Hub {
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsGlobalHubsGetArgs,
    ) -> String {
        format!("gcp::networkconnectivity::Hub/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::Hub"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with NetworkconnectivityProjectsLocationsGlobalHubsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsGlobalHubsGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsGlobalHubsGetIamPolicyArgs,
    ) -> String {
        format!("gcp::networkconnectivity::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListHubsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListHubsResponse with NetworkconnectivityProjectsLocationsGlobalHubsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsGlobalHubsListArgs>
    for ListHubsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsGlobalHubsListArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::ListHubsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::ListHubsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListHubSpokesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListHubSpokesResponse with NetworkconnectivityProjectsLocationsGlobalHubsListSpokesArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsGlobalHubsListSpokesArgs>
    for ListHubSpokesResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsGlobalHubsListSpokesArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::ListHubSpokesResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::ListHubSpokesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsGlobalHubsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsGlobalHubsPatchArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsGlobalHubsPatchArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for QueryHubStatusResponse
// =============================================================================

/// ResourceIdentifier implementation for QueryHubStatusResponse with NetworkconnectivityProjectsLocationsGlobalHubsQueryStatusArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsGlobalHubsQueryStatusArgs>
    for QueryHubStatusResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsGlobalHubsQueryStatusArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::QueryHubStatusResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::QueryHubStatusResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsGlobalHubsRejectSpokeArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsGlobalHubsRejectSpokeArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsGlobalHubsRejectSpokeArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsGlobalHubsRejectSpokeUpdateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsGlobalHubsRejectSpokeUpdateArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsGlobalHubsRejectSpokeUpdateArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with NetworkconnectivityProjectsLocationsGlobalHubsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsGlobalHubsSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsGlobalHubsSetIamPolicyArgs,
    ) -> String {
        format!("gcp::networkconnectivity::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with NetworkconnectivityProjectsLocationsGlobalHubsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsGlobalHubsTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsGlobalHubsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Group
// =============================================================================

/// ResourceIdentifier implementation for Group with NetworkconnectivityProjectsLocationsGlobalHubsGroupsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsGlobalHubsGroupsGetArgs> for Group {
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsGlobalHubsGroupsGetArgs,
    ) -> String {
        format!("gcp::networkconnectivity::Group/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::Group"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with NetworkconnectivityProjectsLocationsGlobalHubsGroupsGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsGlobalHubsGroupsGetIamPolicyArgs>
    for Policy
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsGlobalHubsGroupsGetIamPolicyArgs,
    ) -> String {
        format!("gcp::networkconnectivity::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListGroupsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListGroupsResponse with NetworkconnectivityProjectsLocationsGlobalHubsGroupsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsGlobalHubsGroupsListArgs>
    for ListGroupsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsGlobalHubsGroupsListArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::ListGroupsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::ListGroupsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsGlobalHubsGroupsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsGlobalHubsGroupsPatchArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsGlobalHubsGroupsPatchArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with NetworkconnectivityProjectsLocationsGlobalHubsGroupsSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsGlobalHubsGroupsSetIamPolicyArgs>
    for Policy
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsGlobalHubsGroupsSetIamPolicyArgs,
    ) -> String {
        format!("gcp::networkconnectivity::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with NetworkconnectivityProjectsLocationsGlobalHubsGroupsTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsGlobalHubsGroupsTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsGlobalHubsGroupsTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RouteTable
// =============================================================================

/// ResourceIdentifier implementation for RouteTable with NetworkconnectivityProjectsLocationsGlobalHubsRouteTablesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsGlobalHubsRouteTablesGetArgs>
    for RouteTable
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsGlobalHubsRouteTablesGetArgs,
    ) -> String {
        format!("gcp::networkconnectivity::RouteTable/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::RouteTable"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListRouteTablesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListRouteTablesResponse with NetworkconnectivityProjectsLocationsGlobalHubsRouteTablesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsGlobalHubsRouteTablesListArgs>
    for ListRouteTablesResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsGlobalHubsRouteTablesListArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::ListRouteTablesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::ListRouteTablesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Route
// =============================================================================

/// ResourceIdentifier implementation for Route with NetworkconnectivityProjectsLocationsGlobalHubsRouteTablesRoutesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsGlobalHubsRouteTablesRoutesGetArgs>
    for Route
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsGlobalHubsRouteTablesRoutesGetArgs,
    ) -> String {
        format!("gcp::networkconnectivity::Route/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::Route"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListRoutesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListRoutesResponse with NetworkconnectivityProjectsLocationsGlobalHubsRouteTablesRoutesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsGlobalHubsRouteTablesRoutesListArgs>
    for ListRoutesResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsGlobalHubsRouteTablesRoutesListArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::ListRoutesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::ListRoutesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesCreateArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesCreateArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesDeleteArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesDeleteArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for PolicyBasedRoute
// =============================================================================

/// ResourceIdentifier implementation for PolicyBasedRoute with NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesGetArgs>
    for PolicyBasedRoute
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesGetArgs,
    ) -> String {
        format!("gcp::networkconnectivity::PolicyBasedRoute/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::PolicyBasedRoute"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesGetIamPolicyArgs>
    for Policy
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesGetIamPolicyArgs,
    ) -> String {
        format!("gcp::networkconnectivity::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListPolicyBasedRoutesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListPolicyBasedRoutesResponse with NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesListArgs>
    for ListPolicyBasedRoutesResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesListArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::ListPolicyBasedRoutesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::ListPolicyBasedRoutesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesSetIamPolicyArgs>
    for Policy
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesSetIamPolicyArgs,
    ) -> String {
        format!("gcp::networkconnectivity::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesTestIamPermissionsArgs,
    > for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsGlobalPolicyBasedRoutesTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsInternalRangesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsInternalRangesCreateArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsInternalRangesCreateArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsInternalRangesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsInternalRangesDeleteArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsInternalRangesDeleteArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for InternalRange
// =============================================================================

/// ResourceIdentifier implementation for InternalRange with NetworkconnectivityProjectsLocationsInternalRangesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsInternalRangesGetArgs>
    for InternalRange
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsInternalRangesGetArgs,
    ) -> String {
        format!("gcp::networkconnectivity::InternalRange/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::InternalRange"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with NetworkconnectivityProjectsLocationsInternalRangesGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsInternalRangesGetIamPolicyArgs>
    for Policy
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsInternalRangesGetIamPolicyArgs,
    ) -> String {
        format!("gcp::networkconnectivity::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListInternalRangesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListInternalRangesResponse with NetworkconnectivityProjectsLocationsInternalRangesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsInternalRangesListArgs>
    for ListInternalRangesResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsInternalRangesListArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::ListInternalRangesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::ListInternalRangesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsInternalRangesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsInternalRangesPatchArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsInternalRangesPatchArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with NetworkconnectivityProjectsLocationsInternalRangesSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsInternalRangesSetIamPolicyArgs>
    for Policy
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsInternalRangesSetIamPolicyArgs,
    ) -> String {
        format!("gcp::networkconnectivity::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with NetworkconnectivityProjectsLocationsInternalRangesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsInternalRangesTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsInternalRangesTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsCreateArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsCreateArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsDeleteArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsDeleteArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for MulticloudDataTransferConfig
// =============================================================================

/// ResourceIdentifier implementation for MulticloudDataTransferConfig with NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsGetArgs>
    for MulticloudDataTransferConfig
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsGetArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::MulticloudDataTransferConfig/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::MulticloudDataTransferConfig"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListMulticloudDataTransferConfigsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListMulticloudDataTransferConfigsResponse with NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsListArgs>
    for ListMulticloudDataTransferConfigsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsListArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::ListMulticloudDataTransferConfigsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::ListMulticloudDataTransferConfigsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsPatchArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsPatchArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsDestinationsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsDestinationsCreateArgs,
    > for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsDestinationsCreateArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsDestinationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsDestinationsDeleteArgs,
    > for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsDestinationsDeleteArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Destination
// =============================================================================

/// ResourceIdentifier implementation for Destination with NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsDestinationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsDestinationsGetArgs,
    > for Destination
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsDestinationsGetArgs,
    ) -> String {
        format!("gcp::networkconnectivity::Destination/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::Destination"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListDestinationsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListDestinationsResponse with NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsDestinationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsDestinationsListArgs,
    > for ListDestinationsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsDestinationsListArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::ListDestinationsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::ListDestinationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsDestinationsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsDestinationsPatchArgs,
    > for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsMulticloudDataTransferConfigsDestinationsPatchArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for MulticloudDataTransferSupportedService
// =============================================================================

/// ResourceIdentifier implementation for MulticloudDataTransferSupportedService with NetworkconnectivityProjectsLocationsMulticloudDataTransferSupportedServicesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        NetworkconnectivityProjectsLocationsMulticloudDataTransferSupportedServicesGetArgs,
    > for MulticloudDataTransferSupportedService
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsMulticloudDataTransferSupportedServicesGetArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::MulticloudDataTransferSupportedService/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::MulticloudDataTransferSupportedService"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListMulticloudDataTransferSupportedServicesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListMulticloudDataTransferSupportedServicesResponse with NetworkconnectivityProjectsLocationsMulticloudDataTransferSupportedServicesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl
    ResourceIdentifier<
        NetworkconnectivityProjectsLocationsMulticloudDataTransferSupportedServicesListArgs,
    > for ListMulticloudDataTransferSupportedServicesResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsMulticloudDataTransferSupportedServicesListArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::ListMulticloudDataTransferSupportedServicesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::ListMulticloudDataTransferSupportedServicesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with NetworkconnectivityProjectsLocationsOperationsCancelArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsOperationsCancelArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsOperationsCancelArgs,
    ) -> String {
        format!("gcp::networkconnectivity::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Empty
// =============================================================================

/// ResourceIdentifier implementation for Empty with NetworkconnectivityProjectsLocationsOperationsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsOperationsDeleteArgs> for Empty {
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsOperationsDeleteArgs,
    ) -> String {
        format!("gcp::networkconnectivity::Empty/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::Empty"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsOperationsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsOperationsGetArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsOperationsGetArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningListOperationsResponse
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningListOperationsResponse with NetworkconnectivityProjectsLocationsOperationsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsOperationsListArgs>
    for GoogleLongrunningListOperationsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsOperationsListArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningListOperationsResponse/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningListOperationsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsRegionalEndpointsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsRegionalEndpointsCreateArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsRegionalEndpointsCreateArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsRegionalEndpointsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsRegionalEndpointsDeleteArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsRegionalEndpointsDeleteArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RegionalEndpoint
// =============================================================================

/// ResourceIdentifier implementation for RegionalEndpoint with NetworkconnectivityProjectsLocationsRegionalEndpointsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsRegionalEndpointsGetArgs>
    for RegionalEndpoint
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsRegionalEndpointsGetArgs,
    ) -> String {
        format!("gcp::networkconnectivity::RegionalEndpoint/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::RegionalEndpoint"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListRegionalEndpointsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListRegionalEndpointsResponse with NetworkconnectivityProjectsLocationsRegionalEndpointsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsRegionalEndpointsListArgs>
    for ListRegionalEndpointsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsRegionalEndpointsListArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::ListRegionalEndpointsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::ListRegionalEndpointsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for RemoteTransportProfile
// =============================================================================

/// ResourceIdentifier implementation for RemoteTransportProfile with NetworkconnectivityProjectsLocationsRemoteTransportProfilesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsRemoteTransportProfilesGetArgs>
    for RemoteTransportProfile
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsRemoteTransportProfilesGetArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::RemoteTransportProfile/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::RemoteTransportProfile"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListRemoteTransportProfilesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListRemoteTransportProfilesResponse with NetworkconnectivityProjectsLocationsRemoteTransportProfilesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsRemoteTransportProfilesListArgs>
    for ListRemoteTransportProfilesResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsRemoteTransportProfilesListArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::ListRemoteTransportProfilesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::ListRemoteTransportProfilesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsServiceClassesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsServiceClassesDeleteArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsServiceClassesDeleteArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ServiceClass
// =============================================================================

/// ResourceIdentifier implementation for ServiceClass with NetworkconnectivityProjectsLocationsServiceClassesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsServiceClassesGetArgs>
    for ServiceClass
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsServiceClassesGetArgs,
    ) -> String {
        format!("gcp::networkconnectivity::ServiceClass/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::ServiceClass"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListServiceClassesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListServiceClassesResponse with NetworkconnectivityProjectsLocationsServiceClassesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsServiceClassesListArgs>
    for ListServiceClassesResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsServiceClassesListArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::ListServiceClassesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::ListServiceClassesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsServiceClassesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsServiceClassesPatchArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsServiceClassesPatchArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsServiceConnectionMapsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsServiceConnectionMapsCreateArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsServiceConnectionMapsCreateArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsServiceConnectionMapsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsServiceConnectionMapsDeleteArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsServiceConnectionMapsDeleteArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ServiceConnectionMap
// =============================================================================

/// ResourceIdentifier implementation for ServiceConnectionMap with NetworkconnectivityProjectsLocationsServiceConnectionMapsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsServiceConnectionMapsGetArgs>
    for ServiceConnectionMap
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsServiceConnectionMapsGetArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::ServiceConnectionMap/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::ServiceConnectionMap"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListServiceConnectionMapsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListServiceConnectionMapsResponse with NetworkconnectivityProjectsLocationsServiceConnectionMapsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsServiceConnectionMapsListArgs>
    for ListServiceConnectionMapsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsServiceConnectionMapsListArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::ListServiceConnectionMapsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::ListServiceConnectionMapsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsServiceConnectionMapsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsServiceConnectionMapsPatchArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsServiceConnectionMapsPatchArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsServiceConnectionPoliciesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsServiceConnectionPoliciesCreateArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsServiceConnectionPoliciesCreateArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsServiceConnectionPoliciesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsServiceConnectionPoliciesDeleteArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsServiceConnectionPoliciesDeleteArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ServiceConnectionPolicy
// =============================================================================

/// ResourceIdentifier implementation for ServiceConnectionPolicy with NetworkconnectivityProjectsLocationsServiceConnectionPoliciesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsServiceConnectionPoliciesGetArgs>
    for ServiceConnectionPolicy
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsServiceConnectionPoliciesGetArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::ServiceConnectionPolicy/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::ServiceConnectionPolicy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListServiceConnectionPoliciesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListServiceConnectionPoliciesResponse with NetworkconnectivityProjectsLocationsServiceConnectionPoliciesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsServiceConnectionPoliciesListArgs>
    for ListServiceConnectionPoliciesResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsServiceConnectionPoliciesListArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::ListServiceConnectionPoliciesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::ListServiceConnectionPoliciesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsServiceConnectionPoliciesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsServiceConnectionPoliciesPatchArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsServiceConnectionPoliciesPatchArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsServiceConnectionTokensCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsServiceConnectionTokensCreateArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsServiceConnectionTokensCreateArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsServiceConnectionTokensDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsServiceConnectionTokensDeleteArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsServiceConnectionTokensDeleteArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ServiceConnectionToken
// =============================================================================

/// ResourceIdentifier implementation for ServiceConnectionToken with NetworkconnectivityProjectsLocationsServiceConnectionTokensGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsServiceConnectionTokensGetArgs>
    for ServiceConnectionToken
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsServiceConnectionTokensGetArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::ServiceConnectionToken/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::ServiceConnectionToken"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListServiceConnectionTokensResponse
// =============================================================================

/// ResourceIdentifier implementation for ListServiceConnectionTokensResponse with NetworkconnectivityProjectsLocationsServiceConnectionTokensListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsServiceConnectionTokensListArgs>
    for ListServiceConnectionTokensResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsServiceConnectionTokensListArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::ListServiceConnectionTokensResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::ListServiceConnectionTokensResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsSpokesCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsSpokesCreateArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsSpokesCreateArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsSpokesDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsSpokesDeleteArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsSpokesDeleteArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Spoke
// =============================================================================

/// ResourceIdentifier implementation for Spoke with NetworkconnectivityProjectsLocationsSpokesGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsSpokesGetArgs> for Spoke {
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsSpokesGetArgs,
    ) -> String {
        format!("gcp::networkconnectivity::Spoke/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::Spoke"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with NetworkconnectivityProjectsLocationsSpokesGetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsSpokesGetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsSpokesGetIamPolicyArgs,
    ) -> String {
        format!("gcp::networkconnectivity::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListSpokesResponse
// =============================================================================

/// ResourceIdentifier implementation for ListSpokesResponse with NetworkconnectivityProjectsLocationsSpokesListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsSpokesListArgs> for ListSpokesResponse {
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsSpokesListArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::ListSpokesResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::ListSpokesResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsSpokesPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsSpokesPatchArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsSpokesPatchArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Policy
// =============================================================================

/// ResourceIdentifier implementation for Policy with NetworkconnectivityProjectsLocationsSpokesSetIamPolicyArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsSpokesSetIamPolicyArgs> for Policy {
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsSpokesSetIamPolicyArgs,
    ) -> String {
        format!("gcp::networkconnectivity::Policy/{}", input.resource)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::Policy"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for TestIamPermissionsResponse
// =============================================================================

/// ResourceIdentifier implementation for TestIamPermissionsResponse with NetworkconnectivityProjectsLocationsSpokesTestIamPermissionsArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsSpokesTestIamPermissionsArgs>
    for TestIamPermissionsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsSpokesTestIamPermissionsArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::TestIamPermissionsResponse/{}",
            input.resource
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::TestIamPermissionsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsTransportsCreateArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsTransportsCreateArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsTransportsCreateArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsTransportsDeleteArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsTransportsDeleteArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsTransportsDeleteArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for Transport
// =============================================================================

/// ResourceIdentifier implementation for Transport with NetworkconnectivityProjectsLocationsTransportsGetArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsTransportsGetArgs> for Transport {
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsTransportsGetArgs,
    ) -> String {
        format!("gcp::networkconnectivity::Transport/{}", input.name)
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::Transport"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for ListTransportsResponse
// =============================================================================

/// ResourceIdentifier implementation for ListTransportsResponse with NetworkconnectivityProjectsLocationsTransportsListArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsTransportsListArgs>
    for ListTransportsResponse
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsTransportsListArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::ListTransportsResponse/{}",
            input.parent
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::ListTransportsResponse"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}

// =============================================================================
// ResourceIdentifier implementation for GoogleLongrunningOperation
// =============================================================================

/// ResourceIdentifier implementation for GoogleLongrunningOperation with NetworkconnectivityProjectsLocationsTransportsPatchArgs input.
///
/// WHY: Enables automatic state tracking via StoreStateIdentifierTask.
///
/// HOW: Computes resource ID from input path parameters.
impl ResourceIdentifier<NetworkconnectivityProjectsLocationsTransportsPatchArgs>
    for GoogleLongrunningOperation
{
    fn generate_resource_id(
        &self,
        input: &NetworkconnectivityProjectsLocationsTransportsPatchArgs,
    ) -> String {
        format!(
            "gcp::networkconnectivity::GoogleLongrunningOperation/{}",
            input.name
        )
    }

    fn resource_kind(&self) -> &'static str {
        "gcp::networkconnectivity::GoogleLongrunningOperation"
    }

    fn provider(&self) -> &'static str {
        "gcp"
    }
}
